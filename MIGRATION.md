# Migration Notes

Generated by `dogsbay migrate-asciidoc`.

## Summary

- **Site name:** OpenShift Container Platform
- **Source corpus:** `/home/runner/work/_temp/upstream` (AsciiBinder topology (`_topic_maps/_topic_map.yml`), distro `openshift-enterprise`)
- **Output:** `/home/runner/work/_temp/out`
- **Pages migrated:** 1796

## Attributes supplied

  - `product_title` = `"OpenShift Container Platform"`
  - `product_version` = `"4.22"`
  - `openshift_enterprise` = `"true"`

These values were persisted into `dogsbay.config.yml`'s top-level `attributes:` block, so `dogsbay site build` resolves them automatically on every rebuild — no need to re-pass `--attribute` flags. Edit the block to change values, or extend it as you supply more.

The engine emits `{{ name }}` for every AsciiDoc `{name}` ref and `{% if name %}` for every `ifdef::name[]` conditional, regardless of what's supplied here. The attributes context is used by the Minja resolution stage to substitute / evaluate where possible; the rest is preserved verbatim so a downstream pass can finish the job.

## Resolving surviving directives

Anything that wasn't supplied at migration time still lives in the converted markdown as a literal `{{ name }}` / `{% if name %}` directive. Two ways to finish the job without re-running this migration:

1. **Edit `dogsbay.config.yml`'s `attributes:` block** — add the missing keys (create the block if it isn't there). The next `dogsbay site build` resolves them.
2. **Pass `dogsbay site build --attribute name=value`** — repeatable, layered on top of the config block. Use for per-build / per-environment values (CI secrets, deploy targets) that don't belong in the persistent config.

Pages that intentionally contain literal `{{ }}` examples (e.g. docs about Jinja syntax) can opt out per-page with frontmatter:

```yaml
---
title: Writing templates
preprocess: false
---
```

See `docs/asciidoc.md` for the full preprocessor behaviour.

## What survived

- Headings, paragraphs, lists, code blocks, links, emphasis, strong, inline code.
- Definition lists (rendered HTML in default mode).
- Conditional blocks (`ifdef::` / `ifndef::`) — preserved as `{% if %}` Minja directives unless the attribute was supplied.
- Attribute refs (`{name}`) — preserved as `{{ name }}` Minja vars unless supplied.
- Include directives (`include::`) — emitted as `{% include "./<path>.md" %}` directives pointing at the (renamed) sibling markdown file.
- In-doc attribute entries (`:name: value`) — converted to `{%- set name = "value" %}` directives.

## What didn't (yet)

- **Line-form admonition layout**: `NOTE: foo` lines always emit the HTML-shaped `**📌 NOTE**\` prefix regardless of options. Block-form `[NOTE]\n====\n…\n====` does respect the configured `admonitionFormat`.

## Next steps

1. `cd ../../_temp/out`
2. `npx dogsbay site dev` — live preview (auto-installs deps on first run).
3. Edit `./content/*.md` (canonical Dogsbay-MD) from here on.
4. Re-run to refresh:
   ```bash
   npx dogsbay migrate-asciidoc /home/runner/work/_temp/upstream --force --output /home/runner/work/_temp/out --site-name "OpenShift Container Platform" --attribute "product_title=OpenShift Container Platform" --attribute "product_version=4.22" --attribute "openshift_enterprise=true"
   ```
