---
title: Configuring the registry for {{ rh_openstack }}
---

# Configuring the registry for {{ rh_openstack }} {#configuring-registry-storage-openstack}

You configure the image registry to use custom storage on clusters that run on {{ rh_openstack }}. You must configure custom storage to use a Cinder volume in a specific availability zone.
