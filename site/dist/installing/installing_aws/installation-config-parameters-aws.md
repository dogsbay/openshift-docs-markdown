---
title: Installation configuration parameters for AWS
---

# Installation configuration parameters for AWS {#installation-config-parameters-aws}

Before you deploy an OpenShift Container Platform cluster on {{ aws_first }}, you create the `install-config.yaml` file and provide parameters to customize your cluster and the platform that hosts it. You can then modify the `install-config.yaml` file to customize your cluster further.
