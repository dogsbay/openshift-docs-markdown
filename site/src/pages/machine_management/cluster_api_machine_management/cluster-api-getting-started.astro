---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: machine_management/cluster_api_machine_management/cluster-api-getting-started.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"cluster-api-getting-started","text":"Getting started with the Cluster API"},{"depth":2,"slug":"creating-primary-resources_cluster-api-getting-started","text":"Creating the Cluster API primary resources"},{"depth":3,"slug":"capi-creating-machine-template_cluster-api-getting-started","text":"Creating a Cluster API machine template"},{"depth":3,"slug":"capi-creating-machine-set_cluster-api-getting-started","text":"Creating a Cluster API compute machine set"},{"depth":2,"slug":"mapi-to-capi-migration-overview_cluster-api-getting-started","text":"Machine API to Cluster API resource migration"},{"depth":3,"slug":"machine-set-authoritative-api-machines_cluster-api-getting-started","text":"Authoritative API types of compute machines"},{"depth":3,"slug":"migrating-between-capi-mapi_cluster-api-getting-started","text":"Migrating a Machine API resource to use the Cluster API"},{"depth":3,"slug":"deploying-capi-machines-via-mapi-machine-sets_cluster-api-getting-started","text":"Deploying Cluster API compute machines by using a Machine API compute machine set"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster-api-getting-started";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Getting started with the Cluster API";
const description = "The Machine API and Cluster API are distinct API groups that have similar resources. You can use these API groups to automate the management of…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["machine_management","machine_management/cluster_api_machine_management"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Getting started with the Cluster API {#cluster-api-getting-started}\n\nThe Machine API and Cluster API are distinct API groups that have similar resources. You can use these API groups to automate the management of infrastructure resources on your OpenShift Container Platform cluster.\n\n> [!IMPORTANT]\n> Managing machines with the Cluster API is a Technology Preview feature only. Technology Preview features are not supported with Red Hat production service level agreements (SLAs) and might not be functionally complete. Red Hat does not recommend using them in production. These features provide early access to upcoming product features, enabling customers to test functionality and provide feedback during the development process.\n>\n> For more information about the support scope of Red Hat Technology Preview features, see [Technology Preview Features Support Scope](https://access.redhat.com/support/offerings/techpreview/).\n\nWhen you install a standard OpenShift Container Platform cluster that has three control plane nodes, three compute nodes, and uses the default configuration options, the installation program provisions the following infrastructure resources in the `openshift-machine-api` namespace\n\n- One control plane machine set that manages three control plane machines.\n- One or more compute machine sets that manage three compute machines.\n- One machine health check that manages spot instances.\n\nWhen you install a cluster that supports managing infrastructure resources with the Cluster API, the installation program provisions the following resources in the `openshift-cluster-api` namespace:\n\n- One cluster resource.\n- One provider-specific infrastructure cluster resource.\n\nOn clusters that support migrating Machine API resources to Cluster API resources, a two-way synchronization controller creates these primary resources automatically. For more information, see \"Machine API to Cluster API resource migration\".\n\n## Creating the Cluster API primary resources {#creating-primary-resources_cluster-api-getting-started}\n\nFor clusters that do not support migrating Machine API resources to Cluster API resources, you must manually create the following Cluster API resources in the `openshift-cluster-api` namespace:\n\n- One or more machine templates that correspond to compute machine sets.\n- One or more compute machine sets that manage three compute machines.\n\n### Creating a Cluster API machine template {#capi-creating-machine-template_cluster-api-getting-started}\n\nYou can create a provider-specific machine template resource by creating a YAML manifest file and applying it with the OpenShift CLI (`oc`).\n\n**Prerequisites**\n\n- You have deployed an OpenShift Container Platform cluster.\n- You have enabled the use of the Cluster API.\n- You have access to the cluster using an account with `cluster-admin` permissions.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Create a YAML file similar to the following example. This procedure uses `<machine_template_resource_file>.yaml` as an example file name.\n\n   ```yaml\n   apiVersion: infrastructure.cluster.x-k8s.io/v1beta1\n   kind: <machine_template_kind>\n   metadata:\n     name: <template_name>\n     namespace: openshift-cluster-api\n   spec:\n     template:\n       spec:\n   ```\n\n   where:\n\n   `kind`\n   :   Specifies the machine template kind. This value must match the value for your platform.\n\n       The following values are valid:\n\n       | Cluster infrastructure provider | Value |\n       | --- | --- |\n       | Amazon Web Services (AWS) | `AWSMachineTemplate` |\n       | Google Cloud | `GCPMachineTemplate` |\n       | Microsoft Azure | `AzureMachineTemplate` |\n       | Red Hat OpenStack Platform (RHOSP) | `OpenStackMachineTemplate` |\n       | VMware vSphere | `VSphereMachineTemplate` |\n       | Bare metal | `Metal3MachineTemplate` |\n\n   `metadata.name`\n   :   Specifies a name for the machine template.\n\n   `spec.template.spec`\n   :   Specifies the details for your environment. These parameters are provider specific. For more information, see the sample Cluster API machine template YAML for your provider.\n2. Create the machine template CR by running the following command:\n\n   ```terminal\n   $ oc create -f <machine_template_resource_file>.yaml\n   ```\n\n**Verification**\n\n- Confirm that the machine template CR is created by running the following command:\n\n  ```terminal\n  $ oc get <machine_template_kind> -n openshift-cluster-api\n  ```\n\n  Replace `<machine_template_kind>` with the value that corresponds to your platform.\n\n  ```text {title=\"Example output\"}\n  NAME              AGE\n  <template_name>   77m\n  ```\n\n**Additional resources**\n\n- [Machine API to Cluster API resource migration](/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster-api-getting-started#mapi-to-capi-migration-overview_cluster-api-getting-started)\n- [Sample YAML for a Cluster API machine template resource on Amazon Web Services](/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-aws#capi-yaml-machine-template-aws_cluster-api-config-options-aws)\n- [Sample YAML for a Cluster API machine template resource on Google Cloud](/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-gcp#capi-yaml-machine-template-gcp_cluster-api-config-options-gcp)\n- [Sample YAML for a Cluster API machine template resource on Microsoft Azure](/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-azure#capi-yaml-machine-template-azure_cluster-api-config-options-azure)\n- [Sample YAML for a Cluster API machine template resource on RHOSP](/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-rhosp#capi-yaml-machine-template-rhosp_cluster-api-config-options-rhosp)\n- [Sample YAML for a Cluster API machine template resource on VMware vSphere](/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-vsphere#capi-yaml-machine-template-vsphere_cluster-api-config-options-vsphere)\n- [Sample YAML for a Cluster API machine template resource on bare metal](/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-bare-metal#capi-yaml-machine-template-bare-metal_cluster-api-config-options-bare-metal)\n\n### Creating a Cluster API compute machine set {#capi-creating-machine-set_cluster-api-getting-started}\n\nYou can create compute machine sets that use the Cluster API to dynamically manage the machine compute resources for specific workloads of your choice.\n\n**Prerequisites**\n\n- You have deployed an OpenShift Container Platform cluster.\n- You have enabled the use of the Cluster API.\n- You have access to the cluster using an account with `cluster-admin` permissions.\n- You have installed the OpenShift CLI (`oc`).\n- You have created the machine template resource.\n\n**Procedure**\n\n1. Create a YAML file similar to the following. This procedure uses `<machine_set_resource_file>.yaml` as an example file name.\n\n   ```yaml\n   apiVersion: cluster.x-k8s.io/v1beta1\n   kind: MachineSet\n   metadata:\n     name: <machine_set_name>\n     namespace: openshift-cluster-api\n   spec:\n     clusterName: <cluster_name>\n     replicas: 1\n     selector:\n       matchLabels:\n         test: example\n     template:\n       metadata:\n         labels:\n           test: example\n       spec:\n   # ...\n   ```\n\n   where:\n\n   `metadata.name`\n   :   Specifies a name for the compute machine set. The cluster ID, machine role, and region form a typical pattern for this value in the following format: `<cluster_name>-<role>-<region>`.\n\n   `spec.clusterName`\n   :   Specifies the name of the cluster. Obtain the value of the cluster ID by running the following command:\n\n       ```terminal\n       $  oc get infrastructure cluster \\\n          -o jsonpath='{.status.infrastructureName}'\n       ```\n\n   `spec.template.spec`\n   :   Specifies the details for your environment. These parameters are provider specific. For more information, see the sample Cluster API compute machine set YAML for your provider.\n2. Create the compute machine set CR by running the following command:\n\n   ```terminal\n   $ oc create -f <machine_set_resource_file>.yaml\n   ```\n3. Confirm that the compute machine set CR is created by running the following command:\n\n   ```terminal\n   $ oc get machineset.cluster.x-k8s.io -n openshift-cluster-api\n   ```\n\n   ```text {title=\"Example output\"}\n   NAME                 CLUSTER          REPLICAS   READY   AVAILABLE   AGE   VERSION\n   <machine_set_name>   <cluster_name>   1          1       1           17m\n   ```\n\n   When the new compute machine set is available, the `REPLICAS` and `AVAILABLE` values match. If the compute machine set is not available, wait a few minutes and run the command again.\n\n**Verification**\n\n- To verify that the compute machine set is creating machines according to your required configuration, review the lists of machines and nodes in the cluster by running the following commands:\n\n  - View the list of Cluster API machines:\n\n    ```terminal\n    $ oc get machine.cluster.x-k8s.io -n openshift-cluster-api\n    ```\n\n    ```text {title=\"Example output\"}\n    NAME                             CLUSTER          NODENAME                                 PROVIDERID      PHASE     AGE     VERSION\n    <machine_set_name>-<string_id>   <cluster_name>   <ip_address>.<region>.compute.internal   <provider_id>   Running   8m23s\n    ```\n  - View the list of nodes:\n\n    ```terminal\n    $ oc get node\n    ```\n\n    ```text {title=\"Example output\"}\n    NAME                                       STATUS   ROLES    AGE     VERSION\n    <ip_address_1>.<region>.compute.internal   Ready    worker   5h14m   v1.28.5\n    <ip_address_2>.<region>.compute.internal   Ready    master   5h19m   v1.28.5\n    <ip_address_3>.<region>.compute.internal   Ready    worker   7m      v1.28.5\n    ```\n\n**Additional resources**\n\n- [Sample YAML for a Cluster API compute machine set resource on Amazon Web Services](/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-aws#capi-yaml-machine-set-aws_cluster-api-config-options-aws)\n- [Sample YAML for a Cluster API compute machine set resource on Google Cloud](/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-gcp#capi-yaml-machine-set-gcp_cluster-api-config-options-gcp)\n- [Sample YAML for a Cluster API compute machine set resource on Microsoft Azure](/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-azure#capi-yaml-machine-set-azure_cluster-api-config-options-azure)\n- [Sample YAML for a Cluster API compute machine set resource on RHOSP](/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-rhosp#capi-yaml-machine-set-rhosp_cluster-api-config-options-rhosp)\n- [Sample YAML for a Cluster API compute machine set resource on VMware vSphere](/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-vsphere#capi-yaml-machine-set-vsphere_cluster-api-config-options-vsphere)\n- [Sample YAML for a Cluster API compute machine set resource on bare metal](/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-bare-metal#capi-yaml-machine-set-bare-metal_cluster-api-config-options-bare-metal)\n\n## Machine API to Cluster API resource migration {#mapi-to-capi-migration-overview_cluster-api-getting-started}\n\nOn clusters that support migrating Machine API resources to Cluster API resources, a two-way synchronization controller creates the Cluster API resources in the `openshift-cluster-api` namespace.\n\nThese Cluster API resources are listed as follows:\n\n- One or more machine templates that correspond to compute machine sets.\n- One or more compute machine sets that manage three compute machines.\n- One or more Cluster API compute machines that correspond to each Machine API compute machine.\n\n> [!NOTE]\n> The two-way synchronization controller only operates on clusters with the `MachineAPIMigration` feature gate in the `TechPreviewNoUpgrade` feature set enabled.\n\nThese Cluster API resources correspond to the resources that the installation program provisions in the `openshift-machine-api` namespace for a cluster that uses the default configuration options. The Cluster API resources have the same names as their Machine API counterparts and appear in the output of commands, such as `oc get`, that list resources. The synchronization controller creates the Cluster API resources in an unprovisioned (`Paused`) state to prevent unintended reconciliation.\n\nFor supported configurations, you can migrate a Machine API resource to the equivalent Cluster API resource by changing which API it considers authoritative. When you migrate a Machine API resources to the Cluster API, you transfer management of the resource to the Cluster API.\n\nBy migrating a Machine API resource to use the Cluster API, you can verify that everything works as expected before deciding to use the Cluster API in production clusters. After migrating a Machine API resource to an equivalent Cluster API resource, you can examine the new resource to verify that the features and configuration match the original Machine API resource.\n\nWhen you change the authoritative API for a compute machine set, any existing compute machines that the compute machine set manages retain their original authoritative API. As a result, a compute machine set that manages machines that use different authoritative APIs is a valid and expected occurrence in clusters that support migrating between these API types.\n\nWhen you change the authoritative API for a compute machine, the instance on the underlying infrastructure that backs the machine is not recreated or reprovisioned. In-place changes, such as modifying labels, tags, taints, or annotations, are the only changes that the API group can make to the underlying instance that backs the machine.\n\n> [!NOTE]\n> You can only migrate some resources on supported infrastructure types.\n\n**Supported resource conversions**\n\n| Infrastructure | Compute machine | Compute machine set | Machine health check | Control plane machine set | Cluster autoscaler |\n| --- | --- | --- | --- | --- | --- |\n| AWS | Technology Preview | Technology Preview | Not Available | Not Available | Not Available |\n| All other infrastructure types | Not Available | Not Available | Not Available | Not Available | Not Available |\n\n### Authoritative API types of compute machines {#machine-set-authoritative-api-machines_cluster-api-getting-started}\n\nThe values of the `.spec.authoritativeAPI` and `.spec.template.spec.authoritativeAPI` fields in a Machine API compute machine set determine the authoritative API of the compute machines.\n\n**Interaction of `authoritativeAPI` fields when creating compute machines**\n\n|  |  |  |  |  |\n| --- | --- | --- | --- | --- |\n| `.spec.authoritativeAPI` value | `ClusterAPI` | `ClusterAPI` | `MachineAPI` | `MachineAPI` |\n| `.spec.template.spec.authoritativeAPI` value | `ClusterAPI` | `MachineAPI` | `MachineAPI` | `ClusterAPI` |\n| `authoritativeAPI` value for new compute machines | `ClusterAPI` | `ClusterAPI` | `MachineAPI` | `ClusterAPI` |\n\n> [!NOTE]\n> When the `.spec.authoritativeAPI` value is `ClusterAPI`, the Machine API machine set is not authoritative and the `.spec.template.spec.authoritativeAPI` value is not used. As a result, the only combination that creates a compute machine with the Machine API as authoritative is where the `.spec.authoritativeAPI` and `.spec.template.spec.authoritativeAPI` values are `MachineAPI`.\n\n### Migrating a Machine API resource to use the Cluster API {#migrating-between-capi-mapi_cluster-api-getting-started}\n\nYou can migrate individual Machine API objects to equivalent Cluster API objects.\n\n> [!IMPORTANT]\n> Migrating a Machine API resource to use the Cluster API is a Technology Preview feature only. Technology Preview features are not supported with Red Hat production service level agreements (SLAs) and might not be functionally complete. Red Hat does not recommend using them in production. These features provide early access to upcoming product features, enabling customers to test functionality and provide feedback during the development process.\n>\n> For more information about the support scope of Red Hat Technology Preview features, see [Technology Preview Features Support Scope](https://access.redhat.com/support/offerings/techpreview/).\n\n**Prerequisites**\n\n- You have deployed an OpenShift Container Platform cluster on a supported infrastructure type.\n- You have enabled the use of the Cluster API.\n- You have enabled the `MachineAPIMigration` feature gate in the `TechPreviewNoUpgrade` feature set.\n- You have access to the cluster using an account with `cluster-admin` permissions.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Identify the Machine API resource that you want to migrate to a Cluster API resource by running the following command:\n\n   ```terminal\n   $ oc get <resource_kind> -n {{ from_namespace }}\n   ```\n\n   where `<resource_kind>` is one of the following values:\n\n   `machine.{{ from_api_group }}`\n   :   The fully qualified name of the resource kind for a compute or control plane machine.\n\n   `machineset.{{ from_api_group }}`\n   :   The fully qualified name of the resource kind for a compute machine set.\n2. Edit the resource specification by running the following command:\n\n   ```terminal\n   $ oc edit <resource_kind>/<resource_name> -n openshift-machine-api\n   ```\n\n   where:\n\n   `<resource_kind>`\n   :   Specifies a compute machine with `machine.machine.openshift.io` or compute machine set with `machineset.machine.openshift.io`.\n\n   `<resource_name>`\n   :   Specifies the name of the Machine API resource that you want to migrate to a Cluster API resource.\n3. In the resource specification, update the value of the `spec.authoritativeAPI` field:\n\n   ```yaml\n   apiVersion: machine.openshift.io/v1beta1\n   kind: <resource_kind>\n   metadata:\n     name: <resource_name>\n     [...]\n   spec:\n     authoritativeAPI: {{ to_api_value }}\n     [...]\n   status:\n     authoritativeAPI: {{ from_api_value }}\n     [...]\n   ```\n\n   where:\n\n   `kind`\n   :   Specifies the resource kind of the resource that you want to migrate. For example, the resource kind for a compute machine set is `MachineSet` and the resource kind for a compute machine is `Machine`.\n\n   `metadata.name`\n   :   Specifies the name of the resource that you want to migrate.\n\n   `spec.authoritativeAPI`\n   :   Specifies the authoritative API that you want this resource to use. For example, to start migrating a Machine API resource to the Cluster API, specify `{{ to_api_value }}`.\n\n   `status.authoritativeAPI`\n   :   Specifies the value for the current authoritative API. This value indicates which API currently manages this resource. Do not change the value in this part of the specification.\n\n   > [!IMPORTANT]\n   > Do not change other values when you update the value of the `spec.authoritativeAPI` field. Because other controllers might process updates to other values before the synchronization controller processes the `spec.authoritativeAPI` field update, changing other values can cause unexpected behavior.\n   >\n   > For more information, see \"Unexpected behavior when changing resource configurations\".\n\n**Verification**\n\n- Check the status of the conversion by running the following command:\n\n  ```terminal\n  $ oc -n openshift-machine-api get <resource_kind>/<resource_name> -o json | jq .status.authoritativeAPI\n  ```\n\n  where:\n\n  `<resource_kind>`\n  :   Specifies a compute machine with `machine.machine.openshift.io` or compute machine set with `machineset.machine.openshift.io`.\n\n  `<resource_name>`\n  :   Specifies the name of the Machine API resource that you want to migrate to a Cluster API resource.\n\n  - While the conversion progresses, this command returns a value of `Migrating`. If this value persists for a long time, check the logs for the `cluster-capi-operator` deployment in the `openshift-cluster-api` namespace for more information and to identify potential issues.\n  - When the conversion is complete, this command returns a value of `{{ to_api_value }}`.\n\n**Additional resources**\n\n- [Unexpected behavior when changing resource configurations](/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster-api-troubleshooting#ts-capi-migrate-unexpected-behavior_cluster-api-troubleshooting)\n\n### Deploying Cluster API compute machines by using a Machine API compute machine set {#deploying-capi-machines-via-mapi-machine-sets_cluster-api-getting-started}\n\nYou can configure a Machine API compute machine set to deploy Cluster API compute machines. With this process, you can test the Cluster API compute machine creation workflow without creating and scaling a Cluster API compute machine set.\n\nA Machine API compute machine set with this configuration creates nonauthoritative Machine API compute machines that use the Cluster API as authoritative. The two-way synchronization controller then creates corresponding authoritative Cluster API machines that provision on the underlying infrastructure.\n\n> [!IMPORTANT]\n> Deploying Cluster API compute machines by using a Machine API compute machine set is a Technology Preview feature only. Technology Preview features are not supported with Red Hat production service level agreements (SLAs) and might not be functionally complete. Red Hat does not recommend using them in production. These features provide early access to upcoming product features, enabling customers to test functionality and provide feedback during the development process.\n>\n> For more information about the support scope of Red Hat Technology Preview features, see [Technology Preview Features Support Scope](https://access.redhat.com/support/offerings/techpreview/).\n\n**Prerequisites**\n\n- You have deployed an OpenShift Container Platform cluster on a supported infrastructure type.\n- You have enabled the use of the Cluster API.\n- You have enabled the `MachineAPIMigration` feature gate in the `TechPreviewNoUpgrade` feature set.\n- You have access to the cluster using an account with `cluster-admin` permissions.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. List the Machine API compute machine sets in your cluster by running the following command:\n\n   ```terminal\n   $ oc get machineset.machine.openshift.io -n openshift-machine-api\n   ```\n2. Edit the resource specification by running the following command:\n\n   ```terminal\n   $ oc edit machineset.machine.openshift.io <machine_set_name> \\\n     -n openshift-machine-api\n   ```\n\n   Replace `<machine_set_name>` with the name of the Machine API compute machine set that you want to configure to deploy Cluster API compute machines.\n3. In the resource specification, update the value of the `spec.template.spec.authoritativeAPI` field:\n\n   ```yaml\n   apiVersion: machine.openshift.io/v1beta1\n   kind: MachineSet\n   metadata:\n     [...]\n     name: <machine_set_name>\n     [...]\n   spec:\n     authoritativeAPI: MachineAPI\n     [...]\n     template:\n       [...]\n       spec:\n         authoritativeAPI: ClusterAPI\n   status:\n     authoritativeAPI: MachineAPI\n     [...]\n   ```\n\n   where:\n\n   `spec.authoritativeAPI`\n   :   Specifies the unconverted value for the Machine API compute machine set. Do not change the value in this part of the specification.\n\n   `spec.template.spec.authoritativeAPI`\n   :   Specifies the authoritative API for the machine set. To configure the compute machine set to deploy Cluster API compute machines, set this value to `ClusterAPI`.\n\n   `status.authoritativeAPI`\n   :   Specifies the current value for the Machine API compute machine set. Do not change the value in this part of the specification.\n\n**Verification**\n\n1. List the machines that are managed by the updated compute machine set by running the following command:\n\n   ```terminal\n   $ oc get machines.machine.openshift.io \\\n     -n openshift-machine-api \\\n     -l machine.openshift.io/cluster-api-machineset=<machine_set_name>\n   ```\n2. To verify that a machine created by the updated machine set has the correct configuration, examine the `status.authoritativeAPI` field in the CR for one of the new machines by running the following command:\n\n   ```terminal\n   $ oc describe machines.machine.openshift.io <machine_name> \\\n     -n openshift-machine-api\n   ```\n\n   For a Cluster API compute machine, the value of the field is `ClusterAPI`.\n\n**Additional resources**\n\n- [Troubleshooting resource migration](/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster-api-troubleshooting#ts-capi-resource-migration_cluster-api-troubleshooting)\n- [Migrating Cluster API resources to Machine API resources](/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster-api-disabling#capi-to-mapi-migration-overview_cluster-api-disabling)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster-api-getting-started.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="cluster-api-getting-started" class="group scroll-mt-20">Getting started with the Cluster API<a href="#cluster-api-getting-started" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>The Machine API and Cluster API are distinct API groups that have similar resources. You can use these API groups to automate the management of infrastructure resources on your OpenShift Container Platform cluster.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Managing machines with the Cluster API is a Technology Preview feature only. Technology Preview features are not supported with Red Hat production service level agreements (SLAs) and might not be functionally complete. Red Hat does not recommend using them in production. These features provide early access to upcoming product features, enabling customers to test functionality and provide feedback during the development process.</p>
          <p>For more information about the support scope of Red Hat Technology Preview features, see <a href="https://access.redhat.com/support/offerings/techpreview/">Technology Preview Features Support Scope</a>.</p>
        </AlertDescription>
      </Alert>
      <p>When you install a standard OpenShift Container Platform cluster that has three control plane nodes, three compute nodes, and uses the default configuration options, the installation program provisions the following infrastructure resources in the <code>openshift-machine-api</code> namespace</p>
      <ul>
      <li>One control plane machine set that manages three control plane machines.</li>
      <li>One or more compute machine sets that manage three compute machines.</li>
      <li>One machine health check that manages spot instances.</li>
      </ul>
      <p>When you install a cluster that supports managing infrastructure resources with the Cluster API, the installation program provisions the following resources in the <code>openshift-cluster-api</code> namespace:</p>
      <ul>
      <li>One cluster resource.</li>
      <li>One provider-specific infrastructure cluster resource.</li>
      </ul>
      <p>On clusters that support migrating Machine API resources to Cluster API resources, a two-way synchronization controller creates these primary resources automatically. For more information, see "Machine API to Cluster API resource migration".</p>
      <h2 id="creating-primary-resources_cluster-api-getting-started" class="group scroll-mt-20">Creating the Cluster API primary resources<a href="#creating-primary-resources_cluster-api-getting-started" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>For clusters that do not support migrating Machine API resources to Cluster API resources, you must manually create the following Cluster API resources in the <code>openshift-cluster-api</code> namespace:</p>
      <ul>
      <li>One or more machine templates that correspond to compute machine sets.</li>
      <li>One or more compute machine sets that manage three compute machines.</li>
      </ul>
      <h3 id="capi-creating-machine-template_cluster-api-getting-started" class="group scroll-mt-20">Creating a Cluster API machine template<a href="#capi-creating-machine-template_cluster-api-getting-started" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can create a provider-specific machine template resource by creating a YAML manifest file and applying it with the OpenShift CLI (<code>oc</code>).</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have deployed an OpenShift Container Platform cluster.</li>
      <li>You have enabled the use of the Cluster API.</li>
      <li>You have access to the cluster using an account with <code>cluster-admin</code> permissions.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a YAML file similar to the following example. This procedure uses <code>&lt;machine_template_resource_file&gt;.yaml</code> as an example file name.
      <CodeBlock code={"apiVersion: infrastructure.cluster.x-k8s.io/v1beta1\nkind: <machine_template_kind>\nmetadata:\n  name: <template_name>\n  namespace: openshift-cluster-api\nspec:\n  template:\n    spec:"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>kind</code></dt>
      <dd class="ml-6 text-muted-foreground">
      <p>Specifies the machine template kind. This value must match the value for your platform.</p>
      <p>The following values are valid:</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Cluster infrastructure provider</TableHead>
      <TableHead>Value</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>Amazon Web Services (AWS)</TableCell>
      <TableCell><code>AWSMachineTemplate</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Google Cloud</TableCell>
      <TableCell><code>GCPMachineTemplate</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Microsoft Azure</TableCell>
      <TableCell><code>AzureMachineTemplate</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Red Hat OpenStack Platform (RHOSP)</TableCell>
      <TableCell><code>OpenStackMachineTemplate</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell>VMware vSphere</TableCell>
      <TableCell><code>VSphereMachineTemplate</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Bare metal</TableCell>
      <TableCell><code>Metal3MachineTemplate</code></TableCell>
      </TableRow>
      </TableBody>
      </Table>
      </dd>
      <dt class="font-semibold"><code>metadata.name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a name for the machine template.</p></dd>
      <dt class="font-semibold"><code>spec.template.spec</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the details for your environment. These parameters are provider specific. For more information, see the sample Cluster API machine template YAML for your provider.</p></dd>
      </dl>
      </li>
      <li>
      Create the machine template CR by running the following command:
      <CodeBlock code={"$ oc create -f <machine_template_resource_file>.yaml"} lang="terminal" />
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      Confirm that the machine template CR is created by running the following command:
      <CodeBlock code={"$ oc get <machine_template_kind> -n openshift-cluster-api"} lang="terminal" />
      <p>Replace <code>&lt;machine_template_kind&gt;</code> with the value that corresponds to your platform.</p>
      <CodeBlock code={"NAME              AGE\n<template_name>   77m"} lang="text" title="Example output" />
      </li>
      </ul>
      <p><strong>Additional resources</strong></p>
      <ul>
      <li><a href="/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster-api-getting-started#mapi-to-capi-migration-overview_cluster-api-getting-started">Machine API to Cluster API resource migration</a></li>
      <li><a href="/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-aws#capi-yaml-machine-template-aws_cluster-api-config-options-aws">Sample YAML for a Cluster API machine template resource on Amazon Web Services</a></li>
      <li><a href="/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-gcp#capi-yaml-machine-template-gcp_cluster-api-config-options-gcp">Sample YAML for a Cluster API machine template resource on Google Cloud</a></li>
      <li><a href="/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-azure#capi-yaml-machine-template-azure_cluster-api-config-options-azure">Sample YAML for a Cluster API machine template resource on Microsoft Azure</a></li>
      <li><a href="/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-rhosp#capi-yaml-machine-template-rhosp_cluster-api-config-options-rhosp">Sample YAML for a Cluster API machine template resource on RHOSP</a></li>
      <li><a href="/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-vsphere#capi-yaml-machine-template-vsphere_cluster-api-config-options-vsphere">Sample YAML for a Cluster API machine template resource on VMware vSphere</a></li>
      <li><a href="/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-bare-metal#capi-yaml-machine-template-bare-metal_cluster-api-config-options-bare-metal">Sample YAML for a Cluster API machine template resource on bare metal</a></li>
      </ul>
      <h3 id="capi-creating-machine-set_cluster-api-getting-started" class="group scroll-mt-20">Creating a Cluster API compute machine set<a href="#capi-creating-machine-set_cluster-api-getting-started" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can create compute machine sets that use the Cluster API to dynamically manage the machine compute resources for specific workloads of your choice.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have deployed an OpenShift Container Platform cluster.</li>
      <li>You have enabled the use of the Cluster API.</li>
      <li>You have access to the cluster using an account with <code>cluster-admin</code> permissions.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      <li>You have created the machine template resource.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a YAML file similar to the following. This procedure uses <code>&lt;machine_set_resource_file&gt;.yaml</code> as an example file name.
      <CodeBlock code={"apiVersion: cluster.x-k8s.io/v1beta1\nkind: MachineSet\nmetadata:\n  name: <machine_set_name>\n  namespace: openshift-cluster-api\nspec:\n  clusterName: <cluster_name>\n  replicas: 1\n  selector:\n    matchLabels:\n      test: example\n  template:\n    metadata:\n      labels:\n        test: example\n    spec:\n# ..."} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>metadata.name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a name for the compute machine set. The cluster ID, machine role, and region form a typical pattern for this value in the following format: <code>&lt;cluster_name&gt;-&lt;role&gt;-&lt;region&gt;</code>.</p></dd>
      <dt class="font-semibold"><code>spec.clusterName</code></dt>
      <dd class="ml-6 text-muted-foreground">
      <p>Specifies the name of the cluster. Obtain the value of the cluster ID by running the following command:</p>
      <CodeBlock code={"$  oc get infrastructure cluster \\\n   -o jsonpath='{.status.infrastructureName}'"} lang="terminal" />
      </dd>
      <dt class="font-semibold"><code>spec.template.spec</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the details for your environment. These parameters are provider specific. For more information, see the sample Cluster API compute machine set YAML for your provider.</p></dd>
      </dl>
      </li>
      <li>
      Create the compute machine set CR by running the following command:
      <CodeBlock code={"$ oc create -f <machine_set_resource_file>.yaml"} lang="terminal" />
      </li>
      <li>
      Confirm that the compute machine set CR is created by running the following command:
      <CodeBlock code={"$ oc get machineset.cluster.x-k8s.io -n openshift-cluster-api"} lang="terminal" />
      <CodeBlock code={"NAME                 CLUSTER          REPLICAS   READY   AVAILABLE   AGE   VERSION\n<machine_set_name>   <cluster_name>   1          1       1           17m"} lang="text" title="Example output" />
      <p>When the new compute machine set is available, the <code>REPLICAS</code> and <code>AVAILABLE</code> values match. If the compute machine set is not available, wait a few minutes and run the command again.</p>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      To verify that the compute machine set is creating machines according to your required configuration, review the lists of machines and nodes in the cluster by running the following commands:
      <ul>
      <li>
      View the list of Cluster API machines:
      <CodeBlock code={"$ oc get machine.cluster.x-k8s.io -n openshift-cluster-api"} lang="terminal" />
      <CodeBlock code={"NAME                             CLUSTER          NODENAME                                 PROVIDERID      PHASE     AGE     VERSION\n<machine_set_name>-<string_id>   <cluster_name>   <ip_address>.<region>.compute.internal   <provider_id>   Running   8m23s"} lang="text" title="Example output" />
      </li>
      <li>
      View the list of nodes:
      <CodeBlock code={"$ oc get node"} lang="terminal" />
      <CodeBlock code={"NAME                                       STATUS   ROLES    AGE     VERSION\n<ip_address_1>.<region>.compute.internal   Ready    worker   5h14m   v1.28.5\n<ip_address_2>.<region>.compute.internal   Ready    master   5h19m   v1.28.5\n<ip_address_3>.<region>.compute.internal   Ready    worker   7m      v1.28.5"} lang="text" title="Example output" />
      </li>
      </ul>
      </li>
      </ul>
      <p><strong>Additional resources</strong></p>
      <ul>
      <li><a href="/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-aws#capi-yaml-machine-set-aws_cluster-api-config-options-aws">Sample YAML for a Cluster API compute machine set resource on Amazon Web Services</a></li>
      <li><a href="/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-gcp#capi-yaml-machine-set-gcp_cluster-api-config-options-gcp">Sample YAML for a Cluster API compute machine set resource on Google Cloud</a></li>
      <li><a href="/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-azure#capi-yaml-machine-set-azure_cluster-api-config-options-azure">Sample YAML for a Cluster API compute machine set resource on Microsoft Azure</a></li>
      <li><a href="/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-rhosp#capi-yaml-machine-set-rhosp_cluster-api-config-options-rhosp">Sample YAML for a Cluster API compute machine set resource on RHOSP</a></li>
      <li><a href="/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-vsphere#capi-yaml-machine-set-vsphere_cluster-api-config-options-vsphere">Sample YAML for a Cluster API compute machine set resource on VMware vSphere</a></li>
      <li><a href="/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster_api_provider_configurations/cluster-api-config-options-bare-metal#capi-yaml-machine-set-bare-metal_cluster-api-config-options-bare-metal">Sample YAML for a Cluster API compute machine set resource on bare metal</a></li>
      </ul>
      <h2 id="mapi-to-capi-migration-overview_cluster-api-getting-started" class="group scroll-mt-20">Machine API to Cluster API resource migration<a href="#mapi-to-capi-migration-overview_cluster-api-getting-started" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">On clusters that support migrating Machine API resources to Cluster API resources, a two-way synchronization controller creates the Cluster API resources in the <code>openshift-cluster-api</code> namespace.</p>
      <p>These Cluster API resources are listed as follows:</p>
      <ul>
      <li>One or more machine templates that correspond to compute machine sets.</li>
      <li>One or more compute machine sets that manage three compute machines.</li>
      <li>One or more Cluster API compute machines that correspond to each Machine API compute machine.</li>
      </ul>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The two-way synchronization controller only operates on clusters with the <code>MachineAPIMigration</code> feature gate in the <code>TechPreviewNoUpgrade</code> feature set enabled.</p>
        </AlertDescription>
      </Alert>
      <p>These Cluster API resources correspond to the resources that the installation program provisions in the <code>openshift-machine-api</code> namespace for a cluster that uses the default configuration options. The Cluster API resources have the same names as their Machine API counterparts and appear in the output of commands, such as <code>oc get</code>, that list resources. The synchronization controller creates the Cluster API resources in an unprovisioned (<code>Paused</code>) state to prevent unintended reconciliation.</p>
      <p>For supported configurations, you can migrate a Machine API resource to the equivalent Cluster API resource by changing which API it considers authoritative. When you migrate a Machine API resources to the Cluster API, you transfer management of the resource to the Cluster API.</p>
      <p>By migrating a Machine API resource to use the Cluster API, you can verify that everything works as expected before deciding to use the Cluster API in production clusters. After migrating a Machine API resource to an equivalent Cluster API resource, you can examine the new resource to verify that the features and configuration match the original Machine API resource.</p>
      <p>When you change the authoritative API for a compute machine set, any existing compute machines that the compute machine set manages retain their original authoritative API. As a result, a compute machine set that manages machines that use different authoritative APIs is a valid and expected occurrence in clusters that support migrating between these API types.</p>
      <p>When you change the authoritative API for a compute machine, the instance on the underlying infrastructure that backs the machine is not recreated or reprovisioned. In-place changes, such as modifying labels, tags, taints, or annotations, are the only changes that the API group can make to the underlying instance that backs the machine.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>You can only migrate some resources on supported infrastructure types.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Supported resource conversions</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Infrastructure</TableHead>
      <TableHead>Compute machine</TableHead>
      <TableHead>Compute machine set</TableHead>
      <TableHead>Machine health check</TableHead>
      <TableHead>Control plane machine set</TableHead>
      <TableHead>Cluster autoscaler</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>AWS</TableCell>
      <TableCell>Technology Preview</TableCell>
      <TableCell>Technology Preview</TableCell>
      <TableCell>Not Available</TableCell>
      <TableCell>Not Available</TableCell>
      <TableCell>Not Available</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>All other infrastructure types</TableCell>
      <TableCell>Not Available</TableCell>
      <TableCell>Not Available</TableCell>
      <TableCell>Not Available</TableCell>
      <TableCell>Not Available</TableCell>
      <TableCell>Not Available</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h3 id="machine-set-authoritative-api-machines_cluster-api-getting-started" class="group scroll-mt-20">Authoritative API types of compute machines<a href="#machine-set-authoritative-api-machines_cluster-api-getting-started" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">The values of the <code>.spec.authoritativeAPI</code> and <code>.spec.template.spec.authoritativeAPI</code> fields in a Machine API compute machine set determine the authoritative API of the compute machines.</p>
      <p><strong>Interaction of </strong><code>authoritativeAPI</code><strong> fields when creating compute machines</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead></TableHead>
      <TableHead></TableHead>
      <TableHead></TableHead>
      <TableHead></TableHead>
      <TableHead></TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>.spec.authoritativeAPI</code> value</TableCell>
      <TableCell><code>ClusterAPI</code></TableCell>
      <TableCell><code>ClusterAPI</code></TableCell>
      <TableCell><code>MachineAPI</code></TableCell>
      <TableCell><code>MachineAPI</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>.spec.template.spec.authoritativeAPI</code> value</TableCell>
      <TableCell><code>ClusterAPI</code></TableCell>
      <TableCell><code>MachineAPI</code></TableCell>
      <TableCell><code>MachineAPI</code></TableCell>
      <TableCell><code>ClusterAPI</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>authoritativeAPI</code> value for new compute machines</TableCell>
      <TableCell><code>ClusterAPI</code></TableCell>
      <TableCell><code>ClusterAPI</code></TableCell>
      <TableCell><code>MachineAPI</code></TableCell>
      <TableCell><code>ClusterAPI</code></TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>When the <code>.spec.authoritativeAPI</code> value is <code>ClusterAPI</code>, the Machine API machine set is not authoritative and the <code>.spec.template.spec.authoritativeAPI</code> value is not used. As a result, the only combination that creates a compute machine with the Machine API as authoritative is where the <code>.spec.authoritativeAPI</code> and <code>.spec.template.spec.authoritativeAPI</code> values are <code>MachineAPI</code>.</p>
        </AlertDescription>
      </Alert>
      <h3 id="migrating-between-capi-mapi_cluster-api-getting-started" class="group scroll-mt-20">Migrating a Machine API resource to use the Cluster API<a href="#migrating-between-capi-mapi_cluster-api-getting-started" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can migrate individual Machine API objects to equivalent Cluster API objects.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Migrating a Machine API resource to use the Cluster API is a Technology Preview feature only. Technology Preview features are not supported with Red Hat production service level agreements (SLAs) and might not be functionally complete. Red Hat does not recommend using them in production. These features provide early access to upcoming product features, enabling customers to test functionality and provide feedback during the development process.</p>
          <p>For more information about the support scope of Red Hat Technology Preview features, see <a href="https://access.redhat.com/support/offerings/techpreview/">Technology Preview Features Support Scope</a>.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have deployed an OpenShift Container Platform cluster on a supported infrastructure type.</li>
      <li>You have enabled the use of the Cluster API.</li>
      <li>You have enabled the <code>MachineAPIMigration</code> feature gate in the <code>TechPreviewNoUpgrade</code> feature set.</li>
      <li>You have access to the cluster using an account with <code>cluster-admin</code> permissions.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Identify the Machine API resource that you want to migrate to a Cluster API resource by running the following command:
      <CodeBlock code={"$ oc get <resource_kind> -n {{ from_namespace }}"} lang="terminal" />
      <p>where <code>&lt;resource_kind&gt;</code> is one of the following values:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>machine.{"{"}{"{"} from_api_group {"}"}{"}"}</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The fully qualified name of the resource kind for a compute or control plane machine.</p></dd>
      <dt class="font-semibold"><code>machineset.{"{"}{"{"} from_api_group {"}"}{"}"}</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The fully qualified name of the resource kind for a compute machine set.</p></dd>
      </dl>
      </li>
      <li>
      Edit the resource specification by running the following command:
      <CodeBlock code={"$ oc edit <resource_kind>/<resource_name> -n openshift-machine-api"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;resource_kind&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a compute machine with <code>machine.machine.openshift.io</code> or compute machine set with <code>machineset.machine.openshift.io</code>.</p></dd>
      <dt class="font-semibold"><code>&lt;resource_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the Machine API resource that you want to migrate to a Cluster API resource.</p></dd>
      </dl>
      </li>
      <li>
      In the resource specification, update the value of the <code>spec.authoritativeAPI</code> field:
      <CodeBlock code={"apiVersion: machine.openshift.io/v1beta1\nkind: <resource_kind>\nmetadata:\n  name: <resource_name>\n  [...]\nspec:\n  authoritativeAPI: {{ to_api_value }}\n  [...]\nstatus:\n  authoritativeAPI: {{ from_api_value }}\n  [...]"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>kind</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the resource kind of the resource that you want to migrate. For example, the resource kind for a compute machine set is <code>MachineSet</code> and the resource kind for a compute machine is <code>Machine</code>.</p></dd>
      <dt class="font-semibold"><code>metadata.name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the resource that you want to migrate.</p></dd>
      <dt class="font-semibold"><code>spec.authoritativeAPI</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the authoritative API that you want this resource to use. For example, to start migrating a Machine API resource to the Cluster API, specify <code>{"{"}{"{"} to_api_value {"}"}{"}"}</code>.</p></dd>
      <dt class="font-semibold"><code>status.authoritativeAPI</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the value for the current authoritative API. This value indicates which API currently manages this resource. Do not change the value in this part of the specification.</p></dd>
      </dl>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Do not change other values when you update the value of the <code>spec.authoritativeAPI</code> field. Because other controllers might process updates to other values before the synchronization controller processes the <code>spec.authoritativeAPI</code> field update, changing other values can cause unexpected behavior.</p>
          <p>For more information, see "Unexpected behavior when changing resource configurations".</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      Check the status of the conversion by running the following command:
      <CodeBlock code={"$ oc -n openshift-machine-api get <resource_kind>/<resource_name> -o json | jq .status.authoritativeAPI"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;resource_kind&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a compute machine with <code>machine.machine.openshift.io</code> or compute machine set with <code>machineset.machine.openshift.io</code>.</p></dd>
      <dt class="font-semibold"><code>&lt;resource_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the Machine API resource that you want to migrate to a Cluster API resource.</p></dd>
      </dl>
      <ul>
      <li>While the conversion progresses, this command returns a value of <code>Migrating</code>. If this value persists for a long time, check the logs for the <code>cluster-capi-operator</code> deployment in the <code>openshift-cluster-api</code> namespace for more information and to identify potential issues.</li>
      <li>When the conversion is complete, this command returns a value of <code>{"{"}{"{"} to_api_value {"}"}{"}"}</code>.</li>
      </ul>
      </li>
      </ul>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster-api-troubleshooting#ts-capi-migrate-unexpected-behavior_cluster-api-troubleshooting">Unexpected behavior when changing resource configurations</a></li></ul>
      <h3 id="deploying-capi-machines-via-mapi-machine-sets_cluster-api-getting-started" class="group scroll-mt-20">Deploying Cluster API compute machines by using a Machine API compute machine set<a href="#deploying-capi-machines-via-mapi-machine-sets_cluster-api-getting-started" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can configure a Machine API compute machine set to deploy Cluster API compute machines. With this process, you can test the Cluster API compute machine creation workflow without creating and scaling a Cluster API compute machine set.</p>
      <p>A Machine API compute machine set with this configuration creates nonauthoritative Machine API compute machines that use the Cluster API as authoritative. The two-way synchronization controller then creates corresponding authoritative Cluster API machines that provision on the underlying infrastructure.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Deploying Cluster API compute machines by using a Machine API compute machine set is a Technology Preview feature only. Technology Preview features are not supported with Red Hat production service level agreements (SLAs) and might not be functionally complete. Red Hat does not recommend using them in production. These features provide early access to upcoming product features, enabling customers to test functionality and provide feedback during the development process.</p>
          <p>For more information about the support scope of Red Hat Technology Preview features, see <a href="https://access.redhat.com/support/offerings/techpreview/">Technology Preview Features Support Scope</a>.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have deployed an OpenShift Container Platform cluster on a supported infrastructure type.</li>
      <li>You have enabled the use of the Cluster API.</li>
      <li>You have enabled the <code>MachineAPIMigration</code> feature gate in the <code>TechPreviewNoUpgrade</code> feature set.</li>
      <li>You have access to the cluster using an account with <code>cluster-admin</code> permissions.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      List the Machine API compute machine sets in your cluster by running the following command:
      <CodeBlock code={"$ oc get machineset.machine.openshift.io -n openshift-machine-api"} lang="terminal" />
      </li>
      <li>
      Edit the resource specification by running the following command:
      <CodeBlock code={"$ oc edit machineset.machine.openshift.io <machine_set_name> \\\n  -n openshift-machine-api"} lang="terminal" />
      <p>Replace <code>&lt;machine_set_name&gt;</code> with the name of the Machine API compute machine set that you want to configure to deploy Cluster API compute machines.</p>
      </li>
      <li>
      In the resource specification, update the value of the <code>spec.template.spec.authoritativeAPI</code> field:
      <CodeBlock code={"apiVersion: machine.openshift.io/v1beta1\nkind: MachineSet\nmetadata:\n  [...]\n  name: <machine_set_name>\n  [...]\nspec:\n  authoritativeAPI: MachineAPI\n  [...]\n  template:\n    [...]\n    spec:\n      authoritativeAPI: ClusterAPI\nstatus:\n  authoritativeAPI: MachineAPI\n  [...]"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.authoritativeAPI</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the unconverted value for the Machine API compute machine set. Do not change the value in this part of the specification.</p></dd>
      <dt class="font-semibold"><code>spec.template.spec.authoritativeAPI</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the authoritative API for the machine set. To configure the compute machine set to deploy Cluster API compute machines, set this value to <code>ClusterAPI</code>.</p></dd>
      <dt class="font-semibold"><code>status.authoritativeAPI</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the current value for the Machine API compute machine set. Do not change the value in this part of the specification.</p></dd>
      </dl>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>
      List the machines that are managed by the updated compute machine set by running the following command:
      <CodeBlock code={"$ oc get machines.machine.openshift.io \\\n  -n openshift-machine-api \\\n  -l machine.openshift.io/cluster-api-machineset=<machine_set_name>"} lang="terminal" />
      </li>
      <li>
      To verify that a machine created by the updated machine set has the correct configuration, examine the <code>status.authoritativeAPI</code> field in the CR for one of the new machines by running the following command:
      <CodeBlock code={"$ oc describe machines.machine.openshift.io <machine_name> \\\n  -n openshift-machine-api"} lang="terminal" />
      <p>For a Cluster API compute machine, the value of the field is <code>ClusterAPI</code>.</p>
      </li>
      </ol>
      <p><strong>Additional resources</strong></p>
      <ul>
      <li><a href="/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster-api-troubleshooting#ts-capi-resource-migration_cluster-api-troubleshooting">Troubleshooting resource migration</a></li>
      <li><a href="/openshift-docs-markdown/machine_management/cluster_api_machine_management/cluster-api-disabling#capi-to-mapi-migration-overview_cluster-api-disabling">Migrating Cluster API resources to Machine API resources</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
