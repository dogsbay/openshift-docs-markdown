---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: machine_management/control_plane_machine_management/cpmso-about.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"cpmso-about","text":"About control plane machine sets"},{"depth":2,"slug":"cpmso-overview_cpmso-about","text":"Control Plane Machine Set Operator overview"},{"depth":3,"slug":"cpmso-limitations_cpmso-about","text":"Control Plane Machine Set Operator limitations"},{"depth":2,"slug":"additional-resources_cpmso-about","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/machine_management/control_plane_machine_management/cpmso-about";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "About control plane machine sets";
const description = "With control plane machine sets, you can automate management of the control plane machine resources within your OpenShift Container Platform cluster…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["machine_management","machine_management/control_plane_machine_management"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# About control plane machine sets {#cpmso-about}\n\nWith control plane machine sets, you can automate management of the control plane machine resources within your OpenShift Container Platform cluster, simplifying upgrades and recovery from degraded control plane machines.\n\n> [!IMPORTANT]\n> Control plane machine sets cannot manage compute machines, and compute machine sets cannot manage control plane machines.\n\nControl plane machine sets provide for control plane machines similar management capabilities as compute machine sets provide for compute machines. However, these two types of machine sets are separate custom resources defined within the Machine API and have several fundamental differences in their architecture and functionality.\n\n## Control Plane Machine Set Operator overview {#cpmso-overview_cpmso-about}\n\nYou can use the Control Plane Machine Set Operator to automate management of control plane machines in your OpenShift Container Platform cluster, including automated replacement of degraded machines and rollout of configuration changes.\n\nWhen the state of the cluster control plane machine set is set to `Active`, the Operator ensures that the cluster has the correct number of control plane machines with the specified configuration. This allows the automated replacement of degraded control plane machines and rollout of changes to the control plane.\n\nA cluster has only one control plane machine set, and the Operator only manages objects in the `openshift-machine-api` namespace.\n\n### Control Plane Machine Set Operator limitations {#cpmso-limitations_cpmso-about}\n\nBefore using the Control Plane Machine Set Operator, review platform support, cluster requirements, and unsupported configurations to ensure compatibility with your environment.\n\n- Only Amazon Web Services (AWS), Google Cloud, IBM Power(R) Virtual Server, Microsoft Azure, Nutanix, VMware vSphere, and Red Hat OpenStack Platform (RHOSP) clusters are supported.\n- Clusters that do not have preexisting machines that represent the control plane nodes cannot use a control plane machine set or enable the use of a control plane machine set after installation. Generally, preexisting control plane machines are only present if the installation program provisioned the cluster infrastructure.\n\n  To check if a cluster has the required preexisting control plane machines, run the following command as a user with administrator privileges:\n\n  ```terminal\n  $ oc get machine \\\n    -n openshift-machine-api \\\n    -l machine.openshift.io/cluster-api-machine-role=master\n  ```\n\n  ```text {title=\"Example output showing preexisting control plane machines\"}\n  NAME                    PHASE     TYPE         REGION      ZONE         AGE\n  <infrastructure_id>-master-0   Running   m6i.xlarge   us-west-1   us-west-1a   5h19m\n  <infrastructure_id>-master-1   Running   m6i.xlarge   us-west-1   us-west-1b   5h19m\n  <infrastructure_id>-master-2   Running   m6i.xlarge   us-west-1   us-west-1a   5h19m\n  ```\n\n  ```text {title=\"Example output missing preexisting control plane machines\"}\n  No resources found in openshift-machine-api namespace.\n  ```\n- The Operator requires the Machine API Operator to be operational and is therefore not supported on clusters with manually provisioned machines. When installing a OpenShift Container Platform cluster with manually provisioned machines for a platform that creates an active generated `ControlPlaneMachineSet` custom resource (CR), you must remove the Kubernetes manifest files that define the control plane machine set as instructed in the installation process.\n- Only clusters with three control plane machines are supported.\n- Horizontal scaling of the control plane is not supported.\n- Deploying Azure control plane machines on Ephemeral operating system disks increases risk for data loss and is not supported.\n- Deploying control plane machines as AWS Spot Instances, Google Cloud preemptible VMs, or Azure Spot VMs is not supported.\n\n  > [!IMPORTANT]\n  > Attempting to deploy control plane machines as AWS Spot Instances, Google Cloud preemptible VMs, or Azure Spot VMs might cause the cluster to lose etcd quorum. A cluster that loses all control plane machines simultaneously is unrecoverable.\n- Making changes to the control plane machine set during or before installation is not supported. You must make any changes to the control plane machine set only after installation.\n\n## Additional resources {#additional-resources_cpmso-about}\n\n- [Control Plane Machine Set Operator reference](/openshift-docs-markdown/operators/operator-reference#control-plane-machine-set-operator_operator-reference)\n- [`ControlPlaneMachineSet` custom resource](/openshift-docs-markdown/rest_api/machine_apis/controlplanemachineset-machine-openshift-io-v1#controlplanemachineset-machine-openshift-io-v1)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/machine_management/control_plane_machine_management/cpmso-about.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="cpmso-about" class="group scroll-mt-20">About control plane machine sets<a href="#cpmso-about" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>With control plane machine sets, you can automate management of the control plane machine resources within your OpenShift Container Platform cluster, simplifying upgrades and recovery from degraded control plane machines.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Control plane machine sets cannot manage compute machines, and compute machine sets cannot manage control plane machines.</p>
        </AlertDescription>
      </Alert>
      <p>Control plane machine sets provide for control plane machines similar management capabilities as compute machine sets provide for compute machines. However, these two types of machine sets are separate custom resources defined within the Machine API and have several fundamental differences in their architecture and functionality.</p>
      <h2 id="cpmso-overview_cpmso-about" class="group scroll-mt-20">Control Plane Machine Set Operator overview<a href="#cpmso-overview_cpmso-about" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can use the Control Plane Machine Set Operator to automate management of control plane machines in your OpenShift Container Platform cluster, including automated replacement of degraded machines and rollout of configuration changes.</p>
      <p>When the state of the cluster control plane machine set is set to <code>Active</code>, the Operator ensures that the cluster has the correct number of control plane machines with the specified configuration. This allows the automated replacement of degraded control plane machines and rollout of changes to the control plane.</p>
      <p>A cluster has only one control plane machine set, and the Operator only manages objects in the <code>openshift-machine-api</code> namespace.</p>
      <h3 id="cpmso-limitations_cpmso-about" class="group scroll-mt-20">Control Plane Machine Set Operator limitations<a href="#cpmso-limitations_cpmso-about" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">Before using the Control Plane Machine Set Operator, review platform support, cluster requirements, and unsupported configurations to ensure compatibility with your environment.</p>
      <ul>
      <li>Only Amazon Web Services (AWS), Google Cloud, IBM Power(R) Virtual Server, Microsoft Azure, Nutanix, VMware vSphere, and Red Hat OpenStack Platform (RHOSP) clusters are supported.</li>
      <li>
      Clusters that do not have preexisting machines that represent the control plane nodes cannot use a control plane machine set or enable the use of a control plane machine set after installation. Generally, preexisting control plane machines are only present if the installation program provisioned the cluster infrastructure.
      <p>To check if a cluster has the required preexisting control plane machines, run the following command as a user with administrator privileges:</p>
      <CodeBlock code={"$ oc get machine \\\n  -n openshift-machine-api \\\n  -l machine.openshift.io/cluster-api-machine-role=master"} lang="terminal" />
      <CodeBlock code={"NAME                    PHASE     TYPE         REGION      ZONE         AGE\n<infrastructure_id>-master-0   Running   m6i.xlarge   us-west-1   us-west-1a   5h19m\n<infrastructure_id>-master-1   Running   m6i.xlarge   us-west-1   us-west-1b   5h19m\n<infrastructure_id>-master-2   Running   m6i.xlarge   us-west-1   us-west-1a   5h19m"} lang="text" title="Example output showing preexisting control plane machines" />
      <CodeBlock code={"No resources found in openshift-machine-api namespace."} lang="text" title="Example output missing preexisting control plane machines" />
      </li>
      <li>The Operator requires the Machine API Operator to be operational and is therefore not supported on clusters with manually provisioned machines. When installing a OpenShift Container Platform cluster with manually provisioned machines for a platform that creates an active generated <code>ControlPlaneMachineSet</code> custom resource (CR), you must remove the Kubernetes manifest files that define the control plane machine set as instructed in the installation process.</li>
      <li>Only clusters with three control plane machines are supported.</li>
      <li>Horizontal scaling of the control plane is not supported.</li>
      <li>Deploying Azure control plane machines on Ephemeral operating system disks increases risk for data loss and is not supported.</li>
      <li>
      Deploying control plane machines as AWS Spot Instances, Google Cloud preemptible VMs, or Azure Spot VMs is not supported.
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Attempting to deploy control plane machines as AWS Spot Instances, Google Cloud preemptible VMs, or Azure Spot VMs might cause the cluster to lose etcd quorum. A cluster that loses all control plane machines simultaneously is unrecoverable.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>Making changes to the control plane machine set during or before installation is not supported. You must make any changes to the control plane machine set only after installation.</li>
      </ul>
      <h2 id="additional-resources_cpmso-about" class="group scroll-mt-20">Additional resources<a href="#additional-resources_cpmso-about" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/operators/operator-reference#control-plane-machine-set-operator_operator-reference">Control Plane Machine Set Operator reference</a></li>
      <li><a href="/openshift-docs-markdown/rest_api/machine_apis/controlplanemachineset-machine-openshift-io-v1#controlplanemachineset-machine-openshift-io-v1"><code>ControlPlaneMachineSet</code> custom resource</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
