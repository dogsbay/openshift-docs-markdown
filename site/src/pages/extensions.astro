---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: extensions.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";

const headings = [{"depth":1,"slug":"extensions-overview","text":"Extensions overview"},{"depth":2,"slug":"olmv1-highlights_extensions-overview","text":"OLM v1 highlights"},{"depth":2,"slug":"olmv1-about-purpose_extensions-overview","text":"Purpose of Operator Lifecycle Manager"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/extensions";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Extensions overview";
const description = "You can configure the operating system of your nodes and extend capabilities to your cluster by using pre-packaged software extensions. These extensions…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = null;
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Extensions overview {#extensions-overview}\n\nYou can configure the operating system of your nodes and extend capabilities to your cluster by using pre-packaged software extensions. These extensions include ready-to-use tools that customize your environment for your requirements.\n\nOperator Lifecycle Manager (OLM) has been included with OpenShift Container Platform 4 since its initial release. OpenShift Container Platform 4.22 includes components for a next-generation iteration of OLM as a Generally Available (GA) feature, known during this phase as *OLM v1*. This updated framework evolves many of the concepts that have been part of previous versions of OLM and adds new capabilities.\n\n> [!NOTE]\n> For OpenShift Container Platform 4.22, documented procedures for OLM v1 are CLI-based only. Alternatively, administrators can create and view related objects in the web console by using normal methods, such as the **Import YAML** and **Search** pages. However, the existing **Software Catalog** and **Installed Operators** pages do not yet display OLM v1 components.\n\n## OLM v1 highlights {#olmv1-highlights_extensions-overview}\n\nAs an administrator, use OLM v1 to securely manage your cluster configurations and streamline updates through GitOps-based declarative management, granular extension controls, and flexible packaging formats.\n\nAdministrators can explore the following highlights:\n\nFully declarative model that supports GitOps workflows\n:   OLM v1 simplifies extension management through two key APIs:\n\n    - A new `ClusterExtension` API streamlines management of installed extensions, which includes Operators via the `registry+v1` bundle format, by consolidating user-facing APIs into a single object. This API is provided as `clusterextension.olm.operatorframework.io` by the new Operator Controller component. Administrators and SREs can use the API to automate processes and define desired states by using GitOps principles.\n\n    > [!NOTE]\n    > Earlier Technology Preview phases of OLM v1 introduced a new `Operator` API; this API is renamed `ClusterExtension` in OpenShift Container Platform 4.16 to address the following improvements:\n    >\n    > - More accurately reflects the simplified functionality of extending a cluster’s capabilities\n    > - Better represents a more flexible packaging format\n    > - `Cluster` prefix clearly indicates that `ClusterExtension` objects are cluster-scoped, a change from OLM (Classic) where Operators could be either namespace-scoped or cluster-scoped\n\n    - The `Catalog` API, provided by the new catalogd component, serves as the foundation for OLM v1, unpacking catalogs for on-cluster clients so that users can discover installable content, such as Kubernetes extensions and Operators. This provides increased visibility into all available Operator bundle versions, including their details, channels, and update edges.\n\n    For more information, see \"Operator Controller and Catalogd\".\n\nImproved control over extension updates\n:   With improved insight into catalog content, administrators can specify target versions for installation and updates. This grants administrators more control over the target version of extension updates. For more information, see \"Updating a cluster extension\".\n\nFlexible extension packaging format\n:   Administrators can use file-based catalogs to install and manage extensions, such as OLM-based Operators, similar to the OLM (Classic) experience.\n\n    In addition, bundle size is no longer constrained by the etcd value size limit. For more information, see \"Installing extensions\".\n\nSecure catalog communication\n:   OLM v1 uses HTTPS encryption for catalogd server responses.\n\nBasic support for proxied environments and trusted CA certificates\n:   Operator Controller and catalogd can run in proxied environments and include basic support for trusted CA certificates.\n\n## Purpose of Operator Lifecycle Manager {#olmv1-about-purpose_extensions-overview}\n\nThe Operator Lifecycle Manager (OLM) simplifies management of extensions on Kubernetes clusters. It provides administrators with a safe, reliable, and centralized way to install, run, and update cluster extensions.\n\nThe initial version of OLM, which launched with OpenShift Container Platform 4 and is included by default, focused on providing unique support for these specific needs for a particular type of cluster extension, known as Operators. Operators are classified as one or more Kubernetes controllers, shipping with one or more API extensions, as `CustomResourceDefinition` (CRD) objects, to provide additional functionality to the cluster.\n\nAfter running in production clusters for many releases, the next-generation of OLM aims to encompass lifecycles for cluster extensions that are not just Operators.\n\n**Additional resources**\n{._additional-resources}\n\n- [Operator Controller](/openshift-docs-markdown/extensions/arch/operator-controller#operator-controller)\n- [Catalogd](/openshift-docs-markdown/extensions/arch/catalogd#catalogd)\n- [Updating a cluster extension](/openshift-docs-markdown/extensions/ce/managing-ce#olmv1-updating-an-operator_managing-ce)\n- [Installing extensions](/openshift-docs-markdown/extensions/ce/managing-ce#managing-ce)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/extensions.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="extensions-overview" class="group scroll-mt-20">Extensions overview<a href="#extensions-overview" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">You can configure the operating system of your nodes and extend capabilities to your cluster by using pre-packaged software extensions. These extensions include ready-to-use tools that customize your environment for your requirements.</p>
      <p>Operator Lifecycle Manager (OLM) has been included with OpenShift Container Platform 4 since its initial release. OpenShift Container Platform 4.22 includes components for a next-generation iteration of OLM as a Generally Available (GA) feature, known during this phase as <em>OLM v1</em>. This updated framework evolves many of the concepts that have been part of previous versions of OLM and adds new capabilities.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>For OpenShift Container Platform 4.22, documented procedures for OLM v1 are CLI-based only. Alternatively, administrators can create and view related objects in the web console by using normal methods, such as the <strong>Import YAML</strong> and <strong>Search</strong> pages. However, the existing <strong>Software Catalog</strong> and <strong>Installed Operators</strong> pages do not yet display OLM v1 components.</p>
        </AlertDescription>
      </Alert>
      <h2 id="olmv1-highlights_extensions-overview" class="group scroll-mt-20">OLM v1 highlights<a href="#olmv1-highlights_extensions-overview" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">As an administrator, use OLM v1 to securely manage your cluster configurations and streamline updates through GitOps-based declarative management, granular extension controls, and flexible packaging formats.</p>
      <p>Administrators can explore the following highlights:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Fully declarative model that supports GitOps workflows</dt>
      <dd class="ml-6 text-muted-foreground">
      <p>OLM v1 simplifies extension management through two key APIs:</p>
      <ul><li>A new <code>ClusterExtension</code> API streamlines management of installed extensions, which includes Operators via the <code>registry+v1</code> bundle format, by consolidating user-facing APIs into a single object. This API is provided as <code>clusterextension.olm.operatorframework.io</code> by the new Operator Controller component. Administrators and SREs can use the API to automate processes and define desired states by using GitOps principles.</li></ul>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Earlier Technology Preview phases of OLM v1 introduced a new <code>Operator</code> API; this API is renamed <code>ClusterExtension</code> in OpenShift Container Platform 4.16 to address the following improvements:</p>
          <ul>
          <li>More accurately reflects the simplified functionality of extending a cluster’s capabilities</li>
          <li>Better represents a more flexible packaging format</li>
          <li><code>Cluster</code> prefix clearly indicates that <code>ClusterExtension</code> objects are cluster-scoped, a change from OLM (Classic) where Operators could be either namespace-scoped or cluster-scoped</li>
          </ul>
        </AlertDescription>
      </Alert>
      <ul><li>The <code>Catalog</code> API, provided by the new catalogd component, serves as the foundation for OLM v1, unpacking catalogs for on-cluster clients so that users can discover installable content, such as Kubernetes extensions and Operators. This provides increased visibility into all available Operator bundle versions, including their details, channels, and update edges.</li></ul>
      <p>For more information, see "Operator Controller and Catalogd".</p>
      </dd>
      <dt class="font-semibold">Improved control over extension updates</dt>
      <dd class="ml-6 text-muted-foreground"><p>With improved insight into catalog content, administrators can specify target versions for installation and updates. This grants administrators more control over the target version of extension updates. For more information, see "Updating a cluster extension".</p></dd>
      <dt class="font-semibold">Flexible extension packaging format</dt>
      <dd class="ml-6 text-muted-foreground">
      <p>Administrators can use file-based catalogs to install and manage extensions, such as OLM-based Operators, similar to the OLM (Classic) experience.</p>
      <p>In addition, bundle size is no longer constrained by the etcd value size limit. For more information, see "Installing extensions".</p>
      </dd>
      <dt class="font-semibold">Secure catalog communication</dt>
      <dd class="ml-6 text-muted-foreground"><p>OLM v1 uses HTTPS encryption for catalogd server responses.</p></dd>
      <dt class="font-semibold">Basic support for proxied environments and trusted CA certificates</dt>
      <dd class="ml-6 text-muted-foreground"><p>Operator Controller and catalogd can run in proxied environments and include basic support for trusted CA certificates.</p></dd>
      </dl>
      <h2 id="olmv1-about-purpose_extensions-overview" class="group scroll-mt-20">Purpose of Operator Lifecycle Manager<a href="#olmv1-about-purpose_extensions-overview" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The Operator Lifecycle Manager (OLM) simplifies management of extensions on Kubernetes clusters. It provides administrators with a safe, reliable, and centralized way to install, run, and update cluster extensions.</p>
      <p>The initial version of OLM, which launched with OpenShift Container Platform 4 and is included by default, focused on providing unique support for these specific needs for a particular type of cluster extension, known as Operators. Operators are classified as one or more Kubernetes controllers, shipping with one or more API extensions, as <code>CustomResourceDefinition</code> (CRD) objects, to provide additional functionality to the cluster.</p>
      <p>After running in production clusters for many releases, the next-generation of OLM aims to encompass lifecycles for cluster extensions that are not just Operators.</p>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/extensions/arch/operator-controller#operator-controller">Operator Controller</a></li>
      <li><a href="/openshift-docs-markdown/extensions/arch/catalogd#catalogd">Catalogd</a></li>
      <li><a href="/openshift-docs-markdown/extensions/ce/managing-ce#olmv1-updating-an-operator_managing-ce">Updating a cluster extension</a></li>
      <li><a href="/openshift-docs-markdown/extensions/ce/managing-ce#managing-ce">Installing extensions</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
