---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: applications/working_with_helm_charts/installing-helm.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"installing-helm","text":"Installing Helm"},{"depth":2,"slug":"_on_linux","text":"On Linux"},{"depth":2,"slug":"_on_windows","text":"On Windows"},{"depth":2,"slug":"_on_macos","text":"On macOS"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/applications/working_with_helm_charts/installing-helm";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Installing Helm";
const description = "Install the Helm CLI to manage software packages on your OpenShift Container Platform cluster from a local workstation." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["applications","applications/working_with_helm_charts"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Installing Helm {#installing-helm}\n\nInstall the Helm CLI to manage software packages on your OpenShift Container Platform cluster from a local workstation.\n\nYou can also find the URL to the latest binaries from the OpenShift Container Platform web console by clicking the **?** icon in the upper-right corner and selecting **Command Line Tools**.\n\n## On Linux {#_on_linux}\n\n1. Download the Helm binary:\n\n   - Linux (x86_64, amd64)\n\n     ```terminal\n     # curl -L https://mirror.openshift.com/pub/openshift-v4/clients/helm/latest/helm-linux-amd64 -o /usr/local/bin/helm\n     ```\n   - Linux on {{ ibm_z_name }} and {{ ibm_linuxone_name }} (s390x)\n\n     ```terminal\n     # curl -L https://mirror.openshift.com/pub/openshift-v4/clients/helm/latest/helm-linux-s390x -o /usr/local/bin/helm\n     ```\n   - Linux on {{ ibm_power_name }} (ppc64le)\n\n     ```terminal\n     # curl -L https://mirror.openshift.com/pub/openshift-v4/clients/helm/latest/helm-linux-ppc64le -o /usr/local/bin/helm\n     ```\n   - Linux on ARM (arm64)\n\n     ```terminal\n     # curl -L https://mirror.openshift.com/pub/openshift-v4/clients/helm/latest/helm-linux-arm64 -o /usr/local/bin/helm\n     ```\n2. Make the binary file executable:\n\n   ```terminal\n   # chmod +x /usr/local/bin/helm\n   ```\n3. Check the installed version:\n\n   ```terminal\n   $ helm version\n   ```\n\n   ```terminal {title=\"Example output\"}\n   version.BuildInfo{Version:\"v3.0\", GitCommit:\"b31719aab7963acf4887a1c1e6d5e53378e34d93\", GitTreeState:\"clean\", GoVersion:\"go1.13.4\"}\n   ```\n\n## On Windows {#_on_windows}\n\n1. Download the Helm binary: [`.exe` file](https://mirror.openshift.com/pub/openshift-v4/clients/helm/latest/helm-windows-amd64.exe).\n2. Click **Search** and type `env` or `environment`.\n3. Select **Edit environment variables for your account**.\n4. Select **Path** from the **Variable** section and click **Edit**.\n5. Click **New** and type the path to the directory with the exe file into the field or click **Browse** and select the directory, and click **OK**.\n\n## On macOS {#_on_macos}\n\n1. Download the Helm binary:\n\n   - For macOS on Intel (x86_64):\n\n     ```terminal\n     # curl -L https://mirror.openshift.com/pub/openshift-v4/clients/helm/latest/helm-darwin-amd64 -o /usr/local/bin/helm\n     ```\n   - For macOS on ARM (Apple Silicon):\n\n     ```terminal\n     # curl -L https://mirror.openshift.com/pub/openshift-v4/clients/helm/latest/helm-darwin-arm64 -o /usr/local/bin/helm\n     ```\n2. Make the binary file executable:\n\n   ```terminal\n   # chmod +x /usr/local/bin/helm\n   ```\n3. Check the installed version:\n\n   ```terminal\n   $ helm version\n   ```\n\n   ```terminal {title=\"Example output\"}\n   version.BuildInfo{Version:\"v3.0\", GitCommit:\"b31719aab7963acf4887a1c1e6d5e53378e34d93\", GitTreeState:\"clean\", GoVersion:\"go1.13.4\"}\n   ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/applications/working_with_helm_charts/installing-helm.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="installing-helm" class="group scroll-mt-20">Installing Helm<a href="#installing-helm" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="/_attributes/common-attributes.md"></p>
      <p>Install the Helm CLI to manage software packages on your OpenShift Container Platform cluster from a local workstation.</p>
      <p>You can also find the URL to the latest binaries from the OpenShift Container Platform web console by clicking the <strong>?</strong> icon in the upper-right corner and selecting <strong>Command Line Tools</strong>.</p>
      <h2 id="_on_linux" class="group scroll-mt-20">On Linux<a href="#_on_linux" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ol>
      <li>
      Download the Helm binary:
      <ul>
      <li>
      Linux (x86_64, amd64)
      <CodeBlock code={"# curl -L https://mirror.openshift.com/pub/openshift-v4/clients/helm/latest/helm-linux-amd64 -o /usr/local/bin/helm"} lang="terminal" />
      </li>
      <li>
      Linux on {"{"}{"{"} ibm_z_name {"}"}{"}"} and {"{"}{"{"} ibm_linuxone_name {"}"}{"}"} (s390x)
      <CodeBlock code={"# curl -L https://mirror.openshift.com/pub/openshift-v4/clients/helm/latest/helm-linux-s390x -o /usr/local/bin/helm"} lang="terminal" />
      </li>
      <li>
      Linux on {"{"}{"{"} ibm_power_name {"}"}{"}"} (ppc64le)
      <CodeBlock code={"# curl -L https://mirror.openshift.com/pub/openshift-v4/clients/helm/latest/helm-linux-ppc64le -o /usr/local/bin/helm"} lang="terminal" />
      </li>
      <li>
      Linux on ARM (arm64)
      <CodeBlock code={"# curl -L https://mirror.openshift.com/pub/openshift-v4/clients/helm/latest/helm-linux-arm64 -o /usr/local/bin/helm"} lang="terminal" />
      </li>
      </ul>
      </li>
      <li>
      Make the binary file executable:
      <CodeBlock code={"# chmod +x /usr/local/bin/helm"} lang="terminal" />
      </li>
      <li>
      Check the installed version:
      <CodeBlock code={"$ helm version"} lang="terminal" />
      <CodeBlock code={"version.BuildInfo{Version:\"v3.0\", GitCommit:\"b31719aab7963acf4887a1c1e6d5e53378e34d93\", GitTreeState:\"clean\", GoVersion:\"go1.13.4\"}"} lang="terminal" title="Example output" />
      </li>
      </ol>
      <h2 id="_on_windows" class="group scroll-mt-20">On Windows<a href="#_on_windows" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ol>
      <li>Download the Helm binary: <a href="https://mirror.openshift.com/pub/openshift-v4/clients/helm/latest/helm-windows-amd64.exe"><code>.exe</code> file</a>.</li>
      <li>Click <strong>Search</strong> and type <code>env</code> or <code>environment</code>.</li>
      <li>Select <strong>Edit environment variables for your account</strong>.</li>
      <li>Select <strong>Path</strong> from the <strong>Variable</strong> section and click <strong>Edit</strong>.</li>
      <li>Click <strong>New</strong> and type the path to the directory with the exe file into the field or click <strong>Browse</strong> and select the directory, and click <strong>OK</strong>.</li>
      </ol>
      <h2 id="_on_macos" class="group scroll-mt-20">On macOS<a href="#_on_macos" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ol>
      <li>
      Download the Helm binary:
      <ul>
      <li>
      For macOS on Intel (x86_64):
      <CodeBlock code={"# curl -L https://mirror.openshift.com/pub/openshift-v4/clients/helm/latest/helm-darwin-amd64 -o /usr/local/bin/helm"} lang="terminal" />
      </li>
      <li>
      For macOS on ARM (Apple Silicon):
      <CodeBlock code={"# curl -L https://mirror.openshift.com/pub/openshift-v4/clients/helm/latest/helm-darwin-arm64 -o /usr/local/bin/helm"} lang="terminal" />
      </li>
      </ul>
      </li>
      <li>
      Make the binary file executable:
      <CodeBlock code={"# chmod +x /usr/local/bin/helm"} lang="terminal" />
      </li>
      <li>
      Check the installed version:
      <CodeBlock code={"$ helm version"} lang="terminal" />
      <CodeBlock code={"version.BuildInfo{Version:\"v3.0\", GitCommit:\"b31719aab7963acf4887a1c1e6d5e53378e34d93\", GitTreeState:\"clean\", GoVersion:\"go1.13.4\"}"} lang="terminal" title="Example output" />
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
