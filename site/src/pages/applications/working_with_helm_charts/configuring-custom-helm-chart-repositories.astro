---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: applications/working_with_helm_charts/configuring-custom-helm-chart-repositories.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;

const headings = [{"depth":1,"slug":"configuring-custom-helm-chart-repositories","text":"Configuring custom Helm chart repositories"},{"depth":2,"slug":"_using_helm_in_the_web_terminal","text":"Using Helm in the web terminal"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/applications/working_with_helm_charts/configuring-custom-helm-chart-repositories";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring custom Helm chart repositories";
const description = "{% include \"./_attributes/common-attributes.md\" %} You can create Helm releases on an OpenShift Container Platform cluster using the following methods:" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["applications","applications/working_with_helm_charts"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring custom Helm chart repositories {#configuring-custom-helm-chart-repositories}\n\n{% include \"./_attributes/common-attributes.md\" %} You can create Helm releases on an OpenShift Container Platform cluster using the following methods:\n\n- The CLI.\n- The **Developer** perspective of the web console.\n\nThe **Developer Catalog**, in the **Developer** perspective of the web console, displays the Helm charts available in the cluster. By default, it lists the Helm charts from the Red Hat OpenShift Helm chart repository. For a list of the charts, see [the Red Hat `Helm index` file](https://charts.openshift.io/index.yaml).\n\nAs a cluster administrator, you can add multiple cluster-scoped and namespace-scoped Helm chart repositories, separate from the default cluster-scoped Helm repository, and display the Helm charts from these repositories in the **Developer Catalog**.\n\nAs a regular user or project member with the appropriate role-based access control (RBAC) permissions, you can add multiple namespace-scoped Helm chart repositories, apart from the default cluster-scoped Helm repository, and display the Helm charts from these repositories in the **Developer Catalog**.\n\nIn the **Developer** perspective of the web console, you can use the **Helm** page to:\n\n- Create Helm Releases and Repositories using the **Create** button.\n- Create, update, or delete a cluster-scoped or namespace-scoped Helm chart repository.\n- View the list of the existing Helm chart repositories in the Repositories tab, which can also be easily distinguished as either cluster scoped or namespace scoped.\n\n## Using Helm in the web terminal {#_using_helm_in_the_web_terminal}\n\nYou can use Helm by [Accessing the web terminal](/openshift-docs-markdown/web_console/web_terminal/odc-using-web-terminal#odc-access-web-terminal_odc-using-web-terminal) in the **Developer** perspective of the web console.\n\n{% include \"./modules/helm-adding-helm-chart-repositories.md\" %}\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/applications/working_with_helm_charts/configuring-custom-helm-chart-repositories.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="configuring-custom-helm-chart-repositories" class="group scroll-mt-20">Configuring custom Helm chart repositories<a href="#configuring-custom-helm-chart-repositories" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>{"{"}% include "./_attributes/common-attributes.md" %{"}"} You can create Helm releases on an OpenShift Container Platform cluster using the following methods:</p>
      <ul>
      <li>The CLI.</li>
      <li>The <strong>Developer</strong> perspective of the web console.</li>
      </ul>
      <p>The <strong>Developer Catalog</strong>, in the <strong>Developer</strong> perspective of the web console, displays the Helm charts available in the cluster. By default, it lists the Helm charts from the Red Hat OpenShift Helm chart repository. For a list of the charts, see <a href="https://charts.openshift.io/index.yaml">the Red Hat <code>Helm index</code> file</a>.</p>
      <p>As a cluster administrator, you can add multiple cluster-scoped and namespace-scoped Helm chart repositories, separate from the default cluster-scoped Helm repository, and display the Helm charts from these repositories in the <strong>Developer Catalog</strong>.</p>
      <p>As a regular user or project member with the appropriate role-based access control (RBAC) permissions, you can add multiple namespace-scoped Helm chart repositories, apart from the default cluster-scoped Helm repository, and display the Helm charts from these repositories in the <strong>Developer Catalog</strong>.</p>
      <p>In the <strong>Developer</strong> perspective of the web console, you can use the <strong>Helm</strong> page to:</p>
      <ul>
      <li>Create Helm Releases and Repositories using the <strong>Create</strong> button.</li>
      <li>Create, update, or delete a cluster-scoped or namespace-scoped Helm chart repository.</li>
      <li>View the list of the existing Helm chart repositories in the Repositories tab, which can also be easily distinguished as either cluster scoped or namespace scoped.</li>
      </ul>
      <p class="/modules/odc-creating-helm-releases-using-developer-perspective.md"></p>
      <h2 id="_using_helm_in_the_web_terminal" class="group scroll-mt-20">Using Helm in the web terminal<a href="#_using_helm_in_the_web_terminal" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can use Helm by <a href="/openshift-docs-markdown/web_console/web_terminal/odc-using-web-terminal#odc-access-web-terminal_odc-using-web-terminal">Accessing the web terminal</a> in the <strong>Developer</strong> perspective of the web console.</p>
      <p class="/modules/helm-creating-a-custom-helm-chart-on-openshift.md"></p>
      <p class="/modules/helm-creating-credentials-and-certificates-to-add-helm-repositories.md /modules/helm-adding-namespace-scoped-helm-chart-repositories.md">{"{"}% include "./modules/helm-adding-helm-chart-repositories.md" %{"}"} </p>
      <p class="/modules/helm-filtering-helm-charts-by-certification-level.md"></p>
      <p class="/modules/helm-disabling-helm-chart-repositories.md"></p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
