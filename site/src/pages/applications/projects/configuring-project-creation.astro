---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: applications/projects/configuring-project-creation.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"configuring-project-creation","text":"Configuring project creation"},{"depth":2,"slug":"about-project-creation_configuring-project-creation","text":"About project creation"},{"depth":2,"slug":"modifying-template-for-new-projects_configuring-project-creation","text":"Modifying the template for new projects"},{"depth":2,"slug":"disabling-project-self-provisioning_configuring-project-creation","text":"Disabling project self-provisioning"},{"depth":2,"slug":"customizing-project-request-message_configuring-project-creation","text":"Customizing the project request message"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/applications/projects/configuring-project-creation";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring project creation";
const description = "As a cluster administrator, you can allow and configure how developers and service accounts can create, or self-provision, their own projects." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["applications","applications/projects"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring project creation {#configuring-project-creation}\n\nAs a cluster administrator, you can allow and configure how developers and service accounts can create, or *self-provision*, their own projects.\n\nIn OpenShift Container Platform, *projects* are used to group and isolate related objects. When you request to create a new project by using the web console or `oc new-project` command, an endpoint in OpenShift Container Platform provisions the project according to a template. You can customize this template to meet your needs.\n\n## About project creation {#about-project-creation_configuring-project-creation}\n\nWhen you request a new project, the OpenShift Container Platform API server automatically provisions the new project based on the project template. The project template is identified by the `projectRequestTemplate` parameter in the project configuration resource of the cluster.\n\nIf the parameter is not defined, the API server creates a default template that creates a project with the requested name. The API server then assigns the requesting user to the `admin` role for that project.\n\nWhen a project request is submitted, the API substitutes the following parameters into the template:\n\n**Default project template parameters**\n\n| Parameter | Description |\n| --- | --- |\n| `PROJECT_NAME` | The name of the project. Required. |\n| `PROJECT_DISPLAYNAME` | The display name of the project. Might be empty. |\n| `PROJECT_DESCRIPTION` | The description of the project. Might be empty. |\n| `PROJECT_ADMIN_USER` | The user name of the administrating user. |\n| `PROJECT_REQUESTING_USER` | The user name of the requesting user. |\n\nAccess to the API is granted to developers with the `self-provisioner` role and the `self-provisioners` cluster role binding. This role is available to all authenticated developers by default.\n\n## Modifying the template for new projects {#modifying-template-for-new-projects_configuring-project-creation}\n\nTo modify the default project template to customize the resources and settings applied when users create new projects, you can create a custom project template.\n\nAs a cluster administrator, you can modify the default project template so that new projects are created using your custom requirements.\n\nTo create your own custom project template:\n\n**Prerequisites**\n\n- You have access to an OpenShift Container Platform cluster using an account with `cluster-admin` permissions.\n\n**Procedure**\n\n1. Log in as a user with `cluster-admin` privileges.\n2. Generate the default project template:\n\n   ```terminal\n   $ oc adm create-bootstrap-project-template -o yaml > template.yaml\n   ```\n3. Use a text editor to modify the generated `template.yaml` file by adding objects or modifying existing objects.\n4. The project template must be created in the `openshift-config` namespace. Load your modified template:\n\n   ```terminal\n   $ oc create -f template.yaml -n openshift-config\n   ```\n5. Edit the project configuration resource using the web console or CLI.\n\n   - Using the web console, complete the following tasks:\n\n     1. Navigate to the **Administration** -> **Cluster Settings** page.\n     2. Click **Configuration** to view all configuration resources.\n     3. Find the entry for **Project** and click **Edit YAML**.\n   - Using the CLI, complete the following tasks:\n\n     1. Edit the `project.config.openshift.io/cluster` resource:\n\n        ```terminal\n        $ oc edit project.config.openshift.io/cluster\n        ```\n6. Update the `spec` section to include the `projectRequestTemplate` and `name` parameters. Ensure you set the name of your uploaded project template. The default name is `project-request`.\n\n   ```yaml {title=\"Project configuration resource with custom project template\"}\n   apiVersion: config.openshift.io/v1\n   kind: Project\n   metadata:\n   # ...\n   spec:\n     projectRequestTemplate:\n       name: <template_name>\n   # ...\n   ```\n7. After you save your changes, create a new project to verify that your changes were successfully applied.\n\n## Disabling project self-provisioning {#disabling-project-self-provisioning_configuring-project-creation}\n\nYou can prevent an authenticated user group from self-provisioning new projects.\n\n**Procedure**\n\n1. Log in as a user with `cluster-admin` privileges.\n2. View the `self-provisioners` cluster role binding usage by running the following command:\n\n   ```terminal\n   $ oc describe clusterrolebinding.rbac self-provisioners\n   ```\n\n   ```terminal {title=\"Example output\"}\n   Name:\t\tself-provisioners\n   Labels:\t\t<none>\n   Annotations:\trbac.authorization.kubernetes.io/autoupdate=true\n   Role:\n     Kind:\tClusterRole\n     Name:\tself-provisioner\n   Subjects:\n     Kind\tName\t\t\t\tNamespace\n     ----\t----\t\t\t\t---------\n     Group\tsystem:authenticated:oauth\n   ```\n\n   Review the subjects in the `self-provisioners` section.\n3. Remove the `self-provisioner` cluster role from the group `system:authenticated:oauth`.\n\n   - If the `self-provisioners` cluster role binding binds only the `self-provisioner` role to the `system:authenticated:oauth` group, run the following command:\n\n     ```terminal\n     $ oc patch clusterrolebinding.rbac self-provisioners -p '{\"subjects\": null}'\n     ```\n   - If the `self-provisioners` cluster role binding binds the `self-provisioner` role to more users, groups, or service accounts than the `system:authenticated:oauth` group, run the following command:\n\n     ```terminal\n     $ oc adm policy \\\n         remove-cluster-role-from-group self-provisioner \\\n         system:authenticated:oauth\n     ```\n4. Edit the `self-provisioners` cluster role binding to prevent automatic updates to the role. Automatic updates reset the cluster roles to the default state.\n\n   - To update the role binding by using the CLI, complete the following steps:\n\n     1. To edit the `self-provisioners` cluster role binding, enter the following command:\n\n        ```terminal\n        $ oc edit clusterrolebinding.rbac self-provisioners\n        ```\n     2. In the displayed role binding, set the `rbac.authorization.kubernetes.io/autoupdate` parameter value to `false`, as shown in the following example:\n\n        ```yaml\n        apiVersion: authorization.openshift.io/v1\n        kind: ClusterRoleBinding\n        metadata:\n          annotations:\n            rbac.authorization.kubernetes.io/autoupdate: \"false\"\n        # ...\n        ```\n   - To update the role binding, run the following single command:\n\n     ```terminal\n     $ oc patch clusterrolebinding.rbac self-provisioners -p '{ \"metadata\": { \"annotations\": { \"rbac.authorization.kubernetes.io/autoupdate\": \"false\" } } }'\n     ```\n5. Log in as an authenticated user and verify that the user can no longer self-provision a project:\n\n   ```terminal\n   $ oc new-project test\n   ```\n\n   ```terminal {title=\"Example output\"}\n   Error from server (Forbidden): You may not request a new project via this API.\n   ```\n\n   Consider customizing this project request message to provide more helpful instructions specific to your organization.\n\n## Customizing the project request message {#customizing-project-request-message_configuring-project-creation}\n\nA developer or a service account that is unable to self-provision projects can make a project creation request by using the web console or CLI.\n\nThe following error message is returned by default:\n\n```terminal\nYou may not request a new project via this API.\n```\n\nCluster administrators can customize this message. Consider updating the message to provide further instructions on how to request a new project specific to your organization. The following examples show a customized message:\n\n- To request a project, contact your system administrator at `projectname@example.com`.\n- To request a new project, fill out the project request form located at `https://internal.example.com/openshift-project-request`.\n\n**Procedure**\n\n1. Edit the project configuration resource using the web console or CLI.\n\n   - By using the web console, complete the following steps:\n\n     1. Navigate to the **Administration** -> **Cluster Settings** page.\n     2. Click **Configuration** to view all configuration resources.\n     3. Find the entry for **Project** and click **Edit YAML**.\n   - By using the CLI, complete the following steps:\n\n     1. Log in as a user with `cluster-admin` privileges.\n     2. Edit the `project.config.openshift.io/cluster` resource:\n\n        ```terminal\n        $ oc edit project.config.openshift.io/cluster\n        ```\n2. Update the `spec` section to include the `projectRequestMessage` parameter and set the value to your custom message:\n\n   ```yaml {title=\"Project configuration resource with custom project request message\"}\n   apiVersion: config.openshift.io/v1\n   kind: Project\n   metadata:\n   # ...\n   spec:\n     projectRequestMessage: <message_string>\n   # ...\n   ```\n\n   The following example uses actual values:\n\n   ```yaml\n   apiVersion: config.openshift.io/v1\n   kind: Project\n   metadata:\n   # ...\n   spec:\n     projectRequestMessage: To request a project, contact your system administrator at projectname@example.com.\n   # ...\n   ```\n3. After saving your changes, attempt to create a new project by using a developer or service account that cannot self-provision projects. By doing this task, you can verify that your changes were successfully applied.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/applications/projects/configuring-project-creation.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="configuring-project-creation" class="group scroll-mt-20">Configuring project creation<a href="#configuring-project-creation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>As a cluster administrator, you can allow and configure how developers and service accounts can create, or <em>self-provision</em>, their own projects.</p>
      <p>In OpenShift Container Platform, <em>projects</em> are used to group and isolate related objects. When you request to create a new project by using the web console or <code>oc new-project</code> command, an endpoint in OpenShift Container Platform provisions the project according to a template. You can customize this template to meet your needs.</p>
      <h2 id="about-project-creation_configuring-project-creation" class="group scroll-mt-20">About project creation<a href="#about-project-creation_configuring-project-creation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">When you request a new project, the OpenShift Container Platform API server automatically provisions the new project based on the project template. The project template is identified by the <code>projectRequestTemplate</code> parameter in the project configuration resource of the cluster.</p>
      <p>If the parameter is not defined, the API server creates a default template that creates a project with the requested name. The API server then assigns the requesting user to the <code>admin</code> role for that project.</p>
      <p>When a project request is submitted, the API substitutes the following parameters into the template:</p>
      <p><strong>Default project template parameters</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Parameter</TableHead>
      <TableHead>Description</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>PROJECT_NAME</code></TableCell>
      <TableCell>The name of the project. Required.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>PROJECT_DISPLAYNAME</code></TableCell>
      <TableCell>The display name of the project. Might be empty.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>PROJECT_DESCRIPTION</code></TableCell>
      <TableCell>The description of the project. Might be empty.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>PROJECT_ADMIN_USER</code></TableCell>
      <TableCell>The user name of the administrating user.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>PROJECT_REQUESTING_USER</code></TableCell>
      <TableCell>The user name of the requesting user.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>Access to the API is granted to developers with the <code>self-provisioner</code> role and the <code>self-provisioners</code> cluster role binding. This role is available to all authenticated developers by default.</p>
      <h2 id="modifying-template-for-new-projects_configuring-project-creation" class="group scroll-mt-20">Modifying the template for new projects<a href="#modifying-template-for-new-projects_configuring-project-creation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To modify the default project template to customize the resources and settings applied when users create new projects, you can create a custom project template.</p>
      <p>As a cluster administrator, you can modify the default project template so that new projects are created using your custom requirements.</p>
      <p>To create your own custom project template:</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have access to an OpenShift Container Platform cluster using an account with <code>cluster-admin</code> permissions.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Log in as a user with <code>cluster-admin</code> privileges.</li>
      <li>
      Generate the default project template:
      <CodeBlock code={"$ oc adm create-bootstrap-project-template -o yaml > template.yaml"} lang="terminal" />
      </li>
      <li>Use a text editor to modify the generated <code>template.yaml</code> file by adding objects or modifying existing objects.</li>
      <li>
      The project template must be created in the <code>openshift-config</code> namespace. Load your modified template:
      <CodeBlock code={"$ oc create -f template.yaml -n openshift-config"} lang="terminal" />
      </li>
      <li>
      Edit the project configuration resource using the web console or CLI.
      <ul>
      <li>
      Using the web console, complete the following tasks:
      <ol>
      <li>Navigate to the <strong>Administration</strong> -&gt; <strong>Cluster Settings</strong> page.</li>
      <li>Click <strong>Configuration</strong> to view all configuration resources.</li>
      <li>Find the entry for <strong>Project</strong> and click <strong>Edit YAML</strong>.</li>
      </ol>
      </li>
      <li>
      Using the CLI, complete the following tasks:
      <ol>
      <li>
      Edit the <code>project.config.openshift.io/cluster</code> resource:
      <CodeBlock code={"$ oc edit project.config.openshift.io/cluster"} lang="terminal" />
      </li>
      </ol>
      </li>
      </ul>
      </li>
      <li>
      Update the <code>spec</code> section to include the <code>projectRequestTemplate</code> and <code>name</code> parameters. Ensure you set the name of your uploaded project template. The default name is <code>project-request</code>.
      <CodeBlock code={"apiVersion: config.openshift.io/v1\nkind: Project\nmetadata:\n# ...\nspec:\n  projectRequestTemplate:\n    name: <template_name>\n# ..."} lang="yaml" title="Project configuration resource with custom project template" />
      </li>
      <li>After you save your changes, create a new project to verify that your changes were successfully applied.</li>
      </ol>
      <h2 id="disabling-project-self-provisioning_configuring-project-creation" class="group scroll-mt-20">Disabling project self-provisioning<a href="#disabling-project-self-provisioning_configuring-project-creation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can prevent an authenticated user group from self-provisioning new projects.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Log in as a user with <code>cluster-admin</code> privileges.</li>
      <li>
      View the <code>self-provisioners</code> cluster role binding usage by running the following command:
      <CodeBlock code={"$ oc describe clusterrolebinding.rbac self-provisioners"} lang="terminal" />
      <CodeBlock code={"Name:\t\tself-provisioners\nLabels:\t\t<none>\nAnnotations:\trbac.authorization.kubernetes.io/autoupdate=true\nRole:\n  Kind:\tClusterRole\n  Name:\tself-provisioner\nSubjects:\n  Kind\tName\t\t\t\tNamespace\n  ----\t----\t\t\t\t---------\n  Group\tsystem:authenticated:oauth"} lang="terminal" title="Example output" />
      <p>Review the subjects in the <code>self-provisioners</code> section.</p>
      </li>
      <li>
      Remove the <code>self-provisioner</code> cluster role from the group <code>system:authenticated:oauth</code>.
      <ul>
      <li>
      If the <code>self-provisioners</code> cluster role binding binds only the <code>self-provisioner</code> role to the <code>system:authenticated:oauth</code> group, run the following command:
      <CodeBlock code={"$ oc patch clusterrolebinding.rbac self-provisioners -p '{\"subjects\": null}'"} lang="terminal" />
      </li>
      <li>
      If the <code>self-provisioners</code> cluster role binding binds the <code>self-provisioner</code> role to more users, groups, or service accounts than the <code>system:authenticated:oauth</code> group, run the following command:
      <CodeBlock code={"$ oc adm policy \\\n    remove-cluster-role-from-group self-provisioner \\\n    system:authenticated:oauth"} lang="terminal" />
      </li>
      </ul>
      </li>
      <li>
      Edit the <code>self-provisioners</code> cluster role binding to prevent automatic updates to the role. Automatic updates reset the cluster roles to the default state.
      <ul>
      <li>
      To update the role binding by using the CLI, complete the following steps:
      <ol>
      <li>
      To edit the <code>self-provisioners</code> cluster role binding, enter the following command:
      <CodeBlock code={"$ oc edit clusterrolebinding.rbac self-provisioners"} lang="terminal" />
      </li>
      <li>
      In the displayed role binding, set the <code>rbac.authorization.kubernetes.io/autoupdate</code> parameter value to <code>false</code>, as shown in the following example:
      <CodeBlock code={"apiVersion: authorization.openshift.io/v1\nkind: ClusterRoleBinding\nmetadata:\n  annotations:\n    rbac.authorization.kubernetes.io/autoupdate: \"false\"\n# ..."} lang="yaml" />
      </li>
      </ol>
      </li>
      <li>
      To update the role binding, run the following single command:
      <CodeBlock code={"$ oc patch clusterrolebinding.rbac self-provisioners -p '{ \"metadata\": { \"annotations\": { \"rbac.authorization.kubernetes.io/autoupdate\": \"false\" } } }'"} lang="terminal" />
      </li>
      </ul>
      </li>
      <li>
      Log in as an authenticated user and verify that the user can no longer self-provision a project:
      <CodeBlock code={"$ oc new-project test"} lang="terminal" />
      <CodeBlock code={"Error from server (Forbidden): You may not request a new project via this API."} lang="terminal" title="Example output" />
      <p>Consider customizing this project request message to provide more helpful instructions specific to your organization.</p>
      </li>
      </ol>
      <h2 id="customizing-project-request-message_configuring-project-creation" class="group scroll-mt-20">Customizing the project request message<a href="#customizing-project-request-message_configuring-project-creation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">A developer or a service account that is unable to self-provision projects can make a project creation request by using the web console or CLI.</p>
      <p>The following error message is returned by default:</p>
      <CodeBlock code={"You may not request a new project via this API."} lang="terminal" />
      <p>Cluster administrators can customize this message. Consider updating the message to provide further instructions on how to request a new project specific to your organization. The following examples show a customized message:</p>
      <ul>
      <li>To request a project, contact your system administrator at <code>projectname@example.com</code>.</li>
      <li>To request a new project, fill out the project request form located at <code>https://internal.example.com/openshift-project-request</code>.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Edit the project configuration resource using the web console or CLI.
      <ul>
      <li>
      By using the web console, complete the following steps:
      <ol>
      <li>Navigate to the <strong>Administration</strong> -&gt; <strong>Cluster Settings</strong> page.</li>
      <li>Click <strong>Configuration</strong> to view all configuration resources.</li>
      <li>Find the entry for <strong>Project</strong> and click <strong>Edit YAML</strong>.</li>
      </ol>
      </li>
      <li>
      By using the CLI, complete the following steps:
      <ol>
      <li>Log in as a user with <code>cluster-admin</code> privileges.</li>
      <li>
      Edit the <code>project.config.openshift.io/cluster</code> resource:
      <CodeBlock code={"$ oc edit project.config.openshift.io/cluster"} lang="terminal" />
      </li>
      </ol>
      </li>
      </ul>
      </li>
      <li>
      Update the <code>spec</code> section to include the <code>projectRequestMessage</code> parameter and set the value to your custom message:
      <CodeBlock code={"apiVersion: config.openshift.io/v1\nkind: Project\nmetadata:\n# ...\nspec:\n  projectRequestMessage: <message_string>\n# ..."} lang="yaml" title="Project configuration resource with custom project request message" />
      <p>The following example uses actual values:</p>
      <CodeBlock code={"apiVersion: config.openshift.io/v1\nkind: Project\nmetadata:\n# ...\nspec:\n  projectRequestMessage: To request a project, contact your system administrator at projectname@example.com.\n# ..."} lang="yaml" />
      </li>
      <li>After saving your changes, attempt to create a new project by using a developer or service account that cannot self-provision projects. By doing this task, you can verify that your changes were successfully applied.</li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
