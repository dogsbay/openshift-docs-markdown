---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: applications/projects/working-with-projects.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import BlockImage from "@ui/image/Image.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"working-with-projects","text":"Working with projects"},{"depth":2,"slug":"creating-a-project-using-the-web-console_projects","text":"Creating a project by using the web console"},{"depth":2,"slug":"creating-a-project-using-the-CLI_projects","text":"Creating a project by using the CLI"},{"depth":2,"slug":"viewing-a-project-using-the-web-console_projects","text":"Viewing a project by using the web console"},{"depth":2,"slug":"viewing-a-project-using-the-CLI_projects","text":"Viewing a project using the CLI"},{"depth":2,"slug":"odc-providing-project-permissions-using-developer-perspective_projects","text":"Providing access permissions to your project using the Developer perspective"},{"depth":2,"slug":"odc-customizing-available-cluster-roles-using-the-web-console_projects","text":"Customizing the available cluster roles using the web console"},{"depth":2,"slug":"adding-to-a-project_projects","text":"Adding to a project"},{"depth":2,"slug":"checking-project-status-using-the-web-console_projects","text":"Checking project status by using the web console"},{"depth":2,"slug":"checking-project-status-using-the-CLI_projects","text":"Checking project status by using the CLI"},{"depth":2,"slug":"deleting-a-project-using-the-web-console_projects","text":"Deleting a project by using the web console"},{"depth":2,"slug":"deleting-a-project-using-the-CLI_projects","text":"Deleting a project by using the CLI"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/applications/projects/working-with-projects";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Working with projects";
const description = "A project allows a community of users to organize and manage their content in isolation from other communities." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["applications","applications/projects"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Working with projects {#working-with-projects}\n\nA *project* allows a community of users to organize and manage their content in isolation from other communities.\n\n> [!NOTE]\n> Projects starting with `openshift-` and `kube-` are default projects. For more information, see \"Default projects\". These projects host cluster components that run as pods and other infrastructure components. As such, OpenShift Container Platform does not allow you to create projects starting with `openshift-` or `kube-` using the `oc new-project` command. Cluster administrators can create these projects using the `oc adm new-project` command.\n\n> [!IMPORTANT]\n> Do not run workloads in or share access to default projects. Default projects are reserved for running core cluster components.\n>\n> The following default projects are considered highly privileged: `default`, `kube-public`, `kube-system`, `openshift`, `openshift-infra`, `openshift-node`, and other system-created projects that have the `openshift.io/run-level` label set to `0` or `1`. Functionality that relies on admission plugins, such as pod security admission, security context constraints, cluster resource quotas, and image reference resolution, does not work in highly privileged projects.\n\nYou can complete the following tasks on either the OpenShift Container Platform web console or the OpenShift CLI (`oc`):\n\n- Create a project in your cluster.\n- View a project.\n- Check the status of a project.\n- Delete a project.\n\n> [!IMPORTANT]\n> When you delete a project, the server updates the project status to **Terminating** from **Active**. The server then clears all content from a project that is in the **Terminating** state before finally removing the project. While a project is in **Terminating** status, you cannot add new content to the project.\n\n## Creating a project by using the web console {#creating-a-project-using-the-web-console_projects}\n\nYou can use the OpenShift Container Platform web console to create a project in your cluster.\n\n> [!NOTE]\n> Projects starting with `openshift-` and `kube-` are considered critical by OpenShift Container Platform. As such, OpenShift Container Platform does not allow you to create projects starting with `openshift-` using the web console.\n\n**Prerequisites**\n\n- You have the appropriate roles and permissions to create projects, applications, and other workloads in OpenShift Container Platform.\n\n**Procedure**\n\n- If you are using the **Administrator** perspective:\n\n  1. Navigate to **Home** → **Projects**.\n  2. Click **Create Project**:\n\n     1. In the **Create Project** dialog box, enter a unique name, such as `myproject`, in the **Name** field.\n     2. Optional: Add the **Display name** and **Description** details for the project.\n     3. Click **Create**.\n\n        The dashboard for your project is displayed.\n  3. Optional: Select the **Details** tab to view the project details.\n  4. Optional: If you have adequate permissions for a project, you can use the **Project Access** tab to provide or revoke `admin`, `edit`, and `view` privileges for the project.\n- If you are using the **Developer** perspective:\n\n  1. Click the **Project** menu and select **Create Project**:\n\n     **Figure 1. Create project**\n\n     ![Web console image showing the Create project option](/openshift-docs-markdown/images/odc_create_project.png)\n\n     1. In the **Create Project** dialog box, enter a unique name, such as `myproject`, in the **Name** field.\n     2. Optional: Add the **Display name** and **Description** details for the project.\n     3. Click **Create**.\n  2. Optional: Use the left navigation panel to navigate to the **Project** view and see the dashboard for your project.\n  3. Optional: In the project dashboard, select the **Details** tab to view the project details.\n  4. Optional: If you have adequate permissions for a project, you can use the **Project Access** tab of the project dashboard to provide or revoke admin, edit, and view privileges for the project.\n\n**Additional resources**\n\n- [Customizing the available cluster roles using the web console](/openshift-docs-markdown/applications/projects/working-with-projects#odc-customizing-available-cluster-roles-using-the-web-console_projects)\n\n## Creating a project by using the CLI {#creating-a-project-using-the-CLI_projects}\n\nIf your cluster administrator has provided you with the required permissions, you can create a new project.\n\n> [!NOTE]\n> Projects starting with `openshift-` and `kube-` are considered critical by OpenShift Container Platform. OpenShift Container Platform does not allow you to create projects that start with `openshift-` or `kube-` by using the `oc new-project` command. Cluster administrators can create these projects by using the `oc adm new-project` command.\n\n**Procedure**\n\n- To create a project, enter the following command:\n\n  ```terminal\n  $ oc new-project <project_name> \\\n      --description=\"<description>\" --display-name=\"<display_name>\"\n  ```\n\n  The following example uses actual values:\n\n  ```terminal\n  $ oc new-project hello-openshift \\\n      --description=\"This is an example project\" \\\n      --display-name=\"Hello OpenShift\"\n  ```\n\n  > [!NOTE]\n  > The number of projects that you can create might be limited by the system administrator. After your limit is reached, you might have to delete an existing project to create a new one.\n\n## Viewing a project by using the web console {#viewing-a-project-using-the-web-console_projects}\n\nYou can view the projects that you have access to by using the OpenShift Container Platform web console.\n\n> [!IMPORTANT]\n> Starting with OpenShift Container Platform 4.19, the perspectives in the web console have unified. The **Developer** perspective is no longer enabled by default.\n>\n> All users can interact with all OpenShift Container Platform web console features. However, if you are not the cluster owner, you might need to request permission to access certain features from the cluster owner.\n>\n> You can still enable the **Developer** perspective. On the **Getting Started** pane in the web console, you can take a tour of the console, find information on setting up your cluster, view a quick start for enabling the **Developer** perspective, and follow links to explore new features and capabilities.\n\n**Procedure**\n\n- If you are logged in as an administrator, complete the following steps:\n\n  1. Navigate to **Home** → **Projects** in the navigation menu.\n  2. Select a project to view. The **Overview** tab includes a dashboard for your project.\n  3. Select the **Details** tab to view the project details.\n  4. Select the **YAML** tab to view and update the YAML configuration for the project resource.\n  5. Select the **Workloads** tab to see workloads in the project.\n  6. Select the **RoleBindings** tab to view and create role bindings for your project.\n- If you are logged in as a developer, complete the following steps:\n\n  1. Navigate to the **Project** page in the navigation menu.\n  2. Select **All Projects** from the **Project** drop-down menu at the top of the screen to list all of the projects in your cluster.\n  3. Select a project to view.\n  4. Select the **Details** tab to view the project details.\n  5. If you have adequate permissions for a project, select the **Project access** tab to view and update the privileges for the project.\n\n## Viewing a project using the CLI {#viewing-a-project-using-the-CLI_projects}\n\nWhen viewing projects, you are restricted to seeing only the projects you have access to view based on the authorization policy.\n\n**Procedure**\n\n1. To view a list of projects, enter the following command:\n\n   ```terminal\n   $ oc get projects\n   ```\n2. To change from the current project to a different project for CLI operations, enter the following command. The specified project is then used in all subsequent operations that manipulate project-scoped content.\n\n   ```terminal\n   $ oc project <project_name>\n   ```\n\n## Providing access permissions to your project using the Developer perspective {#odc-providing-project-permissions-using-developer-perspective_projects}\n\nYou can use the **Project** view in the **Developer** perspective to grant or revoke access permissions to your project. You can add users to your project and provide them with **Admin**, **Edit**, or **View** access.\n\n**Prerequisites**\n\n- You have created a project.\n\n**Procedure**\n\n1. In the **Developer** perspective, navigate to the **Project** page.\n2. Select your project from the **Project** menu.\n3. Select the **Project Access** tab.\n4. Click **Add access** to add a new row of permissions to the default ones.\n\n   **Figure 2. Project permissions**\n\n   ![odc_project_permissions](/openshift-docs-markdown/images/odc_project_permissions.png)\n5. Enter the user name, click the **Select a role** drop-down list, and select an appropriate role.\n6. Click **Save** to add the new permissions.\n7. Optional: You can complete any of the following additional tasks:\n\n   - The **Select a role** drop-down list, to modify the access permissions of an existing user.\n   - The **Remove Access** icon, to completely remove the access permissions of an existing user to the project.\n\n     > [!NOTE]\n     > Advanced role-based access control is managed in the **Roles** and **Roles Binding** views in the **Administrator** perspective.\n\n## Customizing the available cluster roles using the web console {#odc-customizing-available-cluster-roles-using-the-web-console_projects}\n\nIn the **Developer** perspective of the web console, the **Project** → **Project access** page enables a project administrator to grant roles to users in a project. By default, the available cluster roles that can be granted to users in a project are `admin`, `edit`, and `view`.\n\nAs a cluster administrator, you can define which cluster roles are available in the **Project access** page for all projects cluster-wide. You can specify the available roles by customizing the `spec.customization.projectAccess.availableClusterRoles` object in the `Console` configuration resource.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n\n**Procedure**\n\n1. In the **Administrator** perspective, navigate to **Administration** → **Cluster settings**.\n2. Click the **Configuration** tab.\n3. From the **Configuration resource** list, select **Console `operator.openshift.io`**.\n4. Navigate to the **YAML** tab to view and edit the YAML code.\n5. In the YAML code under `spec`, customize the list of available cluster roles for project access. The following example specifies the default `admin`, `edit`, and `view` roles:\n\n   ```yaml\n   apiVersion: operator.openshift.io/v1\n   kind: Console\n   metadata:\n     name: cluster\n   # ...\n   spec:\n     customization:\n       projectAccess:\n         availableClusterRoles:\n         - admin\n         - edit\n         - view\n   ```\n6. Click **Save** to save the changes to the `Console` configuration resource.\n\n**Verification**\n\n1. In the **Developer** perspective, navigate to the **Project** page.\n2. Select a project from the **Project** menu.\n3. Select the **Project access** tab.\n4. Click the menu in the **Role** column and verify that the available roles match the configuration that you applied to the `Console` resource configuration.\n\n## Adding to a project {#adding-to-a-project_projects}\n\nYou can add items to your project by using the **+Add** page.\n\n**Prerequisites**\n\n- You have created a project.\n\n**Procedure**\n\n1. Navigate to the **+Add** page.\n2. Select your project from the **Project** menu.\n3. Click an item on the **+Add** page and then follow the workflow.\n\n   > [!NOTE]\n   > You can also use the search feature in the **+Add** page to find additional items to add to your project. Click **+** under **Add** at the top of the page and type the name of a component in the search field.\n\n## Checking project status by using the web console {#checking-project-status-using-the-web-console_projects}\n\nYou can review the status of your project by using the web console.\n\n**Prerequisites**\n\n- You have created a project.\n\n**Procedure**\n\n1. Navigate to **Home** → **Projects**.\n2. Select a project from the list.\n3. Review the project status in the **Overview** page.\n\n## Checking project status by using the CLI {#checking-project-status-using-the-CLI_projects}\n\nYou can review the status of your project by using the OpenShift CLI (`oc`).\n\n**Prerequisites**\n\n- You have installed the OpenShift CLI (`oc`).\n- You have created a project.\n\n**Procedure**\n\n1. Switch to your project:\n\n   ```terminal\n   $ oc project <project_name>\n   ```\n\n   - Replace `<project_name>` with the name of your project.\n2. Obtain a high-level overview of the project:\n\n   ```terminal\n   $ oc status\n   ```\n\n## Deleting a project by using the web console {#deleting-a-project-using-the-web-console_projects}\n\nYou can delete a project by using the web console.\n\n**Prerequisites**\n\n- You have created a project.\n- You have the required permissions to delete the project.\n\n**Procedure**\n\n- If you are using the **Administrator** perspective, complete the following steps:\n\n  1. Navigate to **Home** → **Projects**.\n  2. Select a project from the list.\n  3. Click the **Actions** drop-down menu for the project and select **Delete Project**.\n\n     > [!NOTE]\n     > The **Delete Project** option is not available if you do not have the required permissions to delete the project.\n  4. In the **Delete Project?** pane, confirm the deletion by entering the name of your project.\n  5. Click **Delete**.\n- If you are using the **Developer** perspective, complete the following steps:\n\n  1. Navigate to the **Project** page.\n  2. Select the project that you want to delete from the **Project** menu.\n  3. Click the **Actions** drop-down menu for the project and select **Delete Project**.\n\n     > [!NOTE]\n     > If you do not have the required permissions to delete the project, the **Delete Project** option is not available.\n  4. In the **Delete Project?** pane, confirm the deletion by entering the name of your project.\n  5. Click **Delete**.\n\n## Deleting a project by using the CLI {#deleting-a-project-using-the-CLI_projects}\n\nYou can delete a project by using the OpenShift CLI (`oc`).\n\n**Prerequisites**\n\n- You have installed the OpenShift CLI (`oc`).\n- You have created a project.\n- You have the required permissions to delete the project.\n\n**Procedure**\n\n- Delete your project by entering the following command:\n\n  ```terminal\n  $ oc delete project <project_name>\n  ```\n\n  - Replace `<project_name>` with the name of the project that you want to delete.\n\n**Additional resources**\n{._additional-resources}\n\n- [Default projects](/openshift-docs-markdown/authentication/using-rbac#rbac-default-projects_using-rbac)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/applications/projects/working-with-projects.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="working-with-projects" class="group scroll-mt-20">Working with projects<a href="#working-with-projects" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">A <em>project</em> allows a community of users to organize and manage their content in isolation from other communities.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Projects starting with <code>openshift-</code> and <code>kube-</code> are default projects. For more information, see "Default projects". These projects host cluster components that run as pods and other infrastructure components. As such, OpenShift Container Platform does not allow you to create projects starting with <code>openshift-</code> or <code>kube-</code> using the <code>oc new-project</code> command. Cluster administrators can create these projects using the <code>oc adm new-project</code> command.</p>
        </AlertDescription>
      </Alert>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Do not run workloads in or share access to default projects. Default projects are reserved for running core cluster components.</p>
          <p>The following default projects are considered highly privileged: <code>default</code>, <code>kube-public</code>, <code>kube-system</code>, <code>openshift</code>, <code>openshift-infra</code>, <code>openshift-node</code>, and other system-created projects that have the <code>openshift.io/run-level</code> label set to <code>0</code> or <code>1</code>. Functionality that relies on admission plugins, such as pod security admission, security context constraints, cluster resource quotas, and image reference resolution, does not work in highly privileged projects.</p>
        </AlertDescription>
      </Alert>
      <p>You can complete the following tasks on either the OpenShift Container Platform web console or the OpenShift CLI (<code>oc</code>):</p>
      <ul>
      <li>Create a project in your cluster.</li>
      <li>View a project.</li>
      <li>Check the status of a project.</li>
      <li>Delete a project.</li>
      </ul>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>When you delete a project, the server updates the project status to <strong>Terminating</strong> from <strong>Active</strong>. The server then clears all content from a project that is in the <strong>Terminating</strong> state before finally removing the project. While a project is in <strong>Terminating</strong> status, you cannot add new content to the project.</p>
        </AlertDescription>
      </Alert>
      <h2 id="creating-a-project-using-the-web-console_projects" class="group scroll-mt-20">Creating a project by using the web console<a href="#creating-a-project-using-the-web-console_projects" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can use the OpenShift Container Platform web console to create a project in your cluster.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Projects starting with <code>openshift-</code> and <code>kube-</code> are considered critical by OpenShift Container Platform. As such, OpenShift Container Platform does not allow you to create projects starting with <code>openshift-</code> using the web console.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have the appropriate roles and permissions to create projects, applications, and other workloads in OpenShift Container Platform.</li></ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      If you are using the <strong>Administrator</strong> perspective:
      <ol>
      <li>Navigate to <strong>Home</strong> → <strong>Projects</strong>.</li>
      <li>
      Click <strong>Create Project</strong>:
      <ol>
      <li>In the <strong>Create Project</strong> dialog box, enter a unique name, such as <code>myproject</code>, in the <strong>Name</strong> field.</li>
      <li>Optional: Add the <strong>Display name</strong> and <strong>Description</strong> details for the project.</li>
      <li>
      Click <strong>Create</strong>.
      <p>The dashboard for your project is displayed.</p>
      </li>
      </ol>
      </li>
      <li>Optional: Select the <strong>Details</strong> tab to view the project details.</li>
      <li>Optional: If you have adequate permissions for a project, you can use the <strong>Project Access</strong> tab to provide or revoke <code>admin</code>, <code>edit</code>, and <code>view</code> privileges for the project.</li>
      </ol>
      </li>
      <li>
      If you are using the <strong>Developer</strong> perspective:
      <ol>
      <li>
      Click the <strong>Project</strong> menu and select <strong>Create Project</strong>:
      <p><strong>Figure 1. Create project</strong></p>
      <BlockImage src="/openshift-docs-markdown/images/odc_create_project.png" alt="Web console image showing the Create project option" />
      <ol>
      <li>In the <strong>Create Project</strong> dialog box, enter a unique name, such as <code>myproject</code>, in the <strong>Name</strong> field.</li>
      <li>Optional: Add the <strong>Display name</strong> and <strong>Description</strong> details for the project.</li>
      <li>Click <strong>Create</strong>.</li>
      </ol>
      </li>
      <li>Optional: Use the left navigation panel to navigate to the <strong>Project</strong> view and see the dashboard for your project.</li>
      <li>Optional: In the project dashboard, select the <strong>Details</strong> tab to view the project details.</li>
      <li>Optional: If you have adequate permissions for a project, you can use the <strong>Project Access</strong> tab of the project dashboard to provide or revoke admin, edit, and view privileges for the project.</li>
      </ol>
      </li>
      </ul>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/applications/projects/working-with-projects#odc-customizing-available-cluster-roles-using-the-web-console_projects">Customizing the available cluster roles using the web console</a></li></ul>
      <h2 id="creating-a-project-using-the-CLI_projects" class="group scroll-mt-20">Creating a project by using the CLI<a href="#creating-a-project-using-the-CLI_projects" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">If your cluster administrator has provided you with the required permissions, you can create a new project.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Projects starting with <code>openshift-</code> and <code>kube-</code> are considered critical by OpenShift Container Platform. OpenShift Container Platform does not allow you to create projects that start with <code>openshift-</code> or <code>kube-</code> by using the <code>oc new-project</code> command. Cluster administrators can create these projects by using the <code>oc adm new-project</code> command.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      To create a project, enter the following command:
      <CodeBlock code={"$ oc new-project <project_name> \\\n    --description=\"<description>\" --display-name=\"<display_name>\""} lang="terminal" />
      <p>The following example uses actual values:</p>
      <CodeBlock code={"$ oc new-project hello-openshift \\\n    --description=\"This is an example project\" \\\n    --display-name=\"Hello OpenShift\""} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The number of projects that you can create might be limited by the system administrator. After your limit is reached, you might have to delete an existing project to create a new one.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      <h2 id="viewing-a-project-using-the-web-console_projects" class="group scroll-mt-20">Viewing a project by using the web console<a href="#viewing-a-project-using-the-web-console_projects" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can view the projects that you have access to by using the OpenShift Container Platform web console.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Starting with OpenShift Container Platform 4.19, the perspectives in the web console have unified. The <strong>Developer</strong> perspective is no longer enabled by default.</p>
          <p>All users can interact with all OpenShift Container Platform web console features. However, if you are not the cluster owner, you might need to request permission to access certain features from the cluster owner.</p>
          <p>You can still enable the <strong>Developer</strong> perspective. On the <strong>Getting Started</strong> pane in the web console, you can take a tour of the console, find information on setting up your cluster, view a quick start for enabling the <strong>Developer</strong> perspective, and follow links to explore new features and capabilities.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      If you are logged in as an administrator, complete the following steps:
      <ol>
      <li>Navigate to <strong>Home</strong> → <strong>Projects</strong> in the navigation menu.</li>
      <li>Select a project to view. The <strong>Overview</strong> tab includes a dashboard for your project.</li>
      <li>Select the <strong>Details</strong> tab to view the project details.</li>
      <li>Select the <strong>YAML</strong> tab to view and update the YAML configuration for the project resource.</li>
      <li>Select the <strong>Workloads</strong> tab to see workloads in the project.</li>
      <li>Select the <strong>RoleBindings</strong> tab to view and create role bindings for your project.</li>
      </ol>
      </li>
      <li>
      If you are logged in as a developer, complete the following steps:
      <ol>
      <li>Navigate to the <strong>Project</strong> page in the navigation menu.</li>
      <li>Select <strong>All Projects</strong> from the <strong>Project</strong> drop-down menu at the top of the screen to list all of the projects in your cluster.</li>
      <li>Select a project to view.</li>
      <li>Select the <strong>Details</strong> tab to view the project details.</li>
      <li>If you have adequate permissions for a project, select the <strong>Project access</strong> tab to view and update the privileges for the project.</li>
      </ol>
      </li>
      </ul>
      <h2 id="viewing-a-project-using-the-CLI_projects" class="group scroll-mt-20">Viewing a project using the CLI<a href="#viewing-a-project-using-the-CLI_projects" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">When viewing projects, you are restricted to seeing only the projects you have access to view based on the authorization policy.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      To view a list of projects, enter the following command:
      <CodeBlock code={"$ oc get projects"} lang="terminal" />
      </li>
      <li>
      To change from the current project to a different project for CLI operations, enter the following command. The specified project is then used in all subsequent operations that manipulate project-scoped content.
      <CodeBlock code={"$ oc project <project_name>"} lang="terminal" />
      </li>
      </ol>
      <h2 id="odc-providing-project-permissions-using-developer-perspective_projects" class="group scroll-mt-20">Providing access permissions to your project using the Developer perspective<a href="#odc-providing-project-permissions-using-developer-perspective_projects" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can use the <strong>Project</strong> view in the <strong>Developer</strong> perspective to grant or revoke access permissions to your project. You can add users to your project and provide them with <strong>Admin</strong>, <strong>Edit</strong>, or <strong>View</strong> access.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have created a project.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the <strong>Developer</strong> perspective, navigate to the <strong>Project</strong> page.</li>
      <li>Select your project from the <strong>Project</strong> menu.</li>
      <li>Select the <strong>Project Access</strong> tab.</li>
      <li>
      Click <strong>Add access</strong> to add a new row of permissions to the default ones.
      <p><strong>Figure 2. Project permissions</strong></p>
      <BlockImage src="/openshift-docs-markdown/images/odc_project_permissions.png" alt="odc_project_permissions" />
      </li>
      <li>Enter the user name, click the <strong>Select a role</strong> drop-down list, and select an appropriate role.</li>
      <li>Click <strong>Save</strong> to add the new permissions.</li>
      <li>
      Optional: You can complete any of the following additional tasks:
      <ul>
      <li>The <strong>Select a role</strong> drop-down list, to modify the access permissions of an existing user.</li>
      <li>
      The <strong>Remove Access</strong> icon, to completely remove the access permissions of an existing user to the project.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Advanced role-based access control is managed in the <strong>Roles</strong> and <strong>Roles Binding</strong> views in the <strong>Administrator</strong> perspective.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      </li>
      </ol>
      <h2 id="odc-customizing-available-cluster-roles-using-the-web-console_projects" class="group scroll-mt-20">Customizing the available cluster roles using the web console<a href="#odc-customizing-available-cluster-roles-using-the-web-console_projects" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">In the <strong>Developer</strong> perspective of the web console, the <strong>Project</strong> → <strong>Project access</strong> page enables a project administrator to grant roles to users in a project. By default, the available cluster roles that can be granted to users in a project are <code>admin</code>, <code>edit</code>, and <code>view</code>.</p>
      <p>As a cluster administrator, you can define which cluster roles are available in the <strong>Project access</strong> page for all projects cluster-wide. You can specify the available roles by customizing the <code>spec.customization.projectAccess.availableClusterRoles</code> object in the <code>Console</code> configuration resource.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the <strong>Administrator</strong> perspective, navigate to <strong>Administration</strong> → <strong>Cluster settings</strong>.</li>
      <li>Click the <strong>Configuration</strong> tab.</li>
      <li>From the <strong>Configuration resource</strong> list, select <strong>Console </strong><code>operator.openshift.io</code>.</li>
      <li>Navigate to the <strong>YAML</strong> tab to view and edit the YAML code.</li>
      <li>
      In the YAML code under <code>spec</code>, customize the list of available cluster roles for project access. The following example specifies the default <code>admin</code>, <code>edit</code>, and <code>view</code> roles:
      <CodeBlock code={"apiVersion: operator.openshift.io/v1\nkind: Console\nmetadata:\n  name: cluster\n# ...\nspec:\n  customization:\n    projectAccess:\n      availableClusterRoles:\n      - admin\n      - edit\n      - view"} lang="yaml" />
      </li>
      <li>Click <strong>Save</strong> to save the changes to the <code>Console</code> configuration resource.</li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>In the <strong>Developer</strong> perspective, navigate to the <strong>Project</strong> page.</li>
      <li>Select a project from the <strong>Project</strong> menu.</li>
      <li>Select the <strong>Project access</strong> tab.</li>
      <li>Click the menu in the <strong>Role</strong> column and verify that the available roles match the configuration that you applied to the <code>Console</code> resource configuration.</li>
      </ol>
      <h2 id="adding-to-a-project_projects" class="group scroll-mt-20">Adding to a project<a href="#adding-to-a-project_projects" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can add items to your project by using the <strong>+Add</strong> page.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have created a project.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to the <strong>+Add</strong> page.</li>
      <li>Select your project from the <strong>Project</strong> menu.</li>
      <li>
      Click an item on the <strong>+Add</strong> page and then follow the workflow.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>You can also use the search feature in the <strong>+Add</strong> page to find additional items to add to your project. Click <strong>+</strong> under <strong>Add</strong> at the top of the page and type the name of a component in the search field.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <h2 id="checking-project-status-using-the-web-console_projects" class="group scroll-mt-20">Checking project status by using the web console<a href="#checking-project-status-using-the-web-console_projects" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can review the status of your project by using the web console.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have created a project.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to <strong>Home</strong> → <strong>Projects</strong>.</li>
      <li>Select a project from the list.</li>
      <li>Review the project status in the <strong>Overview</strong> page.</li>
      </ol>
      <h2 id="checking-project-status-using-the-CLI_projects" class="group scroll-mt-20">Checking project status by using the CLI<a href="#checking-project-status-using-the-CLI_projects" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can review the status of your project by using the OpenShift CLI (<code>oc</code>).</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      <li>You have created a project.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Switch to your project:
      <CodeBlock code={"$ oc project <project_name>"} lang="terminal" />
      <ul><li>Replace <code>&lt;project_name&gt;</code> with the name of your project.</li></ul>
      </li>
      <li>
      Obtain a high-level overview of the project:
      <CodeBlock code={"$ oc status"} lang="terminal" />
      </li>
      </ol>
      <h2 id="deleting-a-project-using-the-web-console_projects" class="group scroll-mt-20">Deleting a project by using the web console<a href="#deleting-a-project-using-the-web-console_projects" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can delete a project by using the web console.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have created a project.</li>
      <li>You have the required permissions to delete the project.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      If you are using the <strong>Administrator</strong> perspective, complete the following steps:
      <ol>
      <li>Navigate to <strong>Home</strong> → <strong>Projects</strong>.</li>
      <li>Select a project from the list.</li>
      <li>
      Click the <strong>Actions</strong> drop-down menu for the project and select <strong>Delete Project</strong>.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The <strong>Delete Project</strong> option is not available if you do not have the required permissions to delete the project.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>In the <strong>Delete Project?</strong> pane, confirm the deletion by entering the name of your project.</li>
      <li>Click <strong>Delete</strong>.</li>
      </ol>
      </li>
      <li>
      If you are using the <strong>Developer</strong> perspective, complete the following steps:
      <ol>
      <li>Navigate to the <strong>Project</strong> page.</li>
      <li>Select the project that you want to delete from the <strong>Project</strong> menu.</li>
      <li>
      Click the <strong>Actions</strong> drop-down menu for the project and select <strong>Delete Project</strong>.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If you do not have the required permissions to delete the project, the <strong>Delete Project</strong> option is not available.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>In the <strong>Delete Project?</strong> pane, confirm the deletion by entering the name of your project.</li>
      <li>Click <strong>Delete</strong>.</li>
      </ol>
      </li>
      </ul>
      <h2 id="deleting-a-project-using-the-CLI_projects" class="group scroll-mt-20">Deleting a project by using the CLI<a href="#deleting-a-project-using-the-CLI_projects" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can delete a project by using the OpenShift CLI (<code>oc</code>).</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      <li>You have created a project.</li>
      <li>You have the required permissions to delete the project.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Delete your project by entering the following command:
      <CodeBlock code={"$ oc delete project <project_name>"} lang="terminal" />
      <ul><li>Replace <code>&lt;project_name&gt;</code> with the name of the project that you want to delete.</li></ul>
      </li>
      </ul>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="/openshift-docs-markdown/authentication/using-rbac#rbac-default-projects_using-rbac">Default projects</a></li></ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
