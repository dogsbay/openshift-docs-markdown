---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: hosted_control_planes/hcp-disconnected/disconnected-install-ibmz-hcp.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;

const headings = [{"depth":1,"slug":"disconnected-install-ibmz-hcp","text":"Deploying {{ hcp }} on {{ ibm_z_title }} in a disconnected environment"}];
const nav = navData;
const currentPath = "/hosted_control_planes/hcp-disconnected/disconnected-install-ibmz-hcp";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Deploying {{ hcp }} on {{ ibm_z_title }} in a disconnected environment";
const description = "{{ hcp_capital }} deployments in disconnected environments function differently than in a standalone OpenShift Container Platform." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["hosted_control_planes","hosted_control_planes/hcp-disconnected"];
const pageTaxonomies = null;
const tagsIndexPath = "/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Deploying {{ hcp }} on {{ ibm_z_title }} in a disconnected environment {#disconnected-install-ibmz-hcp}\n\n{{ hcp_capital }} deployments in disconnected environments function differently than in a standalone OpenShift Container Platform.\n\n{{ hcp_capital }} involves two distinct environments:\n\n- Control plane: Located in the management cluster, where the {{ hcp }} pods are run and managed by the Control Plane Operator.\n- Data plane: Located in the workers of the hosted cluster, where the workload and a few other pods run, managed by the Hosted Cluster Config Operator.\n\nThe `ImageContentSourcePolicy` (ICSP) custom resource for the data plane is managed through the `ImageContentSources` API in the hosted cluster manifest.\n\nFor the control plane, ICSP objects are managed in the management cluster. These objects are parsed by the HyperShift Operator and are shared as `registry-overrides` entries with the Control Plane Operator. These entries are injected into any one of the available  deployments in the {{ hcp }} namespace as an argument.\n\nTo work with disconnected registries in the {{ hcp }}, you must first create the appropriate ICSP in the management cluster. Then, to deploy disconnected workloads in the data plane, you need to add the entries that you want into the `ImageContentSources` field in the hosted cluster manifest.\n\n**Additional resources**\n\n- [Mirror registry for Red Hat OpenShift introduction](/disconnected/installing-mirroring-creating-registry#mirror-registry-introduction_installing-mirroring-creating-registry)\n- [Mirroring images for a disconnected installation by using the oc-mirror plugin v2](/disconnected/about-installing-oc-mirror-v2#about-installing-oc-mirror-v2)\n","mdUrl":"/hosted_control_planes/hcp-disconnected/disconnected-install-ibmz-hcp.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={true}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={""}
  navMode={"client"}
  pagefindUrl={"/pagefind/"}
  favicon={"/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <p class="/_attributes/common-attributes.md"></p>
      <h1 id="disconnected-install-ibmz-hcp" class="group scroll-mt-20">Deploying {"{"}{"{"} hcp {"}"}{"}"} on {"{"}{"{"} ibm_z_title {"}"}{"}"} in a disconnected environment<a href="#disconnected-install-ibmz-hcp" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>{"{"}{"{"} hcp_capital {"}"}{"}"} deployments in disconnected environments function differently than in a standalone OpenShift Container Platform.</p>
      <p>{"{"}{"{"} hcp_capital {"}"}{"}"} involves two distinct environments:</p>
      <ul>
      <li>Control plane: Located in the management cluster, where the {"{"}{"{"} hcp {"}"}{"}"} pods are run and managed by the Control Plane Operator.</li>
      <li>Data plane: Located in the workers of the hosted cluster, where the workload and a few other pods run, managed by the Hosted Cluster Config Operator.</li>
      </ul>
      <p>The <code>ImageContentSourcePolicy</code> (ICSP) custom resource for the data plane is managed through the <code>ImageContentSources</code> API in the hosted cluster manifest.</p>
      <p>For the control plane, ICSP objects are managed in the management cluster. These objects are parsed by the HyperShift Operator and are shared as <code>registry-overrides</code> entries with the Control Plane Operator. These entries are injected into any one of the available  deployments in the {"{"}{"{"} hcp {"}"}{"}"} namespace as an argument.</p>
      <p>To work with disconnected registries in the {"{"}{"{"} hcp {"}"}{"}"}, you must first create the appropriate ICSP in the management cluster. Then, to deploy disconnected workloads in the data plane, you need to add the entries that you want into the <code>ImageContentSources</code> field in the hosted cluster manifest.</p>
      <p class="/modules/hcp-ibm-z-dc-prereqs.md"></p>
      <p><strong>Additional resources</strong></p>
      <ul>
      <li><a href="/disconnected/installing-mirroring-creating-registry#mirror-registry-introduction_installing-mirroring-creating-registry">Mirror registry for Red Hat OpenShift introduction</a></li>
      <li><a href="/disconnected/about-installing-oc-mirror-v2#about-installing-oc-mirror-v2">Mirroring images for a disconnected installation by using the oc-mirror plugin v2</a></li>
      </ul>
      <p class="/modules/hcp-ibm-z-update-reg-ca.md"></p>
      <p class="/modules/hcp-ibm-z-adding-reg-ca-hostedcluster.md"></p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
