---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/networking_operators/external_dns_operator/nw-creating-dns-records-on-infoblox.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"creating-dns-records-on-infoblox","text":"Creating DNS records on Infoblox"},{"depth":2,"slug":"nw-control-dns-records-public-dns-zone-infoblox_creating-dns-records-on-infoblox","text":"Creating DNS records on a public DNS zone on Infoblox"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/networking_operators/external_dns_operator/nw-creating-dns-records-on-infoblox";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Creating DNS records on Infoblox";
const description = "To create DNS records on Infoblox, use the External DNS Operator. The Operator manages external name resolution for your cluster services." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/networking_operators","networking/networking_operators/external_dns_operator"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Creating DNS records on Infoblox {#creating-dns-records-on-infoblox}\n\nTo create DNS records on Infoblox, use the External DNS Operator. The Operator manages external name resolution for your cluster services.\n\n## Creating DNS records on a public DNS zone on Infoblox {#nw-control-dns-records-public-dns-zone-infoblox_creating-dns-records-on-infoblox}\n\nTo create DNS records on Infoblox, use the External DNS Operator. The Operator manages external name resolution for your cluster services.\n\n**Prerequisites**\n\n- You have access to the OpenShift CLI (`oc`).\n- You have access to the Infoblox UI.\n\n**Procedure**\n\n1. Create a `secret` object with Infoblox credentials by running the following command:\n\n   ```terminal\n   $ oc -n external-dns-operator create secret generic infoblox-credentials --from-literal=EXTERNAL_DNS_INFOBLOX_WAPI_USERNAME=<infoblox_username> --from-literal=EXTERNAL_DNS_INFOBLOX_WAPI_PASSWORD=<infoblox_password>\n   ```\n2. Get a list of routes by running the following command:\n\n   ```terminal\n   $ oc get routes --all-namespaces | grep console\n   ```\n\n   ```terminal {title=\"Example output\"}\n   openshift-console          console             console-openshift-console.apps.test.example.com                       console             https   reencrypt/Redirect     None\n   openshift-console          downloads           downloads-openshift-console.apps.test.example.com                     downloads           http    edge/Redirect          None\n   ```\n3. Create a YAML file, for example, `external-dns-sample-infoblox.yaml`, that defines the `ExternalDNS` object:\n\n   ```yaml {title=\"Example external-dns-sample-infoblox.yaml file\"}\n   apiVersion: externaldns.olm.openshift.io/v1beta1\n   kind: ExternalDNS\n   metadata:\n     name: sample-infoblox\n   spec:\n     provider:\n       type: Infoblox\n       infoblox:\n         credentials:\n           name: infoblox-credentials\n         gridHost: ${INFOBLOX_GRID_PUBLIC_IP}\n         wapiPort: 443\n         wapiVersion: \"2.3.1\"\n     domains:\n     - filterType: Include\n       matchType: Exact\n       name: test.example.com\n     source:\n       type: OpenShiftRoute\n       openshiftRouteOptions:\n         routerName: default\n   ```\n\n   where:\n\n   `metadata.name`\n   :   Specifies the External DNS name.\n\n`provider.type`\n:   Specifies the provider type.\n\n`source.type`\n:   Specifies options for the source of DNS records.\n\n`routerName`\n:   If the source type is `OpenShiftRoute`, you can pass the OpenShift Ingress Controller name. External DNS selects the canonical hostname of that router as the target while creating a CNAME record.\n\n1. Create the `ExternalDNS` resource on Infoblox by running the following command:\n\n   ```terminal\n   $ oc create -f external-dns-sample-infoblox.yaml\n   ```\n2. From the Infoblox UI, check the DNS records created for `console` routes:\n\n   1. Click **Data Management** -> **DNS** -> **Zones**.\n   2. Select the zone name.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/networking_operators/external_dns_operator/nw-creating-dns-records-on-infoblox.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="creating-dns-records-on-infoblox" class="group scroll-mt-20">Creating DNS records on Infoblox<a href="#creating-dns-records-on-infoblox" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>To create DNS records on Infoblox, use the External DNS Operator. The Operator manages external name resolution for your cluster services.</p>
      <h2 id="nw-control-dns-records-public-dns-zone-infoblox_creating-dns-records-on-infoblox" class="group scroll-mt-20">Creating DNS records on a public DNS zone on Infoblox<a href="#nw-control-dns-records-public-dns-zone-infoblox_creating-dns-records-on-infoblox" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To create DNS records on Infoblox, use the External DNS Operator. The Operator manages external name resolution for your cluster services.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the OpenShift CLI (<code>oc</code>).</li>
      <li>You have access to the Infoblox UI.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a <code>secret</code> object with Infoblox credentials by running the following command:
      <CodeBlock code={"$ oc -n external-dns-operator create secret generic infoblox-credentials --from-literal=EXTERNAL_DNS_INFOBLOX_WAPI_USERNAME=<infoblox_username> --from-literal=EXTERNAL_DNS_INFOBLOX_WAPI_PASSWORD=<infoblox_password>"} lang="terminal" />
      </li>
      <li>
      Get a list of routes by running the following command:
      <CodeBlock code={"$ oc get routes --all-namespaces | grep console"} lang="terminal" />
      <CodeBlock code={"openshift-console          console             console-openshift-console.apps.test.example.com                       console             https   reencrypt/Redirect     None\nopenshift-console          downloads           downloads-openshift-console.apps.test.example.com                     downloads           http    edge/Redirect          None"} lang="terminal" title="Example output" />
      </li>
      <li>
      Create a YAML file, for example, <code>external-dns-sample-infoblox.yaml</code>, that defines the <code>ExternalDNS</code> object:
      <CodeBlock code={"apiVersion: externaldns.olm.openshift.io/v1beta1\nkind: ExternalDNS\nmetadata:\n  name: sample-infoblox\nspec:\n  provider:\n    type: Infoblox\n    infoblox:\n      credentials:\n        name: infoblox-credentials\n      gridHost: ${INFOBLOX_GRID_PUBLIC_IP}\n      wapiPort: 443\n      wapiVersion: \"2.3.1\"\n  domains:\n  - filterType: Include\n    matchType: Exact\n    name: test.example.com\n  source:\n    type: OpenShiftRoute\n    openshiftRouteOptions:\n      routerName: default"} lang="yaml" title="Example external-dns-sample-infoblox.yaml file" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>metadata.name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the External DNS name.</p></dd>
      </dl>
      </li>
      </ol>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>provider.type</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the provider type.</p></dd>
      <dt class="font-semibold"><code>source.type</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies options for the source of DNS records.</p></dd>
      <dt class="font-semibold"><code>routerName</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>If the source type is <code>OpenShiftRoute</code>, you can pass the OpenShift Ingress Controller name. External DNS selects the canonical hostname of that router as the target while creating a CNAME record.</p></dd>
      </dl>
      <ol>
      <li>
      Create the <code>ExternalDNS</code> resource on Infoblox by running the following command:
      <CodeBlock code={"$ oc create -f external-dns-sample-infoblox.yaml"} lang="terminal" />
      </li>
      <li>
      From the Infoblox UI, check the DNS records created for <code>console</code> routes:
      <ol>
      <li>Click <strong>Data Management</strong> -&gt; <strong>DNS</strong> -&gt; <strong>Zones</strong>.</li>
      <li>Select the zone name.</li>
      </ol>
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
