---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/networking_operators/external_dns_operator/understanding-external-dns-operator.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"external-dns-operator","text":"Understanding the External DNS Operator"},{"depth":2,"slug":"nw-external-dns-operator-domain-limitations_external-dns-operator","text":"External DNS Operator domain name limitations"},{"depth":2,"slug":"nw-external-dns-operator_external-dns-operator","text":"Deploying the External DNS Operator"},{"depth":2,"slug":"nw-external-dns-operator-logs_external-dns-operator","text":"Viewing External DNS Operator logs"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/networking_operators/external_dns_operator/understanding-external-dns-operator";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Understanding the External DNS Operator";
const description = "The External DNS Operator deploys and manages ExternalDNS to provide the name resolution for services and routes from the external DNS provider to…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/networking_operators","networking/networking_operators/external_dns_operator"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Understanding the External DNS Operator {#external-dns-operator}\n\nThe External DNS Operator deploys and manages `ExternalDNS` to provide the name resolution for services and routes from the external DNS provider to OpenShift Container Platform.\n\n## External DNS Operator domain name limitations {#nw-external-dns-operator-domain-limitations_external-dns-operator}\n\nTo prevent configuration errors when deploying the `ExternalDNS` resource, review the domain name limitations enforced by the External DNS Operator. Understanding these constraints ensures that your requested hostnames and domains are compatible with your underlying DNS provider.\n\nThe External DNS Operator uses the TXT registry that adds the prefix for TXT records. This reduces the maximum length of the domain name for TXT records. A DNS record cannot be present without a corresponding TXT record, so the domain name of the DNS record must follow the same limit as the TXT records. For example, a DNS record of `<domain_name_from_source>` results in a TXT record of `external-dns-<record_type>-<domain_name_from_source>`.\n\nThe domain name of the DNS records generated by the External DNS Operator has the following limitations:\n\n| Record type | Number of characters |\n| --- | --- |\n| CNAME | 44 |\n| Wildcard CNAME records on AzureDNS | 42 |\n| A | 48 |\n| Wildcard A records on AzureDNS | 46 |\n\nThe following error shows in the External DNS Operator logs if the generated domain name exceeds any of the domain name limitations:\n\n```terminal\ntime=\"2022-09-02T08:53:57Z\" level=error msg=\"Failure in zone test.example.io. [Id: /hostedzone/Z06988883Q0H0RL6UMXXX]\"\ntime=\"2022-09-02T08:53:57Z\" level=error msg=\"InvalidChangeBatch: [FATAL problem: DomainLabelTooLong (Domain label is too long) encountered with 'external-dns-a-hello-openshift-aaaaaaaaaa-bbbbbbbbbb-ccccccc']\\n\\tstatus code: 400, request id: e54dfd5a-06c6-47b0-bcb9-a4f7c3a4e0c6\"\n```\n\n## Deploying the External DNS Operator {#nw-external-dns-operator_external-dns-operator}\n\nThe External DNS Operator implements the External DNS API from the `olm.openshift.io` API group. The External DNS Operator updates services, routes, and external DNS providers.\n\n**Prerequisites**\n\n- You have installed the `yq` CLI tool.\n\n**Procedure**\n\n1. Check the name of an install plan, such as `install-zcvlr`, by running the following command:\n\n   ```terminal\n   $ oc -n external-dns-operator get sub external-dns-operator -o yaml | yq '.status.installplan.name'\n   ```\n2. Check if the status of an install plan is `Complete` by running the following command:\n\n   ```terminal\n   $ oc -n external-dns-operator get ip <install_plan_name> -o yaml | yq '.status.phase'\n   ```\n3. View the status of the `external-dns-operator` deployment by running the following command:\n\n   ```terminal\n   $ oc get -n external-dns-operator deployment/external-dns-operator\n   ```\n\n   ```terminal {title=\"Example output\"}\n   NAME                    READY     UP-TO-DATE   AVAILABLE   AGE\n   external-dns-operator   1/1       1            1           23h\n   ```\n\n## Viewing External DNS Operator logs {#nw-external-dns-operator-logs_external-dns-operator}\n\nTo troubleshoot DNS configuration issues, view the External DNS Operator logs. Use the `oc logs` command to retrieve diagnostic information directly from the Operator pod.\n\n**Procedure**\n\n- View the logs of the External DNS Operator by running the following command:\n\n  ```terminal\n  $ oc logs -n external-dns-operator deployment/external-dns-operator -c external-dns-operator\n  ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/networking_operators/external_dns_operator/understanding-external-dns-operator.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="external-dns-operator" class="group scroll-mt-20">Understanding the External DNS Operator<a href="#external-dns-operator" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">The External DNS Operator deploys and manages <code>ExternalDNS</code> to provide the name resolution for services and routes from the external DNS provider to OpenShift Container Platform.</p>
      <h2 id="nw-external-dns-operator-domain-limitations_external-dns-operator" class="group scroll-mt-20">External DNS Operator domain name limitations<a href="#nw-external-dns-operator-domain-limitations_external-dns-operator" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To prevent configuration errors when deploying the <code>ExternalDNS</code> resource, review the domain name limitations enforced by the External DNS Operator. Understanding these constraints ensures that your requested hostnames and domains are compatible with your underlying DNS provider.</p>
      <p>The External DNS Operator uses the TXT registry that adds the prefix for TXT records. This reduces the maximum length of the domain name for TXT records. A DNS record cannot be present without a corresponding TXT record, so the domain name of the DNS record must follow the same limit as the TXT records. For example, a DNS record of <code>&lt;domain_name_from_source&gt;</code> results in a TXT record of <code>external-dns-&lt;record_type&gt;-&lt;domain_name_from_source&gt;</code>.</p>
      <p>The domain name of the DNS records generated by the External DNS Operator has the following limitations:</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Record type</TableHead>
      <TableHead>Number of characters</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>CNAME</TableCell>
      <TableCell>44</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Wildcard CNAME records on AzureDNS</TableCell>
      <TableCell>42</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>A</TableCell>
      <TableCell>48</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Wildcard A records on AzureDNS</TableCell>
      <TableCell>46</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>The following error shows in the External DNS Operator logs if the generated domain name exceeds any of the domain name limitations:</p>
      <CodeBlock code={"time=\"2022-09-02T08:53:57Z\" level=error msg=\"Failure in zone test.example.io. [Id: /hostedzone/Z06988883Q0H0RL6UMXXX]\"\ntime=\"2022-09-02T08:53:57Z\" level=error msg=\"InvalidChangeBatch: [FATAL problem: DomainLabelTooLong (Domain label is too long) encountered with 'external-dns-a-hello-openshift-aaaaaaaaaa-bbbbbbbbbb-ccccccc']\\n\\tstatus code: 400, request id: e54dfd5a-06c6-47b0-bcb9-a4f7c3a4e0c6\""} lang="terminal" />
      <h2 id="nw-external-dns-operator_external-dns-operator" class="group scroll-mt-20">Deploying the External DNS Operator<a href="#nw-external-dns-operator_external-dns-operator" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The External DNS Operator implements the External DNS API from the <code>olm.openshift.io</code> API group. The External DNS Operator updates services, routes, and external DNS providers.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have installed the <code>yq</code> CLI tool.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Check the name of an install plan, such as <code>install-zcvlr</code>, by running the following command:
      <CodeBlock code={"$ oc -n external-dns-operator get sub external-dns-operator -o yaml | yq '.status.installplan.name'"} lang="terminal" />
      </li>
      <li>
      Check if the status of an install plan is <code>Complete</code> by running the following command:
      <CodeBlock code={"$ oc -n external-dns-operator get ip <install_plan_name> -o yaml | yq '.status.phase'"} lang="terminal" />
      </li>
      <li>
      View the status of the <code>external-dns-operator</code> deployment by running the following command:
      <CodeBlock code={"$ oc get -n external-dns-operator deployment/external-dns-operator"} lang="terminal" />
      <CodeBlock code={"NAME                    READY     UP-TO-DATE   AVAILABLE   AGE\nexternal-dns-operator   1/1       1            1           23h"} lang="terminal" title="Example output" />
      </li>
      </ol>
      <h2 id="nw-external-dns-operator-logs_external-dns-operator" class="group scroll-mt-20">Viewing External DNS Operator logs<a href="#nw-external-dns-operator-logs_external-dns-operator" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To troubleshoot DNS configuration issues, view the External DNS Operator logs. Use the <code>oc logs</code> command to retrieve diagnostic information directly from the Operator pod.</p>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      View the logs of the External DNS Operator by running the following command:
      <CodeBlock code={"$ oc logs -n external-dns-operator deployment/external-dns-operator -c external-dns-operator"} lang="terminal" />
      </li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
