---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/advanced_networking/associating-secondary-interfaces-metrics-to-network-attachments.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"associating-secondary-interfaces-metrics-to-network-attachments","text":"Associating secondary interfaces metrics to network attachments"},{"depth":2,"slug":"cnf-associating-secondary-interfaces-metrics-to-network-attachments_secondary-interfaces-metrics","text":"Extending secondary network metrics for monitoring"},{"depth":2,"slug":"cnf-network-metrics-daemon_secondary-interfaces-metrics","text":"Network Metrics Daemon"},{"depth":2,"slug":"cnf-metrics-secondary-interfaces-by-name_secondary-interfaces-metrics","text":"Metrics with network name"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/advanced_networking/associating-secondary-interfaces-metrics-to-network-attachments";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Associating secondary interfaces metrics to network attachments";
const description = "To gain better visibility into cluster traffic, you can associate secondary interface metrics with specific network attachments. By using the…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/advanced_networking"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Associating secondary interfaces metrics to network attachments {#associating-secondary-interfaces-metrics-to-network-attachments}\n\nTo gain better visibility into cluster traffic, you can associate secondary interface metrics with specific network attachments. By using the `pod_network_info` metric to label interfaces based on their `NetworkAttachmentDefinition` resource, you can more easily monitor performance and troubleshoot connectivity issues across your network.\n\n## Extending secondary network metrics for monitoring {#cnf-associating-secondary-interfaces-metrics-to-network-attachments_secondary-interfaces-metrics}\n\nTo monitor and manage network traffic effectively, you can extend secondary network metrics with identifying information. By using the `pod_network_name_info` metric to label interfaces based on their `NetworkAttachmentDefinition` resource, you can classify interface types to enable precise metric aggregation and alerting.\n\nSecondary devices, or interfaces, are used for different purposes. Metrics from secondary network interfaces need to be classified to allow for effective aggregation and monitoring.\n\nExposed metrics contain the interface but do not specify where the interface originates. This is workable when there are no additional interfaces. However, relying on interface names alone becomes problematic when secondary interfaces are added because it is difficult to identify their purpose and use their metrics effectively..\n\nWhen adding secondary interfaces, their names depend on the order in which they are added. Secondary interfaces can belong to distinct networks that can each serve a different purposes.\n\nWith `pod_network_name_info` it is possible to extend the current metrics with additional information that identifies the interface type. In this way, it is possible to aggregate the metrics and to add specific alarms to specific interface types.\n\nThe network type is generated from the name of the `NetworkAttachmentDefinition` resource, which distinguishes different secondary network classes. For example, different interfaces belonging to different networks or using different CNIs use different network attachment definition names.\n\n## Network Metrics Daemon {#cnf-network-metrics-daemon_secondary-interfaces-metrics}\n\nThe Network Metrics Daemon collects and publishes network-related metrics to support performance management in complex pod environments. This component provides metadata for secondary interfaces, which is required for accurate traffic monitoring across distinct network attachments.\n\nThe kubelet is already publishing network related metrics you can observe. These metrics are:\n\n- `container_network_receive_bytes_total`\n- `container_network_receive_errors_total`\n- `container_network_receive_packets_total`\n- `container_network_receive_packets_dropped_total`\n- `container_network_transmit_bytes_total`\n- `container_network_transmit_errors_total`\n- `container_network_transmit_packets_total`\n- `container_network_transmit_packets_dropped_total`\n\nThe labels in these metrics contain, among others:\n\n- Pod name\n- Pod namespace\n- Interface name (such as `eth0`)\n\nThese metrics work well until new interfaces are added to the pod, for example via [Multus](https://github.com/intel/multus-cni), as it is not clear what the interface names refer to.\n\nThe interface label refers to the interface name, but it is not clear what that interface is meant for. In case of many different interfaces, it would be impossible to understand what network the metrics you are monitoring refer to.\n\nThis is addressed by introducing the new `pod_network_name_info` described in the following section.\n\n## Metrics with network name {#cnf-metrics-secondary-interfaces-by-name_secondary-interfaces-metrics}\n\nTo simplify the monitoring of secondary networks, you can use the `pod_network_name_info` metric to correlate network performance data with specific network names. By joining this metric with container network metrics, you can identify traffic patterns and errors across distinct network attachment definitions.\n\n```bash {title=\"Example of pod_network_name_info\"}\npod_network_name_info{interface=\"net0\",namespace=\"namespacename\",network_name=\"nadnamespace/firstNAD\",pod=\"podname\"} 0\n```\n\nThe network name label is produced using the annotation added by Multus. It is the concatenation of the namespace the network attachment definition belongs to, plus the name of the network attachment definition. The Network Metrics daemonset publishes a `pod_network_name_info` gauge metric, with a fixed value of `0`.\n\nThe new metric alone does not provide much value, but combined with the network related `container_network_*` metrics, it offers better support for monitoring secondary networks.\n\nUsing a `promql` query like the following ones, it is possible to get a new metric containing the value and the network name retrieved from the `k8s.v1.cni.cncf.io/network-status` annotation:\n\n```bash\n(container_network_receive_bytes_total) + on(namespace,pod,interface) group_left(network_name) ( pod_network_name_info )\n(container_network_receive_errors_total) + on(namespace,pod,interface) group_left(network_name) ( pod_network_name_info )\n(container_network_receive_packets_total) + on(namespace,pod,interface) group_left(network_name) ( pod_network_name_info )\n(container_network_receive_packets_dropped_total) + on(namespace,pod,interface) group_left(network_name) ( pod_network_name_info )\n(container_network_transmit_bytes_total) + on(namespace,pod,interface) group_left(network_name) ( pod_network_name_info )\n(container_network_transmit_errors_total) + on(namespace,pod,interface) group_left(network_name) ( pod_network_name_info )\n(container_network_transmit_packets_total) + on(namespace,pod,interface) group_left(network_name) ( pod_network_name_info )\n(container_network_transmit_packets_dropped_total) + on(namespace,pod,interface) group_left(network_name)\n```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/advanced_networking/associating-secondary-interfaces-metrics-to-network-attachments.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="associating-secondary-interfaces-metrics-to-network-attachments" class="group scroll-mt-20">Associating secondary interfaces metrics to network attachments<a href="#associating-secondary-interfaces-metrics-to-network-attachments" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>To gain better visibility into cluster traffic, you can associate secondary interface metrics with specific network attachments. By using the <code>pod_network_info</code> metric to label interfaces based on their <code>NetworkAttachmentDefinition</code> resource, you can more easily monitor performance and troubleshoot connectivity issues across your network.</p>
      <h2 id="cnf-associating-secondary-interfaces-metrics-to-network-attachments_secondary-interfaces-metrics" class="group scroll-mt-20">Extending secondary network metrics for monitoring<a href="#cnf-associating-secondary-interfaces-metrics-to-network-attachments_secondary-interfaces-metrics" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To monitor and manage network traffic effectively, you can extend secondary network metrics with identifying information. By using the <code>pod_network_name_info</code> metric to label interfaces based on their <code>NetworkAttachmentDefinition</code> resource, you can classify interface types to enable precise metric aggregation and alerting.</p>
      <p>Secondary devices, or interfaces, are used for different purposes. Metrics from secondary network interfaces need to be classified to allow for effective aggregation and monitoring.</p>
      <p>Exposed metrics contain the interface but do not specify where the interface originates. This is workable when there are no additional interfaces. However, relying on interface names alone becomes problematic when secondary interfaces are added because it is difficult to identify their purpose and use their metrics effectively..</p>
      <p>When adding secondary interfaces, their names depend on the order in which they are added. Secondary interfaces can belong to distinct networks that can each serve a different purposes.</p>
      <p>With <code>pod_network_name_info</code> it is possible to extend the current metrics with additional information that identifies the interface type. In this way, it is possible to aggregate the metrics and to add specific alarms to specific interface types.</p>
      <p>The network type is generated from the name of the <code>NetworkAttachmentDefinition</code> resource, which distinguishes different secondary network classes. For example, different interfaces belonging to different networks or using different CNIs use different network attachment definition names.</p>
      <h2 id="cnf-network-metrics-daemon_secondary-interfaces-metrics" class="group scroll-mt-20">Network Metrics Daemon<a href="#cnf-network-metrics-daemon_secondary-interfaces-metrics" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The Network Metrics Daemon collects and publishes network-related metrics to support performance management in complex pod environments. This component provides metadata for secondary interfaces, which is required for accurate traffic monitoring across distinct network attachments.</p>
      <p>The kubelet is already publishing network related metrics you can observe. These metrics are:</p>
      <ul>
      <li><code>container_network_receive_bytes_total</code></li>
      <li><code>container_network_receive_errors_total</code></li>
      <li><code>container_network_receive_packets_total</code></li>
      <li><code>container_network_receive_packets_dropped_total</code></li>
      <li><code>container_network_transmit_bytes_total</code></li>
      <li><code>container_network_transmit_errors_total</code></li>
      <li><code>container_network_transmit_packets_total</code></li>
      <li><code>container_network_transmit_packets_dropped_total</code></li>
      </ul>
      <p>The labels in these metrics contain, among others:</p>
      <ul>
      <li>Pod name</li>
      <li>Pod namespace</li>
      <li>Interface name (such as <code>eth0</code>)</li>
      </ul>
      <p>These metrics work well until new interfaces are added to the pod, for example via <a href="https://github.com/intel/multus-cni">Multus</a>, as it is not clear what the interface names refer to.</p>
      <p>The interface label refers to the interface name, but it is not clear what that interface is meant for. In case of many different interfaces, it would be impossible to understand what network the metrics you are monitoring refer to.</p>
      <p>This is addressed by introducing the new <code>pod_network_name_info</code> described in the following section.</p>
      <h2 id="cnf-metrics-secondary-interfaces-by-name_secondary-interfaces-metrics" class="group scroll-mt-20">Metrics with network name<a href="#cnf-metrics-secondary-interfaces-by-name_secondary-interfaces-metrics" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To simplify the monitoring of secondary networks, you can use the <code>pod_network_name_info</code> metric to correlate network performance data with specific network names. By joining this metric with container network metrics, you can identify traffic patterns and errors across distinct network attachment definitions.</p>
      <CodeBlock code={"pod_network_name_info{interface=\"net0\",namespace=\"namespacename\",network_name=\"nadnamespace/firstNAD\",pod=\"podname\"} 0"} lang="bash" title="Example of pod_network_name_info" />
      <p>The network name label is produced using the annotation added by Multus. It is the concatenation of the namespace the network attachment definition belongs to, plus the name of the network attachment definition. The Network Metrics daemonset publishes a <code>pod_network_name_info</code> gauge metric, with a fixed value of <code>0</code>.</p>
      <p>The new metric alone does not provide much value, but combined with the network related <code>container_network_*</code> metrics, it offers better support for monitoring secondary networks.</p>
      <p>Using a <code>promql</code> query like the following ones, it is possible to get a new metric containing the value and the network name retrieved from the <code>k8s.v1.cni.cncf.io/network-status</code> annotation:</p>
      <CodeBlock code={"(container_network_receive_bytes_total) + on(namespace,pod,interface) group_left(network_name) ( pod_network_name_info )\n(container_network_receive_errors_total) + on(namespace,pod,interface) group_left(network_name) ( pod_network_name_info )\n(container_network_receive_packets_total) + on(namespace,pod,interface) group_left(network_name) ( pod_network_name_info )\n(container_network_receive_packets_dropped_total) + on(namespace,pod,interface) group_left(network_name) ( pod_network_name_info )\n(container_network_transmit_bytes_total) + on(namespace,pod,interface) group_left(network_name) ( pod_network_name_info )\n(container_network_transmit_errors_total) + on(namespace,pod,interface) group_left(network_name) ( pod_network_name_info )\n(container_network_transmit_packets_total) + on(namespace,pod,interface) group_left(network_name) ( pod_network_name_info )\n(container_network_transmit_packets_dropped_total) + on(namespace,pod,interface) group_left(network_name)"} lang="bash" />
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
