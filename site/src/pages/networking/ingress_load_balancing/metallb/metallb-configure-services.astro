---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/ingress_load_balancing/metallb/metallb-configure-services.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"metallb-configure-services","text":"Configuring services to use MetalLB"},{"depth":2,"slug":"request-specific-ip-address_configure-services-metallb","text":"Request a specific IP address"},{"depth":2,"slug":"request-ip-address-from-pool_configure-services-metallb","text":"Request an IP address from a specific pool"},{"depth":2,"slug":"accept-any-ip-address_configure-services-metallb","text":"Accept any IP address"},{"depth":2,"slug":"share-specific-ip-address_configure-services-metallb","text":"Share a specific IP address"},{"depth":2,"slug":"nw-metallb-configure-svc_configure-services-metallb","text":"Configuring a service with MetalLB"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/ingress_load_balancing/metallb/metallb-configure-services";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring services to use MetalLB";
const description = "To ensure predictable network endpoints, control how MetalLB assigns IP addresses to services of type LoadBalancer. Requesting specific addresses or pools…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/ingress_load_balancing","networking/ingress_load_balancing/metallb"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring services to use MetalLB {#metallb-configure-services}\n\nTo ensure predictable network endpoints, control how MetalLB assigns IP addresses to services of type `LoadBalancer`. Requesting specific addresses or pools ensures that your applications receive valid IP assignments that align with your specific network addressing plan.\n\n## Request a specific IP address {#request-specific-ip-address_configure-services-metallb}\n\nTo assign a specific, static IP address to a service, configure the `spec.loadBalancerIP` parameter in the service specification.\n\nMetalLB attempts to assign the requested address from the configured address pools, ensuring that your service is reachable at a designated, static network endpoint. If the requested IP address is not within any range, MetalLB reports a warning.\n\n```yaml {title=\"Example service YAML for a specific IP address\"}\napiVersion: v1\nkind: Service\nmetadata:\n  name: <service_name>\n  annotations:\n    metallb.io/address-pool: <address_pool_name>\nspec:\n  selector:\n    <label_key>: <label_value>\n  ports:\n    - port: 8080\n      targetPort: 8080\n      protocol: TCP\n  type: LoadBalancer\n  loadBalancerIP: <ip_address>\n```\n\nIf MetalLB cannot assign the requested IP address, the `EXTERNAL-IP` for the service reports `<pending>` and running `oc describe service <service_name>` includes an event like the following example:\n\n```terminal {title=\"Example event when MetalLB cannot assign a requested IP address\"}\n  ...\nEvents:\n  Type     Reason            Age    From                Message\n  ----     ------            ----   ----                -------\n  Warning  AllocationFailed  3m16s  metallb-controller  Failed to allocate IP for \"default/invalid-request\": \"4.3.2.1\" is not allowed in config\n```\n\n## Request an IP address from a specific pool {#request-ip-address-from-pool_configure-services-metallb}\n\nTo ensure predictable network endpoints, control how MetalLB assigns IP addresses to services of type `LoadBalancer`. Requesting specific addresses or pools ensures that your applications receive valid IP assignments that align with your specific network addressing plan.\n\n```yaml {title=\"Example service YAML for an IP address from a specific pool\"}\napiVersion: v1\nkind: Service\nmetadata:\n  name: <service_name>\n  annotations:\n    metallb.io/address-pool: <address_pool_name>\nspec:\n  selector:\n    <label_key>: <label_value>\n  ports:\n    - port: 8080\n      targetPort: 8080\n      protocol: TCP\n  type: LoadBalancer\n```\n\nIf the address pool that you specify for `<address_pool_name>` does not exist, MetalLB attempts to assign an IP address from any pool that permits automatic assignment.\n\n## Accept any IP address {#accept-any-ip-address_configure-services-metallb}\n\nTo automatically allocate IP addresses to services without manual specification, configure MetalLB address pools to permit automatic assignment. MetalLB dynamically assigns available addresses from these pools, ensuring seamless service deployment and network connectivity.\n\n```yaml {title=\"Example service YAML for accepting any IP address\"}\napiVersion: v1\nkind: Service\nmetadata:\n  name: <service_name>\nspec:\n  selector:\n    <label_key>: <label_value>\n  ports:\n    - port: 8080\n      targetPort: 8080\n      protocol: TCP\n  type: LoadBalancer\n```\n\n## Share a specific IP address {#share-specific-ip-address_configure-services-metallb}\n\nTo colocate multiple services on a single IP address, apply the `metallb.io/allow-shared-ip` annotation to your service specifications. Configuring this annotation authorizes MetalLB to assign the same IP to multiple services, enabling efficient address usage while distinguishing traffic by port.\n\nBy default, services do not share IP addresses.\n\n```yaml\napiVersion: v1\nkind: Service\nmetadata:\n  name: service-http\n  annotations:\n    metallb.io/address-pool: doc-example\n    metallb.io/allow-shared-ip: \"web-server-svc\"\nspec:\n  ports:\n    - name: http\n      port: 80\n      protocol: TCP\n      targetPort: 8080\n  selector:\n    <label_key>: <label_value>\n  type: LoadBalancer\n  loadBalancerIP: 172.31.249.7\n# ...\n---\napiVersion: v1\nkind: Service\nmetadata:\n  name: service-https\n  annotations:\n    metallb.io/address-pool: doc-example\n    metallb.io/allow-shared-ip: \"web-server-svc\"\nspec:\n  ports:\n    - name: https\n      port: 443\n      protocol: TCP\n      targetPort: 8080\n  selector:\n    <label_key>: <label_value>\n  type: LoadBalancer\n  loadBalancerIP: 172.31.249.7\n# ...\n```\n\nwhere:\n\n`metallb.io/allow-shared-ip`\n:   Specifies the same value for the `metallb.io/allow-shared-ip` annotation. This value is referred to as the *sharing key*.\n\n`name.port`\n:   Specifies different port numbers for the services.\n\n`spec.selector`\n:   Specifies identical pod selectors if you must specify `externalTrafficPolicy: local` so the services send traffic to the same set of pods. If you use the `cluster` external traffic policy, then the pod selectors do not need to be identical.\n\n`spec.loadBalancerIP`\n:   Optional parameter. If you specify the three preceding items, MetalLB might colocate the services on the same IP address. To ensure that services share an IP address, specify the IP address to share.\n\nBy default, Kubernetes does not allow multiprotocol load balancer services. This limitation would normally make it impossible to run a service like DNS that needs to listen on both TCP and UDP. To work around this limitation of Kubernetes with MetalLB, create two services:\n\n- For one service, specify TCP and for the second service, specify UDP.\n- In both services, specify the same pod selector.\n- Specify the same sharing key and `spec.loadBalancerIP` value to colocate the TCP and UDP services on the same IP address.\n\n## Configuring a service with MetalLB {#nw-metallb-configure-svc_configure-services-metallb}\n\nTo expose an application to external network traffic, configure a load-balancing service. MetalLB assigns an external IP address from a configured address pool, ensuring that your application is reachable from outside the cluster.\n\n**Prerequisites**\n\n- Install the OpenShift CLI (`oc`).\n- Install the MetalLB Operator and start MetalLB.\n- Configure at least one address pool.\n- Configure your network to route traffic from the clients to the host network for the cluster.\n\n**Procedure**\n\n1. Create a deployment and service for your application. Save the following YAML to a file named `example-metallb-app.yaml`:\n\n   ```yaml\n   apiVersion: v1\n   kind: Namespace\n   metadata:\n     name: example-metallb\n   ---\n   apiVersion: apps/v1\n   kind: Deployment\n   metadata:\n     name: example-app\n     namespace: example-metallb\n   spec:\n     replicas: 1\n     selector:\n       matchLabels:\n         app: example-app\n     template:\n       metadata:\n         labels:\n           app: example-app\n       spec:\n         containers:\n         - name: example-app\n           image: registry.access.redhat.com/ubi9/httpd-24:latest\n           ports:\n           - containerPort: 8080\n   ---\n   apiVersion: v1\n   kind: Service\n   metadata:\n     name: example-metallb-svc\n     namespace: example-metallb\n     annotations:\n       metallb.io/address-pool: <ipaddresspool_name>\n   spec:\n     type: LoadBalancer\n     selector:\n       app: example-app\n     ports:\n     - port: 80\n       targetPort: 8080\n   ```\n\n   - `<ipaddresspool_name>` specifies the name of the `IPAddressPool` resource, for example `doc-example-l2`. If you do not set this annotation, MetalLB automatically assigns an IP address from any pool that has `autoAssign` set to `true`.\n2. Create the deployment and service:\n\n   ```terminal\n   $ oc apply -f example-metallb-app.yaml\n   ```\n\n   The following is example output:\n\n   ```terminal\n   namespace/example-metallb created\n   deployment.apps/example-app created\n   service/example-metallb-svc created\n   ```\n\n**Verification**\n\n- Describe the service:\n\n  ```terminal\n  $ oc describe service example-metallb-svc -n example-metallb\n  ```\n\n  The following is example output:\n\n  ```\n  Name:                     example-metallb-svc\n  Namespace:                example-metallb\n  Labels:                   <none>\n  Annotations:              metallb.io/address-pool: doc-example-l2\n                            metallb.io/ip-allocated-from-pool: doc-example-l2\n  Selector:                 app=example-app\n  Type:                     LoadBalancer\n  IP Family Policy:         SingleStack\n  IP Families:              IPv4\n  IP:                       10.105.237.254\n  IPs:                      10.105.237.254\n  LoadBalancer Ingress:     192.168.100.5\n  Port:                     <unset>  80/TCP\n  TargetPort:               8080/TCP\n  NodePort:                 <unset>  30550/TCP\n  Endpoints:                10.244.0.50:8080\n  Session Affinity:         None\n  External Traffic Policy:  Cluster\n  Events:\n    Type    Reason        Age                From                Message\n    ----    ------        ----               ----                -------\n    Normal  IPAllocated   12s                metallb-controller  Assigned IP [\"192.168.100.5\"]\n    Normal  nodeAssigned  12s (x2 over 12s)  metallb-speaker     announcing from node \"<node_name>\"\n  ```\n\n  where:\n\n  `Annotations`\n  :   Specifies the annotation that is present if you request an IP address from a specific pool.\n\n  `Type`\n  :   Specifies the service type that must indicate `LoadBalancer`.\n\n  `LoadBalancer Ingress`\n  :   Specifies the external IP address that is assigned to the service.\n\n  `Events`\n  :   Specifies the events parameter that indicates the node name that is assigned to announce the external IP address. If you experience an error, the events parameter indicates the reason for the error.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/ingress_load_balancing/metallb/metallb-configure-services.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="metallb-configure-services" class="group scroll-mt-20">Configuring services to use MetalLB<a href="#metallb-configure-services" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>To ensure predictable network endpoints, control how MetalLB assigns IP addresses to services of type <code>LoadBalancer</code>. Requesting specific addresses or pools ensures that your applications receive valid IP assignments that align with your specific network addressing plan.</p>
      <h2 id="request-specific-ip-address_configure-services-metallb" class="group scroll-mt-20">Request a specific IP address<a href="#request-specific-ip-address_configure-services-metallb" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To assign a specific, static IP address to a service, configure the <code>spec.loadBalancerIP</code> parameter in the service specification. </p>
      <p>MetalLB attempts to assign the requested address from the configured address pools, ensuring that your service is reachable at a designated, static network endpoint. If the requested IP address is not within any range, MetalLB reports a warning.</p>
      <CodeBlock code={"apiVersion: v1\nkind: Service\nmetadata:\n  name: <service_name>\n  annotations:\n    metallb.io/address-pool: <address_pool_name>\nspec:\n  selector:\n    <label_key>: <label_value>\n  ports:\n    - port: 8080\n      targetPort: 8080\n      protocol: TCP\n  type: LoadBalancer\n  loadBalancerIP: <ip_address>"} lang="yaml" title="Example service YAML for a specific IP address" />
      <p>If MetalLB cannot assign the requested IP address, the <code>EXTERNAL-IP</code> for the service reports <code>&lt;pending&gt;</code> and running <code>oc describe service &lt;service_name&gt;</code> includes an event like the following example:</p>
      <CodeBlock code={"  ...\nEvents:\n  Type     Reason            Age    From                Message\n  ----     ------            ----   ----                -------\n  Warning  AllocationFailed  3m16s  metallb-controller  Failed to allocate IP for \"default/invalid-request\": \"4.3.2.1\" is not allowed in config"} lang="terminal" title="Example event when MetalLB cannot assign a requested IP address" />
      <h2 id="request-ip-address-from-pool_configure-services-metallb" class="group scroll-mt-20">Request an IP address from a specific pool<a href="#request-ip-address-from-pool_configure-services-metallb" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To ensure predictable network endpoints, control how MetalLB assigns IP addresses to services of type <code>LoadBalancer</code>. Requesting specific addresses or pools ensures that your applications receive valid IP assignments that align with your specific network addressing plan.</p>
      <CodeBlock code={"apiVersion: v1\nkind: Service\nmetadata:\n  name: <service_name>\n  annotations:\n    metallb.io/address-pool: <address_pool_name>\nspec:\n  selector:\n    <label_key>: <label_value>\n  ports:\n    - port: 8080\n      targetPort: 8080\n      protocol: TCP\n  type: LoadBalancer"} lang="yaml" title="Example service YAML for an IP address from a specific pool" />
      <p>If the address pool that you specify for <code>&lt;address_pool_name&gt;</code> does not exist, MetalLB attempts to assign an IP address from any pool that permits automatic assignment.</p>
      <h2 id="accept-any-ip-address_configure-services-metallb" class="group scroll-mt-20">Accept any IP address<a href="#accept-any-ip-address_configure-services-metallb" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To automatically allocate IP addresses to services without manual specification, configure MetalLB address pools to permit automatic assignment. MetalLB dynamically assigns available addresses from these pools, ensuring seamless service deployment and network connectivity. </p>
      <CodeBlock code={"apiVersion: v1\nkind: Service\nmetadata:\n  name: <service_name>\nspec:\n  selector:\n    <label_key>: <label_value>\n  ports:\n    - port: 8080\n      targetPort: 8080\n      protocol: TCP\n  type: LoadBalancer"} lang="yaml" title="Example service YAML for accepting any IP address" />
      <h2 id="share-specific-ip-address_configure-services-metallb" class="group scroll-mt-20">Share a specific IP address<a href="#share-specific-ip-address_configure-services-metallb" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To colocate multiple services on a single IP address, apply the <code>metallb.io/allow-shared-ip</code> annotation to your service specifications. Configuring this annotation authorizes MetalLB to assign the same IP to multiple services, enabling efficient address usage while distinguishing traffic by port.</p>
      <p>By default, services do not share IP addresses.</p>
      <CodeBlock code={"apiVersion: v1\nkind: Service\nmetadata:\n  name: service-http\n  annotations:\n    metallb.io/address-pool: doc-example\n    metallb.io/allow-shared-ip: \"web-server-svc\"\nspec:\n  ports:\n    - name: http\n      port: 80 \n      protocol: TCP\n      targetPort: 8080\n  selector:\n    <label_key>: <label_value>\n  type: LoadBalancer\n  loadBalancerIP: 172.31.249.7\n# ...\n---\napiVersion: v1\nkind: Service\nmetadata:\n  name: service-https\n  annotations:\n    metallb.io/address-pool: doc-example\n    metallb.io/allow-shared-ip: \"web-server-svc\"\nspec:\n  ports:\n    - name: https\n      port: 443\n      protocol: TCP\n      targetPort: 8080\n  selector:\n    <label_key>: <label_value>\n  type: LoadBalancer\n  loadBalancerIP: 172.31.249.7\n# ..."} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>metallb.io/allow-shared-ip</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the same value for the <code>metallb.io/allow-shared-ip</code> annotation. This value is referred to as the <em>sharing key</em>.</p></dd>
      <dt class="font-semibold"><code>name.port</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies different port numbers for the services.</p></dd>
      <dt class="font-semibold"><code>spec.selector</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies identical pod selectors if you must specify <code>externalTrafficPolicy: local</code> so the services send traffic to the same set of pods. If you use the <code>cluster</code> external traffic policy, then the pod selectors do not need to be identical.</p></dd>
      <dt class="font-semibold"><code>spec.loadBalancerIP</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional parameter. If you specify the three preceding items, MetalLB might colocate the services on the same IP address. To ensure that services share an IP address, specify the IP address to share.</p></dd>
      </dl>
      <p>By default, Kubernetes does not allow multiprotocol load balancer services. This limitation would normally make it impossible to run a service like DNS that needs to listen on both TCP and UDP. To work around this limitation of Kubernetes with MetalLB, create two services:</p>
      <ul>
      <li>For one service, specify TCP and for the second service, specify UDP.</li>
      <li>In both services, specify the same pod selector.</li>
      <li>Specify the same sharing key and <code>spec.loadBalancerIP</code> value to colocate the TCP and UDP services on the same IP address.</li>
      </ul>
      <h2 id="nw-metallb-configure-svc_configure-services-metallb" class="group scroll-mt-20">Configuring a service with MetalLB<a href="#nw-metallb-configure-svc_configure-services-metallb" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To expose an application to external network traffic, configure a load-balancing service. MetalLB assigns an external IP address from a configured address pool, ensuring that your application is reachable from outside the cluster.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>Install the OpenShift CLI (<code>oc</code>).</li>
      <li>Install the MetalLB Operator and start MetalLB.</li>
      <li>Configure at least one address pool.</li>
      <li>Configure your network to route traffic from the clients to the host network for the cluster.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a deployment and service for your application. Save the following YAML to a file named <code>example-metallb-app.yaml</code>:
      <CodeBlock code={"apiVersion: v1\nkind: Namespace\nmetadata:\n  name: example-metallb\n---\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: example-app\n  namespace: example-metallb\nspec:\n  replicas: 1\n  selector:\n    matchLabels:\n      app: example-app\n  template:\n    metadata:\n      labels:\n        app: example-app\n    spec:\n      containers:\n      - name: example-app\n        image: registry.access.redhat.com/ubi9/httpd-24:latest\n        ports:\n        - containerPort: 8080\n---\napiVersion: v1\nkind: Service\nmetadata:\n  name: example-metallb-svc\n  namespace: example-metallb\n  annotations:\n    metallb.io/address-pool: <ipaddresspool_name>\nspec:\n  type: LoadBalancer\n  selector:\n    app: example-app\n  ports:\n  - port: 80\n    targetPort: 8080"} lang="yaml" />
      <ul><li><code>&lt;ipaddresspool_name&gt;</code> specifies the name of the <code>IPAddressPool</code> resource, for example <code>doc-example-l2</code>. If you do not set this annotation, MetalLB automatically assigns an IP address from any pool that has <code>autoAssign</code> set to <code>true</code>.</li></ul>
      </li>
      <li>
      Create the deployment and service:
      <CodeBlock code={"$ oc apply -f example-metallb-app.yaml"} lang="terminal" />
      <p>The following is example output:</p>
      <CodeBlock code={"namespace/example-metallb created\ndeployment.apps/example-app created\nservice/example-metallb-svc created"} lang="terminal" />
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      Describe the service:
      <CodeBlock code={"$ oc describe service example-metallb-svc -n example-metallb"} lang="terminal" />
      <p>The following is example output:</p>
      <CodeBlock code={"Name:                     example-metallb-svc\nNamespace:                example-metallb\nLabels:                   <none>\nAnnotations:              metallb.io/address-pool: doc-example-l2\n                          metallb.io/ip-allocated-from-pool: doc-example-l2\nSelector:                 app=example-app\nType:                     LoadBalancer\nIP Family Policy:         SingleStack\nIP Families:              IPv4\nIP:                       10.105.237.254\nIPs:                      10.105.237.254\nLoadBalancer Ingress:     192.168.100.5\nPort:                     <unset>  80/TCP\nTargetPort:               8080/TCP\nNodePort:                 <unset>  30550/TCP\nEndpoints:                10.244.0.50:8080\nSession Affinity:         None\nExternal Traffic Policy:  Cluster\nEvents:\n  Type    Reason        Age                From                Message\n  ----    ------        ----               ----                -------\n  Normal  IPAllocated   12s                metallb-controller  Assigned IP [\"192.168.100.5\"]\n  Normal  nodeAssigned  12s (x2 over 12s)  metallb-speaker     announcing from node \"<node_name>\""} lang="plaintext" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>Annotations</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the annotation that is present if you request an IP address from a specific pool.</p></dd>
      <dt class="font-semibold"><code>Type</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the service type that must indicate <code>LoadBalancer</code>.</p></dd>
      <dt class="font-semibold"><code>LoadBalancer Ingress</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the external IP address that is assigned to the service.</p></dd>
      <dt class="font-semibold"><code>Events</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the events parameter that indicates the node name that is assigned to announce the external IP address. If you experience an error, the events parameter indicates the reason for the error.</p></dd>
      </dl>
      </li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
