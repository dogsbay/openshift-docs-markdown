---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/ingress_load_balancing/metallb/metallb-configure-community-alias.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"metallb-configure-community-alias","text":"Configuring community alias"},{"depth":2,"slug":"nw-metallb-community-cr_configure-community-alias","text":"About the community custom resource"},{"depth":2,"slug":"nw-metallb-configure-BGP-advertisement-community-alias_configure-community-alias","text":"Configuring MetalLB with a BGP advertisement and community alias"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/ingress_load_balancing/metallb/metallb-configure-community-alias";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring community alias";
const description = "As a cluster administrator, you can configure a community alias and use it across different advertisements." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/ingress_load_balancing","networking/ingress_load_balancing/metallb"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring community alias {#metallb-configure-community-alias}\n\nAs a cluster administrator, you can configure a community alias and use it across different advertisements.\n\n## About the community custom resource {#nw-metallb-community-cr_configure-community-alias}\n\nTo simplify BGP configuration, define named aliases for community values by using the community custom resource. You can reference these aliases when advertising `ipAddressPools` with the `BGPAdvertisement` resource.\n\nThe fields for the `community` custom resource are described in the following table.\n\n> [!NOTE]\n> The `community` CRD applies only to BGPAdvertisement.\n\n**MetalLB community custom resource**\n\n<table>\n<thead>\n<tr>\n  <th>Field</th>\n  <th>Type</th>\n  <th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td><code>metadata.name</code></td>\n  <td><code>string</code></td>\n  <td>Specifies the name for the <code>community</code>.</td>\n</tr>\n<tr>\n  <td><code>metadata.namespace</code></td>\n  <td><code>string</code></td>\n  <td>Specifies the namespace for the <code>community</code>. Specify the same namespace that the MetalLB Operator uses.</td>\n</tr>\n<tr>\n  <td><code>spec.communities</code></td>\n  <td><code>string</code></td>\n  <td>Specifies a list of BGP community aliases that can be used in BGPAdvertisements. A community alias consists of a pair of name (alias) and value (number:number). Link the BGPAdvertisement to a community alias by referring to the alias name in its <code>spec.communities</code> field.</td>\n</tr>\n</tbody>\n</table>\n\n**CommunityAlias**\n\n<table>\n<thead>\n<tr>\n  <th>Field</th>\n  <th>Type</th>\n  <th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td><code>name</code></td>\n  <td><code>string</code></td>\n  <td>The name of the alias for the <code>community</code>.</td>\n</tr>\n<tr>\n  <td><code>value</code></td>\n  <td><code>string</code></td>\n  <td>The BGP <code>community</code> value corresponding to the given name.</td>\n</tr>\n</tbody>\n</table>\n\n## Configuring MetalLB with a BGP advertisement and community alias {#nw-metallb-configure-BGP-advertisement-community-alias_configure-community-alias}\n\nTo advertise an `IPAddressPool` by using the BGP protocol, configure MetalLB with a community alias. This configuration sets the alias to the numeric value of the `NO_ADVERTISE` community.\n\nIn the following example, the peer BGP router `doc-example-peer-community` receives one `203.0.113.200/32` route and one `fc00:f853:ccd:e799::1/128` route for each load-balancer IP address that MetalLB assigns to a service. A community alias is configured with the `NO_ADVERTISE` community.\n\n**Prerequisites**\n\n- Install the OpenShift CLI (`oc`)\n- Log in as a user with `cluster-admin` privileges.\n\n**Procedure**\n\n1. Create an IP address pool.\n\n   1. Create a file, such as `ipaddresspool.yaml`, with content like the following example:\n\n      ```yaml\n      apiVersion: metallb.io/v1beta1\n      kind: IPAddressPool\n      metadata:\n        namespace: metallb-system\n        name: doc-example-bgp-community\n      spec:\n        addresses:\n          - 203.0.113.200/30\n          - fc00:f853:ccd:e799::/124\n      ```\n   2. Apply the configuration for the IP address pool:\n\n      ```terminal\n      $ oc apply -f ipaddresspool.yaml\n      ```\n2. Create a community alias named `community1`.\n\n   ```yaml\n   apiVersion: metallb.io/v1beta1\n   kind: Community\n   metadata:\n     name: community1\n     namespace: metallb-system\n   spec:\n     communities:\n       - name: NO_ADVERTISE\n         value: '65535:65282'\n   ```\n3. Create a BGP peer named `doc-example-bgp-peer`.\n\n   1. Create a file, such as `bgppeer.yaml`, with content like the following example:\n\n      ```yaml\n      apiVersion: metallb.io/v1beta2\n      kind: BGPPeer\n      metadata:\n        namespace: metallb-system\n        name: doc-example-bgp-peer\n      spec:\n        peerAddress: 10.0.0.1\n        peerASN: 64501\n        myASN: 64500\n        routerID: 10.10.10.10\n      ```\n   2. Apply the configuration for the BGP peer:\n\n      ```terminal\n      $ oc apply -f bgppeer.yaml\n      ```\n4. Create a BGP advertisement with the community alias.\n\n   1. Create a file, such as `bgpadvertisement.yaml`, with content like the following example:\n\n      ```yaml\n      apiVersion: metallb.io/v1beta1\n      kind: BGPAdvertisement\n      metadata:\n        name: bgp-community-sample\n        namespace: metallb-system\n      spec:\n        aggregationLength: 32\n        aggregationLengthV6: 128\n        communities:\n          - NO_ADVERTISE\n        ipAddressPools:\n          - doc-example-bgp-community\n        peers:\n          - doc-example-peer\n      ```\n\n      where:\n\n      `NO_ADVERTISE`: Specifies the `CommunityAlias.name` here and not the community custom resource (CR) name.\n   2. Apply the configuration:\n\n      ```terminal\n      $ oc apply -f bgpadvertisement.yaml\n      ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/ingress_load_balancing/metallb/metallb-configure-community-alias.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="metallb-configure-community-alias" class="group scroll-mt-20">Configuring community alias<a href="#metallb-configure-community-alias" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">As a cluster administrator, you can configure a community alias and use it across different advertisements.</p>
      <h2 id="nw-metallb-community-cr_configure-community-alias" class="group scroll-mt-20">About the community custom resource<a href="#nw-metallb-community-cr_configure-community-alias" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To simplify BGP configuration, define named aliases for community values by using the community custom resource. You can reference these aliases when advertising <code>ipAddressPools</code> with the <code>BGPAdvertisement</code> resource.</p>
      <p>The fields for the <code>community</code> custom resource are described in the following table.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The <code>community</code> CRD applies only to BGPAdvertisement.</p>
        </AlertDescription>
      </Alert>
      <p><strong>MetalLB community custom resource</strong></p>
      <Fragment set:html={"<table>\n<thead>\n<tr>\n  <th>Field</th>\n  <th>Type</th>\n  <th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td><code>metadata.name</code></td>\n  <td><code>string</code></td>\n  <td>Specifies the name for the <code>community</code>.</td>\n</tr>\n<tr>\n  <td><code>metadata.namespace</code></td>\n  <td><code>string</code></td>\n  <td>Specifies the namespace for the <code>community</code>. Specify the same namespace that the MetalLB Operator uses.</td>\n</tr>\n<tr>\n  <td><code>spec.communities</code></td>\n  <td><code>string</code></td>\n  <td>Specifies a list of BGP community aliases that can be used in BGPAdvertisements. A community alias consists of a pair of name (alias) and value (number:number). Link the BGPAdvertisement to a community alias by referring to the alias name in its <code>spec.communities</code> field.</td>\n</tr>\n</tbody>\n</table>\n"} />
      <p><strong>CommunityAlias</strong></p>
      <Fragment set:html={"<table>\n<thead>\n<tr>\n  <th>Field</th>\n  <th>Type</th>\n  <th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td><code>name</code></td>\n  <td><code>string</code></td>\n  <td>The name of the alias for the <code>community</code>.</td>\n</tr>\n<tr>\n  <td><code>value</code></td>\n  <td><code>string</code></td>\n  <td>The BGP <code>community</code> value corresponding to the given name.</td>\n</tr>\n</tbody>\n</table>\n"} />
      <h2 id="nw-metallb-configure-BGP-advertisement-community-alias_configure-community-alias" class="group scroll-mt-20">Configuring MetalLB with a BGP advertisement and community alias<a href="#nw-metallb-configure-BGP-advertisement-community-alias_configure-community-alias" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To advertise an <code>IPAddressPool</code> by using the BGP protocol, configure MetalLB with a community alias. This configuration sets the alias to the numeric value of the <code>NO_ADVERTISE</code> community.</p>
      <p>In the following example, the peer BGP router <code>doc-example-peer-community</code> receives one <code>203.0.113.200/32</code> route and one <code>fc00:f853:ccd:e799::1/128</code> route for each load-balancer IP address that MetalLB assigns to a service. A community alias is configured with the <code>NO_ADVERTISE</code> community.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>Install the OpenShift CLI (<code>oc</code>)</li>
      <li>Log in as a user with <code>cluster-admin</code> privileges.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create an IP address pool.
      <ol>
      <li>
      Create a file, such as <code>ipaddresspool.yaml</code>, with content like the following example:
      <CodeBlock code={"apiVersion: metallb.io/v1beta1\nkind: IPAddressPool\nmetadata:\n  namespace: metallb-system\n  name: doc-example-bgp-community\nspec:\n  addresses:\n    - 203.0.113.200/30\n    - fc00:f853:ccd:e799::/124"} lang="yaml" />
      </li>
      <li>
      Apply the configuration for the IP address pool:
      <CodeBlock code={"$ oc apply -f ipaddresspool.yaml"} lang="terminal" />
      </li>
      </ol>
      </li>
      <li>
      Create a community alias named <code>community1</code>.
      <CodeBlock code={"apiVersion: metallb.io/v1beta1\nkind: Community\nmetadata:\n  name: community1\n  namespace: metallb-system\nspec:\n  communities:\n    - name: NO_ADVERTISE\n      value: '65535:65282'"} lang="yaml" />
      </li>
      <li>
      Create a BGP peer named <code>doc-example-bgp-peer</code>.
      <ol>
      <li>
      Create a file, such as <code>bgppeer.yaml</code>, with content like the following example:
      <CodeBlock code={"apiVersion: metallb.io/v1beta2\nkind: BGPPeer\nmetadata:\n  namespace: metallb-system\n  name: doc-example-bgp-peer\nspec:\n  peerAddress: 10.0.0.1\n  peerASN: 64501\n  myASN: 64500\n  routerID: 10.10.10.10"} lang="yaml" />
      </li>
      <li>
      Apply the configuration for the BGP peer:
      <CodeBlock code={"$ oc apply -f bgppeer.yaml"} lang="terminal" />
      </li>
      </ol>
      </li>
      <li>
      Create a BGP advertisement with the community alias.
      <ol>
      <li>
      Create a file, such as <code>bgpadvertisement.yaml</code>, with content like the following example:
      <CodeBlock code={"apiVersion: metallb.io/v1beta1\nkind: BGPAdvertisement\nmetadata:\n  name: bgp-community-sample\n  namespace: metallb-system\nspec:\n  aggregationLength: 32\n  aggregationLengthV6: 128\n  communities:\n    - NO_ADVERTISE\n  ipAddressPools:\n    - doc-example-bgp-community\n  peers:\n    - doc-example-peer"} lang="yaml" />
      <p>where:</p>
      <p><code>NO_ADVERTISE</code>: Specifies the <code>CommunityAlias.name</code> here and not the community custom resource (CR) name.</p>
      </li>
      <li>
      Apply the configuration:
      <CodeBlock code={"$ oc apply -f bgpadvertisement.yaml"} lang="terminal" />
      </li>
      </ol>
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
