---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/ingress_load_balancing/configuring_gateway_api/assigning-network-addresses-gateways.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"assigning-network-addresses-gateways","text":"Assign network addresses to gateways"},{"depth":2,"slug":"understand-gateway-address-assignment_assigning-network-addresses-gateways","text":"Understand gateway address assignment and types"},{"depth":3,"slug":"_address_types","text":"Address types"},{"depth":2,"slug":"on-premise-gateway-routing-requirements_assigning-network-addresses-gateways","text":"On-premise gateway routing requirements"},{"depth":2,"slug":"configuring-automatic-address-assignment-gateway_assigning-network-addresses-gateways","text":"Configure automatic address assignment for a gateway"},{"depth":2,"slug":"configuring-internal-lb-gateway_assigning-network-addresses-gateways","text":"Configure an internal load balancer for a gateway"},{"depth":3,"slug":"internal-lb-annotations-reference_assigning-network-addresses-gateways","text":"Cloud provider annotations for internal load balancers"},{"depth":2,"slug":"configuring-dns-on-premise-gateways_assigning-network-addresses-gateways","text":"Configuring DNS for on-premise gateways"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/ingress_load_balancing/configuring_gateway_api/assigning-network-addresses-gateways";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Assign network addresses to gateways";
const description = "You can configure network addresses for your gateway to provide a predictable entry point for external and internal traffic. This ensures that clients can…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/ingress_load_balancing","networking/ingress_load_balancing/configuring_gateway_api"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Assign network addresses to gateways {#assigning-network-addresses-gateways}\n\nYou can configure network addresses for your gateway to provide a predictable entry point for external and internal traffic. This ensures that clients can reliably resolve and route requests to your load balancers.\n\nGateway API uses addresses to define the specific network locations that are assigned to your `Gateway` resource. In OpenShift Container Platform, you rely on the gateway controller to automatically provision and bind the necessary network addresses, such as an external or internal load balancer IP, to your gateway. On on-premise environments, this automatic provisioning requires a configured load balancer controller.\n\nTo successfully assign network addresses to your gateway, complete the following tasks:\n\n- Understand gateway address assignment and types to plan your DNS and load balancer configuration.\n- Understand on-premise gateway routing requirements to ensure your infrastructure can support Gateway API.\n- Configure automatic address assignment for a gateway to successfully deploy it without violating manual address constraints.\n- Configure an internal load balancer to restrict your gateway traffic to your private network.\n- Review cloud provider annotations to ensure your internal load balancer provisions correctly on your specific infrastructure.\n- Configure DNS for on-premise gateways to ensure clients can reliably resolve your gateway.\n\n## Understand gateway address assignment and types {#understand-gateway-address-assignment_assigning-network-addresses-gateways}\n\nOpenShift Container Platform automatically handles address assignment by provisioning a `LoadBalancer` service when you create a `Gateway` resource. The network address assigned to your gateway corresponds to the IP address or hostname of this underlying load balancer.\n\n> [!IMPORTANT]\n> Do not define the `spec.addresses` field. Manually requesting specific network addresses is not currently supported in OpenShift Container Platform. If you attempt to request a specific address manually, the gateway enters an error state.\n>\n> The `status.addresses` field is populated automatically by the gateway controller. This field lists the actual, active network address assigned to your gateway by the load balancing infrastructure.\n\n### Address types {#_address_types}\n\nWhen the controller dynamically assigns an address to your gateway and populates the `status.addresses` field, it uses one of the following primary types to reflect the underlying load balancer:\n\n`Hostname`\n:   Represents a DNS-based ingress point. This concept is typically used for cloud load balancers where a DNS name exposes the load balancer.\n\n`IPAddress`\n:   A textual representation of a numeric IP address (IPv4 or IPv6) assigned by the load balancing infrastructure.\n\n## On-premise gateway routing requirements {#on-premise-gateway-routing-requirements_assigning-network-addresses-gateways}\n\nUnderstand the specific routing and load balancing requirements for on-premise Gateway API deployments to ensure your gateway functions correctly.\n\nUnlike cloud environments where load balancers are dynamically provisioned, on-premise clusters require a preconfigured load balancer controller. Red Hat tests and certifies Gateway API on on-premise platforms specifically with MetalLB.\n\n> [!WARNING]\n> If you attempt to use Gateway API on an on-premise cluster without a functional load balancer controller, the gateway service will remain in a \"pending\" state indefinitely.\n\nAdditionally, be aware of the following topology and load balancer limitations for on-premise environments:\n\n- Third-party load balancers: Red Hat does not currently test Gateway API with third-party load balancers such as F5 or Avi Kubernetes Operator (AKO). If you use an untested load balancer, the cluster administrator is responsible for ensuring it is configured and working properly.\n- Unsupported topologies: Environments without a load balancer controller are not supported. For example, you cannot use annotations to enforce a `NodePort` service type in place of a load balancer.\n\n## Configure automatic address assignment for a gateway {#configuring-automatic-address-assignment-gateway_assigning-network-addresses-gateways}\n\nWhen you create a gateway resource, you must configure it for automatic address provisioning to successfully deploy the gateway without violating OpenShift Container Platform manual address constraints. By intentionally omitting the addresses field, you allow the controller to seamlessly provision and bind the necessary external network addresses to your gateway.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- You have installed the OpenShift CLI (`oc`).\n- You have an existing `GatewayClass` custom resource, such as `openshift-default`.\n\n**Procedure**\n\n1. Create a YAML file, such as `hello-gateway.yaml`, that defines your `Gateway` object.\n\n   ```yaml\n   apiVersion: gateway.networking.k8s.io/v1\n   kind: Gateway\n   metadata:\n     name: sample-gateway\n     namespace: openshift-ingress\n   spec:\n     gatewayClassName: openshift-default\n     listeners:\n     - name: http\n       hostname: \"*.gwapi.<cluster_domain>\"\n       port: 80\n       protocol: HTTP\n       allowedRoutes:\n         namespaces:\n           from: Selector\n           selector:\n             matchLabels:\n               shared-gateway-access: \"true\"\n   ```\n\n   - `metadata.name`: Specify the name of your `Gateway` object. The name must consist of a maximum of 63 lowercase alphanumeric characters or hyphens (`-`). The name must also start and end with an alphanumeric character.\n   - `spec.gatewayClassName`: Specify the `GatewayClass` object whose controller provisions the address and populates the `status.addresses` field.\n   - `spec.listeners[].hostname`: Specify the listener hostname. Replace `<cluster_domain>` with your actual cluster ingress domain (for example, `example.com`). Setting a hostname limits which route hostnames can match this listener.\n   - `spec.listeners[].allowedRoutes.namespaces`: Allow route attachment only from namespaces that have the `shared-gateway-access: \"true\"` label.\n2. Apply the `Gateway` configuration by running the following command:\n\n   ```terminal\n   $ oc apply -f hello-gateway.yaml\n   ```\n3. Verify that the controller automatically assigned an address to your gateway by running the following command:\n\n   ```terminal\n   $ oc -n openshift-ingress get gateway sample-gateway\n   ```\n\n   ```terminal {title=\"Example output\"}\n   NAME             CLASS               ADDRESS             PROGRAMMED   AGE\n   sample-gateway   openshift-default   <gateway_address>   True         6m16s\n   ```\n\n   The `ADDRESS` column in the output displays the dynamically provisioned network address for your gateway.\n\n## Configure an internal load balancer for a gateway {#configuring-internal-lb-gateway_assigning-network-addresses-gateways}\n\nBy default, Gateway API provisions an external load balancer. To restrict your gateway traffic to your private network, you can configure Gateway API to provision an internal load balancer by adding a cloud-specific annotation to your `Gateway` custom resource (CR).\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- You have installed the OpenShift CLI (`oc`).\n- You have configured a `GatewayClass` object.\n\n**Procedure**\n\n1. Create or edit your `Gateway` CR to include the cloud-specific annotation under `spec.infrastructure.annotations`.\n\n   The following example provisions an internal load balancer for an AWS cluster:\n\n   ```yaml {title=\"Example Gateway CR for an AWS internal load balancer\"}\n   apiVersion: gateway.networking.k8s.io/v1\n   kind: Gateway\n   metadata:\n     name: mygateway\n     namespace: openshift-ingress\n   spec:\n     gatewayClassName: openshift-default\n     infrastructure:\n       annotations:\n       # Specifies the cloud provider annotation and value required to provision an internal load balancer:\n         service.beta.kubernetes.io/aws-load-balancer-internal: \"true\"\n     listeners:\n     - name: https\n       hostname: \"*.example.com\"\n       port: 443\n       protocol: HTTPS\n       tls:\n         mode: Terminate\n         certificateRefs:\n         - name: gateway-tls-secret\n   # ...\n   ```\n2. Apply the updated `Gateway` CR by running the following command:\n\n   ```terminal\n   $ oc apply -f <gateway_filename>.yaml\n   ```\n\n**Verification**\n\n- Verify that the load balancer service is provisioned and has an internal IP address by running the following command:\n\n  ```terminal\n  $ oc -n openshift-ingress get svc\n  ```\n\n### Cloud provider annotations for internal load balancers {#internal-lb-annotations-reference_assigning-network-addresses-gateways}\n\nTo provision an internal load balancer for clusters deployed in private environments, you must add specific annotations to the `spec.infrastructure.annotations` field of your `Gateway` custom resource (CR).\n\nThis configuration is supported on Amazon Web Services (AWS), Microsoft Azure, Google Cloud, Red Hat OpenStack Platform (RHOSP), and IBM Cloud. The following table details the required cloud-specific annotations and their corresponding values.\n\n**Internal load balancer annotations by cloud provider**\n\n| Cloud Provider | Annotation | Value |\n| --- | --- | --- |\n| AWS | `service.beta.kubernetes.io/aws-load-balancer-internal` | `\"true\"` |\n| Azure | `service.beta.kubernetes.io/azure-load-balancer-internal` | `\"true\"` |\n| Google Cloud | `cloud.google.com/load-balancer-type` | `\"Internal\"` |\n| RHOSP | `service.beta.kubernetes.io/openstack-internal-load-balancer` | `\"true\"` |\n| IBM Cloud/ IBM Power Virtual Server | `service.kubernetes.io/ibm-load-balancer-cloud-provider-ip-type` | `\"private\"` |\n\n## Configuring DNS for on-premise gateways {#configuring-dns-on-premise-gateways_assigning-network-addresses-gateways}\n\nConfigure DNS records manually on on-premise environments to ensure clients can reliably resolve your gateway.\n\nAlthough the Ingress Operator automatically creates a `DNSRecord` custom resource (CR) using the hostname from the listener, this record is marked as \"unmanaged\" on on-premise platforms because the cluster Ingress Operator does not implement on-premise DNS providers. You must manually configure DNS records to point to the IP address of your load balancer.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- You have installed the OpenShift CLI (`oc`).\n- You have configured a load balancer controller, such as MetalLB, for your cluster.\n- Your gateway has been assigned an external network address by the load balancer.\n- Your gateway is located in the `openshift-ingress` namespace.\n\n**Procedure**\n\n1. Retrieve the external IP address assigned to your gateway by the load balancer by running the following command:\n\n   ```terminal\n   $ oc -n openshift-ingress get gateway <gateway_name>\n   ```\n\n   Note the IP address listed in the `ADDRESS` column.\n2. Access your organization’s DNS provider or server.\n3. Create a DNS record, such as an A record or wildcard A record, that maps the listener’s hostname to the external IP address of your gateway.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/ingress_load_balancing/configuring_gateway_api/assigning-network-addresses-gateways.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="assigning-network-addresses-gateways" class="group scroll-mt-20">Assign network addresses to gateways<a href="#assigning-network-addresses-gateways" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">You can configure network addresses for your gateway to provide a predictable entry point for external and internal traffic. This ensures that clients can reliably resolve and route requests to your load balancers.</p>
      <p>Gateway API uses addresses to define the specific network locations that are assigned to your <code>Gateway</code> resource. In OpenShift Container Platform, you rely on the gateway controller to automatically provision and bind the necessary network addresses, such as an external or internal load balancer IP, to your gateway. On on-premise environments, this automatic provisioning requires a configured load balancer controller.</p>
      <p>To successfully assign network addresses to your gateway, complete the following tasks:</p>
      <ul>
      <li>Understand gateway address assignment and types to plan your DNS and load balancer configuration.</li>
      <li>Understand on-premise gateway routing requirements to ensure your infrastructure can support Gateway API.</li>
      <li>Configure automatic address assignment for a gateway to successfully deploy it without violating manual address constraints.</li>
      <li>Configure an internal load balancer to restrict your gateway traffic to your private network.</li>
      <li>Review cloud provider annotations to ensure your internal load balancer provisions correctly on your specific infrastructure.</li>
      <li>Configure DNS for on-premise gateways to ensure clients can reliably resolve your gateway.</li>
      </ul>
      <h2 id="understand-gateway-address-assignment_assigning-network-addresses-gateways" class="group scroll-mt-20">Understand gateway address assignment and types<a href="#understand-gateway-address-assignment_assigning-network-addresses-gateways" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">OpenShift Container Platform automatically handles address assignment by provisioning a <code>LoadBalancer</code> service when you create a <code>Gateway</code> resource. The network address assigned to your gateway corresponds to the IP address or hostname of this underlying load balancer.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Do not define the <code>spec.addresses</code> field. Manually requesting specific network addresses is not currently supported in OpenShift Container Platform. If you attempt to request a specific address manually, the gateway enters an error state.</p>
          <p>The <code>status.addresses</code> field is populated automatically by the gateway controller. This field lists the actual, active network address assigned to your gateway by the load balancing infrastructure.</p>
        </AlertDescription>
      </Alert>
      <h3 id="_address_types" class="group scroll-mt-20">Address types<a href="#_address_types" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>When the controller dynamically assigns an address to your gateway and populates the <code>status.addresses</code> field, it uses one of the following primary types to reflect the underlying load balancer:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>Hostname</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Represents a DNS-based ingress point. This concept is typically used for cloud load balancers where a DNS name exposes the load balancer.</p></dd>
      <dt class="font-semibold"><code>IPAddress</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>A textual representation of a numeric IP address (IPv4 or IPv6) assigned by the load balancing infrastructure.</p></dd>
      </dl>
      <h2 id="on-premise-gateway-routing-requirements_assigning-network-addresses-gateways" class="group scroll-mt-20">On-premise gateway routing requirements<a href="#on-premise-gateway-routing-requirements_assigning-network-addresses-gateways" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Understand the specific routing and load balancing requirements for on-premise Gateway API deployments to ensure your gateway functions correctly.</p>
      <p>Unlike cloud environments where load balancers are dynamically provisioned, on-premise clusters require a preconfigured load balancer controller. Red Hat tests and certifies Gateway API on on-premise platforms specifically with MetalLB.</p>
      <Alert variant="warning" class="my-4">
        <AlertTitle>Warning</AlertTitle>
        <AlertDescription>
          <p>If you attempt to use Gateway API on an on-premise cluster without a functional load balancer controller, the gateway service will remain in a "pending" state indefinitely.</p>
        </AlertDescription>
      </Alert>
      <p>Additionally, be aware of the following topology and load balancer limitations for on-premise environments:</p>
      <ul>
      <li>Third-party load balancers: Red Hat does not currently test Gateway API with third-party load balancers such as F5 or Avi Kubernetes Operator (AKO). If you use an untested load balancer, the cluster administrator is responsible for ensuring it is configured and working properly.</li>
      <li>Unsupported topologies: Environments without a load balancer controller are not supported. For example, you cannot use annotations to enforce a <code>NodePort</code> service type in place of a load balancer.</li>
      </ul>
      <h2 id="configuring-automatic-address-assignment-gateway_assigning-network-addresses-gateways" class="group scroll-mt-20">Configure automatic address assignment for a gateway<a href="#configuring-automatic-address-assignment-gateway_assigning-network-addresses-gateways" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">When you create a gateway resource, you must configure it for automatic address provisioning to successfully deploy the gateway without violating OpenShift Container Platform manual address constraints. By intentionally omitting the addresses field, you allow the controller to seamlessly provision and bind the necessary external network addresses to your gateway.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      <li>You have an existing <code>GatewayClass</code> custom resource, such as <code>openshift-default</code>.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a YAML file, such as <code>hello-gateway.yaml</code>, that defines your <code>Gateway</code> object.
      <CodeBlock code={"apiVersion: gateway.networking.k8s.io/v1\nkind: Gateway\nmetadata:\n  name: sample-gateway\n  namespace: openshift-ingress\nspec:\n  gatewayClassName: openshift-default\n  listeners:\n  - name: http\n    hostname: \"*.gwapi.<cluster_domain>\"\n    port: 80\n    protocol: HTTP\n    allowedRoutes:\n      namespaces:\n        from: Selector\n        selector:\n          matchLabels:\n            shared-gateway-access: \"true\""} lang="yaml" />
      <ul>
      <li><code>metadata.name</code>: Specify the name of your <code>Gateway</code> object. The name must consist of a maximum of 63 lowercase alphanumeric characters or hyphens (<code>-</code>). The name must also start and end with an alphanumeric character.</li>
      <li><code>spec.gatewayClassName</code>: Specify the <code>GatewayClass</code> object whose controller provisions the address and populates the <code>status.addresses</code> field.</li>
      <li><code>spec.listeners[].hostname</code>: Specify the listener hostname. Replace <code>&lt;cluster_domain&gt;</code> with your actual cluster ingress domain (for example, <code>example.com</code>). Setting a hostname limits which route hostnames can match this listener.</li>
      <li><code>spec.listeners[].allowedRoutes.namespaces</code>: Allow route attachment only from namespaces that have the <code>shared-gateway-access: "true"</code> label.</li>
      </ul>
      </li>
      <li>
      Apply the <code>Gateway</code> configuration by running the following command:
      <CodeBlock code={"$ oc apply -f hello-gateway.yaml"} lang="terminal" />
      </li>
      <li>
      Verify that the controller automatically assigned an address to your gateway by running the following command:
      <CodeBlock code={"$ oc -n openshift-ingress get gateway sample-gateway"} lang="terminal" />
      <CodeBlock code={"NAME             CLASS               ADDRESS             PROGRAMMED   AGE\nsample-gateway   openshift-default   <gateway_address>   True         6m16s"} lang="terminal" title="Example output" />
      <p>The <code>ADDRESS</code> column in the output displays the dynamically provisioned network address for your gateway.</p>
      </li>
      </ol>
      <h2 id="configuring-internal-lb-gateway_assigning-network-addresses-gateways" class="group scroll-mt-20">Configure an internal load balancer for a gateway<a href="#configuring-internal-lb-gateway_assigning-network-addresses-gateways" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">By default, Gateway API provisions an external load balancer. To restrict your gateway traffic to your private network, you can configure Gateway API to provision an internal load balancer by adding a cloud-specific annotation to your <code>Gateway</code> custom resource (CR).</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      <li>You have configured a <code>GatewayClass</code> object.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create or edit your <code>Gateway</code> CR to include the cloud-specific annotation under <code>spec.infrastructure.annotations</code>.
      <p>The following example provisions an internal load balancer for an AWS cluster:</p>
      <CodeBlock code={"apiVersion: gateway.networking.k8s.io/v1\nkind: Gateway\nmetadata:\n  name: mygateway\n  namespace: openshift-ingress\nspec:\n  gatewayClassName: openshift-default\n  infrastructure:\n    annotations:\n    # Specifies the cloud provider annotation and value required to provision an internal load balancer:\n      service.beta.kubernetes.io/aws-load-balancer-internal: \"true\"\n  listeners:\n  - name: https\n    hostname: \"*.example.com\"\n    port: 443\n    protocol: HTTPS\n    tls:\n      mode: Terminate\n      certificateRefs:\n      - name: gateway-tls-secret\n# ..."} lang="yaml" title="Example Gateway CR for an AWS internal load balancer" />
      </li>
      <li>
      Apply the updated <code>Gateway</code> CR by running the following command:
      <CodeBlock code={"$ oc apply -f <gateway_filename>.yaml"} lang="terminal" />
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      Verify that the load balancer service is provisioned and has an internal IP address by running the following command:
      <CodeBlock code={"$ oc -n openshift-ingress get svc"} lang="terminal" />
      </li>
      </ul>
      <h3 id="internal-lb-annotations-reference_assigning-network-addresses-gateways" class="group scroll-mt-20">Cloud provider annotations for internal load balancers<a href="#internal-lb-annotations-reference_assigning-network-addresses-gateways" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">To provision an internal load balancer for clusters deployed in private environments, you must add specific annotations to the <code>spec.infrastructure.annotations</code> field of your <code>Gateway</code> custom resource (CR). </p>
      <p>This configuration is supported on Amazon Web Services (AWS), Microsoft Azure, Google Cloud, Red Hat OpenStack Platform (RHOSP), and IBM Cloud. The following table details the required cloud-specific annotations and their corresponding values.</p>
      <p><strong>Internal load balancer annotations by cloud provider</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Cloud Provider</TableHead>
      <TableHead>Annotation</TableHead>
      <TableHead>Value</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>AWS</TableCell>
      <TableCell><code>service.beta.kubernetes.io/aws-load-balancer-internal</code></TableCell>
      <TableCell><code>"true"</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Azure</TableCell>
      <TableCell><code>service.beta.kubernetes.io/azure-load-balancer-internal</code></TableCell>
      <TableCell><code>"true"</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Google Cloud</TableCell>
      <TableCell><code>cloud.google.com/load-balancer-type</code></TableCell>
      <TableCell><code>"Internal"</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell>RHOSP</TableCell>
      <TableCell><code>service.beta.kubernetes.io/openstack-internal-load-balancer</code></TableCell>
      <TableCell><code>"true"</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell>IBM Cloud/ IBM Power Virtual Server</TableCell>
      <TableCell><code>service.kubernetes.io/ibm-load-balancer-cloud-provider-ip-type</code></TableCell>
      <TableCell><code>"private"</code></TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="configuring-dns-on-premise-gateways_assigning-network-addresses-gateways" class="group scroll-mt-20">Configuring DNS for on-premise gateways<a href="#configuring-dns-on-premise-gateways_assigning-network-addresses-gateways" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Configure DNS records manually on on-premise environments to ensure clients can reliably resolve your gateway.</p>
      <p>Although the Ingress Operator automatically creates a <code>DNSRecord</code> custom resource (CR) using the hostname from the listener, this record is marked as "unmanaged" on on-premise platforms because the cluster Ingress Operator does not implement on-premise DNS providers. You must manually configure DNS records to point to the IP address of your load balancer.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      <li>You have configured a load balancer controller, such as MetalLB, for your cluster.</li>
      <li>Your gateway has been assigned an external network address by the load balancer.</li>
      <li>Your gateway is located in the <code>openshift-ingress</code> namespace.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Retrieve the external IP address assigned to your gateway by the load balancer by running the following command:
      <CodeBlock code={"$ oc -n openshift-ingress get gateway <gateway_name>"} lang="terminal" />
      <p>Note the IP address listed in the <code>ADDRESS</code> column.</p>
      </li>
      <li>Access your organization’s DNS provider or server.</li>
      <li>Create a DNS record, such as an A record or wildcard A record, that maps the listener’s hostname to the external IP address of your gateway.</li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
