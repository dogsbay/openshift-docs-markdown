---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/ingress_load_balancing/configuring_gateway_api/enable-gateway-api.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"enable-gateway-api_{{ context }}","text":"Enable Gateway API"},{"depth":2,"slug":"enable-gateway-api-ingress-operator_enable-gateway-api","text":"Enable Gateway API for the Ingress Operator"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/ingress_load_balancing/configuring_gateway_api/enable-gateway-api";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Enable Gateway API";
const description = "To route traffic using Gateway API, you must first enable the feature on your cluster. You can enable Gateway API by creating a GatewayClass custom…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/ingress_load_balancing","networking/ingress_load_balancing/configuring_gateway_api"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Enable Gateway API {#enable-gateway-api_{{ context }}}\n\nTo route traffic using Gateway API, you must first enable the feature on your cluster. You can enable Gateway API by creating a `GatewayClass` custom resource, which triggers the Ingress Operator to provision the necessary controller and components.\n\nAfter you successfully enable Gateway API, you can begin deploying gateways, assigning network addresses, and configuring listeners to control your network traffic flow.\n\n## Enable Gateway API for the Ingress Operator {#enable-gateway-api-ingress-operator_enable-gateway-api}\n\nTo configure Gateway API for use on your cluster, you must create a `GatewayClass` resource. During the creation of the `GatewayClass` resource, the Ingress Operator installs a lightweight Istio control plane, based on Red Hat OpenShift Service Mesh, in the `openshift-ingress` namespace.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Create a `GatewayClass` object:\n\n   1. Create a YAML file, `openshift-default.yaml`, that contains the following information:\n\n      ```yaml {title=\"Example GatewayClass CR\"}\n      apiVersion: gateway.networking.k8s.io/v1\n      kind: GatewayClass\n      metadata:\n        name: openshift-default\n      spec:\n        controllerName: openshift.io/gateway-controller/v1\n      ```\n\n      - `metadata.name`: The name of your `GatewayClass` object. The name must consist of a maximum of 63 lowercase alphanumeric characters or hyphens (`-`). The name must also start and end with an alphanumeric character.\n\n        > [!IMPORTANT]\n        > The `controllerName` value must be exactly as shown for the Ingress Operator to manage it. If you set this field to anything else, the Ingress Operator ignores the `GatewayClass` object and all associated `Gateway`, `GRPCRoute`, and `HTTPRoute` objects. The controller name is tied to the implementation of Gateway API in OpenShift Container Platform, and `openshift.io/gateway-controller/v1` is the only controller name allowed.\n   2. Run the following command to create the `GatewayClass` resource:\n\n      ```terminal\n      $ oc create -f openshift-default.yaml\n      ```\n\n      ```terminal {title=\"Example output\"}\n      gatewayclass.gateway.networking.k8s.io/openshift-default created\n      ```\n\n**Verification**\n\n- Verify that the `GatewayClass` CR has been accepted and the controller is successfully installed by running the following command:\n\n  ```terminal\n  $ oc get gatewayclass openshift-default -o yaml\n  ```\n\n  ```yaml {title=\"Example output\"}\n  # ...\n  status:\n    conditions:\n    - lastTransitionTime: \"2026-05-15T10:00:00Z\"\n      message: The GatewayClass has been accepted\n      reason: Accepted\n      status: \"True\"\n      type: Accepted\n    - lastTransitionTime: \"2026-05-15T10:00:00Z\"\n      message: Istio installed successfully\n      reason: Installed\n      status: \"True\"\n      type: ControllerInstalled\n    - lastTransitionTime: \"2026-05-15T10:00:00Z\"\n      message: Istio CRDs are being managed by cluster-ingress-operator\n      reason: ManagedByCIO\n      status: \"True\"\n      type: CRDsReady\n  # ...\n  ```\n\n  Inspect the `status.conditions` block in the output. A healthy `GatewayClass` CR reports a status of `True` for the `Accepted`, `ControllerInstalled`, and `CRDsReady` conditions.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/ingress_load_balancing/configuring_gateway_api/enable-gateway-api.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="enable-gateway-api_{{ context }}" class="group scroll-mt-20">Enable Gateway API<a href="#enable-gateway-api_{{ context }}" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>To route traffic using Gateway API, you must first enable the feature on your cluster. You can enable Gateway API by creating a <code>GatewayClass</code> custom resource, which triggers the Ingress Operator to provision the necessary controller and components.</p>
      <p>After you successfully enable Gateway API, you can begin deploying gateways, assigning network addresses, and configuring listeners to control your network traffic flow.</p>
      <h2 id="enable-gateway-api-ingress-operator_enable-gateway-api" class="group scroll-mt-20">Enable Gateway API for the Ingress Operator<a href="#enable-gateway-api-ingress-operator_enable-gateway-api" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To configure Gateway API for use on your cluster, you must create a <code>GatewayClass</code> resource. During the creation of the <code>GatewayClass</code> resource, the Ingress Operator installs a lightweight Istio control plane, based on Red Hat OpenShift Service Mesh, in the <code>openshift-ingress</code> namespace.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a <code>GatewayClass</code> object:
      <ol>
      <li>
      Create a YAML file, <code>openshift-default.yaml</code>, that contains the following information:
      <CodeBlock code={"apiVersion: gateway.networking.k8s.io/v1\nkind: GatewayClass\nmetadata:\n  name: openshift-default\nspec:\n  controllerName: openshift.io/gateway-controller/v1"} lang="yaml" title="Example GatewayClass CR" />
      <ul>
      <li>
      <code>metadata.name</code>: The name of your <code>GatewayClass</code> object. The name must consist of a maximum of 63 lowercase alphanumeric characters or hyphens (<code>-</code>). The name must also start and end with an alphanumeric character.
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>The <code>controllerName</code> value must be exactly as shown for the Ingress Operator to manage it. If you set this field to anything else, the Ingress Operator ignores the <code>GatewayClass</code> object and all associated <code>Gateway</code>, <code>GRPCRoute</code>, and <code>HTTPRoute</code> objects. The controller name is tied to the implementation of Gateway API in OpenShift Container Platform, and <code>openshift.io/gateway-controller/v1</code> is the only controller name allowed.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      </li>
      <li>
      Run the following command to create the <code>GatewayClass</code> resource:
      <CodeBlock code={"$ oc create -f openshift-default.yaml"} lang="terminal" />
      <CodeBlock code={"gatewayclass.gateway.networking.k8s.io/openshift-default created"} lang="terminal" title="Example output" />
      </li>
      </ol>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      Verify that the <code>GatewayClass</code> CR has been accepted and the controller is successfully installed by running the following command:
      <CodeBlock code={"$ oc get gatewayclass openshift-default -o yaml"} lang="terminal" />
      <CodeBlock code={"# ...\nstatus:\n  conditions:\n  - lastTransitionTime: \"2026-05-15T10:00:00Z\"\n    message: The GatewayClass has been accepted\n    reason: Accepted\n    status: \"True\"\n    type: Accepted\n  - lastTransitionTime: \"2026-05-15T10:00:00Z\"\n    message: Istio installed successfully\n    reason: Installed\n    status: \"True\"\n    type: ControllerInstalled\n  - lastTransitionTime: \"2026-05-15T10:00:00Z\"\n    message: Istio CRDs are being managed by cluster-ingress-operator\n    reason: ManagedByCIO\n    status: \"True\"\n    type: CRDsReady\n# ..."} lang="yaml" title="Example output" />
      <p>Inspect the <code>status.conditions</code> block in the output. A healthy <code>GatewayClass</code> CR reports a status of <code>True</code> for the <code>Accepted</code>, <code>ControllerInstalled</code>, and <code>CRDsReady</code> conditions.</p>
      </li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
