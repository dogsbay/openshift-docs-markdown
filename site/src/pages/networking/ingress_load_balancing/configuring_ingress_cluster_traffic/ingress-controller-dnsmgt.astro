---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/ingress_load_balancing/configuring_ingress_cluster_traffic/ingress-controller-dnsmgt.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"ingress-controller-dnsmgt","text":"Understanding DNS management policies"},{"depth":2,"slug":"creating-a-custom-ingress-controller_ingress-controller-dnsmgt","text":"Creating an Ingress Controller for manual DNS management"},{"depth":2,"slug":"modifying-an-existing-ingress-controller_ingress-controller-dnsmgt","text":"Modifying an existing Ingress Controller for manual DNS management"},{"depth":2,"slug":"configuring-ingress-controller-dns-management-additional-resources","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/ingress_load_balancing/configuring_ingress_cluster_traffic/ingress-controller-dnsmgt";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Understanding DNS management policies";
const description = "As a cluster administrator, when you create an Ingress Controller, the Operator manages the DNS records automatically. This approach has some limitations…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/ingress_load_balancing","networking/ingress_load_balancing/configuring_ingress_cluster_traffic"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Understanding DNS management policies {#ingress-controller-dnsmgt}\n\nAs a cluster administrator, when you create an Ingress Controller, the Operator manages the DNS records automatically. This approach has some limitations when the required DNS zone is different from the cluster DNS zone or when the DNS zone is hosted outside the cloud provider.\n\nThe following list details key aspects for a managed DNS management policy:\n\n- The Managed DNS management policy for Ingress Controllers ensures that the lifecycle of the wildcard DNS record on the cloud provider is automatically managed by the Operator. This is the default behavior.\n- When you change an Ingress Controller from `Managed` to `Unmanaged` DNS management policy, the Operator does not clean up the previous wildcard DNS record provisioned on the cloud.\n- When you change an Ingress Controller from `Unmanaged` to `Managed` DNS management policy, the Operator attempts to create the DNS record on the cloud provider if it does not exist or updates the DNS record if it already exists.\n\nThe following list details key aspects for a unmanaged DNS management policy:\n\n- The Unmanaged DNS management policy for Ingress Controllers ensures that the lifecycle of the wildcard DNS record on the cloud provider is not automatically managed; instead, it becomes the responsibility of the cluster administrator.\n\n  > [!NOTE]\n  > For Google Cloud installations, you can use a custom DNS solution. Refer to the `DNSRecord` CR for information on what you need to include in the DNS record. For more information, see [Enabling a user-managed DNS](/openshift-docs-markdown/installing/installing_gcp/installing-gcp-customizations#installation-gcp-enabling-user-managed-DNS_installing-gcp-customizations) and [Provisioning your own DNS records](/openshift-docs-markdown/installing/installing_gcp/installing-gcp-customizations#installation-gcp-provisioning-own-dns-records_installing-gcp-customizations).\n\n## Creating an Ingress Controller for manual DNS management {#creating-a-custom-ingress-controller_ingress-controller-dnsmgt}\n\nAs a cluster administrator, you can create a new custom Ingress Controller with the Unmanaged DNS management policy.\n\n**Prerequisites**\n\n- You have installed the OpenShift CLI (`oc`).\n- You are logged in as a user with `cluster-admin` privileges.\n\n**Procedure**\n\n1. Create an `IngressController` custom resource (CR) file named `sample-ingress.yaml` with the following content:\n\n   ```yaml\n   apiVersion: operator.openshift.io/v1\n   kind: IngressController\n   metadata:\n     namespace: openshift-ingress-operator\n     name: <name>\n   spec:\n     domain: <domain>\n     endpointPublishingStrategy:\n       type: LoadBalancerService\n       loadBalancer:\n         scope: External\n         dnsManagementPolicy: Unmanaged\n   ```\n\n   where:\n\n   `metadata.name`\n   :   Specify the `<name>` with a name for the `IngressController` object.\n\n   `spec.domain`\n   :   Specify the `domain` based on the DNS record that was created as a prerequisite.\n\n   `loadBalancer.scope`\n   :   Specify the `scope` as `External` to expose the load balancer externally. `loadBalancer.dnsManagementPolicy`: Specifies if the Ingress Controller is managing the lifecycle of the wildcard DNS record associated with the load balancer. The valid values are `Managed` and `Unmanaged`. The default value is `Managed`.\n2. Apply the manifest to create the `IngressController` object:\n\n   ```terminal\n   $ oc apply -f sample-ingress.yaml\n   ```\n3. Verify that the Ingress Controller was created with the correct policy by running the following command:\n\n   ```terminal\n   $ oc get ingresscontroller <name> -n openshift-ingress-operator -o=jsonpath={.spec.endpointPublishingStrategy.loadBalancer}\n   ```\n\n   Inspect the output and confirm that `dnsManagementPolicy` is set to `Unmanaged`.\n\n## Modifying an existing Ingress Controller for manual DNS management {#modifying-an-existing-ingress-controller_ingress-controller-dnsmgt}\n\nAs a cluster administrator, you can modify an existing Ingress Controller to manually manage the DNS record lifecycle.\n\n**Prerequisites**\n\n- You have installed the OpenShift CLI (`oc`).\n- You are logged in as a user with `cluster-admin` privileges.\n\n**Procedure**\n\n1. Modify the chosen Ingress Controller to set the `dnsManagementPolicy` parameter:\n\n   ```terminal\n   $ SCOPE=$(oc -n openshift-ingress-operator get ingresscontroller <name> -o=jsonpath=\"{.status.endpointPublishingStrategy.loadBalancer.scope}\")\n   ```\n\n   ```terminal\n   $ oc -n openshift-ingress-operator patch ingresscontrollers/default --type=merge --patch=\"{\\\"spec\\\":{\\\"endpointPublishingStrategy\\\":{\\\"type\\\":\\\"LoadBalancerService\\\",\\\"loadBalancer\\\":{\\\"dnsManagementPolicy\\\":\\\"Unmanaged\\\", \\\"scope\\\":\\\"${SCOPE}\\\"}}}}\"\n   ingresscontroller.operator.openshift.io/default patched\n   ```\n2. Verify that the Ingress Controller was modified correctly by running the following command:\n\n   ```terminal\n   $ oc get ingresscontroller <name> -n openshift-ingress-operator -o=jsonpath={.spec.endpointPublishingStrategy.loadBalancer}\n   ```\n\n   Inspect the output and confirm that `dnsManagementPolicy` is set to `Unmanaged`.\n\n## Additional resources {#configuring-ingress-controller-dns-management-additional-resources}\n\n- [Ingress Controller configuration parameters](/openshift-docs-markdown/networking/networking_operators/ingress-operator#nw-ingress-controller-configuration-parameters_configuring-ingress)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/ingress_load_balancing/configuring_ingress_cluster_traffic/ingress-controller-dnsmgt.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="ingress-controller-dnsmgt" class="group scroll-mt-20">Understanding DNS management policies<a href="#ingress-controller-dnsmgt" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>As a cluster administrator, when you create an Ingress Controller, the Operator manages the DNS records automatically. This approach has some limitations when the required DNS zone is different from the cluster DNS zone or when the DNS zone is hosted outside the cloud provider.</p>
      <p>The following list details key aspects for a managed DNS management policy:</p>
      <ul>
      <li>The Managed DNS management policy for Ingress Controllers ensures that the lifecycle of the wildcard DNS record on the cloud provider is automatically managed by the Operator. This is the default behavior.</li>
      <li>When you change an Ingress Controller from <code>Managed</code> to <code>Unmanaged</code> DNS management policy, the Operator does not clean up the previous wildcard DNS record provisioned on the cloud.</li>
      <li>When you change an Ingress Controller from <code>Unmanaged</code> to <code>Managed</code> DNS management policy, the Operator attempts to create the DNS record on the cloud provider if it does not exist or updates the DNS record if it already exists.</li>
      </ul>
      <p>The following list details key aspects for a unmanaged DNS management policy:</p>
      <ul>
      <li>
      The Unmanaged DNS management policy for Ingress Controllers ensures that the lifecycle of the wildcard DNS record on the cloud provider is not automatically managed; instead, it becomes the responsibility of the cluster administrator.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>For Google Cloud installations, you can use a custom DNS solution. Refer to the <code>DNSRecord</code> CR for information on what you need to include in the DNS record. For more information, see <a href="/openshift-docs-markdown/installing/installing_gcp/installing-gcp-customizations#installation-gcp-enabling-user-managed-DNS_installing-gcp-customizations">Enabling a user-managed DNS</a> and <a href="/openshift-docs-markdown/installing/installing_gcp/installing-gcp-customizations#installation-gcp-provisioning-own-dns-records_installing-gcp-customizations">Provisioning your own DNS records</a>.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      <h2 id="creating-a-custom-ingress-controller_ingress-controller-dnsmgt" class="group scroll-mt-20">Creating an Ingress Controller for manual DNS management<a href="#creating-a-custom-ingress-controller_ingress-controller-dnsmgt" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">As a cluster administrator, you can create a new custom Ingress Controller with the Unmanaged DNS management policy.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      <li>You are logged in as a user with <code>cluster-admin</code> privileges.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create an <code>IngressController</code> custom resource (CR) file named <code>sample-ingress.yaml</code> with the following content:
      <CodeBlock code={"apiVersion: operator.openshift.io/v1\nkind: IngressController\nmetadata:\n  namespace: openshift-ingress-operator\n  name: <name>\nspec:\n  domain: <domain>\n  endpointPublishingStrategy:\n    type: LoadBalancerService\n    loadBalancer:\n      scope: External\n      dnsManagementPolicy: Unmanaged"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>metadata.name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specify the <code>&lt;name&gt;</code> with a name for the <code>IngressController</code> object.</p></dd>
      <dt class="font-semibold"><code>spec.domain</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specify the <code>domain</code> based on the DNS record that was created as a prerequisite.</p></dd>
      <dt class="font-semibold"><code>loadBalancer.scope</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specify the <code>scope</code> as <code>External</code> to expose the load balancer externally. <code>loadBalancer.dnsManagementPolicy</code>: Specifies if the Ingress Controller is managing the lifecycle of the wildcard DNS record associated with the load balancer. The valid values are <code>Managed</code> and <code>Unmanaged</code>. The default value is <code>Managed</code>.</p></dd>
      </dl>
      </li>
      <li>
      Apply the manifest to create the <code>IngressController</code> object:
      <CodeBlock code={"$ oc apply -f sample-ingress.yaml"} lang="terminal" />
      </li>
      <li>
      Verify that the Ingress Controller was created with the correct policy by running the following command:
      <CodeBlock code={"$ oc get ingresscontroller <name> -n openshift-ingress-operator -o=jsonpath={.spec.endpointPublishingStrategy.loadBalancer}"} lang="terminal" />
      <p>Inspect the output and confirm that <code>dnsManagementPolicy</code> is set to <code>Unmanaged</code>.</p>
      </li>
      </ol>
      <h2 id="modifying-an-existing-ingress-controller_ingress-controller-dnsmgt" class="group scroll-mt-20">Modifying an existing Ingress Controller for manual DNS management<a href="#modifying-an-existing-ingress-controller_ingress-controller-dnsmgt" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">As a cluster administrator, you can modify an existing Ingress Controller to manually manage the DNS record lifecycle.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      <li>You are logged in as a user with <code>cluster-admin</code> privileges.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Modify the chosen Ingress Controller to set the <code>dnsManagementPolicy</code> parameter:
      <CodeBlock code={"$ SCOPE=$(oc -n openshift-ingress-operator get ingresscontroller <name> -o=jsonpath=\"{.status.endpointPublishingStrategy.loadBalancer.scope}\")"} lang="terminal" />
      <CodeBlock code={"$ oc -n openshift-ingress-operator patch ingresscontrollers/default --type=merge --patch=\"{\\\"spec\\\":{\\\"endpointPublishingStrategy\\\":{\\\"type\\\":\\\"LoadBalancerService\\\",\\\"loadBalancer\\\":{\\\"dnsManagementPolicy\\\":\\\"Unmanaged\\\", \\\"scope\\\":\\\"${SCOPE}\\\"}}}}\"\ningresscontroller.operator.openshift.io/default patched"} lang="terminal" />
      </li>
      <li>
      Verify that the Ingress Controller was modified correctly by running the following command:
      <CodeBlock code={"$ oc get ingresscontroller <name> -n openshift-ingress-operator -o=jsonpath={.spec.endpointPublishingStrategy.loadBalancer}"} lang="terminal" />
      <p>Inspect the output and confirm that <code>dnsManagementPolicy</code> is set to <code>Unmanaged</code>.</p>
      </li>
      </ol>
      <h2 id="configuring-ingress-controller-dns-management-additional-resources" class="group scroll-mt-20">Additional resources<a href="#configuring-ingress-controller-dns-management-additional-resources" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul><li><a href="/openshift-docs-markdown/networking/networking_operators/ingress-operator#nw-ingress-controller-configuration-parameters_configuring-ingress">Ingress Controller configuration parameters</a></li></ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
