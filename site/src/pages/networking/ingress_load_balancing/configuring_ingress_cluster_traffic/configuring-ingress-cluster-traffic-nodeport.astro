---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/ingress_load_balancing/configuring_ingress_cluster_traffic/configuring-ingress-cluster-traffic-nodeport.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"configuring-ingress-cluster-traffic-nodeport","text":"Configuring ingress cluster traffic by using a NodePort"},{"depth":2,"slug":"nw-using-nodeport_configuring-ingress-cluster-traffic-nodeport","text":"Using a NodePort to get traffic into the cluster"},{"depth":2,"slug":"nw-creating-project-and-service_configuring-ingress-cluster-traffic-nodeport","text":"Creating a project and service"},{"depth":2,"slug":"nw-exposing-service_configuring-ingress-cluster-traffic-nodeport","text":"Exposing the service by creating a route"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/ingress_load_balancing/configuring_ingress_cluster_traffic/configuring-ingress-cluster-traffic-nodeport";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring ingress cluster traffic by using a NodePort";
const description = "To enable external access to your application for specific networking requirements, expose a service by using a NodePort." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/ingress_load_balancing","networking/ingress_load_balancing/configuring_ingress_cluster_traffic"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring ingress cluster traffic by using a NodePort {#configuring-ingress-cluster-traffic-nodeport}\n\nTo enable external access to your application for specific networking requirements, expose a service by using a `NodePort`.\n\nThis configuration opens a specific port on every node in the cluster, allowing external traffic to reach your workloads by using an IP address of any node.\n\nOpenShift Container Platform provides methods for communicating from outside the cluster with services running in the cluster. This method uses a `NodePort`.\n\nBefore starting the following procedures, the administrator must complete the following prerequisite tasks:\n\n- Set up the external port to the cluster networking environment so that requests can reach the cluster.\n- Have an OpenShift Container Platform cluster with at least one control plane node, at least one compute node, and a system outside the cluster that has network access to the cluster. This procedure assumes that the external system is on the same subnet as the cluster. The additional networking required for external systems on a different subnet is out-of-scope for this topic.\n- Make sure there is at least one user with cluster admin role. To add this role to a user, run the following command:\n\n  ```\n  $ oc adm policy add-cluster-role-to-user cluster-admin <user_name>\n  ```\n\n## Using a NodePort to get traffic into the cluster {#nw-using-nodeport_configuring-ingress-cluster-traffic-nodeport}\n\nUse a `NodePort`-type `Service` resource to expose a service on a specific port on all nodes in the cluster.\n\nThe port is specified in the `Service` resource’s `.spec.ports[*].nodePort` parameter\n\n> [!IMPORTANT]\n> Using a node port requires additional port resources.\n\nA `NodePort` exposes the service on a static port on the IP address of a node. A `NodePort` spans the `30000` to `32767` IP address ranges by default, which means a `NodePort` is unlikely to match the intended port of a service. For example, port `8080` might be exposed as port `31020` on the node.\n\nThe administrator must ensure the external IP addresses are routed to the nodes.\n\nA `NodePort` and external IPs are independent and both can be used concurrently.\n\n## Creating a project and service {#nw-creating-project-and-service_configuring-ingress-cluster-traffic-nodeport}\n\nIf the project and service that you want to expose does not exist, create the project and then create the service.\n\nIf the project and service already exists, skip to the procedure on exposing the service to create a route.\n\n**Prerequisites**\n\n- Install the OpenShift CLI (`oc`) and log in as a cluster administrator.\n\n**Procedure**\n\n1. Create a new project for your service by running the `oc new-project` command:\n\n   ```terminal\n   $ oc new-project <project_name>\n   ```\n2. Use the `oc new-app` command to create your service:\n\n   ```terminal\n   $ oc new-app nodejs:12~https://github.com/sclorg/nodejs-ex.git\n   ```\n3. To verify that the service was created, run the following command:\n\n   ```terminal\n   $ oc get svc -n <project_name>\n   ```\n\n   ```terminal {title=\"Example output\"}\n   NAME        TYPE        CLUSTER-IP       EXTERNAL-IP   PORT(S)    AGE\n   nodejs-ex   ClusterIP   172.30.197.157   <none>        8080/TCP   70s\n   ```\n\n   > [!NOTE]\n   > By default, the new service does not have an external IP address.\n\n## Exposing the service by creating a route {#nw-exposing-service_configuring-ingress-cluster-traffic-nodeport}\n\nTo enable external access to your application that runs on OpenShift Container Platform, you can expose the service as a route by using the `oc expose` command.\n\n**Prerequisites**\n\n- You logged into OpenShift Container Platform.\n\n**Procedure**\n\n1. Log in to the project where the service you want to expose is located:\n\n   ```terminal\n   $ oc project <project_name>\n   ```\n2. To expose a node port for the application, modify the custom resource definition (CRD) of a service by entering the following command:\n\n   ```terminal\n   $ oc edit svc <service_name>\n   ```\n\n   ```yaml {title=\"Example output\"}\n   spec:\n     ports:\n     - name: 8443-tcp\n       nodePort: 30327\n       port: 8443\n       protocol: TCP\n       targetPort: 8443\n     sessionAffinity: None\n     type: NodePort\n   ```\n\n   - `nodePort`: Optional parameter. Specifies the node port range for the application. By default, OpenShift Container Platform selects an available port in the `30000-32767` range.\n   - `type`: Specifies the service type.\n3. Optional: To confirm the service is available with a node port exposed, enter the following command:\n\n   ```terminal\n   $ oc get svc -n myproject\n   ```\n\n   ```terminal {title=\"Example output\"}\n   NAME                TYPE        CLUSTER-IP       EXTERNAL-IP   PORT(S)          AGE\n   nodejs-ex           ClusterIP   172.30.217.127   <none>        3306/TCP         9m44s\n   nodejs-ex-ingress   NodePort    172.30.107.72    <none>        3306:31345/TCP   39s\n   ```\n4. Optional: To remove the service created automatically by the `oc new-app` command, enter the following command:\n\n   ```terminal\n   $ oc delete svc nodejs-ex\n   ```\n\n**Verification**\n\n- To check that the service node port is updated with a port in the `30000-32767` range, enter the following command:\n\n  ```terminal\n  $ oc get svc\n  ```\n\n  In the following example output, the updated port is `30327`:\n\n  ```terminal {title=\"Example output\"}\n  NAME    TYPE       CLUSTER-IP      EXTERNAL-IP   PORT(S)          AGE\n  httpd   NodePort   172.xx.xx.xx    <none>        8443:30327/TCP   109s\n  ```\n\n**Additional resources**\n{._additional-resources}\n\n- [Configuring the node port service range](/openshift-docs-markdown/networking/configuring_network_settings/configuring-node-port-service-range#configuring-node-port-service-range)\n- [Adding a single NodePort service to an Ingress Controller](/openshift-docs-markdown/networking/ingress_load_balancing/configuring_ingress_cluster_traffic/nw-configuring-ingress-controller-endpoint-publishing-strategy#nw-ingress-controller-nodeportservice-projects_nw-configuring-ingress-controller-endpoint-publishing-strategy)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/ingress_load_balancing/configuring_ingress_cluster_traffic/configuring-ingress-cluster-traffic-nodeport.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="configuring-ingress-cluster-traffic-nodeport" class="group scroll-mt-20">Configuring ingress cluster traffic by using a NodePort<a href="#configuring-ingress-cluster-traffic-nodeport" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">To enable external access to your application for specific networking requirements, expose a service by using a <code>NodePort</code>. </p>
      <p>This configuration opens a specific port on every node in the cluster, allowing external traffic to reach your workloads by using an IP address of any node.</p>
      <p>OpenShift Container Platform provides methods for communicating from outside the cluster with services running in the cluster. This method uses a <code>NodePort</code>.</p>
      <p>Before starting the following procedures, the administrator must complete the following prerequisite tasks:</p>
      <ul>
      <li>Set up the external port to the cluster networking environment so that requests can reach the cluster.</li>
      <li>Have an OpenShift Container Platform cluster with at least one control plane node, at least one compute node, and a system outside the cluster that has network access to the cluster. This procedure assumes that the external system is on the same subnet as the cluster. The additional networking required for external systems on a different subnet is out-of-scope for this topic.</li>
      <li>
      Make sure there is at least one user with cluster admin role. To add this role to a user, run the following command:
      <CodeBlock code={"$ oc adm policy add-cluster-role-to-user cluster-admin <user_name>"} lang="plaintext" />
      </li>
      </ul>
      <h2 id="nw-using-nodeport_configuring-ingress-cluster-traffic-nodeport" class="group scroll-mt-20">Using a NodePort to get traffic into the cluster<a href="#nw-using-nodeport_configuring-ingress-cluster-traffic-nodeport" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Use a <code>NodePort</code>-type <code>Service</code> resource to expose a service on a specific port on all nodes in the cluster. </p>
      <p>The port is specified in the <code>Service</code> resource’s <code>.spec.ports[*].nodePort</code> parameter</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Using a node port requires additional port resources.</p>
        </AlertDescription>
      </Alert>
      <p>A <code>NodePort</code> exposes the service on a static port on the IP address of a node. A <code>NodePort</code> spans the <code>30000</code> to <code>32767</code> IP address ranges by default, which means a <code>NodePort</code> is unlikely to match the intended port of a service. For example, port <code>8080</code> might be exposed as port <code>31020</code> on the node.</p>
      <p>The administrator must ensure the external IP addresses are routed to the nodes.</p>
      <p>A <code>NodePort</code> and external IPs are independent and both can be used concurrently.</p>
      <h2 id="nw-creating-project-and-service_configuring-ingress-cluster-traffic-nodeport" class="group scroll-mt-20">Creating a project and service<a href="#nw-creating-project-and-service_configuring-ingress-cluster-traffic-nodeport" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">If the project and service that you want to expose does not exist, create the project and then create the service.</p>
      <p>If the project and service already exists, skip to the procedure on exposing the service to create a route.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>Install the OpenShift CLI (<code>oc</code>) and log in as a cluster administrator.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a new project for your service by running the <code>oc new-project</code> command:
      <CodeBlock code={"$ oc new-project <project_name>"} lang="terminal" />
      </li>
      <li>
      Use the <code>oc new-app</code> command to create your service:
      <CodeBlock code={"$ oc new-app nodejs:12~https://github.com/sclorg/nodejs-ex.git"} lang="terminal" />
      </li>
      <li>
      To verify that the service was created, run the following command:
      <CodeBlock code={"$ oc get svc -n <project_name>"} lang="terminal" />
      <CodeBlock code={"NAME        TYPE        CLUSTER-IP       EXTERNAL-IP   PORT(S)    AGE\nnodejs-ex   ClusterIP   172.30.197.157   <none>        8080/TCP   70s"} lang="terminal" title="Example output" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>By default, the new service does not have an external IP address.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <h2 id="nw-exposing-service_configuring-ingress-cluster-traffic-nodeport" class="group scroll-mt-20">Exposing the service by creating a route<a href="#nw-exposing-service_configuring-ingress-cluster-traffic-nodeport" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To enable external access to your application that runs on OpenShift Container Platform, you can expose the service as a route by using the <code>oc expose</code> command.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You logged into OpenShift Container Platform.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Log in to the project where the service you want to expose is located:
      <CodeBlock code={"$ oc project <project_name>"} lang="terminal" />
      </li>
      <li>
      To expose a node port for the application, modify the custom resource definition (CRD) of a service by entering the following command:
      <CodeBlock code={"$ oc edit svc <service_name>"} lang="terminal" />
      <CodeBlock code={"spec:\n  ports:\n  - name: 8443-tcp\n    nodePort: 30327\n    port: 8443\n    protocol: TCP\n    targetPort: 8443\n  sessionAffinity: None\n  type: NodePort"} lang="yaml" title="Example output" />
      <ul>
      <li><code>nodePort</code>: Optional parameter. Specifies the node port range for the application. By default, OpenShift Container Platform selects an available port in the <code>30000-32767</code> range.</li>
      <li><code>type</code>: Specifies the service type.</li>
      </ul>
      </li>
      <li>
      Optional: To confirm the service is available with a node port exposed, enter the following command:
      <CodeBlock code={"$ oc get svc -n myproject"} lang="terminal" />
      <CodeBlock code={"NAME                TYPE        CLUSTER-IP       EXTERNAL-IP   PORT(S)          AGE\nnodejs-ex           ClusterIP   172.30.217.127   <none>        3306/TCP         9m44s\nnodejs-ex-ingress   NodePort    172.30.107.72    <none>        3306:31345/TCP   39s"} lang="terminal" title="Example output" />
      </li>
      <li>
      Optional: To remove the service created automatically by the <code>oc new-app</code> command, enter the following command:
      <CodeBlock code={"$ oc delete svc nodejs-ex"} lang="terminal" />
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      To check that the service node port is updated with a port in the <code>30000-32767</code> range, enter the following command:
      <CodeBlock code={"$ oc get svc"} lang="terminal" />
      <p>In the following example output, the updated port is <code>30327</code>:</p>
      <CodeBlock code={"NAME    TYPE       CLUSTER-IP      EXTERNAL-IP   PORT(S)          AGE\nhttpd   NodePort   172.xx.xx.xx    <none>        8443:30327/TCP   109s"} lang="terminal" title="Example output" />
      </li>
      </ul>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/networking/configuring_network_settings/configuring-node-port-service-range#configuring-node-port-service-range">Configuring the node port service range</a></li>
      <li><a href="/openshift-docs-markdown/networking/ingress_load_balancing/configuring_ingress_cluster_traffic/nw-configuring-ingress-controller-endpoint-publishing-strategy#nw-ingress-controller-nodeportservice-projects_nw-configuring-ingress-controller-endpoint-publishing-strategy">Adding a single NodePort service to an Ingress Controller</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
