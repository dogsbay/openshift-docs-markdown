---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/multiple_networks/assigning-a-secondary-network-to-a-vrf.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"assigning-a-secondary-network-to-a-vrf","text":"Assigning a secondary network to a VRF"},{"depth":2,"slug":"cnf-creating-an-additional-network-attachment-with-the-cni-vrf-plug-in_assigning-a-secondary-network-to-a-vrf","text":"Creating a secondary network attachment with the CNI VRF plugin"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/multiple_networks/assigning-a-secondary-network-to-a-vrf";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Assigning a secondary network to a VRF";
const description = "As a cluster administrator, you can configure a secondary network for a virtual routing and forwarding (VRF) domain by using the CNI VRF plugin. The…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/multiple_networks"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Assigning a secondary network to a VRF {#assigning-a-secondary-network-to-a-vrf}\n\nAs a cluster administrator, you can configure a secondary network for a virtual routing and forwarding (VRF) domain by using the CNI VRF plugin. The virtual network that this plugin creates is associated with the physical interface that you specify.\n\nUsing a secondary network with a VRF instance has the following advantages:\n\nWorkload isolation\n:   Isolate workload traffic by configuring a VRF instance for the secondary network.\n\nImproved security\n:   Enable improved security through isolated network paths in the VRF domain.\n\nMulti-tenancy support\n:   Support multi-tenancy through network segmentation with a unique routing table in the VRF domain for each tenant.\n\n> [!NOTE]\n> Applications that use VRFs must bind to a specific device. The common usage is to use the `SO_BINDTODEVICE` option for a socket. The `SO_BINDTODEVICE` option binds the socket to the device that is specified in the passed interface name, for example, `eth1`. To use the `SO_BINDTODEVICE` option, the application must have `CAP_NET_RAW` capabilities.\n>\n> Using a VRF through the `ip vrf exec` command is not supported in OpenShift Container Platform pods. To use VRF, bind applications directly to the VRF interface.\n\n**Additional resources**\n{._additional-resources}\n\n- [About virtual routing and forwarding](/openshift-docs-markdown/networking/multiple_networks/about-virtual-routing-and-forwarding#cnf-about-virtual-routing-and-forwarding_about-virtual-routing-and-forwarding)\n\n## Creating a secondary network attachment with the CNI VRF plugin {#cnf-creating-an-additional-network-attachment-with-the-cni-vrf-plug-in_assigning-a-secondary-network-to-a-vrf}\n\nThe Cluster Network Operator (CNO) manages secondary network definitions. When you specify a secondary network in the cluster-scoped `Network` custom resource (CR), the CNO automatically creates the `NetworkAttachmentDefinition` CR.\n\n> [!NOTE]\n> Do not edit the `NetworkAttachmentDefinition` CRs that the Cluster Network Operator manages. Doing so might disrupt network traffic on your secondary network.\n\n**Prerequisites**\n\n- Install the OpenShift CLI (`oc`).\n- Log in to the cluster as a user with `cluster-admin` privileges.\n\n**Procedure**\n\n1. Create the `Network` CR for the additional network attachment and insert the `rawCNIConfig` configuration for the secondary network. Save as the `additional-network-attachment.yaml` file.\n\n   ```yaml\n   apiVersion: operator.openshift.io/v1\n   kind: Network\n   metadata:\n     name: cluster\n   spec:\n     additionalNetworks:\n       - name: test-network-1\n         namespace: additional-network-1\n         type: Raw\n         rawCNIConfig: '{\n           \"cniVersion\": \"0.3.1\",\n           \"name\": \"macvlan-vrf\",\n           \"plugins\": [\n           {\n             \"type\": \"macvlan\",\n             \"master\": \"eth1\",\n             \"ipam\": {\n                 \"type\": \"static\",\n                 \"addresses\": [\n                 {\n                     \"address\": \"191.168.1.23/24\"\n                 }\n                 ]\n             }\n           },\n           {\n             \"type\": \"vrf\",\n             \"vrfname\": \"vrf-1\",\n             \"table\": 1001\n           }]\n         }'\n   ```\n\n   where:\n\n   `plugins`\n   :   You must specify a list. The first item in the list must be the secondary network underpinning the VRF network. The second item in the list is the VRF plugin configuration.\n\n   `type`\n   :   You must set this parameter to `vrf`.\n\n   `vrfname`\n   :   The name of the VRF that the interface is assigned to. If the VRF does not exist in the pod, the CNI creates the VRF.\n\n   `table`\n   :   Optional parameter. Specify the routing table ID. By default, the `tableid` parameter is used. If you do not specify a table ID, the CNI assigns a free routing table ID to the VRF.\n\n   > [!NOTE]\n   > VRF functions correctly only when the resource is of type `netdevice`.\n2. Create the `Network` resource:\n\n   ```terminal\n   $ oc create -f additional-network-attachment.yaml\n   ```\n3. Confirm that the CNO created the `NetworkAttachmentDefinition` CR by running the following command. Replace `<namespace>` with the namespace that you specified when configuring the network attachment, for example, `additional-network-1`. The expected output shows the name of the NAD CR and the creation age in minutes.\n\n   ```terminal\n   $ oc get network-attachment-definitions -n <namespace>\n   ```\n\n   > [!NOTE]\n   > A delay might exist before the CNO creates the CR.\n\n**Verification**\n\n1. Create a pod and assign the pod to the secondary network that includes the VRF plugin configuration.\n\n   1. Create a YAML file that defines the `Pod` resource, as demonstrated in the following `pod-additional-net.yaml` file:\n\n      ```yaml\n      apiVersion: v1\n      kind: Pod\n      metadata:\n       name: pod-additional-net\n       annotations:\n         k8s.v1.cni.cncf.io/networks: '[\n             {\n                     \"name\": \"test-network-1\" (1)\n             }\n       ]'\n      spec:\n       containers:\n       - name: example-pod-1\n         command: [\"/bin/bash\", \"-c\", \"sleep 9000000\"]\n         image: centos:8\n      ```\n\n      where:\n\n      `name`\n      :   Specify the name of the secondary network that includes the VRF plugin configuration.\n   2. Create the `Pod` resource by running the following command. The expected output shows the name of the `Pod` resource and the creation age in minutes.\n\n      ```terminal\n      $ oc create -f pod-additional-net.yaml\n      ```\n2. Verify that the pod network attachment connects to the VRF secondary network. Start a remote session with the pod and run the following command. The expected output shows the name of the VRF interface and its unique ID in the routing table.\n\n   ```terminal\n   $ ip vrf show\n   ```\n3. Confirm that the VRF interface is the controller for the secondary interface by entering the following command:\n\n   ```terminal\n   $ ip link\n   ```\n\n   ```terminal\n   5: net1: <BROADCAST,MULTICAST,UP,LOWER_UP> mtu 1500 qdisc noqueue master red state UP mode\n   ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/multiple_networks/assigning-a-secondary-network-to-a-vrf.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="assigning-a-secondary-network-to-a-vrf" class="group scroll-mt-20">Assigning a secondary network to a VRF<a href="#assigning-a-secondary-network-to-a-vrf" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">As a cluster administrator, you can configure a secondary network for a virtual routing and forwarding (VRF) domain by using the CNI VRF plugin. The virtual network that this plugin creates is associated with the physical interface that you specify.</p>
      <p>Using a secondary network with a VRF instance has the following advantages:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Workload isolation</dt>
      <dd class="ml-6 text-muted-foreground"><p>Isolate workload traffic by configuring a VRF instance for the secondary network.</p></dd>
      <dt class="font-semibold">Improved security</dt>
      <dd class="ml-6 text-muted-foreground"><p>Enable improved security through isolated network paths in the VRF domain.</p></dd>
      <dt class="font-semibold">Multi-tenancy support</dt>
      <dd class="ml-6 text-muted-foreground"><p>Support multi-tenancy through network segmentation with a unique routing table in the VRF domain for each tenant.</p></dd>
      </dl>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Applications that use VRFs must bind to a specific device. The common usage is to use the <code>SO_BINDTODEVICE</code> option for a socket. The <code>SO_BINDTODEVICE</code> option binds the socket to the device that is specified in the passed interface name, for example, <code>eth1</code>. To use the <code>SO_BINDTODEVICE</code> option, the application must have <code>CAP_NET_RAW</code> capabilities.</p>
          <p>Using a VRF through the <code>ip vrf exec</code> command is not supported in OpenShift Container Platform pods. To use VRF, bind applications directly to the VRF interface.</p>
        </AlertDescription>
      </Alert>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="/openshift-docs-markdown/networking/multiple_networks/about-virtual-routing-and-forwarding#cnf-about-virtual-routing-and-forwarding_about-virtual-routing-and-forwarding">About virtual routing and forwarding</a></li></ul>
      </section>
      <h2 id="cnf-creating-an-additional-network-attachment-with-the-cni-vrf-plug-in_assigning-a-secondary-network-to-a-vrf" class="group scroll-mt-20">Creating a secondary network attachment with the CNI VRF plugin<a href="#cnf-creating-an-additional-network-attachment-with-the-cni-vrf-plug-in_assigning-a-secondary-network-to-a-vrf" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The Cluster Network Operator (CNO) manages secondary network definitions. When you specify a secondary network in the cluster-scoped <code>Network</code> custom resource (CR), the CNO automatically creates the <code>NetworkAttachmentDefinition</code> CR.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Do not edit the <code>NetworkAttachmentDefinition</code> CRs that the Cluster Network Operator manages. Doing so might disrupt network traffic on your secondary network.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>Install the OpenShift CLI (<code>oc</code>).</li>
      <li>Log in to the cluster as a user with <code>cluster-admin</code> privileges.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create the <code>Network</code> CR for the additional network attachment and insert the <code>rawCNIConfig</code> configuration for the secondary network. Save as the <code>additional-network-attachment.yaml</code> file.
      <CodeBlock code={"apiVersion: operator.openshift.io/v1\nkind: Network\nmetadata:\n  name: cluster\nspec:\n  additionalNetworks:\n    - name: test-network-1\n      namespace: additional-network-1\n      type: Raw\n      rawCNIConfig: '{\n        \"cniVersion\": \"0.3.1\",\n        \"name\": \"macvlan-vrf\",\n        \"plugins\": [\n        {\n          \"type\": \"macvlan\", \n          \"master\": \"eth1\",\n          \"ipam\": {\n              \"type\": \"static\",\n              \"addresses\": [\n              {\n                  \"address\": \"191.168.1.23/24\"\n              }\n              ]\n          }\n        },\n        {\n          \"type\": \"vrf\",\n          \"vrfname\": \"vrf-1\",\n          \"table\": 1001\n        }]\n      }'"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>plugins</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>You must specify a list. The first item in the list must be the secondary network underpinning the VRF network. The second item in the list is the VRF plugin configuration.</p></dd>
      <dt class="font-semibold"><code>type</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>You must set this parameter to <code>vrf</code>.</p></dd>
      <dt class="font-semibold"><code>vrfname</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The name of the VRF that the interface is assigned to. If the VRF does not exist in the pod, the CNI creates the VRF.</p></dd>
      <dt class="font-semibold"><code>table</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional parameter. Specify the routing table ID. By default, the <code>tableid</code> parameter is used. If you do not specify a table ID, the CNI assigns a free routing table ID to the VRF.</p></dd>
      </dl>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>VRF functions correctly only when the resource is of type <code>netdevice</code>.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      Create the <code>Network</code> resource:
      <CodeBlock code={"$ oc create -f additional-network-attachment.yaml"} lang="terminal" />
      </li>
      <li>
      Confirm that the CNO created the <code>NetworkAttachmentDefinition</code> CR by running the following command. Replace <code>&lt;namespace&gt;</code> with the namespace that you specified when configuring the network attachment, for example, <code>additional-network-1</code>. The expected output shows the name of the NAD CR and the creation age in minutes.
      <CodeBlock code={"$ oc get network-attachment-definitions -n <namespace>"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>A delay might exist before the CNO creates the CR.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>
      Create a pod and assign the pod to the secondary network that includes the VRF plugin configuration.
      <ol>
      <li>
      Create a YAML file that defines the <code>Pod</code> resource, as demonstrated in the following <code>pod-additional-net.yaml</code> file:
      <CodeBlock code={"apiVersion: v1\nkind: Pod\nmetadata:\n name: pod-additional-net\n annotations:\n   k8s.v1.cni.cncf.io/networks: '[\n       {\n               \"name\": \"test-network-1\" (1)\n       }\n ]'\nspec:\n containers:\n - name: example-pod-1\n   command: [\"/bin/bash\", \"-c\", \"sleep 9000000\"]\n   image: centos:8"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specify the name of the secondary network that includes the VRF plugin configuration.</p></dd>
      </dl>
      </li>
      <li>
      Create the <code>Pod</code> resource by running the following command. The expected output shows the name of the <code>Pod</code> resource and the creation age in minutes.
      <CodeBlock code={"$ oc create -f pod-additional-net.yaml"} lang="terminal" />
      </li>
      </ol>
      </li>
      <li>
      Verify that the pod network attachment connects to the VRF secondary network. Start a remote session with the pod and run the following command. The expected output shows the name of the VRF interface and its unique ID in the routing table.
      <CodeBlock code={"$ ip vrf show"} lang="terminal" />
      </li>
      <li>
      Confirm that the VRF interface is the controller for the secondary interface by entering the following command:
      <CodeBlock code={"$ ip link"} lang="terminal" />
      <CodeBlock code={"5: net1: <BROADCAST,MULTICAST,UP,LOWER_UP> mtu 1500 qdisc noqueue master red state UP mode"} lang="terminal" />
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
