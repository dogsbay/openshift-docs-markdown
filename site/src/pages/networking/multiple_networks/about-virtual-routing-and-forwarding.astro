---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/multiple_networks/about-virtual-routing-and-forwarding.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;

const headings = [{"depth":1,"slug":"virtual-routing-and-forwarding","text":"Virtual routing and forwarding"},{"depth":2,"slug":"cnf-about-virtual-routing-and-forwarding_about-virtual-routing-and-forwarding","text":"About virtual routing and forwarding"},{"depth":3,"slug":"cnf-benefits-secondary-networks-telco-ops_about-virtual-routing-and-forwarding","text":"Benefits of secondary networks for pods for telecommunications operators"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/multiple_networks/about-virtual-routing-and-forwarding";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Virtual routing and forwarding";
const description = "You can use virtual routing and forwarding (VRF) to provide multi-tenancy functionality. For example, where each tenant has its own unique routing tables…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/multiple_networks"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Virtual routing and forwarding {#virtual-routing-and-forwarding}\n\n## About virtual routing and forwarding {#cnf-about-virtual-routing-and-forwarding_about-virtual-routing-and-forwarding}\n\nYou can use virtual routing and forwarding (VRF) to provide multi-tenancy functionality. For example, where each tenant has its own unique routing tables and requires different default gateways.\n\nVRF reduces the number of permissions needed by cloud-native network function (CNF), and provides increased visibility of the network topology of secondary networks. VRF devices combined with IP address rules provide the ability to create virtual routing and forwarding domains.\n\nProcesses can bind a socket to the VRF device. Packets through the binded socket use the routing table associated with the VRF device. An important feature of VRF is that it impacts only OSI model layer 3 traffic and above so L2 tools, such as LLDP, are not affected. This allows higher priority IP address rules such as policy-based routing to take precedence over the VRF device rules directing specific traffic.\n\n### Benefits of secondary networks for pods for telecommunications operators {#cnf-benefits-secondary-networks-telco-ops_about-virtual-routing-and-forwarding}\n\nYou can connect network functions to different customers' infrastructure by using the same IP address with the Container Network Interface (CNI) virtual routing and forwarding (VRF) plugin. Using the CNI VRF plugin keeps different customers isolated.\n\nIn telecommunications use cases, each CNF can potentially be connected to many different networks sharing the same address space. These secondary networks can potentially conflict with the cluster’s main network CIDR.\n\nWith the CNI VRF plugin, IP addresses are overlapped with the OpenShift Container Platform IP address space. The CNI VRF plugin also reduces the number of permissions needed by CNF and increases the visibility of the network topologies of secondary networks.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/multiple_networks/about-virtual-routing-and-forwarding.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virtual-routing-and-forwarding" class="group scroll-mt-20">Virtual routing and forwarding<a href="#virtual-routing-and-forwarding" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <h2 id="cnf-about-virtual-routing-and-forwarding_about-virtual-routing-and-forwarding" class="group scroll-mt-20">About virtual routing and forwarding<a href="#cnf-about-virtual-routing-and-forwarding_about-virtual-routing-and-forwarding" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can use virtual routing and forwarding (VRF) to provide multi-tenancy functionality. For example, where each tenant has its own unique routing tables and requires different default gateways.</p>
      <p>VRF reduces the number of permissions needed by cloud-native network function (CNF), and provides increased visibility of the network topology of secondary networks. VRF devices combined with IP address rules provide the ability to create virtual routing and forwarding domains.</p>
      <p>Processes can bind a socket to the VRF device. Packets through the binded socket use the routing table associated with the VRF device. An important feature of VRF is that it impacts only OSI model layer 3 traffic and above so L2 tools, such as LLDP, are not affected. This allows higher priority IP address rules such as policy-based routing to take precedence over the VRF device rules directing specific traffic.</p>
      <h3 id="cnf-benefits-secondary-networks-telco-ops_about-virtual-routing-and-forwarding" class="group scroll-mt-20">Benefits of secondary networks for pods for telecommunications operators<a href="#cnf-benefits-secondary-networks-telco-ops_about-virtual-routing-and-forwarding" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can connect network functions to different customers' infrastructure by using the same IP address with the Container Network Interface (CNI) virtual routing and forwarding (VRF) plugin. Using the CNI VRF plugin keeps different customers isolated.</p>
      <p>In telecommunications use cases, each CNF can potentially be connected to many different networks sharing the same address space. These secondary networks can potentially conflict with the cluster’s main network CIDR.</p>
      <p>With the CNI VRF plugin, IP addresses are overlapped with the OpenShift Container Platform IP address space. The CNI VRF plugin also reduces the number of permissions needed by CNF and increases the visibility of the network topologies of secondary networks.</p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
