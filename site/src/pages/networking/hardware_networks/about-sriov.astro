---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/hardware_networks/about-sriov.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"about-sriov","text":"About Single Root I/O Virtualization (SR-IOV) hardware networks"},{"depth":2,"slug":"additional-resources-about-sr-iov_about-sriov","text":"Additional resources"},{"depth":2,"slug":"components-sr-iov-network-devices","text":"Components that manage SR-IOV network devices"},{"depth":2,"slug":"configure-multi-networks-additional-resources","text":"Additional resources"},{"depth":2,"slug":"about-sriov-next-steps","text":"Next steps"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/hardware_networks/about-sriov";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "About Single Root I/O Virtualization (SR-IOV) hardware networks";
const description = "To share a single physical device with multiple pods, implement the Single Root I/O Virtualization (SR-IOV) specification. This standard enables flexible…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/hardware_networks"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# About Single Root I/O Virtualization (SR-IOV) hardware networks {#about-sriov}\n\nTo share a single physical device with multiple pods, implement the Single Root I/O Virtualization (SR-IOV) specification. This standard enables flexible PCI device assignment, allowing a device to show as multiple separate physical devices for efficient resource allocation.\n\n> [!NOTE]\n> As of OpenShift Container Platform 4.21, the SR-IOV Operator can support ARM hardware.\n\nYou can configure a Single Root I/O Virtualization (SR-IOV) device in your cluster by using the [SR-IOV Operator](/openshift-docs-markdown/networking/networking_operators/sr-iov-operator/installing-sriov-operator#installing-sriov-operator).\n\nSR-IOV can segment a compliant network device, recognized on the host node as a physical function (PF), into multiple virtual functions (VFs). The VF is used like any other network device. The SR-IOV network device driver for the device determines how the VF is exposed in the container:\n\n- `netdevice` driver: A regular kernel network device in the `netns` of the container\n- `vfio-pci` driver: A character device mounted in the container\n\nYou can use SR-IOV network devices with additional networks on your OpenShift Container Platform cluster installed on bare metal or Red Hat OpenStack Platform (RHOSP) infrastructure for applications that require high bandwidth or low latency.\n\nThe SR-IOV Network Operator is supported on the following platforms:\n\n- Bare metal\n- Red Hat OpenStack Platform (RHOSP)\n\n> [!NOTE]\n> For a list of devices, such as network interface controllers (NICs) that OpenShift Container Platform supports, see [Red Hat certified hardware](https://catalog.redhat.com/en/hardware) on the Red Hat Ecosystem Catalog. The following example, finds the Intel X710 network adapter:\n>\n> 1. From the Red Hat certified hardware webpage, click **Explore** from the **Components** tile.\n> 2. From the **Provider** drop-down menu, click the **Intel Corporation** checkbox.\n> 3. From the **Platform** drop-down menu, click the **Red Hat OpenShift Container Platform** checkbox.\n> 4. Find the **Intel® Ethernet Server Adapter X710** network adapter from the list and then click on its tile. A new webpage opens that shows information for the network adapter.\n\nYou can configure multi-network policies for SR-IOV networks. The support for this is technology preview and SR-IOV additional networks are only supported with kernel NICs. They are not supported for Data Plane Development Kit (DPDK) applications.\n\n> [!NOTE]\n> Creating multi-network policies on SR-IOV networks might not deliver the same performance to applications compared to SR-IOV networks without a multi-network policy configured.\n\n> [!IMPORTANT]\n> Multi-network policies for SR-IOV network is a Technology Preview feature only. Technology Preview features are not supported with Red Hat production service level agreements (SLAs) and might not be functionally complete. Red Hat does not recommend using them in production. These features provide early access to upcoming product features, enabling customers to test functionality and provide feedback during the development process.\n>\n> For more information about the support scope of Red Hat Technology Preview features, see [Technology Preview Features Support Scope](https://access.redhat.com/support/offerings/techpreview/).\n\nYou can enable SR-IOV on a node by using the following command:\n\n```terminal\n$ oc label node <node_name> feature.node.kubernetes.io/network-sriov.capable=\"true\"\n```\n\n## Additional resources {#additional-resources-about-sr-iov_about-sriov}\n\n- [Installing the SR-IOV Network Operator](/openshift-docs-markdown/networking/networking_operators/sr-iov-operator/installing-sriov-operator#installing-sriov-operator)\n\n## Components that manage SR-IOV network devices {#components-sr-iov-network-devices}\n\nThe SR-IOV Network Operator creates and manages the components of the SR-IOV stack. The Operator performs the following functions:\n\n- Orchestrates discovery and management of SR-IOV network devices\n- Generates `NetworkAttachmentDefinition` custom resources for the SR-IOV Container Network Interface (CNI)\n- Creates and updates the configuration of the SR-IOV network device plugin\n- Creates node specific `SriovNetworkNodeState` custom resources\n- Updates the `spec.interfaces` field in each `SriovNetworkNodeState` custom resource\n\nThe Operator provisions the following components:\n\nSR-IOV network configuration daemon\n:   A daemon set that is deployed on worker nodes when the SR-IOV Network Operator starts. The daemon is responsible for discovering and initializing SR-IOV network devices in the cluster.\n\nSR-IOV Network Operator webhook\n:   A dynamic admission controller webhook that validates the Operator custom resource and sets appropriate default values for unset fields.\n\nSR-IOV Network resources injector\n:   A dynamic admission controller webhook that provides functionality for patching Kubernetes pod specifications with requests and limits for custom network resources such as SR-IOV VFs. The SR-IOV network resources injector adds the `resource` field to only the first container in a pod automatically.\n\nSR-IOV network device plugin\n:   A device plugin that discovers, advertises, and allocates SR-IOV network virtual function (VF) resources. Device plugins are used in Kubernetes to enable the use of limited resources, typically in physical devices. Device plugins give the Kubernetes scheduler awareness of resource availability, so that the scheduler can schedule pods on nodes with sufficient resources.\n\nSR-IOV CNI plugin\n:   A CNI plugin that attaches VF interfaces allocated from the SR-IOV network device plugin directly into a pod.\n\nSR-IOV InfiniBand CNI plugin\n:   A CNI plugin that attaches InfiniBand (IB) VF interfaces allocated from the SR-IOV network device plugin directly into a pod.\n\n> [!NOTE]\n> The SR-IOV Network resources injector and SR-IOV Network Operator webhook are enabled by default and can be disabled by editing the `default` `SriovOperatorConfig` CR. Use caution when disabling the SR-IOV Network Operator Admission Controller webhook. You can disable the webhook under specific circumstances, such as troubleshooting, or if you want to use unsupported devices.\n\n## Additional resources {#configure-multi-networks-additional-resources}\n\n- [Configuring multi-network policy](/openshift-docs-markdown/networking/multiple_networks/secondary_networks/configuring-multi-network-policy#configuring-multi-network-policy)\n\n## Next steps {#about-sriov-next-steps}\n\n- [Configuring the SR-IOV Network Operator](/openshift-docs-markdown/networking/networking_operators/sr-iov-operator/configuring-sriov-operator#configuring-sriov-operator)\n- [Configuring an SR-IOV network device](/openshift-docs-markdown/networking/hardware_networks/configuring-sriov-device#configuring-sriov-device)\n- If you use OpenShift Virtualization: [Connecting a virtual machine to an SR-IOV network](/openshift-docs-markdown/virt/vm_networking/virt-connecting-vm-to-sriov#virt-connecting-vm-to-sriov)\n- [Configuring an SR-IOV network attachment](/openshift-docs-markdown/networking/hardware_networks/configuring-sriov-net-attach#configuring-sriov-net-attach)\n- [Ethernet network attachement: Adding a pod to an SR-IOV additional network](/openshift-docs-markdown/networking/hardware_networks/configuring-sriov-net-attach#configuring-sriov-net-attach)\n- [InfiniBand network attachement: Adding a pod to an SR-IOV additional network](/openshift-docs-markdown/networking/hardware_networks/configuring-sriov-ib-attach#configuring-sriov-ib-attach)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/hardware_networks/about-sriov.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="about-sriov" class="group scroll-mt-20">About Single Root I/O Virtualization (SR-IOV) hardware networks<a href="#about-sriov" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>To share a single physical device with multiple pods, implement the Single Root I/O Virtualization (SR-IOV) specification. This standard enables flexible PCI device assignment, allowing a device to show as multiple separate physical devices for efficient resource allocation.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>As of OpenShift Container Platform 4.21, the SR-IOV Operator can support ARM hardware.</p>
        </AlertDescription>
      </Alert>
      <p>You can configure a Single Root I/O Virtualization (SR-IOV) device in your cluster by using the <a href="/openshift-docs-markdown/networking/networking_operators/sr-iov-operator/installing-sriov-operator#installing-sriov-operator">SR-IOV Operator</a>.</p>
      <p>SR-IOV can segment a compliant network device, recognized on the host node as a physical function (PF), into multiple virtual functions (VFs). The VF is used like any other network device. The SR-IOV network device driver for the device determines how the VF is exposed in the container:</p>
      <ul>
      <li><code>netdevice</code> driver: A regular kernel network device in the <code>netns</code> of the container</li>
      <li><code>vfio-pci</code> driver: A character device mounted in the container</li>
      </ul>
      <p>You can use SR-IOV network devices with additional networks on your OpenShift Container Platform cluster installed on bare metal or Red Hat OpenStack Platform (RHOSP) infrastructure for applications that require high bandwidth or low latency.</p>
      <p>The SR-IOV Network Operator is supported on the following platforms:</p>
      <ul>
      <li>Bare metal</li>
      <li>Red Hat OpenStack Platform (RHOSP)</li>
      </ul>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>For a list of devices, such as network interface controllers (NICs) that OpenShift Container Platform supports, see <a href="https://catalog.redhat.com/en/hardware">Red Hat certified hardware</a> on the Red Hat Ecosystem Catalog. The following example, finds the Intel X710 network adapter:</p>
          <ol>
          <li>From the Red Hat certified hardware webpage, click <strong>Explore</strong> from the <strong>Components</strong> tile.</li>
          <li>From the <strong>Provider</strong> drop-down menu, click the <strong>Intel Corporation</strong> checkbox.</li>
          <li>From the <strong>Platform</strong> drop-down menu, click the <strong>Red Hat OpenShift Container Platform</strong> checkbox.</li>
          <li>Find the <strong>Intel® Ethernet Server Adapter X710</strong> network adapter from the list and then click on its tile. A new webpage opens that shows information for the network adapter.</li>
          </ol>
        </AlertDescription>
      </Alert>
      <p>You can configure multi-network policies for SR-IOV networks. The support for this is technology preview and SR-IOV additional networks are only supported with kernel NICs. They are not supported for Data Plane Development Kit (DPDK) applications.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Creating multi-network policies on SR-IOV networks might not deliver the same performance to applications compared to SR-IOV networks without a multi-network policy configured.</p>
        </AlertDescription>
      </Alert>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Multi-network policies for SR-IOV network is a Technology Preview feature only. Technology Preview features are not supported with Red Hat production service level agreements (SLAs) and might not be functionally complete. Red Hat does not recommend using them in production. These features provide early access to upcoming product features, enabling customers to test functionality and provide feedback during the development process.</p>
          <p>For more information about the support scope of Red Hat Technology Preview features, see <a href="https://access.redhat.com/support/offerings/techpreview/">Technology Preview Features Support Scope</a>.</p>
        </AlertDescription>
      </Alert>
      <p>You can enable SR-IOV on a node by using the following command:</p>
      <CodeBlock code={"$ oc label node <node_name> feature.node.kubernetes.io/network-sriov.capable=\"true\""} lang="terminal" />
      <h2 id="additional-resources-about-sr-iov_about-sriov" class="group scroll-mt-20">Additional resources<a href="#additional-resources-about-sr-iov_about-sriov" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul><li><a href="/openshift-docs-markdown/networking/networking_operators/sr-iov-operator/installing-sriov-operator#installing-sriov-operator">Installing the SR-IOV Network Operator</a></li></ul>
      <h2 id="components-sr-iov-network-devices" class="group scroll-mt-20">Components that manage SR-IOV network devices<a href="#components-sr-iov-network-devices" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>The SR-IOV Network Operator creates and manages the components of the SR-IOV stack. The Operator performs the following functions:</p>
      <ul>
      <li>Orchestrates discovery and management of SR-IOV network devices</li>
      <li>Generates <code>NetworkAttachmentDefinition</code> custom resources for the SR-IOV Container Network Interface (CNI)</li>
      <li>Creates and updates the configuration of the SR-IOV network device plugin</li>
      <li>Creates node specific <code>SriovNetworkNodeState</code> custom resources</li>
      <li>Updates the <code>spec.interfaces</code> field in each <code>SriovNetworkNodeState</code> custom resource</li>
      </ul>
      <p>The Operator provisions the following components:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">SR-IOV network configuration daemon</dt>
      <dd class="ml-6 text-muted-foreground"><p>A daemon set that is deployed on worker nodes when the SR-IOV Network Operator starts. The daemon is responsible for discovering and initializing SR-IOV network devices in the cluster.</p></dd>
      <dt class="font-semibold">SR-IOV Network Operator webhook</dt>
      <dd class="ml-6 text-muted-foreground"><p>A dynamic admission controller webhook that validates the Operator custom resource and sets appropriate default values for unset fields.</p></dd>
      <dt class="font-semibold">SR-IOV Network resources injector</dt>
      <dd class="ml-6 text-muted-foreground"><p>A dynamic admission controller webhook that provides functionality for patching Kubernetes pod specifications with requests and limits for custom network resources such as SR-IOV VFs. The SR-IOV network resources injector adds the <code>resource</code> field to only the first container in a pod automatically.</p></dd>
      <dt class="font-semibold">SR-IOV network device plugin</dt>
      <dd class="ml-6 text-muted-foreground"><p>A device plugin that discovers, advertises, and allocates SR-IOV network virtual function (VF) resources. Device plugins are used in Kubernetes to enable the use of limited resources, typically in physical devices. Device plugins give the Kubernetes scheduler awareness of resource availability, so that the scheduler can schedule pods on nodes with sufficient resources.</p></dd>
      <dt class="font-semibold">SR-IOV CNI plugin</dt>
      <dd class="ml-6 text-muted-foreground"><p>A CNI plugin that attaches VF interfaces allocated from the SR-IOV network device plugin directly into a pod.</p></dd>
      <dt class="font-semibold">SR-IOV InfiniBand CNI plugin</dt>
      <dd class="ml-6 text-muted-foreground"><p>A CNI plugin that attaches InfiniBand (IB) VF interfaces allocated from the SR-IOV network device plugin directly into a pod.</p></dd>
      </dl>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The SR-IOV Network resources injector and SR-IOV Network Operator webhook are enabled by default and can be disabled by editing the <code>default</code> <code>SriovOperatorConfig</code> CR. Use caution when disabling the SR-IOV Network Operator Admission Controller webhook. You can disable the webhook under specific circumstances, such as troubleshooting, or if you want to use unsupported devices.</p>
        </AlertDescription>
      </Alert>
      <h2 id="configure-multi-networks-additional-resources" class="group scroll-mt-20">Additional resources<a href="#configure-multi-networks-additional-resources" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul><li><a href="/openshift-docs-markdown/networking/multiple_networks/secondary_networks/configuring-multi-network-policy#configuring-multi-network-policy">Configuring multi-network policy</a></li></ul>
      <h2 id="about-sriov-next-steps" class="group scroll-mt-20">Next steps<a href="#about-sriov-next-steps" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/networking/networking_operators/sr-iov-operator/configuring-sriov-operator#configuring-sriov-operator">Configuring the SR-IOV Network Operator</a></li>
      <li><a href="/openshift-docs-markdown/networking/hardware_networks/configuring-sriov-device#configuring-sriov-device">Configuring an SR-IOV network device</a></li>
      <li>If you use OpenShift Virtualization: <a href="/openshift-docs-markdown/virt/vm_networking/virt-connecting-vm-to-sriov#virt-connecting-vm-to-sriov">Connecting a virtual machine to an SR-IOV network</a></li>
      <li><a href="/openshift-docs-markdown/networking/hardware_networks/configuring-sriov-net-attach#configuring-sriov-net-attach">Configuring an SR-IOV network attachment</a></li>
      <li><a href="/openshift-docs-markdown/networking/hardware_networks/configuring-sriov-net-attach#configuring-sriov-net-attach">Ethernet network attachement: Adding a pod to an SR-IOV additional network</a></li>
      <li><a href="/openshift-docs-markdown/networking/hardware_networks/configuring-sriov-ib-attach#configuring-sriov-ib-attach">InfiniBand network attachement: Adding a pod to an SR-IOV additional network</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
