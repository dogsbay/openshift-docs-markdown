---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/configuring_network_settings/configuring-cluster-network-range.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"configuring-cluster-network-range","text":"Configuring the cluster network range"},{"depth":2,"slug":"nw-cluster-network-range-edit_configuring-cluster-network-range","text":"Expanding the cluster network IP address range"},{"depth":2,"slug":"additional-resources_configuring-cluster-network-range","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/configuring_network_settings/configuring-cluster-network-range";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring the cluster network range";
const description = "To expand the cluster network range in OpenShift Container Platform to support more nodes and IP addresses, you can modify the cluster network CIDR mask…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/configuring_network_settings"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring the cluster network range {#configuring-cluster-network-range}\n\nTo expand the cluster network range in OpenShift Container Platform to support more nodes and IP addresses, you can modify the cluster network CIDR mask after cluster installation. This procedure requires the OVN-Kubernetes network plugin and provides more IP space for additional nodes.\n\nFor example, if you deployed a cluster and specified `10.128.0.0/19` as the cluster network range and a host prefix of `23`, you are limited to 16 nodes. You can expand that to 510 nodes by changing the CIDR mask on a cluster to `/14`.\n\nThe following limitations apply when modifying the cluster network IP address range:\n\n- The CIDR mask size specified must always be smaller than the currently configured CIDR mask size, because you can only increase IP space by adding more nodes to an installed cluster\n- The host prefix cannot be modified\n- Pods that are configured with an overridden default gateway must be recreated after the cluster network expands\n\n> [!IMPORTANT]\n> You cannot expand the service network CIDR range after installing the cluster, either directly or through the ServiceCIDR API. You must configure the service network CIDR during installation using the `install-config.yaml` file. To avoid service IP address exhaustion, ensure that your initial service network range is large enough to accommodate future growth.\n\n## Expanding the cluster network IP address range {#nw-cluster-network-range-edit_configuring-cluster-network-range}\n\nTo expand the cluster network IP address range in OpenShift Container Platform to support more nodes, you can modify the cluster network CIDR mask using the `oc patch` command.\n\n> [!NOTE]\n> This change requires rolling out a new Operator configuration across the cluster, and can take up to 30 minutes to take effect.\n>\n> For clusters configured with cluster proxy, expanding the cluster network range also triggers a MachineConfigPool update that reboots all nodes. Plan this operation during a maintenance window to avoid service disruption.\n\n**Prerequisites**\n\n- You have installed the OpenShift CLI (`oc`).\n- You have logged in to the cluster with a user with `cluster-admin` privileges.\n- You have ensured that the cluster uses the OVN-Kubernetes network plugin.\n\n**Procedure**\n\n1. To obtain the cluster network range and host prefix for your cluster, enter the following command:\n\n   ```terminal\n   $ oc get network.operator.openshift.io \\\n     -o jsonpath=\"{.items[0].spec.clusterNetwork}\"\n   ```\n\n   ```text {title=\"Example output\"}\n   [{\"cidr\":\"10.217.0.0/22\",\"hostPrefix\":23}]\n   ```\n2. To expand the cluster network IP address range, enter the following command. Use the CIDR IP address range and host prefix returned from the output of the previous command.\n\n   ```terminal\n   $ oc patch Network.config.openshift.io cluster --type='merge' --patch \\\n     '{\n       \"spec\":{\n         \"clusterNetwork\": [ {\"cidr\":\"<network>/<cidr>\",\"hostPrefix\":<prefix>} ],\n         \"networkType\": \"OVNKubernetes\"\n       }\n     }'\n   ```\n\n   where:\n\n   `<network>`\n   :   Specifies the network part of the `cidr` field that you obtained from the previous step. You cannot change this value.\n\n   `<cidr>`\n   :   Specifies the network prefix length. For example, `14`. Change this value to a smaller number than the value from the output in the previous step to expand the cluster network range.\n\n   `<prefix>`\n   :   Specifies the current host prefix for your cluster. This value must be the same value for the `hostPrefix` field that you obtained from the previous step.\n\n   ```terminal {title=\"Example command\"}\n   $ oc patch Network.config.openshift.io cluster --type='merge' --patch \\\n     '{\n       \"spec\":{\n         \"clusterNetwork\": [ {\"cidr\":\"10.217.0.0/14\",\"hostPrefix\": 23} ],\n         \"networkType\": \"OVNKubernetes\"\n       }\n     }'\n   ```\n\n   ```text {title=\"Example output\"}\n   network.config.openshift.io/cluster patched\n   ```\n3. To confirm that the configuration is active, enter the following command. It can take up to 30 minutes for this change to take effect.\n\n   ```terminal\n   $ oc get network.operator.openshift.io \\\n     -o jsonpath=\"{.items[0].spec.clusterNetwork}\"\n   ```\n\n   ```text {title=\"Example output\"}\n   [{\"cidr\":\"10.217.0.0/14\",\"hostPrefix\":23}]\n   ```\n\n## Additional resources {#additional-resources_configuring-cluster-network-range}\n\n- [OVN-Kubernetes network plugin](/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/about-ovn-kubernetes#about-ovn-kubernetes)\n- [Red Hat OpenShift Network Calculator](https://access.redhat.com/labs/ocpnc/)\n- [About the OVN-Kubernetes network plugin](/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/about-ovn-kubernetes#about-ovn-kubernetes)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/configuring_network_settings/configuring-cluster-network-range.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="configuring-cluster-network-range" class="group scroll-mt-20">Configuring the cluster network range<a href="#configuring-cluster-network-range" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>To expand the cluster network range in OpenShift Container Platform to support more nodes and IP addresses, you can modify the cluster network CIDR mask after cluster installation. This procedure requires the OVN-Kubernetes network plugin and provides more IP space for additional nodes.</p>
      <p>For example, if you deployed a cluster and specified <code>10.128.0.0/19</code> as the cluster network range and a host prefix of <code>23</code>, you are limited to 16 nodes. You can expand that to 510 nodes by changing the CIDR mask on a cluster to <code>/14</code>.</p>
      <p>The following limitations apply when modifying the cluster network IP address range:</p>
      <ul>
      <li>The CIDR mask size specified must always be smaller than the currently configured CIDR mask size, because you can only increase IP space by adding more nodes to an installed cluster</li>
      <li>The host prefix cannot be modified</li>
      <li>Pods that are configured with an overridden default gateway must be recreated after the cluster network expands</li>
      </ul>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>You cannot expand the service network CIDR range after installing the cluster, either directly or through the ServiceCIDR API. You must configure the service network CIDR during installation using the <code>install-config.yaml</code> file. To avoid service IP address exhaustion, ensure that your initial service network range is large enough to accommodate future growth.</p>
        </AlertDescription>
      </Alert>
      <h2 id="nw-cluster-network-range-edit_configuring-cluster-network-range" class="group scroll-mt-20">Expanding the cluster network IP address range<a href="#nw-cluster-network-range-edit_configuring-cluster-network-range" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To expand the cluster network IP address range in OpenShift Container Platform to support more nodes, you can modify the cluster network CIDR mask using the <code>oc patch</code> command.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>This change requires rolling out a new Operator configuration across the cluster, and can take up to 30 minutes to take effect.</p>
          <p>For clusters configured with cluster proxy, expanding the cluster network range also triggers a MachineConfigPool update that reboots all nodes. Plan this operation during a maintenance window to avoid service disruption.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      <li>You have logged in to the cluster with a user with <code>cluster-admin</code> privileges.</li>
      <li>You have ensured that the cluster uses the OVN-Kubernetes network plugin.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      To obtain the cluster network range and host prefix for your cluster, enter the following command:
      <CodeBlock code={"$ oc get network.operator.openshift.io \\\n  -o jsonpath=\"{.items[0].spec.clusterNetwork}\""} lang="terminal" />
      <CodeBlock code={"[{\"cidr\":\"10.217.0.0/22\",\"hostPrefix\":23}]"} lang="text" title="Example output" />
      </li>
      <li>
      To expand the cluster network IP address range, enter the following command. Use the CIDR IP address range and host prefix returned from the output of the previous command.
      <CodeBlock code={"$ oc patch Network.config.openshift.io cluster --type='merge' --patch \\\n  '{\n    \"spec\":{\n      \"clusterNetwork\": [ {\"cidr\":\"<network>/<cidr>\",\"hostPrefix\":<prefix>} ],\n      \"networkType\": \"OVNKubernetes\"\n    }\n  }'"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;network&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the network part of the <code>cidr</code> field that you obtained from the previous step. You cannot change this value.</p></dd>
      <dt class="font-semibold"><code>&lt;cidr&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the network prefix length. For example, <code>14</code>. Change this value to a smaller number than the value from the output in the previous step to expand the cluster network range.</p></dd>
      <dt class="font-semibold"><code>&lt;prefix&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the current host prefix for your cluster. This value must be the same value for the <code>hostPrefix</code> field that you obtained from the previous step.</p></dd>
      </dl>
      <CodeBlock code={"$ oc patch Network.config.openshift.io cluster --type='merge' --patch \\\n  '{\n    \"spec\":{\n      \"clusterNetwork\": [ {\"cidr\":\"10.217.0.0/14\",\"hostPrefix\": 23} ],\n      \"networkType\": \"OVNKubernetes\"\n    }\n  }'"} lang="terminal" title="Example command" />
      <CodeBlock code={"network.config.openshift.io/cluster patched"} lang="text" title="Example output" />
      </li>
      <li>
      To confirm that the configuration is active, enter the following command. It can take up to 30 minutes for this change to take effect.
      <CodeBlock code={"$ oc get network.operator.openshift.io \\\n  -o jsonpath=\"{.items[0].spec.clusterNetwork}\""} lang="terminal" />
      <CodeBlock code={"[{\"cidr\":\"10.217.0.0/14\",\"hostPrefix\":23}]"} lang="text" title="Example output" />
      </li>
      </ol>
      <h2 id="additional-resources_configuring-cluster-network-range" class="group scroll-mt-20">Additional resources<a href="#additional-resources_configuring-cluster-network-range" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/about-ovn-kubernetes#about-ovn-kubernetes">OVN-Kubernetes network plugin</a></li>
      <li><a href="https://access.redhat.com/labs/ocpnc/">Red Hat OpenShift Network Calculator</a></li>
      <li><a href="/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/about-ovn-kubernetes#about-ovn-kubernetes">About the OVN-Kubernetes network plugin</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
