---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/network_security/egress_firewall/viewing-egress-firewall-ovn.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"viewing-egress-firewall-ovn","text":"Viewing an egress firewall for a project"},{"depth":2,"slug":"nw-egress-firewall-view_viewing-egress-firewall-ovn","text":"Viewing an EgressFirewall custom resource (CR)"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/network_security/egress_firewall/viewing-egress-firewall-ovn";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Viewing an egress firewall for a project";
const description = "As a cluster administrator, you can list the names of any existing egress firewalls and view the traffic rules for a specific egress firewall." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/network_security","networking/network_security/egress_firewall"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Viewing an egress firewall for a project {#viewing-egress-firewall-ovn}\n\nAs a cluster administrator, you can list the names of any existing egress firewalls and view the traffic rules for a specific egress firewall.\n\n## Viewing an EgressFirewall custom resource (CR) {#nw-egress-firewall-view_viewing-egress-firewall-ovn}\n\nYou can view an `EgressFirewall` CR in your cluster.\n\n**Prerequisites**\n\n- A cluster using the OVN-Kubernetes network plugin.\n- Install the OpenShift Command-line Interface (CLI), commonly known as `oc`.\n- You must log in to the cluster.\n\n**Procedure**\n\n1. Optional: To view the names of the `EgressFirewall` CR defined in your cluster, enter the following command:\n\n   ```terminal\n   $ oc get egressfirewall --all-namespaces\n   ```\n2. To inspect a policy, enter the following command. Replace `<policy_name>` with the name of the policy to inspect.\n\n   ```terminal\n   $ oc describe egressfirewall <policy_name>\n   ```\n\n   ```terminal {title=\"Example output\"}\n   Name:\t\tdefault\n   Namespace:\tproject1\n   Created:\t20 minutes ago\n   Labels:\t\t<none>\n   Annotations:\t<none>\n   Rule:\t\tAllow to 1.2.3.0/24\n   Rule:\t\tAllow to www.example.com\n   Rule:\t\tDeny to 0.0.0.0/0\n   ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/network_security/egress_firewall/viewing-egress-firewall-ovn.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="viewing-egress-firewall-ovn" class="group scroll-mt-20">Viewing an egress firewall for a project<a href="#viewing-egress-firewall-ovn" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>As a cluster administrator, you can list the names of any existing egress firewalls and view the traffic rules for a specific egress firewall.</p>
      <h2 id="nw-egress-firewall-view_viewing-egress-firewall-ovn" class="group scroll-mt-20">Viewing an EgressFirewall custom resource (CR)<a href="#nw-egress-firewall-view_viewing-egress-firewall-ovn" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can view an <code>EgressFirewall</code> CR in your cluster.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>A cluster using the OVN-Kubernetes network plugin.</li>
      <li>Install the OpenShift Command-line Interface (CLI), commonly known as <code>oc</code>.</li>
      <li>You must log in to the cluster.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Optional: To view the names of the <code>EgressFirewall</code> CR defined in your cluster, enter the following command:
      <CodeBlock code={"$ oc get egressfirewall --all-namespaces"} lang="terminal" />
      </li>
      <li>
      To inspect a policy, enter the following command. Replace <code>&lt;policy_name&gt;</code> with the name of the policy to inspect.
      <CodeBlock code={"$ oc describe egressfirewall <policy_name>"} lang="terminal" />
      <CodeBlock code={"Name:\t\tdefault\nNamespace:\tproject1\nCreated:\t20 minutes ago\nLabels:\t\t<none>\nAnnotations:\t<none>\nRule:\t\tAllow to 1.2.3.0/24\nRule:\t\tAllow to www.example.com\nRule:\t\tDeny to 0.0.0.0/0"} lang="terminal" title="Example output" />
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
