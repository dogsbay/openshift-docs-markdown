---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/network_security/AdminNetworkPolicy/ovn-k-anp.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"ovn-k-anp","text":"OVN-Kubernetes AdminNetworkPolicy"},{"depth":2,"slug":"adminnetworkpolicy_ovn-k-anp","text":"AdminNetworkPolicy"},{"depth":3,"slug":"adminnetworkpolicy-example_ovn-k-anp","text":"AdminNetworkPolicy example"},{"depth":3,"slug":"adminnetworkpolicy-actions-for-rules_ovn-k-anp","text":"AdminNetworkPolicy actions for rules"},{"depth":4,"slug":"adminnetworkpolicy-allow-example_ovn-k-anp","text":"AdminNetworkPolicy Allow example"},{"depth":4,"slug":"adminnetworkpolicy-deny-example_ovn-k-anp","text":"AdminNetworkPolicy Deny example"},{"depth":4,"slug":"adminnetworkpolicy-pass-example_ovn-k-anp","text":"AdminNetworkPolicy Pass example"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/network_security/AdminNetworkPolicy/ovn-k-anp";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "OVN-Kubernetes AdminNetworkPolicy";
const description = "In OpenShift Container Platform, you can configure AdminNetworkPolicy resources to enforce cluster-wide ingress and egress rules that namespace-scoped…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/network_security","networking/network_security/AdminNetworkPolicy"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# OVN-Kubernetes AdminNetworkPolicy {#ovn-k-anp}\n\nIn OpenShift Container Platform, you can configure `AdminNetworkPolicy` resources to enforce cluster-wide ingress and egress rules that namespace-scoped `NetworkPolicy` objects cannot override, which preserves administrative control over multi-tenant isolation and platform security.\n\n## AdminNetworkPolicy {#adminnetworkpolicy_ovn-k-anp}\n\nAn `AdminNetworkPolicy` (ANP) is a cluster-scoped custom resource definition (CRD) that you can define to establish ingress and egress rules before namespaces are created. You can use ANPs to ensure tenant `NetworkPolicy` objects cannot override your administrative controls.\n\nThe key difference between `AdminNetworkPolicy` and `NetworkPolicy` objects is that the former is for administrators and is cluster scoped, while the latter is for tenant owners and is namespace scoped.\n\nAn ANP allows administrators to specify the following:\n\n- A `priority` value that determines the order of its evaluation. The lower the value the higher the precedence.\n- A set of pods that consists of a set of namespaces or namespace on which the policy is applied.\n- A list of ingress rules to be applied for all ingress traffic towards the `subject`.\n- A list of egress rules to be applied for all egress traffic from the `subject`.\n\n### AdminNetworkPolicy example {#adminnetworkpolicy-example_ovn-k-anp}\n\n```yaml {title=\"Example YAML file for an ANP\"}\napiVersion: policy.networking.k8s.io/v1alpha1\nkind: AdminNetworkPolicy\nmetadata:\n  name: sample-anp-deny-pass-rules\nspec:\n  priority: 50\n  subject:\n    namespaces:\n      matchLabels:\n          kubernetes.io/metadata.name: example.name\n  ingress:\n  - name: \"deny-all-ingress-tenant-1\"\n    action: \"Deny\"\n    from:\n    - pods:\n        namespaceSelector:\n          matchLabels:\n            custom-anp: tenant-1\n        podSelector:\n          matchLabels:\n            custom-anp: tenant-1\n  egress:\n  - name: \"pass-all-egress-to-tenant-1\"\n    action: \"Pass\"\n    to:\n    - pods:\n        namespaceSelector:\n          matchLabels:\n            custom-anp: tenant-1\n        podSelector:\n          matchLabels:\n            custom-anp: tenant-1\n```\n\nwhere:\n\n`metadata.name`\n:   Specifies a name for your ANP.\n\n`spec.priority`\n:   Specifies the priority of the ANP. Supports a maximum of 100 ANPs in the range of values `0-99` in a cluster. The lower the value, the higher the precedence because the range is read in order from the lowest to highest value. Because there is no guarantee which policy takes precedence when ANPs are created at the same priority, set ANPs at different priorities so that precedence is deliberate.\n\n`spec.subject.namespaces.matchLabels`\n:   Specifies the namespace to apply the ANP resource.\n\n`spec.ingress.name`\n:   Specifies a name for the `ingress.name`.\n\n`spec.ingress.action`\n:   Specifies an ingress rule for the ANP. ANP have both ingress and egress rules. Accepts values of `Pass`, `Deny`, and `Allow`.\n\n`spec.ingress.from.pods.namespaceSelector.matchLabels`\n:   Specifies `podSelector.matchLabels` to select pods within the namespaces selected by `namespaceSelector.matchLabels` as ingress peers.\n\n`spec.egress.action`\n:   Specifies an egress rule for the ANP. Accepts values of `Pass`, `Deny`, and `Allow`.\n\n**Additional resources**\n\n- [Network Policy API Working Group](https://network-policy-api.sigs.k8s.io/)\n\n### AdminNetworkPolicy actions for rules {#adminnetworkpolicy-actions-for-rules_ovn-k-anp}\n\nYou can set the `action` field to `Allow`, `Deny`, or `Pass` on each `AdminNetworkPolicy`(ANP) custom resource rule so OVN-Kubernetes tiered ACLs apply administrative decisions before lower-tier networkpolicies can take effect.\n\nBecause OVN-Kubernetes uses tiered ACLs to evaluate network traffic rules, an ANP CR lets you define strong policy rules that can only be changed by an administrator modifying them, deleting the rule, or overriding them by setting a higher priority rule.\n\n#### AdminNetworkPolicy Allow example {#adminnetworkpolicy-allow-example_ovn-k-anp}\n\nThe following ANP that is defined at priority 9 ensures all ingress traffic is allowed from the `monitoring` namespace towards any tenant (all other namespaces) in the cluster.\n\n```yaml {title=\"Example YAML file for a strong Allow ANP\"}\napiVersion: policy.networking.k8s.io/v1alpha1\nkind: AdminNetworkPolicy\nmetadata:\n  name: allow-monitoring\nspec:\n  priority: 9\n  subject:\n    namespaces: {} # Use the empty selector with caution because it also selects OpenShift namespaces as well.\n  ingress:\n  - name: \"allow-ingress-from-monitoring\"\n    action: \"Allow\"\n    from:\n    - namespaces:\n        matchLabels:\n          kubernetes.io/metadata.name: monitoring\n# ...\n```\n\nThis is an example of a strong `Allow` ANP because it is non-overridable by all the parties involved. No tenants can block themselves from being monitored using `NetworkPolicy` objects and the monitoring tenant also has no say in what it can or cannot monitor.\n\n#### AdminNetworkPolicy Deny example {#adminnetworkpolicy-deny-example_ovn-k-anp}\n\nThe following ANP that is defined at priority 5 ensures all ingress traffic from the `monitoring` namespace is blocked towards restricted tenants (namespaces that have labels `security: restricted`).\n\n```yaml {title=\"Example YAML file for a strong Deny ANP\"}\napiVersion: policy.networking.k8s.io/v1alpha1\nkind: AdminNetworkPolicy\nmetadata:\n  name: block-monitoring\nspec:\n  priority: 5\n  subject:\n    namespaces:\n      matchLabels:\n        security: restricted\n  ingress:\n  - name: \"deny-ingress-from-monitoring\"\n    action: \"Deny\"\n    from:\n    - namespaces:\n        matchLabels:\n          kubernetes.io/metadata.name: monitoring\n# ...\n```\n\nThis is a strong `Deny` ANP that is non-overridable by all the parties involved. The restricted tenant owners cannot authorize themselves to allow monitoring traffic, and the infrastructure’s monitoring service cannot scrape anything from these sensitive namespaces.\n\nWhen combined with the strong `Allow` example, the `block-monitoring` ANP has a lower priority value giving it higher precedence, which ensures `restricted` tenants are never monitored.\n\n#### AdminNetworkPolicy Pass example {#adminnetworkpolicy-pass-example_ovn-k-anp}\n\nThe following ANP that is defined at priority 7 ensures all ingress traffic from the `monitoring` namespace towards internal infrastructure tenants (namespaces that have labels `security: internal`) are passed on to tier 2 of the ACLs and evaluated by the namespaces’ `NetworkPolicy` objects.\n\n```yaml {title=\"Example YAML file for a strong Pass ANP\"}\napiVersion: policy.networking.k8s.io/v1alpha1\nkind: AdminNetworkPolicy\nmetadata:\n  name: pass-monitoring\nspec:\n  priority: 7\n  subject:\n    namespaces:\n      matchLabels:\n        security: internal\n  ingress:\n  - name: \"pass-ingress-from-monitoring\"\n    action: \"Pass\"\n    from:\n    - namespaces:\n        matchLabels:\n          kubernetes.io/metadata.name: monitoring\n# ...\n```\n\nThis example is a strong `Pass` action ANP because it delegates the decision to `NetworkPolicy` objects defined by tenant owners. This `pass-monitoring` ANP allows all tenant owners grouped at security level `internal` to choose if their metrics should be scraped by the infrastructures' monitoring service using namespace scoped `NetworkPolicy` objects.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/network_security/AdminNetworkPolicy/ovn-k-anp.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="ovn-k-anp" class="group scroll-mt-20">OVN-Kubernetes AdminNetworkPolicy<a href="#ovn-k-anp" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">In OpenShift Container Platform, you can configure <code>AdminNetworkPolicy</code> resources to enforce cluster-wide ingress and egress rules that namespace-scoped <code>NetworkPolicy</code> objects cannot override, which preserves administrative control over multi-tenant isolation and platform security.</p>
      <h2 id="adminnetworkpolicy_ovn-k-anp" class="group scroll-mt-20">AdminNetworkPolicy<a href="#adminnetworkpolicy_ovn-k-anp" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">An <code>AdminNetworkPolicy</code> (ANP) is a cluster-scoped custom resource definition (CRD) that you can define to establish ingress and egress rules before namespaces are created. You can use ANPs to ensure tenant <code>NetworkPolicy</code> objects cannot override your administrative controls.</p>
      <p>The key difference between <code>AdminNetworkPolicy</code> and <code>NetworkPolicy</code> objects is that the former is for administrators and is cluster scoped, while the latter is for tenant owners and is namespace scoped.</p>
      <p>An ANP allows administrators to specify the following:</p>
      <ul>
      <li>A <code>priority</code> value that determines the order of its evaluation. The lower the value the higher the precedence.</li>
      <li>A set of pods that consists of a set of namespaces or namespace on which the policy is applied.</li>
      <li>A list of ingress rules to be applied for all ingress traffic towards the <code>subject</code>.</li>
      <li>A list of egress rules to be applied for all egress traffic from the <code>subject</code>.</li>
      </ul>
      <h3 id="adminnetworkpolicy-example_ovn-k-anp" class="group scroll-mt-20">AdminNetworkPolicy example<a href="#adminnetworkpolicy-example_ovn-k-anp" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <CodeBlock code={"apiVersion: policy.networking.k8s.io/v1alpha1\nkind: AdminNetworkPolicy\nmetadata:\n  name: sample-anp-deny-pass-rules\nspec:\n  priority: 50\n  subject:\n    namespaces:\n      matchLabels:\n          kubernetes.io/metadata.name: example.name\n  ingress:\n  - name: \"deny-all-ingress-tenant-1\"\n    action: \"Deny\"\n    from:\n    - pods:\n        namespaceSelector:\n          matchLabels:\n            custom-anp: tenant-1\n        podSelector:\n          matchLabels:\n            custom-anp: tenant-1\n  egress:\n  - name: \"pass-all-egress-to-tenant-1\"\n    action: \"Pass\"\n    to:\n    - pods:\n        namespaceSelector:\n          matchLabels:\n            custom-anp: tenant-1\n        podSelector:\n          matchLabels:\n            custom-anp: tenant-1"} lang="yaml" title="Example YAML file for an ANP" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>metadata.name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a name for your ANP.</p></dd>
      <dt class="font-semibold"><code>spec.priority</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the priority of the ANP. Supports a maximum of 100 ANPs in the range of values <code>0-99</code> in a cluster. The lower the value, the higher the precedence because the range is read in order from the lowest to highest value. Because there is no guarantee which policy takes precedence when ANPs are created at the same priority, set ANPs at different priorities so that precedence is deliberate.</p></dd>
      <dt class="font-semibold"><code>spec.subject.namespaces.matchLabels</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the namespace to apply the ANP resource.</p></dd>
      <dt class="font-semibold"><code>spec.ingress.name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a name for the <code>ingress.name</code>.</p></dd>
      <dt class="font-semibold"><code>spec.ingress.action</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies an ingress rule for the ANP. ANP have both ingress and egress rules. Accepts values of <code>Pass</code>, <code>Deny</code>, and <code>Allow</code>.</p></dd>
      <dt class="font-semibold"><code>spec.ingress.from.pods.namespaceSelector.matchLabels</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies <code>podSelector.matchLabels</code> to select pods within the namespaces selected by <code>namespaceSelector.matchLabels</code> as ingress peers.</p></dd>
      <dt class="font-semibold"><code>spec.egress.action</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies an egress rule for the ANP. Accepts values of <code>Pass</code>, <code>Deny</code>, and <code>Allow</code>.</p></dd>
      </dl>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="https://network-policy-api.sigs.k8s.io/">Network Policy API Working Group</a></li></ul>
      <h3 id="adminnetworkpolicy-actions-for-rules_ovn-k-anp" class="group scroll-mt-20">AdminNetworkPolicy actions for rules<a href="#adminnetworkpolicy-actions-for-rules_ovn-k-anp" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can set the <code>action</code> field to <code>Allow</code>, <code>Deny</code>, or <code>Pass</code> on each <code>AdminNetworkPolicy</code>(ANP) custom resource rule so OVN-Kubernetes tiered ACLs apply administrative decisions before lower-tier networkpolicies can take effect.</p>
      <p>Because OVN-Kubernetes uses tiered ACLs to evaluate network traffic rules, an ANP CR lets you define strong policy rules that can only be changed by an administrator modifying them, deleting the rule, or overriding them by setting a higher priority rule.</p>
      <h4 id="adminnetworkpolicy-allow-example_ovn-k-anp" class="group scroll-mt-20">AdminNetworkPolicy Allow example<a href="#adminnetworkpolicy-allow-example_ovn-k-anp" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h4>
      <p>The following ANP that is defined at priority 9 ensures all ingress traffic is allowed from the <code>monitoring</code> namespace towards any tenant (all other namespaces) in the cluster.</p>
      <CodeBlock code={"apiVersion: policy.networking.k8s.io/v1alpha1\nkind: AdminNetworkPolicy\nmetadata:\n  name: allow-monitoring\nspec:\n  priority: 9\n  subject:\n    namespaces: {} # Use the empty selector with caution because it also selects OpenShift namespaces as well.\n  ingress:\n  - name: \"allow-ingress-from-monitoring\"\n    action: \"Allow\"\n    from:\n    - namespaces:\n        matchLabels:\n          kubernetes.io/metadata.name: monitoring\n# ..."} lang="yaml" title="Example YAML file for a strong Allow ANP" />
      <p>This is an example of a strong <code>Allow</code> ANP because it is non-overridable by all the parties involved. No tenants can block themselves from being monitored using <code>NetworkPolicy</code> objects and the monitoring tenant also has no say in what it can or cannot monitor.</p>
      <h4 id="adminnetworkpolicy-deny-example_ovn-k-anp" class="group scroll-mt-20">AdminNetworkPolicy Deny example<a href="#adminnetworkpolicy-deny-example_ovn-k-anp" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h4>
      <p>The following ANP that is defined at priority 5 ensures all ingress traffic from the <code>monitoring</code> namespace is blocked towards restricted tenants (namespaces that have labels <code>security: restricted</code>).</p>
      <CodeBlock code={"apiVersion: policy.networking.k8s.io/v1alpha1\nkind: AdminNetworkPolicy\nmetadata:\n  name: block-monitoring\nspec:\n  priority: 5\n  subject:\n    namespaces:\n      matchLabels:\n        security: restricted\n  ingress:\n  - name: \"deny-ingress-from-monitoring\"\n    action: \"Deny\"\n    from:\n    - namespaces:\n        matchLabels:\n          kubernetes.io/metadata.name: monitoring\n# ..."} lang="yaml" title="Example YAML file for a strong Deny ANP" />
      <p>This is a strong <code>Deny</code> ANP that is non-overridable by all the parties involved. The restricted tenant owners cannot authorize themselves to allow monitoring traffic, and the infrastructure’s monitoring service cannot scrape anything from these sensitive namespaces.</p>
      <p>When combined with the strong <code>Allow</code> example, the <code>block-monitoring</code> ANP has a lower priority value giving it higher precedence, which ensures <code>restricted</code> tenants are never monitored.</p>
      <h4 id="adminnetworkpolicy-pass-example_ovn-k-anp" class="group scroll-mt-20">AdminNetworkPolicy Pass example<a href="#adminnetworkpolicy-pass-example_ovn-k-anp" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h4>
      <p>The following ANP that is defined at priority 7 ensures all ingress traffic from the <code>monitoring</code> namespace towards internal infrastructure tenants (namespaces that have labels <code>security: internal</code>) are passed on to tier 2 of the ACLs and evaluated by the namespaces’ <code>NetworkPolicy</code> objects.</p>
      <CodeBlock code={"apiVersion: policy.networking.k8s.io/v1alpha1\nkind: AdminNetworkPolicy\nmetadata:\n  name: pass-monitoring\nspec:\n  priority: 7\n  subject:\n    namespaces:\n      matchLabels:\n        security: internal\n  ingress:\n  - name: \"pass-ingress-from-monitoring\"\n    action: \"Pass\"\n    from:\n    - namespaces:\n        matchLabels:\n          kubernetes.io/metadata.name: monitoring\n# ..."} lang="yaml" title="Example YAML file for a strong Pass ANP" />
      <p>This example is a strong <code>Pass</code> action ANP because it delegates the decision to <code>NetworkPolicy</code> objects defined by tenant owners. This <code>pass-monitoring</code> ANP allows all tenant owners grouped at security level <code>internal</code> to choose if their metrics should be scraped by the infrastructures' monitoring service using namespace scoped <code>NetworkPolicy</code> objects.</p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
