---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/network_security/AdminNetworkPolicy/ovn-k-anp-recommended-practices.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";

const headings = [{"depth":1,"slug":"ovn-k-anp-recommended-practices","text":"Best practices for AdminNetworkPolicy"},{"depth":2,"slug":"anp-best-practices_ovn-k-anp-recommended-practices","text":"Designing AdminNetworkPolicy"},{"depth":3,"slug":"considerations-using-banp_ovn-k-anp-recommended-practices","text":"Considerations for using BaselineAdminNetworkPolicy"},{"depth":3,"slug":"differences-with-netpol_ovn-k-anp-recommended-practices","text":"Differences to consider between AdminNetworkPolicy and NetworkPolicy"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/network_security/AdminNetworkPolicy/ovn-k-anp-recommended-practices";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Best practices for AdminNetworkPolicy";
const description = "To apply cluster-wide network policy in OpenShift Container Platform, you can follow recommended practices for AdminNetworkPolicy and…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/network_security","networking/network_security/AdminNetworkPolicy"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Best practices for AdminNetworkPolicy {#ovn-k-anp-recommended-practices}\n\nTo apply cluster-wide network policy in OpenShift Container Platform, you can follow recommended practices for `AdminNetworkPolicy` and `BaselineAdminNetworkPolicy` design, including priorities, actions, and selectors that avoid system namespaces.\n\n## Designing AdminNetworkPolicy {#anp-best-practices_ovn-k-anp-recommended-practices}\n\nYou can use this reference when you design `AdminNetworkPolicy` and `BaselineAdminNetworkPolicy` resources in OpenShift Container Platform. It describes priority ranges, actions, selector rules, BANP patterns, and how these policies differ from `NetworkPolicy`.\n\nWhen building `AdminNetworkPolicy` (ANP) resources, you might consider the following when creating your policies:\n\n- You can create ANPs that have the same priority. If you do create two ANPs at the same priority, ensure that they do not apply overlapping rules to the same traffic. Only one rule per value is applied and there is no guarantee which rule is applied when there is more than one at the same priority value. Because there is no guarantee which policy takes precedence when overlapping ANPs are created, set ANPs at different priorities so that precedence is well defined.\n- Administrators must create ANPs that apply to user namespaces not system namespaces.\n\n> [!IMPORTANT]\n> Applying ANP and `BaselineAdminNetworkPolicy` (BANP) to system namespaces (`default`, `kube-system`, any namespace whose name starts with `openshift-`, etc) is not supported, and this can leave your cluster unresponsive and in a non-functional state.\n\n- Because `0-100` is the supported priority range, you might design your ANP to use a middle range like `30-70`. This leaves some placeholder for priorities before and after. Even in the middle range, you might want to leave gaps so that as your infrastructure requirements evolve over time, you are able to insert new ANPs when needed at the right priority level. If you pack your ANPs, then you might need to recreate all of them to accommodate any changes in the future.\n- When using `0.0.0.0/0` or `::/0` to create a strong `Deny` policy, ensure that you have higher priority `Allow` or `Pass` rules for essential traffic.\n- Use `Allow` as your `action` field when you want to ensure that a connection is allowed no matter what. An `Allow` rule in an ANP means that the connection will always be allowed, and `NetworkPolicy` will be ignored.\n- Use `Pass` as your `action` field to delegate the policy decision of allowing or denying the connection to the `NetworkPolicy` layer.\n- Ensure that the selectors across multiple rules do not overlap so that the same IPs do not appear in multiple policies, which can cause performance and scale limitations.\n- Avoid using `namedPorts` in conjunction with `PortNumber` and `PortRange` because this creates 6 ACLs and causes inefficiencies in your cluster.\n\n### Considerations for using BaselineAdminNetworkPolicy {#considerations-using-banp_ovn-k-anp-recommended-practices}\n\n- You can define only a single `BaselineAdminNetworkPolicy` (BANP) resource within a cluster. The following are supported uses for BANP that administrators might consider in designing their BANP:\n\n  - You can set a default deny policy for cluster-local ingress in user namespaces. This BANP will force developers to have to add `NetworkPolicy` objects to allow the ingress traffic that they want to allow, and if they do not add network policies for ingress it will be denied.\n  - You can set a default deny policy for cluster-local egress in user namespaces. This BANP will force developers to have to add `NetworkPolicy` objects to allow the egress traffic that they want to allow, and if they do not add network policies it will be denied.\n  - You can set a default allow policy for egress to the in-cluster DNS service. Such a BANP ensures that the namespaced users do not have to set an allow egress `NetworkPolicy` to the in-cluster DNS service.\n  - You can set an egress policy that allows internal egress traffic to all pods but denies access to all external endpoints (i.e `0.0.0.0/0` and `::/0`). This BANP allows user workloads to send traffic to other in-cluster endpoints, but not to external endpoints by default. `NetworkPolicy` can then be used by developers in order to allow their applications to send traffic to an explicit set of external services.\n- Ensure you scope your BANP so that it only denies traffic to user namespaces and not to system namespaces. This is because the system namespaces do not have `NetworkPolicy` objects to override your BANP.\n\n### Differences to consider between AdminNetworkPolicy and NetworkPolicy {#differences-with-netpol_ovn-k-anp-recommended-practices}\n\n- Unlike `NetworkPolicy` objects, you must use explicit labels to reference your workloads within ANP and BANP rather than using the empty (`{}`) catch all selector to avoid accidental traffic selection.\n\n> [!IMPORTANT]\n> An empty namespace selector applied to a infrastructure namespace can make your cluster unresponsive and in a non-functional state.\n\n- In API semantics for ANP, you have to explicitly define allow or deny rules when you create the policy, unlike `NetworkPolicy` objects which have an implicit deny.\n- Unlike `NetworkPolicy` objects, `AdminNetworkPolicy` objects ingress rules are limited to in-cluster pods and namespaces so you cannot, and do not need to, set rules for ingress from the host network.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/network_security/AdminNetworkPolicy/ovn-k-anp-recommended-practices.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="ovn-k-anp-recommended-practices" class="group scroll-mt-20">Best practices for AdminNetworkPolicy<a href="#ovn-k-anp-recommended-practices" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>To apply cluster-wide network policy in OpenShift Container Platform, you can follow recommended practices for <code>AdminNetworkPolicy</code> and <code>BaselineAdminNetworkPolicy</code> design, including priorities, actions, and selectors that avoid system namespaces.</p>
      <h2 id="anp-best-practices_ovn-k-anp-recommended-practices" class="group scroll-mt-20">Designing AdminNetworkPolicy<a href="#anp-best-practices_ovn-k-anp-recommended-practices" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can use this reference when you design <code>AdminNetworkPolicy</code> and <code>BaselineAdminNetworkPolicy</code> resources in OpenShift Container Platform. It describes priority ranges, actions, selector rules, BANP patterns, and how these policies differ from <code>NetworkPolicy</code>.</p>
      <p>When building <code>AdminNetworkPolicy</code> (ANP) resources, you might consider the following when creating your policies:</p>
      <ul>
      <li>You can create ANPs that have the same priority. If you do create two ANPs at the same priority, ensure that they do not apply overlapping rules to the same traffic. Only one rule per value is applied and there is no guarantee which rule is applied when there is more than one at the same priority value. Because there is no guarantee which policy takes precedence when overlapping ANPs are created, set ANPs at different priorities so that precedence is well defined.</li>
      <li>Administrators must create ANPs that apply to user namespaces not system namespaces.</li>
      </ul>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Applying ANP and <code>BaselineAdminNetworkPolicy</code> (BANP) to system namespaces (<code>default</code>, <code>kube-system</code>, any namespace whose name starts with <code>openshift-</code>, etc) is not supported, and this can leave your cluster unresponsive and in a non-functional state.</p>
        </AlertDescription>
      </Alert>
      <ul>
      <li>Because <code>0-100</code> is the supported priority range, you might design your ANP to use a middle range like <code>30-70</code>. This leaves some placeholder for priorities before and after. Even in the middle range, you might want to leave gaps so that as your infrastructure requirements evolve over time, you are able to insert new ANPs when needed at the right priority level. If you pack your ANPs, then you might need to recreate all of them to accommodate any changes in the future.</li>
      <li>When using <code>0.0.0.0/0</code> or <code>::/0</code> to create a strong <code>Deny</code> policy, ensure that you have higher priority <code>Allow</code> or <code>Pass</code> rules for essential traffic.</li>
      <li>Use <code>Allow</code> as your <code>action</code> field when you want to ensure that a connection is allowed no matter what. An <code>Allow</code> rule in an ANP means that the connection will always be allowed, and <code>NetworkPolicy</code> will be ignored.</li>
      <li>Use <code>Pass</code> as your <code>action</code> field to delegate the policy decision of allowing or denying the connection to the <code>NetworkPolicy</code> layer.</li>
      <li>Ensure that the selectors across multiple rules do not overlap so that the same IPs do not appear in multiple policies, which can cause performance and scale limitations.</li>
      <li>Avoid using <code>namedPorts</code> in conjunction with <code>PortNumber</code> and <code>PortRange</code> because this creates 6 ACLs and causes inefficiencies in your cluster.</li>
      </ul>
      <h3 id="considerations-using-banp_ovn-k-anp-recommended-practices" class="group scroll-mt-20">Considerations for using BaselineAdminNetworkPolicy<a href="#considerations-using-banp_ovn-k-anp-recommended-practices" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <ul>
      <li>
      You can define only a single <code>BaselineAdminNetworkPolicy</code> (BANP) resource within a cluster. The following are supported uses for BANP that administrators might consider in designing their BANP:
      <ul>
      <li>You can set a default deny policy for cluster-local ingress in user namespaces. This BANP will force developers to have to add <code>NetworkPolicy</code> objects to allow the ingress traffic that they want to allow, and if they do not add network policies for ingress it will be denied.</li>
      <li>You can set a default deny policy for cluster-local egress in user namespaces. This BANP will force developers to have to add <code>NetworkPolicy</code> objects to allow the egress traffic that they want to allow, and if they do not add network policies it will be denied.</li>
      <li>You can set a default allow policy for egress to the in-cluster DNS service. Such a BANP ensures that the namespaced users do not have to set an allow egress <code>NetworkPolicy</code> to the in-cluster DNS service.</li>
      <li>You can set an egress policy that allows internal egress traffic to all pods but denies access to all external endpoints (i.e <code>0.0.0.0/0</code> and <code>::/0</code>). This BANP allows user workloads to send traffic to other in-cluster endpoints, but not to external endpoints by default. <code>NetworkPolicy</code> can then be used by developers in order to allow their applications to send traffic to an explicit set of external services.</li>
      </ul>
      </li>
      <li>Ensure you scope your BANP so that it only denies traffic to user namespaces and not to system namespaces. This is because the system namespaces do not have <code>NetworkPolicy</code> objects to override your BANP.</li>
      </ul>
      <h3 id="differences-with-netpol_ovn-k-anp-recommended-practices" class="group scroll-mt-20">Differences to consider between AdminNetworkPolicy and NetworkPolicy<a href="#differences-with-netpol_ovn-k-anp-recommended-practices" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <ul><li>Unlike <code>NetworkPolicy</code> objects, you must use explicit labels to reference your workloads within ANP and BANP rather than using the empty (<code>{"{"}{"}"}</code>) catch all selector to avoid accidental traffic selection.</li></ul>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>An empty namespace selector applied to a infrastructure namespace can make your cluster unresponsive and in a non-functional state.</p>
        </AlertDescription>
      </Alert>
      <ul>
      <li>In API semantics for ANP, you have to explicitly define allow or deny rules when you create the policy, unlike <code>NetworkPolicy</code> objects which have an implicit deny.</li>
      <li>Unlike <code>NetworkPolicy</code> objects, <code>AdminNetworkPolicy</code> objects ingress rules are limited to in-cluster pods and namespaces so you cannot, and do not need to, set rules for ingress from the host network.</li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
