---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/network_security/network_policy/deleting-network-policy.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"deleting-network-policy","text":"Deleting a network policy"},{"depth":2,"slug":"nw-networkpolicy-delete-cli_deleting-network-policy","text":"Delete a network policy using the CLI"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/network_security/network_policy/deleting-network-policy";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Deleting a network policy";
const description = "As a cluster administrator, you can delete a network policy from a namespace." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/network_security","networking/network_security/network_policy"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Deleting a network policy {#deleting-network-policy}\n\nAs a cluster administrator, you can delete a network policy from a namespace.\n\n## Delete a network policy using the CLI {#nw-networkpolicy-delete-cli_deleting-network-policy}\n\nYou can delete a network policy in a namespace.\n\n> [!NOTE]\n> If you log in with `cluster-admin` privileges, you can delete network policies in any namespace in the cluster.\n\n> [!NOTE]\n> If you log in with `cluster-admin` privileges, you can delete network policies in any namespace in the cluster. In the web console, you can delete policies directly in YAML or by using the **Actions** menu.\n\n**Prerequisites**\n\n- Your cluster uses a network plugin that supports `NetworkPolicy` objects, such as the OVN-Kubernetes network plugin, with `mode: NetworkPolicy` set.\n- You installed the OpenShift CLI (`oc`).\n- You logged in to the cluster with a user with `{{ role }}` privileges.\n- You are working in the namespace where the network policy exists.\n\n**Procedure**\n\n- To delete a network policy object, enter the following command. Successful output lists the name of the policy object and the `deleted` status.\n\n  ```terminal\n  $ oc delete {{ name }}policy <policy_name> -n <namespace>\n  ```\n\n  where:\n\n  `<policy_name>`\n  :   Specifies the name of the network policy.\n\n  `<namespace>`\n  :   Optional parameter. If you defined the object in a different namespace than the current namespace, the parameter specifices the namespace.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/network_security/network_policy/deleting-network-policy.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="deleting-network-policy" class="group scroll-mt-20">Deleting a network policy<a href="#deleting-network-policy" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>As a cluster administrator, you can delete a network policy from a namespace.</p>
      <h2 id="nw-networkpolicy-delete-cli_deleting-network-policy" class="group scroll-mt-20">Delete a network policy using the CLI<a href="#nw-networkpolicy-delete-cli_deleting-network-policy" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can delete a network policy in a namespace.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If you log in with <code>cluster-admin</code> privileges, you can delete network policies in any namespace in the cluster.</p>
        </AlertDescription>
      </Alert>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If you log in with <code>cluster-admin</code> privileges, you can delete network policies in any namespace in the cluster. In the web console, you can delete policies directly in YAML or by using the <strong>Actions</strong> menu.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>Your cluster uses a network plugin that supports <code>NetworkPolicy</code> objects, such as the OVN-Kubernetes network plugin, with <code>mode: NetworkPolicy</code> set.</li>
      <li>You installed the OpenShift CLI (<code>oc</code>).</li>
      <li>You logged in to the cluster with a user with <code>{"{"}{"{"} role {"}"}{"}"}</code> privileges.</li>
      <li>You are working in the namespace where the network policy exists.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      To delete a network policy object, enter the following command. Successful output lists the name of the policy object and the <code>deleted</code> status.
      <CodeBlock code={"$ oc delete {{ name }}policy <policy_name> -n <namespace>"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;policy_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the network policy.</p></dd>
      <dt class="font-semibold"><code>&lt;namespace&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional parameter. If you defined the object in a different namespace than the current namespace, the parameter specifices the namespace.</p></dd>
      </dl>
      </li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
