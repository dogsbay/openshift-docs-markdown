---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/ovn_kubernetes_network_provider/configuring-gateway.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"configuring-gateway","text":"Configuring a gateway"},{"depth":2,"slug":"nwt-configure-egress-routing-policies_configuring-gateway-mode","text":"Configuring egress routing policies"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/configuring-gateway";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring a gateway";
const description = "As a cluster administrator you can configure the gatewayConfig object to manage how external traffic leaves the cluster. You do so by setting the…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/ovn_kubernetes_network_provider"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring a gateway {#configuring-gateway}\n\nAs a cluster administrator you can configure the `gatewayConfig` object to manage how external traffic leaves the cluster. You do so by setting the `routingViaHost` parameter to one of the following values:\n\n- `true` means that egress traffic routes through a specific local gateway on the node that hosts the pod. Egress traffic routes through the host and this traffic applies to the routing table of the host.\n- `false` means that egress traffic routes through a dedicated node but a group of nodes share the same gateway. Egress traffic does not route through the host. The Open vSwitch (OVS) outputs traffic directly to the node IP interface.\n\n## Configuring egress routing policies {#nwt-configure-egress-routing-policies_configuring-gateway-mode}\n\nAs a cluster administrator you can configure egress routing policies by using the `gatewayConfig` specification in the Cluster Network Operator (CNO). You can use the following procedure to set the `routingViaHost` field to `true` or  `false`.\n\nYou can follow the optional step in the procedure to enable IP forwarding alongside the `routingViaHost=true` configuration if you need the host network of the node to act as a router for traffic not related to OVN-Kubernetes. For example, possible use cases for combining local gateway with IP forwarding include:\n\n- Configuring all pod egress traffic to be forwarded via the node’s IP\n- Integrating OVN-Kubernetes CNI with external network address translation (NAT) devices\n- Configuring OVN-Kubernetes CNI to use a kernel routing table\n\n**Prerequisites**\n\n- You are logged in as a user with administrator privileges.\n\n**Procedure**\n\n1. Back up the existing network configuration by running the following command:\n\n   ```terminal\n   $ oc get network.operator cluster -o yaml > network-config-backup.yaml\n   ```\n2. Set the `routingViaHost` parameter to `true` by entering the following command. Egress traffic then gets routed through a specific gateway according to the routes that you configured on the node.\n\n   ```terminal\n   $ oc patch networks.operator.openshift.io cluster --type=merge -p '{\"spec\":{\"defaultNetwork\":{\"ovnKubernetesConfig\":{\"gatewayConfig\":{\"routingViaHost\": true}}}}}'\n   ```\n3. Verify the correct application of the `routingViaHost=true` configuration by running the following command:\n\n   ```terminal\n   $ oc get networks.operator.openshift.io cluster -o yaml | grep -A 5 \"gatewayConfig\"\n   ```\n\n   ```terminal {title=\"Example output\"}\n   apiVersion: operator.openshift.io/v1\n   kind: Network\n   metadata:\n     name: cluster\n   # ...\n   gatewayConfig:\n           ipv4: {}\n           ipv6: {}\n           routingViaHost: true\n         genevePort: 6081\n         ipsecConfig:\n   # ...\n   ```\n\n   - `routingViaHost`:: Specifies a value of `true` means that egress traffic gets routed through a specific local gateway on the node that hosts the pod. A value of `false` for the parameter means that a group of nodes share a single gateway so traffic does not get routed through a single host.\n4. Optional: Enable IP forwarding globally by running the following command:\n\n   ```terminal\n   $ oc patch network.operator cluster --type=merge -p '{\"spec\":{\"defaultNetwork\":{\"ovnKubernetesConfig\":{\"gatewayConfig\":{\"ipForwarding\": \"Global\"}}}}}'\n   ```\n\n   1. Verify that the `ipForwarding` spec has been set to `Global` by running the following command:\n\n      ```terminal\n      $ oc get networks.operator.openshift.io cluster -o yaml | grep -A 5 \"gatewayConfig\"\n      ```\n\n      ```terminal {title=\"Example output\"}\n      apiVersion: operator.openshift.io/v1\n      kind: Network\n      metadata:\n        name: cluster\n      # ...\n      gatewayConfig:\n              ipForwarding: Global\n              ipv4: {}\n              ipv6: {}\n              routingViaHost: true\n            genevePort: 6081\n      # ...\n      ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/configuring-gateway.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="configuring-gateway" class="group scroll-mt-20">Configuring a gateway<a href="#configuring-gateway" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">As a cluster administrator you can configure the <code>gatewayConfig</code> object to manage how external traffic leaves the cluster. You do so by setting the <code>routingViaHost</code> parameter to one of the following values:</p>
      <ul>
      <li><code>true</code> means that egress traffic routes through a specific local gateway on the node that hosts the pod. Egress traffic routes through the host and this traffic applies to the routing table of the host.</li>
      <li><code>false</code> means that egress traffic routes through a dedicated node but a group of nodes share the same gateway. Egress traffic does not route through the host. The Open vSwitch (OVS) outputs traffic directly to the node IP interface.</li>
      </ul>
      <h2 id="nwt-configure-egress-routing-policies_configuring-gateway-mode" class="group scroll-mt-20">Configuring egress routing policies<a href="#nwt-configure-egress-routing-policies_configuring-gateway-mode" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">As a cluster administrator you can configure egress routing policies by using the <code>gatewayConfig</code> specification in the Cluster Network Operator (CNO). You can use the following procedure to set the <code>routingViaHost</code> field to <code>true</code> or  <code>false</code>.</p>
      <p>You can follow the optional step in the procedure to enable IP forwarding alongside the <code>routingViaHost=true</code> configuration if you need the host network of the node to act as a router for traffic not related to OVN-Kubernetes. For example, possible use cases for combining local gateway with IP forwarding include:</p>
      <ul>
      <li>Configuring all pod egress traffic to be forwarded via the node’s IP</li>
      <li>Integrating OVN-Kubernetes CNI with external network address translation (NAT) devices</li>
      <li>Configuring OVN-Kubernetes CNI to use a kernel routing table</li>
      </ul>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You are logged in as a user with administrator privileges.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Back up the existing network configuration by running the following command:
      <CodeBlock code={"$ oc get network.operator cluster -o yaml > network-config-backup.yaml"} lang="terminal" />
      </li>
      <li>
      Set the <code>routingViaHost</code> parameter to <code>true</code> by entering the following command. Egress traffic then gets routed through a specific gateway according to the routes that you configured on the node.
      <CodeBlock code={"$ oc patch networks.operator.openshift.io cluster --type=merge -p '{\"spec\":{\"defaultNetwork\":{\"ovnKubernetesConfig\":{\"gatewayConfig\":{\"routingViaHost\": true}}}}}'"} lang="terminal" />
      </li>
      <li>
      Verify the correct application of the <code>routingViaHost=true</code> configuration by running the following command:
      <CodeBlock code={"$ oc get networks.operator.openshift.io cluster -o yaml | grep -A 5 \"gatewayConfig\""} lang="terminal" />
      <CodeBlock code={"apiVersion: operator.openshift.io/v1\nkind: Network\nmetadata:\n  name: cluster\n# ...\ngatewayConfig:\n        ipv4: {}\n        ipv6: {}\n        routingViaHost: true\n      genevePort: 6081\n      ipsecConfig:\n# ..."} lang="terminal" title="Example output" />
      <ul><li><code>routingViaHost</code>:: Specifies a value of <code>true</code> means that egress traffic gets routed through a specific local gateway on the node that hosts the pod. A value of <code>false</code> for the parameter means that a group of nodes share a single gateway so traffic does not get routed through a single host.</li></ul>
      </li>
      <li>
      Optional: Enable IP forwarding globally by running the following command:
      <CodeBlock code={"$ oc patch network.operator cluster --type=merge -p '{\"spec\":{\"defaultNetwork\":{\"ovnKubernetesConfig\":{\"gatewayConfig\":{\"ipForwarding\": \"Global\"}}}}}'"} lang="terminal" />
      <ol>
      <li>
      Verify that the <code>ipForwarding</code> spec has been set to <code>Global</code> by running the following command:
      <CodeBlock code={"$ oc get networks.operator.openshift.io cluster -o yaml | grep -A 5 \"gatewayConfig\""} lang="terminal" />
      <CodeBlock code={"apiVersion: operator.openshift.io/v1\nkind: Network\nmetadata:\n  name: cluster\n# ...\ngatewayConfig:\n        ipForwarding: Global\n        ipv4: {}\n        ipv6: {}\n        routingViaHost: true\n      genevePort: 6081\n# ..."} lang="terminal" title="Example output" />
      </li>
      </ol>
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
