---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/ovn_kubernetes_network_provider/configure-ovn-kubernetes-subnets.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"configure-ovn-kubernetes-subnets","text":"Configuring OVN-Kubernetes internal IP address subnets"},{"depth":2,"slug":"nw-ovn-kubernetes-change-join-subnet_configure-ovn-kubernetes-subnets","text":"Configuring the OVN-Kubernetes join subnet"},{"depth":2,"slug":"nw-ovn-k-day-2-masq-subnet_configure-ovn-kubernetes-subnets","text":"Configuring the OVN-Kubernetes masquerade subnet as a post-installation operation"},{"depth":2,"slug":"nw-ovn-kubernetes-change-transit-subnet_configure-ovn-kubernetes-subnets","text":"Configuring the OVN-Kubernetes transit subnet"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/configure-ovn-kubernetes-subnets";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring OVN-Kubernetes internal IP address subnets";
const description = "As a cluster administrator, you can change the IP address ranges that the OVN-Kubernetes network plugin uses for the join and transit subnets." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/ovn_kubernetes_network_provider"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring OVN-Kubernetes internal IP address subnets {#configure-ovn-kubernetes-subnets}\n\nAs a cluster administrator, you can change the IP address ranges that the OVN-Kubernetes network plugin uses for the join and transit subnets.\n\n## Configuring the OVN-Kubernetes join subnet {#nw-ovn-kubernetes-change-join-subnet_configure-ovn-kubernetes-subnets}\n\nYou can change the join subnet used by OVN-Kubernetes to avoid conflicting with any existing subnets already in use in your environment.\n\n**Prerequisites**\n\n- Install the OpenShift CLI (`oc`).\n- Log in to the cluster with a user with `cluster-admin` privileges.\n- Ensure that the cluster uses the OVN-Kubernetes network plugin.\n\n**Procedure**\n\n- To change the OVN-Kubernetes join subnet, enter the following command:\n\n  ```terminal\n  $ oc patch network.operator.openshift.io cluster --type='merge' \\\n    -p='{\"spec\":{\"defaultNetwork\":{\"ovnKubernetesConfig\":\n      {\"ipv4\":{\"internalJoinSubnet\": \"<join_subnet>\"},\n      \"ipv6\":{\"internalJoinSubnet\": \"<join_subnet>\"}}}}}'\n  ```\n\n  where:\n\n  `<join_subnet>`\n  :   Specifies an IP address subnet for internal use by OVN-Kubernetes. The subnet must be larger than the number of nodes in the cluster and it must be large enough to accommodate one IP address per node in the cluster. This subnet cannot overlap with any other subnets used by OpenShift Container Platform or on the host itself. The default value for IPv4 is `100.64.0.0/16` and the default value for IPv6 is `fd98::/64`.\n\n  ```text {title=\"Example output\"}\n  network.operator.openshift.io/cluster patched\n  ```\n\n**Verification**\n\n- To confirm that the configuration is active, enter the following command:\n\n  ```terminal\n  $ oc get network.operator.openshift.io \\\n    -o jsonpath=\"{.items[0].spec.defaultNetwork}\"\n  ```\n\n  The command operation can take up to 30 minutes for this change to take effect.\n\n  ```text {title=\"Example output\"}\n  {\n    \"ovnKubernetesConfig\": {\n      \"ipv4\": {\n        \"internalJoinSubnet\": \"100.64.1.0/16\"\n      },\n    },\n    \"type\": \"OVNKubernetes\"\n  }\n  ```\n\n## Configuring the OVN-Kubernetes masquerade subnet as a post-installation operation {#nw-ovn-k-day-2-masq-subnet_configure-ovn-kubernetes-subnets}\n\nYou can change the masquerade subnet used by OVN-Kubernetes as a post-installation operation to avoid conflicts with any existing subnets that are already in use in your environment.\n\n**Prerequisites**\n\n- Install the OpenShift CLI (`oc`).\n- Log in to the cluster as a user with `cluster-admin` privileges.\n\n**Procedure**\n\n- Change your cluster’s masquerade subnet:\n\n  - For dualstack clusters using IPv6, run the following command:\n\n    ```terminal\n    $ oc patch networks.operator.openshift.io cluster --type=merge -p '{\"spec\":{\"defaultNetwork\":{\"ovnKubernetesConfig\":{\"gatewayConfig\":{\"ipv4\":{\"internalMasqueradeSubnet\": \"<ipv4_masquerade_subnet>\"},\"ipv6\":{\"internalMasqueradeSubnet\": \"<ipv6_masquerade_subnet>\"}}}}}}'\n    ```\n\n    where:\n\n    `ipv4_masquerade_subnet`\n    :   Specifies an IP address to be used as the IPv4 masquerade subnet. This range cannot overlap with any other subnets used by OpenShift Container Platform or on the host itself. In versions of OpenShift Container Platform earlier than 4.17, the default value for IPv4 was `169.254.169.0/29`, and clusters that were upgraded to version 4.17 maintain this value. For new clusters starting from version 4.17, the default value is `169.254.0.0/17`.\n\n    `ipv6_masquerade_subnet`\n    :   Specifies an IP address to be used as the IPv6 masquerade subnet. This range cannot overlap with any other subnets used by OpenShift Container Platform or on the host itself. The default value for IPv6 is `fd69::/125`.\n  - For clusters using IPv4, run the following command:\n\n    ```terminal\n    $ oc patch networks.operator.openshift.io cluster --type=merge -p '{\"spec\":{\"defaultNetwork\":{\"ovnKubernetesConfig\":{\"gatewayConfig\":{\"ipv4\":{\"internalMasqueradeSubnet\": \"<ipv4_masquerade_subnet>\"}}}}}}'\n    ```\n\n    where:\n\n    `ipv4_masquerade_subnet`:Specifies[] an IP address to be used as the IPv4 masquerade subnet. This range cannot overlap with any other subnets used by OpenShift Container Platform or on the host itself. In versions of OpenShift Container Platform earlier than 4.17, the default value for IPv4 was `169.254.169.0/29`, and clusters that were upgraded to version 4.17 maintain this value. For new clusters starting from version 4.17, the default value is `169.254.0.0/17`.\n\n## Configuring the OVN-Kubernetes transit subnet {#nw-ovn-kubernetes-change-transit-subnet_configure-ovn-kubernetes-subnets}\n\nYou can change the transit subnet used by OVN-Kubernetes to avoid conflicting with any existing subnets already in use in your environment.\n\n**Prerequisites**\n\n- Install the OpenShift CLI (`oc`).\n- Log in to the cluster with a user with `cluster-admin` privileges.\n- Ensure that the cluster uses the OVN-Kubernetes network plugin.\n\n**Procedure**\n\n- To change the OVN-Kubernetes transit subnet, enter the following command:\n\n  ```terminal\n  $ oc patch network.operator.openshift.io cluster --type='merge' \\\n    -p='{\"spec\":{\"defaultNetwork\":{\"ovnKubernetesConfig\":\n      {\"ipv4\":{\"internalTransitSwitchSubnet\": \"<transit_subnet>\"},\n      \"ipv6\":{\"internalTransitSwitchSubnet\": \"<transit_subnet>\"}}}}}'\n  ```\n\n  where:\n\n  `<transit_subnet>`\n  :   Specifies an IP address subnet for the distributed transit switch that enables east-west traffic. This subnet cannot overlap with any other subnets used by OVN-Kubernetes or on the host itself. The default value for IPv4 is `100.88.0.0/16` and the default value for IPv6 is `fd97::/64`.\n\n  ```text {title=\"Example output\"}\n  network.operator.openshift.io/cluster patched\n  ```\n\n**Verification**\n\n- To confirm that the configuration is active, enter the following command:\n\n  ```terminal\n  $ oc get network.operator.openshift.io \\\n    -o jsonpath=\"{.items[0].spec.defaultNetwork}\"\n  ```\n\n  It can take up to 30 minutes for this change to take effect.\n\n  ```text {title=\"Example output\"}\n  {\n    \"ovnKubernetesConfig\": {\n      \"ipv4\": {\n        \"internalTransitSwitchSubnet\": \"100.88.1.0/16\"\n      },\n    },\n    \"type\": \"OVNKubernetes\"\n  }\n  ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/configure-ovn-kubernetes-subnets.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="configure-ovn-kubernetes-subnets" class="group scroll-mt-20">Configuring OVN-Kubernetes internal IP address subnets<a href="#configure-ovn-kubernetes-subnets" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">As a cluster administrator, you can change the IP address ranges that the OVN-Kubernetes network plugin uses for the join and transit subnets.</p>
      <h2 id="nw-ovn-kubernetes-change-join-subnet_configure-ovn-kubernetes-subnets" class="group scroll-mt-20">Configuring the OVN-Kubernetes join subnet<a href="#nw-ovn-kubernetes-change-join-subnet_configure-ovn-kubernetes-subnets" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can change the join subnet used by OVN-Kubernetes to avoid conflicting with any existing subnets already in use in your environment.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>Install the OpenShift CLI (<code>oc</code>).</li>
      <li>Log in to the cluster with a user with <code>cluster-admin</code> privileges.</li>
      <li>Ensure that the cluster uses the OVN-Kubernetes network plugin.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      To change the OVN-Kubernetes join subnet, enter the following command:
      <CodeBlock code={"$ oc patch network.operator.openshift.io cluster --type='merge' \\\n  -p='{\"spec\":{\"defaultNetwork\":{\"ovnKubernetesConfig\":\n    {\"ipv4\":{\"internalJoinSubnet\": \"<join_subnet>\"},\n    \"ipv6\":{\"internalJoinSubnet\": \"<join_subnet>\"}}}}}'"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;join_subnet&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies an IP address subnet for internal use by OVN-Kubernetes. The subnet must be larger than the number of nodes in the cluster and it must be large enough to accommodate one IP address per node in the cluster. This subnet cannot overlap with any other subnets used by OpenShift Container Platform or on the host itself. The default value for IPv4 is <code>100.64.0.0/16</code> and the default value for IPv6 is <code>fd98::/64</code>.</p></dd>
      </dl>
      <CodeBlock code={"network.operator.openshift.io/cluster patched"} lang="text" title="Example output" />
      </li>
      </ul>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      To confirm that the configuration is active, enter the following command:
      <CodeBlock code={"$ oc get network.operator.openshift.io \\\n  -o jsonpath=\"{.items[0].spec.defaultNetwork}\""} lang="terminal" />
      <p>The command operation can take up to 30 minutes for this change to take effect.</p>
      <CodeBlock code={"{\n  \"ovnKubernetesConfig\": {\n    \"ipv4\": {\n      \"internalJoinSubnet\": \"100.64.1.0/16\"\n    },\n  },\n  \"type\": \"OVNKubernetes\"\n}"} lang="text" title="Example output" />
      </li>
      </ul>
      <h2 id="nw-ovn-k-day-2-masq-subnet_configure-ovn-kubernetes-subnets" class="group scroll-mt-20">Configuring the OVN-Kubernetes masquerade subnet as a post-installation operation<a href="#nw-ovn-k-day-2-masq-subnet_configure-ovn-kubernetes-subnets" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can change the masquerade subnet used by OVN-Kubernetes as a post-installation operation to avoid conflicts with any existing subnets that are already in use in your environment.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>Install the OpenShift CLI (<code>oc</code>).</li>
      <li>Log in to the cluster as a user with <code>cluster-admin</code> privileges.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Change your cluster’s masquerade subnet:
      <ul>
      <li>
      For dualstack clusters using IPv6, run the following command:
      <CodeBlock code={"$ oc patch networks.operator.openshift.io cluster --type=merge -p '{\"spec\":{\"defaultNetwork\":{\"ovnKubernetesConfig\":{\"gatewayConfig\":{\"ipv4\":{\"internalMasqueradeSubnet\": \"<ipv4_masquerade_subnet>\"},\"ipv6\":{\"internalMasqueradeSubnet\": \"<ipv6_masquerade_subnet>\"}}}}}}'"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>ipv4_masquerade_subnet</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies an IP address to be used as the IPv4 masquerade subnet. This range cannot overlap with any other subnets used by OpenShift Container Platform or on the host itself. In versions of OpenShift Container Platform earlier than 4.17, the default value for IPv4 was <code>169.254.169.0/29</code>, and clusters that were upgraded to version 4.17 maintain this value. For new clusters starting from version 4.17, the default value is <code>169.254.0.0/17</code>.</p></dd>
      <dt class="font-semibold"><code>ipv6_masquerade_subnet</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies an IP address to be used as the IPv6 masquerade subnet. This range cannot overlap with any other subnets used by OpenShift Container Platform or on the host itself. The default value for IPv6 is <code>fd69::/125</code>.</p></dd>
      </dl>
      </li>
      <li>
      For clusters using IPv4, run the following command:
      <CodeBlock code={"$ oc patch networks.operator.openshift.io cluster --type=merge -p '{\"spec\":{\"defaultNetwork\":{\"ovnKubernetesConfig\":{\"gatewayConfig\":{\"ipv4\":{\"internalMasqueradeSubnet\": \"<ipv4_masquerade_subnet>\"}}}}}}'"} lang="terminal" />
      <p>where:</p>
      <p><code>ipv4_masquerade_subnet</code><Fragment set:html={":Specifies[]"} /> an IP address to be used as the IPv4 masquerade subnet. This range cannot overlap with any other subnets used by OpenShift Container Platform or on the host itself. In versions of OpenShift Container Platform earlier than 4.17, the default value for IPv4 was <code>169.254.169.0/29</code>, and clusters that were upgraded to version 4.17 maintain this value. For new clusters starting from version 4.17, the default value is <code>169.254.0.0/17</code>.</p>
      </li>
      </ul>
      </li>
      </ul>
      <h2 id="nw-ovn-kubernetes-change-transit-subnet_configure-ovn-kubernetes-subnets" class="group scroll-mt-20">Configuring the OVN-Kubernetes transit subnet<a href="#nw-ovn-kubernetes-change-transit-subnet_configure-ovn-kubernetes-subnets" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can change the transit subnet used by OVN-Kubernetes to avoid conflicting with any existing subnets already in use in your environment.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>Install the OpenShift CLI (<code>oc</code>).</li>
      <li>Log in to the cluster with a user with <code>cluster-admin</code> privileges.</li>
      <li>Ensure that the cluster uses the OVN-Kubernetes network plugin.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      To change the OVN-Kubernetes transit subnet, enter the following command:
      <CodeBlock code={"$ oc patch network.operator.openshift.io cluster --type='merge' \\\n  -p='{\"spec\":{\"defaultNetwork\":{\"ovnKubernetesConfig\":\n    {\"ipv4\":{\"internalTransitSwitchSubnet\": \"<transit_subnet>\"},\n    \"ipv6\":{\"internalTransitSwitchSubnet\": \"<transit_subnet>\"}}}}}'"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;transit_subnet&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies an IP address subnet for the distributed transit switch that enables east-west traffic. This subnet cannot overlap with any other subnets used by OVN-Kubernetes or on the host itself. The default value for IPv4 is <code>100.88.0.0/16</code> and the default value for IPv6 is <code>fd97::/64</code>.</p></dd>
      </dl>
      <CodeBlock code={"network.operator.openshift.io/cluster patched"} lang="text" title="Example output" />
      </li>
      </ul>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      To confirm that the configuration is active, enter the following command:
      <CodeBlock code={"$ oc get network.operator.openshift.io \\\n  -o jsonpath=\"{.items[0].spec.defaultNetwork}\""} lang="terminal" />
      <p>It can take up to 30 minutes for this change to take effect.</p>
      <CodeBlock code={"{\n  \"ovnKubernetesConfig\": {\n    \"ipv4\": {\n      \"internalTransitSwitchSubnet\": \"100.88.1.0/16\"\n    },\n  },\n  \"type\": \"OVNKubernetes\"\n}"} lang="text" title="Example output" />
      </li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
