---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/ovn_kubernetes_network_provider/about-ovn-kubernetes.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"about-ovn-kubernetes","text":"About the OVN-Kubernetes network plugin"},{"depth":2,"slug":"nw-ovn-kubernetes-purpose_about-ovn-kubernetes","text":"OVN-Kubernetes purpose"},{"depth":2,"slug":"nw-ovn-kubernetes-limitations_about-ovn-kubernetes","text":"OVN-Kubernetes IPv6 and dual-stack limitations"},{"depth":2,"slug":"nw-ovn-kubernetes-session-affinity_about-ovn-kubernetes","text":"Session affinity"},{"depth":3,"slug":"nw-ovn-kubernetes-session-affinity-stickyness-timeout_about-ovn-kubernetes","text":"Stickiness timeout for session affinity"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/about-ovn-kubernetes";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "About the OVN-Kubernetes network plugin";
const description = "The OpenShift Container Platform cluster uses a virtualized network for pod and service networks. The OVN-Kubernetes network plugin is the default…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/ovn_kubernetes_network_provider"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# About the OVN-Kubernetes network plugin {#about-ovn-kubernetes}\n\nThe OpenShift Container Platform cluster uses a virtualized network for pod and service networks. The OVN-Kubernetes network plugin is the default provider that implements this virtualized overlay network by transforming configurations into OpenFlow rules to enable advanced routing and security features.\n\nPart of Red Hat OpenShift Networking, the OVN-Kubernetes network plugin is the default network provider for OpenShift Container Platform. OVN-Kubernetes is based on Open Virtual Network (OVN) and provides an overlay-based networking implementation. A cluster that uses the OVN-Kubernetes plugin also runs Open vSwitch (OVS) on each node. OVN configures OVS on each node to implement the declared network configuration.\n\n> [!NOTE]\n> OVN-Kubernetes is the default networking solution for OpenShift Container Platform and single-node OpenShift deployments.\n\nOVN-Kubernetes, which arose from the OVS project, uses many of the same constructs. For example, the plugin applies OpenFlow rules to direct packet routing through the network infrastructure. For more information, see the \"Open Virtual Network website\".\n\nOVN-Kubernetes is a series of daemons for OVS that transform virtual network configurations into `OpenFlow` rules. `OpenFlow` is a protocol for communicating with network switches and routers, providing a means for remotely controlling the flow of network traffic on a network device. This means that network administrators can configure, manage, and monitor the flow of network traffic.\n\nOVN-Kubernetes provides more of the advanced functionality not available with `OpenFlow`. OVN supports distributed virtual routing, distributed logical switches, access control, Dynamic Host Configuration Protocol (DHCP), and DNS. OVN implements distributed virtual routing within logic flows that equate to open flows. For example, if you have a pod that sends out a DHCP request to the DHCP server on the network, a logic flow rule in the request enables the OVN-Kubernetes plugin to process the packet. This means that the server can respond with gateway, DNS server, IP address, and other information.\n\nOVN-Kubernetes runs a daemon on each node. There are daemon sets for the databases and for the OVN controller that run on every node. The OVN controller programs the Open vSwitch daemon on the nodes to support the following network provider features:\n\n- Egress IPs\n- Firewalls\n- Hardware offloading\n- Hybrid networking\n- Internet Protocol Security (IPsec) encryption\n- IPv6\n- Multicast.\n- Network policy and network policy logs\n- Routers\n\n## OVN-Kubernetes purpose {#nw-ovn-kubernetes-purpose_about-ovn-kubernetes}\n\nThe OVN-Kubernetes network plugin is an open-source, fully-featured Kubernetes CNI plugin that uses Open Virtual Network (OVN) to manage network traffic flows. OVN is a community developed, vendor-agnostic network virtualization solution. The OVN-Kubernetes network plugin uses the following technologies:\n\n- OVN to manage network traffic flows.\n- Kubernetes network policy support and logs, including ingress and egress rules.\n- The Generic Network Virtualization Encapsulation (Geneve) protocol, rather than Virtual Extensible LAN (VXLAN), to create an overlay network between nodes.\n\nThe OVN-Kubernetes network plugin supports the following capabilities:\n\n- Hybrid clusters that can run both Linux and Microsoft Windows workloads. This environment is known as *hybrid networking*.\n- Offloading of network data processing from the host central processing unit (CPU) to compatible network cards and data processing units (DPUs). This is known as *hardware offloading*.\n- IPv4-primary dual-stack networking on bare-metal, VMware vSphere, IBM Power(R), IBM Z(R), and Red Hat OpenStack Platform (RHOSP) platforms.\n- IPv6 single-stack networking on RHOSP and bare metal platforms.\n- IPv6-primary dual-stack networking for a cluster running on a bare-metal, a VMware vSphere, or an RHOSP platform.\n- Egress firewall devices and egress IP addresses.\n- Egress router devices that operate in redirect mode.\n- IPsec encryption of intracluster communications.\n\nRed Hat does not support the following postinstallation configurations that use the OVN-Kubernetes network plugin:\n\n- Configuring the primary network interface, including using the NMState Operator to configure bonding for the interface.\n- Configuring a sub-interface or additional network interface on a network device that uses the Open vSwitch (OVS) or an OVN-Kubernetes `br-ex` bridge network.\n- Creating additional virtual local area networks (VLANs) on the primary network interface.\n- Using the primary network interface, such as `eth0` or `bond0`, that you created for a node during cluster installation to create additional secondary networks.\n\nRed Hat does support the following postinstallation configurations that use the OVN-Kubernetes network plugin:\n\n- Creating additional VLANs from the base physical interface, such as `eth0.100`, where you configured the primary network interface as a VLAN for a node during cluster installation. This works because the Open vSwitch (OVS) bridge attaches to the initial VLAN sub-interface, such as `eth0.100`, leaving the base physical interface available for new configurations.\n- Creating an additional OVN secondary network with a `localnet` topology network requires that you define the secondary network in a `NodeNetworkConfigurationPolicy` (NNCP) object. After you create the network, pods or virtual machines (VMs) can then attach to the network. These secondary networks give a dedicated connection to the physical network, which might or might not use VLAN tagging. You cannot access these networks from the host network of a node where the host does not have the required setup, such as the required network settings.\n- Defining additional VLANs in the `vlanID` parameter of the `NetworkAttachmentDefinition` (NAD) custom resource (CR). By mapping the `br-ex` bridge to a physical interface, the interface acts as a trunk port that can carry multiple VLANs. OVN-Kubernetes can then manage tagging and untagging of network packets, also known as *Ethernet frames*. To ensure strong connectivity, configure the physical switch ports as trunks.\n\n## OVN-Kubernetes IPv6 and dual-stack limitations {#nw-ovn-kubernetes-limitations_about-ovn-kubernetes}\n\nThe OVN-Kubernetes network plugin has specific IPv6 and dual-stack networking configuration limitations. These limitations affect gateway configuration, routing layouts, and infrastructure environment stability.\n\n- For clusters configured for dual-stack networking, both IPv4 and IPv6 traffic must use the same network interface as the default gateway.\n\n  If this requirement is not met, pods on the host in the `ovnkube-node` daemon set enter the `CrashLoopBackOff` state.\n\n  If you display a pod with a command such as `oc get pod -n openshift-ovn-kubernetes -l app=ovnkube-node -o yaml`, the `status` field has more than one message about the default gateway, as shown in the following output:\n\n  ```terminal\n  I1006 16:09:50.985852   60651 helper_linux.go:73] Found default gateway interface br-ex 192.168.127.1\n  I1006 16:09:50.985923   60651 helper_linux.go:73] Found default gateway interface ens4 fe80::5054:ff:febe:bcd4\n  F1006 16:09:50.985939   60651 ovnkube.go:130] multiple gateway interfaces detected: br-ex ens4\n  ```\n\n  The only resolution is to reconfigure the host networking so that both IP families use the same network interface for the default gateway.\n- For clusters configured for dual-stack networking, both the IPv4 and IPv6 routing tables must contain the default gateway.\n\n  If this requirement is not met, pods on the host in the `ovnkube-node` daemon set enter the `CrashLoopBackOff` state.\n\n  If you display a pod with a command such as `oc get pod -n openshift-ovn-kubernetes -l app=ovnkube-node -o yaml`, the `status` field has more than one message about the default gateway, as shown in the following output:\n\n  ```terminal\n  I0512 19:07:17.589083  108432 helper_linux.go:74] Found default gateway interface br-ex 192.168.123.1\n  F0512 19:07:17.589141  108432 ovnkube.go:133] failed to get default gateway interface\n  ```\n\n  The only resolution is to reconfigure the host networking so that both IP families contain the default gateway.\n- If you set the `ipv6.disable` parameter to `1` in the `kernelArgument` section of the `MachineConfig` custom resource (CR) for your cluster, OVN-Kubernetes pods enter a `CrashLoopBackOff` state. Additionally, updating your cluster to a later version of OpenShift Container Platform fails because the Network Operator remains on a `Degraded` state. Red Hat does not support disabling IPv6 addresses for your cluster so do not set the `ipv6.disable` parameter to `1`.\n\n## Session affinity {#nw-ovn-kubernetes-session-affinity_about-ovn-kubernetes}\n\nYou can use *session affinity* if you want to ensure that each time you connect to a <service_VIP>:<Port>, the traffic is always load balanced to the same back end. For more information, including how to set session affinity based on a client’s IP address, see \"Session affinity in Kubernetes\".\n\n### Stickiness timeout for session affinity {#nw-ovn-kubernetes-session-affinity-stickyness-timeout_about-ovn-kubernetes}\n\nThe OVN-Kubernetes network plugin for OpenShift Container Platform calculates the stickiness timeout for a session from a client based on the last packet.\n\nFor example, if you run a `curl` command 10 times, the sticky session timer starts from the tenth packet not the first.\n\nAs a result, if the client is continuously contacting the service, then the session never times out. The timeout starts when the service has not received a packet for the amount of time set by the `timeoutSeconds` parameter. For more information about `timeoutSeconds`, see \"Session stickiness timeout in Kubernetes\".\n\n- [Session affinity in Kubernetes](https://kubernetes.io/docs/reference/networking/virtual-ips/#session-affinity)\n- [Session stickiness timeout in Kubernetes](https://kubernetes.io/docs/reference/networking/virtual-ips/#session-stickiness-timeout)\n\n**Additional resources**\n\n- [Open Virtual Network website](https://www.ovn.org/en/)\n- [Configuring an egress firewall for a project](/openshift-docs-markdown/networking/network_security/egress_firewall/configuring-egress-firewall-ovn#configuring-egress-firewall-ovn)\n- [About network policy](/openshift-docs-markdown/networking/network_security/network_policy/about-network-policy#about-network-policy)\n- [Logging network policy events](/openshift-docs-markdown/networking/network_security/logging-network-security#logging-network-security)\n- [Enabling multicast for a project](/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/enabling-multicast#nw-ovn-kubernetes-enabling-multicast)\n- [Configuring IPsec encryption](/openshift-docs-markdown/networking/network_security/configuring-ipsec-ovn#configuring-ipsec-ovn)\n- \\[Network [operator.openshift.io/v1\\]](/openshift-docs-markdown/rest_api/operator_apis/network-operator-openshift-io-v1#network-operator-openshift-io-v1)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/about-ovn-kubernetes.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="about-ovn-kubernetes" class="group scroll-mt-20">About the OVN-Kubernetes network plugin<a href="#about-ovn-kubernetes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>The OpenShift Container Platform cluster uses a virtualized network for pod and service networks. The OVN-Kubernetes network plugin is the default provider that implements this virtualized overlay network by transforming configurations into OpenFlow rules to enable advanced routing and security features.</p>
      <p>Part of Red Hat OpenShift Networking, the OVN-Kubernetes network plugin is the default network provider for OpenShift Container Platform. OVN-Kubernetes is based on Open Virtual Network (OVN) and provides an overlay-based networking implementation. A cluster that uses the OVN-Kubernetes plugin also runs Open vSwitch (OVS) on each node. OVN configures OVS on each node to implement the declared network configuration.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>OVN-Kubernetes is the default networking solution for OpenShift Container Platform and single-node OpenShift deployments.</p>
        </AlertDescription>
      </Alert>
      <p>OVN-Kubernetes, which arose from the OVS project, uses many of the same constructs. For example, the plugin applies OpenFlow rules to direct packet routing through the network infrastructure. For more information, see the "Open Virtual Network website".</p>
      <p>OVN-Kubernetes is a series of daemons for OVS that transform virtual network configurations into <code>OpenFlow</code> rules. <code>OpenFlow</code> is a protocol for communicating with network switches and routers, providing a means for remotely controlling the flow of network traffic on a network device. This means that network administrators can configure, manage, and monitor the flow of network traffic.</p>
      <p>OVN-Kubernetes provides more of the advanced functionality not available with <code>OpenFlow</code>. OVN supports distributed virtual routing, distributed logical switches, access control, Dynamic Host Configuration Protocol (DHCP), and DNS. OVN implements distributed virtual routing within logic flows that equate to open flows. For example, if you have a pod that sends out a DHCP request to the DHCP server on the network, a logic flow rule in the request enables the OVN-Kubernetes plugin to process the packet. This means that the server can respond with gateway, DNS server, IP address, and other information.</p>
      <p>OVN-Kubernetes runs a daemon on each node. There are daemon sets for the databases and for the OVN controller that run on every node. The OVN controller programs the Open vSwitch daemon on the nodes to support the following network provider features:</p>
      <ul>
      <li>Egress IPs</li>
      <li>Firewalls</li>
      <li>Hardware offloading</li>
      <li>Hybrid networking</li>
      <li>Internet Protocol Security (IPsec) encryption</li>
      <li>IPv6</li>
      <li>Multicast.</li>
      <li>Network policy and network policy logs</li>
      <li>Routers</li>
      </ul>
      <h2 id="nw-ovn-kubernetes-purpose_about-ovn-kubernetes" class="group scroll-mt-20">OVN-Kubernetes purpose<a href="#nw-ovn-kubernetes-purpose_about-ovn-kubernetes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>The OVN-Kubernetes network plugin is an open-source, fully-featured Kubernetes CNI plugin that uses Open Virtual Network (OVN) to manage network traffic flows. OVN is a community developed, vendor-agnostic network virtualization solution. The OVN-Kubernetes network plugin uses the following technologies:</p>
      <ul>
      <li>OVN to manage network traffic flows.</li>
      <li>Kubernetes network policy support and logs, including ingress and egress rules.</li>
      <li>The Generic Network Virtualization Encapsulation (Geneve) protocol, rather than Virtual Extensible LAN (VXLAN), to create an overlay network between nodes.</li>
      </ul>
      <p>The OVN-Kubernetes network plugin supports the following capabilities:</p>
      <ul>
      <li>Hybrid clusters that can run both Linux and Microsoft Windows workloads. This environment is known as <em>hybrid networking</em>.</li>
      <li>Offloading of network data processing from the host central processing unit (CPU) to compatible network cards and data processing units (DPUs). This is known as <em>hardware offloading</em>.</li>
      <li>IPv4-primary dual-stack networking on bare-metal, VMware vSphere, IBM Power(R), IBM Z(R), and Red Hat OpenStack Platform (RHOSP) platforms.</li>
      <li>IPv6 single-stack networking on RHOSP and bare metal platforms.</li>
      <li>IPv6-primary dual-stack networking for a cluster running on a bare-metal, a VMware vSphere, or an RHOSP platform.</li>
      <li>Egress firewall devices and egress IP addresses.</li>
      <li>Egress router devices that operate in redirect mode.</li>
      <li>IPsec encryption of intracluster communications.</li>
      </ul>
      <p>Red Hat does not support the following postinstallation configurations that use the OVN-Kubernetes network plugin:</p>
      <ul>
      <li>Configuring the primary network interface, including using the NMState Operator to configure bonding for the interface.</li>
      <li>Configuring a sub-interface or additional network interface on a network device that uses the Open vSwitch (OVS) or an OVN-Kubernetes <code>br-ex</code> bridge network.</li>
      <li>Creating additional virtual local area networks (VLANs) on the primary network interface.</li>
      <li>Using the primary network interface, such as <code>eth0</code> or <code>bond0</code>, that you created for a node during cluster installation to create additional secondary networks.</li>
      </ul>
      <p>Red Hat does support the following postinstallation configurations that use the OVN-Kubernetes network plugin:</p>
      <ul>
      <li>Creating additional VLANs from the base physical interface, such as <code>eth0.100</code>, where you configured the primary network interface as a VLAN for a node during cluster installation. This works because the Open vSwitch (OVS) bridge attaches to the initial VLAN sub-interface, such as <code>eth0.100</code>, leaving the base physical interface available for new configurations.</li>
      <li>Creating an additional OVN secondary network with a <code>localnet</code> topology network requires that you define the secondary network in a <code>NodeNetworkConfigurationPolicy</code> (NNCP) object. After you create the network, pods or virtual machines (VMs) can then attach to the network. These secondary networks give a dedicated connection to the physical network, which might or might not use VLAN tagging. You cannot access these networks from the host network of a node where the host does not have the required setup, such as the required network settings.</li>
      <li>Defining additional VLANs in the <code>vlanID</code> parameter of the <code>NetworkAttachmentDefinition</code> (NAD) custom resource (CR). By mapping the <code>br-ex</code> bridge to a physical interface, the interface acts as a trunk port that can carry multiple VLANs. OVN-Kubernetes can then manage tagging and untagging of network packets, also known as <em>Ethernet frames</em>. To ensure strong connectivity, configure the physical switch ports as trunks.</li>
      </ul>
      <h2 id="nw-ovn-kubernetes-limitations_about-ovn-kubernetes" class="group scroll-mt-20">OVN-Kubernetes IPv6 and dual-stack limitations<a href="#nw-ovn-kubernetes-limitations_about-ovn-kubernetes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The OVN-Kubernetes network plugin has specific IPv6 and dual-stack networking configuration limitations. These limitations affect gateway configuration, routing layouts, and infrastructure environment stability.</p>
      <ul>
      <li>
      For clusters configured for dual-stack networking, both IPv4 and IPv6 traffic must use the same network interface as the default gateway.
      <p>If this requirement is not met, pods on the host in the <code>ovnkube-node</code> daemon set enter the <code>CrashLoopBackOff</code> state.</p>
      <p>If you display a pod with a command such as <code>oc get pod -n openshift-ovn-kubernetes -l app=ovnkube-node -o yaml</code>, the <code>status</code> field has more than one message about the default gateway, as shown in the following output:</p>
      <CodeBlock code={"I1006 16:09:50.985852   60651 helper_linux.go:73] Found default gateway interface br-ex 192.168.127.1\nI1006 16:09:50.985923   60651 helper_linux.go:73] Found default gateway interface ens4 fe80::5054:ff:febe:bcd4\nF1006 16:09:50.985939   60651 ovnkube.go:130] multiple gateway interfaces detected: br-ex ens4"} lang="terminal" />
      <p>The only resolution is to reconfigure the host networking so that both IP families use the same network interface for the default gateway.</p>
      </li>
      <li>
      For clusters configured for dual-stack networking, both the IPv4 and IPv6 routing tables must contain the default gateway.
      <p>If this requirement is not met, pods on the host in the <code>ovnkube-node</code> daemon set enter the <code>CrashLoopBackOff</code> state.</p>
      <p>If you display a pod with a command such as <code>oc get pod -n openshift-ovn-kubernetes -l app=ovnkube-node -o yaml</code>, the <code>status</code> field has more than one message about the default gateway, as shown in the following output:</p>
      <CodeBlock code={"I0512 19:07:17.589083  108432 helper_linux.go:74] Found default gateway interface br-ex 192.168.123.1\nF0512 19:07:17.589141  108432 ovnkube.go:133] failed to get default gateway interface"} lang="terminal" />
      <p>The only resolution is to reconfigure the host networking so that both IP families contain the default gateway.</p>
      </li>
      <li>If you set the <code>ipv6.disable</code> parameter to <code>1</code> in the <code>kernelArgument</code> section of the <code>MachineConfig</code> custom resource (CR) for your cluster, OVN-Kubernetes pods enter a <code>CrashLoopBackOff</code> state. Additionally, updating your cluster to a later version of OpenShift Container Platform fails because the Network Operator remains on a <code>Degraded</code> state. Red Hat does not support disabling IPv6 addresses for your cluster so do not set the <code>ipv6.disable</code> parameter to <code>1</code>.</li>
      </ul>
      <h2 id="nw-ovn-kubernetes-session-affinity_about-ovn-kubernetes" class="group scroll-mt-20">Session affinity<a href="#nw-ovn-kubernetes-session-affinity_about-ovn-kubernetes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can use <em>session affinity</em> if you want to ensure that each time you connect to a &lt;service_VIP&gt;:&lt;Port&gt;, the traffic is always load balanced to the same back end. For more information, including how to set session affinity based on a client’s IP address, see "Session affinity in Kubernetes".</p>
      <h3 id="nw-ovn-kubernetes-session-affinity-stickyness-timeout_about-ovn-kubernetes" class="group scroll-mt-20">Stickiness timeout for session affinity<a href="#nw-ovn-kubernetes-session-affinity-stickyness-timeout_about-ovn-kubernetes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The OVN-Kubernetes network plugin for OpenShift Container Platform calculates the stickiness timeout for a session from a client based on the last packet.</p>
      <p>For example, if you run a <code>curl</code> command 10 times, the sticky session timer starts from the tenth packet not the first.</p>
      <p>As a result, if the client is continuously contacting the service, then the session never times out. The timeout starts when the service has not received a packet for the amount of time set by the <code>timeoutSeconds</code> parameter. For more information about <code>timeoutSeconds</code>, see "Session stickiness timeout in Kubernetes".</p>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="https://kubernetes.io/docs/reference/networking/virtual-ips/#session-affinity">Session affinity in Kubernetes</a></li>
      <li><a href="https://kubernetes.io/docs/reference/networking/virtual-ips/#session-stickiness-timeout">Session stickiness timeout in Kubernetes</a></li>
      </ul>
      </section>
      <p><strong>Additional resources</strong></p>
      <ul>
      <li><a href="https://www.ovn.org/en/">Open Virtual Network website</a></li>
      <li><a href="/openshift-docs-markdown/networking/network_security/egress_firewall/configuring-egress-firewall-ovn#configuring-egress-firewall-ovn">Configuring an egress firewall for a project</a></li>
      <li><a href="/openshift-docs-markdown/networking/network_security/network_policy/about-network-policy#about-network-policy">About network policy</a></li>
      <li><a href="/openshift-docs-markdown/networking/network_security/logging-network-security#logging-network-security">Logging network policy events</a></li>
      <li><a href="/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/enabling-multicast#nw-ovn-kubernetes-enabling-multicast">Enabling multicast for a project</a></li>
      <li><a href="/openshift-docs-markdown/networking/network_security/configuring-ipsec-ovn#configuring-ipsec-ovn">Configuring IPsec encryption</a></li>
      <li>[Network <a href="/openshift-docs-markdown/rest_api/operator_apis/network-operator-openshift-io-v1#network-operator-openshift-io-v1">operator.openshift.io/v1]</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
