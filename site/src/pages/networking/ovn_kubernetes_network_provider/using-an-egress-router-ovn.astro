---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: networking/ovn_kubernetes_network_provider/using-an-egress-router-ovn.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"using-an-egress-router-ovn","text":"Considerations for the use of an egress router pod"},{"depth":2,"slug":"nw-egress-router-about_using-an-egress-router-ovn","text":"About an egress router pod"},{"depth":3,"slug":"nw-egress-router-about-modes_using-an-egress-router-ovn","text":"Egress router modes"},{"depth":3,"slug":"nw-egress-router-about-router-pod-implementation_using-an-egress-router-ovn","text":"Egress router pod implementation"},{"depth":3,"slug":"nw-egress-router-about-deployments_using-an-egress-router-ovn","text":"Deployment considerations"},{"depth":3,"slug":"nw-egress-router-about-failover_using-an-egress-router-ovn","text":"Failover configuration"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/using-an-egress-router-ovn";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Considerations for the use of an egress router pod";
const description = "Before you use the egress router pod, you must understand how the pod works. Doing so can prevent situations such as creating large numbers of egress…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["networking","networking/ovn_kubernetes_network_provider"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Considerations for the use of an egress router pod {#using-an-egress-router-ovn}\n\nBefore you use the egress router pod, you must understand how the pod works. Doing so can prevent situations such as creating large numbers of egress router pods that exceed the limits of your network hardware.\n\n## About an egress router pod {#nw-egress-router-about_using-an-egress-router-ovn}\n\nThe OpenShift Container Platform egress router pod redirects traffic to a specified remote server from a private source IP address that is not used for any other purpose. An egress router pod can send network traffic to servers that are set up to allow access only from specific IP addresses.\n\n> [!NOTE]\n> The egress router pod is not intended for every outgoing connection. Creating large numbers of egress router pods can exceed the limits of your network hardware. For example, creating an egress router pod for every project or application could exceed the number of local MAC addresses that the network interface can handle before reverting to filtering MAC addresses in software.\n\n> [!IMPORTANT]\n> The egress router image is not compatible with Amazon Web Services (AWS), Microsoft Azure, or any other cloud platform that does not support layer 2 manipulations due to their incompatibility with macvlan traffic.\n\n### Egress router modes {#nw-egress-router-about-modes_using-an-egress-router-ovn}\n\nIn *redirect mode*, an egress router pod configures `iptables` rules to redirect traffic from its own IP address to one or more destination IP addresses. Client pods that need to use the reserved source IP address must be configured to access the service for the egress router rather than connecting directly to the destination IP. You can access the destination service and port from the application pod by using the `curl` command. For example:\n\n```terminal\n$ curl <router_service_IP> <port>\n```\n\n> [!NOTE]\n> The egress router CNI plugin supports redirect mode only. The egress router CNI plugin does not support HTTP proxy mode or DNS proxy mode.\n\n### Egress router pod implementation {#nw-egress-router-about-router-pod-implementation_using-an-egress-router-ovn}\n\nThe egress router implementation uses the egress router Container Network Interface (CNI) plugin. The plugin adds a secondary network interface to a pod.\n\nAn egress router is a pod that has two network interfaces. For example, the pod can have `eth0` and `net1` network interfaces. The `eth0` interface is on the cluster network and the pod continues to use the interface for ordinary cluster-related network traffic. The `net1` interface is on a secondary network and has an IP address and gateway for that network. Other pods in the OpenShift Container Platform cluster can access the egress router service and the service enables the pods to access external services. The egress router acts as a bridge between pods and an external system.\n\nTraffic that leaves the egress router exits through a node, but the packets have the MAC address of the `net1` interface from the egress router pod.\n\nWhen you add an egress router custom resource, the Cluster Network Operator creates the following objects:\n\n- The network attachment definition for the `net1` secondary network interface of the pod.\n- A deployment for the egress router.\n\nIf you delete an egress router custom resource, the Operator deletes the two objects in the preceding list that are associated with the egress router.\n\n### Deployment considerations {#nw-egress-router-about-deployments_using-an-egress-router-ovn}\n\nAn egress router pod adds an additional IP address and MAC address to the primary network interface of the node. As a result, you might need to configure your hypervisor or cloud provider to allow the additional address.\n\nRed Hat OpenStack Platform (RHOSP)\n:   If you deploy OpenShift Container Platform on RHOSP, you must allow traffic from the IP and MAC addresses of the egress router pod on your OpenStack environment. If you do not allow the traffic, see \"OpenShift on OpenStack: Egress router not working\" Knowledgebase article in the *Additional resources* section.\n\n    ```terminal\n    $ openstack port set --allowed-address \\\n      ip_address=<ip_address>,mac_address=<mac_address> <neutron_port_uuid>\n    ```\n\nVMware vSphere\n:   If you are using VMware vSphere, see the VMware documentation for securing vSphere standard switches. View and change VMware vSphere default settings by selecting the host virtual switch from the vSphere Web Client. Specifically, ensure that you enable the following components:\n\n    - MAC address changes\n    - Forged transits\n    - Promiscuous Mode Operation\n\nFor more information on these components, see the *Additional resources* section.\n\n### Failover configuration {#nw-egress-router-about-failover_using-an-egress-router-ovn}\n\nTo avoid downtime, the Cluster Network Operator deploys the egress router pod as a deployment resource. The deployment name is `egress-router-cni-deployment`. The pod that corresponds to the deployment has a label of `app=egress-router-cni`.\n\nTo create a new service for the deployment, use the `oc expose deployment/egress-router-cni-deployment --port <port_number>` command or create a file like the following example:\n\n```yaml\napiVersion: v1\nkind: Service\nmetadata:\n  name: app-egress\nspec:\n  ports:\n  - name: tcp-8080\n    protocol: TCP\n    port: 8080\n  - name: tcp-8443\n    protocol: TCP\n    port: 8443\n  - name: udp-80\n    protocol: UDP\n    port: 80\n  type: ClusterIP\n  selector:\n    app: egress-router-cni\n```\n\n**Additional resources**\n{._additional-resources}\n\n- [Deploying an egress router in redirection mode](/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/deploying-egress-router-ovn-redirection#deploying-egress-router-ovn-redirection)\n- [OpenShift on OpenStack: Egress router not working](https://access.redhat.com/solutions/2803331)\n- [MAC Address Changes](https://docs.vmware.com/en/VMware-vSphere/6.0/com.vmware.vsphere.security.doc/GUID-942BD3AA-731B-4A05-8196-66F2B4BF1ACB.html)\n- [Forged Transits](https://docs.vmware.com/en/VMware-vSphere/6.0/com.vmware.vsphere.security.doc/GUID-7DC6486F-5400-44DF-8A62-6273798A2F80.html)\n- [Promiscuous Mode Operation](https://docs.vmware.com/en/VMware-vSphere/6.0/com.vmware.vsphere.security.doc/GUID-92F3AB1F-B4C5-4F25-A010-8820D7250350.html)\n- [VMware documentation for securing vSphere standard switches](https://docs.vmware.com/en/VMware-vSphere/6.0/com.vmware.vsphere.security.doc/GUID-3507432E-AFEA-4B6B-B404-17A020575358.html)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/using-an-egress-router-ovn.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="using-an-egress-router-ovn" class="group scroll-mt-20">Considerations for the use of an egress router pod<a href="#using-an-egress-router-ovn" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Before you use the egress router pod, you must understand how the pod works. Doing so can prevent situations such as creating large numbers of egress router pods that exceed the limits of your network hardware.</p>
      <h2 id="nw-egress-router-about_using-an-egress-router-ovn" class="group scroll-mt-20">About an egress router pod<a href="#nw-egress-router-about_using-an-egress-router-ovn" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The OpenShift Container Platform egress router pod redirects traffic to a specified remote server from a private source IP address that is not used for any other purpose. An egress router pod can send network traffic to servers that are set up to allow access only from specific IP addresses.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The egress router pod is not intended for every outgoing connection. Creating large numbers of egress router pods can exceed the limits of your network hardware. For example, creating an egress router pod for every project or application could exceed the number of local MAC addresses that the network interface can handle before reverting to filtering MAC addresses in software.</p>
        </AlertDescription>
      </Alert>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>The egress router image is not compatible with Amazon Web Services (AWS), Microsoft Azure, or any other cloud platform that does not support layer 2 manipulations due to their incompatibility with macvlan traffic.</p>
        </AlertDescription>
      </Alert>
      <h3 id="nw-egress-router-about-modes_using-an-egress-router-ovn" class="group scroll-mt-20">Egress router modes<a href="#nw-egress-router-about-modes_using-an-egress-router-ovn" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>In <em>redirect mode</em>, an egress router pod configures <code>iptables</code> rules to redirect traffic from its own IP address to one or more destination IP addresses. Client pods that need to use the reserved source IP address must be configured to access the service for the egress router rather than connecting directly to the destination IP. You can access the destination service and port from the application pod by using the <code>curl</code> command. For example:</p>
      <CodeBlock code={"$ curl <router_service_IP> <port>"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The egress router CNI plugin supports redirect mode only. The egress router CNI plugin does not support HTTP proxy mode or DNS proxy mode.</p>
        </AlertDescription>
      </Alert>
      <h3 id="nw-egress-router-about-router-pod-implementation_using-an-egress-router-ovn" class="group scroll-mt-20">Egress router pod implementation<a href="#nw-egress-router-about-router-pod-implementation_using-an-egress-router-ovn" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The egress router implementation uses the egress router Container Network Interface (CNI) plugin. The plugin adds a secondary network interface to a pod.</p>
      <p>An egress router is a pod that has two network interfaces. For example, the pod can have <code>eth0</code> and <code>net1</code> network interfaces. The <code>eth0</code> interface is on the cluster network and the pod continues to use the interface for ordinary cluster-related network traffic. The <code>net1</code> interface is on a secondary network and has an IP address and gateway for that network. Other pods in the OpenShift Container Platform cluster can access the egress router service and the service enables the pods to access external services. The egress router acts as a bridge between pods and an external system.</p>
      <p>Traffic that leaves the egress router exits through a node, but the packets have the MAC address of the <code>net1</code> interface from the egress router pod.</p>
      <p>When you add an egress router custom resource, the Cluster Network Operator creates the following objects:</p>
      <ul>
      <li>The network attachment definition for the <code>net1</code> secondary network interface of the pod.</li>
      <li>A deployment for the egress router.</li>
      </ul>
      <p>If you delete an egress router custom resource, the Operator deletes the two objects in the preceding list that are associated with the egress router.</p>
      <h3 id="nw-egress-router-about-deployments_using-an-egress-router-ovn" class="group scroll-mt-20">Deployment considerations<a href="#nw-egress-router-about-deployments_using-an-egress-router-ovn" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>An egress router pod adds an additional IP address and MAC address to the primary network interface of the node. As a result, you might need to configure your hypervisor or cloud provider to allow the additional address.</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Red Hat OpenStack Platform (RHOSP)</dt>
      <dd class="ml-6 text-muted-foreground">
      <p>If you deploy OpenShift Container Platform on RHOSP, you must allow traffic from the IP and MAC addresses of the egress router pod on your OpenStack environment. If you do not allow the traffic, see "OpenShift on OpenStack: Egress router not working" Knowledgebase article in the <em>Additional resources</em> section.</p>
      <CodeBlock code={"$ openstack port set --allowed-address \\\n  ip_address=<ip_address>,mac_address=<mac_address> <neutron_port_uuid>"} lang="terminal" />
      </dd>
      <dt class="font-semibold">VMware vSphere</dt>
      <dd class="ml-6 text-muted-foreground">
      <p>If you are using VMware vSphere, see the VMware documentation for securing vSphere standard switches. View and change VMware vSphere default settings by selecting the host virtual switch from the vSphere Web Client. Specifically, ensure that you enable the following components:</p>
      <ul>
      <li>MAC address changes</li>
      <li>Forged transits</li>
      <li>Promiscuous Mode Operation</li>
      </ul>
      </dd>
      </dl>
      <p>For more information on these components, see the <em>Additional resources</em> section.</p>
      <h3 id="nw-egress-router-about-failover_using-an-egress-router-ovn" class="group scroll-mt-20">Failover configuration<a href="#nw-egress-router-about-failover_using-an-egress-router-ovn" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>To avoid downtime, the Cluster Network Operator deploys the egress router pod as a deployment resource. The deployment name is <code>egress-router-cni-deployment</code>. The pod that corresponds to the deployment has a label of <code>app=egress-router-cni</code>.</p>
      <p>To create a new service for the deployment, use the <code>oc expose deployment/egress-router-cni-deployment --port &lt;port_number&gt;</code> command or create a file like the following example:</p>
      <CodeBlock code={"apiVersion: v1\nkind: Service\nmetadata:\n  name: app-egress\nspec:\n  ports:\n  - name: tcp-8080\n    protocol: TCP\n    port: 8080\n  - name: tcp-8443\n    protocol: TCP\n    port: 8443\n  - name: udp-80\n    protocol: UDP\n    port: 80\n  type: ClusterIP\n  selector:\n    app: egress-router-cni"} lang="yaml" />
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/deploying-egress-router-ovn-redirection#deploying-egress-router-ovn-redirection">Deploying an egress router in redirection mode</a></li>
      <li><a href="https://access.redhat.com/solutions/2803331">OpenShift on OpenStack: Egress router not working</a></li>
      <li><a href="https://docs.vmware.com/en/VMware-vSphere/6.0/com.vmware.vsphere.security.doc/GUID-942BD3AA-731B-4A05-8196-66F2B4BF1ACB.html">MAC Address Changes</a></li>
      <li><a href="https://docs.vmware.com/en/VMware-vSphere/6.0/com.vmware.vsphere.security.doc/GUID-7DC6486F-5400-44DF-8A62-6273798A2F80.html">Forged Transits</a></li>
      <li><a href="https://docs.vmware.com/en/VMware-vSphere/6.0/com.vmware.vsphere.security.doc/GUID-92F3AB1F-B4C5-4F25-A010-8820D7250350.html">Promiscuous Mode Operation</a></li>
      <li><a href="https://docs.vmware.com/en/VMware-vSphere/6.0/com.vmware.vsphere.security.doc/GUID-3507432E-AFEA-4B6B-B404-17A020575358.html">VMware documentation for securing vSphere standard switches</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
