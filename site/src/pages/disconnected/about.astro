---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: disconnected/about.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"about-disconnected-environments","text":"About disconnected environments"},{"depth":2,"slug":"glossary-disconnected_about-disconnected-environments","text":"Glossary of disconnected environment terms"},{"depth":2,"slug":"preferred-methods_about-disconnected-environments","text":"Preferred methods for working with disconnected environments"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/disconnected/about";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "About disconnected environments";
const description = "A disconnected environment is an environment that does not have full access to the internet." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["disconnected"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# About disconnected environments {#about-disconnected-environments}\n\nA disconnected environment is an environment that does not have full access to the internet.\n\nOpenShift Container Platform is designed to perform many automatic functions that depend on an internet connection, such as retrieving release images from a registry or retrieving update paths and recommendations for the cluster. Without a direct internet connection, you must perform additional setup and configuration for your cluster to maintain full functionality in the disconnected environment.\n\n## Glossary of disconnected environment terms {#glossary-disconnected_about-disconnected-environments}\n\nAlthough it is used throughout the OpenShift Container Platform documentation, *disconnected environment* is a broad term that can refer to environments with various levels of internet connectivity. Other terms are sometimes used to refer to a specific level of internet connectivity, and these environments might require additional unique configurations.\n\nThe following table describes the different terms used to refer to environments without a full internet connection:\n\n**Disconnected environment terms**\n\n| Term | Description |\n| --- | --- |\n| Air-gapped network | An environment or network that is completely isolated from an external network.<br>This isolation depends on a physical separation, or an \"air gap\", between machines on the internal network and any other part of an external network. Air-gapped environments are often used in industries with strict security or regulatory requirements. |\n| Disconnected environment | An environment or network that has some level of isolation from an external network.<br>This isolation could be enabled by physical or logical separation between machines on the internal network and an external network. Regardless of the level of isolation from the external network, a cluster in a disconnected environment does not have access to public services hosted by Red Hat and requires additional setup to maintain full cluster functionality. |\n| Restricted Network | An environment or network with limited connection to an external network.<br>A physical connection may exist between machines on the internal network and an external network, but network traffic is limited by additional configurations, such as with firewalls and proxies. |\n\n## Preferred methods for working with disconnected environments {#preferred-methods_about-disconnected-environments}\n\nYou can choose between multiple options for most aspects of managing a cluster in a disconnected environment. For example, when mirroring images you can choose between using the oc-mirror OpenShift CLI (`oc`) plugin or using the `oc adm` command.\n\nHowever, some options provide a simpler and more convenient user experience for disconnected environments, and are the preferred method over their alternatives.\n\nUnless your organizational needs require you to choose another option, use the following methods for mirroring images, installing your cluster, and updating your cluster:\n\n- Mirror your images using the oc-mirror plugin v2. For more information, see \"Mirroring images for a disconnected installation by using the oc-mirror plugin v2\".\n- Install your cluster using the Agent-based Installer. For more information, see \"Installing a cluster with customizations\".\n- Update your cluster using a local OpenShift Update Service instance. For more information, see \"Updating a cluster in a disconnected environment using the OpenShift Update Service\".\n\n**Additional resources**\n{._additional-resources}\n\n- [Mirroring images for a disconnected installation by using the oc-mirror plugin v2](/openshift-docs-markdown/disconnected/about-installing-oc-mirror-v2#about-installing-oc-mirror-v2)\n- [Installing a cluster with customizations](/openshift-docs-markdown/installing/installing_with_agent_based_installer/installing-with-agent-based-installer#installing-with-agent-based-installer)\n- [Updating a cluster in a disconnected environment using the OpenShift Update Service](/openshift-docs-markdown/disconnected/updating/disconnected-update-osus#updating-disconnected-cluster-osus)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/disconnected/about.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="about-disconnected-environments" class="group scroll-mt-20">About disconnected environments<a href="#about-disconnected-environments" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">A disconnected environment is an environment that does not have full access to the internet.</p>
      <p>OpenShift Container Platform is designed to perform many automatic functions that depend on an internet connection, such as retrieving release images from a registry or retrieving update paths and recommendations for the cluster. Without a direct internet connection, you must perform additional setup and configuration for your cluster to maintain full functionality in the disconnected environment.</p>
      <h2 id="glossary-disconnected_about-disconnected-environments" class="group scroll-mt-20">Glossary of disconnected environment terms<a href="#glossary-disconnected_about-disconnected-environments" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Although it is used throughout the OpenShift Container Platform documentation, <em>disconnected environment</em> is a broad term that can refer to environments with various levels of internet connectivity. Other terms are sometimes used to refer to a specific level of internet connectivity, and these environments might require additional unique configurations.</p>
      <p>The following table describes the different terms used to refer to environments without a full internet connection:</p>
      <p><strong>Disconnected environment terms</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Term</TableHead>
      <TableHead>Description</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>Air-gapped network</TableCell>
      <TableCell>An environment or network that is completely isolated from an external network.<Fragment set:html={"<br>"} />This isolation depends on a physical separation, or an "air gap", between machines on the internal network and any other part of an external network. Air-gapped environments are often used in industries with strict security or regulatory requirements.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Disconnected environment</TableCell>
      <TableCell>An environment or network that has some level of isolation from an external network.<Fragment set:html={"<br>"} />This isolation could be enabled by physical or logical separation between machines on the internal network and an external network. Regardless of the level of isolation from the external network, a cluster in a disconnected environment does not have access to public services hosted by Red Hat and requires additional setup to maintain full cluster functionality.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Restricted Network</TableCell>
      <TableCell>An environment or network with limited connection to an external network.<Fragment set:html={"<br>"} />A physical connection may exist between machines on the internal network and an external network, but network traffic is limited by additional configurations, such as with firewalls and proxies.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="preferred-methods_about-disconnected-environments" class="group scroll-mt-20">Preferred methods for working with disconnected environments<a href="#preferred-methods_about-disconnected-environments" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can choose between multiple options for most aspects of managing a cluster in a disconnected environment. For example, when mirroring images you can choose between using the oc-mirror OpenShift CLI (<code>oc</code>) plugin or using the <code>oc adm</code> command.</p>
      <p>However, some options provide a simpler and more convenient user experience for disconnected environments, and are the preferred method over their alternatives.</p>
      <p>Unless your organizational needs require you to choose another option, use the following methods for mirroring images, installing your cluster, and updating your cluster:</p>
      <ul>
      <li>Mirror your images using the oc-mirror plugin v2. For more information, see "Mirroring images for a disconnected installation by using the oc-mirror plugin v2".</li>
      <li>Install your cluster using the Agent-based Installer. For more information, see "Installing a cluster with customizations".</li>
      <li>Update your cluster using a local OpenShift Update Service instance. For more information, see "Updating a cluster in a disconnected environment using the OpenShift Update Service".</li>
      </ul>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/disconnected/about-installing-oc-mirror-v2#about-installing-oc-mirror-v2">Mirroring images for a disconnected installation by using the oc-mirror plugin v2</a></li>
      <li><a href="/openshift-docs-markdown/installing/installing_with_agent_based_installer/installing-with-agent-based-installer#installing-with-agent-based-installer">Installing a cluster with customizations</a></li>
      <li><a href="/openshift-docs-markdown/disconnected/updating/disconnected-update-osus#updating-disconnected-cluster-osus">Updating a cluster in a disconnected environment using the OpenShift Update Service</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
