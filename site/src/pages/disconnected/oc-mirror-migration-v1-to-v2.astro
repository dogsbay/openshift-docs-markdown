---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: disconnected/oc-mirror-migration-v1-to-v2.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"oc-mirror-migration-v1-to-v2","text":"Migrating from oc-mirror plugin v1 to v2"},{"depth":2,"slug":"oc-mirror-migration-differences_oc-mirror-migration-v1-to-v2","text":"Changes from oc-mirror plugin v1 to v2"},{"depth":2,"slug":"oc-mirror-migration-process_oc-mirror-migration-v1-to-v2","text":"Migrating to oc-mirror plugin v2"},{"depth":2,"slug":"additional-resources_oc-mirror-migration-v1-to-v2","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/disconnected/oc-mirror-migration-v1-to-v2";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Migrating from oc-mirror plugin v1 to v2";
const description = "The oc-mirror v2 plugin introduces major changes to image mirroring workflows. Understand the migration process and check for compatibility with oc-mirror…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["disconnected"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Migrating from oc-mirror plugin v1 to v2 {#oc-mirror-migration-v1-to-v2}\n\nThe oc-mirror v2 plugin introduces major changes to image mirroring workflows. Understand the migration process and check for compatibility with oc-mirror plugin v2 to ensure a successful migration.\n\n> [!IMPORTANT]\n> You must manually update the configurations by modifying the API version and removing deprecated fields. For more information, see \"Changes from oc-mirror plugin v1 to v2\".\n\n## Changes from oc-mirror plugin v1 to v2 {#oc-mirror-migration-differences_oc-mirror-migration-v1-to-v2}\n\nBefore migrating from oc-mirror plugin v1 to v2, you must review the updated mirroring workflow commands, which have changed to align with the new v2 workflow.\n\n- For mirror-to-disk, run the following command:\n\n  ```terminal\n  $ oc-mirror --config isc.yaml file://<directory_name> --v2\n  ```\n- For disk-to-mirror, run the following command:\n\n  ```terminal\n  $ oc-mirror --config isc.yaml --from file://<directory_name> docker://<remote_registry> --v2\n  ```\n- For mirror-to-mirror, run the following command:\n\n  ```terminal\n  $ oc-mirror --config isc.yaml --workspace file://<directory_name> docker://<remote_registry> --v2\n  ```\n\n  > [!NOTE]\n  > `--workspace` is now required for mirror-to-mirror operation.\n\n  - API version update: The `ImageSetConfiguration` API version changes from `v1alpha2` (v1) to `v2alpha1` (v2). You must manually update the configuration files before migration.\n  - Configuration changes:\n\n    - `storageConfig` must be removed in oc-mirror plugin v2.\n    - Incremental mirroring is now handled automatically through the working directory or local cache.\n  - Changes in results directory: All custom resources to be applied to the disconnected cluster are generated in the `<workspace_path>/working-dir/cluster-resources` directory after the migration.\n\n    - Outputs in oc-mirror plugin v2 are not stored in the same location as oc-mirror plugin v1.\n    - You must check the `cluster-resources` folder under the working directory for the following resources:\n- `ImageDigestMirrorSet` (IDMS)\n- `ImageTagMirrorSet` (ITMS)\n- `CatalogSource`\n- `ClusterCatalog`\n- `UpdateService`\n\n  - Workspace and directory naming: Follow the new oc-mirror v2 convention to prevent any potential data inconsistencies when transitioning between versions.\n\n    - The oc-mirror plugin v1 `oc-mirror-workspace` directory is no longer needed.\n    - Use a new directory for oc-mirror plugin v2 to avoid conflicts.\n  - Replacing `ImageContentSourcePolicy` (ICSP) resources with IDMS/ITMS:\n\n    > [!IMPORTANT]\n    > Deleting all `ImageContentSourcePolicy` (ICSP) resources might remove configurations unrelated to oc-mirror.\n    >\n    > To avoid unintended deletions, identify ICSP resources generated by oc-mirror before removing them. If you are unsure, check with your cluster administrator. For more information, see \"Mirroring images for a disconnected installation by using the oc-mirror plugin v2\".\n\n    - In oc-mirror plugin v2, the ICSP resource is replaced by `ImageDigestMirrorSet` (IDMS) and `ImageTagMirrorSet` (ITMS) resources.\n\n## Migrating to oc-mirror plugin v2 {#oc-mirror-migration-process_oc-mirror-migration-v1-to-v2}\n\nTo migrate from oc-mirror plugin v1 to v2, you must manually update the `ImageSetConfiguration` file, modify mirroring commands, and clean up v1 artifacts.\n\n**Procedure**\n\n1. Modify the API version and remove deprecated fields in your `ImageSetConfiguration`.\n\n   ```yaml {title=\"Example ImageSetConfiguration file with oc-mirror plugin v1 configuration\"}\n   kind: ImageSetConfiguration\n   apiVersion: mirror.openshift.io/v1alpha2\n   mirror:\n     platform:\n       channels:\n         - name: stable-4.17\n       graph: true\n     helm:\n       repositories:\n         - name: sbo\n           url: https://redhat-developer.github.io/service-binding-operator-helm-chart/\n     additionalImages:\n       - name: registry.redhat.io/ubi8/ubi:latest\n       - name: quay.io/openshifttest/hello-openshift@sha256:example_hash\n     operators:\n       - catalog: oci:///test/redhat-operator-index\n         packages:\n           - name: aws-load-balancer-operator\n   storageConfig:  # REMOVE this field in v2\n     local:\n       path: /var/lib/oc-mirror\n   ```\n\n   ```yaml {title=\"Example ImageSetConfiguration file with oc-mirror plugin v2 configuration\"}\n   kind: ImageSetConfiguration\n   apiVersion: mirror.openshift.io/v2alpha1\n   mirror:\n     platform:\n       channels:\n         - name: stable-4.17\n       graph: true\n     helm:\n       repositories:\n         - name: sbo\n           url: https://redhat-developer.github.io/service-binding-operator-helm-chart/\n     additionalImages:\n       - name: registry.redhat.io/ubi8/ubi:latest\n       - name: quay.io/openshifttest/hello-openshift@sha256:example_hash\n     operators:\n       - catalog: oci:///test/redhat-operator-index\n         packages:\n           - name: aws-load-balancer-operator\n   ```\n\n   > [!NOTE]\n   > When migrating from oc-mirror plugin v1 to v2, you must use explicit registry hostnames for all images listed under `additionalImages`. Otherwise, images are mirrored to incorrect target paths.\n2. Check the `cluster-resources` directory inside the working directory for IDMS, ITMS, `CatalogSource`, and `ClusterCatalog` resources by running the following command:\n\n   ```terminal\n   $ ls <v2_workspace>/working-dir/cluster-resources/\n   ```\n3. Once the migration is complete, verify that mirrored images and catalogs are available:\n\n   - Ensure that no errors or warnings occurred during mirroring.\n   - Ensure that no error file was generated (`working-dir/logs/mirroring_errors_YYYYMMdd_HHmmss.txt`).\n4. Verify that mirrored images and catalogs are available using the following the commands:\n\n   ```terminal\n   $ oc get catalogsource -n openshift-marketplace\n   ```\n\n   ```terminal\n   $ oc get imagedigestmirrorset,imagetagmirrorset\n   ```\n\n   For more information, refer to \"Mirroring images for a disconnected installation using oc-mirror plugin v2\".\n5. Optional: Remove images mirrored using oc-mirror plugin v1:\n\n   1. Mirror the images using oc-mirror plugin v1.\n   2. Update the API version in the `ImageSetConfiguration` file from `v1alpha2` (v1) to `v2alpha1` (v2), then run the following command:\n\n      ```terminal\n      $ oc-mirror -c isc.yaml file://some-dir --v2\n      ```\n\n      > [!NOTE]\n      > `storageConfig` is not a valid field in the `ImageSetConfiguration` and `DeleteImageSetConfiguration` files. Remove this field when updating to oc-mirror plugin v2.\n   3. Generate a delete manifest and delete v1 images by running the following command:\n\n      ```terminal\n      $ oc-mirror delete --config=delete-isc.yaml --generate --delete-v1-images --workspace file://some-dir docker://registry.example:5000  --v2\n      ```\n\n      > [!IMPORTANT]\n      > oc-mirror plugin v2 does not automatically prune the destination registry, unlike oc-mirror plugin v1. To clean up images that are no longer needed, use the delete functionality in v2 with the `--delete-v1-images` command flag.\n      >\n      > Once all images mirrored with oc-mirror plugin v1 are removed, you no longer need to use this flag. If you need to delete images mirrored with oc-mirror plugin v2, do not set `--delete-v1-images`.\n\n      For more information about deleting images, see \"Deletion of images from your disconnected environment\".\n   4. Delete images based on the generated manifest by running the following command:\n\n      ```terminal\n      $ oc-mirror delete --delete-yaml-file some-dir/working-dir/delete/delete-images.yaml docker://registry.example:5000 --v2\n      ```\n\n## Additional resources {#additional-resources_oc-mirror-migration-v1-to-v2}\n\n- [Mirroring an image set in a partially disconnected environment](/openshift-docs-markdown/disconnected/about-installing-oc-mirror-v2#oc-mirror-workflows-partially-disconnected-v2_about-installing-oc-mirror-v2)\n- [Mirroring an image set in a fully disconnected environment](/openshift-docs-markdown/disconnected/about-installing-oc-mirror-v2#oc-mirror-workflows-fully-disconnected-v2_about-installing-oc-mirror-v2)\n- [Changes from oc-mirror plugin v1 to v2](/openshift-docs-markdown/disconnected/oc-mirror-migration-v1-to-v2#oc-mirror-migration-differences_oc-mirror-migration-v1-to-v2)\n- [Deletion of images from your disconnected environment](/openshift-docs-markdown/disconnected/about-installing-oc-mirror-v2#oc-mirror-procedure-delete-v2_about-installing-oc-mirror-v2)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/disconnected/oc-mirror-migration-v1-to-v2.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="oc-mirror-migration-v1-to-v2" class="group scroll-mt-20">Migrating from oc-mirror plugin v1 to v2<a href="#oc-mirror-migration-v1-to-v2" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>The oc-mirror v2 plugin introduces major changes to image mirroring workflows. Understand the migration process and check for compatibility with oc-mirror plugin v2 to ensure a successful migration.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>You must manually update the configurations by modifying the API version and removing deprecated fields. For more information, see "Changes from oc-mirror plugin v1 to v2".</p>
        </AlertDescription>
      </Alert>
      <h2 id="oc-mirror-migration-differences_oc-mirror-migration-v1-to-v2" class="group scroll-mt-20">Changes from oc-mirror plugin v1 to v2<a href="#oc-mirror-migration-differences_oc-mirror-migration-v1-to-v2" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Before migrating from oc-mirror plugin v1 to v2, you must review the updated mirroring workflow commands, which have changed to align with the new v2 workflow.</p>
      <ul>
      <li>
      For mirror-to-disk, run the following command:
      <CodeBlock code={"$ oc-mirror --config isc.yaml file://<directory_name> --v2"} lang="terminal" />
      </li>
      <li>
      For disk-to-mirror, run the following command:
      <CodeBlock code={"$ oc-mirror --config isc.yaml --from file://<directory_name> docker://<remote_registry> --v2"} lang="terminal" />
      </li>
      <li>
      For mirror-to-mirror, run the following command:
      <CodeBlock code={"$ oc-mirror --config isc.yaml --workspace file://<directory_name> docker://<remote_registry> --v2"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p><code>--workspace</code> is now required for mirror-to-mirror operation.</p>
        </AlertDescription>
      </Alert>
      <ul>
      <li>API version update: The <code>ImageSetConfiguration</code> API version changes from <code>v1alpha2</code> (v1) to <code>v2alpha1</code> (v2). You must manually update the configuration files before migration.</li>
      <li>
      Configuration changes:
      <ul>
      <li><code>storageConfig</code> must be removed in oc-mirror plugin v2.</li>
      <li>Incremental mirroring is now handled automatically through the working directory or local cache.</li>
      </ul>
      </li>
      <li>
      Changes in results directory: All custom resources to be applied to the disconnected cluster are generated in the <code>&lt;workspace_path&gt;/working-dir/cluster-resources</code> directory after the migration.
      <ul>
      <li>Outputs in oc-mirror plugin v2 are not stored in the same location as oc-mirror plugin v1.</li>
      <li>You must check the <code>cluster-resources</code> folder under the working directory for the following resources:</li>
      </ul>
      </li>
      </ul>
      </li>
      <li><code>ImageDigestMirrorSet</code> (IDMS)</li>
      <li><code>ImageTagMirrorSet</code> (ITMS)</li>
      <li><code>CatalogSource</code></li>
      <li><code>ClusterCatalog</code></li>
      <li>
      <code>UpdateService</code>
      <ul>
      <li>
      Workspace and directory naming: Follow the new oc-mirror v2 convention to prevent any potential data inconsistencies when transitioning between versions.
      <ul>
      <li>The oc-mirror plugin v1 <code>oc-mirror-workspace</code> directory is no longer needed.</li>
      <li>Use a new directory for oc-mirror plugin v2 to avoid conflicts.</li>
      </ul>
      </li>
      <li>
      Replacing <code>ImageContentSourcePolicy</code> (ICSP) resources with IDMS/ITMS:
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Deleting all <code>ImageContentSourcePolicy</code> (ICSP) resources might remove configurations unrelated to oc-mirror.</p>
          <p>To avoid unintended deletions, identify ICSP resources generated by oc-mirror before removing them. If you are unsure, check with your cluster administrator. For more information, see "Mirroring images for a disconnected installation by using the oc-mirror plugin v2".</p>
        </AlertDescription>
      </Alert>
      <ul><li>In oc-mirror plugin v2, the ICSP resource is replaced by <code>ImageDigestMirrorSet</code> (IDMS) and <code>ImageTagMirrorSet</code> (ITMS) resources.</li></ul>
      </li>
      </ul>
      </li>
      </ul>
      <h2 id="oc-mirror-migration-process_oc-mirror-migration-v1-to-v2" class="group scroll-mt-20">Migrating to oc-mirror plugin v2<a href="#oc-mirror-migration-process_oc-mirror-migration-v1-to-v2" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To migrate from oc-mirror plugin v1 to v2, you must manually update the <code>ImageSetConfiguration</code> file, modify mirroring commands, and clean up v1 artifacts.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Modify the API version and remove deprecated fields in your <code>ImageSetConfiguration</code>.
      <CodeBlock code={"kind: ImageSetConfiguration\napiVersion: mirror.openshift.io/v1alpha2\nmirror:\n  platform:\n    channels:\n      - name: stable-4.17\n    graph: true\n  helm:\n    repositories:\n      - name: sbo\n        url: https://redhat-developer.github.io/service-binding-operator-helm-chart/\n  additionalImages:\n    - name: registry.redhat.io/ubi8/ubi:latest\n    - name: quay.io/openshifttest/hello-openshift@sha256:example_hash\n  operators:\n    - catalog: oci:///test/redhat-operator-index\n      packages:\n        - name: aws-load-balancer-operator\nstorageConfig:  # REMOVE this field in v2\n  local:\n    path: /var/lib/oc-mirror"} lang="yaml" title="Example ImageSetConfiguration file with oc-mirror plugin v1 configuration" />
      <CodeBlock code={"kind: ImageSetConfiguration\napiVersion: mirror.openshift.io/v2alpha1\nmirror:\n  platform:\n    channels:\n      - name: stable-4.17\n    graph: true\n  helm:\n    repositories:\n      - name: sbo\n        url: https://redhat-developer.github.io/service-binding-operator-helm-chart/\n  additionalImages:\n    - name: registry.redhat.io/ubi8/ubi:latest\n    - name: quay.io/openshifttest/hello-openshift@sha256:example_hash\n  operators:\n    - catalog: oci:///test/redhat-operator-index\n      packages:\n        - name: aws-load-balancer-operator"} lang="yaml" title="Example ImageSetConfiguration file with oc-mirror plugin v2 configuration" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>When migrating from oc-mirror plugin v1 to v2, you must use explicit registry hostnames for all images listed under <code>additionalImages</code>. Otherwise, images are mirrored to incorrect target paths.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      Check the <code>cluster-resources</code> directory inside the working directory for IDMS, ITMS, <code>CatalogSource</code>, and <code>ClusterCatalog</code> resources by running the following command:
      <CodeBlock code={"$ ls <v2_workspace>/working-dir/cluster-resources/"} lang="terminal" />
      </li>
      <li>
      Once the migration is complete, verify that mirrored images and catalogs are available:
      <ul>
      <li>Ensure that no errors or warnings occurred during mirroring.</li>
      <li>Ensure that no error file was generated (<code>working-dir/logs/mirroring_errors_YYYYMMdd_HHmmss.txt</code>).</li>
      </ul>
      </li>
      <li>
      Verify that mirrored images and catalogs are available using the following the commands:
      <CodeBlock code={"$ oc get catalogsource -n openshift-marketplace"} lang="terminal" />
      <CodeBlock code={"$ oc get imagedigestmirrorset,imagetagmirrorset"} lang="terminal" />
      <p>For more information, refer to "Mirroring images for a disconnected installation using oc-mirror plugin v2".</p>
      </li>
      <li>
      Optional: Remove images mirrored using oc-mirror plugin v1:
      <ol>
      <li>Mirror the images using oc-mirror plugin v1.</li>
      <li>
      Update the API version in the <code>ImageSetConfiguration</code> file from <code>v1alpha2</code> (v1) to <code>v2alpha1</code> (v2), then run the following command:
      <CodeBlock code={"$ oc-mirror -c isc.yaml file://some-dir --v2"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p><code>storageConfig</code> is not a valid field in the <code>ImageSetConfiguration</code> and <code>DeleteImageSetConfiguration</code> files. Remove this field when updating to oc-mirror plugin v2.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      Generate a delete manifest and delete v1 images by running the following command:
      <CodeBlock code={"$ oc-mirror delete --config=delete-isc.yaml --generate --delete-v1-images --workspace file://some-dir docker://registry.example:5000  --v2"} lang="terminal" />
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>oc-mirror plugin v2 does not automatically prune the destination registry, unlike oc-mirror plugin v1. To clean up images that are no longer needed, use the delete functionality in v2 with the <code>--delete-v1-images</code> command flag.</p>
          <p>Once all images mirrored with oc-mirror plugin v1 are removed, you no longer need to use this flag. If you need to delete images mirrored with oc-mirror plugin v2, do not set <code>--delete-v1-images</code>.</p>
        </AlertDescription>
      </Alert>
      <p>For more information about deleting images, see "Deletion of images from your disconnected environment".</p>
      </li>
      <li>
      Delete images based on the generated manifest by running the following command:
      <CodeBlock code={"$ oc-mirror delete --delete-yaml-file some-dir/working-dir/delete/delete-images.yaml docker://registry.example:5000 --v2"} lang="terminal" />
      </li>
      </ol>
      </li>
      </ol>
      <h2 id="additional-resources_oc-mirror-migration-v1-to-v2" class="group scroll-mt-20">Additional resources<a href="#additional-resources_oc-mirror-migration-v1-to-v2" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/disconnected/about-installing-oc-mirror-v2#oc-mirror-workflows-partially-disconnected-v2_about-installing-oc-mirror-v2">Mirroring an image set in a partially disconnected environment</a></li>
      <li><a href="/openshift-docs-markdown/disconnected/about-installing-oc-mirror-v2#oc-mirror-workflows-fully-disconnected-v2_about-installing-oc-mirror-v2">Mirroring an image set in a fully disconnected environment</a></li>
      <li><a href="/openshift-docs-markdown/disconnected/oc-mirror-migration-v1-to-v2#oc-mirror-migration-differences_oc-mirror-migration-v1-to-v2">Changes from oc-mirror plugin v1 to v2</a></li>
      <li><a href="/openshift-docs-markdown/disconnected/about-installing-oc-mirror-v2#oc-mirror-procedure-delete-v2_about-installing-oc-mirror-v2">Deletion of images from your disconnected environment</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
