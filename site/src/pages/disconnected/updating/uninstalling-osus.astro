---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: disconnected/updating/uninstalling-osus.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"uninstalling-osus","text":"Uninstalling the OpenShift Update Service from a cluster"},{"depth":2,"slug":"update-service-delete-service_uninstalling-osus","text":"Deleting an OpenShift Update Service application"},{"depth":3,"slug":"update-service-delete-service-web-console_uninstalling-osus","text":"Deleting an OpenShift Update Service application by using the web console"},{"depth":3,"slug":"update-service-delete-service-cli_uninstalling-osus","text":"Deleting an OpenShift Update Service application by using the CLI"},{"depth":2,"slug":"update-service-uninstall_uninstalling-osus","text":"Uninstalling the OpenShift Update Service Operator"},{"depth":3,"slug":"update-service-uninstall-web-console_uninstalling-osus","text":"Uninstalling the OpenShift Update Service Operator by using the web console"},{"depth":3,"slug":"update-service-uninstall-cli_uninstalling-osus","text":"Uninstalling the OpenShift Update Service Operator by using the CLI"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/disconnected/updating/uninstalling-osus";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Uninstalling the OpenShift Update Service from a cluster";
const description = "You can remove a local copy of the OpenShift Update Service (OSUS) from your cluster. To do this, you must first delete the OSUS application and then…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["disconnected","disconnected/updating"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Uninstalling the OpenShift Update Service from a cluster {#uninstalling-osus}\n\nYou can remove a local copy of the OpenShift Update Service (OSUS) from your cluster. To do this, you must first delete the OSUS application and then uninstall the OSUS Operator.\n\n## Deleting an OpenShift Update Service application {#update-service-delete-service_uninstalling-osus}\n\nYou can delete an OpenShift Update Service application by using the OpenShift Container Platform web console or CLI.\n\n### Deleting an OpenShift Update Service application by using the web console {#update-service-delete-service-web-console_uninstalling-osus}\n\nYou can use the OpenShift Container Platform web console to delete an OpenShift Update Service application by using the OpenShift Update Service Operator.\n\n**Prerequisites**\n\n- The OpenShift Update Service Operator has been installed.\n\n**Procedure**\n\n1. In the web console, click **Ecosystem** -> **Installed Operators**.\n2. Choose **OpenShift Update Service** from the list of installed Operators.\n3. Click the **Update Service** tab.\n4. From the list of installed OpenShift Update Service applications, select the application to be deleted and then click **Delete UpdateService**.\n5. From the **Delete UpdateService?** confirmation dialog, click **Delete** to confirm the deletion.\n\n### Deleting an OpenShift Update Service application by using the CLI {#update-service-delete-service-cli_uninstalling-osus}\n\nYou can use the OpenShift CLI (`oc`) to delete an OpenShift Update Service application.\n\n**Procedure**\n\n1. Get the OpenShift Update Service application name using the namespace the OpenShift Update Service application was created in, for example, `openshift-update-service`:\n\n   ```terminal\n   $ oc get updateservice -n openshift-update-service\n   ```\n\n   ```terminal {title=\"Example output\"}\n   NAME      AGE\n   service   6s\n   ```\n2. Delete the OpenShift Update Service application using the `NAME` value from the previous step and the namespace the OpenShift Update Service application was created in, for example, `openshift-update-service`:\n\n   ```terminal\n   $ oc delete updateservice service -n openshift-update-service\n   ```\n\n   ```terminal {title=\"Example output\"}\n   updateservice.updateservice.operator.openshift.io \"service\" deleted\n   ```\n\n## Uninstalling the OpenShift Update Service Operator {#update-service-uninstall_uninstalling-osus}\n\nYou can uninstall the OpenShift Update Service Operator by using the OpenShift Container Platform web console or CLI.\n\n### Uninstalling the OpenShift Update Service Operator by using the web console {#update-service-uninstall-web-console_uninstalling-osus}\n\nYou can use the OpenShift Container Platform web console to uninstall the OpenShift Update Service Operator.\n\n**Prerequisites**\n\n- All OpenShift Update Service applications have been deleted.\n\n**Procedure**\n\n1. In the web console, click **Ecosystem** -> **Installed Operators**.\n2. Select **OpenShift Update Service** from the list of installed Operators and click **Uninstall Operator**.\n3. From the **Uninstall Operator?** confirmation dialog, click **Uninstall** to confirm the uninstallation.\n\n### Uninstalling the OpenShift Update Service Operator by using the CLI {#update-service-uninstall-cli_uninstalling-osus}\n\nYou can use the OpenShift CLI (`oc`) to uninstall the OpenShift Update Service Operator.\n\n**Prerequisites**\n\n- All OpenShift Update Service applications have been deleted.\n\n**Procedure**\n\n1. Change to the project containing the OpenShift Update Service Operator, for example, `openshift-update-service`:\n\n   ```terminal\n   $ oc project openshift-update-service\n   ```\n\n   ```terminal {title=\"Example output\"}\n   Now using project \"openshift-update-service\" on server \"https://example.com:6443\".\n   ```\n2. Get the name of the OpenShift Update Service Operator operator group:\n\n   ```terminal\n   $ oc get operatorgroup\n   ```\n\n   ```terminal {title=\"Example output\"}\n   NAME                             AGE\n   openshift-update-service-fprx2   4m41s\n   ```\n3. Delete the operator group, for example, `openshift-update-service-fprx2`:\n\n   ```terminal\n   $ oc delete operatorgroup openshift-update-service-fprx2\n   ```\n\n   ```terminal {title=\"Example output\"}\n   operatorgroup.operators.coreos.com \"openshift-update-service-fprx2\" deleted\n   ```\n4. Get the name of the OpenShift Update Service Operator subscription:\n\n   ```terminal\n   $ oc get subscription\n   ```\n\n   ```terminal {title=\"Example output\"}\n   NAME                      PACKAGE                   SOURCE                        CHANNEL\n   update-service-operator   update-service-operator   updateservice-index-catalog   v1\n   ```\n5. Using the `Name` value from the previous step, check the current version of the subscribed OpenShift Update Service Operator in the `currentCSV` field:\n\n   ```terminal\n   $ oc get subscription update-service-operator -o yaml | grep \" currentCSV\"\n   ```\n\n   ```terminal {title=\"Example output\"}\n     currentCSV: update-service-operator.v0.0.1\n   ```\n6. Delete the subscription, for example, `update-service-operator`:\n\n   ```terminal\n   $ oc delete subscription update-service-operator\n   ```\n\n   ```terminal {title=\"Example output\"}\n   subscription.operators.coreos.com \"update-service-operator\" deleted\n   ```\n7. Delete the CSV for the OpenShift Update Service Operator using the `currentCSV` value from the previous step:\n\n   ```terminal\n   $ oc delete clusterserviceversion update-service-operator.v0.0.1\n   ```\n\n   ```terminal {title=\"Example output\"}\n   clusterserviceversion.operators.coreos.com \"update-service-operator.v0.0.1\" deleted\n   ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/disconnected/updating/uninstalling-osus.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="uninstalling-osus" class="group scroll-mt-20">Uninstalling the OpenShift Update Service from a cluster<a href="#uninstalling-osus" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can remove a local copy of the OpenShift Update Service (OSUS) from your cluster. To do this, you must first delete the OSUS application and then uninstall the OSUS Operator.</p>
      <h2 id="update-service-delete-service_uninstalling-osus" class="group scroll-mt-20">Deleting an OpenShift Update Service application<a href="#update-service-delete-service_uninstalling-osus" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can delete an OpenShift Update Service application by using the OpenShift Container Platform web console or CLI.</p>
      <h3 id="update-service-delete-service-web-console_uninstalling-osus" class="group scroll-mt-20">Deleting an OpenShift Update Service application by using the web console<a href="#update-service-delete-service-web-console_uninstalling-osus" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can use the OpenShift Container Platform web console to delete an OpenShift Update Service application by using the OpenShift Update Service Operator.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>The OpenShift Update Service Operator has been installed.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the web console, click <strong>Ecosystem</strong> -&gt; <strong>Installed Operators</strong>.</li>
      <li>Choose <strong>OpenShift Update Service</strong> from the list of installed Operators.</li>
      <li>Click the <strong>Update Service</strong> tab.</li>
      <li>From the list of installed OpenShift Update Service applications, select the application to be deleted and then click <strong>Delete UpdateService</strong>.</li>
      <li>From the <strong>Delete UpdateService?</strong> confirmation dialog, click <strong>Delete</strong> to confirm the deletion.</li>
      </ol>
      <h3 id="update-service-delete-service-cli_uninstalling-osus" class="group scroll-mt-20">Deleting an OpenShift Update Service application by using the CLI<a href="#update-service-delete-service-cli_uninstalling-osus" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can use the OpenShift CLI (<code>oc</code>) to delete an OpenShift Update Service application.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Get the OpenShift Update Service application name using the namespace the OpenShift Update Service application was created in, for example, <code>openshift-update-service</code>:
      <CodeBlock code={"$ oc get updateservice -n openshift-update-service"} lang="terminal" />
      <CodeBlock code={"NAME      AGE\nservice   6s"} lang="terminal" title="Example output" />
      </li>
      <li>
      Delete the OpenShift Update Service application using the <code>NAME</code> value from the previous step and the namespace the OpenShift Update Service application was created in, for example, <code>openshift-update-service</code>:
      <CodeBlock code={"$ oc delete updateservice service -n openshift-update-service"} lang="terminal" />
      <CodeBlock code={"updateservice.updateservice.operator.openshift.io \"service\" deleted"} lang="terminal" title="Example output" />
      </li>
      </ol>
      <h2 id="update-service-uninstall_uninstalling-osus" class="group scroll-mt-20">Uninstalling the OpenShift Update Service Operator<a href="#update-service-uninstall_uninstalling-osus" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can uninstall the OpenShift Update Service Operator by using the OpenShift Container Platform web console or CLI.</p>
      <h3 id="update-service-uninstall-web-console_uninstalling-osus" class="group scroll-mt-20">Uninstalling the OpenShift Update Service Operator by using the web console<a href="#update-service-uninstall-web-console_uninstalling-osus" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can use the OpenShift Container Platform web console to uninstall the OpenShift Update Service Operator.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>All OpenShift Update Service applications have been deleted.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the web console, click <strong>Ecosystem</strong> -&gt; <strong>Installed Operators</strong>.</li>
      <li>Select <strong>OpenShift Update Service</strong> from the list of installed Operators and click <strong>Uninstall Operator</strong>.</li>
      <li>From the <strong>Uninstall Operator?</strong> confirmation dialog, click <strong>Uninstall</strong> to confirm the uninstallation.</li>
      </ol>
      <h3 id="update-service-uninstall-cli_uninstalling-osus" class="group scroll-mt-20">Uninstalling the OpenShift Update Service Operator by using the CLI<a href="#update-service-uninstall-cli_uninstalling-osus" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can use the OpenShift CLI (<code>oc</code>) to uninstall the OpenShift Update Service Operator.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>All OpenShift Update Service applications have been deleted.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Change to the project containing the OpenShift Update Service Operator, for example, <code>openshift-update-service</code>:
      <CodeBlock code={"$ oc project openshift-update-service"} lang="terminal" />
      <CodeBlock code={"Now using project \"openshift-update-service\" on server \"https://example.com:6443\"."} lang="terminal" title="Example output" />
      </li>
      <li>
      Get the name of the OpenShift Update Service Operator operator group:
      <CodeBlock code={"$ oc get operatorgroup"} lang="terminal" />
      <CodeBlock code={"NAME                             AGE\nopenshift-update-service-fprx2   4m41s"} lang="terminal" title="Example output" />
      </li>
      <li>
      Delete the operator group, for example, <code>openshift-update-service-fprx2</code>:
      <CodeBlock code={"$ oc delete operatorgroup openshift-update-service-fprx2"} lang="terminal" />
      <CodeBlock code={"operatorgroup.operators.coreos.com \"openshift-update-service-fprx2\" deleted"} lang="terminal" title="Example output" />
      </li>
      <li>
      Get the name of the OpenShift Update Service Operator subscription:
      <CodeBlock code={"$ oc get subscription"} lang="terminal" />
      <CodeBlock code={"NAME                      PACKAGE                   SOURCE                        CHANNEL\nupdate-service-operator   update-service-operator   updateservice-index-catalog   v1"} lang="terminal" title="Example output" />
      </li>
      <li>
      Using the <code>Name</code> value from the previous step, check the current version of the subscribed OpenShift Update Service Operator in the <code>currentCSV</code> field:
      <CodeBlock code={"$ oc get subscription update-service-operator -o yaml | grep \" currentCSV\""} lang="terminal" />
      <CodeBlock code={"  currentCSV: update-service-operator.v0.0.1"} lang="terminal" title="Example output" />
      </li>
      <li>
      Delete the subscription, for example, <code>update-service-operator</code>:
      <CodeBlock code={"$ oc delete subscription update-service-operator"} lang="terminal" />
      <CodeBlock code={"subscription.operators.coreos.com \"update-service-operator\" deleted"} lang="terminal" title="Example output" />
      </li>
      <li>
      Delete the CSV for the OpenShift Update Service Operator using the <code>currentCSV</code> value from the previous step:
      <CodeBlock code={"$ oc delete clusterserviceversion update-service-operator.v0.0.1"} lang="terminal" />
      <CodeBlock code={"clusterserviceversion.operators.coreos.com \"update-service-operator.v0.0.1\" deleted"} lang="terminal" title="Example output" />
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
