---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: disconnected/updating/mirroring-image-repository.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";

const headings = [{"depth":1,"slug":"mirroring-ocp-image-repository","text":"Mirroring OpenShift Container Platform images"}];
const nav = navData;
const currentPath = "/disconnected/updating/mirroring-image-repository";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Mirroring OpenShift Container Platform images";
const description = "You must mirror container images onto a mirror registry to update a cluster in a disconnected environment. In connected environments, mirroring images…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["disconnected","disconnected/updating"];
const pageTaxonomies = null;
const tagsIndexPath = "/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Mirroring OpenShift Container Platform images {#mirroring-ocp-image-repository}\n\nYou must mirror container images onto a mirror registry to update a cluster in a disconnected environment. In connected environments, mirroring images ensures that your clusters run only approved container images that meet your organizational controls for external content.\n\n> [!NOTE]\n> Your mirror registry must be running at all times while the cluster is running.\n\nThe following steps outline the high-level workflow about mirroring images to a mirror registry:\n\n1. Install the OpenShift CLI (`oc`) on all devices that you use to retrieve and push release images.\n2. Download the registry pull secret and add it to your cluster.\n3. Choose the appropriate method to mirror your images:\n\n   1. Method 1: If you use the oc-mirror OpenShift CLI (`oc`) plugin:\n\n      1. Install the oc-mirror plugin on all devices that you use to retrieve and push release images.\n      2. Create an image set configuration file for the plugin to use when determining which release images to mirror. You can edit this configuration file later to change which release images that the plugin mirrors.\n      3. Mirror your targeted release images directly to a mirror registry, or to removable media and then to a mirror registry.\n      4. Configure your cluster to use the resources generated by the oc-mirror plugin.\n      5. Repeat these steps as needed to update your mirror registry.\n   2. Method 2: If you use the `oc adm release mirror` command:\n\n      1. Set environment variables that correspond to your environment and the release images you want to mirror.\n      2. Mirror your targeted release images directly to a mirror registry, or to removable media and then to a mirror registry.\n      3. Repeat these steps as needed to update your mirror registry.\n\nThe `oc-mirror` plugin offers the following advantages over the `oc adm release mirror` command:\n\n- Mirrors content other than container images.\n- Simplifies updating images in the registry after the initial mirror.\n- Automates mirroring the release payload from Quay and builds the latest graph data image for the OpenShift Update Service in disconnected environments.\n\n**Additional resources**\n\n- [Mirroring images for a disconnected installation by using the oc-mirror plugin v2](/disconnected/about-installing-oc-mirror-v2#about-installing-oc-mirror-v2)\n\n**Additional resources**\n\n- [Deploying Red Hat Quay for proof-of-concept purposes](https://docs.redhat.com/en/documentation/red_hat_quay/3/html/proof_of_concept_-_deploying_red_hat_quay/index)\n- [Deploying Red Hat Quay by using the Quay Operator](https://access.redhat.com/documentation/en-us/red_hat_quay/3/html/deploying_the_red_hat_quay_operator_on_openshift_container_platform/index)\n- [Mirror registry for Red Hat OpenShift](/disconnected/installing-mirroring-creating-registry#installing-mirroring-creating-registry)\n\n**Additional resources**\n\n- [Installing and using CLI plugins](/cli_reference/openshift_cli/extending-cli-plugins#cli-installing-plugins_cli-extend-plugins)\n","mdUrl":"/disconnected/updating/mirroring-image-repository.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={""}
  navMode={"client"}
  pagefindUrl={"/pagefind/"}
  favicon={"/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="mirroring-ocp-image-repository" class="group scroll-mt-20">Mirroring OpenShift Container Platform images<a href="#mirroring-ocp-image-repository" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="/_attributes/common-attributes.md"></p>
      <p>You must mirror container images onto a mirror registry to update a cluster in a disconnected environment. In connected environments, mirroring images ensures that your clusters run only approved container images that meet your organizational controls for external content.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Your mirror registry must be running at all times while the cluster is running.</p>
        </AlertDescription>
      </Alert>
      <p>The following steps outline the high-level workflow about mirroring images to a mirror registry:</p>
      <ol>
      <li>Install the OpenShift CLI (<code>oc</code>) on all devices that you use to retrieve and push release images.</li>
      <li>Download the registry pull secret and add it to your cluster.</li>
      <li>
      Choose the appropriate method to mirror your images:
      <ol>
      <li>
      Method 1: If you use the oc-mirror OpenShift CLI (<code>oc</code>) plugin:
      <ol>
      <li>Install the oc-mirror plugin on all devices that you use to retrieve and push release images.</li>
      <li>Create an image set configuration file for the plugin to use when determining which release images to mirror. You can edit this configuration file later to change which release images that the plugin mirrors.</li>
      <li>Mirror your targeted release images directly to a mirror registry, or to removable media and then to a mirror registry.</li>
      <li>Configure your cluster to use the resources generated by the oc-mirror plugin.</li>
      <li>Repeat these steps as needed to update your mirror registry.</li>
      </ol>
      </li>
      <li>
      Method 2: If you use the <code>oc adm release mirror</code> command:
      <ol>
      <li>Set environment variables that correspond to your environment and the release images you want to mirror.</li>
      <li>Mirror your targeted release images directly to a mirror registry, or to removable media and then to a mirror registry.</li>
      <li>Repeat these steps as needed to update your mirror registry.</li>
      </ol>
      </li>
      </ol>
      </li>
      </ol>
      <p>The <code>oc-mirror</code> plugin offers the following advantages over the <code>oc adm release mirror</code> command:</p>
      <ul>
      <li>Mirrors content other than container images.</li>
      <li>Simplifies updating images in the registry after the initial mirror.</li>
      <li>Automates mirroring the release payload from Quay and builds the latest graph data image for the OpenShift Update Service in disconnected environments.</li>
      </ul>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/disconnected/about-installing-oc-mirror-v2#about-installing-oc-mirror-v2">Mirroring images for a disconnected installation by using the oc-mirror plugin v2</a></li></ul>
      <p><strong>Additional resources</strong></p>
      <ul>
      <li><a href="https://docs.redhat.com/en/documentation/red_hat_quay/3/html/proof_of_concept_-_deploying_red_hat_quay/index">Deploying Red Hat Quay for proof-of-concept purposes</a></li>
      <li><a href="https://access.redhat.com/documentation/en-us/red_hat_quay/3/html/deploying_the_red_hat_quay_operator_on_openshift_container_platform/index">Deploying Red Hat Quay by using the Quay Operator</a></li>
      <li><a href="/disconnected/installing-mirroring-creating-registry#installing-mirroring-creating-registry">Mirror registry for Red Hat OpenShift</a></li>
      </ul>
      <p class="/modules/cli-installing-cli-linux.md"></p>
      <p class="/modules/cli-installing-cli-windows.md"></p>
      <p class="/modules/cli-installing-cli-macos.md"></p>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/cli_reference/openshift_cli/extending-cli-plugins#cli-installing-plugins_cli-extend-plugins">Installing and using CLI plugins</a></li></ul>
      <p class="/modules/update-mirror-repository.md"></p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
