---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: support/troubleshooting/verifying-node-health.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"verifying-node-health","text":"Verifying node health"},{"depth":2,"slug":"reviewing-node-status-use-and-configuration_verifying-node-health","text":"Reviewing node status, resource usage, and configuration"},{"depth":2,"slug":"querying-kubelet-status-on-a-node_verifying-node-health","text":"Querying the kubelet’s status on a node"},{"depth":2,"slug":"querying-cluster-node-journal-logs_verifying-node-health","text":"Querying cluster node journal logs"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/support/troubleshooting/verifying-node-health";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Verifying node health";
const description = "You can verify and troubleshoot node-related issues by reviewing the status, resource usage, and configuration of a node." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["support","support/troubleshooting"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Verifying node health {#verifying-node-health}\n\nYou can verify and troubleshoot node-related issues by reviewing the status, resource usage, and configuration of a node.\n\n## Reviewing node status, resource usage, and configuration {#reviewing-node-status-use-and-configuration_verifying-node-health}\n\nReview cluster node health status, resource consumption statistics, and node logs. Additionally, query `kubelet` status on individual nodes.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n- List the name, status, and role for all nodes in the cluster:\n\n  ```terminal\n  $ oc get nodes\n  ```\n- Summarize CPU and memory usage for each node within the cluster:\n\n  ```terminal\n  $ oc adm top nodes\n  ```\n- Summarize CPU and memory usage for a specific node:\n\n  ```terminal\n  $ oc adm top node my-node\n  ```\n\n## Querying the kubelet’s status on a node {#querying-kubelet-status-on-a-node_verifying-node-health}\n\nYou can review cluster node health status, resource consumption statistics, and node logs. Additionally, you can query `kubelet` status on individual nodes.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- Your API service is still functional.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. The kubelet is managed using a systemd service on each node. Review the kubelet’s status by querying the `kubelet` systemd service within a debug pod.\n\n   1. Start a debug pod for a node:\n\n      ```terminal\n      $ oc debug node/my-node\n      ```\n\n      > [!NOTE]\n      > If you are running `oc debug` on a control plane node, you can find administrative `kubeconfig` files in the `/etc/kubernetes/static-pod-resources/kube-apiserver-certs/secrets/node-kubeconfigs` directory.\n   2. Set `/host` as the root directory within the debug shell. The debug pod mounts the host’s root file system in `/host` within the pod. By changing the root directory to `/host`, you can run binaries contained in the host’s executable paths:\n\n      ```terminal\n      # chroot /host\n      ```\n\n      > [!NOTE]\n      > OpenShift Container Platform 4.22 cluster nodes running Red Hat Enterprise Linux CoreOS (RHCOS) are immutable and rely on Operators to apply cluster changes. Accessing cluster nodes by using SSH is not recommended. However, if the OpenShift Container Platform API is not available, or `kubelet` is not properly functioning on the target node, `oc` operations will be impacted. In such situations, it is possible to access nodes using `ssh core@<node>.<cluster_name>.<base_domain>` instead.\n   3. Check whether the `kubelet` systemd service is active on the node:\n\n      ```terminal\n      # systemctl is-active kubelet\n      ```\n   4. Output a more detailed `kubelet.service` status summary:\n\n      ```terminal\n      # systemctl status kubelet\n      ```\n\n## Querying cluster node journal logs {#querying-cluster-node-journal-logs_verifying-node-health}\n\nYou can gather `journald` unit logs and other logs within `/var/log` on individual cluster nodes.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- You have installed the OpenShift CLI (`oc`).\n- Your API service is still functional.\n- You have SSH access to your hosts.\n\n**Procedure**\n\n1. Query `kubelet` `journald` unit logs from OpenShift Container Platform cluster nodes. The following example queries control plane nodes only:\n\n   ```terminal\n   $ oc adm node-logs --role=master -u kubelet  (1)\n   ```\n\n   `kubelet`\n   :   Replace as appropriate to query other unit logs.\n2. Collect logs from specific subdirectories under `/var/log/` on cluster nodes.\n\n   1. Retrieve a list of logs contained within a `/var/log/` subdirectory. The following example lists files in `/var/log/openshift-apiserver/` on all control plane nodes:\n\n      ```terminal\n      $ oc adm node-logs --role=master --path=openshift-apiserver\n      ```\n   2. Inspect a specific log within a `/var/log/` subdirectory. The following example outputs `/var/log/openshift-apiserver/audit.log` contents from all control plane nodes:\n\n      ```terminal\n      $ oc adm node-logs --role=master --path=openshift-apiserver/audit.log\n      ```\n   3. If the API is not functional, review the logs on each node using SSH instead. The following example tails `/var/log/openshift-apiserver/audit.log`:\n\n      ```terminal\n      $ ssh core@<master-node>.<cluster_name>.<base_domain> sudo tail -f /var/log/openshift-apiserver/audit.log\n      ```\n\n      > [!NOTE]\n      > OpenShift Container Platform 4.22 cluster nodes running Red Hat Enterprise Linux CoreOS (RHCOS) are immutable and rely on Operators to apply cluster changes. Accessing cluster nodes by using SSH is not recommended. Before attempting to collect diagnostic data over SSH, review whether the data collected by running `oc adm must gather` and other `oc` commands is sufficient instead. However, if the OpenShift Container Platform API is not available, or the kubelet is not properly functioning on the target node, `oc` operations will be impacted. In such situations, it is possible to access nodes using `ssh core@<node>.<cluster_name>.<base_domain>`.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/support/troubleshooting/verifying-node-health.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="verifying-node-health" class="group scroll-mt-20">Verifying node health<a href="#verifying-node-health" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can verify and troubleshoot node-related issues by reviewing the status, resource usage, and configuration of a node.</p>
      <h2 id="reviewing-node-status-use-and-configuration_verifying-node-health" class="group scroll-mt-20">Reviewing node status, resource usage, and configuration<a href="#reviewing-node-status-use-and-configuration_verifying-node-health" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Review cluster node health status, resource consumption statistics, and node logs. Additionally, query <code>kubelet</code> status on individual nodes.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      List the name, status, and role for all nodes in the cluster:
      <CodeBlock code={"$ oc get nodes"} lang="terminal" />
      </li>
      <li>
      Summarize CPU and memory usage for each node within the cluster:
      <CodeBlock code={"$ oc adm top nodes"} lang="terminal" />
      </li>
      <li>
      Summarize CPU and memory usage for a specific node:
      <CodeBlock code={"$ oc adm top node my-node"} lang="terminal" />
      </li>
      </ul>
      <h2 id="querying-kubelet-status-on-a-node_verifying-node-health" class="group scroll-mt-20">Querying the kubelet’s status on a node<a href="#querying-kubelet-status-on-a-node_verifying-node-health" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can review cluster node health status, resource consumption statistics, and node logs. Additionally, you can query <code>kubelet</code> status on individual nodes.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>Your API service is still functional.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      The kubelet is managed using a systemd service on each node. Review the kubelet’s status by querying the <code>kubelet</code> systemd service within a debug pod.
      <ol>
      <li>
      Start a debug pod for a node:
      <CodeBlock code={"$ oc debug node/my-node"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If you are running <code>oc debug</code> on a control plane node, you can find administrative <code>kubeconfig</code> files in the <code>/etc/kubernetes/static-pod-resources/kube-apiserver-certs/secrets/node-kubeconfigs</code> directory.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      Set <code>/host</code> as the root directory within the debug shell. The debug pod mounts the host’s root file system in <code>/host</code> within the pod. By changing the root directory to <code>/host</code>, you can run binaries contained in the host’s executable paths:
      <CodeBlock code={"# chroot /host"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>OpenShift Container Platform 4.22 cluster nodes running Red Hat Enterprise Linux CoreOS (RHCOS) are immutable and rely on Operators to apply cluster changes. Accessing cluster nodes by using SSH is not recommended. However, if the OpenShift Container Platform API is not available, or <code>kubelet</code> is not properly functioning on the target node, <code>oc</code> operations will be impacted. In such situations, it is possible to access nodes using <code>ssh core@&lt;node&gt;.&lt;cluster_name&gt;.&lt;base_domain&gt;</code> instead.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      Check whether the <code>kubelet</code> systemd service is active on the node:
      <CodeBlock code={"# systemctl is-active kubelet"} lang="terminal" />
      </li>
      <li>
      Output a more detailed <code>kubelet.service</code> status summary:
      <CodeBlock code={"# systemctl status kubelet"} lang="terminal" />
      </li>
      </ol>
      </li>
      </ol>
      <h2 id="querying-cluster-node-journal-logs_verifying-node-health" class="group scroll-mt-20">Querying cluster node journal logs<a href="#querying-cluster-node-journal-logs_verifying-node-health" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can gather <code>journald</code> unit logs and other logs within <code>/var/log</code> on individual cluster nodes.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      <li>Your API service is still functional.</li>
      <li>You have SSH access to your hosts.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Query <code>kubelet</code> <code>journald</code> unit logs from OpenShift Container Platform cluster nodes. The following example queries control plane nodes only:
      <CodeBlock code={"$ oc adm node-logs --role=master -u kubelet  (1)"} lang="terminal" />
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>kubelet</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Replace as appropriate to query other unit logs.</p></dd>
      </dl>
      </li>
      <li>
      Collect logs from specific subdirectories under <code>/var/log/</code> on cluster nodes.
      <ol>
      <li>
      Retrieve a list of logs contained within a <code>/var/log/</code> subdirectory. The following example lists files in <code>/var/log/openshift-apiserver/</code> on all control plane nodes:
      <CodeBlock code={"$ oc adm node-logs --role=master --path=openshift-apiserver"} lang="terminal" />
      </li>
      <li>
      Inspect a specific log within a <code>/var/log/</code> subdirectory. The following example outputs <code>/var/log/openshift-apiserver/audit.log</code> contents from all control plane nodes:
      <CodeBlock code={"$ oc adm node-logs --role=master --path=openshift-apiserver/audit.log"} lang="terminal" />
      </li>
      <li>
      If the API is not functional, review the logs on each node using SSH instead. The following example tails <code>/var/log/openshift-apiserver/audit.log</code>:
      <CodeBlock code={"$ ssh core@<master-node>.<cluster_name>.<base_domain> sudo tail -f /var/log/openshift-apiserver/audit.log"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>OpenShift Container Platform 4.22 cluster nodes running Red Hat Enterprise Linux CoreOS (RHCOS) are immutable and rely on Operators to apply cluster changes. Accessing cluster nodes by using SSH is not recommended. Before attempting to collect diagnostic data over SSH, review whether the data collected by running <code>oc adm must gather</code> and other <code>oc</code> commands is sufficient instead. However, if the OpenShift Container Platform API is not available, or the kubelet is not properly functioning on the target node, <code>oc</code> operations will be impacted. In such situations, it is possible to access nodes using <code>ssh core@&lt;node&gt;.&lt;cluster_name&gt;.&lt;base_domain&gt;</code>.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
