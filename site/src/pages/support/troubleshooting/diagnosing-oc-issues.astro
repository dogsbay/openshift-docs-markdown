---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: support/troubleshooting/diagnosing-oc-issues.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"diagnosing-oc-issues","text":"Diagnosing OpenShift CLI () issues"},{"depth":2,"slug":"understanding-oc-log-levels_diagnosing-oc-issues","text":"Understanding OpenShift CLI () log levels"},{"depth":2,"slug":"specifying-oc-log-levels_diagnosing-oc-issues","text":"Specifying OpenShift CLI () log levels"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/support/troubleshooting/diagnosing-oc-issues";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Diagnosing OpenShift CLI () issues";
const description = "You can investigate OpenShift CLI (oc) issues by increasing the log level to get more detailed diagnostic information." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["support","support/troubleshooting"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Diagnosing OpenShift CLI (`oc`) issues {#diagnosing-oc-issues}\n\nYou can investigate OpenShift CLI (`oc`) issues by increasing the log level to get more detailed diagnostic information.\n\n## Understanding OpenShift CLI (`oc`) log levels {#understanding-oc-log-levels_diagnosing-oc-issues}\n\nWith the OpenShift CLI (`oc`), you can create applications and manage OpenShift Container Platform projects from a terminal.\n\nIf `oc` command-specific issues arise, increase the `oc` log level to output API request, API response, and `curl` request details generated by the command. This provides a granular view of a particular `oc` command’s underlying operation, which in turn might provide insight into the nature of a failure.\n\n`oc` log levels range from 1 to 10. The following table provides a list of `oc` log levels, along with their descriptions.\n\n**OpenShift CLI (`oc`) log levels**\n\n| Log level | Description |\n| --- | --- |\n| 1 to 5 | No additional logging to stderr. |\n| 6 | Log API requests to stderr. |\n| 7 | Log API requests and headers to stderr. |\n| 8 | Log API requests, headers, and body, plus API response headers and body to stderr. |\n| 9 | Log API requests, headers, and body, API response headers and body, plus `curl` requests to stderr. |\n| 10 | Log API requests, headers, and body, API response headers and body, plus `curl` requests to stderr, in verbose detail. |\n\n## Specifying OpenShift CLI (`oc`) log levels {#specifying-oc-log-levels_diagnosing-oc-issues}\n\nYou can investigate OpenShift CLI (`oc`) issues by increasing the command’s log level.\n\nThe OpenShift Container Platform user’s current session token is typically included in logged `curl` requests where required. You can also obtain the current user’s session token manually, for use when testing aspects of an `oc` command’s underlying process step-by-step.\n\n**Prerequisites**\n\n- Install the OpenShift CLI (`oc`).\n\n**Procedure**\n\n- Specify the `oc` log level when running an `oc` command:\n\n  ```terminal\n  $ oc <command> --loglevel <log_level>\n  ```\n\n  where:\n\n  <command>\n  :   Specifies the command you are running.\n\n  <log_level>\n  :   Specifies the log level to apply to the command.\n- To obtain the current user’s session token, run the following command:\n\n  ```terminal\n  $ oc whoami -t\n  ```\n\n  ```text {title=\"Example output\"}\n  sha256~RCV3Qcn7H-OEfqCGVI0CvnZ6...\n  ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/support/troubleshooting/diagnosing-oc-issues.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="diagnosing-oc-issues" class="group scroll-mt-20">Diagnosing OpenShift CLI (<code>oc</code>) issues<a href="#diagnosing-oc-issues" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can investigate OpenShift CLI (<code>oc</code>) issues by increasing the log level to get more detailed diagnostic information.</p>
      <h2 id="understanding-oc-log-levels_diagnosing-oc-issues" class="group scroll-mt-20">Understanding OpenShift CLI (<code>oc</code>) log levels<a href="#understanding-oc-log-levels_diagnosing-oc-issues" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">With the OpenShift CLI (<code>oc</code>), you can create applications and manage OpenShift Container Platform projects from a terminal.</p>
      <p>If <code>oc</code> command-specific issues arise, increase the <code>oc</code> log level to output API request, API response, and <code>curl</code> request details generated by the command. This provides a granular view of a particular <code>oc</code> command’s underlying operation, which in turn might provide insight into the nature of a failure.</p>
      <p><code>oc</code> log levels range from 1 to 10. The following table provides a list of <code>oc</code> log levels, along with their descriptions.</p>
      <p><strong>OpenShift CLI (</strong><code>oc</code><strong>) log levels</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Log level</TableHead>
      <TableHead>Description</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>1 to 5</TableCell>
      <TableCell>No additional logging to stderr.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>6</TableCell>
      <TableCell>Log API requests to stderr.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>7</TableCell>
      <TableCell>Log API requests and headers to stderr.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>8</TableCell>
      <TableCell>Log API requests, headers, and body, plus API response headers and body to stderr.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>9</TableCell>
      <TableCell>Log API requests, headers, and body, API response headers and body, plus <code>curl</code> requests to stderr.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>10</TableCell>
      <TableCell>Log API requests, headers, and body, API response headers and body, plus <code>curl</code> requests to stderr, in verbose detail.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="specifying-oc-log-levels_diagnosing-oc-issues" class="group scroll-mt-20">Specifying OpenShift CLI (<code>oc</code>) log levels<a href="#specifying-oc-log-levels_diagnosing-oc-issues" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can investigate OpenShift CLI (<code>oc</code>) issues by increasing the command’s log level.</p>
      <p>The OpenShift Container Platform user’s current session token is typically included in logged <code>curl</code> requests where required. You can also obtain the current user’s session token manually, for use when testing aspects of an <code>oc</code> command’s underlying process step-by-step.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>Install the OpenShift CLI (<code>oc</code>).</li></ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Specify the <code>oc</code> log level when running an <code>oc</code> command:
      <CodeBlock code={"$ oc <command> --loglevel <log_level>"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">&lt;command&gt;</dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the command you are running.</p></dd>
      <dt class="font-semibold">&lt;log_level&gt;</dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the log level to apply to the command.</p></dd>
      </dl>
      </li>
      <li>
      To obtain the current user’s session token, run the following command:
      <CodeBlock code={"$ oc whoami -t"} lang="terminal" />
      <CodeBlock code={"sha256~RCV3Qcn7H-OEfqCGVI0CvnZ6..."} lang="text" title="Example output" />
      </li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
