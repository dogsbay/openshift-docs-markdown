---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: support/troubleshooting/investigating-pod-issues.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"investigating-pod-issues","text":"Investigating pod issues"},{"depth":2,"slug":"understanding-pod-error-states_investigating-pod-issues","text":"Understanding pod error states"},{"depth":2,"slug":"reviewing-pod-status_investigating-pod-issues","text":"Reviewing pod status"},{"depth":2,"slug":"inspecting-pod-and-container-logs_investigating-pod-issues","text":"Inspecting pod and container logs"},{"depth":2,"slug":"accessing-running-pods_investigating-pod-issues","text":"Accessing running pods"},{"depth":2,"slug":"starting-debug-pods-with-root-access_investigating-pod-issues","text":"Starting debug pods with root access"},{"depth":2,"slug":"copying-files-pods-and-containers_investigating-pod-issues","text":"Copying files to and from pods and containers"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/support/troubleshooting/investigating-pod-issues";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Investigating pod issues";
const description = "OpenShift Container Platform leverages the Kubernetes concept of a pod, which is one or more containers deployed together on one host. A pod is the…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["support","support/troubleshooting"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Investigating pod issues {#investigating-pod-issues}\n\nOpenShift Container Platform leverages the Kubernetes concept of a pod, which is one or more containers deployed together on one host. A pod is the smallest compute unit that can be defined, deployed, and managed on OpenShift Container Platform 4.22.\n\nAfter a pod is defined, it is assigned to run on a node until its containers exit, or until it is removed. Depending on policy and exit code, pods are either removed after exiting or retained so that their logs can be accessed.\n\nThe first thing to check when pod issues arise is the pod’s status. If an explicit pod failure has occurred, observe the pod’s error state to identify specific image, container, or pod network issues. Focus diagnostic data collection according to the error state. Review pod event messages, as well as pod and container log information. Diagnose issues dynamically by accessing running Pods on the command line, or start a debug pod with root access based on a problematic pod’s deployment configuration.\n\n## Understanding pod error states {#understanding-pod-error-states_investigating-pod-issues}\n\nPod failures return explicit error states that can be observed in the `status` field in the output of `oc get pods`. Pod error states cover image, container, and container network related failures.\n\nThe following table provides a list of pod error states along with their descriptions.\n\n**Pod error states**\n\n| Pod error state | Description |\n| --- | --- |\n| `ErrImagePull` | Generic image retrieval error. |\n| `ErrImagePullBackOff` | Image retrieval failed and is backed off. |\n| `ErrInvalidImageName` | The specified image name was invalid. |\n| `ErrImageInspect` | Image inspection did not succeed. |\n| `ErrImageNeverPull` | `PullPolicy` is set to `NeverPullImage` and the target image is not present locally on the host. |\n| `ErrRegistryUnavailable` | When attempting to retrieve an image from a registry, an HTTP error was encountered. |\n| `ErrContainerNotFound` | The specified container is either not present or not managed by the kubelet, within the declared pod. |\n| `ErrRunInitContainer` | Container initialization failed. |\n| `ErrRunContainer` | None of the pod’s containers started successfully. |\n| `ErrKillContainer` | None of the pod’s containers were killed successfully. |\n| `ErrCrashLoopBackOff` | A container has terminated. The kubelet will not attempt to restart it. |\n| `ErrVerifyNonRoot` | A container or image attempted to run with root privileges. |\n| `ErrCreatePodSandbox` | Pod sandbox creation did not succeed. |\n| `ErrConfigPodSandbox` | Pod sandbox configuration was not obtained. |\n| `ErrKillPodSandbox` | A pod sandbox did not stop successfully. |\n| `ErrSetupNetwork` | Network initialization failed. |\n| `ErrTeardownNetwork` | Network termination failed. |\n\n## Reviewing pod status {#reviewing-pod-status_investigating-pod-issues}\n\nYou can query pod status and error states. You can also query a pod’s associated deployment configuration and review base image availability.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- You have installed the OpenShift CLI (`oc`).\n- `skopeo` is installed.\n\n**Procedure**\n\n1. Switch into a project:\n\n   ```terminal\n   $ oc project <project_name>\n   ```\n2. List pods running within the namespace, as well as pod status, error states, restarts, and age:\n\n   ```terminal\n   $ oc get pods\n   ```\n3. Determine whether the namespace is managed by a deployment configuration:\n\n   ```terminal\n   $ oc status\n   ```\n\n   If the namespace is managed by a deployment configuration, the output includes the deployment configuration name and a base image reference.\n4. Inspect the base image referenced in the preceding command’s output:\n\n   ```terminal\n   $ skopeo inspect docker://<image_reference>\n   ```\n5. If the base image reference is not correct, update the reference in the deployment configuration:\n\n   ```terminal\n   $ oc edit deployment/my-deployment\n   ```\n6. When deployment configuration changes on exit, the configuration will automatically redeploy. Watch pod status as the deployment progresses, to determine whether the issue has been resolved:\n\n   ```terminal\n   $ oc get pods -w\n   ```\n7. Review events within the namespace for diagnostic information relating to pod failures:\n\n   ```terminal\n   $ oc get events\n   ```\n\n## Inspecting pod and container logs {#inspecting-pod-and-container-logs_investigating-pod-issues}\n\nYou can inspect pod and container logs for warnings and error messages related to explicit pod failures. Depending on policy and exit code, pod and container logs remain available after pods have been terminated.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- Your API service is still functional.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Query logs for a specific pod:\n\n   ```terminal\n   $ oc logs <pod_name>\n   ```\n2. Query logs for a specific container within a pod:\n\n   ```terminal\n   $ oc logs <pod_name> -c <container_name>\n   ```\n\n   Logs retrieved using the preceding `oc logs` commands are composed of messages sent to stdout within pods or containers.\n3. Inspect logs contained in `/var/log/` within a pod.\n\n   1. List log files and subdirectories contained in `/var/log` within a pod:\n\n      ```terminal\n      $ oc exec <pod_name>  -- ls -alh /var/log\n      ```\n\n      ```text {title=\"Example output\"}\n      total 124K\n      drwxr-xr-x. 1 root root   33 Aug 11 11:23 .\n      drwxr-xr-x. 1 root root   28 Sep  6  2022 ..\n      -rw-rw----. 1 root utmp    0 Jul 10 10:31 btmp\n      -rw-r--r--. 1 root root  33K Jul 17 10:07 dnf.librepo.log\n      -rw-r--r--. 1 root root  69K Jul 17 10:07 dnf.log\n      -rw-r--r--. 1 root root 8.8K Jul 17 10:07 dnf.rpm.log\n      -rw-r--r--. 1 root root  480 Jul 17 10:07 hawkey.log\n      -rw-rw-r--. 1 root utmp    0 Jul 10 10:31 lastlog\n      drwx------. 2 root root   23 Aug 11 11:14 openshift-apiserver\n      drwx------. 2 root root    6 Jul 10 10:31 private\n      drwxr-xr-x. 1 root root   22 Mar  9 08:05 rhsm\n      -rw-rw-r--. 1 root utmp    0 Jul 10 10:31 wtmp\n      ```\n   2. Query a specific log file contained in `/var/log` within a pod:\n\n      ```terminal\n      $ oc exec <pod_name> cat /var/log/<path_to_log>\n      ```\n\n      ```text {title=\"Example output\"}\n      2023-07-10T10:29:38+0000 INFO --- logging initialized ---\n      2023-07-10T10:29:38+0000 DDEBUG timer: config: 13 ms\n      2023-07-10T10:29:38+0000 DEBUG Loaded plugins: builddep, changelog, config-manager, copr, debug, debuginfo-install, download, generate_completion_cache, groups-manager, needs-restarting, playground, product-id, repoclosure, repodiff, repograph, repomanage, reposync, subscription-manager, uploadprofile\n      2023-07-10T10:29:38+0000 INFO Updating Subscription Management repositories.\n      2023-07-10T10:29:38+0000 INFO Unable to read consumer identity\n      2023-07-10T10:29:38+0000 INFO Subscription Manager is operating in container mode.\n      2023-07-10T10:29:38+0000 INFO\n      ```\n   3. List log files and subdirectories contained in `/var/log` within a specific container:\n\n      ```terminal\n      $ oc exec <pod_name> -c <container_name> ls /var/log\n      ```\n   4. Query a specific log file contained in `/var/log` within a specific container:\n\n      ```terminal\n      $ oc exec <pod_name> -c <container_name> cat /var/log/<path_to_log>\n      ```\n\n## Accessing running pods {#accessing-running-pods_investigating-pod-issues}\n\nYou can review running pods dynamically by opening a shell inside a pod or by gaining network access through port forwarding.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- Your API service is still functional.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Switch into the project that contains the pod you would like to access. This is necessary because the `oc rsh` command does not accept the `-n` namespace option:\n\n   ```terminal\n   $ oc project <namespace>\n   ```\n2. Start a remote shell into a pod:\n\n   ```terminal\n   $ oc rsh <pod_name>\n   ```\n\n   where:\n\n   `<pod_name>`\n   :   If a pod has multiple containers, `oc rsh` defaults to the first container unless `-c <container_name>` is specified.\n3. Start a remote shell into a specific container within a pod:\n\n   ```terminal\n   $ oc rsh -c <container_name> pod/<pod_name>\n   ```\n4. Create a port forwarding session to a port on a pod:\n\n   ```terminal\n   $ oc port-forward <pod_name> <host_port>:<pod_port>\n   ```\n\n   where:\n\n   `<pod_name> <host_port>:<pod_port>`\n   :   Enter `Ctrl+C` to cancel the port forwarding session.\n\n## Starting debug pods with root access {#starting-debug-pods-with-root-access_investigating-pod-issues}\n\nYou can start a debug pod with root access, based on a problematic pod’s deployment or deployment configuration. Pod users typically run with non-root privileges, but running troubleshooting pods with temporary root privileges can be useful during issue investigation.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- Your API service is still functional.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Start a debug pod with root access, based on a deployment.\n\n   1. Obtain a project’s deployment name:\n\n      ```terminal\n      $ oc get deployment -n <project_name>\n      ```\n   2. Start a debug pod with root privileges, based on the deployment:\n\n      ```terminal\n      $ oc debug deployment/my-deployment --as-root -n <project_name>\n      ```\n2. Start a debug pod with root access, based on a deployment configuration.\n\n   1. Obtain a project’s deployment configuration name:\n\n      ```terminal\n      $ oc get deploymentconfigs -n <project_name>\n      ```\n   2. Start a debug pod with root privileges, based on the deployment configuration:\n\n      ```terminal\n      $ oc debug deploymentconfig/my-deployment-configuration --as-root -n <project_name>\n      ```\n\n      > [!NOTE]\n      > You can append `-- <command>` to the preceding `oc debug` commands to run individual commands within a debug pod, instead of running an interactive shell.\n\n## Copying files to and from pods and containers {#copying-files-pods-and-containers_investigating-pod-issues}\n\nYou can copy files to and from a pod to test configuration changes or gather diagnostic information.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- Your API service is still functional.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Copy a file to a pod:\n\n   ```terminal\n   $ oc cp <local_path> <pod_name>:/<path> -c <container_name>\n   ```\n\n   `-c <container_name>` refers to the desired container in a pod. If you do not specify a container with the `-c` option, then the first container in a pod is selected.\n2. Copy a file from a pod:\n\n   ```terminal\n   $ oc cp <pod_name>:/<path> -c <container_name> <local_path>\n   ```\n\n   `-c <container_name>` refers to the desired container in a pod. If you do not specify a container with the `-c` option, then the first container in a pod is selected.\n\n   > [!NOTE]\n   > For `oc cp` to function, the `tar` binary must be available within the container.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/support/troubleshooting/investigating-pod-issues.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="investigating-pod-issues" class="group scroll-mt-20">Investigating pod issues<a href="#investigating-pod-issues" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>OpenShift Container Platform leverages the Kubernetes concept of a pod, which is one or more containers deployed together on one host. A pod is the smallest compute unit that can be defined, deployed, and managed on OpenShift Container Platform 4.22.</p>
      <p>After a pod is defined, it is assigned to run on a node until its containers exit, or until it is removed. Depending on policy and exit code, pods are either removed after exiting or retained so that their logs can be accessed.</p>
      <p>The first thing to check when pod issues arise is the pod’s status. If an explicit pod failure has occurred, observe the pod’s error state to identify specific image, container, or pod network issues. Focus diagnostic data collection according to the error state. Review pod event messages, as well as pod and container log information. Diagnose issues dynamically by accessing running Pods on the command line, or start a debug pod with root access based on a problematic pod’s deployment configuration.</p>
      <h2 id="understanding-pod-error-states_investigating-pod-issues" class="group scroll-mt-20">Understanding pod error states<a href="#understanding-pod-error-states_investigating-pod-issues" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Pod failures return explicit error states that can be observed in the <code>status</code> field in the output of <code>oc get pods</code>. Pod error states cover image, container, and container network related failures.</p>
      <p>The following table provides a list of pod error states along with their descriptions.</p>
      <p><strong>Pod error states</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Pod error state</TableHead>
      <TableHead>Description</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>ErrImagePull</code></TableCell>
      <TableCell>Generic image retrieval error.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ErrImagePullBackOff</code></TableCell>
      <TableCell>Image retrieval failed and is backed off.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ErrInvalidImageName</code></TableCell>
      <TableCell>The specified image name was invalid.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ErrImageInspect</code></TableCell>
      <TableCell>Image inspection did not succeed.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ErrImageNeverPull</code></TableCell>
      <TableCell><code>PullPolicy</code> is set to <code>NeverPullImage</code> and the target image is not present locally on the host.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ErrRegistryUnavailable</code></TableCell>
      <TableCell>When attempting to retrieve an image from a registry, an HTTP error was encountered.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ErrContainerNotFound</code></TableCell>
      <TableCell>The specified container is either not present or not managed by the kubelet, within the declared pod.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ErrRunInitContainer</code></TableCell>
      <TableCell>Container initialization failed.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ErrRunContainer</code></TableCell>
      <TableCell>None of the pod’s containers started successfully.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ErrKillContainer</code></TableCell>
      <TableCell>None of the pod’s containers were killed successfully.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ErrCrashLoopBackOff</code></TableCell>
      <TableCell>A container has terminated. The kubelet will not attempt to restart it.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ErrVerifyNonRoot</code></TableCell>
      <TableCell>A container or image attempted to run with root privileges.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ErrCreatePodSandbox</code></TableCell>
      <TableCell>Pod sandbox creation did not succeed.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ErrConfigPodSandbox</code></TableCell>
      <TableCell>Pod sandbox configuration was not obtained.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ErrKillPodSandbox</code></TableCell>
      <TableCell>A pod sandbox did not stop successfully.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ErrSetupNetwork</code></TableCell>
      <TableCell>Network initialization failed.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ErrTeardownNetwork</code></TableCell>
      <TableCell>Network termination failed.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="reviewing-pod-status_investigating-pod-issues" class="group scroll-mt-20">Reviewing pod status<a href="#reviewing-pod-status_investigating-pod-issues" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can query pod status and error states. You can also query a pod’s associated deployment configuration and review base image availability.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      <li><code>skopeo</code> is installed.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Switch into a project:
      <CodeBlock code={"$ oc project <project_name>"} lang="terminal" />
      </li>
      <li>
      List pods running within the namespace, as well as pod status, error states, restarts, and age:
      <CodeBlock code={"$ oc get pods"} lang="terminal" />
      </li>
      <li>
      Determine whether the namespace is managed by a deployment configuration:
      <CodeBlock code={"$ oc status"} lang="terminal" />
      <p>If the namespace is managed by a deployment configuration, the output includes the deployment configuration name and a base image reference.</p>
      </li>
      <li>
      Inspect the base image referenced in the preceding command’s output:
      <CodeBlock code={"$ skopeo inspect docker://<image_reference>"} lang="terminal" />
      </li>
      <li>
      If the base image reference is not correct, update the reference in the deployment configuration:
      <CodeBlock code={"$ oc edit deployment/my-deployment"} lang="terminal" />
      </li>
      <li>
      When deployment configuration changes on exit, the configuration will automatically redeploy. Watch pod status as the deployment progresses, to determine whether the issue has been resolved:
      <CodeBlock code={"$ oc get pods -w"} lang="terminal" />
      </li>
      <li>
      Review events within the namespace for diagnostic information relating to pod failures:
      <CodeBlock code={"$ oc get events"} lang="terminal" />
      </li>
      </ol>
      <h2 id="inspecting-pod-and-container-logs_investigating-pod-issues" class="group scroll-mt-20">Inspecting pod and container logs<a href="#inspecting-pod-and-container-logs_investigating-pod-issues" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can inspect pod and container logs for warnings and error messages related to explicit pod failures. Depending on policy and exit code, pod and container logs remain available after pods have been terminated.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>Your API service is still functional.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Query logs for a specific pod:
      <CodeBlock code={"$ oc logs <pod_name>"} lang="terminal" />
      </li>
      <li>
      Query logs for a specific container within a pod:
      <CodeBlock code={"$ oc logs <pod_name> -c <container_name>"} lang="terminal" />
      <p>Logs retrieved using the preceding <code>oc logs</code> commands are composed of messages sent to stdout within pods or containers.</p>
      </li>
      <li>
      Inspect logs contained in <code>/var/log/</code> within a pod.
      <ol>
      <li>
      List log files and subdirectories contained in <code>/var/log</code> within a pod:
      <CodeBlock code={"$ oc exec <pod_name>  -- ls -alh /var/log"} lang="terminal" />
      <CodeBlock code={"total 124K\ndrwxr-xr-x. 1 root root   33 Aug 11 11:23 .\ndrwxr-xr-x. 1 root root   28 Sep  6  2022 ..\n-rw-rw----. 1 root utmp    0 Jul 10 10:31 btmp\n-rw-r--r--. 1 root root  33K Jul 17 10:07 dnf.librepo.log\n-rw-r--r--. 1 root root  69K Jul 17 10:07 dnf.log\n-rw-r--r--. 1 root root 8.8K Jul 17 10:07 dnf.rpm.log\n-rw-r--r--. 1 root root  480 Jul 17 10:07 hawkey.log\n-rw-rw-r--. 1 root utmp    0 Jul 10 10:31 lastlog\ndrwx------. 2 root root   23 Aug 11 11:14 openshift-apiserver\ndrwx------. 2 root root    6 Jul 10 10:31 private\ndrwxr-xr-x. 1 root root   22 Mar  9 08:05 rhsm\n-rw-rw-r--. 1 root utmp    0 Jul 10 10:31 wtmp"} lang="text" title="Example output" />
      </li>
      <li>
      Query a specific log file contained in <code>/var/log</code> within a pod:
      <CodeBlock code={"$ oc exec <pod_name> cat /var/log/<path_to_log>"} lang="terminal" />
      <CodeBlock code={"2023-07-10T10:29:38+0000 INFO --- logging initialized ---\n2023-07-10T10:29:38+0000 DDEBUG timer: config: 13 ms\n2023-07-10T10:29:38+0000 DEBUG Loaded plugins: builddep, changelog, config-manager, copr, debug, debuginfo-install, download, generate_completion_cache, groups-manager, needs-restarting, playground, product-id, repoclosure, repodiff, repograph, repomanage, reposync, subscription-manager, uploadprofile\n2023-07-10T10:29:38+0000 INFO Updating Subscription Management repositories.\n2023-07-10T10:29:38+0000 INFO Unable to read consumer identity\n2023-07-10T10:29:38+0000 INFO Subscription Manager is operating in container mode.\n2023-07-10T10:29:38+0000 INFO"} lang="text" title="Example output" />
      </li>
      <li>
      List log files and subdirectories contained in <code>/var/log</code> within a specific container:
      <CodeBlock code={"$ oc exec <pod_name> -c <container_name> ls /var/log"} lang="terminal" />
      </li>
      <li>
      Query a specific log file contained in <code>/var/log</code> within a specific container:
      <CodeBlock code={"$ oc exec <pod_name> -c <container_name> cat /var/log/<path_to_log>"} lang="terminal" />
      </li>
      </ol>
      </li>
      </ol>
      <h2 id="accessing-running-pods_investigating-pod-issues" class="group scroll-mt-20">Accessing running pods<a href="#accessing-running-pods_investigating-pod-issues" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can review running pods dynamically by opening a shell inside a pod or by gaining network access through port forwarding.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>Your API service is still functional.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Switch into the project that contains the pod you would like to access. This is necessary because the <code>oc rsh</code> command does not accept the <code>-n</code> namespace option:
      <CodeBlock code={"$ oc project <namespace>"} lang="terminal" />
      </li>
      <li>
      Start a remote shell into a pod:
      <CodeBlock code={"$ oc rsh <pod_name>"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;pod_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>If a pod has multiple containers, <code>oc rsh</code> defaults to the first container unless <code>-c &lt;container_name&gt;</code> is specified.</p></dd>
      </dl>
      </li>
      <li>
      Start a remote shell into a specific container within a pod:
      <CodeBlock code={"$ oc rsh -c <container_name> pod/<pod_name>"} lang="terminal" />
      </li>
      <li>
      Create a port forwarding session to a port on a pod:
      <CodeBlock code={"$ oc port-forward <pod_name> <host_port>:<pod_port>"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;pod_name&gt; &lt;host_port&gt;:&lt;pod_port&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Enter <code>Ctrl+C</code> to cancel the port forwarding session.</p></dd>
      </dl>
      </li>
      </ol>
      <h2 id="starting-debug-pods-with-root-access_investigating-pod-issues" class="group scroll-mt-20">Starting debug pods with root access<a href="#starting-debug-pods-with-root-access_investigating-pod-issues" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can start a debug pod with root access, based on a problematic pod’s deployment or deployment configuration. Pod users typically run with non-root privileges, but running troubleshooting pods with temporary root privileges can be useful during issue investigation.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>Your API service is still functional.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Start a debug pod with root access, based on a deployment.
      <ol>
      <li>
      Obtain a project’s deployment name:
      <CodeBlock code={"$ oc get deployment -n <project_name>"} lang="terminal" />
      </li>
      <li>
      Start a debug pod with root privileges, based on the deployment:
      <CodeBlock code={"$ oc debug deployment/my-deployment --as-root -n <project_name>"} lang="terminal" />
      </li>
      </ol>
      </li>
      <li>
      Start a debug pod with root access, based on a deployment configuration.
      <ol>
      <li>
      Obtain a project’s deployment configuration name:
      <CodeBlock code={"$ oc get deploymentconfigs -n <project_name>"} lang="terminal" />
      </li>
      <li>
      Start a debug pod with root privileges, based on the deployment configuration:
      <CodeBlock code={"$ oc debug deploymentconfig/my-deployment-configuration --as-root -n <project_name>"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>You can append <code>-- &lt;command&gt;</code> to the preceding <code>oc debug</code> commands to run individual commands within a debug pod, instead of running an interactive shell.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      </li>
      </ol>
      <h2 id="copying-files-pods-and-containers_investigating-pod-issues" class="group scroll-mt-20">Copying files to and from pods and containers<a href="#copying-files-pods-and-containers_investigating-pod-issues" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can copy files to and from a pod to test configuration changes or gather diagnostic information.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>Your API service is still functional.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Copy a file to a pod:
      <CodeBlock code={"$ oc cp <local_path> <pod_name>:/<path> -c <container_name>"} lang="terminal" />
      <p><code>-c &lt;container_name&gt;</code> refers to the desired container in a pod. If you do not specify a container with the <code>-c</code> option, then the first container in a pod is selected.</p>
      </li>
      <li>
      Copy a file from a pod:
      <CodeBlock code={"$ oc cp <pod_name>:/<path> -c <container_name> <local_path>"} lang="terminal" />
      <p><code>-c &lt;container_name&gt;</code> refers to the desired container in a pod. If you do not specify a container with the <code>-c</code> option, then the first container in a pod is selected.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>For <code>oc cp</code> to function, the <code>tar</code> binary must be available within the container.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
