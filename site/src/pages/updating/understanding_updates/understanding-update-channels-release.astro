---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: updating/understanding_updates/understanding-update-channels-release.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"understanding-update-channels-releases","text":"Understanding update channels and releases"},{"depth":2,"slug":"understanding-update-channels-overview_understanding-update-channels-releases","text":"Overview of update channels"},{"depth":2,"slug":"understanding-update-channels_understanding-update-channels-releases","text":"Update channels"},{"depth":3,"slug":"fast-version-channel_understanding-update-channels-releases","text":"fast-4.22 channel"},{"depth":3,"slug":"stable-version-channel_understanding-update-channels-releases","text":"stable-4.22 channel"},{"depth":3,"slug":"eus-4y-channel_understanding-update-channels-releases","text":"eus-4.y channel"},{"depth":3,"slug":"candidate-version-channel_understanding-update-channels-releases","text":"candidate-4.22 channel"},{"depth":2,"slug":"restricted-network-clusters_understanding-update-channels-releases","text":"Restricted network clusters"},{"depth":2,"slug":"upgrade-version-paths_understanding-update-channels-releases","text":"Update recommendations in the channel"},{"depth":2,"slug":"conditional-updates-overview_understanding-update-channels-releases","text":"Update recommendations and Conditional Updates"},{"depth":2,"slug":"fast-stable-channel-strategies_understanding-update-channels-releases","text":"What to consider when choosing an update channel"},{"depth":2,"slug":"switching-between-channels_understanding-update-channels-releases","text":"Considerations for switching between channels"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/updating/understanding_updates/understanding-update-channels-release";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Understanding update channels and releases";
const description = "Update channels are the mechanism by which users declare the OpenShift Container Platform minor version they intend to update their clusters to. They also…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["updating","updating/understanding_updates"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Understanding update channels and releases {#understanding-update-channels-releases}\n\nUpdate channels are the mechanism by which users declare the OpenShift Container Platform minor version they intend to update their clusters to. They also allow users to choose the timing and level of support their updates will have through the `fast`, `stable`, `candidate`, and `eus` channel options.\n\nThe Cluster Version Operator uses an update graph based on the channel declaration, along with other conditional information, to provide a list of recommended and conditional updates available to the cluster.\n\n## Overview of update channels {#understanding-update-channels-overview_understanding-update-channels-releases}\n\nUpdate channels correspond to a minor version of OpenShift Container Platform. The version number in the channel represents the target minor version that the cluster will eventually be updated to, even if it is higher than the cluster’s current minor version.\n\nFor instance, OpenShift Container Platform 4.10 update channels provide the following recommendations:\n\n- Updates within 4.10.\n- Updates within 4.9.\n- Updates from 4.9 to 4.10, allowing all 4.9 clusters to eventually update to 4.10, even if they do not immediately meet the minimum z-stream version requirements.\n- `eus-4.10` only: updates within 4.8.\n- `eus-4.10` only: updates from 4.8 to 4.9 to 4.10, allowing all 4.8 clusters to eventually update to 4.10.\n\n4.10 update channels do not recommend updates to 4.11 or later releases. This strategy ensures that administrators must explicitly decide to update to the next minor version of OpenShift Container Platform.\n\nUpdate channels control only release selection and do not impact the version of the cluster that you install. The `openshift-install` binary file for a specific version of OpenShift Container Platform always installs that version.\n\nOpenShift Container Platform 4.22 offers the following update channels:\n\n- `stable-{{ product_version }}`\n- `eus-4.y` (only offered for EUS versions and meant to facilitate updates between EUS versions)\n- `fast-{{ product_version }}`\n- `candidate-{{ product_version }}`\n\nIf you do not want the Cluster Version Operator to fetch available updates from the update recommendation service, you can use the `oc adm upgrade channel` command in the OpenShift CLI to configure an empty channel. This configuration can be helpful if, for example, a cluster has restricted network access and there is no local, reachable update recommendation service.\n\n> [!WARNING]\n> Red Hat recommends updating only to versions suggested by OpenShift Update Service. For a minor version update, versions must be contiguous. Red Hat does not test updates to noncontiguous versions and cannot guarantee compatibility with earlier versions.\n\n## Update channels {#understanding-update-channels_understanding-update-channels-releases}\n\nOpenShift Container Platform offers several update channels for you to choose from, depending on your desired update strategy.\n\n### fast-4.22 channel {#fast-version-channel_understanding-update-channels-releases}\n\nThe `fast-{{ product_version }}` channel is updated with new versions of OpenShift Container Platform 4.22 as soon as Red Hat declares the version as a general availability (GA) release. As such, these releases are fully supported and purposed to be used in production environments.\n\n### stable-4.22 channel {#stable-version-channel_understanding-update-channels-releases}\n\nWhile the `fast-{{ product_version }}` channel contains releases as soon as their errata are published, releases are added to the `stable-{{ product_version }}` channel after a delay. During this delay, data is collected from multiple sources and analyzed for indications of product regressions. Once a significant number of data points have been collected, these releases are added to the stable channel.\n\n> [!NOTE]\n> Since the time required to obtain a significant number of data points varies based on many factors, Service LeveL Objective (SLO) is not offered for the delay duration between fast and stable channels. For more information, please see \"Choosing the correct channel for your cluster\"\n\nNewly installed clusters default to using stable channels.\n\n### eus-4.y channel {#eus-4y-channel_understanding-update-channels-releases}\n\nIn addition to the stable channel, all even-numbered minor versions of OpenShift Container Platform offer [Extended Update Support](https://access.redhat.com/support/policy/updates/openshift#ocp4_phases) (EUS). Releases promoted to the stable channel are also simultaneously promoted to the EUS channels. The primary purpose of the EUS channels is to serve as a convenience for clusters performing a Control Plane Only update.\n\n> [!NOTE]\n> Both standard and non-EUS subscribers can access all EUS repositories and necessary RPMs (`rhel-*-eus-rpms`) to be able to support critical purposes such as debugging and building drivers.\n\n### candidate-4.22 channel {#candidate-version-channel_understanding-update-channels-releases}\n\nThe `candidate-{{ product_version }}` channel offers unsupported early access to releases as soon as they are built. Releases present only in candidate channels may not contain the full feature set of eventual GA releases or features may be removed prior to GA. Additionally, these releases have not been subject to full Red Hat Quality Assurance and may not offer update paths to later GA releases. Given these caveats, the candidate channel is only suitable for testing purposes where destroying and recreating a cluster is acceptable.\n\n## Restricted network clusters {#restricted-network-clusters_understanding-update-channels-releases}\n\nIf you manage the container images for your OpenShift Container Platform clusters yourself, you must consult the Red Hat errata that is associated with product releases and note any comments that impact updates.\n\nDuring an update, the user interface might warn you about switching between these versions, so you must ensure that you selected an appropriate version before you bypass those warnings.\n\n## Update recommendations in the channel {#upgrade-version-paths_understanding-update-channels-releases}\n\nOpenShift Container Platform maintains an update recommendation service that knows your installed OpenShift Container Platform version and the path to take within the channel to get you to the next release.\n\nUpdate paths are also limited to versions relevant to your currently selected channel and its promotion characteristics.\n\nYou can imagine seeing the following releases in your channel:\n\n- 4.22.0\n- 4.22.1\n- 4.22.3\n- 4.22.4\n\nThe service recommends only updates that have been tested and have no known serious regressions. For example, if your cluster is on 4.22.1 and OpenShift Container Platform suggests 4.22.4, then it is recommended to update from 4.22.1 to 4.22.4.\n\n> [!IMPORTANT]\n> Do not rely on consecutive patch numbers. In this example, 4.22.2 is not and never was available in the channel, therefore updates to 4.22.2 are not recommended or supported.\n\n## Update recommendations and Conditional Updates {#conditional-updates-overview_understanding-update-channels-releases}\n\nRed Hat monitors newly released versions and update paths associated with those versions before and after they are added to supported channels.\n\nIf Red Hat removes update recommendations from any supported release, a superseding update recommendation will be provided to a future version that corrects the regression. There may however be a delay while the defect is corrected, tested, and promoted to your selected channel.\n\nBeginning in OpenShift Container Platform 4.10, when update risks are confirmed, they are declared as Conditional Update risks for the relevant updates. Each known risk may apply to all clusters or only clusters matching certain conditions. Some examples include having the `Platform` set to `None` or the CNI provider set to `OpenShiftSDN`. The Cluster Version Operator (CVO) continually evaluates known risks against the current cluster state. If no risks match, the update is recommended. If the risk matches, those update paths are labeled as *updates with known issues*, and a reference link to the known issues is provided. The reference link helps the cluster admin decide if they want to accept the risk and continue to update their cluster.\n\nWhen Red Hat chooses to declare Conditional Update risks, that action is taken in all relevant channels simultaneously. Declaration of a Conditional Update risk may happen either before or after the update has been promoted to supported channels.\n\n## What to consider when choosing an update channel {#fast-stable-channel-strategies_understanding-update-channels-releases}\n\nChoosing the appropriate update channel for your cluster involves two decisions.\n\nFirst, select the minor version you want for your cluster update. Selecting a channel which matches your current version ensures that you only apply z-stream updates and do not receive feature updates. Selecting an available channel which has a version greater than your current version will ensure that after one or more updates your cluster will have updated to that version. Your cluster will only be offered channels which match its current version, the next version, or the next EUS version.\n\n> [!NOTE]\n> Due to the complexity involved in planning updates between versions many minors apart, channels that assist in planning updates beyond a single Control Plane Only update are not offered.\n\nSecond, you should choose your desired rollout strategy. You may choose to update as soon as Red Hat declares a release GA by selecting from fast channels or you may want to wait for Red Hat to promote releases to the stable channel. Update recommendations offered in the `fast-{{ product_version }}` and `stable-{{ product_version }}` are both fully supported and benefit equally from ongoing data analysis. The promotion delay before promoting a release to the stable channel represents the only difference between the two channels. Updates to the latest z-streams are generally promoted to the stable channel within a week or two, however the delay when initially rolling out updates to the latest minor is much longer, generally 45-90 days. Please consider the promotion delay when choosing your desired channel, as waiting for promotion to the stable channel may affect your scheduling plans.\n\nAdditionally, there are several factors which may lead an organization to move clusters to the fast channel either permanently or temporarily including the following:\n\n- The desire to apply a specific fix known to affect your environment without delay.\n- Application of CVE fixes without delay. CVE fixes may introduce regressions, so promotion delays still apply to z-streams with CVE fixes.\n- Internal testing processes. If it takes your organization several weeks to qualify releases it is best test concurrently with our promotion process rather than waiting. This also assures that any telemetry signal provided to Red Hat is a factored into our rollout, so issues relevant to you can be fixed faster.\n\n## Considerations for switching between channels {#switching-between-channels_understanding-update-channels-releases}\n\nYou can switch your cluster’s update channel through the web console or the CLI, in order to access different update recommendations for your cluster.\n\nYou can switch the channel from the CLI by running the following command:\n\n```terminal\n$ oc adm upgrade channel <channel>\n```\n\nThe web console will display an alert if you switch to a channel that does not include the current release. The web console does not recommend any updates while on a channel without the current release. You can return to the original channel at any point, however.\n\nChanging your channel might impact the supportability of your cluster. The following conditions might apply:\n\n- Your cluster is still supported if you change from the `stable-{{ product_version }}` channel to the `fast-{{ product_version }}` channel.\n- You can switch to the `candidate-{{ product_version }}` channel at any time, but some releases for this channel might be unsupported.\n- You can switch from the `candidate-{{ product_version }}` channel to the `fast-{{ product_version }}` channel if your current release is a general availability release.\n- You can always switch from the `fast-{{ product_version }}` channel to the `stable-{{ product_version }}` channel. There is a possible delay of up to a day for the release to be promoted to `stable-{{ product_version }}` if the current release was recently promoted.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/updating/understanding_updates/understanding-update-channels-release.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="understanding-update-channels-releases" class="group scroll-mt-20">Understanding update channels and releases<a href="#understanding-update-channels-releases" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Update channels are the mechanism by which users declare the OpenShift Container Platform minor version they intend to update their clusters to. They also allow users to choose the timing and level of support their updates will have through the <code>fast</code>, <code>stable</code>, <code>candidate</code>, and <code>eus</code> channel options.</p>
      <p>The Cluster Version Operator uses an update graph based on the channel declaration, along with other conditional information, to provide a list of recommended and conditional updates available to the cluster.</p>
      <h2 id="understanding-update-channels-overview_understanding-update-channels-releases" class="group scroll-mt-20">Overview of update channels<a href="#understanding-update-channels-overview_understanding-update-channels-releases" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Update channels correspond to a minor version of OpenShift Container Platform. The version number in the channel represents the target minor version that the cluster will eventually be updated to, even if it is higher than the cluster’s current minor version.</p>
      <p>For instance, OpenShift Container Platform 4.10 update channels provide the following recommendations:</p>
      <ul>
      <li>Updates within 4.10.</li>
      <li>Updates within 4.9.</li>
      <li>Updates from 4.9 to 4.10, allowing all 4.9 clusters to eventually update to 4.10, even if they do not immediately meet the minimum z-stream version requirements.</li>
      <li><code>eus-4.10</code> only: updates within 4.8.</li>
      <li><code>eus-4.10</code> only: updates from 4.8 to 4.9 to 4.10, allowing all 4.8 clusters to eventually update to 4.10.</li>
      </ul>
      <p>4.10 update channels do not recommend updates to 4.11 or later releases. This strategy ensures that administrators must explicitly decide to update to the next minor version of OpenShift Container Platform.</p>
      <p>Update channels control only release selection and do not impact the version of the cluster that you install. The <code>openshift-install</code> binary file for a specific version of OpenShift Container Platform always installs that version.</p>
      <p>OpenShift Container Platform 4.22 offers the following update channels:</p>
      <ul>
      <li><code>stable-{"{"}{"{"} product_version {"}"}{"}"}</code></li>
      <li><code>eus-4.y</code> (only offered for EUS versions and meant to facilitate updates between EUS versions)</li>
      <li><code>fast-{"{"}{"{"} product_version {"}"}{"}"}</code></li>
      <li><code>candidate-{"{"}{"{"} product_version {"}"}{"}"}</code></li>
      </ul>
      <p>If you do not want the Cluster Version Operator to fetch available updates from the update recommendation service, you can use the <code>oc adm upgrade channel</code> command in the OpenShift CLI to configure an empty channel. This configuration can be helpful if, for example, a cluster has restricted network access and there is no local, reachable update recommendation service.</p>
      <Alert variant="warning" class="my-4">
        <AlertTitle>Warning</AlertTitle>
        <AlertDescription>
          <p>Red Hat recommends updating only to versions suggested by OpenShift Update Service. For a minor version update, versions must be contiguous. Red Hat does not test updates to noncontiguous versions and cannot guarantee compatibility with earlier versions.</p>
        </AlertDescription>
      </Alert>
      <h2 id="understanding-update-channels_understanding-update-channels-releases" class="group scroll-mt-20">Update channels<a href="#understanding-update-channels_understanding-update-channels-releases" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">OpenShift Container Platform offers several update channels for you to choose from, depending on your desired update strategy.</p>
      <h3 id="fast-version-channel_understanding-update-channels-releases" class="group scroll-mt-20">fast-4.22 channel<a href="#fast-version-channel_understanding-update-channels-releases" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The <code>fast-{"{"}{"{"} product_version {"}"}{"}"}</code> channel is updated with new versions of OpenShift Container Platform 4.22 as soon as Red Hat declares the version as a general availability (GA) release. As such, these releases are fully supported and purposed to be used in production environments.</p>
      <h3 id="stable-version-channel_understanding-update-channels-releases" class="group scroll-mt-20">stable-4.22 channel<a href="#stable-version-channel_understanding-update-channels-releases" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>While the <code>fast-{"{"}{"{"} product_version {"}"}{"}"}</code> channel contains releases as soon as their errata are published, releases are added to the <code>stable-{"{"}{"{"} product_version {"}"}{"}"}</code> channel after a delay. During this delay, data is collected from multiple sources and analyzed for indications of product regressions. Once a significant number of data points have been collected, these releases are added to the stable channel.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Since the time required to obtain a significant number of data points varies based on many factors, Service LeveL Objective (SLO) is not offered for the delay duration between fast and stable channels. For more information, please see "Choosing the correct channel for your cluster"</p>
        </AlertDescription>
      </Alert>
      <p>Newly installed clusters default to using stable channels.</p>
      <h3 id="eus-4y-channel_understanding-update-channels-releases" class="group scroll-mt-20">eus-4.y channel<a href="#eus-4y-channel_understanding-update-channels-releases" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>In addition to the stable channel, all even-numbered minor versions of OpenShift Container Platform offer <a href="https://access.redhat.com/support/policy/updates/openshift#ocp4_phases">Extended Update Support</a> (EUS). Releases promoted to the stable channel are also simultaneously promoted to the EUS channels. The primary purpose of the EUS channels is to serve as a convenience for clusters performing a Control Plane Only update.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Both standard and non-EUS subscribers can access all EUS repositories and necessary RPMs (<code>rhel-*-eus-rpms</code>) to be able to support critical purposes such as debugging and building drivers.</p>
        </AlertDescription>
      </Alert>
      <h3 id="candidate-version-channel_understanding-update-channels-releases" class="group scroll-mt-20">candidate-4.22 channel<a href="#candidate-version-channel_understanding-update-channels-releases" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The <code>candidate-{"{"}{"{"} product_version {"}"}{"}"}</code> channel offers unsupported early access to releases as soon as they are built. Releases present only in candidate channels may not contain the full feature set of eventual GA releases or features may be removed prior to GA. Additionally, these releases have not been subject to full Red Hat Quality Assurance and may not offer update paths to later GA releases. Given these caveats, the candidate channel is only suitable for testing purposes where destroying and recreating a cluster is acceptable.</p>
      <h2 id="restricted-network-clusters_understanding-update-channels-releases" class="group scroll-mt-20">Restricted network clusters<a href="#restricted-network-clusters_understanding-update-channels-releases" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">If you manage the container images for your OpenShift Container Platform clusters yourself, you must consult the Red Hat errata that is associated with product releases and note any comments that impact updates.</p>
      <p>During an update, the user interface might warn you about switching between these versions, so you must ensure that you selected an appropriate version before you bypass those warnings.</p>
      <h2 id="upgrade-version-paths_understanding-update-channels-releases" class="group scroll-mt-20">Update recommendations in the channel<a href="#upgrade-version-paths_understanding-update-channels-releases" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">OpenShift Container Platform maintains an update recommendation service that knows your installed OpenShift Container Platform version and the path to take within the channel to get you to the next release.</p>
      <p>Update paths are also limited to versions relevant to your currently selected channel and its promotion characteristics.</p>
      <p>You can imagine seeing the following releases in your channel:</p>
      <ul>
      <li>4.22.0</li>
      <li>4.22.1</li>
      <li>4.22.3</li>
      <li>4.22.4</li>
      </ul>
      <p>The service recommends only updates that have been tested and have no known serious regressions. For example, if your cluster is on 4.22.1 and OpenShift Container Platform suggests 4.22.4, then it is recommended to update from 4.22.1 to 4.22.4.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Do not rely on consecutive patch numbers. In this example, 4.22.2 is not and never was available in the channel, therefore updates to 4.22.2 are not recommended or supported.</p>
        </AlertDescription>
      </Alert>
      <h2 id="conditional-updates-overview_understanding-update-channels-releases" class="group scroll-mt-20">Update recommendations and Conditional Updates<a href="#conditional-updates-overview_understanding-update-channels-releases" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Red Hat monitors newly released versions and update paths associated with those versions before and after they are added to supported channels.</p>
      <p>If Red Hat removes update recommendations from any supported release, a superseding update recommendation will be provided to a future version that corrects the regression. There may however be a delay while the defect is corrected, tested, and promoted to your selected channel.</p>
      <p>Beginning in OpenShift Container Platform 4.10, when update risks are confirmed, they are declared as Conditional Update risks for the relevant updates. Each known risk may apply to all clusters or only clusters matching certain conditions. Some examples include having the <code>Platform</code> set to <code>None</code> or the CNI provider set to <code>OpenShiftSDN</code>. The Cluster Version Operator (CVO) continually evaluates known risks against the current cluster state. If no risks match, the update is recommended. If the risk matches, those update paths are labeled as <em>updates with known issues</em>, and a reference link to the known issues is provided. The reference link helps the cluster admin decide if they want to accept the risk and continue to update their cluster.</p>
      <p>When Red Hat chooses to declare Conditional Update risks, that action is taken in all relevant channels simultaneously. Declaration of a Conditional Update risk may happen either before or after the update has been promoted to supported channels.</p>
      <h2 id="fast-stable-channel-strategies_understanding-update-channels-releases" class="group scroll-mt-20">What to consider when choosing an update channel<a href="#fast-stable-channel-strategies_understanding-update-channels-releases" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Choosing the appropriate update channel for your cluster involves two decisions.</p>
      <p>First, select the minor version you want for your cluster update. Selecting a channel which matches your current version ensures that you only apply z-stream updates and do not receive feature updates. Selecting an available channel which has a version greater than your current version will ensure that after one or more updates your cluster will have updated to that version. Your cluster will only be offered channels which match its current version, the next version, or the next EUS version.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Due to the complexity involved in planning updates between versions many minors apart, channels that assist in planning updates beyond a single Control Plane Only update are not offered.</p>
        </AlertDescription>
      </Alert>
      <p>Second, you should choose your desired rollout strategy. You may choose to update as soon as Red Hat declares a release GA by selecting from fast channels or you may want to wait for Red Hat to promote releases to the stable channel. Update recommendations offered in the <code>fast-{"{"}{"{"} product_version {"}"}{"}"}</code> and <code>stable-{"{"}{"{"} product_version {"}"}{"}"}</code> are both fully supported and benefit equally from ongoing data analysis. The promotion delay before promoting a release to the stable channel represents the only difference between the two channels. Updates to the latest z-streams are generally promoted to the stable channel within a week or two, however the delay when initially rolling out updates to the latest minor is much longer, generally 45-90 days. Please consider the promotion delay when choosing your desired channel, as waiting for promotion to the stable channel may affect your scheduling plans.</p>
      <p>Additionally, there are several factors which may lead an organization to move clusters to the fast channel either permanently or temporarily including the following:</p>
      <ul>
      <li>The desire to apply a specific fix known to affect your environment without delay.</li>
      <li>Application of CVE fixes without delay. CVE fixes may introduce regressions, so promotion delays still apply to z-streams with CVE fixes.</li>
      <li>Internal testing processes. If it takes your organization several weeks to qualify releases it is best test concurrently with our promotion process rather than waiting. This also assures that any telemetry signal provided to Red Hat is a factored into our rollout, so issues relevant to you can be fixed faster.</li>
      </ul>
      <h2 id="switching-between-channels_understanding-update-channels-releases" class="group scroll-mt-20">Considerations for switching between channels<a href="#switching-between-channels_understanding-update-channels-releases" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can switch your cluster’s update channel through the web console or the CLI, in order to access different update recommendations for your cluster.</p>
      <p>You can switch the channel from the CLI by running the following command:</p>
      <CodeBlock code={"$ oc adm upgrade channel <channel>"} lang="terminal" />
      <p>The web console will display an alert if you switch to a channel that does not include the current release. The web console does not recommend any updates while on a channel without the current release. You can return to the original channel at any point, however.</p>
      <p>Changing your channel might impact the supportability of your cluster. The following conditions might apply:</p>
      <ul>
      <li>Your cluster is still supported if you change from the <code>stable-{"{"}{"{"} product_version {"}"}{"}"}</code> channel to the <code>fast-{"{"}{"{"} product_version {"}"}{"}"}</code> channel.</li>
      <li>You can switch to the <code>candidate-{"{"}{"{"} product_version {"}"}{"}"}</code> channel at any time, but some releases for this channel might be unsupported.</li>
      <li>You can switch from the <code>candidate-{"{"}{"{"} product_version {"}"}{"}"}</code> channel to the <code>fast-{"{"}{"{"} product_version {"}"}{"}"}</code> channel if your current release is a general availability release.</li>
      <li>You can always switch from the <code>fast-{"{"}{"{"} product_version {"}"}{"}"}</code> channel to the <code>stable-{"{"}{"{"} product_version {"}"}{"}"}</code> channel. There is a possible delay of up to a day for the release to be promoted to <code>stable-{"{"}{"{"} product_version {"}"}{"}"}</code> if the current release was recently promoted.</li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
