---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: updating/preparing_for_updates/kmm-preflight-validation.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"kmm-preflight-validation","text":"Preflight validation for Kernel Module Management (KMM) Modules"},{"depth":2,"slug":"kmm-validation-kickoff_kmm-preflight-validation","text":"Validation kickoff"},{"depth":2,"slug":"kmm-validation-lifecycle_kmm-preflight-validation","text":"Validation lifecycle"},{"depth":2,"slug":"kmm-validation-status_kmm-preflight-validation","text":"Validation status"},{"depth":2,"slug":"kmm-image-validation-stage_kmm-preflight-validation","text":"Image validation stage"},{"depth":2,"slug":"kmm-example-cr_kmm-preflight-validation","text":"Example PreflightValidationOCP resource"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/updating/preparing_for_updates/kmm-preflight-validation";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Preflight validation for Kernel Module Management (KMM) Modules";
const description = "Before you upgrade a cluster that uses Kernel Module Management (KMM) modules, verify that the kernel modules can be installed on the nodes after the…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["updating","updating/preparing_for_updates"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Preflight validation for Kernel Module Management (KMM) Modules {#kmm-preflight-validation}\n\nBefore you upgrade a cluster that uses Kernel Module Management (KMM) modules, verify that the kernel modules can be installed on the nodes after the upgrade. This preflight validation helps you avoid unexpected module failures caused by kernel changes.\n\nPreflight attempts to validate every `Module` loaded in the cluster, in parallel. Preflight does not wait for validation of one `Module` to complete before starting validation of another `Module`.\n\n## Validation kickoff {#kmm-validation-kickoff_kmm-preflight-validation}\n\nCreate a `PreflightValidationOCP` resource to trigger preflight validation and specify the kernel version and DTK image for validation.\n\nPreflight validation is triggered by creating a `PreflightValidationOCP` resource in the cluster. This resource contains the following fields:\n\n`dtkImage`\n:   The DTK container image released for the specific OpenShift Container Platform version of the cluster. If this value is not set, the `DTK_AUTO` feature cannot be used.\n\n    You can obtain the image by running one of the following commands in the cluster:\n\n    ```terminal\n    # For x86_64 image:\n    $ oc adm release info quay.io/openshift-release-dev/ocp-release:4.22.0-x86_64 --image-for=driver-toolkit\n    ```\n\n    ```terminal\n    # For ARM64 image:\n    $ oc adm release info quay.io/openshift-release-dev/ocp-release:4.22.0-aarch64 --image-for=driver-toolkit\n    ```\n\n`kernelVersion`\n:   Required field that provides the version of the kernel that the cluster is upgraded to.\n\n    You can obtain the version by running the following command in the cluster:\n\n    ```terminal\n    $ podman run -it --rm $(oc adm release info quay.io/openshift-release-dev/ocp-release:4.22.0-x86_64 --image-for=driver-toolkit) cat /etc/driver-toolkit-release.json\n    ```\n\n`pushBuiltImage`\n:   If `true`, then the images created during the Build and Sign validation are pushed to their repositories. This field is `false` by default.\n\n## Validation lifecycle {#kmm-validation-lifecycle_kmm-preflight-validation}\n\nPreflight validation continuously validates all cluster modules, retrying failures after changes until all modules succeed or the validation resource is deleted.\n\nEach module stops being validated after it succeeds individually. Failed modules are retried in subsequent validation loops.\n\nIf you want to run Preflight validation for an additional kernel, then you should create another `PreflightValidationOCP` resource for that kernel. After all the modules have been validated, it is recommended to delete the `PreflightValidationOCP` resource.\n\n## Validation status {#kmm-validation-status_kmm-preflight-validation}\n\nThe `PreflightValidationOCP` resource reports validation status and progress for each cluster module in its `.status.modules` list.\n\nThe following outlines the fields included in the `.status.modules` list:\n\n`name`\n:   The name of the `Module` resource.\n\n`namespace`\n:   The namespace of the `Module` resource.\n\n`statusReason`\n:   Verbal explanation regarding the status.\n\n`verificationStage`\n:   Describes the validation stage being executed:\n\n    - `Image`: Image existence verification\n    - `Done`: Verification is done\n\n`verificationStatus`\n:   The status of the Module verification:\n\n- `Success`: Verified\n- `Failure`: Verification failed\n- `InProgress`: Verification is in progress\n\n## Image validation stage {#kmm-image-validation-stage_kmm-preflight-validation}\n\nImage validation checks whether kernel module images exist and are accessible before attempting to build or sign new images.\n\nImage validation is always the first stage of the preflight validation to be executed. If image validation is successful, no other validations are run on that specific module. The Operator uses the container runtime to check the image existence and accessibility for the updated kernel in the module.\n\nIf the image validation fails and there is a `build/sign` section in the module that is relevant to the upgraded kernel, the controller tries to build or sign the image. If the `PushBuiltImage` flag is defined in the `PreflightValidationOCP` resource, the controller will also try to push the resulting image into its repository. The resulting image name is taken from the definition of the `containerImage` field of the `Module` CR.\n\n> [!NOTE]\n> In case a `build` section exists, the input image in the `sign` section is used as the output image by the `build` section. Therefore, in order for the input image to be available to the `sign` section, the `PushBuiltImage` flag must be defined in the `PreflightValidationOCP` CR.\n\n## Example PreflightValidationOCP resource {#kmm-example-cr_kmm-preflight-validation}\n\nThe example `PreflightValidationOCP` resource validates kernel modules and pushes built images to repositories.\n\nThe example verifies all of the currently present modules against the upcoming `5.14.0-570.19.1.el9_6.x86_64` kernel. Because `.spec.pushBuiltImage` is set to `true`, KMM pushes the resulting images of Build/Sign in to the defined repositories.\n\n```yaml\napiVersion: kmm.sigs.x-k8s.io/v1beta2\nkind: PreflightValidationOCP\nmetadata:\n  name: preflight\nspec:\n  kernelVersion: 5.14.0-570.19.1.el9_6.x86_64\n  dtkImage: quay.io/openshift-release-dev/ocp-v4.0-art-dev@sha256:fe0322730440f1cbe6fffaaa8cac131b56574bec8abe3ec5b462e17557fecb32\n  pushBuiltImage: true\n```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/updating/preparing_for_updates/kmm-preflight-validation.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="kmm-preflight-validation" class="group scroll-mt-20">Preflight validation for Kernel Module Management (KMM) Modules<a href="#kmm-preflight-validation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Before you upgrade a cluster that uses Kernel Module Management (KMM) modules, verify that the kernel modules can be installed on the nodes after the upgrade. This preflight validation helps you avoid unexpected module failures caused by kernel changes.</p>
      <p>Preflight attempts to validate every <code>Module</code> loaded in the cluster, in parallel. Preflight does not wait for validation of one <code>Module</code> to complete before starting validation of another <code>Module</code>.</p>
      <h2 id="kmm-validation-kickoff_kmm-preflight-validation" class="group scroll-mt-20">Validation kickoff<a href="#kmm-validation-kickoff_kmm-preflight-validation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Create a <code>PreflightValidationOCP</code> resource to trigger preflight validation and specify the kernel version and DTK image for validation.</p>
      <p>Preflight validation is triggered by creating a <code>PreflightValidationOCP</code> resource in the cluster. This resource contains the following fields:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>dtkImage</code></dt>
      <dd class="ml-6 text-muted-foreground">
      <p>The DTK container image released for the specific OpenShift Container Platform version of the cluster. If this value is not set, the <code>DTK_AUTO</code> feature cannot be used.</p>
      <p>You can obtain the image by running one of the following commands in the cluster:</p>
      <CodeBlock code={"# For x86_64 image:\n$ oc adm release info quay.io/openshift-release-dev/ocp-release:4.22.0-x86_64 --image-for=driver-toolkit"} lang="terminal" />
      <CodeBlock code={"# For ARM64 image:\n$ oc adm release info quay.io/openshift-release-dev/ocp-release:4.22.0-aarch64 --image-for=driver-toolkit"} lang="terminal" />
      </dd>
      <dt class="font-semibold"><code>kernelVersion</code></dt>
      <dd class="ml-6 text-muted-foreground">
      <p>Required field that provides the version of the kernel that the cluster is upgraded to.</p>
      <p>You can obtain the version by running the following command in the cluster:</p>
      <CodeBlock code={"$ podman run -it --rm $(oc adm release info quay.io/openshift-release-dev/ocp-release:4.22.0-x86_64 --image-for=driver-toolkit) cat /etc/driver-toolkit-release.json"} lang="terminal" />
      </dd>
      <dt class="font-semibold"><code>pushBuiltImage</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>If <code>true</code>, then the images created during the Build and Sign validation are pushed to their repositories. This field is <code>false</code> by default.</p></dd>
      </dl>
      <h2 id="kmm-validation-lifecycle_kmm-preflight-validation" class="group scroll-mt-20">Validation lifecycle<a href="#kmm-validation-lifecycle_kmm-preflight-validation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Preflight validation continuously validates all cluster modules, retrying failures after changes until all modules succeed or the validation resource is deleted.</p>
      <p>Each module stops being validated after it succeeds individually. Failed modules are retried in subsequent validation loops.</p>
      <p>If you want to run Preflight validation for an additional kernel, then you should create another <code>PreflightValidationOCP</code> resource for that kernel. After all the modules have been validated, it is recommended to delete the <code>PreflightValidationOCP</code> resource.</p>
      <h2 id="kmm-validation-status_kmm-preflight-validation" class="group scroll-mt-20">Validation status<a href="#kmm-validation-status_kmm-preflight-validation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The <code>PreflightValidationOCP</code> resource reports validation status and progress for each cluster module in its <code>.status.modules</code> list.</p>
      <p>The following outlines the fields included in the <code>.status.modules</code> list:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The name of the <code>Module</code> resource.</p></dd>
      <dt class="font-semibold"><code>namespace</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The namespace of the <code>Module</code> resource.</p></dd>
      <dt class="font-semibold"><code>statusReason</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Verbal explanation regarding the status.</p></dd>
      <dt class="font-semibold"><code>verificationStage</code></dt>
      <dd class="ml-6 text-muted-foreground">
      <p>Describes the validation stage being executed:</p>
      <ul>
      <li><code>Image</code>: Image existence verification</li>
      <li><code>Done</code>: Verification is done</li>
      </ul>
      </dd>
      <dt class="font-semibold"><code>verificationStatus</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The status of the Module verification:</p></dd>
      </dl>
      <ul>
      <li><code>Success</code>: Verified</li>
      <li><code>Failure</code>: Verification failed</li>
      <li><code>InProgress</code>: Verification is in progress</li>
      </ul>
      <h2 id="kmm-image-validation-stage_kmm-preflight-validation" class="group scroll-mt-20">Image validation stage<a href="#kmm-image-validation-stage_kmm-preflight-validation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Image validation checks whether kernel module images exist and are accessible before attempting to build or sign new images.</p>
      <p>Image validation is always the first stage of the preflight validation to be executed. If image validation is successful, no other validations are run on that specific module. The Operator uses the container runtime to check the image existence and accessibility for the updated kernel in the module.</p>
      <p>If the image validation fails and there is a <code>build/sign</code> section in the module that is relevant to the upgraded kernel, the controller tries to build or sign the image. If the <code>PushBuiltImage</code> flag is defined in the <code>PreflightValidationOCP</code> resource, the controller will also try to push the resulting image into its repository. The resulting image name is taken from the definition of the <code>containerImage</code> field of the <code>Module</code> CR.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>In case a <code>build</code> section exists, the input image in the <code>sign</code> section is used as the output image by the <code>build</code> section. Therefore, in order for the input image to be available to the <code>sign</code> section, the <code>PushBuiltImage</code> flag must be defined in the <code>PreflightValidationOCP</code> CR.</p>
        </AlertDescription>
      </Alert>
      <h2 id="kmm-example-cr_kmm-preflight-validation" class="group scroll-mt-20">Example PreflightValidationOCP resource<a href="#kmm-example-cr_kmm-preflight-validation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The example <code>PreflightValidationOCP</code> resource validates kernel modules and pushes built images to repositories.</p>
      <p>The example verifies all of the currently present modules against the upcoming <code>5.14.0-570.19.1.el9_6.x86_64</code> kernel. Because <code>.spec.pushBuiltImage</code> is set to <code>true</code>, KMM pushes the resulting images of Build/Sign in to the defined repositories.</p>
      <CodeBlock code={"apiVersion: kmm.sigs.x-k8s.io/v1beta2\nkind: PreflightValidationOCP\nmetadata:\n  name: preflight\nspec:\n  kernelVersion: 5.14.0-570.19.1.el9_6.x86_64\n  dtkImage: quay.io/openshift-release-dev/ocp-v4.0-art-dev@sha256:fe0322730440f1cbe6fffaaa8cac131b56574bec8abe3ec5b462e17557fecb32\n  pushBuiltImage: true"} lang="yaml" />
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
