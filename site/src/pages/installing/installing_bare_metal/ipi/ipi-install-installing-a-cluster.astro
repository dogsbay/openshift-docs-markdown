---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: installing/installing_bare_metal/ipi/ipi-install-installing-a-cluster.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"ipi-install-installing-a-cluster","text":"Installing a cluster"},{"depth":2,"slug":"ipi-install-troubleshooting-cleaning-up-previous-installations_ipi-install-installing-a-cluster","text":"Cleaning up previous installations"},{"depth":2,"slug":"deploying-the-cluster-via-the-openshift-installer_ipi-install-installing-a-cluster","text":"Deploying the cluster via the OpenShift Container Platform installer"},{"depth":2,"slug":"ipi-install-following-the-progress-of-the-installation_ipi-install-installing-a-cluster","text":"Following the progress of the installation"},{"depth":2,"slug":"verifying-static-ip-address-configuration_ipi-install-installing-a-cluster","text":"Verifying static IP address configuration"},{"depth":2,"slug":"additional-resources_creating_manifest_ignition","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/installing/installing_bare_metal/ipi/ipi-install-installing-a-cluster";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Installing a cluster";
const description = "You can install a cluster after setting up the environment for an OpenShift Container Platform installation. Ensure you have completed the prerequisites…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["installing","installing/installing_bare_metal","installing/installing_bare_metal/ipi"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Installing a cluster {#ipi-install-installing-a-cluster}\n\nYou can install a cluster after setting up the environment for an OpenShift Container Platform installation. Ensure you have completed the prerequisites before installing a cluster.\n\n## Cleaning up previous installations {#ipi-install-troubleshooting-cleaning-up-previous-installations_ipi-install-installing-a-cluster}\n\nYou must remove the artifacts from any failed deployment attempt before trying to deploy OpenShift Container Platform again.\n\n**Procedure**\n\n1. Power off all bare-metal nodes before installing the OpenShift Container Platform cluster by running the following command:\n\n   ```terminal\n   $ ipmitool -I lanplus -U <user> -P <password> -H <management_server_ip> power off\n   ```\n2. Remove all old bootstrap resources if any remain from an earlier deployment attempt by running the following script:\n\n   ```bash\n   for i in $(sudo virsh list | tail -n +3 | grep bootstrap | awk {'print $2'});\n   do\n     sudo virsh destroy $i;\n     sudo virsh undefine $i;\n     sudo virsh vol-delete $i --pool $i;\n     sudo virsh vol-delete $i.ign --pool $i;\n     sudo virsh pool-destroy $i;\n     sudo virsh pool-undefine $i;\n   done\n   ```\n3. Delete the artifacts that the earlier installation generated by running the following command:\n\n   ```terminal\n   $ cd ; /bin/rm -rf auth/ bootstrap.ign master.ign worker.ign metadata.json \\\n   .openshift_install.log .openshift_install_state.json\n   ```\n4. Re-create the OpenShift Container Platform manifests by running the following command:\n\n   ```terminal\n   $ ./openshift-baremetal-install --dir ~/clusterconfigs create manifests\n   ```\n\n## Deploying the cluster via the OpenShift Container Platform installer {#deploying-the-cluster-via-the-openshift-installer_ipi-install-installing-a-cluster}\n\nYou can deploy the cluster by running the OpenShift Container Platform installer.\n\n**Procedure**\n\n- Run the OpenShift Container Platform installer:\n\n  ```terminal\n  $ ./openshift-baremetal-install --dir ~/clusterconfigs --log-level debug create cluster\n  ```\n\n## Following the progress of the installation {#ipi-install-following-the-progress-of-the-installation_ipi-install-installing-a-cluster}\n\nDuring the deployment process, you can check the installation’s overall status by issuing the `tail` command to the `.openshift_install.log` log file in the install directory folder.\n\n**Procedure**\n\n- Track installation progress by running the following command:\n\n  ```terminal\n  $ tail -f /path/to/install-dir/.openshift_install.log\n  ```\n\n## Verifying static IP address configuration {#verifying-static-ip-address-configuration_ipi-install-installing-a-cluster}\n\nEnsure that all required steps are complete and verify the network configuration is working properly.\n\nIf the DHCP reservation for a cluster node specifies an infinite lease, after the installer successfully provisions the node, the dispatcher script checks the node’s network configuration.\n\nIf the script determines that the network configuration contains an infinite DHCP lease, it creates a new connection by using the IP address of the DHCP lease as a static IP address.\n\n> [!NOTE]\n> The dispatcher script might run on successfully provisioned nodes while the provisioning of other nodes in the cluster is ongoing.\n\n**Procedure**\n\n1. Check the network interface configuration on the node.\n2. Turn off the DHCP server and reboot the OpenShift Container Platform node and ensure that the network configuration works properly.\n\n## Additional resources {#additional-resources_creating_manifest_ignition}\n\n- [Understanding update channels and releases](/openshift-docs-markdown/updating/understanding_updates/understanding-update-channels-release#understanding-update-channels-releases)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/installing/installing_bare_metal/ipi/ipi-install-installing-a-cluster.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="ipi-install-installing-a-cluster" class="group scroll-mt-20">Installing a cluster<a href="#ipi-install-installing-a-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can install a cluster after setting up the environment for an OpenShift Container Platform installation. Ensure you have completed the prerequisites before installing a cluster.</p>
      <h2 id="ipi-install-troubleshooting-cleaning-up-previous-installations_ipi-install-installing-a-cluster" class="group scroll-mt-20">Cleaning up previous installations<a href="#ipi-install-troubleshooting-cleaning-up-previous-installations_ipi-install-installing-a-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You must remove the artifacts from any failed deployment attempt before trying to deploy OpenShift Container Platform again.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Power off all bare-metal nodes before installing the OpenShift Container Platform cluster by running the following command:
      <CodeBlock code={"$ ipmitool -I lanplus -U <user> -P <password> -H <management_server_ip> power off"} lang="terminal" />
      </li>
      <li>
      Remove all old bootstrap resources if any remain from an earlier deployment attempt by running the following script:
      <CodeBlock code={"for i in $(sudo virsh list | tail -n +3 | grep bootstrap | awk {'print $2'});\ndo\n  sudo virsh destroy $i;\n  sudo virsh undefine $i;\n  sudo virsh vol-delete $i --pool $i;\n  sudo virsh vol-delete $i.ign --pool $i;\n  sudo virsh pool-destroy $i;\n  sudo virsh pool-undefine $i;\ndone"} lang="bash" />
      </li>
      <li>
      Delete the artifacts that the earlier installation generated by running the following command:
      <CodeBlock code={"$ cd ; /bin/rm -rf auth/ bootstrap.ign master.ign worker.ign metadata.json \\\n.openshift_install.log .openshift_install_state.json"} lang="terminal" />
      </li>
      <li>
      Re-create the OpenShift Container Platform manifests by running the following command:
      <CodeBlock code={"$ ./openshift-baremetal-install --dir ~/clusterconfigs create manifests"} lang="terminal" />
      </li>
      </ol>
      <h2 id="deploying-the-cluster-via-the-openshift-installer_ipi-install-installing-a-cluster" class="group scroll-mt-20">Deploying the cluster via the OpenShift Container Platform installer<a href="#deploying-the-cluster-via-the-openshift-installer_ipi-install-installing-a-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can deploy the cluster by running the OpenShift Container Platform installer.</p>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Run the OpenShift Container Platform installer:
      <CodeBlock code={"$ ./openshift-baremetal-install --dir ~/clusterconfigs --log-level debug create cluster"} lang="terminal" />
      </li>
      </ul>
      <h2 id="ipi-install-following-the-progress-of-the-installation_ipi-install-installing-a-cluster" class="group scroll-mt-20">Following the progress of the installation<a href="#ipi-install-following-the-progress-of-the-installation_ipi-install-installing-a-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">During the deployment process, you can check the installation’s overall status by issuing the <code>tail</code> command to the <code>.openshift_install.log</code> log file in the install directory folder.</p>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Track installation progress by running the following command:
      <CodeBlock code={"$ tail -f /path/to/install-dir/.openshift_install.log"} lang="terminal" />
      </li>
      </ul>
      <h2 id="verifying-static-ip-address-configuration_ipi-install-installing-a-cluster" class="group scroll-mt-20">Verifying static IP address configuration<a href="#verifying-static-ip-address-configuration_ipi-install-installing-a-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Ensure that all required steps are complete and verify the network configuration is working properly.</p>
      <p>If the DHCP reservation for a cluster node specifies an infinite lease, after the installer successfully provisions the node, the dispatcher script checks the node’s network configuration.</p>
      <p>If the script determines that the network configuration contains an infinite DHCP lease, it creates a new connection by using the IP address of the DHCP lease as a static IP address.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The dispatcher script might run on successfully provisioned nodes while the provisioning of other nodes in the cluster is ongoing.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Check the network interface configuration on the node.</li>
      <li>Turn off the DHCP server and reboot the OpenShift Container Platform node and ensure that the network configuration works properly.</li>
      </ol>
      <h2 id="additional-resources_creating_manifest_ignition" class="group scroll-mt-20">Additional resources<a href="#additional-resources_creating_manifest_ignition" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul><li><a href="/openshift-docs-markdown/updating/understanding_updates/understanding-update-channels-release#understanding-update-channels-releases">Understanding update channels and releases</a></li></ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
