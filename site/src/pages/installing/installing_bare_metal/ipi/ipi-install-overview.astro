---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: installing/installing_bare_metal/ipi/ipi-install-overview.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import BlockImage from "@ui/image/Image.astro";

const headings = [{"depth":1,"slug":"ipi-install-overview","text":"Overview"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/installing/installing_bare_metal/ipi/ipi-install-overview";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Overview";
const description = "Installer-provisioned installation on bare metal nodes deploys and configures the infrastructure that an OpenShift Container Platform cluster runs on. By…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["installing","installing/installing_bare_metal","installing/installing_bare_metal/ipi"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Overview {#ipi-install-overview}\n\nInstaller-provisioned installation on bare metal nodes deploys and configures the infrastructure that an OpenShift Container Platform cluster runs on. By understanding the components of bare metal installation, you can prepare your infrastructure for a successful installation.\n\nThe following diagram illustrates phase 1 of deployment:\n\n**Figure 1. Deployment phase one**\n\n![Deployment phase one](/openshift-docs-markdown/_assets/images/210_OpenShift_Baremetal_IPI_Deployment_updates_0122_1.png)\n\nFor the installation, the key elements in the deployment phase one diagram are:\n\n- **Provisioner**: A physical machine that runs the installation program and hosts the bootstrap VM that deploys the control plane of a new OpenShift Container Platform cluster.\n- **Bootstrap VM**: A virtual machine used in the process of deploying an OpenShift Container Platform cluster.\n- **Network bridges**: The bootstrap VM connects to the bare metal network and to the provisioning network, if present, via network bridges, `eno1` and `eno2`.\n- **API VIP**: An API virtual IP address (VIP) is used to provide failover of the API server across the control plane nodes. The API VIP first resides on the bootstrap VM. A script generates the `keepalived.conf` configuration file before launching the service. The VIP moves to one of the control plane nodes after the bootstrap process has completed and the bootstrap VM stops.\n\n> [!IMPORTANT]\n> The provisioning network is optional, but it is required for PXE booting. If you deploy without a provisioning network, you must use a virtual media baseboard management controller (BMC) addressing option such as `redfish-virtualmedia` or `idrac-virtualmedia`. Installing a cluster using a provisioning network has not been tested and validated with Dell iDRAC 10. If you are installing a cluster with installer-provisioned infrastructure on iDRAC 10, it is recommended to use virtual media, which has been validated with iDRAC 10.\n\nIn phase 2 of the deployment, the provisioner destroys the bootstrap VM automatically and moves the virtual IP addresses (VIPs) to the appropriate nodes.\n\nThe `keepalived.conf` file sets the control plane machines with a lower Virtual Router Redundancy Protocol (VRRP) priority than the bootstrap VM, which ensures that the API on the control plane machines is fully functional before the API VIP moves from the bootstrap VM to the control plane. Once the API VIP moves to one of the control plane nodes, traffic sent from external clients to the API VIP routes to an `haproxy` load balancer running on that control plane node. This instance of `haproxy` load balances the API VIP traffic across the control plane nodes.\n\nThe Ingress VIP moves to the compute nodes. The `keepalived` instance also manages the Ingress VIP.\n\nThe following diagram illustrates phase 2 of deployment:\n\n**Figure 2. Deployment phase two**\n\n![Deployment phase two](/openshift-docs-markdown/_assets/images/210_OpenShift_Baremetal_IPI_Deployment_updates_0122_2.png)\n\nAfter this point, the node used by the provisioner can be removed or repurposed. From here, all additional provisioning tasks are carried out by the control plane.\n\n> [!NOTE]\n> For installer-provisioned infrastructure installations, CoreDNS exposes port 53 at the node level, making it accessible from other routable networks.\n\n**Additional resources**\n{._additional-resources}\n\n- [Using DNS forwarding](/openshift-docs-markdown/networking/networking_operators/dns-operator#nw-dns-forward_dns-operator)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/installing/installing_bare_metal/ipi/ipi-install-overview.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="ipi-install-overview" class="group scroll-mt-20">Overview<a href="#ipi-install-overview" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Installer-provisioned installation on bare metal nodes deploys and configures the infrastructure that an OpenShift Container Platform cluster runs on. By understanding the components of bare metal installation, you can prepare your infrastructure for a successful installation.</p>
      <p>The following diagram illustrates phase 1 of deployment:</p>
      <p><strong>Figure 1. Deployment phase one</strong></p>
      <BlockImage src="/openshift-docs-markdown/_assets/images/210_OpenShift_Baremetal_IPI_Deployment_updates_0122_1.png" alt="Deployment phase one" />
      <p>For the installation, the key elements in the deployment phase one diagram are:</p>
      <ul>
      <li><strong>Provisioner</strong>: A physical machine that runs the installation program and hosts the bootstrap VM that deploys the control plane of a new OpenShift Container Platform cluster.</li>
      <li><strong>Bootstrap VM</strong>: A virtual machine used in the process of deploying an OpenShift Container Platform cluster.</li>
      <li><strong>Network bridges</strong>: The bootstrap VM connects to the bare metal network and to the provisioning network, if present, via network bridges, <code>eno1</code> and <code>eno2</code>.</li>
      <li><strong>API VIP</strong>: An API virtual IP address (VIP) is used to provide failover of the API server across the control plane nodes. The API VIP first resides on the bootstrap VM. A script generates the <code>keepalived.conf</code> configuration file before launching the service. The VIP moves to one of the control plane nodes after the bootstrap process has completed and the bootstrap VM stops.</li>
      </ul>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>The provisioning network is optional, but it is required for PXE booting. If you deploy without a provisioning network, you must use a virtual media baseboard management controller (BMC) addressing option such as <code>redfish-virtualmedia</code> or <code>idrac-virtualmedia</code>. Installing a cluster using a provisioning network has not been tested and validated with Dell iDRAC 10. If you are installing a cluster with installer-provisioned infrastructure on iDRAC 10, it is recommended to use virtual media, which has been validated with iDRAC 10.</p>
        </AlertDescription>
      </Alert>
      <p>In phase 2 of the deployment, the provisioner destroys the bootstrap VM automatically and moves the virtual IP addresses (VIPs) to the appropriate nodes.</p>
      <p>The <code>keepalived.conf</code> file sets the control plane machines with a lower Virtual Router Redundancy Protocol (VRRP) priority than the bootstrap VM, which ensures that the API on the control plane machines is fully functional before the API VIP moves from the bootstrap VM to the control plane. Once the API VIP moves to one of the control plane nodes, traffic sent from external clients to the API VIP routes to an <code>haproxy</code> load balancer running on that control plane node. This instance of <code>haproxy</code> load balances the API VIP traffic across the control plane nodes.</p>
      <p>The Ingress VIP moves to the compute nodes. The <code>keepalived</code> instance also manages the Ingress VIP.</p>
      <p>The following diagram illustrates phase 2 of deployment:</p>
      <p><strong>Figure 2. Deployment phase two</strong></p>
      <BlockImage src="/openshift-docs-markdown/_assets/images/210_OpenShift_Baremetal_IPI_Deployment_updates_0122_2.png" alt="Deployment phase two" />
      <p>After this point, the node used by the provisioner can be removed or repurposed. From here, all additional provisioning tasks are carried out by the control plane.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>For installer-provisioned infrastructure installations, CoreDNS exposes port 53 at the node level, making it accessible from other routable networks.</p>
        </AlertDescription>
      </Alert>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="/openshift-docs-markdown/networking/networking_operators/dns-operator#nw-dns-forward_dns-operator">Using DNS forwarding</a></li></ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
