---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: installing/installing_with_agent_based_installer/understanding-disconnected-installation-mirroring.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"understanding-disconnected-installation-mirroring","text":"Understanding disconnected installation mirroring"},{"depth":2,"slug":"agent-install-about-mirroring-for-disconnected-registry_understanding-disconnected-installation-mirroring","text":"About mirroring the OpenShift Container Platform image repository for a disconnected registry"},{"depth":3,"slug":"agent-install-configuring-for-disconnected-registry_understanding-disconnected-installation-mirroring","text":"Configuring the Agent-based Installer to use mirrored images"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/installing/installing_with_agent_based_installer/understanding-disconnected-installation-mirroring";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Understanding disconnected installation mirroring";
const description = "You can use a mirror registry for disconnected installations and to ensure that your clusters only use container images that satisfy your organization’s…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["installing","installing/installing_with_agent_based_installer"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Understanding disconnected installation mirroring {#understanding-disconnected-installation-mirroring}\n\nYou can use a mirror registry for disconnected installations and to ensure that your clusters only use container images that satisfy your organization’s controls on external content.\n\nBefore you install a cluster on infrastructure that you provision in a disconnected environment, you must mirror the required container images into that environment. To mirror container images, you must have a registry for mirroring.\n\nYou can use one of the following procedures to mirror your OpenShift Container Platform image repository to your mirror registry:\n\n- \"Mirroring images for a disconnected installation by using the oc-mirror plugin v2\"\n- \"Mirroring images for a disconnected installation\"\n\n## About mirroring the OpenShift Container Platform image repository for a disconnected registry {#agent-install-about-mirroring-for-disconnected-registry_understanding-disconnected-installation-mirroring}\n\nTo use mirror images for a disconnected installation with the Agent-based Installer, you must modify the `install-config.yaml` file.\n\nYou can mirror the release image by using the output of the `oc mirror` command.\n\n> [!IMPORTANT]\n> The `oc adm release mirror` command is deprecated as of OpenShift Container Platform 4.22 and will be removed in a future release.\n>\n> As an alternative, use the oc-mirror plugin v2.\n\nThe following example shows the output of the `oc adm release mirror` command.\n\n```terminal\n$ oc adm release mirror\n```\n\n```terminal {title=\"Example output\"}\nTo use the new mirrored repository to install, add the following\nsection to the install-config.yaml:\n\nimageContentSources:\n\nmirrors:\nvirthost.ostest.test.metalkube.org:5000/localimages/local-release-image\nsource: quay.io/openshift-release-dev/ocp-v4.0-art-dev\nmirrors:\nvirthost.ostest.test.metalkube.org:5000/localimages/local-release-image\nsource: registry.ci.openshift.org/ocp/release\n```\n\nThe following example shows part of the `imageContentSourcePolicy.yaml` file generated by the oc-mirror plugin. The file can be found in the results directory, for example `oc-mirror-workspace/results-1682697932/`.\n\n```yaml {title=\"Example imageContentSourcePolicy.yaml file\"}\nspec:\n  repositoryDigestMirrors:\n  - mirrors:\n    - virthost.ostest.test.metalkube.org:5000/openshift/release\n    source: quay.io/openshift-release-dev/ocp-v4.0-art-dev\n  - mirrors:\n    - virthost.ostest.test.metalkube.org:5000/openshift/release-images\n    source: quay.io/openshift-release-dev/ocp-release\n```\n\n### Configuring the Agent-based Installer to use mirrored images {#agent-install-configuring-for-disconnected-registry_understanding-disconnected-installation-mirroring}\n\nYou must use the output of either the `oc adm release mirror` command or the oc-mirror plugin to configure the Agent-based Installer to use mirrored images.\n\n**Procedure**\n\n1. If you used the oc-mirror plugin to mirror your release images:\n\n   1. Open the `imageContentSourcePolicy.yaml` located in the results directory, for example `oc-mirror-workspace/results-1682697932/`.\n   2. Copy the text in the `repositoryDigestMirrors` section of the yaml file.\n2. If you used the `oc adm release mirror` command to mirror your release images:\n\n   - Copy the text in the `imageContentSources` section of the command output.\n3. Paste the copied text into the `imageContentSources` field of the `install-config.yaml` file.\n4. Add the certificate file used for the mirror registry to the `additionalTrustBundle` field of the yaml file.\n\n   > [!IMPORTANT]\n   > The value must be the contents of the certificate file that you used for your mirror registry. The certificate file can be an existing, trusted certificate authority, or the self-signed certificate that you generated for the mirror registry.\n\n   ```yaml {title=\"Example install-config.yaml file\"}\n     additionalTrustBundle: |\n       -----BEGIN CERTIFICATE-----\n       ZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZ\n       -----END CERTIFICATE-----\n   ```\n5. If you are using GitOps ZTP manifests: add the `registries.conf` and `ca-bundle.crt` files  to the `mirror` path to add the mirror configuration in the agent ISO image.\n\n   > [!NOTE]\n   > You can create the `registries.conf` file from the output of either the `oc adm release mirror` command or the `oc mirror` plugin. The format of the `/etc/containers/registries.conf` file has changed. It is now version 2 and in TOML format.\n\n   ```toml {title=\"Example registries.conf file\"}\n   [[registry]]\n   location = \"registry.ci.openshift.org/ocp/release\" mirror-by-digest-only = true\n\n   [[registry.mirror]] location = \"virthost.ostest.test.metalkube.org:5000/localimages/local-release-image\"\n\n   [[registry]]\n   location = \"quay.io/openshift-release-dev/ocp-v4.0-art-dev\" mirror-by-digest-only = true\n\n   [[registry.mirror]] location = \"virthost.ostest.test.metalkube.org:5000/localimages/local-release-image\"\n   ```\n\n**Additional resources**\n{._additional-resources}\n\n- [Mirroring images for a disconnected installation by using the oc-mirror plugin v2](/openshift-docs-markdown/disconnected/about-installing-oc-mirror-v2#about-installing-oc-mirror-v2)\n- [Mirroring images for a disconnected installation](/openshift-docs-markdown/disconnected/installing-mirroring-installation-images#installing-mirroring-installation-images)\n- [Installing an OpenShift Container Platform cluster with the Agent-based Installer](/openshift-docs-markdown/installing/installing_with_agent_based_installer/installing-with-agent-based-installer#installing-with-agent-based-installer)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/installing/installing_with_agent_based_installer/understanding-disconnected-installation-mirroring.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="understanding-disconnected-installation-mirroring" class="group scroll-mt-20">Understanding disconnected installation mirroring<a href="#understanding-disconnected-installation-mirroring" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">You can use a mirror registry for disconnected installations and to ensure that your clusters only use container images that satisfy your organization’s controls on external content.</p>
      <p>Before you install a cluster on infrastructure that you provision in a disconnected environment, you must mirror the required container images into that environment. To mirror container images, you must have a registry for mirroring.</p>
      <p>You can use one of the following procedures to mirror your OpenShift Container Platform image repository to your mirror registry:</p>
      <ul>
      <li>"Mirroring images for a disconnected installation by using the oc-mirror plugin v2"</li>
      <li>"Mirroring images for a disconnected installation"</li>
      </ul>
      <h2 id="agent-install-about-mirroring-for-disconnected-registry_understanding-disconnected-installation-mirroring" class="group scroll-mt-20">About mirroring the OpenShift Container Platform image repository for a disconnected registry<a href="#agent-install-about-mirroring-for-disconnected-registry_understanding-disconnected-installation-mirroring" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To use mirror images for a disconnected installation with the Agent-based Installer, you must modify the <code>install-config.yaml</code> file.</p>
      <p>You can mirror the release image by using the output of the <code>oc mirror</code> command.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>The <code>oc adm release mirror</code> command is deprecated as of OpenShift Container Platform 4.22 and will be removed in a future release.</p>
          <p>As an alternative, use the oc-mirror plugin v2.</p>
        </AlertDescription>
      </Alert>
      <p>The following example shows the output of the <code>oc adm release mirror</code> command.</p>
      <CodeBlock code={"$ oc adm release mirror"} lang="terminal" />
      <CodeBlock code={"To use the new mirrored repository to install, add the following\nsection to the install-config.yaml:\n\nimageContentSources:\n\nmirrors:\nvirthost.ostest.test.metalkube.org:5000/localimages/local-release-image\nsource: quay.io/openshift-release-dev/ocp-v4.0-art-dev\nmirrors:\nvirthost.ostest.test.metalkube.org:5000/localimages/local-release-image\nsource: registry.ci.openshift.org/ocp/release"} lang="terminal" title="Example output" />
      <p>The following example shows part of the <code>imageContentSourcePolicy.yaml</code> file generated by the oc-mirror plugin. The file can be found in the results directory, for example <code>oc-mirror-workspace/results-1682697932/</code>.</p>
      <CodeBlock code={"spec:\n  repositoryDigestMirrors:\n  - mirrors:\n    - virthost.ostest.test.metalkube.org:5000/openshift/release\n    source: quay.io/openshift-release-dev/ocp-v4.0-art-dev\n  - mirrors:\n    - virthost.ostest.test.metalkube.org:5000/openshift/release-images\n    source: quay.io/openshift-release-dev/ocp-release"} lang="yaml" title="Example imageContentSourcePolicy.yaml file" />
      <h3 id="agent-install-configuring-for-disconnected-registry_understanding-disconnected-installation-mirroring" class="group scroll-mt-20">Configuring the Agent-based Installer to use mirrored images<a href="#agent-install-configuring-for-disconnected-registry_understanding-disconnected-installation-mirroring" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You must use the output of either the <code>oc adm release mirror</code> command or the oc-mirror plugin to configure the Agent-based Installer to use mirrored images.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      If you used the oc-mirror plugin to mirror your release images:
      <ol>
      <li>Open the <code>imageContentSourcePolicy.yaml</code> located in the results directory, for example <code>oc-mirror-workspace/results-1682697932/</code>.</li>
      <li>Copy the text in the <code>repositoryDigestMirrors</code> section of the yaml file.</li>
      </ol>
      </li>
      <li>
      If you used the <code>oc adm release mirror</code> command to mirror your release images:
      <ul><li>Copy the text in the <code>imageContentSources</code> section of the command output.</li></ul>
      </li>
      <li>Paste the copied text into the <code>imageContentSources</code> field of the <code>install-config.yaml</code> file.</li>
      <li>
      Add the certificate file used for the mirror registry to the <code>additionalTrustBundle</code> field of the yaml file.
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>The value must be the contents of the certificate file that you used for your mirror registry. The certificate file can be an existing, trusted certificate authority, or the self-signed certificate that you generated for the mirror registry.</p>
        </AlertDescription>
      </Alert>
      <CodeBlock code={"  additionalTrustBundle: |\n    -----BEGIN CERTIFICATE-----\n    ZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZ\n    -----END CERTIFICATE-----"} lang="yaml" title="Example install-config.yaml file" />
      </li>
      <li>
      If you are using GitOps ZTP manifests: add the <code>registries.conf</code> and <code>ca-bundle.crt</code> files  to the <code>mirror</code> path to add the mirror configuration in the agent ISO image.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>You can create the <code>registries.conf</code> file from the output of either the <code>oc adm release mirror</code> command or the <code>oc mirror</code> plugin. The format of the <code>/etc/containers/registries.conf</code> file has changed. It is now version 2 and in TOML format.</p>
        </AlertDescription>
      </Alert>
      <CodeBlock code={"[[registry]]\nlocation = \"registry.ci.openshift.org/ocp/release\" mirror-by-digest-only = true\n\n[[registry.mirror]] location = \"virthost.ostest.test.metalkube.org:5000/localimages/local-release-image\"\n\n[[registry]]\nlocation = \"quay.io/openshift-release-dev/ocp-v4.0-art-dev\" mirror-by-digest-only = true\n\n[[registry.mirror]] location = \"virthost.ostest.test.metalkube.org:5000/localimages/local-release-image\""} lang="toml" title="Example registries.conf file" />
      </li>
      </ol>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/disconnected/about-installing-oc-mirror-v2#about-installing-oc-mirror-v2">Mirroring images for a disconnected installation by using the oc-mirror plugin v2</a></li>
      <li><a href="/openshift-docs-markdown/disconnected/installing-mirroring-installation-images#installing-mirroring-installation-images">Mirroring images for a disconnected installation</a></li>
      <li><a href="/openshift-docs-markdown/installing/installing_with_agent_based_installer/installing-with-agent-based-installer#installing-with-agent-based-installer">Installing an OpenShift Container Platform cluster with the Agent-based Installer</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
