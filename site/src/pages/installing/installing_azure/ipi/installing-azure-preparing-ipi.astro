---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: installing/installing_azure/ipi/installing-azure-preparing-ipi.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"installing-azure-preparing-ipi","text":"Preparing to install a cluster on Azure"},{"depth":2,"slug":"prerequisites-installing-azure_installing-azure-preparing-ipi","text":"Prerequisites for installing a cluster on Azure"},{"depth":2,"slug":"cluster-entitlements_installing-azure-preparing-ipi","text":"Internet access for OpenShift Container Platform"},{"depth":2,"slug":"ssh-agent-using_installing-azure-preparing-ipi","text":"Generating a key pair for cluster node SSH access"},{"depth":2,"slug":"installation-obtaining-installer_installing-azure-preparing-ipi","text":"Obtaining the installation program"},{"depth":2,"slug":"cli-installing-cli-linux_installing-azure-preparing-ipi","text":"Installing the OpenShift CLI on Linux"},{"depth":2,"slug":"cli-installing-cli-windows_installing-azure-preparing-ipi","text":"Installing the OpenShift CLI on Windows"},{"depth":2,"slug":"cli-installing-cli-macos_installing-azure-preparing-ipi","text":"Installing the OpenShift CLI on macOS"},{"depth":2,"slug":"cluster-telemetry_installing-azure-preparing-ipi","text":"Telemetry access for OpenShift Container Platform"},{"depth":2,"slug":"installation-azure-day2-operations-diskencryptionsets.adoc_installing-azure-preparing-ipi","text":"Preparing an Azure Disk Encryption Set for Day2 Operator"},{"depth":2,"slug":"preparing-disk-encryption-sets_installing-azure-preparing-ipi","text":"Preparing an Azure Disk Encryption Set"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/installing/installing_azure/ipi/installing-azure-preparing-ipi";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Preparing to install a cluster on Azure";
const description = "Before you install an OpenShift Container Platform cluster on Azure with installer-provisioned infrastructure, you must configure your account, obtain the…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["installing","installing/installing_azure","installing/installing_azure/ipi"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Preparing to install a cluster on Azure {#installing-azure-preparing-ipi}\n\nBefore you install an OpenShift Container Platform cluster on Azure with installer-provisioned infrastructure, you must configure your account, obtain the installation program and `oc` CLI, and optionally prepare disk encryption sets.\n\n## Prerequisites for installing a cluster on Azure {#prerequisites-installing-azure_installing-azure-preparing-ipi}\n\nBefore you install an OpenShift Container Platform cluster on Azure, ensure that you have selected a cluster installation method and configured an Azure account to host the cluster and determine the tested and validated region to deploy the cluster to.\n\nIf you use a firewall, you must configure it to allow the sites that your cluster requires access to.\n\n**Additional resources**\n\n- [Select a cluster installation method](/openshift-docs-markdown/installing/overview/installing-preparing#installing-preparing)\n- [Configure an Azure account](/openshift-docs-markdown/installing/installing_azure/installing-azure-account#installing-azure-account)\n- [Configure your firewall](/openshift-docs-markdown/installing/install_config/configuring-firewall#configuring-firewall-module_configuring-firewall)\n\n## Internet access for OpenShift Container Platform {#cluster-entitlements_installing-azure-preparing-ipi}\n\nIn OpenShift Container Platform 4.22, you require access to the internet to install\n\nyour cluster.\n\nYou must have internet access to perform the following actions:\n\n- Access Red Hat Hybrid Cloud Console to download the installation program and perform subscription management. If the cluster has internet access and you do not disable Telemetry, that service automatically entitles your cluster.\n- Access Quay.io to obtain the packages that are required to install your cluster.\n- Obtain the packages that are required to perform cluster updates.\n\n> [!IMPORTANT]\n> If your cluster cannot have direct internet access, you can perform a restricted network installation on some types of infrastructure that you provision. During that process, you download the required content and use it to populate a mirror registry with the installation packages. With some installation types, the environment that you install your cluster in will not require internet access. Before you update the cluster, you update the content of the mirror registry.\n\n## Generating a key pair for cluster node SSH access {#ssh-agent-using_installing-azure-preparing-ipi}\n\nDuring an OpenShift Container Platform installation, you can provide an SSH public key to the installation program. The key is passed to the Red Hat Enterprise Linux CoreOS (RHCOS) nodes through their Ignition config files and is used to authenticate SSH access to the nodes. The key is added to the `~/.ssh/authorized_keys` list for the `core` user on each node, which enables password-less authentication.\n\nThe key is added to the `~/.ssh/authorized_keys` list for the `core` user on each node, which enables password-less authentication. After the key is passed to the nodes, you can use the key pair to SSH in to the RHCOS nodes as the user `core`. To access the nodes through SSH, the private key identity must be managed by SSH for your local user.\n\nIf you want to SSH in to your cluster nodes to perform installation debugging or disaster recovery, you must provide the SSH public key during the installation process. The `./openshift-install gather` command also requires the SSH public key to be in place on the cluster nodes.\n\n> [!IMPORTANT]\n> Do not skip this procedure in production environments, where disaster recovery and debugging is required.\n\n> [!NOTE]\n> You must use a local key, not one that you configured with platform-specific approaches.\n\n**Procedure**\n\n1. If you do not have an existing SSH key pair on your local machine to use for authentication onto your cluster nodes, create one. For example, on a computer that uses a Linux operating system, run the following command:\n\n   ```terminal\n   $ ssh-keygen -t ed25519 -N '' -f <path>/<file_name>\n   ```\n\n   Specifies the path and file name, such as `~/.ssh/id_ed25519`, of the new SSH key. If you have an existing key pair, ensure your public key is in the your `~/.ssh` directory.\n\n   > [!NOTE]\n   > If you plan to install an OpenShift Container Platform cluster that uses the RHEL cryptographic libraries that have been submitted to NIST for FIPS 140-2/140-3 Validation on only the `x86_64`, `ppc64le`, and `s390x` architectures, do not create a key that uses the `ed25519` algorithm. Instead, create a key that uses the `rsa` or `ecdsa` algorithm.\n2. View the public SSH key:\n\n   ```terminal\n   $ cat <path>/<file_name>.pub\n   ```\n\n   For example, run the following to view the `~/.ssh/id_ed25519.pub` public key:\n\n   ```terminal\n   $ cat ~/.ssh/id_ed25519.pub\n   ```\n3. Add the SSH private key identity to the SSH agent for your local user, if it has not already been added. SSH agent management of the key is required for password-less SSH authentication onto your cluster nodes, or if you want to use the `./openshift-install gather` command.\n\n   > [!NOTE]\n   > On some distributions, default SSH private key identities such as `~/.ssh/id_rsa` and `~/.ssh/id_dsa` are managed automatically.\n\n   1. If the `ssh-agent` process is not already running for your local user, start it as a background task:\n\n      ```terminal\n      $ eval \"$(ssh-agent -s)\"\n      ```\n\n      ```terminal {title=\"Example output\"}\n      Agent pid 31874\n      ```\n\n      > [!NOTE]\n      > If your cluster is in FIPS mode, only use FIPS-compliant algorithms to generate the SSH key. The key must be either RSA or ECDSA.\n4. Add your SSH private key to the `ssh-agent`:\n\n   ```terminal\n   $ ssh-add <path>/<file_name>\n   ```\n\n   Specifies the path and file name for your SSH private key, such as `~/.ssh/id_ed25519`\n\n   ```terminal {title=\"Example output\"}\n   Identity added: /home/<you>/<path>/<file_name> (<computer_name>)\n   ```\n\n**Next steps**\n\n- When you install OpenShift Container Platform, provide the SSH public key to the installation program.\n\n## Obtaining the installation program {#installation-obtaining-installer_installing-azure-preparing-ipi}\n\nBefore you install OpenShift Container Platform, download the installation file on\n\nthe host you are using for installation.\n\n**Prerequisites**\n\n- You have a computer that runs Linux or macOS, with 500 MB of local disk space.\n\n**Procedure**\n\n1. Go to the [Cluster Type](https://console.redhat.com/openshift/install) page on the Red Hat Hybrid Cloud Console. If you have a Red Hat account, log in with your credentials. If you do not, create an account.\n\n   > [!TIP]\n   > You can also [download the binaries for a specific OpenShift Container Platform release](https://mirror.openshift.com/pub/openshift-v4/clients/ocp/).\n2. Select your infrastructure provider from the **Run it yourself** section of the page.\n3. Select your host operating system and architecture from the dropdown menus under **OpenShift Installer** and click **Download Installer**.\n4. Place the downloaded file in the directory where you want to store the installation configuration files.\n\n   > [!IMPORTANT]\n   > - The installation program creates several files on the computer that you use to install your cluster. You must keep the installation program and the files that the installation program creates after you finish installing the cluster. Both of the files are required to delete the cluster.\n   > - Deleting the files created by the installation program does not remove your cluster, even if the cluster failed during installation. To remove your cluster, complete the OpenShift Container Platform uninstallation procedures for your specific cloud provider.\n5. Extract the installation program. For example, on a computer that uses a Linux operating system, run the following command:\n\n   ```terminal\n   $ tar -xvf openshift-install-linux.tar.gz\n   ```\n6. Download your installation [pull secret from Red Hat OpenShift Cluster Manager](https://console.redhat.com/openshift/install/pull-secret). This pull secret allows you to authenticate with the services that are provided by the included authorities, including Quay.io, which serves the container images for OpenShift Container Platform components.\n\n   ```\n   :::tip\n\n   Alternatively, you can retrieve the installation program from the [Red&#160;Hat Customer Portal](https://access.redhat.com/downloads/content/290/), where you can specify a version of the installation program to download.\n   However, you must have an active subscription to access this page.\n\n   :::\n   ```\n\n## Installing the OpenShift CLI on Linux {#cli-installing-cli-linux_installing-azure-preparing-ipi}\n\nTo manage your cluster and deploy applications from the command line on Linux, install the OpenShift CLI (`oc`) binary. You can download the OpenShift CLI (`oc`) from the Red  Customer Portal.\n\n> [!IMPORTANT]\n> If you installed an earlier version of `oc`, you cannot use it to complete all of the commands in OpenShift Container Platform.\n>\n> Download and install the new version of `oc`.\n\n**Procedure**\n\n1. Navigate to the [Download OpenShift Container Platform](https://access.redhat.com/downloads/content/290) page on the Red Hat Customer Portal.\n2. Select the architecture from the **Product Variant** list.\n3. Select the appropriate version from the **Version** list.\n4. Click **Download Now** next to the **OpenShift v4.22 Linux Clients** entry and save the file.\n5. Unpack the archive:\n\n   ```terminal\n   $ tar xvf <file>\n   ```\n6. Place the `oc` binary in a directory that is on your `PATH`.\n\n   To check your `PATH`, execute the following command:\n\n   ```terminal\n   $ echo $PATH\n   ```\n\n**Verification**\n\n- After you install the OpenShift CLI, it is available using the `oc` command:\n\n  ```terminal\n  $ oc <command>\n  ```\n\n## Installing the OpenShift CLI on Windows {#cli-installing-cli-windows_installing-azure-preparing-ipi}\n\nTo manage your cluster and deploy applications from the command line on Windows, install the OpenShift CLI (`oc`) binary. You can download the OpenShift CLI (`oc`) from the Red  Customer Portal.\n\n> [!IMPORTANT]\n> If you installed an earlier version of `oc`, you cannot use it to complete all of the commands in OpenShift Container Platform.\n>\n> Download and install the new version of `oc`.\n\n**Procedure**\n\n1. Navigate to the [Download OpenShift Container Platform](https://access.redhat.com/downloads/content/290) page on the Red Hat Customer Portal.\n2. Select the appropriate version from the **Version** list.\n3. Click **Download Now** next to the **OpenShift v4.22 Windows Client** entry and save the file.\n4. Extract the archive with a ZIP program.\n5. Move the `oc` binary to a directory that is on your `PATH` variable.\n\n   To check your `PATH` variable, open the command prompt and execute the following command:\n\n   ```terminal\n   C:\\> path\n   ```\n\n**Verification**\n\n- After you install the OpenShift CLI, it is available using the `oc` command:\n\n  ```terminal\n  C:\\> oc <command>\n  ```\n\n## Installing the OpenShift CLI on macOS {#cli-installing-cli-macos_installing-azure-preparing-ipi}\n\nTo manage your cluster and deploy applications from the command line on macOS, install the OpenShift CLI (`oc`) binary. You can download the OpenShift CLI (`oc`) from the Red  Customer Portal.\n\n> [!IMPORTANT]\n> If you installed an earlier version of `oc`, you cannot use it to complete all of the commands in OpenShift Container Platform.\n>\n> Download and install the new version of `oc`.\n\n**Procedure**\n\n1. Navigate to the [Download OpenShift Container Platform](https://access.redhat.com/downloads/content/290) page on the Red Hat Customer Portal.\n2. Select the architecture from the **Product Variant** list.\n3. Select the appropriate version from the **Version** list.\n4. Click **Download Now** next to the **OpenShift v4.22 macOS Clients** entry and save the file.\n\n   > [!NOTE]\n   > For macOS arm64, choose the **OpenShift v4.22 macOS arm64 Client** entry.\n5. Unpack and unzip the archive.\n6. Move the `oc` binary to a directory on your `PATH` variable.\n\n   To check your `PATH` variable, open a terminal and execute the following command:\n\n   ```terminal\n   $ echo $PATH\n   ```\n\n**Verification**\n\n- Verify your installation by using an `oc` command:\n\n  ```terminal\n  $ oc <command>\n  ```\n\n## Telemetry access for OpenShift Container Platform {#cluster-telemetry_installing-azure-preparing-ipi}\n\nTo provide metrics about cluster health and the success of updates, the Telemetry service requires internet access. When connected, this service runs automatically by default and registers your cluster to [OpenShift Cluster Manager](https://console.redhat.com/openshift).\n\nAfter you confirm that your [OpenShift Cluster Manager](https://console.redhat.com/openshift) inventory is correct, either maintained automatically by Telemetry or manually by using OpenShift Cluster Manager,use subscription watch to track your OpenShift Container Platform subscriptions at the account or multi-cluster level. For more information about subscription watch, see \"Data Gathered and Used by Red Hat’s subscription services\" in the *Additional resources* section.\n\n**Additional resources**\n\n- [About remote health monitoring](/openshift-docs-markdown/support/remote_health_monitoring/about-remote-health-monitoring#about-remote-health-monitoring)\n\n## Preparing an Azure Disk Encryption Set for Day2 Operator {#installation-azure-day2-operations-diskencryptionsets.adoc_installing-azure-preparing-ipi}\n\nThe OpenShift Container Platform installation program can use an existing Disk Encryption Set with a user-managed key. To enable this feature, create a `DiskEncryptionSet` object in Azure and provide the key to the installation program.\n\n**Prerequisite**\n\n- You enabled the `EncryptionAtHost` feature in your Azure subscription. For more information, see \"Use the Azure portal to enable end-to-end encryption using encryption at host\". .Procedure\n\n  1. Mark the node from the `encyptionAtHost` cluster resource group as unschedulable by using the following command:\n\n     ```terminal\n     $ oc adm cordon <node_name>\n     ```\n  2. Evacuate the pods from the compute node. There are several ways to do this. For example, you can evacuate all the pods or the selected pods on a node:\n\n     ```terminal\n     $ oc adm drain <compute_node> [--pod-selector=<pod_selector>]\n     ```\n\n     > [!NOTE]\n     > For other options to evacuate pods from a node, see the \"Understanding how to evacuate pods on nodes\" section.\n  3. De-allocate the node by running the following command:\n\n     ```terminal\n     $ az vm deallocate -n <node_name> -g <cluster_resource_group>\n     ```\n  4. Set the `encryptionAtHost` property to `true` by running the following command:\n\n     ```terminal\n     $ az vm update -n <node_name> -g <cluster_resource_group> --set securityProfile.encryptionAtHost=true\n     ```\n  5. Start the node by running the following commands:\n\n     ```terminal\n     $ az vm start -n <node_name> -g <cluster_resource_group>\n     ```\n  6. Mark the node as schedulable by using the following command:\n\n     ```terminal\n     $ oc adm uncordon <node_name>\n     ```\n  7. Verify that all cluster Operators are available:\n\n     ```terminal\n     $ oc get clusteroperators\n     ```\n\n     All Operators should show `AVAILABLE=True`, `PROGRESSING=False`, and `DEGRADED=False`.\n  8. Repeat the above steps on all the nodes that run `encryptionAtHost`.\n\n> [!NOTE]\n> If you want to enable encryption for your host during cluster installation, specify the following parameters in the `install-config.yaml` file:\n>\n> - `compute.platform.azure.encryptionAtHost`\n> - `controlPlane.platform.azure.encryptionAtHost`\n> - `platform.azure.defaultMachinePlatform.encryptionAtHost`\n\n## Preparing an Azure Disk Encryption Set {#preparing-disk-encryption-sets_installing-azure-preparing-ipi}\n\nTo use an existing Disk Encryption Set with a user-managed key in OpenShift Container Platform on Azure, you must create a Disk Encryption Set in Azure and provide the key to the installation program.\n\n**Procedure**\n\n1. Set the environment variables for the Azure resource group by running the following command:\n\n   ```terminal\n   $ export RESOURCEGROUP=\"<resource_group>\" \\\n       LOCATION=\"<location>\"\n   ```\n\n   where:\n\n   `<resource_group>`\n   :   Specifies the name of the Azure resource group where you create the Disk Encryption Set and encryption key. To prevent losing access to your keys when you delete the cluster, create the Disk Encryption Set in a separate resource group from the one where you install the cluster.\n\n   `<location>`\n   :   Specifies the Azure location where you create the resource group.\n2. Set the environment variables for the Azure Key Vault and Disk Encryption Set by running the following command:\n\n   ```terminal\n   $ export KEYVAULT_NAME=\"<keyvault_name>\" \\\n       KEYVAULT_KEY_NAME=\"<keyvault_key_name>\" \\\n       DISK_ENCRYPTION_SET_NAME=\"<disk_encryption_set_name>\"\n   ```\n\n   where:\n\n   `<keyvault_name>`\n   :   Specifies the name of the Azure Key Vault that you create.\n\n   `<keyvault_key_name>`\n   :   Specifies the name of the encryption key that you create.\n\n   `<disk_encryption_set_name>`\n   :   Specifies the name of the disk encryption set that you create.\n3. Set the environment variable for the ID of your Azure service principal by running the following command:\n\n   ```terminal\n   $ export CLUSTER_SP_ID=\"<service_principal_id>\"\n   ```\n\n   `<service_principal_id>`\n   :   Specifies the ID of the service principal that you use for installation.\n4. Enable host-level encryption in Azure by running the following command:\n\n   ```terminal\n   $ az feature register --namespace \"Microsoft.Compute\" --name \"EncryptionAtHost\"\n   ```\n\n   ```terminal\n   $ az feature show --namespace Microsoft.Compute --name EncryptionAtHost\n   ```\n\n   ```terminal\n   $ az provider register -n Microsoft.Compute\n   ```\n5. Create an Azure resource group to hold the disk encryption set and associated resources by running the following command:\n\n   ```terminal\n   $ az group create --name $RESOURCEGROUP --location $LOCATION\n   ```\n6. Create an Azure Key Vault by running the following command:\n\n   ```terminal\n   $ az keyvault create -n $KEYVAULT_NAME -g $RESOURCEGROUP -l $LOCATION \\\n       --enable-purge-protection true\n   ```\n7. Create an encryption key in the key vault by running the following command:\n\n   ```terminal\n   $ az keyvault key create --vault-name $KEYVAULT_NAME -n $KEYVAULT_KEY_NAME \\\n       --protection software\n   ```\n8. Capture the ID of the key vault by running the following command:\n\n   ```terminal\n   $ KEYVAULT_ID=$(az keyvault show --name $KEYVAULT_NAME --query \"[id]\" -o tsv)\n   ```\n9. Capture the key URL in the key vault by running the following command:\n\n   ```terminal\n   $ KEYVAULT_KEY_URL=$(az keyvault key show --vault-name $KEYVAULT_NAME --name \\\n       $KEYVAULT_KEY_NAME --query \"[key.kid]\" -o tsv)\n   ```\n10. Create a disk encryption set by running the following command:\n\n    ```terminal\n    $ az disk-encryption-set create -n $DISK_ENCRYPTION_SET_NAME -l $LOCATION -g \\\n        $RESOURCEGROUP --source-vault $KEYVAULT_ID --key-url $KEYVAULT_KEY_URL\n    ```\n11. Grant the `DiskEncryptionSet` resource access to the key vault by running the following commands:\n\n    ```terminal\n    $ DES_IDENTITY=$(az disk-encryption-set show -n $DISK_ENCRYPTION_SET_NAME -g \\\n        $RESOURCEGROUP --query \"[identity.principalId]\" -o tsv)\n    ```\n\n    ```terminal\n    $ az keyvault set-policy -n $KEYVAULT_NAME -g $RESOURCEGROUP --object-id \\\n        $DES_IDENTITY --key-permissions wrapkey unwrapkey get\n    ```\n12. Grant the Azure service principal permission to read the Disk Encryption Set by running the following commands:\n\n    ```terminal\n    $ DES_RESOURCE_ID=$(az disk-encryption-set show -n $DISK_ENCRYPTION_SET_NAME -g \\\n        $RESOURCEGROUP --query \"[id]\" -o tsv)\n    ```\n\n    ```terminal\n    $ az role assignment create --assignee $CLUSTER_SP_ID --role \"<reader_role>\" \\\n        --scope $DES_RESOURCE_ID -o jsonc\n    ```\n\n    `<reader_role>`\n    :   Specifies an Azure role with read permissions to the disk encryption set. You can use the `Owner` role or a custom role with the necessary permissions.\n\n**Additional resources**\n\n- [Install a cluster with customizations on installer-provisioned infrastructure](/openshift-docs-markdown/installing/installing_azure/ipi/installing-azure-customizations#installing-azure-customizations)\n- [Install a cluster into an existing VNet on installer-provisioned infrastructure](/openshift-docs-markdown/installing/installing_azure/ipi/installing-azure-vnet#installing-azure-vnet)\n- [Install a private cluster on installer-provisioned infrastructure](/openshift-docs-markdown/installing/installing_azure/ipi/installing-azure-private#installing-azure-private)\n- [Install a cluster into an government region on installer-provisioned infrastructure](/openshift-docs-markdown/installing/installing_azure/ipi/installing-azure-government-region#installing-azure-government-region)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/installing/installing_azure/ipi/installing-azure-preparing-ipi.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="installing-azure-preparing-ipi" class="group scroll-mt-20">Preparing to install a cluster on Azure<a href="#installing-azure-preparing-ipi" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Before you install an OpenShift Container Platform cluster on Azure with installer-provisioned infrastructure, you must configure your account, obtain the installation program and <code>oc</code> CLI, and optionally prepare disk encryption sets.</p>
      <h2 id="prerequisites-installing-azure_installing-azure-preparing-ipi" class="group scroll-mt-20">Prerequisites for installing a cluster on Azure<a href="#prerequisites-installing-azure_installing-azure-preparing-ipi" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Before you install an OpenShift Container Platform cluster on Azure, ensure that you have selected a cluster installation method and configured an Azure account to host the cluster and determine the tested and validated region to deploy the cluster to.</p>
      <p>If you use a firewall, you must configure it to allow the sites that your cluster requires access to.</p>
      <p><strong>Additional resources</strong></p>
      <ul>
      <li><a href="/openshift-docs-markdown/installing/overview/installing-preparing#installing-preparing">Select a cluster installation method</a></li>
      <li><a href="/openshift-docs-markdown/installing/installing_azure/installing-azure-account#installing-azure-account">Configure an Azure account</a></li>
      <li><a href="/openshift-docs-markdown/installing/install_config/configuring-firewall#configuring-firewall-module_configuring-firewall">Configure your firewall</a></li>
      </ul>
      <h2 id="cluster-entitlements_installing-azure-preparing-ipi" class="group scroll-mt-20">Internet access for OpenShift Container Platform<a href="#cluster-entitlements_installing-azure-preparing-ipi" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>In OpenShift Container Platform 4.22, you require access to the internet to install</p>
      <p class="_abstract">your cluster.</p>
      <p>You must have internet access to perform the following actions:</p>
      <ul>
      <li>Access Red Hat Hybrid Cloud Console to download the installation program and perform subscription management. If the cluster has internet access and you do not disable Telemetry, that service automatically entitles your cluster.</li>
      <li>Access Quay.io to obtain the packages that are required to install your cluster.</li>
      <li>Obtain the packages that are required to perform cluster updates.</li>
      </ul>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>If your cluster cannot have direct internet access, you can perform a restricted network installation on some types of infrastructure that you provision. During that process, you download the required content and use it to populate a mirror registry with the installation packages. With some installation types, the environment that you install your cluster in will not require internet access. Before you update the cluster, you update the content of the mirror registry.</p>
        </AlertDescription>
      </Alert>
      <h2 id="ssh-agent-using_installing-azure-preparing-ipi" class="group scroll-mt-20">Generating a key pair for cluster node SSH access<a href="#ssh-agent-using_installing-azure-preparing-ipi" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">During an OpenShift Container Platform installation, you can provide an SSH public key to the installation program. The key is passed to the Red Hat Enterprise Linux CoreOS (RHCOS) nodes through their Ignition config files and is used to authenticate SSH access to the nodes. The key is added to the <code>~/.ssh/authorized_keys</code> list for the <code>core</code> user on each node, which enables password-less authentication.</p>
      <p>The key is added to the <code>~/.ssh/authorized_keys</code> list for the <code>core</code> user on each node, which enables password-less authentication. After the key is passed to the nodes, you can use the key pair to SSH in to the RHCOS nodes as the user <code>core</code>. To access the nodes through SSH, the private key identity must be managed by SSH for your local user.</p>
      <p>If you want to SSH in to your cluster nodes to perform installation debugging or disaster recovery, you must provide the SSH public key during the installation process. The <code>./openshift-install gather</code> command also requires the SSH public key to be in place on the cluster nodes.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Do not skip this procedure in production environments, where disaster recovery and debugging is required.</p>
        </AlertDescription>
      </Alert>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>You must use a local key, not one that you configured with platform-specific approaches.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      If you do not have an existing SSH key pair on your local machine to use for authentication onto your cluster nodes, create one. For example, on a computer that uses a Linux operating system, run the following command:
      <CodeBlock code={"$ ssh-keygen -t ed25519 -N '' -f <path>/<file_name>"} lang="terminal" />
      <p>Specifies the path and file name, such as <code>~/.ssh/id_ed25519</code>, of the new SSH key. If you have an existing key pair, ensure your public key is in the your <code>~/.ssh</code> directory.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If you plan to install an OpenShift Container Platform cluster that uses the RHEL cryptographic libraries that have been submitted to NIST for FIPS 140-2/140-3 Validation on only the <code>x86_64</code>, <code>ppc64le</code>, and <code>s390x</code> architectures, do not create a key that uses the <code>ed25519</code> algorithm. Instead, create a key that uses the <code>rsa</code> or <code>ecdsa</code> algorithm.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      View the public SSH key:
      <CodeBlock code={"$ cat <path>/<file_name>.pub"} lang="terminal" />
      <p>For example, run the following to view the <code>~/.ssh/id_ed25519.pub</code> public key:</p>
      <CodeBlock code={"$ cat ~/.ssh/id_ed25519.pub"} lang="terminal" />
      </li>
      <li>
      Add the SSH private key identity to the SSH agent for your local user, if it has not already been added. SSH agent management of the key is required for password-less SSH authentication onto your cluster nodes, or if you want to use the <code>./openshift-install gather</code> command.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>On some distributions, default SSH private key identities such as <code>~/.ssh/id_rsa</code> and <code>~/.ssh/id_dsa</code> are managed automatically.</p>
        </AlertDescription>
      </Alert>
      <ol>
      <li>
      If the <code>ssh-agent</code> process is not already running for your local user, start it as a background task:
      <CodeBlock code={"$ eval \"$(ssh-agent -s)\""} lang="terminal" />
      <CodeBlock code={"Agent pid 31874"} lang="terminal" title="Example output" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If your cluster is in FIPS mode, only use FIPS-compliant algorithms to generate the SSH key. The key must be either RSA or ECDSA.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      </li>
      <li>
      Add your SSH private key to the <code>ssh-agent</code>:
      <CodeBlock code={"$ ssh-add <path>/<file_name>"} lang="terminal" />
      <p>Specifies the path and file name for your SSH private key, such as <code>~/.ssh/id_ed25519</code></p>
      <CodeBlock code={"Identity added: /home/<you>/<path>/<file_name> (<computer_name>)"} lang="terminal" title="Example output" />
      </li>
      </ol>
      <p><strong>Next steps</strong></p>
      <ul><li>When you install OpenShift Container Platform, provide the SSH public key to the installation program.</li></ul>
      <h2 id="installation-obtaining-installer_installing-azure-preparing-ipi" class="group scroll-mt-20">Obtaining the installation program<a href="#installation-obtaining-installer_installing-azure-preparing-ipi" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Before you install OpenShift Container Platform, download the installation file on</p>
      <p>the host you are using for installation.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have a computer that runs Linux or macOS, with 500 MB of local disk space.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Go to the <a href="https://console.redhat.com/openshift/install">Cluster Type</a> page on the Red Hat Hybrid Cloud Console. If you have a Red Hat account, log in with your credentials. If you do not, create an account.
      <Alert variant="tip" class="my-4">
        <AlertTitle>Tip</AlertTitle>
        <AlertDescription>
          <p>You can also <a href="https://mirror.openshift.com/pub/openshift-v4/clients/ocp/">download the binaries for a specific OpenShift Container Platform release</a>.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>Select your infrastructure provider from the <strong>Run it yourself</strong> section of the page.</li>
      <li>Select your host operating system and architecture from the dropdown menus under <strong>OpenShift Installer</strong> and click <strong>Download Installer</strong>.</li>
      <li>
      Place the downloaded file in the directory where you want to store the installation configuration files.
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <ul>
          <li>The installation program creates several files on the computer that you use to install your cluster. You must keep the installation program and the files that the installation program creates after you finish installing the cluster. Both of the files are required to delete the cluster.</li>
          <li>Deleting the files created by the installation program does not remove your cluster, even if the cluster failed during installation. To remove your cluster, complete the OpenShift Container Platform uninstallation procedures for your specific cloud provider.</li>
          </ul>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      Extract the installation program. For example, on a computer that uses a Linux operating system, run the following command:
      <CodeBlock code={"$ tar -xvf openshift-install-linux.tar.gz"} lang="terminal" />
      </li>
      <li>
      Download your installation <a href="https://console.redhat.com/openshift/install/pull-secret">pull secret from Red Hat OpenShift Cluster Manager</a>. This pull secret allows you to authenticate with the services that are provided by the included authorities, including Quay.io, which serves the container images for OpenShift Container Platform components.
      <CodeBlock code={":::tip\n\nAlternatively, you can retrieve the installation program from the [Red&#160;Hat Customer Portal](https://access.redhat.com/downloads/content/290/), where you can specify a version of the installation program to download.\nHowever, you must have an active subscription to access this page.\n\n:::"} lang="plaintext" />
      </li>
      </ol>
      <h2 id="cli-installing-cli-linux_installing-azure-preparing-ipi" class="group scroll-mt-20">Installing the OpenShift CLI on Linux<a href="#cli-installing-cli-linux_installing-azure-preparing-ipi" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To manage your cluster and deploy applications from the command line on Linux, install the OpenShift CLI (<code>oc</code>) binary. You can download the OpenShift CLI (<code>oc</code>) from the Red  Customer Portal.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>If you installed an earlier version of <code>oc</code>, you cannot use it to complete all of the commands in OpenShift Container Platform.</p>
          <p>Download and install the new version of <code>oc</code>.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to the <a href="https://access.redhat.com/downloads/content/290">Download OpenShift Container Platform</a> page on the Red Hat Customer Portal.</li>
      <li>Select the architecture from the <strong>Product Variant</strong> list.</li>
      <li>Select the appropriate version from the <strong>Version</strong> list.</li>
      <li>Click <strong>Download Now</strong> next to the <strong>OpenShift v4.22 Linux Clients</strong> entry and save the file.</li>
      <li>
      Unpack the archive:
      <CodeBlock code={"$ tar xvf <file>"} lang="terminal" />
      </li>
      <li>
      Place the <code>oc</code> binary in a directory that is on your <code>PATH</code>.
      <p>To check your <code>PATH</code>, execute the following command:</p>
      <CodeBlock code={"$ echo $PATH"} lang="terminal" />
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      After you install the OpenShift CLI, it is available using the <code>oc</code> command:
      <CodeBlock code={"$ oc <command>"} lang="terminal" />
      </li>
      </ul>
      <h2 id="cli-installing-cli-windows_installing-azure-preparing-ipi" class="group scroll-mt-20">Installing the OpenShift CLI on Windows<a href="#cli-installing-cli-windows_installing-azure-preparing-ipi" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To manage your cluster and deploy applications from the command line on Windows, install the OpenShift CLI (<code>oc</code>) binary. You can download the OpenShift CLI (<code>oc</code>) from the Red  Customer Portal.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>If you installed an earlier version of <code>oc</code>, you cannot use it to complete all of the commands in OpenShift Container Platform.</p>
          <p>Download and install the new version of <code>oc</code>.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to the <a href="https://access.redhat.com/downloads/content/290">Download OpenShift Container Platform</a> page on the Red Hat Customer Portal.</li>
      <li>Select the appropriate version from the <strong>Version</strong> list.</li>
      <li>Click <strong>Download Now</strong> next to the <strong>OpenShift v4.22 Windows Client</strong> entry and save the file.</li>
      <li>Extract the archive with a ZIP program.</li>
      <li>
      Move the <code>oc</code> binary to a directory that is on your <code>PATH</code> variable.
      <p>To check your <code>PATH</code> variable, open the command prompt and execute the following command:</p>
      <CodeBlock code={"C:\\> path"} lang="terminal" />
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      After you install the OpenShift CLI, it is available using the <code>oc</code> command:
      <CodeBlock code={"C:\\> oc <command>"} lang="terminal" />
      </li>
      </ul>
      <h2 id="cli-installing-cli-macos_installing-azure-preparing-ipi" class="group scroll-mt-20">Installing the OpenShift CLI on macOS<a href="#cli-installing-cli-macos_installing-azure-preparing-ipi" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To manage your cluster and deploy applications from the command line on macOS, install the OpenShift CLI (<code>oc</code>) binary. You can download the OpenShift CLI (<code>oc</code>) from the Red  Customer Portal.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>If you installed an earlier version of <code>oc</code>, you cannot use it to complete all of the commands in OpenShift Container Platform.</p>
          <p>Download and install the new version of <code>oc</code>.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to the <a href="https://access.redhat.com/downloads/content/290">Download OpenShift Container Platform</a> page on the Red Hat Customer Portal.</li>
      <li>Select the architecture from the <strong>Product Variant</strong> list.</li>
      <li>Select the appropriate version from the <strong>Version</strong> list.</li>
      <li>
      Click <strong>Download Now</strong> next to the <strong>OpenShift v4.22 macOS Clients</strong> entry and save the file.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>For macOS arm64, choose the <strong>OpenShift v4.22 macOS arm64 Client</strong> entry.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>Unpack and unzip the archive.</li>
      <li>
      Move the <code>oc</code> binary to a directory on your <code>PATH</code> variable.
      <p>To check your <code>PATH</code> variable, open a terminal and execute the following command:</p>
      <CodeBlock code={"$ echo $PATH"} lang="terminal" />
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      Verify your installation by using an <code>oc</code> command:
      <CodeBlock code={"$ oc <command>"} lang="terminal" />
      </li>
      </ul>
      <h2 id="cluster-telemetry_installing-azure-preparing-ipi" class="group scroll-mt-20">Telemetry access for OpenShift Container Platform<a href="#cluster-telemetry_installing-azure-preparing-ipi" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To provide metrics about cluster health and the success of updates, the Telemetry service requires internet access. When connected, this service runs automatically by default and registers your cluster to <a href="https://console.redhat.com/openshift">OpenShift Cluster Manager</a>.</p>
      <p>After you confirm that your <a href="https://console.redhat.com/openshift">OpenShift Cluster Manager</a> inventory is correct, either maintained automatically by Telemetry or manually by using OpenShift Cluster Manager,use subscription watch to track your OpenShift Container Platform subscriptions at the account or multi-cluster level. For more information about subscription watch, see "Data Gathered and Used by Red Hat’s subscription services" in the <em>Additional resources</em> section.</p>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/support/remote_health_monitoring/about-remote-health-monitoring#about-remote-health-monitoring">About remote health monitoring</a></li></ul>
      <h2 id="installation-azure-day2-operations-diskencryptionsets.adoc_installing-azure-preparing-ipi" class="group scroll-mt-20">Preparing an Azure Disk Encryption Set for Day2 Operator<a href="#installation-azure-day2-operations-diskencryptionsets.adoc_installing-azure-preparing-ipi" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>The OpenShift Container Platform installation program can use an existing Disk Encryption Set with a user-managed key. To enable this feature, create a <code>DiskEncryptionSet</code> object in Azure and provide the key to the installation program.</p>
      <p><strong>Prerequisite</strong></p>
      <ul>
      <li>
      You enabled the <code>EncryptionAtHost</code> feature in your Azure subscription. For more information, see "Use the Azure portal to enable end-to-end encryption using encryption at host". .Procedure
      <ol>
      <li>
      Mark the node from the <code>encyptionAtHost</code> cluster resource group as unschedulable by using the following command:
      <CodeBlock code={"$ oc adm cordon <node_name>"} lang="terminal" />
      </li>
      <li>
      Evacuate the pods from the compute node. There are several ways to do this. For example, you can evacuate all the pods or the selected pods on a node:
      <CodeBlock code={"$ oc adm drain <compute_node> [--pod-selector=<pod_selector>]"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>For other options to evacuate pods from a node, see the "Understanding how to evacuate pods on nodes" section.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      De-allocate the node by running the following command:
      <CodeBlock code={"$ az vm deallocate -n <node_name> -g <cluster_resource_group>"} lang="terminal" />
      </li>
      <li>
      Set the <code>encryptionAtHost</code> property to <code>true</code> by running the following command:
      <CodeBlock code={"$ az vm update -n <node_name> -g <cluster_resource_group> --set securityProfile.encryptionAtHost=true"} lang="terminal" />
      </li>
      <li>
      Start the node by running the following commands:
      <CodeBlock code={"$ az vm start -n <node_name> -g <cluster_resource_group>"} lang="terminal" />
      </li>
      <li>
      Mark the node as schedulable by using the following command:
      <CodeBlock code={"$ oc adm uncordon <node_name>"} lang="terminal" />
      </li>
      <li>
      Verify that all cluster Operators are available:
      <CodeBlock code={"$ oc get clusteroperators"} lang="terminal" />
      <p>All Operators should show <code>AVAILABLE=True</code>, <code>PROGRESSING=False</code>, and <code>DEGRADED=False</code>.</p>
      </li>
      <li>Repeat the above steps on all the nodes that run <code>encryptionAtHost</code>.</li>
      </ol>
      </li>
      </ul>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If you want to enable encryption for your host during cluster installation, specify the following parameters in the <code>install-config.yaml</code> file:</p>
          <ul>
          <li><code>compute.platform.azure.encryptionAtHost</code></li>
          <li><code>controlPlane.platform.azure.encryptionAtHost</code></li>
          <li><code>platform.azure.defaultMachinePlatform.encryptionAtHost</code></li>
          </ul>
        </AlertDescription>
      </Alert>
      <h2 id="preparing-disk-encryption-sets_installing-azure-preparing-ipi" class="group scroll-mt-20">Preparing an Azure Disk Encryption Set<a href="#preparing-disk-encryption-sets_installing-azure-preparing-ipi" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To use an existing Disk Encryption Set with a user-managed key in OpenShift Container Platform on Azure, you must create a Disk Encryption Set in Azure and provide the key to the installation program.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Set the environment variables for the Azure resource group by running the following command:
      <CodeBlock code={"$ export RESOURCEGROUP=\"<resource_group>\" \\\n    LOCATION=\"<location>\""} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;resource_group&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the Azure resource group where you create the Disk Encryption Set and encryption key. To prevent losing access to your keys when you delete the cluster, create the Disk Encryption Set in a separate resource group from the one where you install the cluster.</p></dd>
      <dt class="font-semibold"><code>&lt;location&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the Azure location where you create the resource group.</p></dd>
      </dl>
      </li>
      <li>
      Set the environment variables for the Azure Key Vault and Disk Encryption Set by running the following command:
      <CodeBlock code={"$ export KEYVAULT_NAME=\"<keyvault_name>\" \\\n    KEYVAULT_KEY_NAME=\"<keyvault_key_name>\" \\\n    DISK_ENCRYPTION_SET_NAME=\"<disk_encryption_set_name>\""} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;keyvault_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the Azure Key Vault that you create.</p></dd>
      <dt class="font-semibold"><code>&lt;keyvault_key_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the encryption key that you create.</p></dd>
      <dt class="font-semibold"><code>&lt;disk_encryption_set_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the disk encryption set that you create.</p></dd>
      </dl>
      </li>
      <li>
      Set the environment variable for the ID of your Azure service principal by running the following command:
      <CodeBlock code={"$ export CLUSTER_SP_ID=\"<service_principal_id>\""} lang="terminal" />
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;service_principal_id&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the ID of the service principal that you use for installation.</p></dd>
      </dl>
      </li>
      <li>
      Enable host-level encryption in Azure by running the following command:
      <CodeBlock code={"$ az feature register --namespace \"Microsoft.Compute\" --name \"EncryptionAtHost\""} lang="terminal" />
      <CodeBlock code={"$ az feature show --namespace Microsoft.Compute --name EncryptionAtHost"} lang="terminal" />
      <CodeBlock code={"$ az provider register -n Microsoft.Compute"} lang="terminal" />
      </li>
      <li>
      Create an Azure resource group to hold the disk encryption set and associated resources by running the following command:
      <CodeBlock code={"$ az group create --name $RESOURCEGROUP --location $LOCATION"} lang="terminal" />
      </li>
      <li>
      Create an Azure Key Vault by running the following command:
      <CodeBlock code={"$ az keyvault create -n $KEYVAULT_NAME -g $RESOURCEGROUP -l $LOCATION \\\n    --enable-purge-protection true"} lang="terminal" />
      </li>
      <li>
      Create an encryption key in the key vault by running the following command:
      <CodeBlock code={"$ az keyvault key create --vault-name $KEYVAULT_NAME -n $KEYVAULT_KEY_NAME \\\n    --protection software"} lang="terminal" />
      </li>
      <li>
      Capture the ID of the key vault by running the following command:
      <CodeBlock code={"$ KEYVAULT_ID=$(az keyvault show --name $KEYVAULT_NAME --query \"[id]\" -o tsv)"} lang="terminal" />
      </li>
      <li>
      Capture the key URL in the key vault by running the following command:
      <CodeBlock code={"$ KEYVAULT_KEY_URL=$(az keyvault key show --vault-name $KEYVAULT_NAME --name \\\n    $KEYVAULT_KEY_NAME --query \"[key.kid]\" -o tsv)"} lang="terminal" />
      </li>
      <li>
      Create a disk encryption set by running the following command:
      <CodeBlock code={"$ az disk-encryption-set create -n $DISK_ENCRYPTION_SET_NAME -l $LOCATION -g \\\n    $RESOURCEGROUP --source-vault $KEYVAULT_ID --key-url $KEYVAULT_KEY_URL"} lang="terminal" />
      </li>
      <li>
      Grant the <code>DiskEncryptionSet</code> resource access to the key vault by running the following commands:
      <CodeBlock code={"$ DES_IDENTITY=$(az disk-encryption-set show -n $DISK_ENCRYPTION_SET_NAME -g \\\n    $RESOURCEGROUP --query \"[identity.principalId]\" -o tsv)"} lang="terminal" />
      <CodeBlock code={"$ az keyvault set-policy -n $KEYVAULT_NAME -g $RESOURCEGROUP --object-id \\\n    $DES_IDENTITY --key-permissions wrapkey unwrapkey get"} lang="terminal" />
      </li>
      <li>
      Grant the Azure service principal permission to read the Disk Encryption Set by running the following commands:
      <CodeBlock code={"$ DES_RESOURCE_ID=$(az disk-encryption-set show -n $DISK_ENCRYPTION_SET_NAME -g \\\n    $RESOURCEGROUP --query \"[id]\" -o tsv)"} lang="terminal" />
      <CodeBlock code={"$ az role assignment create --assignee $CLUSTER_SP_ID --role \"<reader_role>\" \\\n    --scope $DES_RESOURCE_ID -o jsonc"} lang="terminal" />
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;reader_role&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies an Azure role with read permissions to the disk encryption set. You can use the <code>Owner</code> role or a custom role with the necessary permissions.</p></dd>
      </dl>
      </li>
      </ol>
      <p><strong>Additional resources</strong></p>
      <ul>
      <li><a href="/openshift-docs-markdown/installing/installing_azure/ipi/installing-azure-customizations#installing-azure-customizations">Install a cluster with customizations on installer-provisioned infrastructure</a></li>
      <li><a href="/openshift-docs-markdown/installing/installing_azure/ipi/installing-azure-vnet#installing-azure-vnet">Install a cluster into an existing VNet on installer-provisioned infrastructure</a></li>
      <li><a href="/openshift-docs-markdown/installing/installing_azure/ipi/installing-azure-private#installing-azure-private">Install a private cluster on installer-provisioned infrastructure</a></li>
      <li><a href="/openshift-docs-markdown/installing/installing_azure/ipi/installing-azure-government-region#installing-azure-government-region">Install a cluster into an government region on installer-provisioned infrastructure</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
