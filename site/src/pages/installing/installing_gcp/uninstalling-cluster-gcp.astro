---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: installing/installing_gcp/uninstalling-cluster-gcp.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"uninstalling-cluster-gcp","text":"Uninstalling a cluster on Google Cloud"},{"depth":2,"slug":"installation-uninstall-clouds_uninstalling-cluster-gcp","text":"Removing a cluster that uses installer-provisioned infrastructure"},{"depth":2,"slug":"cco-ccoctl-deleting-sts-resources_uninstalling-cluster-gcp","text":"Deleting Google Cloud resources with the Cloud Credential Operator utility"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/installing/installing_gcp/uninstalling-cluster-gcp";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Uninstalling a cluster on {{ gcp_short }}";
const description = "You can remove a cluster that you deployed to Google Cloud and delete the associated cloud provider resources when you no longer need the cluster, to free…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["installing","installing/installing_gcp"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Uninstalling a cluster on Google Cloud {#uninstalling-cluster-gcp}\n\nYou can remove a cluster that you deployed to Google Cloud and delete the associated cloud provider resources when you no longer need the cluster, to free up cloud resources and stop incurring costs.\n\n## Removing a cluster that uses installer-provisioned infrastructure {#installation-uninstall-clouds_uninstalling-cluster-gcp}\n\nTo remove an OpenShift Container Platform cluster that uses installer-provisioned infrastructure, you can use the installation program and the installation files from your original deployment to uninstall the cluster from your cloud platform.\n\n> [!NOTE]\n> After uninstallation, check your cloud provider for any resources that were not removed properly, especially with user-provisioned infrastructure clusters. Some resources might exist because either the installation program did not create the resource or could not access the resource. For example, some Google Cloud resources require [IAM permissions](https://cloud.google.com/iam/docs/overview#concepts_related_to_access_management) in shared VPC host projects, or there might be unused [health checks that must be deleted](https://cloud.google.com/sdk/gcloud/reference/compute/health-checks/delete).\n\n**Prerequisites**\n\n- You have a copy of the installation program that you used to deploy the cluster.\n- You have the files that the installation program generated when you created your cluster.\n\n**Procedure**\n\n1. From the directory that has the installation program on the computer that you used to install the cluster, run the following command:\n\n   ```terminal\n   $ ./openshift-install destroy cluster \\\n   --dir <installation_directory> --log-level info\n   ```\n\n   where:\n\n   `<installation_directory>`\n   :   Specify the path to the directory that you stored the installation files in.\n\n   `--log-level info`\n   :   To view different details, specify `warn`, `debug`, or `error` instead of `info`.\n\n   > [!NOTE]\n   > You must specify the directory that includes the cluster definition files for your cluster. The installation program requires the `metadata.json` file in this directory to delete the cluster.\n2. Optional: Delete the `<installation_directory>` directory and the OpenShift Container Platform installation program.\n\n## Deleting Google Cloud resources with the Cloud Credential Operator utility {#cco-ccoctl-deleting-sts-resources_uninstalling-cluster-gcp}\n\nAfter uninstalling an OpenShift Container Platform cluster that uses short-term credentials managed outside the cluster, you can use the Cloud Credential Operator (CCO) utility (`ccoctl`) to remove the Google Cloud resources that `ccoctl` created during installation.\n\n**Prerequisites**\n\n- Extract and prepare the `ccoctl` binary.\n- Uninstall an OpenShift Container Platform cluster on Google Cloud that uses short-term credentials.\n\n**Procedure**\n\n1. Set a `$RELEASE_IMAGE` variable with the release image from your installation file by running the following command:\n\n   ```terminal\n   $ RELEASE_IMAGE=$(./openshift-install version | awk '/release image/ {print $3}')\n   ```\n2. Extract the list of `CredentialsRequest` custom resources (CRs) from the OpenShift Container Platform release image by running the following command:\n\n   ```terminal\n   $ oc adm release extract \\\n     --from=$RELEASE_IMAGE \\\n     --credentials-requests \\\n     --included \\\n     --to=<path_to_directory_for_credentials_requests>\n   ```\n\n   where:\n\n   `--included`\n   :   The parameter includes only the manifests that your specific cluster configuration requires.\n\n   `<path_to_directory_for_credentials_requests>`\n   :   Specify the path to the directory where you want to store the `CredentialsRequest` objects. If the specified directory does not exist, this command creates it.\n3. Delete the Google Cloud resources that `ccoctl` created by running the following command:\n\n   ```terminal\n   $ ccoctl {{ cp_name }} delete \\\n     --name=<name> \\\n   ```\n\n{% if aws_sts %} --region=<{{ cp_name }}\\_region> {% endif %} {% if gcp_workload_id %} --project=<{{ cp_name }}\\_project_id>\n--credentials-requests-dir=<path_to_credentials_requests_directory>\n--force-delete-custom-roles {% endif %} {% if azure_workload_id %} --region=<{{ cp_name }}\\_region>\n--subscription-id=<{{ cp_name }}\\_subscription_id>\n--delete-oidc-resource-group {%- endif %} \\`\\`\\`\n\n```\n    where:\n\n    `<name>`\n    :   Matches the name that was originally used to create and tag the cloud resources.\n\n    `<{{ cp_name }}_project_id>`\n    :   The Google Cloud project ID in which to delete cloud resources.\n\n    `force-delete-custom-roles`\n    :   Optional: This parameter deletes the custom roles that the `ccoctl` utility creates during installation. Google Cloud does not permanently delete custom roles immediately. For more information, see Google Cloud documentation about [deleting a custom role](https://cloud.google.com/iam/docs/creating-custom-roles#deleting-custom-role).\n```\n\n**Verification**\n\n```\n*   To verify that the resources are deleted, query Google Cloud. For more information, refer to Google Cloud documentation.\n```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/installing/installing_gcp/uninstalling-cluster-gcp.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="uninstalling-cluster-gcp" class="group scroll-mt-20">Uninstalling a cluster on Google Cloud<a href="#uninstalling-cluster-gcp" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can remove a cluster that you deployed to Google Cloud and delete the associated cloud provider resources when you no longer need the cluster, to free up cloud resources and stop incurring costs.</p>
      <h2 id="installation-uninstall-clouds_uninstalling-cluster-gcp" class="group scroll-mt-20">Removing a cluster that uses installer-provisioned infrastructure<a href="#installation-uninstall-clouds_uninstalling-cluster-gcp" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To remove an OpenShift Container Platform cluster that uses installer-provisioned infrastructure, you can use the installation program and the installation files from your original deployment to uninstall the cluster from your cloud platform.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>After uninstallation, check your cloud provider for any resources that were not removed properly, especially with user-provisioned infrastructure clusters. Some resources might exist because either the installation program did not create the resource or could not access the resource. For example, some Google Cloud resources require <a href="https://cloud.google.com/iam/docs/overview#concepts_related_to_access_management">IAM permissions</a> in shared VPC host projects, or there might be unused <a href="https://cloud.google.com/sdk/gcloud/reference/compute/health-checks/delete">health checks that must be deleted</a>.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have a copy of the installation program that you used to deploy the cluster.</li>
      <li>You have the files that the installation program generated when you created your cluster.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      From the directory that has the installation program on the computer that you used to install the cluster, run the following command:
      <CodeBlock code={"$ ./openshift-install destroy cluster \\\n--dir <installation_directory> --log-level info"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;installation_directory&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specify the path to the directory that you stored the installation files in.</p></dd>
      <dt class="font-semibold"><code>--log-level info</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>To view different details, specify <code>warn</code>, <code>debug</code>, or <code>error</code> instead of <code>info</code>.</p></dd>
      </dl>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>You must specify the directory that includes the cluster definition files for your cluster. The installation program requires the <code>metadata.json</code> file in this directory to delete the cluster.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>Optional: Delete the <code>&lt;installation_directory&gt;</code> directory and the OpenShift Container Platform installation program.</li>
      </ol>
      <h2 id="cco-ccoctl-deleting-sts-resources_uninstalling-cluster-gcp" class="group scroll-mt-20">Deleting Google Cloud resources with the Cloud Credential Operator utility<a href="#cco-ccoctl-deleting-sts-resources_uninstalling-cluster-gcp" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">After uninstalling an OpenShift Container Platform cluster that uses short-term credentials managed outside the cluster, you can use the Cloud Credential Operator (CCO) utility (<code>ccoctl</code>) to remove the Google Cloud resources that <code>ccoctl</code> created during installation.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>Extract and prepare the <code>ccoctl</code> binary.</li>
      <li>Uninstall an OpenShift Container Platform cluster on Google Cloud that uses short-term credentials.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Set a <code>$RELEASE_IMAGE</code> variable with the release image from your installation file by running the following command:
      <CodeBlock code={"$ RELEASE_IMAGE=$(./openshift-install version | awk '/release image/ {print $3}')"} lang="terminal" />
      </li>
      <li>
      Extract the list of <code>CredentialsRequest</code> custom resources (CRs) from the OpenShift Container Platform release image by running the following command:
      <CodeBlock code={"$ oc adm release extract \\\n  --from=$RELEASE_IMAGE \\\n  --credentials-requests \\\n  --included \\\n  --to=<path_to_directory_for_credentials_requests>"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>--included</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The parameter includes only the manifests that your specific cluster configuration requires.</p></dd>
      <dt class="font-semibold"><code>&lt;path_to_directory_for_credentials_requests&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specify the path to the directory where you want to store the <code>CredentialsRequest</code> objects. If the specified directory does not exist, this command creates it.</p></dd>
      </dl>
      </li>
      <li>
      Delete the Google Cloud resources that <code>ccoctl</code> created by running the following command:
      <CodeBlock code={"$ ccoctl {{ cp_name }} delete \\\n  --name=<name> \\"} lang="terminal" />
      </li>
      </ol>
      <p>{"{"}% if aws_sts %{"}"} --region=&lt;{"{"}{"{"} cp_name {"}"}{"}"}_region&gt; {"{"}% endif %{"}"} {"{"}% if gcp_workload_id %{"}"} --project=&lt;{"{"}{"{"} cp_name {"}"}{"}"}_project_id&gt; <br />--credentials-requests-dir=&lt;path_to_credentials_requests_directory&gt; <br />--force-delete-custom-roles {"{"}% endif %{"}"} {"{"}% if azure_workload_id %{"}"} --region=&lt;{"{"}{"{"} cp_name {"}"}{"}"}_region&gt; <br />--subscription-id=&lt;{"{"}{"{"} cp_name {"}"}{"}"}_subscription_id&gt; <br />--delete-oidc-resource-group {"{"}%- endif %{"}"} ```</p>
      <CodeBlock code={"    where:\n\n    `<name>`\n    :   Matches the name that was originally used to create and tag the cloud resources.\n\n\n\n    `<{{ cp_name }}_project_id>`\n    :   The Google Cloud project ID in which to delete cloud resources.\n\n    `force-delete-custom-roles`\n    :   Optional: This parameter deletes the custom roles that the `ccoctl` utility creates during installation. Google Cloud does not permanently delete custom roles immediately. For more information, see Google Cloud documentation about [deleting a custom role](https://cloud.google.com/iam/docs/creating-custom-roles#deleting-custom-role)."} lang="plaintext" />
      <p><strong>Verification</strong></p>
      <CodeBlock code={"*   To verify that the resources are deleted, query Google Cloud. For more information, refer to Google Cloud documentation."} lang="plaintext" />
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
