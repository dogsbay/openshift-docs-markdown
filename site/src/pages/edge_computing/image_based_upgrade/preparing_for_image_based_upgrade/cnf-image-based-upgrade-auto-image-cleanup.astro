---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: edge_computing/image_based_upgrade/preparing_for_image_based_upgrade/cnf-image-based-upgrade-auto-image-cleanup.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"cnf-image-based-upgrade-configure-auto-image-cleanup","text":"Configuring the automatic image cleanup of the container storage disk"},{"depth":2,"slug":"ztp-image-based-upgrade-configure-threshold_auto-cleanup","text":"Configuring the automatic image cleanup of the container storage disk"},{"depth":2,"slug":"ztp-image-based-upgrade-disable-container-storage-image-cleanup_auto-cleanup","text":"Disable the automatic image cleanup of the container storage disk"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/edge_computing/image_based_upgrade/preparing_for_image_based_upgrade/cnf-image-based-upgrade-auto-image-cleanup";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring the automatic image cleanup of the container storage disk";
const description = "Configure when the Lifecycle Agent cleans up unpinned images in the Prep stage by setting a minimum threshold for available storage space through…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["edge_computing","edge_computing/image_based_upgrade","edge_computing/image_based_upgrade/preparing_for_image_based_upgrade"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring the automatic image cleanup of the container storage disk {#cnf-image-based-upgrade-configure-auto-image-cleanup}\n\nConfigure when the Lifecycle Agent cleans up unpinned images in the `Prep` stage by setting a minimum threshold for available storage space through annotations. The default container storage disk usage threshold is 50%.\n\nThe Lifecycle Agent does not delete images that are pinned in CRI-O or are currently used. The Operator selects the images for deletion by starting with dangling images and then sorting the images from oldest to newest that is determined by the image `Created` timestamp.\n\n## Configuring the automatic image cleanup of the container storage disk {#ztp-image-based-upgrade-configure-threshold_auto-cleanup}\n\nConfigure the minimum threshold for available storage space through annotations.\n\n**Prerequisites**\n\n- You have created an `ImageBasedUpgrade` CR.\n\n**Procedure**\n\n1. Increase the threshold to 65% by running the following command:\n\n   ```terminal\n   $ oc -n openshift-lifecycle-agent annotate ibu upgrade image-cleanup.lca.openshift.io/disk-usage-threshold-percent='65'\n   ```\n2. (Optional) Remove the threshold override by running the following command:\n\n   ```terminal\n   $ oc -n  openshift-lifecycle-agent annotate ibu upgrade image-cleanup.lca.openshift.io/disk-usage-threshold-percent-\n   ```\n\n## Disable the automatic image cleanup of the container storage disk {#ztp-image-based-upgrade-disable-container-storage-image-cleanup_auto-cleanup}\n\nDisable the automatic image cleanup threshold.\n\n**Procedure**\n\n1. Disable the automatic image cleanup by running the following command:\n\n   ```terminal\n   $ oc -n openshift-lifecycle-agent annotate ibu upgrade image-cleanup.lca.openshift.io/on-prep='Disabled'\n   ```\n2. (Optional) Enable automatic image cleanup again by running the following command:\n\n   ```terminal\n   $ oc -n  openshift-lifecycle-agent annotate ibu upgrade image-cleanup.lca.openshift.io/on-prep-\n   ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/edge_computing/image_based_upgrade/preparing_for_image_based_upgrade/cnf-image-based-upgrade-auto-image-cleanup.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="cnf-image-based-upgrade-configure-auto-image-cleanup" class="group scroll-mt-20">Configuring the automatic image cleanup of the container storage disk<a href="#cnf-image-based-upgrade-configure-auto-image-cleanup" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Configure when the Lifecycle Agent cleans up unpinned images in the <code>Prep</code> stage by setting a minimum threshold for available storage space through annotations. The default container storage disk usage threshold is 50%.</p>
      <p>The Lifecycle Agent does not delete images that are pinned in CRI-O or are currently used. The Operator selects the images for deletion by starting with dangling images and then sorting the images from oldest to newest that is determined by the image <code>Created</code> timestamp.</p>
      <h2 id="ztp-image-based-upgrade-configure-threshold_auto-cleanup" class="group scroll-mt-20">Configuring the automatic image cleanup of the container storage disk<a href="#ztp-image-based-upgrade-configure-threshold_auto-cleanup" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Configure the minimum threshold for available storage space through annotations.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have created an <code>ImageBasedUpgrade</code> CR.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Increase the threshold to 65% by running the following command:
      <CodeBlock code={"$ oc -n openshift-lifecycle-agent annotate ibu upgrade image-cleanup.lca.openshift.io/disk-usage-threshold-percent='65'"} lang="terminal" />
      </li>
      <li>
      (Optional) Remove the threshold override by running the following command:
      <CodeBlock code={"$ oc -n  openshift-lifecycle-agent annotate ibu upgrade image-cleanup.lca.openshift.io/disk-usage-threshold-percent-"} lang="terminal" />
      </li>
      </ol>
      <h2 id="ztp-image-based-upgrade-disable-container-storage-image-cleanup_auto-cleanup" class="group scroll-mt-20">Disable the automatic image cleanup of the container storage disk<a href="#ztp-image-based-upgrade-disable-container-storage-image-cleanup_auto-cleanup" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Disable the automatic image cleanup threshold.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Disable the automatic image cleanup by running the following command:
      <CodeBlock code={"$ oc -n openshift-lifecycle-agent annotate ibu upgrade image-cleanup.lca.openshift.io/on-prep='Disabled'"} lang="terminal" />
      </li>
      <li>
      (Optional) Enable automatic image cleanup again by running the following command:
      <CodeBlock code={"$ oc -n  openshift-lifecycle-agent annotate ibu upgrade image-cleanup.lca.openshift.io/on-prep-"} lang="terminal" />
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
