---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: edge_computing/ztp-deploying-far-edge-clusters-at-scale.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import BlockImage from "@ui/image/Image.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"ztp-deploying-far-edge-clusters-at-scale","text":"Challenges of the network far edge"},{"depth":2,"slug":"ztp-challenges-of-far-edge-deployments_ztp-deploying-far-edge-clusters-at-scale","text":"Overcoming the challenges of the network far edge"},{"depth":2,"slug":"about-ztp_ztp-deploying-far-edge-clusters-at-scale","text":"Using GitOps ZTP to provision clusters at the network far edge"},{"depth":2,"slug":"ztp-creating-ztp-crs-for-multiple-managed-clusters_ztp-deploying-far-edge-clusters-at-scale","text":"Installing managed clusters with ClusterInstance resources and RHACM"},{"depth":2,"slug":"ztp-configuring-cluster-policies_ztp-deploying-far-edge-clusters-at-scale","text":"Configuring managed clusters with policies and PolicyGenerator resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/edge_computing/ztp-deploying-far-edge-clusters-at-scale";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Challenges of the network far edge";
const description = "Edge computing presents complex challenges when managing many sites in geographically displaced locations. Use GitOps Zero Touch Provisioning (ZTP) to…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["edge_computing"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Challenges of the network far edge {#ztp-deploying-far-edge-clusters-at-scale}\n\nEdge computing presents complex challenges when managing many sites in geographically displaced locations. Use GitOps Zero Touch Provisioning (ZTP) to provision and manage sites at the far edge of the network.\n\n## Overcoming the challenges of the network far edge {#ztp-challenges-of-far-edge-deployments_ztp-deploying-far-edge-clusters-at-scale}\n\nToday, service providers want to deploy their infrastructure at the edge of the network. This presents significant challenges:\n\n- How do you handle deployments of many edge sites in parallel?\n- What happens when you need to deploy sites in disconnected environments?\n- How do you manage the lifecycle of large fleets of clusters?\n\nGitOps Zero Touch Provisioning (ZTP) and *GitOps* meets these challenges by allowing you to provision remote edge sites at scale with declarative site definitions and configurations for bare-metal equipment. Template or overlay configurations install OpenShift Container Platform features that are required for CNF workloads. The full lifecycle of installation and upgrades is handled through the GitOps ZTP pipeline.\n\nGitOps ZTP uses GitOps for infrastructure deployments. With GitOps, you use declarative YAML files and other defined patterns stored in Git repositories. Red Hat Advanced Cluster Management (RHACM) uses your Git repositories to drive the deployment of your infrastructure.\n\nGitOps provides traceability, role-based access control (RBAC), and a single source of truth for the desired state of each site. Scalability issues are addressed by Git methodologies and event driven operations through webhooks.\n\nYou start the GitOps ZTP workflow by creating declarative site definition and configuration custom resources (CRs) that the GitOps ZTP pipeline delivers to the edge nodes.\n\nThe following diagram shows how GitOps ZTP works within the far edge framework.\n\n![GitOps ZTP at the network far edge](/openshift-docs-markdown/_assets/images/217_OpenShift_Zero_Touch_Provisioning_updates_1022_1.png)\n\n## Using GitOps ZTP to provision clusters at the network far edge {#about-ztp_ztp-deploying-far-edge-clusters-at-scale}\n\nRed Hat Advanced Cluster Management (RHACM) manages clusters in a hub-and-spoke architecture, where a single hub cluster manages many spoke clusters. Hub clusters running RHACM provision and deploy the managed clusters by using GitOps Zero Touch Provisioning (ZTP) and the assisted service that is deployed when you install RHACM.\n\nThe assisted service handles provisioning of OpenShift Container Platform on single node clusters, three-node clusters, or standard clusters running on bare metal.\n\nA high-level overview of using GitOps ZTP to provision and maintain bare-metal hosts with OpenShift Container Platform is as follows:\n\n- A hub cluster running RHACM manages an OpenShift image registry that mirrors the OpenShift Container Platform release images. RHACM uses the OpenShift image registry to provision the managed clusters.\n- You manage the bare-metal hosts in a YAML format inventory file, versioned in a Git repository.\n- You make the hosts ready for provisioning as managed clusters, and use RHACM and the assisted service to install the bare-metal hosts on site.\n\nInstalling and deploying the clusters is a two-stage process, involving an initial installation phase, and a subsequent configuration and deployment phase. The following diagram illustrates this workflow:\n\n![Using GitOps and GitOps ZTP to install and deploy managed clusters](/openshift-docs-markdown/_assets/images/474_OpenShift_OpenShift_RAN_RDS_arch_updates_1023.png)\n\n## Installing managed clusters with ClusterInstance resources and RHACM {#ztp-creating-ztp-crs-for-multiple-managed-clusters_ztp-deploying-far-edge-clusters-at-scale}\n\nGitOps Zero Touch Provisioning (ZTP) uses `ClusterInstance` custom resources (CRs) in a Git repository to manage the processes that install OpenShift Container Platform clusters. The `ClusterInstance` CR contains cluster-specific parameters required for installation. It has options for applying select configuration CRs during installation including user defined extra manifests.\n\nThe GitOps ZTP plugin processes `ClusterInstance` CRs to generate a collection of CRs on the hub cluster. This triggers the assisted service in Red Hat Advanced Cluster Management (RHACM) to install OpenShift Container Platform on the bare-metal host. You can find installation status and error messages in these CRs on the hub cluster. You can provision single clusters manually or in batches with GitOps ZTP:\n\nProvisioning a single cluster\n:   Create a single `ClusterInstance` CR and related configuration CRs for the cluster, and apply them in the hub cluster to begin cluster provisioning. This is a good way to test your CRs before deploying on a larger scale.\n\nProvisioning many clusters\n:   Install managed clusters in batches of up to 500 by defining `ClusterInstance` and related CRs in a Git repository. ArgoCD uses the `ClusterInstance` CRs to deploy the clusters. The RHACM policy generator creates the manifests and applies them to the hub cluster. This starts the cluster provisioning process.\n\n## Configuring managed clusters with policies and PolicyGenerator resources {#ztp-configuring-cluster-policies_ztp-deploying-far-edge-clusters-at-scale}\n\nGitOps Zero Touch Provisioning (ZTP) uses Red Hat Advanced Cluster Management (RHACM) to configure clusters by using a policy-based governance approach to applying the configuration.\n\nThe policy generator is a plugin for the GitOps Operator that enables the creation of RHACM policies from a concise template. The tool can combine multiple CRs into a single policy, and you can generate multiple policies that apply to various subsets of clusters in your fleet.\n\n> [!NOTE]\n> For scalability and to reduce the complexity of managing configurations across the fleet of clusters, use configuration CRs with as much commonality as possible.\n>\n> - Where possible, apply configuration CRs using a fleet-wide common policy.\n> - The next preference is to create logical groupings of clusters to manage as much of the remaining configurations as possible under a group policy.\n> - When a configuration is unique to an individual site, use RHACM templating on the hub cluster to inject the site-specific data into a common or group policy. Alternatively, apply an individual site policy for the site.\n\nThe following diagram shows how the policy generator interacts with GitOps and RHACM in the configuration phase of cluster deployment.\n\n![Policy generator](/openshift-docs-markdown/_assets/images/217_OpenShift_Zero_Touch_Provisioning_updates_1022_3.png)\n\nFor large fleets of clusters, it is typical for there to be a high-level of consistency in the configuration of those clusters.\n\nThe following recommended structuring of policies combines configuration CRs to meet several goals:\n\n- Describe common configurations once and apply to the fleet.\n- Minimize the number of maintained and managed policies.\n- Support flexibility in common configurations for cluster variants.\n\n**Recommended PolicyGenerator policy categories**\n\n| Policy category | Description |\n| --- | --- |\n| Common | A policy that exists in the common category is applied to all clusters in the fleet. Use common `{{ policy_gen_cr }}` CRs to apply common installation settings across all cluster types. |\n| Groups | A policy that exists in the groups category is applied to a group of clusters in the fleet. Use group `{{ policy_gen_cr }}` CRs to manage specific aspects of single-node, three-node, and standard cluster installations. Cluster groups can also follow geographic region, hardware variant, etc. |\n| Sites | A policy that exists in the sites category is applied to a specific cluster site. Any cluster can have its own specific policies maintained. |\n\n> [!IMPORTANT]\n> Using `PolicyGenTemplate` CRs to manage and deploy policies to managed clusters will be deprecated in an upcoming OpenShift Container Platform release. Equivalent and improved functionality is available using Red Hat Advanced Cluster Management (RHACM) and `PolicyGenerator` CRs.\n>\n> For more information about `PolicyGenerator` resources, see the RHACM [Integrating Policy Generator](https://docs.redhat.com/en/documentation/red_hat_advanced_cluster_management_for_kubernetes/2.17/html-single/governance/index#integrate-policy-generator) documentation.\n\n**Additional resources**\n\n- [Configuring managed cluster policies by using PolicyGenerator resources](/openshift-docs-markdown/edge_computing/policygenerator_for_ztp/ztp-configuring-managed-clusters-policygenerator#ztp-configuring-managed-clusters-policygenerator)\n- [Comparing RHACM PolicyGenerator and PolicyGenTemplate resource patching](/openshift-docs-markdown/edge_computing/policygenerator_for_ztp/ztp-configuring-managed-clusters-policygenerator#ztp-comparing-pgt-and-rhacm-pg-patching-strategies_ztp-configuring-managed-clusters-policygenerator)\n- [Preparing the GitOps ZTP Git repository](/openshift-docs-markdown/edge_computing/ztp-preparing-the-hub-cluster#ztp-preparing-the-ztp-git-repository_ztp-preparing-the-hub-cluster)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/edge_computing/ztp-deploying-far-edge-clusters-at-scale.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="ztp-deploying-far-edge-clusters-at-scale" class="group scroll-mt-20">Challenges of the network far edge<a href="#ztp-deploying-far-edge-clusters-at-scale" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Edge computing presents complex challenges when managing many sites in geographically displaced locations. Use GitOps Zero Touch Provisioning (ZTP) to provision and manage sites at the far edge of the network.</p>
      <h2 id="ztp-challenges-of-far-edge-deployments_ztp-deploying-far-edge-clusters-at-scale" class="group scroll-mt-20">Overcoming the challenges of the network far edge<a href="#ztp-challenges-of-far-edge-deployments_ztp-deploying-far-edge-clusters-at-scale" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Today, service providers want to deploy their infrastructure at the edge of the network. This presents significant challenges:</p>
      <ul>
      <li>How do you handle deployments of many edge sites in parallel?</li>
      <li>What happens when you need to deploy sites in disconnected environments?</li>
      <li>How do you manage the lifecycle of large fleets of clusters?</li>
      </ul>
      <p>GitOps Zero Touch Provisioning (ZTP) and <em>GitOps</em> meets these challenges by allowing you to provision remote edge sites at scale with declarative site definitions and configurations for bare-metal equipment. Template or overlay configurations install OpenShift Container Platform features that are required for CNF workloads. The full lifecycle of installation and upgrades is handled through the GitOps ZTP pipeline.</p>
      <p>GitOps ZTP uses GitOps for infrastructure deployments. With GitOps, you use declarative YAML files and other defined patterns stored in Git repositories. Red Hat Advanced Cluster Management (RHACM) uses your Git repositories to drive the deployment of your infrastructure.</p>
      <p>GitOps provides traceability, role-based access control (RBAC), and a single source of truth for the desired state of each site. Scalability issues are addressed by Git methodologies and event driven operations through webhooks.</p>
      <p>You start the GitOps ZTP workflow by creating declarative site definition and configuration custom resources (CRs) that the GitOps ZTP pipeline delivers to the edge nodes.</p>
      <p>The following diagram shows how GitOps ZTP works within the far edge framework.</p>
      <BlockImage src="/openshift-docs-markdown/_assets/images/217_OpenShift_Zero_Touch_Provisioning_updates_1022_1.png" alt="GitOps ZTP at the network far edge" />
      <h2 id="about-ztp_ztp-deploying-far-edge-clusters-at-scale" class="group scroll-mt-20">Using GitOps ZTP to provision clusters at the network far edge<a href="#about-ztp_ztp-deploying-far-edge-clusters-at-scale" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Red Hat Advanced Cluster Management (RHACM) manages clusters in a hub-and-spoke architecture, where a single hub cluster manages many spoke clusters. Hub clusters running RHACM provision and deploy the managed clusters by using GitOps Zero Touch Provisioning (ZTP) and the assisted service that is deployed when you install RHACM.</p>
      <p>The assisted service handles provisioning of OpenShift Container Platform on single node clusters, three-node clusters, or standard clusters running on bare metal.</p>
      <p>A high-level overview of using GitOps ZTP to provision and maintain bare-metal hosts with OpenShift Container Platform is as follows:</p>
      <ul>
      <li>A hub cluster running RHACM manages an OpenShift image registry that mirrors the OpenShift Container Platform release images. RHACM uses the OpenShift image registry to provision the managed clusters.</li>
      <li>You manage the bare-metal hosts in a YAML format inventory file, versioned in a Git repository.</li>
      <li>You make the hosts ready for provisioning as managed clusters, and use RHACM and the assisted service to install the bare-metal hosts on site.</li>
      </ul>
      <p>Installing and deploying the clusters is a two-stage process, involving an initial installation phase, and a subsequent configuration and deployment phase. The following diagram illustrates this workflow:</p>
      <BlockImage src="/openshift-docs-markdown/_assets/images/474_OpenShift_OpenShift_RAN_RDS_arch_updates_1023.png" alt="Using GitOps and GitOps ZTP to install and deploy managed clusters" />
      <h2 id="ztp-creating-ztp-crs-for-multiple-managed-clusters_ztp-deploying-far-edge-clusters-at-scale" class="group scroll-mt-20">Installing managed clusters with ClusterInstance resources and RHACM<a href="#ztp-creating-ztp-crs-for-multiple-managed-clusters_ztp-deploying-far-edge-clusters-at-scale" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">GitOps Zero Touch Provisioning (ZTP) uses <code>ClusterInstance</code> custom resources (CRs) in a Git repository to manage the processes that install OpenShift Container Platform clusters. The <code>ClusterInstance</code> CR contains cluster-specific parameters required for installation. It has options for applying select configuration CRs during installation including user defined extra manifests.</p>
      <p>The GitOps ZTP plugin processes <code>ClusterInstance</code> CRs to generate a collection of CRs on the hub cluster. This triggers the assisted service in Red Hat Advanced Cluster Management (RHACM) to install OpenShift Container Platform on the bare-metal host. You can find installation status and error messages in these CRs on the hub cluster. You can provision single clusters manually or in batches with GitOps ZTP:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Provisioning a single cluster</dt>
      <dd class="ml-6 text-muted-foreground"><p>Create a single <code>ClusterInstance</code> CR and related configuration CRs for the cluster, and apply them in the hub cluster to begin cluster provisioning. This is a good way to test your CRs before deploying on a larger scale.</p></dd>
      <dt class="font-semibold">Provisioning many clusters</dt>
      <dd class="ml-6 text-muted-foreground"><p>Install managed clusters in batches of up to 500 by defining <code>ClusterInstance</code> and related CRs in a Git repository. ArgoCD uses the <code>ClusterInstance</code> CRs to deploy the clusters. The RHACM policy generator creates the manifests and applies them to the hub cluster. This starts the cluster provisioning process.</p></dd>
      </dl>
      <h2 id="ztp-configuring-cluster-policies_ztp-deploying-far-edge-clusters-at-scale" class="group scroll-mt-20">Configuring managed clusters with policies and PolicyGenerator resources<a href="#ztp-configuring-cluster-policies_ztp-deploying-far-edge-clusters-at-scale" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">GitOps Zero Touch Provisioning (ZTP) uses Red Hat Advanced Cluster Management (RHACM) to configure clusters by using a policy-based governance approach to applying the configuration.</p>
      <p>The policy generator is a plugin for the GitOps Operator that enables the creation of RHACM policies from a concise template. The tool can combine multiple CRs into a single policy, and you can generate multiple policies that apply to various subsets of clusters in your fleet.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>For scalability and to reduce the complexity of managing configurations across the fleet of clusters, use configuration CRs with as much commonality as possible.</p>
          <ul>
          <li>Where possible, apply configuration CRs using a fleet-wide common policy.</li>
          <li>The next preference is to create logical groupings of clusters to manage as much of the remaining configurations as possible under a group policy.</li>
          <li>When a configuration is unique to an individual site, use RHACM templating on the hub cluster to inject the site-specific data into a common or group policy. Alternatively, apply an individual site policy for the site.</li>
          </ul>
        </AlertDescription>
      </Alert>
      <p>The following diagram shows how the policy generator interacts with GitOps and RHACM in the configuration phase of cluster deployment.</p>
      <BlockImage src="/openshift-docs-markdown/_assets/images/217_OpenShift_Zero_Touch_Provisioning_updates_1022_3.png" alt="Policy generator" />
      <p>For large fleets of clusters, it is typical for there to be a high-level of consistency in the configuration of those clusters.</p>
      <p>The following recommended structuring of policies combines configuration CRs to meet several goals:</p>
      <ul>
      <li>Describe common configurations once and apply to the fleet.</li>
      <li>Minimize the number of maintained and managed policies.</li>
      <li>Support flexibility in common configurations for cluster variants.</li>
      </ul>
      <p><strong>Recommended PolicyGenerator policy categories</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Policy category</TableHead>
      <TableHead>Description</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>Common</TableCell>
      <TableCell>A policy that exists in the common category is applied to all clusters in the fleet. Use common <code>{"{"}{"{"} policy_gen_cr {"}"}{"}"}</code> CRs to apply common installation settings across all cluster types.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Groups</TableCell>
      <TableCell>A policy that exists in the groups category is applied to a group of clusters in the fleet. Use group <code>{"{"}{"{"} policy_gen_cr {"}"}{"}"}</code> CRs to manage specific aspects of single-node, three-node, and standard cluster installations. Cluster groups can also follow geographic region, hardware variant, etc.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Sites</TableCell>
      <TableCell>A policy that exists in the sites category is applied to a specific cluster site. Any cluster can have its own specific policies maintained.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Using <code>PolicyGenTemplate</code> CRs to manage and deploy policies to managed clusters will be deprecated in an upcoming OpenShift Container Platform release. Equivalent and improved functionality is available using Red Hat Advanced Cluster Management (RHACM) and <code>PolicyGenerator</code> CRs.</p>
          <p>For more information about <code>PolicyGenerator</code> resources, see the RHACM <a href="https://docs.redhat.com/en/documentation/red_hat_advanced_cluster_management_for_kubernetes/2.17/html-single/governance/index#integrate-policy-generator">Integrating Policy Generator</a> documentation.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Additional resources</strong></p>
      <ul>
      <li><a href="/openshift-docs-markdown/edge_computing/policygenerator_for_ztp/ztp-configuring-managed-clusters-policygenerator#ztp-configuring-managed-clusters-policygenerator">Configuring managed cluster policies by using PolicyGenerator resources</a></li>
      <li><a href="/openshift-docs-markdown/edge_computing/policygenerator_for_ztp/ztp-configuring-managed-clusters-policygenerator#ztp-comparing-pgt-and-rhacm-pg-patching-strategies_ztp-configuring-managed-clusters-policygenerator">Comparing RHACM PolicyGenerator and PolicyGenTemplate resource patching</a></li>
      <li><a href="/openshift-docs-markdown/edge_computing/ztp-preparing-the-hub-cluster#ztp-preparing-the-ztp-git-repository_ztp-preparing-the-hub-cluster">Preparing the GitOps ZTP Git repository</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
