---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: edge_computing/sno_ip_configuration/cnf-troubleshooting-sno-ip-configuration.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"cnf-troubleshooting-sno-ip-configuration","text":"Troubleshoot single-node OpenShift network reconfiguration"},{"depth":2,"slug":"cnf-gathering-sno-ip-configuration-diagnostics_troubleshooting-sno-ip-configuration","text":"Gather diagnostic information for network reconfiguration issues"},{"depth":2,"slug":"cnf-troubleshooting-sno-ip-configuration_troubleshooting-sno-ip-configuration","text":"Network reconfiguration troubleshooting reference"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/edge_computing/sno_ip_configuration/cnf-troubleshooting-sno-ip-configuration";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Troubleshoot single-node OpenShift network reconfiguration";
const description = "Use the following information to diagnose and resolve network reconfiguration issues on single-node OpenShift clusters." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["edge_computing","edge_computing/sno_ip_configuration"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Troubleshoot single-node OpenShift network reconfiguration {#cnf-troubleshooting-sno-ip-configuration}\n\nUse the following information to diagnose and resolve network reconfiguration issues on single-node OpenShift clusters.\n\n## Gather diagnostic information for network reconfiguration issues {#cnf-gathering-sno-ip-configuration-diagnostics_troubleshooting-sno-ip-configuration}\n\nYou can gather diagnostic information to help troubleshoot network reconfiguration issues on single-node OpenShift clusters.\n\n**Procedure**\n\n1. Inspect the `IPConfig` custom resource (CR) status by running the following command:\n\n   ```terminal\n   $ oc get ipc ipconfig -o yaml\n   ```\n\n   Review the `status.conditions` field for the current state, reason, and message. Check `status.validNextStages` for possible stage transitions, and `status.history` for timestamps of stage progression.\n2. View the Lifecycle Agent controller logs by running the following command:\n\n   ```terminal\n   $ oc logs -n openshift-lifecycle-agent deployment/lifecycle-agent-controller-manager -c manager\n   ```\n3. Create a debug session on the target node by running the following command:\n\n   ```terminal\n   $ oc debug node/<node_name>\n   # chroot /host\n   ```\n\n   - Replace `<node_name>` with the name of your single-node OpenShift node.\n4. View the relevant service logs depending on which phase you are troubleshooting by running one of the following commands:\n\n   - View the logs for pre-pivot issues by running the following command:\n\n     ```terminal\n     $ sudo journalctl -u lca-ipconfig-pre-pivot -b --no-pager\n     ```\n   - View the logs for post-pivot issues by running the following command:\n\n     ```terminal\n     $ sudo journalctl -u ip-configuration.service -b --no-pager\n     ```\n   - View the logs for `init-monitor` watchdog issues by running the following command:\n\n     ```terminal\n     $ sudo journalctl -u lca-init-monitor.service -b --no-pager\n     ```\n   - View the logs for rollback issues by running the following command:\n\n     ```terminal\n     $ sudo journalctl -u lca-ipconfig-rollback -b --no-pager\n     ```\n\n## Network reconfiguration troubleshooting reference {#cnf-troubleshooting-sno-ip-configuration_troubleshooting-sno-ip-configuration}\n\nUse the following reference information to help diagnose and resolve network reconfiguration issues on single-node OpenShift clusters.\n\n**On-node artifacts for troubleshooting**\n\n| File | Description |\n| --- | --- |\n| `/var/lib/lca/workspace/ip-config-pre-pivot.json` | Pre-pivot configuration data |\n| `/var/lib/lca/workspace/ip-config-post-pivot.json` | Post-pivot configuration data |\n| `/var/lib/lca/workspace/nmstate.yaml` | Generated `nmstate` configuration for network changes |\n| `/var/lib/lca/workspace/recert_config.json` | Configuration for certificate regeneration |\n| `/var/lib/lca/workspace/ip-config-autorollback-config.json` | Auto-rollback configuration |\n| `/var/lib/lca/workspace/recert-pull-secret.json` | Pull secret for the recert image, if a custom pull secret was specified |\n| `/var/lib/lca/ipc.json` | Persistence file that stores `IPConfig` state for rollback and status continuity across restarts |\n\n**Common failure patterns**\n\n| Issue | Cause | Solution |\n| --- | --- | --- |\n| Stage transition rejected | You attempted to transition to a stage not in `status.validNextStages`. | Check `status.validNextStages` and only transition to an allowed stage. |\n| Specification fields cannot be changed | You attempted to modify `spec` fields while the CR is not in the `Idle` stage. | Wait for the current operation to complete and the CR to return to `Idle`. |\n| Health checks never pass | Cluster health blockers are preventing progress. | Investigate cluster health issues. |\n| Post-pivot phase failed | An error occurred during network configuration or certificate regeneration. | Review the post-pivot service logs. If auto-rollback is enabled, the node automatically reverts. Otherwise, manually trigger rollback by setting `spec.stage: Rollback`. |\n| Pods not receiving new IP family | Pre-existing pods might not automatically receive an IP address from the new family because of CNI behavior. | Delete and re-create the affected pods to obtain addresses from the new IP family. |\n| Configuration stuck in disconnected environment | The Lifecycle Agent or recert container might be attempting to pull images not available in the disconnected registry. | Ensure all required images are mirrored to your disconnected registry before starting. Verify the `lca.openshift.io/recert-pull-secret` annotation references a valid pull secret. |\n\n**Additional resources**\n{._additional-resources}\n\n- [Gathering data about your cluster](/openshift-docs-markdown/support/gathering-cluster-data#gathering-cluster-data)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/edge_computing/sno_ip_configuration/cnf-troubleshooting-sno-ip-configuration.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="cnf-troubleshooting-sno-ip-configuration" class="group scroll-mt-20">Troubleshoot single-node OpenShift network reconfiguration<a href="#cnf-troubleshooting-sno-ip-configuration" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Use the following information to diagnose and resolve network reconfiguration issues on single-node OpenShift clusters.</p>
      <h2 id="cnf-gathering-sno-ip-configuration-diagnostics_troubleshooting-sno-ip-configuration" class="group scroll-mt-20">Gather diagnostic information for network reconfiguration issues<a href="#cnf-gathering-sno-ip-configuration-diagnostics_troubleshooting-sno-ip-configuration" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can gather diagnostic information to help troubleshoot network reconfiguration issues on single-node OpenShift clusters.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Inspect the <code>IPConfig</code> custom resource (CR) status by running the following command:
      <CodeBlock code={"$ oc get ipc ipconfig -o yaml"} lang="terminal" />
      <p>Review the <code>status.conditions</code> field for the current state, reason, and message. Check <code>status.validNextStages</code> for possible stage transitions, and <code>status.history</code> for timestamps of stage progression.</p>
      </li>
      <li>
      View the Lifecycle Agent controller logs by running the following command:
      <CodeBlock code={"$ oc logs -n openshift-lifecycle-agent deployment/lifecycle-agent-controller-manager -c manager"} lang="terminal" />
      </li>
      <li>
      Create a debug session on the target node by running the following command:
      <CodeBlock code={"$ oc debug node/<node_name>\n# chroot /host"} lang="terminal" />
      <ul><li>Replace <code>&lt;node_name&gt;</code> with the name of your single-node OpenShift node.</li></ul>
      </li>
      <li>
      View the relevant service logs depending on which phase you are troubleshooting by running one of the following commands:
      <ul>
      <li>
      View the logs for pre-pivot issues by running the following command:
      <CodeBlock code={"$ sudo journalctl -u lca-ipconfig-pre-pivot -b --no-pager"} lang="terminal" />
      </li>
      <li>
      View the logs for post-pivot issues by running the following command:
      <CodeBlock code={"$ sudo journalctl -u ip-configuration.service -b --no-pager"} lang="terminal" />
      </li>
      <li>
      View the logs for <code>init-monitor</code> watchdog issues by running the following command:
      <CodeBlock code={"$ sudo journalctl -u lca-init-monitor.service -b --no-pager"} lang="terminal" />
      </li>
      <li>
      View the logs for rollback issues by running the following command:
      <CodeBlock code={"$ sudo journalctl -u lca-ipconfig-rollback -b --no-pager"} lang="terminal" />
      </li>
      </ul>
      </li>
      </ol>
      <h2 id="cnf-troubleshooting-sno-ip-configuration_troubleshooting-sno-ip-configuration" class="group scroll-mt-20">Network reconfiguration troubleshooting reference<a href="#cnf-troubleshooting-sno-ip-configuration_troubleshooting-sno-ip-configuration" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Use the following reference information to help diagnose and resolve network reconfiguration issues on single-node OpenShift clusters.</p>
      <p><strong>On-node artifacts for troubleshooting</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>File</TableHead>
      <TableHead>Description</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>/var/lib/lca/workspace/ip-config-pre-pivot.json</code></TableCell>
      <TableCell>Pre-pivot configuration data</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>/var/lib/lca/workspace/ip-config-post-pivot.json</code></TableCell>
      <TableCell>Post-pivot configuration data</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>/var/lib/lca/workspace/nmstate.yaml</code></TableCell>
      <TableCell>Generated <code>nmstate</code> configuration for network changes</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>/var/lib/lca/workspace/recert_config.json</code></TableCell>
      <TableCell>Configuration for certificate regeneration</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>/var/lib/lca/workspace/ip-config-autorollback-config.json</code></TableCell>
      <TableCell>Auto-rollback configuration</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>/var/lib/lca/workspace/recert-pull-secret.json</code></TableCell>
      <TableCell>Pull secret for the recert image, if a custom pull secret was specified</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>/var/lib/lca/ipc.json</code></TableCell>
      <TableCell>Persistence file that stores <code>IPConfig</code> state for rollback and status continuity across restarts</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p><strong>Common failure patterns</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Issue</TableHead>
      <TableHead>Cause</TableHead>
      <TableHead>Solution</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>Stage transition rejected</TableCell>
      <TableCell>You attempted to transition to a stage not in <code>status.validNextStages</code>.</TableCell>
      <TableCell>Check <code>status.validNextStages</code> and only transition to an allowed stage.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Specification fields cannot be changed</TableCell>
      <TableCell>You attempted to modify <code>spec</code> fields while the CR is not in the <code>Idle</code> stage.</TableCell>
      <TableCell>Wait for the current operation to complete and the CR to return to <code>Idle</code>.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Health checks never pass</TableCell>
      <TableCell>Cluster health blockers are preventing progress.</TableCell>
      <TableCell>Investigate cluster health issues.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Post-pivot phase failed</TableCell>
      <TableCell>An error occurred during network configuration or certificate regeneration.</TableCell>
      <TableCell>Review the post-pivot service logs. If auto-rollback is enabled, the node automatically reverts. Otherwise, manually trigger rollback by setting <code>spec.stage: Rollback</code>.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Pods not receiving new IP family</TableCell>
      <TableCell>Pre-existing pods might not automatically receive an IP address from the new family because of CNI behavior.</TableCell>
      <TableCell>Delete and re-create the affected pods to obtain addresses from the new IP family.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Configuration stuck in disconnected environment</TableCell>
      <TableCell>The Lifecycle Agent or recert container might be attempting to pull images not available in the disconnected registry.</TableCell>
      <TableCell>Ensure all required images are mirrored to your disconnected registry before starting. Verify the <code>lca.openshift.io/recert-pull-secret</code> annotation references a valid pull secret.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="/openshift-docs-markdown/support/gathering-cluster-data#gathering-cluster-data">Gathering data about your cluster</a></li></ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
