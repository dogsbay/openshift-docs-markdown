---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: authentication/using-service-accounts-as-oauth-client.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"using-service-accounts-as-oauth-client","text":"Using a service account as an OAuth client"},{"depth":2,"slug":"service-accounts-as-oauth-clients_using-service-accounts-as-oauth-client","text":"About service accounts as OAuth clients"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/authentication/using-service-accounts-as-oauth-client";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Using a service account as an OAuth client";
const description = "To authenticate users when restricting access to a specific namespace, you can configure a service account as a constrained OAuth client by using static…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["authentication"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Using a service account as an OAuth client {#using-service-accounts-as-oauth-client}\n\nTo authenticate users when restricting access to a specific namespace, you can configure a service account as a constrained OAuth client by using static or dynamic redirect URI annotations.\n\n## About service accounts as OAuth clients {#service-accounts-as-oauth-clients_using-service-accounts-as-oauth-client}\n\nYou can configure a service account to function as a constrained OAuth client that can request a limited subset of scopes.\n\nService accounts can request only a subset of scopes that allow access to the following basic user information and role-based power inside of the service account’s own namespace:\n\n- `user:info`\n- `user:check-access`\n- `role:<any_role>:<service_account_namespace>`\n- `role:<any_role>:<service_account_namespace>:!`\n\nWhen using a service account as an OAuth client:\n\n- `client_id` is `system:serviceaccount:<service_account_namespace>:<service_account_name>`.\n- `client_secret` can be any of the API tokens for that service account. For example:\n\n  ```terminal\n  $ oc sa get-token <service_account_name>\n  ```\n- To get `WWW-Authenticate` challenges, set an `serviceaccounts.openshift.io/oauth-want-challenges` annotation on the service account to `true`.\n- `redirect_uri` must match an annotation on the service account.\n\nAnnotation keys in service accounts must have the prefix `serviceaccounts.openshift.io/oauth-redirecturi.` or `serviceaccounts.openshift.io/oauth-redirectreference.` such as:\n\n```\nserviceaccounts.openshift.io/oauth-redirecturi.<name>\n```\n\nIn its simplest form, the annotation can be used to directly specify valid redirect URIs. For example:\n\n```\n\"serviceaccounts.openshift.io/oauth-redirecturi.first\":  \"https://example.com\"\n\"serviceaccounts.openshift.io/oauth-redirecturi.second\": \"https://other.com\"\n```\n\nThe `first` and `second` postfixes in the above example are used to separate the two valid redirect URIs.\n\nIn more complex configurations, static redirect URIs may not be enough. For example, perhaps you want all Ingresses for a route to be considered valid. This is where dynamic redirect URIs via the `serviceaccounts.openshift.io/oauth-redirectreference.` prefix come into play.\n\nFor example:\n\n```\n\"serviceaccounts.openshift.io/oauth-redirectreference.first\": \"{\\\"kind\\\":\\\"OAuthRedirectReference\\\",\\\"apiVersion\\\":\\\"v1\\\",\\\"reference\\\":{\\\"kind\\\":\\\"Route\\\",\\\"name\\\":\\\"jenkins\\\"}}\"\n```\n\nSince the value for this annotation contains serialized JSON data, it is easier to see in an expanded format:\n\n```json\n\n{\n  \"kind\": \"OAuthRedirectReference\",\n  \"apiVersion\": \"v1\",\n  \"reference\": {\n    \"kind\": \"Route\",\n    \"name\": \"jenkins\"\n  }\n}\n```\n\nNow you can see that an `OAuthRedirectReference` allows us to reference the route named `jenkins`. Thus, all Ingresses for that route will now be considered valid.  The full specification for an `OAuthRedirectReference` is:\n\n```json\n\n{\n  \"kind\": \"OAuthRedirectReference\",\n  \"apiVersion\": \"v1\",\n  \"reference\": {\n    \"kind\": ...,\n    \"name\": ...,\n    \"group\": ...\n  }\n}\n```\n\nwhere:\n\n`reference.kind`\n:   Specifies the type of the object being referenced. Currently, only `route` is supported.\n\n`reference.name`\n:   Specifies the name of the object. The object must be in the same namespace as the service account.\n\n`reference.group`\n:   Specifies the group of the object. Leave this blank, as the group for a route is the empty string.\n\nBoth annotation prefixes can be combined to override the data provided by the reference object. For example:\n\n```\n\"serviceaccounts.openshift.io/oauth-redirecturi.first\":  \"custompath\"\n\"serviceaccounts.openshift.io/oauth-redirectreference.first\": \"{\\\"kind\\\":\\\"OAuthRedirectReference\\\",\\\"apiVersion\\\":\\\"v1\\\",\\\"reference\\\":{\\\"kind\\\":\\\"Route\\\",\\\"name\\\":\\\"jenkins\\\"}}\"\n```\n\nThe `first` postfix is used to tie the annotations together. Assuming that the `jenkins` route had an Ingress of `https://example.com`, now `https://example.com/custompath` is considered valid, but `https://example.com` is not.  The format for partially supplying override data is as follows:\n\n<table>\n<thead>\n<tr>\n  <th>Type</th>\n  <th>Syntax</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td>Scheme</td>\n  <td>\"\\https://\"</td>\n</tr>\n<tr>\n  <td>Hostname</td>\n  <td>\"//website.com\"</td>\n</tr>\n<tr>\n  <td>Port</td>\n  <td>\"//:8000\"</td>\n</tr>\n<tr>\n  <td>Path</td>\n  <td>\"examplepath\"</td>\n</tr>\n</tbody>\n</table>\n\n> [!NOTE]\n> Specifying a hostname override will replace the hostname data from the referenced object, which is not likely to be desired behavior.\n\nAny combination of the above syntax can be combined using the following format:\n\n`<scheme:>//<hostname><:port>/<path>`\n\nThe same object can be referenced more than once for more flexibility:\n\n```\n\"serviceaccounts.openshift.io/oauth-redirecturi.first\":  \"custompath\"\n\"serviceaccounts.openshift.io/oauth-redirectreference.first\": \"{\\\"kind\\\":\\\"OAuthRedirectReference\\\",\\\"apiVersion\\\":\\\"v1\\\",\\\"reference\\\":{\\\"kind\\\":\\\"Route\\\",\\\"name\\\":\\\"jenkins\\\"}}\"\n\"serviceaccounts.openshift.io/oauth-redirecturi.second\":  \"//:8000\"\n\"serviceaccounts.openshift.io/oauth-redirectreference.second\": \"{\\\"kind\\\":\\\"OAuthRedirectReference\\\",\\\"apiVersion\\\":\\\"v1\\\",\\\"reference\\\":{\\\"kind\\\":\\\"Route\\\",\\\"name\\\":\\\"jenkins\\\"}}\"\n```\n\nAssuming that the route named `jenkins` has an Ingress of `https://example.com`, then both `https://example.com:8000` and `https://example.com/custompath` are considered valid.\n\nStatic and dynamic annotations can be used at the same time to achieve the desired behavior:\n\n```\n\"serviceaccounts.openshift.io/oauth-redirectreference.first\": \"{\\\"kind\\\":\\\"OAuthRedirectReference\\\",\\\"apiVersion\\\":\\\"v1\\\",\\\"reference\\\":{\\\"kind\\\":\\\"Route\\\",\\\"name\\\":\\\"jenkins\\\"}}\"\n\"serviceaccounts.openshift.io/oauth-redirecturi.second\": \"https://other.com\"\n```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/authentication/using-service-accounts-as-oauth-client.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="using-service-accounts-as-oauth-client" class="group scroll-mt-20">Using a service account as an OAuth client<a href="#using-service-accounts-as-oauth-client" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>To authenticate users when restricting access to a specific namespace, you can configure a service account as a constrained OAuth client by using static or dynamic redirect URI annotations.</p>
      <h2 id="service-accounts-as-oauth-clients_using-service-accounts-as-oauth-client" class="group scroll-mt-20">About service accounts as OAuth clients<a href="#service-accounts-as-oauth-clients_using-service-accounts-as-oauth-client" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can configure a service account to function as a constrained OAuth client that can request a limited subset of scopes.</p>
      <p>Service accounts can request only a subset of scopes that allow access to the following basic user information and role-based power inside of the service account’s own namespace:</p>
      <ul>
      <li><code>user:info</code></li>
      <li><code>user:check-access</code></li>
      <li><code>role:&lt;any_role&gt;:&lt;service_account_namespace&gt;</code></li>
      <li><code>role:&lt;any_role&gt;:&lt;service_account_namespace&gt;:!</code></li>
      </ul>
      <p>When using a service account as an OAuth client:</p>
      <ul>
      <li><code>client_id</code> is <code>system:serviceaccount:&lt;service_account_namespace&gt;:&lt;service_account_name&gt;</code>.</li>
      <li>
      <code>client_secret</code> can be any of the API tokens for that service account. For example:
      <CodeBlock code={"$ oc sa get-token <service_account_name>"} lang="terminal" />
      </li>
      <li>To get <code>WWW-Authenticate</code> challenges, set an <code>serviceaccounts.openshift.io/oauth-want-challenges</code> annotation on the service account to <code>true</code>.</li>
      <li><code>redirect_uri</code> must match an annotation on the service account.</li>
      </ul>
      <p>Annotation keys in service accounts must have the prefix <code>serviceaccounts.openshift.io/oauth-redirecturi.</code> or <code>serviceaccounts.openshift.io/oauth-redirectreference.</code> such as:</p>
      <CodeBlock code={"serviceaccounts.openshift.io/oauth-redirecturi.<name>"} lang="plaintext" />
      <p>In its simplest form, the annotation can be used to directly specify valid redirect URIs. For example:</p>
      <CodeBlock code={"\"serviceaccounts.openshift.io/oauth-redirecturi.first\":  \"https://example.com\"\n\"serviceaccounts.openshift.io/oauth-redirecturi.second\": \"https://other.com\""} lang="plaintext" />
      <p>The <code>first</code> and <code>second</code> postfixes in the above example are used to separate the two valid redirect URIs.</p>
      <p>In more complex configurations, static redirect URIs may not be enough. For example, perhaps you want all Ingresses for a route to be considered valid. This is where dynamic redirect URIs via the <code>serviceaccounts.openshift.io/oauth-redirectreference.</code> prefix come into play.</p>
      <p>For example:</p>
      <CodeBlock code={"\"serviceaccounts.openshift.io/oauth-redirectreference.first\": \"{\\\"kind\\\":\\\"OAuthRedirectReference\\\",\\\"apiVersion\\\":\\\"v1\\\",\\\"reference\\\":{\\\"kind\\\":\\\"Route\\\",\\\"name\\\":\\\"jenkins\\\"}}\""} lang="plaintext" />
      <p>Since the value for this annotation contains serialized JSON data, it is easier to see in an expanded format:</p>
      <CodeBlock code={"\n{\n  \"kind\": \"OAuthRedirectReference\",\n  \"apiVersion\": \"v1\",\n  \"reference\": {\n    \"kind\": \"Route\",\n    \"name\": \"jenkins\"\n  }\n}\n"} lang="json" />
      <p>Now you can see that an <code>OAuthRedirectReference</code> allows us to reference the route named <code>jenkins</code>. Thus, all Ingresses for that route will now be considered valid.  The full specification for an <code>OAuthRedirectReference</code> is:</p>
      <CodeBlock code={"\n{\n  \"kind\": \"OAuthRedirectReference\",\n  \"apiVersion\": \"v1\",\n  \"reference\": {\n    \"kind\": ...,\n    \"name\": ...,\n    \"group\": ...\n  }\n}\n"} lang="json" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>reference.kind</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the type of the object being referenced. Currently, only <code>route</code> is supported.</p></dd>
      <dt class="font-semibold"><code>reference.name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the object. The object must be in the same namespace as the service account.</p></dd>
      <dt class="font-semibold"><code>reference.group</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the group of the object. Leave this blank, as the group for a route is the empty string.</p></dd>
      </dl>
      <p>Both annotation prefixes can be combined to override the data provided by the reference object. For example:</p>
      <CodeBlock code={"\"serviceaccounts.openshift.io/oauth-redirecturi.first\":  \"custompath\"\n\"serviceaccounts.openshift.io/oauth-redirectreference.first\": \"{\\\"kind\\\":\\\"OAuthRedirectReference\\\",\\\"apiVersion\\\":\\\"v1\\\",\\\"reference\\\":{\\\"kind\\\":\\\"Route\\\",\\\"name\\\":\\\"jenkins\\\"}}\""} lang="plaintext" />
      <p>The <code>first</code> postfix is used to tie the annotations together. Assuming that the <code>jenkins</code> route had an Ingress of <code>https://example.com</code>, now <code>https://example.com/custompath</code> is considered valid, but <code>https://example.com</code> is not.  The format for partially supplying override data is as follows:</p>
      <Fragment set:html={"<table>\n<thead>\n<tr>\n  <th>Type</th>\n  <th>Syntax</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td>Scheme</td>\n  <td>\"\\https://\"</td>\n</tr>\n<tr>\n  <td>Hostname</td>\n  <td>\"//website.com\"</td>\n</tr>\n<tr>\n  <td>Port</td>\n  <td>\"//:8000\"</td>\n</tr>\n<tr>\n  <td>Path</td>\n  <td>\"examplepath\"</td>\n</tr>\n</tbody>\n</table>\n"} />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Specifying a hostname override will replace the hostname data from the referenced object, which is not likely to be desired behavior.</p>
        </AlertDescription>
      </Alert>
      <p>Any combination of the above syntax can be combined using the following format:</p>
      <p><code>&lt;scheme:&gt;//&lt;hostname&gt;&lt;:port&gt;/&lt;path&gt;</code></p>
      <p>The same object can be referenced more than once for more flexibility:</p>
      <CodeBlock code={"\"serviceaccounts.openshift.io/oauth-redirecturi.first\":  \"custompath\"\n\"serviceaccounts.openshift.io/oauth-redirectreference.first\": \"{\\\"kind\\\":\\\"OAuthRedirectReference\\\",\\\"apiVersion\\\":\\\"v1\\\",\\\"reference\\\":{\\\"kind\\\":\\\"Route\\\",\\\"name\\\":\\\"jenkins\\\"}}\"\n\"serviceaccounts.openshift.io/oauth-redirecturi.second\":  \"//:8000\"\n\"serviceaccounts.openshift.io/oauth-redirectreference.second\": \"{\\\"kind\\\":\\\"OAuthRedirectReference\\\",\\\"apiVersion\\\":\\\"v1\\\",\\\"reference\\\":{\\\"kind\\\":\\\"Route\\\",\\\"name\\\":\\\"jenkins\\\"}}\""} lang="plaintext" />
      <p>Assuming that the route named <code>jenkins</code> has an Ingress of <code>https://example.com</code>, then both <code>https://example.com:8000</code> and <code>https://example.com/custompath</code> are considered valid.</p>
      <p>Static and dynamic annotations can be used at the same time to achieve the desired behavior:</p>
      <CodeBlock code={"\"serviceaccounts.openshift.io/oauth-redirectreference.first\": \"{\\\"kind\\\":\\\"OAuthRedirectReference\\\",\\\"apiVersion\\\":\\\"v1\\\",\\\"reference\\\":{\\\"kind\\\":\\\"Route\\\",\\\"name\\\":\\\"jenkins\\\"}}\"\n\"serviceaccounts.openshift.io/oauth-redirecturi.second\": \"https://other.com\""} lang="plaintext" />
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
