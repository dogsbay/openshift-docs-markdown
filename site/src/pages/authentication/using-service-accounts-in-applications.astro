---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: authentication/using-service-accounts-in-applications.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"using-service-accounts","text":"Using service accounts in applications"},{"depth":2,"slug":"service-accounts-overview_using-service-accounts","text":"Service accounts overview"},{"depth":2,"slug":"service-accounts-default_using-service-accounts","text":"Default service accounts"},{"depth":3,"slug":"default-cluster-service-accounts_using-service-accounts","text":"Default cluster service accounts"},{"depth":3,"slug":"default-service-accounts-and-roles_using-service-accounts","text":"Default project service accounts and roles"},{"depth":3,"slug":"auto-generated-sa-token-secrets_using-service-accounts","text":"Automatically generated image pull secrets"},{"depth":2,"slug":"service-accounts-managing_using-service-accounts","text":"Creating service accounts"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/authentication/using-service-accounts-in-applications";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Using service accounts in applications";
const description = "You can use OpenShift Container Platform service accounts to allow a OpenShift Container Platform component to directly access the API." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["authentication"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Using service accounts in applications {#using-service-accounts}\n\n## Service accounts overview {#service-accounts-overview_using-service-accounts}\n\nYou can use OpenShift Container Platform service accounts to allow a OpenShift Container Platform component to directly access the API.\n\nService accounts are API objects that exist within each project that provide a flexible way to control API access without sharing a regular user’s credentials.\n\nWhen you use the OpenShift Container Platform CLI or web console, your API token authenticates you to the API. You can associate a component with a service account so that they can access the API without using a regular user’s credentials.\n\nFor example, service accounts can allow:\n\n- Replication controllers to make API calls to create or delete pods\n- Applications inside containers to make API calls for discovery purposes\n- External applications to make API calls for monitoring or integration purposes\n\nEach service account’s user name is derived from its project and name:\n\n```text\nsystem:serviceaccount:<project>:<name>\n```\n\nEvery service account is also a member of two groups:\n\n| Group | Description |\n| --- | --- |\n| system:serviceaccounts | Includes all service accounts in the system. |\n| system:serviceaccounts:<project> | Includes all service accounts in the specified project. |\n\n## Default service accounts {#service-accounts-default_using-service-accounts}\n\nYour OpenShift Container Platform cluster contains default service accounts for cluster management and generates more service accounts for each project.\n\n### Default cluster service accounts {#default-cluster-service-accounts_using-service-accounts}\n\nSeveral infrastructure controllers run using service account credentials. The following service accounts are created in the OpenShift Container Platform infrastructure project (`openshift-infra`) at server start, and given the following roles cluster-wide:\n\n| Service account | Description |\n| --- | --- |\n| `replication-controller` | Assigned the `system:replication-controller` role |\n| `deployment-controller` | Assigned the `system:deployment-controller` role |\n| `build-controller` | Assigned the `system:build-controller` role. Additionally, the `build-controller` service account is included in the privileged security context constraint to create privileged build pods. |\n\n### Default project service accounts and roles {#default-service-accounts-and-roles_using-service-accounts}\n\nThree service accounts are automatically created in each project:\n\n<table>\n<thead>\n<tr>\n  <th>Service account</th>\n  <th>Usage</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td><code>builder</code></td>\n  <td>Used by build pods. It is given the <code>system:image-builder</code> role, which allows pushing images to any imagestream in the project using the internal Docker registry.<br><br><dl class=\"db-admonition db-admonition-note\"><dt>Note</dt><dd>The <code>builder</code> service account is not created if the <code>Build</code> cluster capability is not enabled.</dd></dl></td>\n</tr>\n<tr>\n  <td><code>deployer</code></td>\n  <td>Used by deployment pods and given the <code>system:deployer</code> role, which allows viewing and modifying replication controllers and pods in the project.<br><br><dl class=\"db-admonition db-admonition-note\"><dt>Note</dt><dd>The <code>deployer</code> service account is not created if the <code>DeploymentConfig</code> cluster capability is not enabled.</dd></dl></td>\n</tr>\n<tr>\n  <td><code>default</code></td>\n  <td>Used to run all other pods unless they specify a different service account.<br><br><dl class=\"db-admonition db-admonition-important\"><dt>Important</dt><dd>Access rights and security privileges tied to the <code>default</code> service account apply to every pod in the project that does not specify a different service account. To implement the principle of least privilege and improve auditability, create dedicated service accounts for your workloads instead of using the <code>default</code> service account.<br><br>While most OpenShift Container Platform platform components and Operators use dedicated service accounts, the following dynamic tools continue to use the <code>default</code> service account to ensure operational efficiency:<br><br><ul><li><code>oc debug</code>: Uses the <code>default</code> service account to avoid the performance overhead of creating and removing unique service accounts for short-lived troubleshooting sessions.</li><li><code>oc adm must-gather</code>: Uses the <code>default</code> service account to collect diagnostic data across the cluster without requiring extensive manual RBAC modifications.</li></ul></dd></dl></td>\n</tr>\n</tbody>\n</table>\n\nAll service accounts in a project are given the `system:image-puller` role, which allows pulling images from any image stream in the project using the internal container image registry.\n\n### Automatically generated image pull secrets {#auto-generated-sa-token-secrets_using-service-accounts}\n\nOpenShift Container Platform automatically creates image pull secrets for each service account to integrate the internal image registry with user authentication.\n\n> [!NOTE]\n> Prior to OpenShift Container Platform 4.16, a long-lived service account API token secret was also generated for each service account that was created. Starting with OpenShift Container Platform 4.16, this service account API token secret is no longer created.\n>\n> After upgrading to 4.22, any existing long-lived service account API token secrets are not deleted and will continue to function. For information about detecting long-lived API tokens that are in use in your cluster or deleting them if they are not needed, see \"Long-lived service account API tokens in OpenShift Container Platform (Red Hat Knowledgebase)\".\n\nThis image pull secret is necessary to integrate the OpenShift image registry into the cluster’s user authentication and authorization system.\n\nHowever, if you do not enable the `ImageRegistry` capability or if you disable the integrated OpenShift image registry in the Cluster Image Registry Operator’s configuration, an image pull secret is not generated for each service account.\n\nWhen the integrated OpenShift image registry is disabled on a cluster that previously had it enabled, the previously generated image pull secrets are deleted automatically.\n\n## Creating service accounts {#service-accounts-managing_using-service-accounts}\n\nYou can create a service account in a project and grant it permissions by binding it to a role.\n\n**Procedure**\n\n1. Optional: To view the service accounts in the current project:\n\n   ```terminal\n   $ oc get sa\n   ```\n\n   ```terminal {title=\"Example output\"}\n   NAME       SECRETS   AGE\n   builder    1         2d\n   default    1         2d\n   deployer   1         2d\n   ```\n2. To create a new service account in the current project:\n\n   ```terminal\n   $ oc create sa <service_account_name>\n   ```\n\n   To create a service account in a different project, specify `-n <project_name>`.\n\n   ```terminal {title=\"Example output\"}\n   serviceaccount \"robot\" created\n   ```\n\n   > [!TIP]\n   > You can alternatively apply the following YAML to create the service account:\n   >\n   > ```yaml\n   > apiVersion: v1\n   > kind: ServiceAccount\n   > metadata:\n   >   name: <service_account_name>\n   >   namespace: <current_project>\n   > ```\n3. Optional: View the secrets for the service account:\n\n   ```terminal\n   $ oc describe sa robot\n   ```\n\n   ```terminal {title=\"Example output\"}\n   Name:                robot\n   Namespace:           project1\n   Labels:              <none>\n   Annotations:         openshift.io/internal-registry-pull-secret-ref: robot-dockercfg-qzbhb\n   Image pull secrets:  robot-dockercfg-qzbhb\n   Mountable secrets:   robot-dockercfg-qzbhb\n   Tokens:              <none>\n   Events:              <none>\n   ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/authentication/using-service-accounts-in-applications.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="using-service-accounts" class="group scroll-mt-20">Using service accounts in applications<a href="#using-service-accounts" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <h2 id="service-accounts-overview_using-service-accounts" class="group scroll-mt-20">Service accounts overview<a href="#service-accounts-overview_using-service-accounts" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can use OpenShift Container Platform service accounts to allow a OpenShift Container Platform component to directly access the API. </p>
      <p>Service accounts are API objects that exist within each project that provide a flexible way to control API access without sharing a regular user’s credentials.</p>
      <p>When you use the OpenShift Container Platform CLI or web console, your API token authenticates you to the API. You can associate a component with a service account so that they can access the API without using a regular user’s credentials.</p>
      <p>For example, service accounts can allow:</p>
      <ul>
      <li>Replication controllers to make API calls to create or delete pods</li>
      <li>Applications inside containers to make API calls for discovery purposes</li>
      <li>External applications to make API calls for monitoring or integration purposes</li>
      </ul>
      <p>Each service account’s user name is derived from its project and name:</p>
      <CodeBlock code={"system:serviceaccount:<project>:<name>"} lang="text" />
      <p>Every service account is also a member of two groups:</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Group</TableHead>
      <TableHead>Description</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>system:serviceaccounts</TableCell>
      <TableCell>Includes all service accounts in the system.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>system:serviceaccounts:&lt;project&gt;</TableCell>
      <TableCell>Includes all service accounts in the specified project.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="service-accounts-default_using-service-accounts" class="group scroll-mt-20">Default service accounts<a href="#service-accounts-default_using-service-accounts" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Your OpenShift Container Platform cluster contains default service accounts for cluster management and generates more service accounts for each project.</p>
      <h3 id="default-cluster-service-accounts_using-service-accounts" class="group scroll-mt-20">Default cluster service accounts<a href="#default-cluster-service-accounts_using-service-accounts" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Several infrastructure controllers run using service account credentials. The following service accounts are created in the OpenShift Container Platform infrastructure project (<code>openshift-infra</code>) at server start, and given the following roles cluster-wide:</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Service account</TableHead>
      <TableHead>Description</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>replication-controller</code></TableCell>
      <TableCell>Assigned the <code>system:replication-controller</code> role</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>deployment-controller</code></TableCell>
      <TableCell>Assigned the <code>system:deployment-controller</code> role</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>build-controller</code></TableCell>
      <TableCell>Assigned the <code>system:build-controller</code> role. Additionally, the <code>build-controller</code> service account is included in the privileged security context constraint to create privileged build pods.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h3 id="default-service-accounts-and-roles_using-service-accounts" class="group scroll-mt-20">Default project service accounts and roles<a href="#default-service-accounts-and-roles_using-service-accounts" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Three service accounts are automatically created in each project:</p>
      <Fragment set:html={"<table>\n<thead>\n<tr>\n  <th>Service account</th>\n  <th>Usage</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td><code>builder</code></td>\n  <td>Used by build pods. It is given the <code>system:image-builder</code> role, which allows pushing images to any imagestream in the project using the internal Docker registry.<br><br><dl class=\"db-admonition db-admonition-note\"><dt>Note</dt><dd>The <code>builder</code> service account is not created if the <code>Build</code> cluster capability is not enabled.</dd></dl></td>\n</tr>\n<tr>\n  <td><code>deployer</code></td>\n  <td>Used by deployment pods and given the <code>system:deployer</code> role, which allows viewing and modifying replication controllers and pods in the project.<br><br><dl class=\"db-admonition db-admonition-note\"><dt>Note</dt><dd>The <code>deployer</code> service account is not created if the <code>DeploymentConfig</code> cluster capability is not enabled.</dd></dl></td>\n</tr>\n<tr>\n  <td><code>default</code></td>\n  <td>Used to run all other pods unless they specify a different service account.<br><br><dl class=\"db-admonition db-admonition-important\"><dt>Important</dt><dd>Access rights and security privileges tied to the <code>default</code> service account apply to every pod in the project that does not specify a different service account. To implement the principle of least privilege and improve auditability, create dedicated service accounts for your workloads instead of using the <code>default</code> service account.<br><br>While most OpenShift Container Platform platform components and Operators use dedicated service accounts, the following dynamic tools continue to use the <code>default</code> service account to ensure operational efficiency:<br><br><ul><li><code>oc debug</code>: Uses the <code>default</code> service account to avoid the performance overhead of creating and removing unique service accounts for short-lived troubleshooting sessions.</li><li><code>oc adm must-gather</code>: Uses the <code>default</code> service account to collect diagnostic data across the cluster without requiring extensive manual RBAC modifications.</li></ul></dd></dl></td>\n</tr>\n</tbody>\n</table>\n"} />
      <p>All service accounts in a project are given the <code>system:image-puller</code> role, which allows pulling images from any image stream in the project using the internal container image registry.</p>
      <h3 id="auto-generated-sa-token-secrets_using-service-accounts" class="group scroll-mt-20">Automatically generated image pull secrets<a href="#auto-generated-sa-token-secrets_using-service-accounts" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">OpenShift Container Platform automatically creates image pull secrets for each service account to integrate the internal image registry with user authentication.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Prior to OpenShift Container Platform 4.16, a long-lived service account API token secret was also generated for each service account that was created. Starting with OpenShift Container Platform 4.16, this service account API token secret is no longer created.</p>
          <p>After upgrading to 4.22, any existing long-lived service account API token secrets are not deleted and will continue to function. For information about detecting long-lived API tokens that are in use in your cluster or deleting them if they are not needed, see "Long-lived service account API tokens in OpenShift Container Platform (Red Hat Knowledgebase)".</p>
        </AlertDescription>
      </Alert>
      <p>This image pull secret is necessary to integrate the OpenShift image registry into the cluster’s user authentication and authorization system.</p>
      <p>However, if you do not enable the <code>ImageRegistry</code> capability or if you disable the integrated OpenShift image registry in the Cluster Image Registry Operator’s configuration, an image pull secret is not generated for each service account.</p>
      <p>When the integrated OpenShift image registry is disabled on a cluster that previously had it enabled, the previously generated image pull secrets are deleted automatically.</p>
      <h2 id="service-accounts-managing_using-service-accounts" class="group scroll-mt-20">Creating service accounts<a href="#service-accounts-managing_using-service-accounts" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can create a service account in a project and grant it permissions by binding it to a role.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Optional: To view the service accounts in the current project:
      <CodeBlock code={"$ oc get sa"} lang="terminal" />
      <CodeBlock code={"NAME       SECRETS   AGE\nbuilder    1         2d\ndefault    1         2d\ndeployer   1         2d"} lang="terminal" title="Example output" />
      </li>
      <li>
      To create a new service account in the current project:
      <CodeBlock code={"$ oc create sa <service_account_name>"} lang="terminal" />
      <p>To create a service account in a different project, specify <code>-n &lt;project_name&gt;</code>.</p>
      <CodeBlock code={"serviceaccount \"robot\" created"} lang="terminal" title="Example output" />
      <Alert variant="tip" class="my-4">
        <AlertTitle>Tip</AlertTitle>
        <AlertDescription>
          <p>You can alternatively apply the following YAML to create the service account:</p>
          <CodeBlock code={"apiVersion: v1\nkind: ServiceAccount\nmetadata:\n  name: <service_account_name>\n  namespace: <current_project>"} lang="yaml" />
        </AlertDescription>
      </Alert>
      </li>
      <li>
      Optional: View the secrets for the service account:
      <CodeBlock code={"$ oc describe sa robot"} lang="terminal" />
      <CodeBlock code={"Name:                robot\nNamespace:           project1\nLabels:              <none>\nAnnotations:         openshift.io/internal-registry-pull-secret-ref: robot-dockercfg-qzbhb\nImage pull secrets:  robot-dockercfg-qzbhb\nMountable secrets:   robot-dockercfg-qzbhb\nTokens:              <none>\nEvents:              <none>"} lang="terminal" title="Example output" />
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
