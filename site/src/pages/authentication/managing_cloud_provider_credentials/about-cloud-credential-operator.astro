---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: authentication/managing_cloud_provider_credentials/about-cloud-credential-operator.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import BlockImage from "@ui/image/Image.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"about-cloud-credential-operator","text":"About the Cloud Credential Operator"},{"depth":2,"slug":"cco-mode-about_about-cloud-credential-operator","text":"About Cloud Credential Operator modes"},{"depth":2,"slug":"cco-determine-mode_about-cloud-credential-operator","text":"Determining the Cloud Credential Operator mode"},{"depth":3,"slug":"cco-determine-mode-gui_about-cloud-credential-operator","text":"Determining the Cloud Credential Operator mode by using the web console"},{"depth":3,"slug":"cco-determine-mode-cli_about-cloud-credential-operator","text":"Determining the Cloud Credential Operator mode by using the CLI"},{"depth":2,"slug":"cco-about-default-behaviors_about-cloud-credential-operator","text":"About the Cloud Credential Operator default behavior"},{"depth":2,"slug":"additional-resources_about-cloud-credential-operator_about-cloud-credential-operator","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/authentication/managing_cloud_provider_credentials/about-cloud-credential-operator";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "About the Cloud Credential Operator";
const description = "To allow OpenShift Container Platform components to request cloud provider credentials with the specific permissions that are required for the cluster to…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["authentication","authentication/managing_cloud_provider_credentials"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# About the Cloud Credential Operator {#about-cloud-credential-operator}\n\nTo allow OpenShift Container Platform components to request cloud provider credentials with the specific permissions that are required for the cluster to run, you can use the Cloud Credential Operator (CCO) to manage cloud provider credentials as custom resource definitions (CRDs).\n\nYou can configure the Cloud Credential Operator (CCO) to operate in several different modes. These options provide transparency and flexibility in how the CCO uses cloud credentials.\n\n## About Cloud Credential Operator modes {#cco-mode-about_about-cloud-credential-operator}\n\nYou can configure the Cloud Credential Operator (CCO) to operate in several different modes. These options provide transparency and flexibility in how the CCO uses cloud credentials to process `CredentialsRequest` CRs in the cluster to suit the security requirements of your organization.\n\nBy setting different values for the `credentialsMode` parameter in the `install-config.yaml` file, you can configure the CCO to operate in *mint*, *passthrough*, or *manual* mode.\n\n- ***Mint***: In mint mode, the CCO uses the provided admin-level cloud credential to create new credentials for components in the cluster with only the specific permissions that are required.\n- ***Passthrough***: In passthrough mode, the CCO passes the provided cloud credential to the components that request cloud credentials.\n- ***Manual mode with long-term credentials for components***: In manual mode, you can manage long-term cloud credentials instead of the CCO.\n- ***Manual mode with short-term credentials for components***: For some providers, you can use the CCO utility (`ccoctl`) during installation to implement short-term credentials for individual components. These credentials are created and managed outside the OpenShift Container Platform cluster.\n\nIf no mode is specified, or the `credentialsMode` parameter is set to an empty string (\"\"), the CCO operates in its default mode.\n\nNot all CCO modes are supported for all cloud providers, as described in the following table:\n\n**CCO mode support matrix**\n\n| Cloud provider | Mint | Passthrough | Manual with long-term credentials | Manual with short-term credentials |\n| --- | --- | --- | --- | --- |\n| Amazon Web Services (AWS) | X | X | X | X |\n| Global Microsoft Azure |  | X | X | X |\n| Microsoft Azure Stack Hub |  |  | X |  |\n| Google Cloud | X | X | X | X |\n| IBM Cloud(R) |  |  | X [^1]^ |  |\n| Nutanix |  |  | X [^2]^ |  |\n| Red Hat OpenStack Platform (RHOSP) |  | X |  |  |\n| VMware vSphere |  | X |  |  |\n\n1. This platform uses the `ccoctl` utility during installation to configure long-term credentials.\n\n## Determining the Cloud Credential Operator mode {#cco-determine-mode_about-cloud-credential-operator}\n\nFor platforms that support using the CCO in multiple modes, you can determine what mode the CCO is configured to use by using the web console or the CLI.\n\n**Figure 1. Determining the CCO configuration**\n\n![Decision tree showing how to determine the configured CCO credentials mode for your cluster.](/openshift-docs-markdown/_assets/images/334_OpenShift_cluster_updating_and_CCO_workflows_0923_4.11_A_AliCloud_patch.png)\n\n### Determining the Cloud Credential Operator mode by using the web console {#cco-determine-mode-gui_about-cloud-credential-operator}\n\nYou can determine what mode the Cloud Credential Operator (CCO) is configured to use by using the web console.\n\nBefore you perform upgrades or troubleshoot, ensure you understand your cluster’s credential management configuration.\n\n> [!NOTE]\n> Only Amazon Web Services (AWS), global Microsoft Azure, and Google Cloud clusters support multiple CCO modes.\n\n**Prerequisites**\n\n- You have access to an OpenShift Container Platform account with cluster administrator permissions.\n\n**Procedure**\n\n1. Log in to the OpenShift Container Platform web console as a user with the `cluster-admin` role.\n2. Navigate to **Administration** -> **Cluster Settings**.\n3. On the **Cluster Settings** page, select the **Configuration** tab.\n4. Under **Configuration resource**, select **CloudCredential**.\n5. On the **CloudCredential details** page, select the **YAML** tab.\n6. In the YAML block, check the value of `spec.credentialsMode`. The following values are possible, though not all are supported on all platforms:\n\n   - ’'\\`: The CCO is operating in the default mode. In this configuration, the CCO operates in mint or passthrough mode, depending on the credentials provided during installation.\n   - `Mint`: The CCO is operating in mint mode.\n   - `Passthrough`: The CCO is operating in passthrough mode.\n   - `Manual`: The CCO is operating in manual mode.\n\n   > [!IMPORTANT]\n   > To determine the specific configuration of an AWS, Google Cloud, or global Microsoft Azure cluster that has a `spec.credentialsMode` of ’'`, `Mint`, or `Manual\\`, you must investigate further.\n   >\n   > AWS and Google Cloud clusters support using mint mode with the root secret deleted.\n   >\n   > An AWS, Google Cloud, or global Microsoft Azure cluster that uses manual mode might be configured to create and manage cloud credentials from outside of the cluster with AWS STS, Google Cloud Workload Identity, or Microsoft Entra Workload ID. You can determine whether your cluster uses this strategy by examining the cluster `Authentication` object.\n7. AWS or Google Cloud clusters that use the default (’'\\`) only: To determine whether the cluster is operating in mint or passthrough mode, inspect the annotations on the cluster root secret:\n\n   1. Navigate to **Workloads** -> **Secrets** and look for the root secret for your cloud provider.\n\n      > [!NOTE]\n      > Ensure that the **Project** dropdown is set to **All Projects**.\n\n      | Platform | Secret name |\n      | --- | --- |\n      | AWS | `aws-creds` |\n      | Google Cloud | `gcp-credentials` |\n   2. To view the CCO mode that the cluster is using, click `1 annotation` under **Annotations**, and check the value field. The following values are possible:\n\n      - `Mint`: The CCO is operating in mint mode.\n      - `Passthrough`: The CCO is operating in passthrough mode.\n\n      If your cluster uses mint mode, you can also determine whether the cluster is operating without the root secret.\n8. AWS or Google Cloud clusters that use mint mode only: To determine whether the cluster is operating without the root secret, navigate to **Workloads** -> **Secrets** and look for the root secret for your cloud provider.\n\n   > [!NOTE]\n   > Ensure that the **Project** dropdown is set to **All Projects**.\n\n   | Platform | Secret name |\n   | --- | --- |\n   | AWS | `aws-creds` |\n   | Google Cloud | `gcp-credentials` |\n\n   - If you see one of these values, your cluster is using mint or passthrough mode with the root secret present.\n   - If you do not see these values, your cluster is using the CCO in mint mode with the root secret removed.\n9. AWS, Google Cloud, or global Microsoft Azure clusters that use manual mode only: To determine whether the cluster is configured to create and manage cloud credentials from outside of the cluster, you must check the cluster `Authentication` object YAML values.\n\n   1. Navigate to **Administration** -> **Cluster Settings**.\n   2. On the **Cluster Settings** page, select the **Configuration** tab.\n   3. Under **Configuration resource**, select **Authentication**.\n   4. On the **Authentication details** page, select the **YAML** tab.\n   5. In the YAML block, check the value of the `.spec.serviceAccountIssuer` parameter.\n\n      - A value that contains a URL that is associated with your cloud provider indicates that the CCO is using manual mode with short-term credentials for components. These clusters are configured using the `ccoctl` utility to create and manage cloud credentials from outside of the cluster.\n      - An empty value (’'`) indicates that the cluster is using the CCO in manual mode but was not configured using the `ccoctl\\` utility.\n\n### Determining the Cloud Credential Operator mode by using the CLI {#cco-determine-mode-cli_about-cloud-credential-operator}\n\nYou can determine what mode the Cloud Credential Operator (CCO) is configured to use by using the CLI.\n\nBefore you perform upgrades or troubleshoot, ensure you understand your cluster’s credential management configuration.\n\n> [!NOTE]\n> Only Amazon Web Services (AWS), global Microsoft Azure, and Google Cloud clusters support multiple CCO modes.\n\n**Prerequisites**\n\n- You have access to an OpenShift Container Platform account with cluster administrator permissions.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Log in to `oc` on the cluster as a user with the `cluster-admin` role.\n2. To determine the mode that the CCO is configured to use, enter the following command:\n\n   ```terminal\n   $ oc get cloudcredentials cluster \\\n     -o=jsonpath={.spec.credentialsMode}\n   ```\n\n   The following output values are possible, though not all are supported on all platforms:\n\n   - ’'\\`: The CCO is operating in the default mode. In this configuration, the CCO operates in mint or passthrough mode, depending on the credentials provided during installation.\n   - `Mint`: The CCO is operating in mint mode.\n   - `Passthrough`: The CCO is operating in passthrough mode.\n   - `Manual`: The CCO is operating in manual mode.\n\n   > [!IMPORTANT]\n   > To determine the specific configuration of an AWS, Google Cloud, or global Microsoft Azure cluster that has a `spec.credentialsMode` of ’'`, `Mint`, or `Manual\\`, you must investigate further.\n   >\n   > AWS and Google Cloud clusters support using mint mode with the root secret deleted.\n   >\n   > An AWS, Google Cloud, or global Microsoft Azure cluster that uses manual mode might be configured to create and manage cloud credentials from outside of the cluster with AWS STS, Google Cloud Workload Identity, or Microsoft Entra Workload ID. You can determine whether your cluster uses this strategy by examining the cluster `Authentication` object.\n3. AWS or Google Cloud clusters that use the default (’'\\`) only: To determine whether the cluster is operating in mint or passthrough mode, run the following command:\n\n   ```terminal\n   $ oc get secret <secret_name> \\\n     -n kube-system \\\n     -o jsonpath \\\n     --template '{ .metadata.annotations }'\n   ```\n\n   where `<secret_name>` is `aws-creds` for AWS or `gcp-credentials` for Google Cloud.\n\n   This command displays the value of the `.metadata.annotations` parameter in the cluster root secret object. The following output values are possible:\n\n   - `Mint`: The CCO is operating in mint mode.\n   - `Passthrough`: The CCO is operating in passthrough mode.\n\n   If your cluster uses mint mode, you can also determine whether the cluster is operating without the root secret.\n4. AWS or Google Cloud clusters that use mint mode only: To determine whether the cluster is operating without the root secret, run the following command:\n\n   ```terminal\n   $ oc get secret <secret_name> \\\n     -n=kube-system\n   ```\n\n   where `<secret_name>` is `aws-creds` for AWS or `gcp-credentials` for Google Cloud.\n\n   If the root secret is present, the output of this command returns information about the secret. An error indicates that the root secret is not present on the cluster.\n5. AWS, Google Cloud, or global Microsoft Azure clusters that use manual mode only: To determine whether the cluster is configured to create and manage cloud credentials from outside of the cluster, run the following command:\n\n   ```terminal\n   $ oc get authentication cluster \\\n     -o jsonpath \\\n     --template='{ .spec.serviceAccountIssuer }'\n   ```\n\n   This command displays the value of the `.spec.serviceAccountIssuer` parameter in the cluster `Authentication` object.\n\n   - An output of a URL that is associated with your cloud provider indicates that the CCO is using manual mode with short-term credentials for components. These clusters are configured using the `ccoctl` utility to create and manage cloud credentials from outside of the cluster.\n   - An empty output indicates that the cluster is using the CCO in manual mode but was not configured using the `ccoctl` utility.\n\n## About the Cloud Credential Operator default behavior {#cco-about-default-behaviors_about-cloud-credential-operator}\n\nTo better manage cloud credentials, you should familiarize yourself with the default behaviors of the Cloud Credential Operator.\n\nFor platforms on which multiple modes are supported (AWS, Azure, and Google Cloud), when the CCO operates in its default mode, it checks the provided credentials dynamically to determine for which mode they are sufficient to process `CredentialsRequest` CRs.\n\nBy default, the CCO determines whether the credentials are sufficient for mint mode, which is the preferred mode of operation, and uses those credentials to create appropriate credentials for components in the cluster. If the credentials are not sufficient for mint mode, it determines whether they are sufficient for passthrough mode. If the credentials are not sufficient for passthrough mode, the CCO cannot adequately process `CredentialsRequest` CRs.\n\nIf the provided credentials are determined to be insufficient during installation, the installation fails. For AWS, the installation program fails early in the process and indicates which required permissions are missing. Other providers might not provide specific information about the cause of the error until errors are encountered.\n\nIf the credentials are changed after a successful installation and the CCO determines that the new credentials are insufficient, the CCO puts conditions on any new `CredentialsRequest` CRs to indicate that it cannot process them because of the insufficient credentials.\n\nTo resolve insufficient credentials issues, provide a credential with sufficient permissions. If an error occurred during installation, try installing again. For issues with new `CredentialsRequest` CRs, wait for the CCO to try to process the CR again. As an alternative, you can configure your cluster to use a different CCO mode that is supported for your cloud provider.\n\n## Additional resources {#additional-resources_about-cloud-credential-operator_about-cloud-credential-operator}\n\n- [Cluster Operators reference page for the Cloud Credential Operator](/openshift-docs-markdown/operators/operator-reference#cloud-credential-operator_operator-reference)\n- [About the Cloud Credential Operator in mint mode](/openshift-docs-markdown/authentication/managing_cloud_provider_credentials/cco-mode-mint#cco-mode-mint)\n- [About the Cloud Credential Operator in passthrough mode](/openshift-docs-markdown/authentication/managing_cloud_provider_credentials/cco-mode-passthrough#cco-mode-passthrough)\n- [About the Cloud Credential Operator in manual mode with long-term credentials for components](/openshift-docs-markdown/authentication/managing_cloud_provider_credentials/cco-mode-manual#cco-mode-manual)\n- [About the Cloud Credential Operator in manual mode with short-term credentials for components](/openshift-docs-markdown/authentication/managing_cloud_provider_credentials/cco-short-term-creds#cco-short-term-creds)\n\n[^1]: 1\n\n[^2]: 1\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/authentication/managing_cloud_provider_credentials/about-cloud-credential-operator.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="about-cloud-credential-operator" class="group scroll-mt-20">About the Cloud Credential Operator<a href="#about-cloud-credential-operator" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>To allow OpenShift Container Platform components to request cloud provider credentials with the specific permissions that are required for the cluster to run, you can use the Cloud Credential Operator (CCO) to manage cloud provider credentials as custom resource definitions (CRDs).</p>
      <p>You can configure the Cloud Credential Operator (CCO) to operate in several different modes. These options provide transparency and flexibility in how the CCO uses cloud credentials.</p>
      <h2 id="cco-mode-about_about-cloud-credential-operator" class="group scroll-mt-20">About Cloud Credential Operator modes<a href="#cco-mode-about_about-cloud-credential-operator" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can configure the Cloud Credential Operator (CCO) to operate in several different modes. These options provide transparency and flexibility in how the CCO uses cloud credentials to process <code>CredentialsRequest</code> CRs in the cluster to suit the security requirements of your organization.</p>
      <p>By setting different values for the <code>credentialsMode</code> parameter in the <code>install-config.yaml</code> file, you can configure the CCO to operate in <em>mint</em>, <em>passthrough</em>, or <em>manual</em> mode.</p>
      <ul>
      <li><em></em><em><strong>Mint</strong></em><em></em>: In mint mode, the CCO uses the provided admin-level cloud credential to create new credentials for components in the cluster with only the specific permissions that are required.</li>
      <li><em></em><em><strong>Passthrough</strong></em><em></em>: In passthrough mode, the CCO passes the provided cloud credential to the components that request cloud credentials.</li>
      <li><em></em><em><strong>Manual mode with long-term credentials for components</strong></em><em></em>: In manual mode, you can manage long-term cloud credentials instead of the CCO.</li>
      <li><em></em><em><strong>Manual mode with short-term credentials for components</strong></em><em></em>: For some providers, you can use the CCO utility (<code>ccoctl</code>) during installation to implement short-term credentials for individual components. These credentials are created and managed outside the OpenShift Container Platform cluster.</li>
      </ul>
      <p>If no mode is specified, or the <code>credentialsMode</code> parameter is set to an empty string (""), the CCO operates in its default mode.</p>
      <p>Not all CCO modes are supported for all cloud providers, as described in the following table:</p>
      <p><strong>CCO mode support matrix</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Cloud provider</TableHead>
      <TableHead>Mint</TableHead>
      <TableHead>Passthrough</TableHead>
      <TableHead>Manual with long-term credentials</TableHead>
      <TableHead>Manual with short-term credentials</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>Amazon Web Services (AWS)</TableCell>
      <TableCell>X</TableCell>
      <TableCell>X</TableCell>
      <TableCell>X</TableCell>
      <TableCell>X</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Global Microsoft Azure</TableCell>
      <TableCell></TableCell>
      <TableCell>X</TableCell>
      <TableCell>X</TableCell>
      <TableCell>X</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Microsoft Azure Stack Hub</TableCell>
      <TableCell></TableCell>
      <TableCell></TableCell>
      <TableCell>X</TableCell>
      <TableCell></TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Google Cloud</TableCell>
      <TableCell>X</TableCell>
      <TableCell>X</TableCell>
      <TableCell>X</TableCell>
      <TableCell>X</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>IBM Cloud(R)</TableCell>
      <TableCell></TableCell>
      <TableCell></TableCell>
      <TableCell>X <sup><a href="#fn-1" id="fnref-1" class="text-primary hover:underline">[1]</a></sup>^</TableCell>
      <TableCell></TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Nutanix</TableCell>
      <TableCell></TableCell>
      <TableCell></TableCell>
      <TableCell>X <sup><a href="#fn-2" id="fnref-2" class="text-primary hover:underline">[2]</a></sup>^</TableCell>
      <TableCell></TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Red Hat OpenStack Platform (RHOSP)</TableCell>
      <TableCell></TableCell>
      <TableCell>X</TableCell>
      <TableCell></TableCell>
      <TableCell></TableCell>
      </TableRow>
      <TableRow>
      <TableCell>VMware vSphere</TableCell>
      <TableCell></TableCell>
      <TableCell>X</TableCell>
      <TableCell></TableCell>
      <TableCell></TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <ol><li>This platform uses the <code>ccoctl</code> utility during installation to configure long-term credentials.</li></ol>
      <h2 id="cco-determine-mode_about-cloud-credential-operator" class="group scroll-mt-20">Determining the Cloud Credential Operator mode<a href="#cco-determine-mode_about-cloud-credential-operator" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">For platforms that support using the CCO in multiple modes, you can determine what mode the CCO is configured to use by using the web console or the CLI.</p>
      <p><strong>Figure 1. Determining the CCO configuration</strong></p>
      <BlockImage src="/openshift-docs-markdown/_assets/images/334_OpenShift_cluster_updating_and_CCO_workflows_0923_4.11_A_AliCloud_patch.png" alt="Decision tree showing how to determine the configured CCO credentials mode for your cluster." />
      <h3 id="cco-determine-mode-gui_about-cloud-credential-operator" class="group scroll-mt-20">Determining the Cloud Credential Operator mode by using the web console<a href="#cco-determine-mode-gui_about-cloud-credential-operator" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can determine what mode the Cloud Credential Operator (CCO) is configured to use by using the web console.</p>
      <p>Before you perform upgrades or troubleshoot, ensure you understand your cluster’s credential management configuration.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Only Amazon Web Services (AWS), global Microsoft Azure, and Google Cloud clusters support multiple CCO modes.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have access to an OpenShift Container Platform account with cluster administrator permissions.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Log in to the OpenShift Container Platform web console as a user with the <code>cluster-admin</code> role.</li>
      <li>Navigate to <strong>Administration</strong> -&gt; <strong>Cluster Settings</strong>.</li>
      <li>On the <strong>Cluster Settings</strong> page, select the <strong>Configuration</strong> tab.</li>
      <li>Under <strong>Configuration resource</strong>, select <strong>CloudCredential</strong>.</li>
      <li>On the <strong>CloudCredential details</strong> page, select the <strong>YAML</strong> tab.</li>
      <li>
      In the YAML block, check the value of <code>spec.credentialsMode</code>. The following values are possible, though not all are supported on all platforms:
      <ul>
      <li>’'`: The CCO is operating in the default mode. In this configuration, the CCO operates in mint or passthrough mode, depending on the credentials provided during installation.</li>
      <li><code>Mint</code>: The CCO is operating in mint mode.</li>
      <li><code>Passthrough</code>: The CCO is operating in passthrough mode.</li>
      <li><code>Manual</code>: The CCO is operating in manual mode.</li>
      </ul>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>To determine the specific configuration of an AWS, Google Cloud, or global Microsoft Azure cluster that has a <code>spec.credentialsMode</code> of ’'<code>, </code>Mint<code>, or </code>Manual`, you must investigate further.</p>
          <p>AWS and Google Cloud clusters support using mint mode with the root secret deleted.</p>
          <p>An AWS, Google Cloud, or global Microsoft Azure cluster that uses manual mode might be configured to create and manage cloud credentials from outside of the cluster with AWS STS, Google Cloud Workload Identity, or Microsoft Entra Workload ID. You can determine whether your cluster uses this strategy by examining the cluster <code>Authentication</code> object.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      AWS or Google Cloud clusters that use the default (’'`) only: To determine whether the cluster is operating in mint or passthrough mode, inspect the annotations on the cluster root secret:
      <ol>
      <li>
      Navigate to <strong>Workloads</strong> -&gt; <strong>Secrets</strong> and look for the root secret for your cloud provider.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Ensure that the <strong>Project</strong> dropdown is set to <strong>All Projects</strong>.</p>
        </AlertDescription>
      </Alert>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Platform</TableHead>
      <TableHead>Secret name</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>AWS</TableCell>
      <TableCell><code>aws-creds</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Google Cloud</TableCell>
      <TableCell><code>gcp-credentials</code></TableCell>
      </TableRow>
      </TableBody>
      </Table>
      </li>
      <li>
      To view the CCO mode that the cluster is using, click <code>1 annotation</code> under <strong>Annotations</strong>, and check the value field. The following values are possible:
      <ul>
      <li><code>Mint</code>: The CCO is operating in mint mode.</li>
      <li><code>Passthrough</code>: The CCO is operating in passthrough mode.</li>
      </ul>
      <p>If your cluster uses mint mode, you can also determine whether the cluster is operating without the root secret.</p>
      </li>
      </ol>
      </li>
      <li>
      AWS or Google Cloud clusters that use mint mode only: To determine whether the cluster is operating without the root secret, navigate to <strong>Workloads</strong> -&gt; <strong>Secrets</strong> and look for the root secret for your cloud provider.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Ensure that the <strong>Project</strong> dropdown is set to <strong>All Projects</strong>.</p>
        </AlertDescription>
      </Alert>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Platform</TableHead>
      <TableHead>Secret name</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>AWS</TableCell>
      <TableCell><code>aws-creds</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Google Cloud</TableCell>
      <TableCell><code>gcp-credentials</code></TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <ul>
      <li>If you see one of these values, your cluster is using mint or passthrough mode with the root secret present.</li>
      <li>If you do not see these values, your cluster is using the CCO in mint mode with the root secret removed.</li>
      </ul>
      </li>
      <li>
      AWS, Google Cloud, or global Microsoft Azure clusters that use manual mode only: To determine whether the cluster is configured to create and manage cloud credentials from outside of the cluster, you must check the cluster <code>Authentication</code> object YAML values.
      <ol>
      <li>Navigate to <strong>Administration</strong> -&gt; <strong>Cluster Settings</strong>.</li>
      <li>On the <strong>Cluster Settings</strong> page, select the <strong>Configuration</strong> tab.</li>
      <li>Under <strong>Configuration resource</strong>, select <strong>Authentication</strong>.</li>
      <li>On the <strong>Authentication details</strong> page, select the <strong>YAML</strong> tab.</li>
      <li>
      In the YAML block, check the value of the <code>.spec.serviceAccountIssuer</code> parameter.
      <ul>
      <li>A value that contains a URL that is associated with your cloud provider indicates that the CCO is using manual mode with short-term credentials for components. These clusters are configured using the <code>ccoctl</code> utility to create and manage cloud credentials from outside of the cluster.</li>
      <li>An empty value (’'<code>) indicates that the cluster is using the CCO in manual mode but was not configured using the </code>ccoctl` utility.</li>
      </ul>
      </li>
      </ol>
      </li>
      </ol>
      <h3 id="cco-determine-mode-cli_about-cloud-credential-operator" class="group scroll-mt-20">Determining the Cloud Credential Operator mode by using the CLI<a href="#cco-determine-mode-cli_about-cloud-credential-operator" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can determine what mode the Cloud Credential Operator (CCO) is configured to use by using the CLI.</p>
      <p>Before you perform upgrades or troubleshoot, ensure you understand your cluster’s credential management configuration.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Only Amazon Web Services (AWS), global Microsoft Azure, and Google Cloud clusters support multiple CCO modes.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to an OpenShift Container Platform account with cluster administrator permissions.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Log in to <code>oc</code> on the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>
      To determine the mode that the CCO is configured to use, enter the following command:
      <CodeBlock code={"$ oc get cloudcredentials cluster \\\n  -o=jsonpath={.spec.credentialsMode}"} lang="terminal" />
      <p>The following output values are possible, though not all are supported on all platforms:</p>
      <ul>
      <li>’'`: The CCO is operating in the default mode. In this configuration, the CCO operates in mint or passthrough mode, depending on the credentials provided during installation.</li>
      <li><code>Mint</code>: The CCO is operating in mint mode.</li>
      <li><code>Passthrough</code>: The CCO is operating in passthrough mode.</li>
      <li><code>Manual</code>: The CCO is operating in manual mode.</li>
      </ul>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>To determine the specific configuration of an AWS, Google Cloud, or global Microsoft Azure cluster that has a <code>spec.credentialsMode</code> of ’'<code>, </code>Mint<code>, or </code>Manual`, you must investigate further.</p>
          <p>AWS and Google Cloud clusters support using mint mode with the root secret deleted.</p>
          <p>An AWS, Google Cloud, or global Microsoft Azure cluster that uses manual mode might be configured to create and manage cloud credentials from outside of the cluster with AWS STS, Google Cloud Workload Identity, or Microsoft Entra Workload ID. You can determine whether your cluster uses this strategy by examining the cluster <code>Authentication</code> object.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      AWS or Google Cloud clusters that use the default (’'`) only: To determine whether the cluster is operating in mint or passthrough mode, run the following command:
      <CodeBlock code={"$ oc get secret <secret_name> \\\n  -n kube-system \\\n  -o jsonpath \\\n  --template '{ .metadata.annotations }'"} lang="terminal" />
      <p>where <code>&lt;secret_name&gt;</code> is <code>aws-creds</code> for AWS or <code>gcp-credentials</code> for Google Cloud.</p>
      <p>This command displays the value of the <code>.metadata.annotations</code> parameter in the cluster root secret object. The following output values are possible:</p>
      <ul>
      <li><code>Mint</code>: The CCO is operating in mint mode.</li>
      <li><code>Passthrough</code>: The CCO is operating in passthrough mode.</li>
      </ul>
      <p>If your cluster uses mint mode, you can also determine whether the cluster is operating without the root secret.</p>
      </li>
      <li>
      AWS or Google Cloud clusters that use mint mode only: To determine whether the cluster is operating without the root secret, run the following command:
      <CodeBlock code={"$ oc get secret <secret_name> \\\n  -n=kube-system"} lang="terminal" />
      <p>where <code>&lt;secret_name&gt;</code> is <code>aws-creds</code> for AWS or <code>gcp-credentials</code> for Google Cloud.</p>
      <p>If the root secret is present, the output of this command returns information about the secret. An error indicates that the root secret is not present on the cluster.</p>
      </li>
      <li>
      AWS, Google Cloud, or global Microsoft Azure clusters that use manual mode only: To determine whether the cluster is configured to create and manage cloud credentials from outside of the cluster, run the following command:
      <CodeBlock code={"$ oc get authentication cluster \\\n  -o jsonpath \\\n  --template='{ .spec.serviceAccountIssuer }'"} lang="terminal" />
      <p>This command displays the value of the <code>.spec.serviceAccountIssuer</code> parameter in the cluster <code>Authentication</code> object.</p>
      <ul>
      <li>An output of a URL that is associated with your cloud provider indicates that the CCO is using manual mode with short-term credentials for components. These clusters are configured using the <code>ccoctl</code> utility to create and manage cloud credentials from outside of the cluster.</li>
      <li>An empty output indicates that the cluster is using the CCO in manual mode but was not configured using the <code>ccoctl</code> utility.</li>
      </ul>
      </li>
      </ol>
      <h2 id="cco-about-default-behaviors_about-cloud-credential-operator" class="group scroll-mt-20">About the Cloud Credential Operator default behavior<a href="#cco-about-default-behaviors_about-cloud-credential-operator" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To better manage cloud credentials, you should familiarize yourself with the default behaviors of the Cloud Credential Operator.</p>
      <p>For platforms on which multiple modes are supported (AWS, Azure, and Google Cloud), when the CCO operates in its default mode, it checks the provided credentials dynamically to determine for which mode they are sufficient to process <code>CredentialsRequest</code> CRs.</p>
      <p>By default, the CCO determines whether the credentials are sufficient for mint mode, which is the preferred mode of operation, and uses those credentials to create appropriate credentials for components in the cluster. If the credentials are not sufficient for mint mode, it determines whether they are sufficient for passthrough mode. If the credentials are not sufficient for passthrough mode, the CCO cannot adequately process <code>CredentialsRequest</code> CRs.</p>
      <p>If the provided credentials are determined to be insufficient during installation, the installation fails. For AWS, the installation program fails early in the process and indicates which required permissions are missing. Other providers might not provide specific information about the cause of the error until errors are encountered.</p>
      <p>If the credentials are changed after a successful installation and the CCO determines that the new credentials are insufficient, the CCO puts conditions on any new <code>CredentialsRequest</code> CRs to indicate that it cannot process them because of the insufficient credentials.</p>
      <p>To resolve insufficient credentials issues, provide a credential with sufficient permissions. If an error occurred during installation, try installing again. For issues with new <code>CredentialsRequest</code> CRs, wait for the CCO to try to process the CR again. As an alternative, you can configure your cluster to use a different CCO mode that is supported for your cloud provider.</p>
      <h2 id="additional-resources_about-cloud-credential-operator_about-cloud-credential-operator" class="group scroll-mt-20">Additional resources<a href="#additional-resources_about-cloud-credential-operator_about-cloud-credential-operator" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/operators/operator-reference#cloud-credential-operator_operator-reference">Cluster Operators reference page for the Cloud Credential Operator</a></li>
      <li><a href="/openshift-docs-markdown/authentication/managing_cloud_provider_credentials/cco-mode-mint#cco-mode-mint">About the Cloud Credential Operator in mint mode</a></li>
      <li><a href="/openshift-docs-markdown/authentication/managing_cloud_provider_credentials/cco-mode-passthrough#cco-mode-passthrough">About the Cloud Credential Operator in passthrough mode</a></li>
      <li><a href="/openshift-docs-markdown/authentication/managing_cloud_provider_credentials/cco-mode-manual#cco-mode-manual">About the Cloud Credential Operator in manual mode with long-term credentials for components</a></li>
      <li><a href="/openshift-docs-markdown/authentication/managing_cloud_provider_credentials/cco-short-term-creds#cco-short-term-creds">About the Cloud Credential Operator in manual mode with short-term credentials for components</a></li>
      </ul>
      <hr class="my-8 border-t border-border" data-footnotes-separator />
      <section class="footnotes" data-footnotes aria-labelledby="footnote-label">
        <h2 id="footnote-label" class="sr-only">Footnotes</h2>
        <ol class="list-decimal pl-6 space-y-1 text-sm text-muted-foreground" data-footnote-list>
      <li id="fn-1" class="text-sm text-muted-foreground">1 <a href="#fnref-1" class="text-primary hover:underline">↩</a></li>
      <li id="fn-2" class="text-sm text-muted-foreground">1 <a href="#fnref-2" class="text-primary hover:underline">↩</a></li>
        </ol>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
