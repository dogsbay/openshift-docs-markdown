---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: authentication/identity_providers/configuring-google-identity-provider.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"configuring-google-identity-provider","text":"Configuring a Google identity provider"},{"depth":2,"slug":"identity-provider-overview_configuring-google-identity-provider","text":"Identity providers in OpenShift Container Platform"},{"depth":2,"slug":"identity-provider-google-about_configuring-google-identity-provider","text":"Google authentication"},{"depth":2,"slug":"identity-provider-creating-secret_configuring-google-identity-provider","text":"Creating the secret"},{"depth":2,"slug":"identity-provider-google-CR_configuring-google-identity-provider","text":"Sample Google custom resource"},{"depth":2,"slug":"add-identity-provider_configuring-google-identity-provider","text":"Adding an identity provider to your cluster"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/authentication/identity_providers/configuring-google-identity-provider";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring a Google identity provider";
const description = "Configure a Google identity provider so users can authenticate to OpenShift Container Platform with Google accounts. When configured, sign-in is permitted…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["authentication","authentication/identity_providers"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring a Google identity provider {#configuring-google-identity-provider}\n\nConfigure a Google identity provider so users can authenticate to OpenShift Container Platform with Google accounts. When configured, sign-in is permitted only for Google accounts in that hosted domain.\n\n## Identity providers in OpenShift Container Platform {#identity-provider-overview_configuring-google-identity-provider}\n\nYou can configure identity providers by creating a custom resource (CR) that describes the provider and adding it to the cluster. Identity providers enable user authentication in OpenShift Container Platform beyond the default `kubeadmin` user.\n\n> [!NOTE]\n> OpenShift Container Platform usernames containing `/`, `:`, and `%` are not supported.\n\n## Google authentication {#identity-provider-google-about_configuring-google-identity-provider}\n\nBy using Google as an identity provider, you can authenticate to your server. You can use the `hostedDomain` configuration attribute to limit authentication to members of a specific hosted domain.\n\nGoogle authentication uses OpenID Connect through the cluster OAuth server.\n\n> [!NOTE]\n> Using Google as an identity provider requires users to get a token using `<namespace_route>/oauth/token/request` to use with command-line tools.\n\n## Creating the secret {#identity-provider-creating-secret_configuring-google-identity-provider}\n\nCreate a `Secret` object in the `openshift-config` namespace to store the client secret and related credentials for the identity provider configuration.\n\n**Procedure**\n\n1. Create a `Secret` object containing the client secret by running the following command:\n\n   ```terminal\n   $ oc create secret generic <secret_name> --from-literal=clientSecret=<secret> -n openshift-config\n   ```\n2. Optional: Apply the following YAML to create the secret:\n\n   ```yaml\n   apiVersion: v1\n   kind: Secret\n   metadata:\n     name: <secret_name>\n     namespace: openshift-config\n   type: Opaque\n   data:\n     clientSecret: <base64_encoded_client_secret>\n   ```\n3. Create a `Secret` object from a file by running the following command:\n\n   ```terminal\n   $ oc create secret generic <secret_name> --from-file=<path_to_file> -n openshift-config\n   ```\n\n## Sample Google custom resource {#identity-provider-google-CR_configuring-google-identity-provider}\n\nReview the custom resource (CR) fields and acceptable values for configuring a Google identity provider in OpenShift Container Platform. Use these definitions to set client credentials and hosted domain restrictions before applying the configuration to the cluster.\n\n```yaml\napiVersion: config.openshift.io/v1\nkind: OAuth\nmetadata:\n  name: cluster\nspec:\n  identityProviders:\n  - name: googleidp\n    mappingMethod: claim\n    type: Google\n    google:\n      clientID: {...}\n      clientSecret:\n        name: google-secret\n      hostedDomain: \"example.com\"\n```\n\nwhere:\n\n`spec.identityProviders.name`\n:   Specifies the provider name, which is prefixed to the Google numeric user ID to form an identity name. The provider name is also used to build the redirect URL.\n\n`spec.identityProviders.mappingMethod`\n:   Specifies how mappings are established between identities from this provider and `User` objects.\n\n`spec.identityProviders.google.clientID`\n:   Specifies the client ID from the Google Cloud project where you create the OAuth client. The project must be configured with a redirect URI of `https://oauth-openshift.apps.<cluster-name>.<cluster-domain>/oauth2callback/<idp-provider-name>`.\n\n`spec.identityProviders.google.clientSecret`\n:   Specifies a reference to an OpenShift Container Platform `Secret` object containing the client secret issued by Google.\n\n`spec.identityProviders.google.hostedDomain`\n:   Specifies a hosted domain used to restrict sign-in accounts. Optional if the `lookup` `mappingMethod` is used. If empty, any Google account is allowed to authenticate.\n\n**Additional resources**\n{._additional-resources}\n\n- [Identity provider parameters](/openshift-docs-markdown/authentication/understanding-identity-provider#identity-provider-parameters_understanding-identity-provider)\n\n## Adding an identity provider to your cluster {#add-identity-provider_configuring-google-identity-provider}\n\nApply the identity provider custom resource (CR) to your cluster so users can authenticate with the configured identity provider.\n\n**Prerequisites**\n\n- You installed an OpenShift Container Platform cluster.\n- You defined the CR for your identity provider.\n- You are logged in as an administrator.\n\n**Procedure**\n\n1. Apply the defined CR by running the following command:\n\n   ```terminal\n   $ oc apply -f </path/to/CR>\n   ```\n\n   > [!NOTE]\n   > If a CR does not exist, `oc apply` creates a new CR and might trigger the following warning: `Warning: oc apply should be used on resources created by either oc create --save-config or oc apply`. In this case you can safely ignore this warning.\n2. Obtain a token from the OAuth server.\n\n   As long as the `kubeadmin` user has been removed, the `oc login` command provides instructions on how to access a web page where you can retrieve the token.\n\n   You can also access this page from the web console by navigating to **(?) Help** → **Command Line Tools** → **Copy Login Command**.\n3. Log in to the cluster, passing in the token to authenticate, by running the following command:\n\n   ```terminal\n   $ oc login --token=<token>\n   ```\n\n   > [!NOTE]\n   > This identity provider does not support logging in with a username and password.\n4. Confirm that the user logged in successfully and that the username displays by running the following command:\n\n   ```terminal\n   $ oc whoami\n   ```\n\n**Additional resources**\n{._additional-resources}\n\n- [OpenID Connect (Google Identity documentation)](https://developers.google.com/identity/protocols/OpenIDConnect)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/authentication/identity_providers/configuring-google-identity-provider.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="configuring-google-identity-provider" class="group scroll-mt-20">Configuring a Google identity provider<a href="#configuring-google-identity-provider" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Configure a Google identity provider so users can authenticate to OpenShift Container Platform with Google accounts. When configured, sign-in is permitted only for Google accounts in that hosted domain.</p>
      <h2 id="identity-provider-overview_configuring-google-identity-provider" class="group scroll-mt-20">Identity providers in OpenShift Container Platform<a href="#identity-provider-overview_configuring-google-identity-provider" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can configure identity providers by creating a custom resource (CR) that describes the provider and adding it to the cluster. Identity providers enable user authentication in OpenShift Container Platform beyond the default <code>kubeadmin</code> user.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>OpenShift Container Platform usernames containing <code>/</code>, <code>:</code>, and <code>%</code> are not supported.</p>
        </AlertDescription>
      </Alert>
      <h2 id="identity-provider-google-about_configuring-google-identity-provider" class="group scroll-mt-20">Google authentication<a href="#identity-provider-google-about_configuring-google-identity-provider" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">By using Google as an identity provider, you can authenticate to your server. You can use the <code>hostedDomain</code> configuration attribute to limit authentication to members of a specific hosted domain.</p>
      <p>Google authentication uses OpenID Connect through the cluster OAuth server.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Using Google as an identity provider requires users to get a token using <code>&lt;namespace_route&gt;/oauth/token/request</code> to use with command-line tools.</p>
        </AlertDescription>
      </Alert>
      <h2 id="identity-provider-creating-secret_configuring-google-identity-provider" class="group scroll-mt-20">Creating the secret<a href="#identity-provider-creating-secret_configuring-google-identity-provider" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Create a <code>Secret</code> object in the <code>openshift-config</code> namespace to store the client secret and related credentials for the identity provider configuration.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a <code>Secret</code> object containing the client secret by running the following command:
      <CodeBlock code={"$ oc create secret generic <secret_name> --from-literal=clientSecret=<secret> -n openshift-config"} lang="terminal" />
      </li>
      <li>
      Optional: Apply the following YAML to create the secret:
      <CodeBlock code={"apiVersion: v1\nkind: Secret\nmetadata:\n  name: <secret_name>\n  namespace: openshift-config\ntype: Opaque\ndata:\n  clientSecret: <base64_encoded_client_secret>"} lang="yaml" />
      </li>
      <li>
      Create a <code>Secret</code> object from a file by running the following command:
      <CodeBlock code={"$ oc create secret generic <secret_name> --from-file=<path_to_file> -n openshift-config"} lang="terminal" />
      </li>
      </ol>
      <h2 id="identity-provider-google-CR_configuring-google-identity-provider" class="group scroll-mt-20">Sample Google custom resource<a href="#identity-provider-google-CR_configuring-google-identity-provider" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Review the custom resource (CR) fields and acceptable values for configuring a Google identity provider in OpenShift Container Platform. Use these definitions to set client credentials and hosted domain restrictions before applying the configuration to the cluster.</p>
      <CodeBlock code={"apiVersion: config.openshift.io/v1\nkind: OAuth\nmetadata:\n  name: cluster\nspec:\n  identityProviders:\n  - name: googleidp\n    mappingMethod: claim\n    type: Google\n    google:\n      clientID: {...}\n      clientSecret:\n        name: google-secret\n      hostedDomain: \"example.com\""} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.identityProviders.name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the provider name, which is prefixed to the Google numeric user ID to form an identity name. The provider name is also used to build the redirect URL.</p></dd>
      <dt class="font-semibold"><code>spec.identityProviders.mappingMethod</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies how mappings are established between identities from this provider and <code>User</code> objects.</p></dd>
      <dt class="font-semibold"><code>spec.identityProviders.google.clientID</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the client ID from the Google Cloud project where you create the OAuth client. The project must be configured with a redirect URI of <code>https://oauth-openshift.apps.&lt;cluster-name&gt;.&lt;cluster-domain&gt;/oauth2callback/&lt;idp-provider-name&gt;</code>.</p></dd>
      <dt class="font-semibold"><code>spec.identityProviders.google.clientSecret</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a reference to an OpenShift Container Platform <code>Secret</code> object containing the client secret issued by Google.</p></dd>
      <dt class="font-semibold"><code>spec.identityProviders.google.hostedDomain</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a hosted domain used to restrict sign-in accounts. Optional if the <code>lookup</code> <code>mappingMethod</code> is used. If empty, any Google account is allowed to authenticate.</p></dd>
      </dl>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="/openshift-docs-markdown/authentication/understanding-identity-provider#identity-provider-parameters_understanding-identity-provider">Identity provider parameters</a></li></ul>
      </section>
      <h2 id="add-identity-provider_configuring-google-identity-provider" class="group scroll-mt-20">Adding an identity provider to your cluster<a href="#add-identity-provider_configuring-google-identity-provider" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Apply the identity provider custom resource (CR) to your cluster so users can authenticate with the configured identity provider.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You installed an OpenShift Container Platform cluster.</li>
      <li>You defined the CR for your identity provider.</li>
      <li>You are logged in as an administrator.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Apply the defined CR by running the following command:
      <CodeBlock code={"$ oc apply -f </path/to/CR>"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If a CR does not exist, <code>oc apply</code> creates a new CR and might trigger the following warning: <code>Warning: oc apply should be used on resources created by either oc create --save-config or oc apply</code>. In this case you can safely ignore this warning.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      Obtain a token from the OAuth server.
      <p>As long as the <code>kubeadmin</code> user has been removed, the <code>oc login</code> command provides instructions on how to access a web page where you can retrieve the token.</p>
      <p>You can also access this page from the web console by navigating to <strong>(?) Help</strong> → <strong>Command Line Tools</strong> → <strong>Copy Login Command</strong>.</p>
      </li>
      <li>
      Log in to the cluster, passing in the token to authenticate, by running the following command:
      <CodeBlock code={"$ oc login --token=<token>"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>This identity provider does not support logging in with a username and password.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      Confirm that the user logged in successfully and that the username displays by running the following command:
      <CodeBlock code={"$ oc whoami"} lang="terminal" />
      </li>
      </ol>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="https://developers.google.com/identity/protocols/OpenIDConnect">OpenID Connect (Google Identity documentation)</a></li></ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
