---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: tutorials/dev-app-web-console.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import BlockImage from "@ui/image/Image.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"dev-app-web-console","text":"Tutorial: Deploying an application by using the web console"},{"depth":2,"slug":"prerequisites_dev-app-web-console","text":"Prerequisites"},{"depth":2,"slug":"getting-started-web-console-creating-new-project_dev-app-web-console","text":"Creating a project"},{"depth":2,"slug":"getting-started-web-console-granting-permissions_dev-app-web-console","text":"Granting view permissions"},{"depth":2,"slug":"getting-started-web-console-deploying-first-image_dev-app-web-console","text":"Deploying the front-end application"},{"depth":3,"slug":"getting-started-web-console-examining-pod_dev-app-web-console","text":"Viewing pod details"},{"depth":3,"slug":"getting-started-web-console-scaling-app_dev-app-web-console","text":"Scaling up the application"},{"depth":2,"slug":"getting-started-web-console-deploying-python-app_dev-app-web-console","text":"Deploying the back-end application"},{"depth":2,"slug":"getting-started-web-console-connecting-database_dev-app-web-console","text":"Deploying the database application"},{"depth":3,"slug":"getting-started-web-console-creating-secret_dev-app-web-console","text":"Providing access to the database by creating a secret"},{"depth":3,"slug":"getting-started-web-console-load-data-output_dev-app-web-console","text":"Loading data into the database"},{"depth":2,"slug":"getting-started-web-console-view_dev-app-web-console","text":"Viewing the application in a web browser"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/tutorials/dev-app-web-console";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Tutorial: Deploying an application by using the web console";
const description = "To learn how to stand up an application on OpenShift Container Platform by using the web console, follow the provided tutorial. In this tutorial, you will…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["tutorials"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Tutorial: Deploying an application by using the web console {#dev-app-web-console}\n\nTo learn how to stand up an application on OpenShift Container Platform by using the web console, follow the provided tutorial. In this tutorial, you will deploy the services that are required for an application that displays a map of national parks across the world.\n\nTo complete this tutorial, you will perform the following steps:\n\n1. [Create a project for the application](/openshift-docs-markdown/tutorials/dev-app-web-console#getting-started-web-console-creating-new-project_dev-app-web-console).\n\n   This step allows your application to be isolated from other cluster user’s workloads.\n2. [Grant view permissions](/openshift-docs-markdown/tutorials/dev-app-web-console#getting-started-web-console-granting-permissions_dev-app-web-console).\n\n   This step grants `view` permissions to interact with the OpenShift API to help discover services and other resources running within the project.\n3. [Deploy the front-end application](/openshift-docs-markdown/tutorials/dev-app-web-console#getting-started-web-console-deploying-first-image_dev-app-web-console).\n\n   This step deploys the `parksmap` front-end application, exposes it externally, and scales it up to two instances.\n4. [Deploy the back-end application](/openshift-docs-markdown/tutorials/dev-app-web-console#getting-started-web-console-deploying-python-app_dev-app-web-console).\n\n   This step deploys the `nationalparks` back-end application and exposes it externally.\n5. [Deploy the database application](/openshift-docs-markdown/tutorials/dev-app-web-console#getting-started-web-console-connecting-database_dev-app-web-console).\n\n   This step deploys the `mongodb-nationalparks` MongoDB database, loads data into the database, and sets up the necessary credentials to access the database.\n\nAfter you complete these steps, you can [view the national parks application in a web browser](/openshift-docs-markdown/tutorials/dev-app-web-console#getting-started-web-console-view_dev-app-web-console).\n\n## Prerequisites {#prerequisites_dev-app-web-console}\n\nBefore you start this tutorial, ensure that you have the following required prerequisites:\n\n- You have access to a test OpenShift Container Platform cluster.\n\n  If your organization does not have a cluster to test on, you can request access to the [Developer Sandbox](https://developers.redhat.com/developer-sandbox) to get a trial of OpenShift Container Platform.\n- You have the appropriate permissions, such as the `cluster-admin` [cluster role](/openshift-docs-markdown/authentication/using-rbac#viewing-cluster-roles_using-rbac), to create a project and applications within it.\n\n  If you do not have the required permissions, contact your cluster administrator. You need the `self-provisioner` role to create a project and the `admin` role on the project to modify resources in that project.\n\n  If you are using Developer Sandbox, a project is created for you with the required permissions.\n- You have [logged in to the OpenShift Container Platform web console](/openshift-docs-markdown/web_console/web-console#web-console-overview).\n\n## Creating a project {#getting-started-web-console-creating-new-project_dev-app-web-console}\n\nCreate a new project to contain all required resources and application components for the tutorial.\n\nA *project* enables a community of users to organize and manage their content in isolation. Projects are OpenShift Container Platform extensions to Kubernetes namespaces. Projects have additional features that enable user self-provisioning. Each project has its own set of objects, policies, constraints, and service accounts.\n\nCluster administrators can allow developers to create their own projects. In most cases, you automatically have access to your own projects. Administrators can grant access to other projects as needed.\n\nThis procedure creates a new project called `user-getting-started`. You will use this project throughout the rest of this tutorial.\n\n> [!IMPORTANT]\n> If you are using Developer Sandbox to complete this tutorial, skip this procedure. A project has already been created for you.\n\n**Prerequisites**\n\n- You have logged in to the OpenShift Container Platform web console.\n\n**Procedure**\n\n1. Navigate to **Home** -> **Projects**.\n2. Click **Create Project**.\n3. In the **Name** field, enter `user-getting-started`.\n4. Click **Create**.\n\n**Additional resources**\n\n- [Viewing a project by using the web console](/openshift-docs-markdown/applications/projects/working-with-projects#viewing-a-project-using-the-web-console_projects)\n\n## Granting view permissions {#getting-started-web-console-granting-permissions_dev-app-web-console}\n\nConfigure the necessary permissions for the application to access the required cluster resources.\n\nOpenShift Container Platform automatically creates several service accounts in every project. The `default` service account takes responsibility for running the pods. OpenShift Container Platform uses and injects this service account into every pod that launches.\n\nBy default, the `default` service account has limited permissions to interact with the OpenShift API.\n\nAs a requirement of the application, you must assign the `view` role to the `default` service account to allow it to communicate with the OpenShift API to learn about pods, services, and resources within the project.\n\n**Prerequisites**\n\n- You have `cluster-admin` or project-level `admin` privileges.\n\n**Procedure**\n\n1. Navigate to **User Management** -> **RoleBindings**.\n2. Click **Create binding**.\n3. In the **Name** field, enter `sa-user-account`.\n4. In the **Namespace** field, search for and select `user-getting-started`.\n\n   > [!IMPORTANT]\n   > If you are using a different project, select the name of your project.\n5. In the **Role name** field, search for and select `view`.\n6. Under **Subject**, select `ServiceAccount`.\n7. In the **Subject namespace** field, search for and select `user-getting-started`.\n\n   > [!IMPORTANT]\n   > If you are using a different project, select the name of your project.\n8. In the **Subject name** field, enter `default`.\n9. Click **Create**.\n\n**Additional resources**\n\n- [RBAC overview](/openshift-docs-markdown/authentication/using-rbac#authorization-overview_using-rbac)\n\n## Deploying the front-end application {#getting-started-web-console-deploying-first-image_dev-app-web-console}\n\nDeploy the front-end application that provides the external-facing web component for the tutorial.\n\nThe simplest way to deploy an application in OpenShift Container Platform is to run a provided container image.\n\nThe following procedure deploys `parksmap`, which is the front-end component of the `national-parks-app` application. The web application displays an interactive map of the locations of national parks across the world.\n\n**Procedure**\n\n1. From the **Quick create** (![title=\"Quick create menu\"](/openshift-docs-markdown/_assets/images/fa-plus-circle.png)) menu in the upper right corner, click **Container images**.\n2. Select **Image name from external registry** and enter `quay.io/openshiftroadshow/parksmap:latest`.\n3. Scroll to the **General** section.\n4. In the **Application name** field, enter `national-parks-app`.\n5. In the **Name** field, ensure that the value is `parksmap`.\n6. Scroll to the **Deploy** section.\n7. In the **Resource type** field, ensure that **Deployment** is selected.\n8. In the **Advanced options** section, ensure that **Create a route** is selected.\n\n   By default, services running on OpenShift Container Platform are not accessible externally. You must select this option to create a route so that external clients can access your service.\n9. Click the **Labels** hyperlink.\n\n   The application code requires certain labels to be set.\n10. Add the following labels to the text area and press Enter after each key/value pair:\n\n    - `app=national-parks-app`\n    - `component=parksmap`\n    - `role=frontend`\n11. Click **Create**.\n\n    You are redirected to the **Topology** page where you can see the `parksmap` deployment in the `national-parks-app` application.\n\n**Additional resources**\n\n- [Viewing the topology of your application](/openshift-docs-markdown/applications/odc-viewing-application-composition-using-topology-view#odc-viewing-application-topology_viewing-application-composition-using-topology-view)\n\n### Viewing pod details {#getting-started-web-console-examining-pod_dev-app-web-console}\n\nRetrieve detailed pod information to confirm the running status and resource configuration of the applications in this tutorial.\n\nOpenShift Container Platform uses the Kubernetes concept of a *pod*, which is one or more containers deployed together on one host, and the smallest compute unit that can be defined, deployed, and managed. Pods are the rough equivalent of a machine instance, physical or virtual, to a container.\n\nThe **Overview** panel enables you to access many features of the `parksmap` deployment. The **Details** and **Resources** tabs enable you to scale application pods and check the status of builds, services, and routes.\n\n**Prerequisites**\n\n- You have deployed the `parksmap` front-end application.\n\n**Procedure**\n\n1. Navigate to **Workloads** -> **Topology**.\n2. Click the `parksmap` deployment in the `national-parks-app` application. **Figure 1. Parksmap deployment**\n\n   ![Topology view of parksmap deployment](/openshift-docs-markdown/_assets/images/getting-started-examine-pod.png)\n\n   This opens an overview panel with the following tabs:\n\n   - **Details**: View details about your deployment, edit certain settings, and scale your deployment.\n   - **Resources**: View details for the pods, services, and routes associated with your deployment.\n   - **Observe**: View metrics and events for your deployment.\n3. To view the logs for a pod, select the **Resources** tab and click **View logs** next to the `parksmap` pod.\n\n**Additional resources**\n\n- [Interacting with applications and components](/openshift-docs-markdown/applications/odc-viewing-application-composition-using-topology-view#odc-interacting-with-applications-and-components_viewing-application-composition-using-topology-view)\n- [Scaling application pods and checking builds and routes](/openshift-docs-markdown/applications/odc-viewing-application-composition-using-topology-view#odc-scaling-application-pods-and-checking-builds-and-routes_viewing-application-composition-using-topology-view)\n- [Labels and annotations used for the Topology view](/openshift-docs-markdown/applications/odc-viewing-application-composition-using-topology-view#odc-labels-and-annotations-used-for-topology-view_viewing-application-composition-using-topology-view)\n\n### Scaling up the application {#getting-started-web-console-scaling-app_dev-app-web-console}\n\nScale the application deployment up or down to meet workload demands.\n\nIn Kubernetes, a `Deployment` object defines how an application deploys. In most cases when you deploy an application, OpenShift Container Platform creates the `Pod`, `Service`, `ReplicaSet`, and `Deployment` resources for you.\n\nWhen you deploy the `parksmap` image, a deployment resource is created. In this example, only one pod is deployed. You might want to scale up your application to keep up with user demand or to ensure that your application is always running even if one pod is down.\n\nThe following procedure scales the `parksmap` deployment to use two instances.\n\n**Prerequisites**\n\n- You have deployed the `parksmap` front-end application.\n\n**Procedure**\n\n1. Navigate to **Workloads** -> **Topology** and click the `parksmap` deployment.\n2. Select the **Details** tab.\n3. Use the up arrow to scale the pod to two instances. **Figure 1. Scaling application**\n\n   ![Scaling pod to two instances](/openshift-docs-markdown/_assets/images/getting-started-scaling-pod.png)\n\n   > [!TIP]\n   > You can use the down arrow to scale your deployment back down to one pod instance.\n\n**Additional resources**\n\n- [Recommended practices for scaling the cluster](/openshift-docs-markdown/scalability_and_performance/recommended-performance-scale-practices/recommended-control-plane-practices#recommended-scale-practices_recommended-control-plane-practices)\n\n## Deploying the back-end application {#getting-started-web-console-deploying-python-app_dev-app-web-console}\n\nDeploy the back-end application that provides the service that queries the database to return the national park data required for your application.\n\nThe following procedure deploys `nationalparks`, which is the back-end component for the `national-parks-app` application. The Python application performs 2D geo-spatial queries against a MongoDB database to locate and return map coordinates of all national parks in the world.\n\n**Prerequisites**\n\n- You have deployed the `parksmap` front-end application.\n\n**Procedure**\n\n1. From the **Quick create** (![title=\"Quick create menu\"](/openshift-docs-markdown/_assets/images/fa-plus-circle.png)) menu in the upper right corner, click **Import from Git**.\n2. In the **Git Repo URL** field, enter `https://github.com/openshift-roadshow/nationalparks-py.git`.\n\n   A builder image is automatically detected, but the import strategy defaults to Dockerfile instead of Python.\n3. Change the import strategy:\n\n   1. Click **Edit Import Strategy**.\n   2. Select **Builder Image**.\n   3. Select **Python**.\n4. Scroll to the **General** section.\n5. In the **Application** field, ensure that the value is `national-parks-app`.\n6. In the **Name** field, enter `nationalparks`.\n7. Scroll to the **Deploy** section.\n8. In the **Resource type** field, ensure that **Deployment** is selected.\n9. In the **Advanced options** section, ensure that **Create a route** is selected.\n\n   By default, services running on OpenShift Container Platform are not accessible externally. You must select this option to create a route so that external clients can access your service.\n10. Click the **Labels** hyperlink.\n\n    The application code requires certain labels to be set.\n11. Add the following labels to the text area and press Enter after each key/value pair:\n\n    - `app=national-parks-app`\n    - `component=nationalparks`\n    - `role=backend`\n    - `type=parksmap-backend`\n12. Click **Create**.\n\n    You are redirected to the **Topology** page where you can see the `nationalparks` deployment in the `national-parks-app` application.\n\n**Verification**\n\n1. Navigate to **Workloads** -> **Topology**.\n2. Click the `nationalparks` deployment in the `national-parks-app` application.\n3. Click the **Resources** tab.\n\n   Wait for the build to complete successfully.\n\n**Additional resources**\n\n- [Adding services to your application](/openshift-docs-markdown/applications/odc-viewing-application-composition-using-topology-view#odc-adding-services-to-your-application_viewing-application-composition-using-topology-view)\n- [Importing a codebase from Git to create an application](/openshift-docs-markdown/applications/creating_applications/odc-creating-applications-using-developer-perspective#odc-importing-codebase-from-git-to-create-application_odc-creating-applications-using-developer-perspective)\n\n## Deploying the database application {#getting-started-web-console-connecting-database_dev-app-web-console}\n\nDeploy a MongoDB database application to contain the information that your application requires. For this tutorial, you will deploy a database application called `mongodb-nationalparks` that holds the national park location information.\n\n**Prerequisites**\n\n- You have deployed the `parksmap` front-end application.\n- You have deployed the `nationalparks` back-end application.\n\n**Procedure**\n\n1. From the **Quick create** (![title=\"Quick create menu\"](/openshift-docs-markdown/_assets/images/fa-plus-circle.png)) menu in the upper right corner, click **Container images**.\n2. Select **Image name from external registry** and enter `registry.redhat.io/rhmap47/mongodb`.\n3. In the **Runtime icon** field, search for and select `mongodb`.\n4. Scroll to the **General** section.\n5. In the **Application name** field, enter `national-parks-app`.\n6. In the **Name** field, enter `mongodb-nationalparks`.\n7. Scroll to the **Deploy** section.\n8. In the **Resource type** field, ensure that **Deployment** is selected.\n9. Click **Show advanced Deployment option**.\n10. Under **Environment variables (runtime only)**, add the following names and values: **Environment variable names and values**\n\n    | Name | Value |\n    | --- | --- |\n    | `MONGODB_USER` | `mongodb` |\n    | `MONGODB_PASSWORD` | `mongodb` |\n    | `MONGODB_DATABASE` | `mongodb` |\n    | `MONGODB_ADMIN_PASSWORD` | `mongodb` |\n\n    > [!TIP]\n    > Click **Add value** to add each additional environment variable.\n11. In the **Advanced options** section, clear **Create a route**.\n\n    The database application does not need to be accessed externally, so a route is not required.\n12. Click **Create**.\n\n    You are redirected to the **Topology** page where you can see the `mongodb-nationalparks` deployment in the `national-parks-app` application.\n\n### Providing access to the database by creating a secret {#getting-started-web-console-creating-secret_dev-app-web-console}\n\nCreate a `Secret` resource to securely provide the back-end application with the sensitive database connection credentials.\n\nThe `nationalparks` application needs information, such as the database name, username, and passwords, to access the MongoDB database. However, because this information is sensitive, you should not store it directly in the pod.\n\nYou can use a *secret* to store sensitive information, and share that secret with workloads.\n\n`Secret` objects provide a mechanism to hold sensitive information such as passwords, OpenShift Container Platform client configuration files, and private source repository credentials. Secrets decouple sensitive content from the pods. You can mount secrets into containers by using a volume plugin or by passing the secret in as an environment variable. The system can then use secrets to provide the pod with the sensitive information.\n\nThe following procedure creates the `nationalparks-mongodb-parameters` secret and mounts it to the `nationalparks` workload.\n\n**Prerequisites**\n\n- You have deployed the `nationalparks` back-end application.\n- You have deployed the `mongodb-nationalparks` database application.\n\n**Procedure**\n\n1. Navigate to **Workloads** -> **Secrets**.\n2. Click **Create** -> **Key/value secret**.\n3. In the **Secret name** field, enter `nationalparks-mongodb-parameters`.\n4. Enter the following values for **Key** and **Value**: **Secret keys and values**\n\n   | Key | Value |\n   | --- | --- |\n   | `DATABASE_SERVICE_NAME` | `mongodb-nationalparks` |\n   | `MONGODB_USER` | `mongodb` |\n   | `MONGODB_PASSWORD` | `mongodb` |\n   | `MONGODB_DATABASE` | `mongodb` |\n   | `MONGODB_ADMIN_PASSWORD` | `mongodb` |\n\n   > [!TIP]\n   > Click **Add key/value** to add each additional key/value pair.\n5. Click **Create**.\n6. Click **Add Secret to workload**.\n7. From the **Add this secret to workload** list, select `nationalparks`.\n8. Click **Save**.\n\n   This change in configuration triggers a new rollout of the `nationalparks` deployment with the environment variables properly injected.\n\n**Additional resources**\n\n- [Understanding secrets](/openshift-docs-markdown/nodes/pods/nodes-pods-secrets#nodes-pods-secrets-about_nodes-pods-secrets)\n\n### Loading data into the database {#getting-started-web-console-load-data-output_dev-app-web-console}\n\nAfter you have deployed the `mongodb-nationalparks` database application, load the national park location information into the database.\n\n**Prerequisites**\n\n- You have deployed the `nationalparks` back-end application.\n- You have deployed the `mongodb-nationalparks` database application.\n\n**Procedure**\n\n1. Navigate to **Workloads** -> **Topology**.\n2. Click the `nationalparks` deployment and select the **Resources** tab.\n3. Copy the **Location** URL from your route.\n4. Paste the URL into your web browser and add the following at the end of the URL:\n\n   ```text\n   /ws/data/load\n   ```\n\n   For example:\n\n   ```text\n   https://nationalparks-user-getting-started.apps.cluster.example.com/ws/data/load\n   ```\n\n   ```text {title=\"Example output\"}\n   Items inserted in database: 2893\n   ```\n\n## Viewing the application in a web browser {#getting-started-web-console-view_dev-app-web-console}\n\nAfter you have deployed the necessary applications and loaded data into the database, you are now ready view your application through a browser. You can access the application by opening the URL for the front-end application.\n\n**Prerequisites**\n\n- You have deployed the `parksmap` front-end application.\n- You have deployed the `nationalparks` back-end application.\n- You have deployed the `mongodb-nationalparks` database application.\n- You have loaded the data into the `mongodb-nationalparks` database.\n\n**Procedure**\n\n1. Navigate to **Workloads** -> **Topology**.\n2. Click the **Open URL** link from the `parksmap` deployment. **Figure 1. National parks across the world**\n\n   ![Opening the URL for the parksmap deployment](/openshift-docs-markdown/_assets/images/getting-started-parksmap-url.png)\n3. Verify that your web browser displays a map of the national parks across the world. **Figure 2. National parks across the world**\n\n   ![Map of the national parks across the world](/openshift-docs-markdown/_assets/images/getting-started-map-national-parks.png)\n\n   If you allow the application to access your location, the map will center on your location.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/tutorials/dev-app-web-console.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="dev-app-web-console" class="group scroll-mt-20">Tutorial: Deploying an application by using the web console<a href="#dev-app-web-console" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>To learn how to stand up an application on OpenShift Container Platform by using the web console, follow the provided tutorial. In this tutorial, you will deploy the services that are required for an application that displays a map of national parks across the world.</p>
      <p>To complete this tutorial, you will perform the following steps:</p>
      <ol>
      <li>
      <a href="/openshift-docs-markdown/tutorials/dev-app-web-console#getting-started-web-console-creating-new-project_dev-app-web-console">Create a project for the application</a>.
      <p>This step allows your application to be isolated from other cluster user’s workloads.</p>
      </li>
      <li>
      <a href="/openshift-docs-markdown/tutorials/dev-app-web-console#getting-started-web-console-granting-permissions_dev-app-web-console">Grant view permissions</a>.
      <p>This step grants <code>view</code> permissions to interact with the OpenShift API to help discover services and other resources running within the project.</p>
      </li>
      <li>
      <a href="/openshift-docs-markdown/tutorials/dev-app-web-console#getting-started-web-console-deploying-first-image_dev-app-web-console">Deploy the front-end application</a>.
      <p>This step deploys the <code>parksmap</code> front-end application, exposes it externally, and scales it up to two instances.</p>
      </li>
      <li>
      <a href="/openshift-docs-markdown/tutorials/dev-app-web-console#getting-started-web-console-deploying-python-app_dev-app-web-console">Deploy the back-end application</a>.
      <p>This step deploys the <code>nationalparks</code> back-end application and exposes it externally.</p>
      </li>
      <li>
      <a href="/openshift-docs-markdown/tutorials/dev-app-web-console#getting-started-web-console-connecting-database_dev-app-web-console">Deploy the database application</a>.
      <p>This step deploys the <code>mongodb-nationalparks</code> MongoDB database, loads data into the database, and sets up the necessary credentials to access the database.</p>
      </li>
      </ol>
      <p>After you complete these steps, you can <a href="/openshift-docs-markdown/tutorials/dev-app-web-console#getting-started-web-console-view_dev-app-web-console">view the national parks application in a web browser</a>.</p>
      <h2 id="prerequisites_dev-app-web-console" class="group scroll-mt-20">Prerequisites<a href="#prerequisites_dev-app-web-console" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Before you start this tutorial, ensure that you have the following required prerequisites:</p>
      <ul>
      <li>
      You have access to a test OpenShift Container Platform cluster.
      <p>If your organization does not have a cluster to test on, you can request access to the <a href="https://developers.redhat.com/developer-sandbox">Developer Sandbox</a> to get a trial of OpenShift Container Platform.</p>
      </li>
      <li>
      You have the appropriate permissions, such as the <code>cluster-admin</code> <a href="/openshift-docs-markdown/authentication/using-rbac#viewing-cluster-roles_using-rbac">cluster role</a>, to create a project and applications within it.
      <p>If you do not have the required permissions, contact your cluster administrator. You need the <code>self-provisioner</code> role to create a project and the <code>admin</code> role on the project to modify resources in that project.</p>
      <p>If you are using Developer Sandbox, a project is created for you with the required permissions.</p>
      </li>
      <li>You have <a href="/openshift-docs-markdown/web_console/web-console#web-console-overview">logged in to the OpenShift Container Platform web console</a>.</li>
      </ul>
      <h2 id="getting-started-web-console-creating-new-project_dev-app-web-console" class="group scroll-mt-20">Creating a project<a href="#getting-started-web-console-creating-new-project_dev-app-web-console" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Create a new project to contain all required resources and application components for the tutorial.</p>
      <p>A <em>project</em> enables a community of users to organize and manage their content in isolation. Projects are OpenShift Container Platform extensions to Kubernetes namespaces. Projects have additional features that enable user self-provisioning. Each project has its own set of objects, policies, constraints, and service accounts.</p>
      <p>Cluster administrators can allow developers to create their own projects. In most cases, you automatically have access to your own projects. Administrators can grant access to other projects as needed.</p>
      <p>This procedure creates a new project called <code>user-getting-started</code>. You will use this project throughout the rest of this tutorial.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>If you are using Developer Sandbox to complete this tutorial, skip this procedure. A project has already been created for you.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have logged in to the OpenShift Container Platform web console.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to <strong>Home</strong> -&gt; <strong>Projects</strong>.</li>
      <li>Click <strong>Create Project</strong>.</li>
      <li>In the <strong>Name</strong> field, enter <code>user-getting-started</code>.</li>
      <li>Click <strong>Create</strong>.</li>
      </ol>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/applications/projects/working-with-projects#viewing-a-project-using-the-web-console_projects">Viewing a project by using the web console</a></li></ul>
      <h2 id="getting-started-web-console-granting-permissions_dev-app-web-console" class="group scroll-mt-20">Granting view permissions<a href="#getting-started-web-console-granting-permissions_dev-app-web-console" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Configure the necessary permissions for the application to access the required cluster resources.</p>
      <p>OpenShift Container Platform automatically creates several service accounts in every project. The <code>default</code> service account takes responsibility for running the pods. OpenShift Container Platform uses and injects this service account into every pod that launches.</p>
      <p>By default, the <code>default</code> service account has limited permissions to interact with the OpenShift API.</p>
      <p>As a requirement of the application, you must assign the <code>view</code> role to the <code>default</code> service account to allow it to communicate with the OpenShift API to learn about pods, services, and resources within the project.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have <code>cluster-admin</code> or project-level <code>admin</code> privileges.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to <strong>User Management</strong> -&gt; <strong>RoleBindings</strong>.</li>
      <li>Click <strong>Create binding</strong>.</li>
      <li>In the <strong>Name</strong> field, enter <code>sa-user-account</code>.</li>
      <li>
      In the <strong>Namespace</strong> field, search for and select <code>user-getting-started</code>.
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>If you are using a different project, select the name of your project.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>In the <strong>Role name</strong> field, search for and select <code>view</code>.</li>
      <li>Under <strong>Subject</strong>, select <code>ServiceAccount</code>.</li>
      <li>
      In the <strong>Subject namespace</strong> field, search for and select <code>user-getting-started</code>.
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>If you are using a different project, select the name of your project.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>In the <strong>Subject name</strong> field, enter <code>default</code>.</li>
      <li>Click <strong>Create</strong>.</li>
      </ol>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/authentication/using-rbac#authorization-overview_using-rbac">RBAC overview</a></li></ul>
      <h2 id="getting-started-web-console-deploying-first-image_dev-app-web-console" class="group scroll-mt-20">Deploying the front-end application<a href="#getting-started-web-console-deploying-first-image_dev-app-web-console" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Deploy the front-end application that provides the external-facing web component for the tutorial.</p>
      <p>The simplest way to deploy an application in OpenShift Container Platform is to run a provided container image.</p>
      <p>The following procedure deploys <code>parksmap</code>, which is the front-end component of the <code>national-parks-app</code> application. The web application displays an interactive map of the locations of national parks across the world.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>From the <strong>Quick create</strong> (<img src="/openshift-docs-markdown/_assets/images/fa-plus-circle.png" alt="title=&quot;Quick create menu&quot;" loading="lazy" />) menu in the upper right corner, click <strong>Container images</strong>.</li>
      <li>Select <strong>Image name from external registry</strong> and enter <code>quay.io/openshiftroadshow/parksmap:latest</code>.</li>
      <li>Scroll to the <strong>General</strong> section.</li>
      <li>In the <strong>Application name</strong> field, enter <code>national-parks-app</code>.</li>
      <li>In the <strong>Name</strong> field, ensure that the value is <code>parksmap</code>.</li>
      <li>Scroll to the <strong>Deploy</strong> section.</li>
      <li>In the <strong>Resource type</strong> field, ensure that <strong>Deployment</strong> is selected.</li>
      <li>
      In the <strong>Advanced options</strong> section, ensure that <strong>Create a route</strong> is selected.
      <p>By default, services running on OpenShift Container Platform are not accessible externally. You must select this option to create a route so that external clients can access your service.</p>
      </li>
      <li>
      Click the <strong>Labels</strong> hyperlink.
      <p>The application code requires certain labels to be set.</p>
      </li>
      <li>
      Add the following labels to the text area and press Enter after each key/value pair:
      <ul>
      <li><code>app=national-parks-app</code></li>
      <li><code>component=parksmap</code></li>
      <li><code>role=frontend</code></li>
      </ul>
      </li>
      <li>
      Click <strong>Create</strong>.
      <p>You are redirected to the <strong>Topology</strong> page where you can see the <code>parksmap</code> deployment in the <code>national-parks-app</code> application.</p>
      </li>
      </ol>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/applications/odc-viewing-application-composition-using-topology-view#odc-viewing-application-topology_viewing-application-composition-using-topology-view">Viewing the topology of your application</a></li></ul>
      <h3 id="getting-started-web-console-examining-pod_dev-app-web-console" class="group scroll-mt-20">Viewing pod details<a href="#getting-started-web-console-examining-pod_dev-app-web-console" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">Retrieve detailed pod information to confirm the running status and resource configuration of the applications in this tutorial.</p>
      <p>OpenShift Container Platform uses the Kubernetes concept of a <em>pod</em>, which is one or more containers deployed together on one host, and the smallest compute unit that can be defined, deployed, and managed. Pods are the rough equivalent of a machine instance, physical or virtual, to a container.</p>
      <p>The <strong>Overview</strong> panel enables you to access many features of the <code>parksmap</code> deployment. The <strong>Details</strong> and <strong>Resources</strong> tabs enable you to scale application pods and check the status of builds, services, and routes.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have deployed the <code>parksmap</code> front-end application.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to <strong>Workloads</strong> -&gt; <strong>Topology</strong>.</li>
      <li>
      Click the <code>parksmap</code> deployment in the <code>national-parks-app</code> application. <strong>Figure 1. Parksmap deployment</strong>
      <BlockImage src="/openshift-docs-markdown/_assets/images/getting-started-examine-pod.png" alt="Topology view of parksmap deployment" />
      <p>This opens an overview panel with the following tabs:</p>
      <ul>
      <li><strong>Details</strong>: View details about your deployment, edit certain settings, and scale your deployment.</li>
      <li><strong>Resources</strong>: View details for the pods, services, and routes associated with your deployment.</li>
      <li><strong>Observe</strong>: View metrics and events for your deployment.</li>
      </ul>
      </li>
      <li>To view the logs for a pod, select the <strong>Resources</strong> tab and click <strong>View logs</strong> next to the <code>parksmap</code> pod.</li>
      </ol>
      <p><strong>Additional resources</strong></p>
      <ul>
      <li><a href="/openshift-docs-markdown/applications/odc-viewing-application-composition-using-topology-view#odc-interacting-with-applications-and-components_viewing-application-composition-using-topology-view">Interacting with applications and components</a></li>
      <li><a href="/openshift-docs-markdown/applications/odc-viewing-application-composition-using-topology-view#odc-scaling-application-pods-and-checking-builds-and-routes_viewing-application-composition-using-topology-view">Scaling application pods and checking builds and routes</a></li>
      <li><a href="/openshift-docs-markdown/applications/odc-viewing-application-composition-using-topology-view#odc-labels-and-annotations-used-for-topology-view_viewing-application-composition-using-topology-view">Labels and annotations used for the Topology view</a></li>
      </ul>
      <h3 id="getting-started-web-console-scaling-app_dev-app-web-console" class="group scroll-mt-20">Scaling up the application<a href="#getting-started-web-console-scaling-app_dev-app-web-console" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">Scale the application deployment up or down to meet workload demands.</p>
      <p>In Kubernetes, a <code>Deployment</code> object defines how an application deploys. In most cases when you deploy an application, OpenShift Container Platform creates the <code>Pod</code>, <code>Service</code>, <code>ReplicaSet</code>, and <code>Deployment</code> resources for you.</p>
      <p>When you deploy the <code>parksmap</code> image, a deployment resource is created. In this example, only one pod is deployed. You might want to scale up your application to keep up with user demand or to ensure that your application is always running even if one pod is down.</p>
      <p>The following procedure scales the <code>parksmap</code> deployment to use two instances.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have deployed the <code>parksmap</code> front-end application.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to <strong>Workloads</strong> -&gt; <strong>Topology</strong> and click the <code>parksmap</code> deployment.</li>
      <li>Select the <strong>Details</strong> tab.</li>
      <li>
      Use the up arrow to scale the pod to two instances. <strong>Figure 1. Scaling application</strong>
      <BlockImage src="/openshift-docs-markdown/_assets/images/getting-started-scaling-pod.png" alt="Scaling pod to two instances" />
      <Alert variant="tip" class="my-4">
        <AlertTitle>Tip</AlertTitle>
        <AlertDescription>
          <p>You can use the down arrow to scale your deployment back down to one pod instance.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/scalability_and_performance/recommended-performance-scale-practices/recommended-control-plane-practices#recommended-scale-practices_recommended-control-plane-practices">Recommended practices for scaling the cluster</a></li></ul>
      <h2 id="getting-started-web-console-deploying-python-app_dev-app-web-console" class="group scroll-mt-20">Deploying the back-end application<a href="#getting-started-web-console-deploying-python-app_dev-app-web-console" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Deploy the back-end application that provides the service that queries the database to return the national park data required for your application.</p>
      <p>The following procedure deploys <code>nationalparks</code>, which is the back-end component for the <code>national-parks-app</code> application. The Python application performs 2D geo-spatial queries against a MongoDB database to locate and return map coordinates of all national parks in the world.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have deployed the <code>parksmap</code> front-end application.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>From the <strong>Quick create</strong> (<img src="/openshift-docs-markdown/_assets/images/fa-plus-circle.png" alt="title=&quot;Quick create menu&quot;" loading="lazy" />) menu in the upper right corner, click <strong>Import from Git</strong>.</li>
      <li>
      In the <strong>Git Repo URL</strong> field, enter <code>https://github.com/openshift-roadshow/nationalparks-py.git</code>.
      <p>A builder image is automatically detected, but the import strategy defaults to Dockerfile instead of Python.</p>
      </li>
      <li>
      Change the import strategy:
      <ol>
      <li>Click <strong>Edit Import Strategy</strong>.</li>
      <li>Select <strong>Builder Image</strong>.</li>
      <li>Select <strong>Python</strong>.</li>
      </ol>
      </li>
      <li>Scroll to the <strong>General</strong> section.</li>
      <li>In the <strong>Application</strong> field, ensure that the value is <code>national-parks-app</code>.</li>
      <li>In the <strong>Name</strong> field, enter <code>nationalparks</code>.</li>
      <li>Scroll to the <strong>Deploy</strong> section.</li>
      <li>In the <strong>Resource type</strong> field, ensure that <strong>Deployment</strong> is selected.</li>
      <li>
      In the <strong>Advanced options</strong> section, ensure that <strong>Create a route</strong> is selected.
      <p>By default, services running on OpenShift Container Platform are not accessible externally. You must select this option to create a route so that external clients can access your service.</p>
      </li>
      <li>
      Click the <strong>Labels</strong> hyperlink.
      <p>The application code requires certain labels to be set.</p>
      </li>
      <li>
      Add the following labels to the text area and press Enter after each key/value pair:
      <ul>
      <li><code>app=national-parks-app</code></li>
      <li><code>component=nationalparks</code></li>
      <li><code>role=backend</code></li>
      <li><code>type=parksmap-backend</code></li>
      </ul>
      </li>
      <li>
      Click <strong>Create</strong>.
      <p>You are redirected to the <strong>Topology</strong> page where you can see the <code>nationalparks</code> deployment in the <code>national-parks-app</code> application.</p>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>Navigate to <strong>Workloads</strong> -&gt; <strong>Topology</strong>.</li>
      <li>Click the <code>nationalparks</code> deployment in the <code>national-parks-app</code> application.</li>
      <li>
      Click the <strong>Resources</strong> tab.
      <p>Wait for the build to complete successfully.</p>
      </li>
      </ol>
      <p><strong>Additional resources</strong></p>
      <ul>
      <li><a href="/openshift-docs-markdown/applications/odc-viewing-application-composition-using-topology-view#odc-adding-services-to-your-application_viewing-application-composition-using-topology-view">Adding services to your application</a></li>
      <li><a href="/openshift-docs-markdown/applications/creating_applications/odc-creating-applications-using-developer-perspective#odc-importing-codebase-from-git-to-create-application_odc-creating-applications-using-developer-perspective">Importing a codebase from Git to create an application</a></li>
      </ul>
      <h2 id="getting-started-web-console-connecting-database_dev-app-web-console" class="group scroll-mt-20">Deploying the database application<a href="#getting-started-web-console-connecting-database_dev-app-web-console" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Deploy a MongoDB database application to contain the information that your application requires. For this tutorial, you will deploy a database application called <code>mongodb-nationalparks</code> that holds the national park location information.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have deployed the <code>parksmap</code> front-end application.</li>
      <li>You have deployed the <code>nationalparks</code> back-end application.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>From the <strong>Quick create</strong> (<img src="/openshift-docs-markdown/_assets/images/fa-plus-circle.png" alt="title=&quot;Quick create menu&quot;" loading="lazy" />) menu in the upper right corner, click <strong>Container images</strong>.</li>
      <li>Select <strong>Image name from external registry</strong> and enter <code>registry.redhat.io/rhmap47/mongodb</code>.</li>
      <li>In the <strong>Runtime icon</strong> field, search for and select <code>mongodb</code>.</li>
      <li>Scroll to the <strong>General</strong> section.</li>
      <li>In the <strong>Application name</strong> field, enter <code>national-parks-app</code>.</li>
      <li>In the <strong>Name</strong> field, enter <code>mongodb-nationalparks</code>.</li>
      <li>Scroll to the <strong>Deploy</strong> section.</li>
      <li>In the <strong>Resource type</strong> field, ensure that <strong>Deployment</strong> is selected.</li>
      <li>Click <strong>Show advanced Deployment option</strong>.</li>
      <li>
      Under <strong>Environment variables (runtime only)</strong>, add the following names and values: <strong>Environment variable names and values</strong>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Name</TableHead>
      <TableHead>Value</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>MONGODB_USER</code></TableCell>
      <TableCell><code>mongodb</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>MONGODB_PASSWORD</code></TableCell>
      <TableCell><code>mongodb</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>MONGODB_DATABASE</code></TableCell>
      <TableCell><code>mongodb</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>MONGODB_ADMIN_PASSWORD</code></TableCell>
      <TableCell><code>mongodb</code></TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <Alert variant="tip" class="my-4">
        <AlertTitle>Tip</AlertTitle>
        <AlertDescription>
          <p>Click <strong>Add value</strong> to add each additional environment variable.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      In the <strong>Advanced options</strong> section, clear <strong>Create a route</strong>.
      <p>The database application does not need to be accessed externally, so a route is not required.</p>
      </li>
      <li>
      Click <strong>Create</strong>.
      <p>You are redirected to the <strong>Topology</strong> page where you can see the <code>mongodb-nationalparks</code> deployment in the <code>national-parks-app</code> application.</p>
      </li>
      </ol>
      <h3 id="getting-started-web-console-creating-secret_dev-app-web-console" class="group scroll-mt-20">Providing access to the database by creating a secret<a href="#getting-started-web-console-creating-secret_dev-app-web-console" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">Create a <code>Secret</code> resource to securely provide the back-end application with the sensitive database connection credentials.</p>
      <p>The <code>nationalparks</code> application needs information, such as the database name, username, and passwords, to access the MongoDB database. However, because this information is sensitive, you should not store it directly in the pod.</p>
      <p>You can use a <em>secret</em> to store sensitive information, and share that secret with workloads.</p>
      <p><code>Secret</code> objects provide a mechanism to hold sensitive information such as passwords, OpenShift Container Platform client configuration files, and private source repository credentials. Secrets decouple sensitive content from the pods. You can mount secrets into containers by using a volume plugin or by passing the secret in as an environment variable. The system can then use secrets to provide the pod with the sensitive information.</p>
      <p>The following procedure creates the <code>nationalparks-mongodb-parameters</code> secret and mounts it to the <code>nationalparks</code> workload.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have deployed the <code>nationalparks</code> back-end application.</li>
      <li>You have deployed the <code>mongodb-nationalparks</code> database application.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to <strong>Workloads</strong> -&gt; <strong>Secrets</strong>.</li>
      <li>Click <strong>Create</strong> -&gt; <strong>Key/value secret</strong>.</li>
      <li>In the <strong>Secret name</strong> field, enter <code>nationalparks-mongodb-parameters</code>.</li>
      <li>
      Enter the following values for <strong>Key</strong> and <strong>Value</strong>: <strong>Secret keys and values</strong>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Key</TableHead>
      <TableHead>Value</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>DATABASE_SERVICE_NAME</code></TableCell>
      <TableCell><code>mongodb-nationalparks</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>MONGODB_USER</code></TableCell>
      <TableCell><code>mongodb</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>MONGODB_PASSWORD</code></TableCell>
      <TableCell><code>mongodb</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>MONGODB_DATABASE</code></TableCell>
      <TableCell><code>mongodb</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>MONGODB_ADMIN_PASSWORD</code></TableCell>
      <TableCell><code>mongodb</code></TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <Alert variant="tip" class="my-4">
        <AlertTitle>Tip</AlertTitle>
        <AlertDescription>
          <p>Click <strong>Add key/value</strong> to add each additional key/value pair.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>Click <strong>Create</strong>.</li>
      <li>Click <strong>Add Secret to workload</strong>.</li>
      <li>From the <strong>Add this secret to workload</strong> list, select <code>nationalparks</code>.</li>
      <li>
      Click <strong>Save</strong>.
      <p>This change in configuration triggers a new rollout of the <code>nationalparks</code> deployment with the environment variables properly injected.</p>
      </li>
      </ol>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/nodes/pods/nodes-pods-secrets#nodes-pods-secrets-about_nodes-pods-secrets">Understanding secrets</a></li></ul>
      <h3 id="getting-started-web-console-load-data-output_dev-app-web-console" class="group scroll-mt-20">Loading data into the database<a href="#getting-started-web-console-load-data-output_dev-app-web-console" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">After you have deployed the <code>mongodb-nationalparks</code> database application, load the national park location information into the database.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have deployed the <code>nationalparks</code> back-end application.</li>
      <li>You have deployed the <code>mongodb-nationalparks</code> database application.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to <strong>Workloads</strong> -&gt; <strong>Topology</strong>.</li>
      <li>Click the <code>nationalparks</code> deployment and select the <strong>Resources</strong> tab.</li>
      <li>Copy the <strong>Location</strong> URL from your route.</li>
      <li>
      Paste the URL into your web browser and add the following at the end of the URL:
      <CodeBlock code={"/ws/data/load"} lang="text" />
      <p>For example:</p>
      <CodeBlock code={"https://nationalparks-user-getting-started.apps.cluster.example.com/ws/data/load"} lang="text" />
      <CodeBlock code={"Items inserted in database: 2893"} lang="text" title="Example output" />
      </li>
      </ol>
      <h2 id="getting-started-web-console-view_dev-app-web-console" class="group scroll-mt-20">Viewing the application in a web browser<a href="#getting-started-web-console-view_dev-app-web-console" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">After you have deployed the necessary applications and loaded data into the database, you are now ready view your application through a browser. You can access the application by opening the URL for the front-end application.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have deployed the <code>parksmap</code> front-end application.</li>
      <li>You have deployed the <code>nationalparks</code> back-end application.</li>
      <li>You have deployed the <code>mongodb-nationalparks</code> database application.</li>
      <li>You have loaded the data into the <code>mongodb-nationalparks</code> database.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to <strong>Workloads</strong> -&gt; <strong>Topology</strong>.</li>
      <li>
      Click the <strong>Open URL</strong> link from the <code>parksmap</code> deployment. <strong>Figure 1. National parks across the world</strong>
      <BlockImage src="/openshift-docs-markdown/_assets/images/getting-started-parksmap-url.png" alt="Opening the URL for the parksmap deployment" />
      </li>
      <li>
      Verify that your web browser displays a map of the national parks across the world. <strong>Figure 2. National parks across the world</strong>
      <BlockImage src="/openshift-docs-markdown/_assets/images/getting-started-map-national-parks.png" alt="Map of the national parks across the world" />
      <p>If you allow the application to access your location, the map will center on your location.</p>
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
