---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: observability/network_observability/network-observability-kafka-operator-scaling-network-flow-collection.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"network-observability-kafka-operator-scaling-network-flow-collection","text":"Scaling network flow collection with Kafka"},{"depth":2,"slug":"network-observability-kafka-for-large-scale-environments_network-observability-kafka-operator-scaling-network-flow-collection","text":"Kafka deployment scenarios for network flows"},{"depth":3,"slug":"when-to-use-kafka-for-network-flow-collection_network-observability-kafka-operator-scaling-network-flow-collection","text":"When to use Kafka for network flow collection"},{"depth":2,"slug":"network-observability-kafka-compression-benefits_network-observability-kafka-operator-scaling-network-flow-collection","text":"Reducing network bandwidth for flow telemetry"},{"depth":2,"slug":"network-observability-configuring-kafka-compression_network-observability-kafka-operator-scaling-network-flow-collection","text":"Configure Kafka compression"},{"depth":2,"slug":"network-observability-kafka-compression-codec-reference_network-observability-kafka-operator-scaling-network-flow-collection","text":"Kafka compression codec reference"},{"depth":2,"slug":"additional-resources_network-observability-kafka-operator-scaling-network-flow-collection","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/observability/network_observability/network-observability-kafka-operator-scaling-network-flow-collection";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Scaling network flow collection with Kafka";
const description = "Scale your network flow collection by using the Kafka Operator to manage high-volume telemetry. Configure compression to reduce network bandwidth while…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["observability","observability/network_observability"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Scaling network flow collection with Kafka {#network-observability-kafka-operator-scaling-network-flow-collection}\n\nScale your network flow collection by using the Kafka Operator to manage high-volume telemetry. Configure compression to reduce network bandwidth while balancing CPU overhead in large-scale cluster environments.\n\n## Kafka deployment scenarios for network flows {#network-observability-kafka-for-large-scale-environments_network-observability-kafka-operator-scaling-network-flow-collection}\n\nThe Kafka Operator manages high-throughput and low-latency data feeds for network flow forwarding. This architecture provides a resilient and scalable solution for handling telemetry data in large-scale cluster environments.\n\n### When to use Kafka for network flow collection {#when-to-use-kafka-for-network-flow-collection_network-observability-kafka-operator-scaling-network-flow-collection}\n\nConsider using Kafka to manage your network flow data when you experience the following circumstances:\n\n- High flow volumes that overwhelm the default flowlogs-pipeline buffer\n- Need for data persistence and replay capabilities\n- Multiple consumers requiring access to the same flow data\n- Requirements for horizontal scaling across multiple processing nodes\n\nFor smaller deployments with moderate flow volumes, the default configuration without Kafka is typically sufficient.\n\nYou can install the Kafka Operator as Red Hat AMQ Streams from the Operator Hub. See \"Red Hat AMQ Streams\".\n\n> [!NOTE]\n> To uninstall Kafka, refer to the uninstallation process that corresponds with the method you used to install.\n\n## Reducing network bandwidth for flow telemetry {#network-observability-kafka-compression-benefits_network-observability-kafka-operator-scaling-network-flow-collection}\n\nEnable Kafka compression for flow records to optimize network traffic. This configuration reduces network bandwidth consumption while increasing the CPU load on the eBPF agent.\n\nBy default, the Network Observability Operator sends network flow records to Kafka without compression. This minimizes CPU overhead but can consume significant network bandwidth in clusters with high traffic volumes.\n\nEnabling compression reduces the amount of data transmitted over the network, which is beneficial when the following cases are true:\n\n- Network bandwidth is constrained or expensive\n- Flow volumes are high and impacting network performance\n- Kafka brokers are located on different nodes or in remote data centers\n\nThe trade-off is increased CPU usage on the eBPF agent pods, which run as a `DaemonSet` on every node. Each of the different compression algorithms provide the following balances between compression ratio and CPU cost:\n\n- `lz4`: Very low CPU cost with 2 to 3 times compression. Best for most deployments.\n- `zstd`: Moderate CPU cost with 3 to 5 times compression. Good for bandwidth-constrained environments.\n- `snappy`: Similar to lz4 with slightly lower compression ratio.\n- `gzip`: High CPU cost with 3 to 5 times compression. Maximum compression at highest CPU expense.\n- `none`: No compression. Use when CPU is the bottleneck.\n\nFlow records contain many repeated fields (IP addresses, namespaces, node names) and compress efficiently, often reaching the higher end of these compression ratios.\n\n## Configure Kafka compression {#network-observability-configuring-kafka-compression_network-observability-kafka-operator-scaling-network-flow-collection}\n\nConfigure the compression algorithm for network flow records exported to Kafka to optimize bandwidth and storage. This helps manage the data footprint of high-volume network telemetry.\n\n**Prerequisites**\n\n- The Network Observability Operator is installed.\n- The `FlowCollector` custom resource (CR) is configured to export data to a Kafka topic.\n- You have `cluster-admin` permissions to edit the `FlowCollector` CR.\n\n**Procedure**\n\n1. Open the `FlowCollector` custom resource for editing by running the following command:\n\n   ```terminal\n   $ oc edit flowcollector cluster\n   ```\n2. Navigate to the `spec.kafka` section and add the `compression` parameter:\n\n   ```yaml\n   apiVersion: flows.netobserv.io/v1beta2\n   kind: FlowCollector\n   metadata:\n     name: cluster\n   spec:\n     deploymentModel: Kafka\n     kafka:\n       address: \"kafka-cluster-kafka-bootstrap.netobserv:9093\"\n       topic: \"network-flows\"\n       compression: \"lz4\"\n   ```\n\n   where:\n\n   `spec.kafka.compression`\n   :   Specifies the compression algorithm. Accepted values: `gzip`, `snappy`, `lz4`, `zstd`, `none`. Default is `none`.\n3. Save and apply the changes.\n\n**Verification**\n\n1. Confirm that the eBPF agent pods are running in the cluster by running the following command:\n\n   ```terminal\n   $ oc get pods -A -l app=netobserv-ebpf-agent\n   ```\n2. Verify that Kafka compression is active by running the following command:\n\n   ```terminal\n   $ oc logs -n <namespace> <pod_name> | grep \"KafkaCompression\"\n   ```\n\n   The output shows the compression configuration attribute in the eBPF agent pod logs.\n\n## Kafka compression codec reference {#network-observability-kafka-compression-codec-reference_network-observability-kafka-operator-scaling-network-flow-collection}\n\nCompare Kafka compression codecs to choose the optimal algorithm for your environment.\n\nThis reference applies to compression configured in both `spec.kafka.compression` for flow collection and `spec.exporters` for flow export.\n\n**Kafka compression codec comparison**\n\n| Codec | Compression ratio | CPU cost (producer) | Decompression cost | Notes |\n| --- | --- | --- | --- | --- |\n| `none` | 1x | N/A | N/A | No overhead. Use when CPU is the bottleneck. |\n| `lz4` | 2:1 to 3:1 | Very low | Very low | **Recommended default.** Best latency-to-compression ratio trade-off. |\n| `snappy` | 2:1 to 3:1 | Very low | Very low | Similar to `lz4`, with a slightly lower compression ratio. |\n| `zstd` | 3:1 to 5:1 | Moderate | Low | Higher compression ratio. Good for high-throughput clusters. |\n| `gzip` | 3:1 to 5:1 | High | Moderate | Maximum compression ratio but incurs a significant CPU cost. |\n\n> [!NOTE]\n> The compression ratios and CPU cost estimates are approximate values derived from upstream benchmarks, not from Network Observability-specific measurements. Actual results vary depending on flow record characteristics and batch sizes.\n\n## Additional resources {#additional-resources_network-observability-kafka-operator-scaling-network-flow-collection}\n\n- [Red Hat AMQ Streams documentation](https://access.redhat.com/documentation/en-us/red_hat_amq_streams)\n- [Configuring the FlowCollector resource with Kafka](/openshift-docs-markdown/observability/network_observability/configuring-operator#network-observability-flowcollector-kafka-config_network_observability)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/observability/network_observability/network-observability-kafka-operator-scaling-network-flow-collection.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="network-observability-kafka-operator-scaling-network-flow-collection" class="group scroll-mt-20">Scaling network flow collection with Kafka<a href="#network-observability-kafka-operator-scaling-network-flow-collection" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Scale your network flow collection by using the Kafka Operator to manage high-volume telemetry. Configure compression to reduce network bandwidth while balancing CPU overhead in large-scale cluster environments.</p>
      <h2 id="network-observability-kafka-for-large-scale-environments_network-observability-kafka-operator-scaling-network-flow-collection" class="group scroll-mt-20">Kafka deployment scenarios for network flows<a href="#network-observability-kafka-for-large-scale-environments_network-observability-kafka-operator-scaling-network-flow-collection" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The Kafka Operator manages high-throughput and low-latency data feeds for network flow forwarding. This architecture provides a resilient and scalable solution for handling telemetry data in large-scale cluster environments.</p>
      <h3 id="when-to-use-kafka-for-network-flow-collection_network-observability-kafka-operator-scaling-network-flow-collection" class="group scroll-mt-20">When to use Kafka for network flow collection<a href="#when-to-use-kafka-for-network-flow-collection_network-observability-kafka-operator-scaling-network-flow-collection" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Consider using Kafka to manage your network flow data when you experience the following circumstances:</p>
      <ul>
      <li>High flow volumes that overwhelm the default flowlogs-pipeline buffer</li>
      <li>Need for data persistence and replay capabilities</li>
      <li>Multiple consumers requiring access to the same flow data</li>
      <li>Requirements for horizontal scaling across multiple processing nodes</li>
      </ul>
      <p>For smaller deployments with moderate flow volumes, the default configuration without Kafka is typically sufficient.</p>
      <p>You can install the Kafka Operator as Red Hat AMQ Streams from the Operator Hub. See "Red Hat AMQ Streams".</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>To uninstall Kafka, refer to the uninstallation process that corresponds with the method you used to install.</p>
        </AlertDescription>
      </Alert>
      <h2 id="network-observability-kafka-compression-benefits_network-observability-kafka-operator-scaling-network-flow-collection" class="group scroll-mt-20">Reducing network bandwidth for flow telemetry<a href="#network-observability-kafka-compression-benefits_network-observability-kafka-operator-scaling-network-flow-collection" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Enable Kafka compression for flow records to optimize network traffic. This configuration reduces network bandwidth consumption while increasing the CPU load on the eBPF agent.</p>
      <p>By default, the Network Observability Operator sends network flow records to Kafka without compression. This minimizes CPU overhead but can consume significant network bandwidth in clusters with high traffic volumes.</p>
      <p>Enabling compression reduces the amount of data transmitted over the network, which is beneficial when the following cases are true:</p>
      <ul>
      <li>Network bandwidth is constrained or expensive</li>
      <li>Flow volumes are high and impacting network performance</li>
      <li>Kafka brokers are located on different nodes or in remote data centers</li>
      </ul>
      <p>The trade-off is increased CPU usage on the eBPF agent pods, which run as a <code>DaemonSet</code> on every node. Each of the different compression algorithms provide the following balances between compression ratio and CPU cost:</p>
      <ul>
      <li><code>lz4</code>: Very low CPU cost with 2 to 3 times compression. Best for most deployments.</li>
      <li><code>zstd</code>: Moderate CPU cost with 3 to 5 times compression. Good for bandwidth-constrained environments.</li>
      <li><code>snappy</code>: Similar to lz4 with slightly lower compression ratio.</li>
      <li><code>gzip</code>: High CPU cost with 3 to 5 times compression. Maximum compression at highest CPU expense.</li>
      <li><code>none</code>: No compression. Use when CPU is the bottleneck.</li>
      </ul>
      <p>Flow records contain many repeated fields (IP addresses, namespaces, node names) and compress efficiently, often reaching the higher end of these compression ratios.</p>
      <h2 id="network-observability-configuring-kafka-compression_network-observability-kafka-operator-scaling-network-flow-collection" class="group scroll-mt-20">Configure Kafka compression<a href="#network-observability-configuring-kafka-compression_network-observability-kafka-operator-scaling-network-flow-collection" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Configure the compression algorithm for network flow records exported to Kafka to optimize bandwidth and storage. This helps manage the data footprint of high-volume network telemetry.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>The Network Observability Operator is installed.</li>
      <li>The <code>FlowCollector</code> custom resource (CR) is configured to export data to a Kafka topic.</li>
      <li>You have <code>cluster-admin</code> permissions to edit the <code>FlowCollector</code> CR.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Open the <code>FlowCollector</code> custom resource for editing by running the following command:
      <CodeBlock code={"$ oc edit flowcollector cluster"} lang="terminal" />
      </li>
      <li>
      Navigate to the <code>spec.kafka</code> section and add the <code>compression</code> parameter:
      <CodeBlock code={"apiVersion: flows.netobserv.io/v1beta2\nkind: FlowCollector\nmetadata:\n  name: cluster\nspec:\n  deploymentModel: Kafka\n  kafka:\n    address: \"kafka-cluster-kafka-bootstrap.netobserv:9093\"\n    topic: \"network-flows\"\n    compression: \"lz4\""} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.kafka.compression</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the compression algorithm. Accepted values: <code>gzip</code>, <code>snappy</code>, <code>lz4</code>, <code>zstd</code>, <code>none</code>. Default is <code>none</code>.</p></dd>
      </dl>
      </li>
      <li>Save and apply the changes.</li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>
      Confirm that the eBPF agent pods are running in the cluster by running the following command:
      <CodeBlock code={"$ oc get pods -A -l app=netobserv-ebpf-agent"} lang="terminal" />
      </li>
      <li>
      Verify that Kafka compression is active by running the following command:
      <CodeBlock code={"$ oc logs -n <namespace> <pod_name> | grep \"KafkaCompression\""} lang="terminal" />
      <p>The output shows the compression configuration attribute in the eBPF agent pod logs.</p>
      </li>
      </ol>
      <h2 id="network-observability-kafka-compression-codec-reference_network-observability-kafka-operator-scaling-network-flow-collection" class="group scroll-mt-20">Kafka compression codec reference<a href="#network-observability-kafka-compression-codec-reference_network-observability-kafka-operator-scaling-network-flow-collection" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Compare Kafka compression codecs to choose the optimal algorithm for your environment.</p>
      <p>This reference applies to compression configured in both <code>spec.kafka.compression</code> for flow collection and <code>spec.exporters</code> for flow export.</p>
      <p><strong>Kafka compression codec comparison</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Codec</TableHead>
      <TableHead>Compression ratio</TableHead>
      <TableHead>CPU cost (producer)</TableHead>
      <TableHead>Decompression cost</TableHead>
      <TableHead>Notes</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>none</code></TableCell>
      <TableCell>1x</TableCell>
      <TableCell>N/A</TableCell>
      <TableCell>N/A</TableCell>
      <TableCell>No overhead. Use when CPU is the bottleneck.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>lz4</code></TableCell>
      <TableCell>2:1 to 3:1</TableCell>
      <TableCell>Very low</TableCell>
      <TableCell>Very low</TableCell>
      <TableCell><strong>Recommended default.</strong> Best latency-to-compression ratio trade-off.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>snappy</code></TableCell>
      <TableCell>2:1 to 3:1</TableCell>
      <TableCell>Very low</TableCell>
      <TableCell>Very low</TableCell>
      <TableCell>Similar to <code>lz4</code>, with a slightly lower compression ratio.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>zstd</code></TableCell>
      <TableCell>3:1 to 5:1</TableCell>
      <TableCell>Moderate</TableCell>
      <TableCell>Low</TableCell>
      <TableCell>Higher compression ratio. Good for high-throughput clusters.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>gzip</code></TableCell>
      <TableCell>3:1 to 5:1</TableCell>
      <TableCell>High</TableCell>
      <TableCell>Moderate</TableCell>
      <TableCell>Maximum compression ratio but incurs a significant CPU cost.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The compression ratios and CPU cost estimates are approximate values derived from upstream benchmarks, not from Network Observability-specific measurements. Actual results vary depending on flow record characteristics and batch sizes.</p>
        </AlertDescription>
      </Alert>
      <h2 id="additional-resources_network-observability-kafka-operator-scaling-network-flow-collection" class="group scroll-mt-20">Additional resources<a href="#additional-resources_network-observability-kafka-operator-scaling-network-flow-collection" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="https://access.redhat.com/documentation/en-us/red_hat_amq_streams">Red Hat AMQ Streams documentation</a></li>
      <li><a href="/openshift-docs-markdown/observability/network_observability/configuring-operator#network-observability-flowcollector-kafka-config_network_observability">Configuring the FlowCollector resource with Kafka</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
