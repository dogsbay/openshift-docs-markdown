---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: observability/network_observability/netobserv_cli/netobserv-cli-install.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"netobserv-cli-install","text":"Installing the Network Observability CLI"},{"depth":2,"slug":"network-observability-netoberv-cli-about_netobserv-cli-install","text":"About the Network Observability CLI"},{"depth":2,"slug":"network-observability-cli-install_netobserv-cli-install","text":"Installing the Network Observability CLI"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/observability/network_observability/netobserv_cli/netobserv-cli-install";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Installing the Network Observability CLI";
const description = "The Network Observability CLI (oc netobserv) is a standalone OpenShift CLI (oc) plugin used to debug and troubleshoot cluster network traffic. It operates…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["observability","observability/network_observability","observability/network_observability/netobserv_cli"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Installing the Network Observability CLI {#netobserv-cli-install}\n\nThe Network Observability CLI (oc netobserv) is a standalone OpenShift CLI (`oc`) plugin used to debug and troubleshoot cluster network traffic. It operates independently of the Network Observability Operator to gather immediate network performance diagnostics.\n\n## About the Network Observability CLI {#network-observability-netoberv-cli-about_netobserv-cli-install}\n\nUse the Network Observability CLI (`oc netobserv`) to quickly debug and troubleshoot networking issues. This tool provides instant, live insight into flows and packets without installing the Network Observability Operator.\n\nThe Network Observability CLI is a flow and packet visualization tool that relies on eBPF agents to stream collected data to an ephemeral collector pod. It requires no persistent storage during the capture. After the run, the output is transferred to your local machine.\n\n> [!IMPORTANT]\n> CLI capture is meant to run only for short durations, such as 8-10 minutes. If it runs for too long, it can be difficult to delete the running process.\n\n## Installing the Network Observability CLI {#network-observability-cli-install_netobserv-cli-install}\n\nThe Network Observability CLI gives you a lightweight way to quickly debug and troubleshoot network observability. It must be installed separately.\n\nInstalling the Network Observability CLI (`oc netobserv`) is a separate procedure from the Network Observability Operator installation. This means that, even if the Operator is installed from the software catalog, the `CLI` must be installed separately.\n\n> [!NOTE]\n> Users can optionally use Krew to install the `netobserv` CLI plugin. For more information, see \"Installing a CLI plugin with Krew\".\n\n**Prerequisites**\n\n- You must install the OpenShift CLI (`oc`).\n- You must have a macOS or Linux operating system.\n- You must install either `docker` or `podman`.\n\n> [!NOTE]\n> You can use `podman` or `docker` to run the installation commands. This procedure uses `podman`.\n\n**Procedure**\n\n1. Log in to the **Red Hat registry** by running the following command:\n\n   ```terminal\n   $ podman login registry.redhat.io\n   ```\n2. Extract the `oc-netobserv` file from the image by running the following commands:\n\n   ```terminal\n   $ podman create --name netobserv-cli registry.redhat.io/network-observability/network-observability-cli-rhel9:1.11\n   $ podman cp netobserv-cli:/oc-netobserv .\n   $ podman rm netobserv-cli\n   ```\n3. Move the extracted file to a directory that is on the system’s `PATH`, such as `/usr/local/bin/`, by running the following command:\n\n   ```terminal\n   $ sudo mv oc-netobserv /usr/local/bin/\n   ```\n\n**Verification**\n\n1. Verify that `oc netobserv` is available:\n\n   ```terminal\n   $ oc netobserv version\n   ```\n\n   This command should produce an outcome similar to the following example:\n\n   ```terminal\n   Netobserv CLI version <version>\n   ```\n\n**Additional resources**\n\n- [Installing and using CLI plugins](/openshift-docs-markdown/cli_reference/openshift_cli/extending-cli-plugins#cli-installing-plugins_cli-extend-plugins)\n- [Installing the CLI Manager Operator](/openshift-docs-markdown/cli_reference/cli_manager/cli-manager-install#installing-cli-manager)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/observability/network_observability/netobserv_cli/netobserv-cli-install.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="netobserv-cli-install" class="group scroll-mt-20">Installing the Network Observability CLI<a href="#netobserv-cli-install" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>The Network Observability CLI (oc netobserv) is a standalone OpenShift CLI (<code>oc</code>) plugin used to debug and troubleshoot cluster network traffic. It operates independently of the Network Observability Operator to gather immediate network performance diagnostics.</p>
      <h2 id="network-observability-netoberv-cli-about_netobserv-cli-install" class="group scroll-mt-20">About the Network Observability CLI<a href="#network-observability-netoberv-cli-about_netobserv-cli-install" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Use the Network Observability CLI (<code>oc netobserv</code>) to quickly debug and troubleshoot networking issues. This tool provides instant, live insight into flows and packets without installing the Network Observability Operator.</p>
      <p>The Network Observability CLI is a flow and packet visualization tool that relies on eBPF agents to stream collected data to an ephemeral collector pod. It requires no persistent storage during the capture. After the run, the output is transferred to your local machine.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>CLI capture is meant to run only for short durations, such as 8-10 minutes. If it runs for too long, it can be difficult to delete the running process.</p>
        </AlertDescription>
      </Alert>
      <h2 id="network-observability-cli-install_netobserv-cli-install" class="group scroll-mt-20">Installing the Network Observability CLI<a href="#network-observability-cli-install_netobserv-cli-install" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The Network Observability CLI gives you a lightweight way to quickly debug and troubleshoot network observability. It must be installed separately.</p>
      <p>Installing the Network Observability CLI (<code>oc netobserv</code>) is a separate procedure from the Network Observability Operator installation. This means that, even if the Operator is installed from the software catalog, the <code>CLI</code> must be installed separately.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Users can optionally use Krew to install the <code>netobserv</code> CLI plugin. For more information, see "Installing a CLI plugin with Krew".</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You must install the OpenShift CLI (<code>oc</code>).</li>
      <li>You must have a macOS or Linux operating system.</li>
      <li>You must install either <code>docker</code> or <code>podman</code>.</li>
      </ul>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>You can use <code>podman</code> or <code>docker</code> to run the installation commands. This procedure uses <code>podman</code>.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Log in to the <strong>Red Hat registry</strong> by running the following command:
      <CodeBlock code={"$ podman login registry.redhat.io"} lang="terminal" />
      </li>
      <li>
      Extract the <code>oc-netobserv</code> file from the image by running the following commands:
      <CodeBlock code={"$ podman create --name netobserv-cli registry.redhat.io/network-observability/network-observability-cli-rhel9:1.11\n$ podman cp netobserv-cli:/oc-netobserv .\n$ podman rm netobserv-cli"} lang="terminal" />
      </li>
      <li>
      Move the extracted file to a directory that is on the system’s <code>PATH</code>, such as <code>/usr/local/bin/</code>, by running the following command:
      <CodeBlock code={"$ sudo mv oc-netobserv /usr/local/bin/"} lang="terminal" />
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>
      Verify that <code>oc netobserv</code> is available:
      <CodeBlock code={"$ oc netobserv version"} lang="terminal" />
      <p>This command should produce an outcome similar to the following example:</p>
      <CodeBlock code={"Netobserv CLI version <version>"} lang="terminal" />
      </li>
      </ol>
      <p><strong>Additional resources</strong></p>
      <ul>
      <li><a href="/openshift-docs-markdown/cli_reference/openshift_cli/extending-cli-plugins#cli-installing-plugins_cli-extend-plugins">Installing and using CLI plugins</a></li>
      <li><a href="/openshift-docs-markdown/cli_reference/cli_manager/cli-manager-install#installing-cli-manager">Installing the CLI Manager Operator</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
