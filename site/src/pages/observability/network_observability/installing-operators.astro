---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: observability/network_observability/installing-operators.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"installing-network-observability-operators","text":"Installing the Network Observability Operator"},{"depth":2,"slug":"network-observability-without-loki_network_observability","text":"Network observability without Loki"},{"depth":2,"slug":"network-observability-loki-installation_network_observability","text":"Installing the Loki Operator"},{"depth":3,"slug":"network-observability-loki-secret_network_observability","text":"Creating a secret for Loki storage"},{"depth":3,"slug":"network-observability-lokistack-create_network_observability","text":"Creating a LokiStack custom resource"},{"depth":3,"slug":"network-observability-role-based-access-control-for-loki-logs_network_observability","text":"Role-based access control for Loki logs"},{"depth":4,"slug":"grant-non-admin-users-cluster-wide-log-access_network_observability","text":"Grant non-admin users cluster-wide log access"},{"depth":3,"slug":"network-observability-lokistack-configuring-ingestion_network_observability","text":"LokiStack ingestion limits and health alerts"},{"depth":2,"slug":"network-observability-operator-installation_network_observability","text":"Installing the Network Observability Operator"},{"depth":3,"slug":"network-observability-important-flowcollector-configuration-considerations_network_observability","text":"Important FlowCollector configuration considerations"},{"depth":2,"slug":"network-observability-updating-migrating_network_observability","text":"Migrating removed stored versions of the FlowCollector CRD"},{"depth":2,"slug":"network-observability-multi-tenancy_network_observability","text":"Enabling multi-tenancy in network observability"},{"depth":2,"slug":"network-observability-operator-uninstall_network_observability","text":"Uninstalling the Network Observability Operator"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/observability/network_observability/installing-operators";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Installing the Network Observability Operator";
const description = "Installing the Loki Operator is recommended before using the Network Observability Operator. You can use network observability without Loki, but special…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["observability","observability/network_observability"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Installing the Network Observability Operator {#installing-network-observability-operators}\n\nInstalling the Loki Operator is recommended before using the Network Observability Operator. You can use network observability without Loki, but special considerations apply if you only need metrics or external exporters.\n\nThe Loki Operator integrates a gateway that implements multi-tenancy and authentication with Loki for data flow storage. The `LokiStack` resource manages Loki, which is a scalable, highly-available, multi-tenant log aggregation system, and a web proxy with OpenShift Container Platform authentication. The `LokiStack` proxy uses OpenShift Container Platform authentication to enforce multi-tenancy and facilitate the saving and indexing of data in Loki log stores.\n\n## Network observability without Loki {#network-observability-without-loki_network_observability}\n\nCompare the features available with network observability with and without installing the Loki Operator.\n\nIf you only want to export flows to a Kafka consumer or IPFIX collector, or you only need dashboard metrics, then you do not need to install Loki or provide storage for Loki. The following table compares available features with and without Loki.\n\n**Comparison of feature availability with and without Loki**\n\n|  | **With Loki** | **Without Loki** |\n| --- | --- | --- |\n| **Exporters** | X | X |\n| **Multi-tenancy** | X | X |\n| **Complete filtering and aggregations capabilities** <sup>\\[1\\]</sup> | X |  |\n| **Partial filtering and aggregations capabilities** <sup>\\[2\\]</sup> | X | X |\n| **Flow-based metrics and dashboards** | X | X |\n| **Traffic flows view overview** <sup>\\[3\\]</sup> | X | X |\n| **Traffic flows view table** | X |  |\n| **Topology view** | X | X |\n| **OpenShift Container Platform console Network Traffic tab integration** | X | X |\n\n1. Such as per pod.\n2. Such as per workload or namespace.\n3. Statistics on packet drops are only available with Loki.\n\n**Additional resources**\n{._additional-resources}\n\n- [Export enriched network flow data](/openshift-docs-markdown/observability/network_observability/configuring-operator#network-observability-enriched-flows_network_observability)\n\n## Installing the Loki Operator {#network-observability-loki-installation_network_observability}\n\nInstall the supported Loki Operator version from the software catalog to enable the secure `LokiStack` instance, which provides automatic in-cluster authentication and authorization for network observability.\n\nThe [Loki Operator versions 6.0+](https://catalog.redhat.com/software/containers/openshift-logging/loki-rhel9-operator/64479927e1820602a81cdf13) are the supported Loki Operator versions for network observability; these versions provide the ability to create a `LokiStack` instance using the `openshift-network` tenant configuration mode and provide fully-automatic, in-cluster authentication and authorization support for network observability.\n\n**Prerequisites**\n\n- You have administrator permissions.\n- You have access to the OpenShift Container Platform web console.\n- You have access to a supported object store. For example: AWS S3, Google Cloud Storage, Azure, Swift, Minio, or OpenShift Data Foundation.\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, click **Ecosystem** → **Software Catalog**.\n2. Choose  **Loki Operator** from the list of available Operators, and click **Install**.\n3. Under **Installation Mode**, select **All namespaces on the cluster**.\n\n**Verification**\n\n1. Verify that you installed the Loki Operator. Visit the **Ecosystem** → **Installed Operators** page and look for **Loki Operator**.\n2. Verify that **Loki Operator** is listed with **Status** as **Succeeded** in all the projects.\n\n> [!IMPORTANT]\n> To uninstall Loki, refer to the uninstallation process that corresponds with the method you used to install Loki. You might have remaining `ClusterRoles` and `ClusterRoleBindings`, data stored in object store, and persistent volume that must be removed.\n\n### Creating a secret for Loki storage {#network-observability-loki-secret_network_observability}\n\nCreate a secret with cloud storage credentials, such as for Amazon Web Services (AWS), to allow the Loki Operator to access the necessary object store for log persistence.\n\nThe Loki Operator supports a few log storage options, such as AWS S3, Google Cloud Storage, Azure, Swift, Minio, OpenShift Data Foundation. The following example shows how to create a secret for AWS S3 storage. The secret created in this example, `loki-s3`, is referenced in \"Creating a LokiStack custom resource\". You can create this secret in the web console or CLI.\n\n**Procedure**\n\n1. Using the web console, navigate to the **Project** → **All Projects** dropdown and select **Create Project**.\n2. Name the project `netobserv-loki` and click **Create**.\n3. Navigate to the Import icon, **+**, in the top right corner. Paste your YAML file into the editor.\n\n   The following shows an example secret YAML file for S3 storage:\n\n   ```yaml\n   apiVersion: v1\n   kind: Secret\n   metadata:\n     name: loki-s3\n     namespace: netobserv-loki\n   stringData:\n     access_key_id: QUtJQUlPU0ZPRE5ON0VYQU1QTEUK\n     access_key_secret: d0phbHJYVXRuRkVNSS9LN01ERU5HL2JQeFJmaUNZRVhBTVBMRUtFWQo=\n     bucketnames: s3-bucket-name\n     endpoint: https://s3.eu-central-1.amazonaws.com\n     region: eu-central-1\n   ```\n\n   where:\n\n   `metadata.namespace`\n   :   Specifies the namespace for the Loki S3 secret. While this example uses `netobserv-loki`, you can use a different namespace for different components.\n\n   `stringData.access_key_id`\n   :   Specifies the access key ID for the S3 bucket.\n\n   `stringData.access_key_secret`\n   :   Specifies the secret access key for the S3 bucket.\n\n   `stringData.bucketnames`\n   :   Specifies the name of the S3 bucket.\n\n   `stringData.endpoint`\n   :   Specifies the endpoint URL for the S3 service.\n\n   `stringData.region`\n   :   Specifies the AWS region where the bucket is located.\n\n**Verification**\n\n- After you create the secret, you view the secret listed under **Workloads** → **Secrets** in the web console.\n\n### Creating a LokiStack custom resource {#network-observability-lokistack-create_network_observability}\n\nDeploy the `LokiStack` custom resource using the web console or OpenShift CLI (`oc`), ensuring you configure the correct namespace, deployment size, and secret name for Loki object storage.\n\nYou can deploy a `LokiStack` custom resource (CR) to create a namespace or new project.\n\n**Procedure**\n\n1. Navigate to **Ecosystem** → **Installed Operators**, viewing **All projects** from the **Project** dropdown.\n2. Look for **Loki Operator**. In the details, under **Provided APIs**, select **LokiStack**.\n3. Click **Create LokiStack**.\n4. Ensure the following fields are specified in either **Form View** or **YAML view**:\n\n   ```yaml\n   apiVersion: loki.grafana.com/v1\n   kind: LokiStack\n   metadata:\n     name: loki\n     namespace: netobserv-loki\n   spec:\n     size: 1x.small\n     storage:\n       schemas:\n       - version: v13\n         effectiveDate: '2022-06-01'\n       secret:\n         name: loki-s3\n         type: s3\n     storageClassName: gp3\n     tenants:\n       mode: openshift-network\n   ```\n\n   where:\n\n   `metadata.namespace`\n   :   Specifies the namespace for the `LokiStack` resource. While this example uses `netobserv-loki`, you can use a different namespace for different components.\n\n   `spec.size`\n   :   Specifies the deployment size. In Loki Operator 5.8 and later versions, the supported size options for production instances of Loki are `1x.extra-small`, `1x.small`, or `1x.medium`.\n\n   > [!IMPORTANT]\n   > It is not possible to change the number `1x` for the deployment size.\n\n   `spec.storageClassName`\n   :   Specifies a storage class name that is available on the cluster for `ReadWriteOnce` access mode. For best performance, specify a storage class that allocates block storage. Use the `oc get storageclasses` command to see available storage classes on your cluster.\n\n   > [!IMPORTANT]\n   > You must not reuse the same `LokiStack` custom resource that is used for logging.\n5. Click **Create**.\n\n### Role-based access control for Loki logs {#network-observability-role-based-access-control-for-loki-logs_network_observability}\n\nConfigure role-based access control to grant users permission to view application, infrastructure, or audit logs in Loki.\n\nBy default, logging 5.8 and later does not grant users access to logs. You must configure role-based access control to grant users permission to view specific log types.\n\nFor more information on access control for Loki logs, see: \"Fine grained access for Loki logs\" in the Red Hat OpenShift Logging Operator documentation.\n\n#### Grant non-admin users cluster-wide log access {#grant-non-admin-users-cluster-wide-log-access_network_observability}\n\nAdd users to a custom admin group to grant cluster-wide log access without making them cluster administrators. This is useful for senior engineers who need full log visibility but should not have cluster modification privileges.\n\nUsers who are members of any group specified in the `adminGroups` field of the `LokiStack` custom resource (CR) have the same read access to logs as administrators.\n\n```yaml {title=\"Example LokiStack CR\"}\napiVersion: loki.grafana.com/v1\nkind: LokiStack\nmetadata:\n  name: loki\n  namespace: netobserv-loki\nspec:\n  tenants:\n    mode: openshift-network\n    openshift:\n      adminGroups:\n      - cluster-admin\n      - custom-admin-group\n```\n\nwhere:\n\n`spec.tenants.mode`\n:   Specifies the tenant mode. Must be `openshift-network` for network observability.\n\n`spec.tenants.openshift.adminGroups`\n:   Specifies the list of groups whose members have cluster-wide log access. Defaults to `system:cluster-admins`, `cluster-admin`, and `dedicated-admin`. Set to `[]` to disable.\n\n**Additional resources**\n{._additional-resources}\n\n- [Fine grained access for Loki logs](https://docs.redhat.com/en/documentation/red_hat_openshift_logging/6.5/html/configuring_logging/configuring-lokistack-storage#logging-loki-log-access_configuring-the-log-store)\n\n### LokiStack ingestion limits and health alerts {#network-observability-lokistack-configuring-ingestion_network_observability}\n\nThe `LokiStack` instance includes default ingestion and query limits that can be overridden by administrators to manage performance and prevent system alerts or errors.\n\n> [!NOTE]\n> You might want to update the ingestion and query limits if you get Loki errors showing up in the Console plugin, or in `flowlogs-pipeline` logs.\n\nHere is an example of configured limits:\n\n```yaml\nspec:\n  limits:\n    global:\n      ingestion:\n        ingestionBurstSize: 40\n        ingestionRate: 20\n        maxGlobalStreamsPerTenant: 25000\n      queries:\n        maxChunksPerQuery: 2000000\n        maxEntriesLimitPerQuery: 10000\n        maxQuerySeries: 3000\n```\n\nFor more information about these settings, see \"LokiStack API reference\".\n\n**Additional resources**\n{._additional-resources}\n\n- [Creating a LokiStack custom resource](/openshift-docs-markdown/observability/network_observability/installing-operators#network-observability-lokistack-create_network_observability)\n- [Loki object storage](https://docs.redhat.com/en/documentation/red_hat_openshift_logging/latest/html/configuring_logging/configuring-lokistack-storage#logging-loki-storage_configuring-the-log-store)\n- [Loki deployment sizing](https://docs.redhat.com/en/documentation/red_hat_openshift_logging/latest/html/configuring_logging/configuring-lokistack-storage.html#loki-sizing_configuring-the-log-store)\n- [LokiStack API reference](https://loki-operator.dev/docs/api.md/#loki-grafana-com-v1-IngestionLimitSpec)\n- [Flow Collector API Reference](/openshift-docs-markdown/observability/network_observability/flowcollector-api#network-observability-flowcollector-api-specifications_network_observability)\n- [Flow Collector sample resource](/openshift-docs-markdown/observability/network_observability/configuring-operator#network-observability-flowcollector-view_network_observability)\n\n## Installing the Network Observability Operator {#network-observability-operator-installation_network_observability}\n\nInstall the Network Observability Operator and use the setup wizard to create the `FlowCollector` custom resource definition (CRD) to complete the initial configuration.\n\nYou can set specifications in the web console when you create the `FlowCollector`.\n\n> [!IMPORTANT]\n> The actual memory consumption of the Operator depends on your cluster size and the number of resources deployed. Memory consumption might need to be adjusted accordingly. For more information refer to \"Network Observability controller manager pod runs out of memory\" in the \"Important Flow Collector configuration considerations\" section.\n\n**Prerequisites**\n\n- If you choose to use Loki, install the [Loki Operator version 5.7+](https://catalog.redhat.com/software/containers/openshift-logging/loki-rhel8-operator/622b46bcae289285d6fcda39).\n- You must have `cluster-admin` privileges.\n- One of the following supported architectures is required: `amd64`, `ppc64le`, `arm64`, or `s390x`.\n- Any CPU supported by Red Hat Enterprise Linux (RHEL) 9.\n- Must be configured with OVN-Kubernetes as the main network plugin, and optionally using secondary interfaces with Multus and SR-IOV.\n\n> [!NOTE]\n> Additionally, this installation example uses the `netobserv` namespace, which is used across all components. You can optionally use a different namespace.\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, click **Ecosystem** → **Software Catalog**.\n2. Choose  **Network Observability Operator** from the list of available Operators in the software catalog, and click **Install**.\n3. Select the checkbox `Enable Operator recommended cluster monitoring on this Namespace`.\n4. Navigate to **Operators** → **Installed Operators**. Under Provided APIs for Network Observability, select the **Flow Collector** link.\n5. Follow the **Network Observability FlowCollector setup** wizard.\n6. Click **Create**.\n\n**Verification**\n\nTo confirm this was successful, when you navigate to **Observe** you should see **Network Traffic** listed in the options.\n\nIn the absence of **Application Traffic** within the OpenShift Container Platform cluster, default filters might show that there are \"No results\", which results in no visual flow. Beside the filter selections, select **Clear all filters** to see the flow.\n\n### Important FlowCollector configuration considerations {#network-observability-important-flowcollector-configuration-considerations_network_observability}\n\nReview essential `FlowCollector` configuration options before initial deployment to avoid pod disruptions caused by later reconfiguration. Key settings include Kafka integration, enriched flow data exports, SR-IOV traffic monitoring, and advanced tracking for DNS and packet drops.\n\nOnce you create the `FlowCollector` instance, you can reconfigure it, but the pods are terminated and recreated again, which can be disruptive.\n\nTherefore, you can consider configuring the following options when creating the `FlowCollector` for the first time.\n\n**Additional resources**\n{._additional-resources}\n\n- [Configuring the Flow Collector resource with Kafka](/openshift-docs-markdown/observability/network_observability/configuring-operator#network-observability-flowcollector-kafka-config_network_observability)\n- [Export enriched network flow data to Kafka or IPFIX](/openshift-docs-markdown/observability/network_observability/configuring-operator#network-observability-enriched-flows_network_observability)\n- [Configuring monitoring for SR-IOV interface traffic](/openshift-docs-markdown/observability/network_observability/network-observability-secondary-networks#network-observability-SR-IOV-config_network-observability-secondary-networks)\n- [Working with conversation tracking](/openshift-docs-markdown/observability/network_observability/observing-network-traffic#network-observability-working-with-conversations_nw-observe-network-traffic)\n- [Working with DNS tracking](/openshift-docs-markdown/observability/network_observability/observing-network-traffic#network-observability-dns-tracking_nw-observe-network-traffic)\n- [Working with packet drops](/openshift-docs-markdown/observability/network_observability/observing-network-traffic#network-observability-packet-drops_nw-observe-network-traffic)\n- [Flow Collector API Reference](/openshift-docs-markdown/observability/network_observability/flowcollector-api#network-observability-flowcollector-api-specifications_network_observability)\n- [Flow Collector sample resource](/openshift-docs-markdown/observability/network_observability/configuring-operator#network-observability-flowcollector-view_network_observability)\n- [Resource considerations](/openshift-docs-markdown/observability/network_observability/configuring-operator#network-observability-resources-table_network_observability)\n- [Troubleshooting network observability controller manager pod runs out of memory](/openshift-docs-markdown/observability/network_observability/troubleshooting-network-observability#controller-manager-pod-runs-out-of-memory_network-observability-troubleshooting)\n- [Network observability architecture](/openshift-docs-markdown/observability/network_observability/understanding-network-observability-operator#network-observability-architecture_nw-network-observability-operator)\n\n## Migrating removed stored versions of the FlowCollector CRD {#network-observability-updating-migrating_network_observability}\n\nManually remove the deprecated `v1alpha1` version from the `FlowCollector` custom resource definition (CRD) `storedVersion` list to prevent upgrade errors and successfully migrate to Network Observability Operator 1.6.\n\nThere are two options to remove stored versions:\n\n1. Use the Storage Version Migrator Operator.\n2. Uninstall and reinstall the Network Observability Operator, ensuring that the installation is in a clean state.\n\n**Prerequisites**\n\n- You have an older version of the Operator installed, and you want to prepare your cluster to install the latest version of the Operator. Or you have attempted to install the Network Observability Operator 1.6 and run into the error: `Failed risk of data loss updating \"flowcollectors.flows.netobserv.io\": new CRD removes version v1alpha1 that is listed as a stored version on the existing CRD`.\n\n**Procedure**\n\n1. Verify that the old `FlowCollector` CRD version is still referenced in the `storedVersion`:\n\n   ```terminal\n   $ oc get crd flowcollectors.flows.netobserv.io -ojsonpath='{.status.storedVersions}'\n   ```\n2. If `v1alpha1` appears in the list of results, proceed with **Step a** to use the Kubernetes Storage Version Migrator or **Step b** to uninstall and reinstall the CRD and the Operator.\n\n   1. **Option 1: Kubernetes Storage Version Migrator**: Create a YAML to define the `StorageVersionMigration` object, for example `migrate-flowcollector-v1alpha1.yaml`:\n\n      ```yaml\n      apiVersion: migration.k8s.io/v1alpha1\n      kind: StorageVersionMigration\n      metadata:\n        name: migrate-flowcollector-v1alpha1\n      spec:\n        resource:\n          group: flows.netobserv.io\n          resource: flowcollectors\n          version: v1alpha1\n      ```\n\n      1. Save the file.\n      2. Apply the `StorageVersionMigration` by running the following command:\n\n         ```terminal\n         $ oc apply -f migrate-flowcollector-v1alpha1.yaml\n         ```\n      3. Update the `FlowCollector` CRD to manually remove `v1alpha1` from the `storedVersion`:\n\n         ```terminal\n         $ oc edit crd flowcollectors.flows.netobserv.io\n         ```\n   2. **Option 2: Reinstall**: Save the Network Observability Operator 1.5 version of the `FlowCollector` CR to a file, for example `flowcollector-1.5.yaml`.\n\n      ```terminal\n      $ oc get flowcollector cluster -o yaml > flowcollector-1.5.yaml\n      ```\n\n      1. Follow the steps in \"Uninstalling the Network Observability Operator\", which uninstalls the Operator and removes the existing `FlowCollector` CRD.\n      2. Install the Network Observability Operator latest version, 1.6.0.\n      3. Create the `FlowCollector` using backup that was saved in Step b.\n\n**Verification**\n\n- Run the following command:\n\n  ```terminal\n  $ oc get crd flowcollectors.flows.netobserv.io -ojsonpath='{.status.storedVersions}'\n  ```\n\n  The list of results should no longer show `v1alpha1` and only show the latest version, `v1beta1`.\n\n## Enabling multi-tenancy in network observability {#network-observability-multi-tenancy_network_observability}\n\nEnable multi-tenancy in network observability by configuring cluster roles and namespace roles to grant project administrators and developers granular, restricted access to flows and metrics in Loki and Prometheus.\n\nAccess is enabled for project administrators. Project administrators who have limited access to some namespaces can access flows for only those namespaces.\n\nFor Developers, multi-tenancy is available for both Loki and Prometheus but requires different access rights.\n\n**Prerequisite**\n\n- If you are using Loki, you have installed at least [Loki Operator version 5.7](https://catalog.redhat.com/software/containers/openshift-logging/loki-rhel8-operator/622b46bcae289285d6fcda39).\n- You must be logged in as a project administrator.\n\n**Procedure**\n\n- For per-tenant access, you must have the `netobserv-loki-reader` cluster role and the `netobserv-metrics-reader` namespace role to use the developer perspective. Run the following commands for this level of access:\n\n  ```terminal\n  $ oc adm policy add-cluster-role-to-user netobserv-loki-reader <user_group_or_name>\n  ```\n\n  ```terminal\n  $ oc adm policy add-role-to-user netobserv-metrics-reader <user_group_or_name> -n <namespace>\n  ```\n- For cluster-wide access, non-cluster-administrators must have the `netobserv-loki-reader`, `cluster-monitoring-view`, and `netobserv-metrics-reader` cluster roles. In this scenario, you can use either the admin perspective or the developer perspective. Run the following commands for this level of access:\n\n  ```terminal\n  $ oc adm policy add-cluster-role-to-user netobserv-loki-reader <user_group_or_name>\n  ```\n\n  ```terminal\n  $ oc adm policy add-cluster-role-to-user cluster-monitoring-view <user_group_or_name>\n  ```\n\n  ```terminal\n  $ oc adm policy add-cluster-role-to-user netobserv-metrics-reader <user_group_or_name>\n  ```\n\n**Additional resources**\n{._additional-resources}\n\n- [Kubernetes Storage Version Migrator Operator](/openshift-docs-markdown/operators/operator-reference#cluster-kube-storage-version-migrator-operator_operator-reference)\n\n## Uninstalling the Network Observability Operator {#network-observability-operator-uninstall_network_observability}\n\nUninstall the Network Observability Operator using the OpenShift Container Platform web console Operator Hub, working in the **Ecosystem** → **Installed Operators** area.\n\n**Procedure**\n\n1. Remove the `FlowCollector` custom resource.\n\n   1. Click **Flow Collector**, which is next to the **Network Observability Operator** in the **Provided APIs** column.\n   2. Click the Options menu ![](/openshift-docs-markdown/images/kebab.png \"Options menu\") for the **cluster** and select **Delete FlowCollector**.\n2. Uninstall the Network Observability Operator.\n\n   1. Navigate back to the **Ecosystem** → **Installed Operators** area.\n   2. Click the Options menu ![](/openshift-docs-markdown/images/kebab.png \"Options menu\") next to the  **Network Observability Operator** and select **Uninstall Operator**.\n   3. **Home** → **Projects** and select `openshift-netobserv-operator`\n   4. Navigate to **Actions** and select **Delete Project**\n3. Remove the `FlowCollector` custom resource definition (CRD).\n\n   1. Navigate to **Administration** → **CustomResourceDefinitions**.\n   2. Look for **FlowCollector** and click the Options menu ![](/openshift-docs-markdown/images/kebab.png \"Options menu\").\n   3. Select **Delete CustomResourceDefinition**.\n\n      > [!IMPORTANT]\n      > The Loki Operator and Kafka remain if they were installed and must be removed separately. Additionally, you might have remaining data stored in an object store, and a persistent volume that must be removed.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/observability/network_observability/installing-operators.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="installing-network-observability-operators" class="group scroll-mt-20">Installing the Network Observability Operator<a href="#installing-network-observability-operators" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Installing the Loki Operator is recommended before using the Network Observability Operator. You can use network observability without Loki, but special considerations apply if you only need metrics or external exporters.</p>
      <p>The Loki Operator integrates a gateway that implements multi-tenancy and authentication with Loki for data flow storage. The <code>LokiStack</code> resource manages Loki, which is a scalable, highly-available, multi-tenant log aggregation system, and a web proxy with OpenShift Container Platform authentication. The <code>LokiStack</code> proxy uses OpenShift Container Platform authentication to enforce multi-tenancy and facilitate the saving and indexing of data in Loki log stores.</p>
      <h2 id="network-observability-without-loki_network_observability" class="group scroll-mt-20">Network observability without Loki<a href="#network-observability-without-loki_network_observability" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Compare the features available with network observability with and without installing the Loki Operator.</p>
      <p>If you only want to export flows to a Kafka consumer or IPFIX collector, or you only need dashboard metrics, then you do not need to install Loki or provide storage for Loki. The following table compares available features with and without Loki.</p>
      <p><strong>Comparison of feature availability with and without Loki</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead></TableHead>
      <TableHead><strong>With Loki</strong></TableHead>
      <TableHead><strong>Without Loki</strong></TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><strong>Exporters</strong></TableCell>
      <TableCell>X</TableCell>
      <TableCell>X</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Multi-tenancy</strong></TableCell>
      <TableCell>X</TableCell>
      <TableCell>X</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Complete filtering and aggregations capabilities</strong> <Fragment set:html={"<sup>"} />[1]<Fragment set:html={"</sup>"} /></TableCell>
      <TableCell>X</TableCell>
      <TableCell></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Partial filtering and aggregations capabilities</strong> <Fragment set:html={"<sup>"} />[2]<Fragment set:html={"</sup>"} /></TableCell>
      <TableCell>X</TableCell>
      <TableCell>X</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Flow-based metrics and dashboards</strong></TableCell>
      <TableCell>X</TableCell>
      <TableCell>X</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Traffic flows view overview</strong> <Fragment set:html={"<sup>"} />[3]<Fragment set:html={"</sup>"} /></TableCell>
      <TableCell>X</TableCell>
      <TableCell>X</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Traffic flows view table</strong></TableCell>
      <TableCell>X</TableCell>
      <TableCell></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Topology view</strong></TableCell>
      <TableCell>X</TableCell>
      <TableCell>X</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>OpenShift Container Platform console Network Traffic tab integration</strong></TableCell>
      <TableCell>X</TableCell>
      <TableCell>X</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <ol>
      <li>Such as per pod.</li>
      <li>Such as per workload or namespace.</li>
      <li>Statistics on packet drops are only available with Loki.</li>
      </ol>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="/openshift-docs-markdown/observability/network_observability/configuring-operator#network-observability-enriched-flows_network_observability">Export enriched network flow data</a></li></ul>
      </section>
      <h2 id="network-observability-loki-installation_network_observability" class="group scroll-mt-20">Installing the Loki Operator<a href="#network-observability-loki-installation_network_observability" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Install the supported Loki Operator version from the software catalog to enable the secure <code>LokiStack</code> instance, which provides automatic in-cluster authentication and authorization for network observability.</p>
      <p>The <a href="https://catalog.redhat.com/software/containers/openshift-logging/loki-rhel9-operator/64479927e1820602a81cdf13">Loki Operator versions 6.0+</a> are the supported Loki Operator versions for network observability; these versions provide the ability to create a <code>LokiStack</code> instance using the <code>openshift-network</code> tenant configuration mode and provide fully-automatic, in-cluster authentication and authorization support for network observability.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have administrator permissions.</li>
      <li>You have access to the OpenShift Container Platform web console.</li>
      <li>You have access to a supported object store. For example: AWS S3, Google Cloud Storage, Azure, Swift, Minio, or OpenShift Data Foundation.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, click <strong>Ecosystem</strong> → <strong>Software Catalog</strong>.</li>
      <li>Choose  <strong>Loki Operator</strong> from the list of available Operators, and click <strong>Install</strong>.</li>
      <li>Under <strong>Installation Mode</strong>, select <strong>All namespaces on the cluster</strong>.</li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>Verify that you installed the Loki Operator. Visit the <strong>Ecosystem</strong> → <strong>Installed Operators</strong> page and look for <strong>Loki Operator</strong>.</li>
      <li>Verify that <strong>Loki Operator</strong> is listed with <strong>Status</strong> as <strong>Succeeded</strong> in all the projects.</li>
      </ol>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>To uninstall Loki, refer to the uninstallation process that corresponds with the method you used to install Loki. You might have remaining <code>ClusterRoles</code> and <code>ClusterRoleBindings</code>, data stored in object store, and persistent volume that must be removed.</p>
        </AlertDescription>
      </Alert>
      <h3 id="network-observability-loki-secret_network_observability" class="group scroll-mt-20">Creating a secret for Loki storage<a href="#network-observability-loki-secret_network_observability" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">Create a secret with cloud storage credentials, such as for Amazon Web Services (AWS), to allow the Loki Operator to access the necessary object store for log persistence.</p>
      <p>The Loki Operator supports a few log storage options, such as AWS S3, Google Cloud Storage, Azure, Swift, Minio, OpenShift Data Foundation. The following example shows how to create a secret for AWS S3 storage. The secret created in this example, <code>loki-s3</code>, is referenced in "Creating a LokiStack custom resource". You can create this secret in the web console or CLI.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Using the web console, navigate to the <strong>Project</strong> → <strong>All Projects</strong> dropdown and select <strong>Create Project</strong>.</li>
      <li>Name the project <code>netobserv-loki</code> and click <strong>Create</strong>.</li>
      <li>
      Navigate to the Import icon, <strong>+</strong>, in the top right corner. Paste your YAML file into the editor.
      <p>The following shows an example secret YAML file for S3 storage:</p>
      <CodeBlock code={"apiVersion: v1\nkind: Secret\nmetadata:\n  name: loki-s3\n  namespace: netobserv-loki\nstringData:\n  access_key_id: QUtJQUlPU0ZPRE5ON0VYQU1QTEUK\n  access_key_secret: d0phbHJYVXRuRkVNSS9LN01ERU5HL2JQeFJmaUNZRVhBTVBMRUtFWQo=\n  bucketnames: s3-bucket-name\n  endpoint: https://s3.eu-central-1.amazonaws.com\n  region: eu-central-1"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>metadata.namespace</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the namespace for the Loki S3 secret. While this example uses <code>netobserv-loki</code>, you can use a different namespace for different components.</p></dd>
      <dt class="font-semibold"><code>stringData.access_key_id</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the access key ID for the S3 bucket.</p></dd>
      <dt class="font-semibold"><code>stringData.access_key_secret</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the secret access key for the S3 bucket.</p></dd>
      <dt class="font-semibold"><code>stringData.bucketnames</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the S3 bucket.</p></dd>
      <dt class="font-semibold"><code>stringData.endpoint</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the endpoint URL for the S3 service.</p></dd>
      <dt class="font-semibold"><code>stringData.region</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the AWS region where the bucket is located.</p></dd>
      </dl>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul><li>After you create the secret, you view the secret listed under <strong>Workloads</strong> → <strong>Secrets</strong> in the web console.</li></ul>
      <h3 id="network-observability-lokistack-create_network_observability" class="group scroll-mt-20">Creating a LokiStack custom resource<a href="#network-observability-lokistack-create_network_observability" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">Deploy the <code>LokiStack</code> custom resource using the web console or OpenShift CLI (<code>oc</code>), ensuring you configure the correct namespace, deployment size, and secret name for Loki object storage.</p>
      <p>You can deploy a <code>LokiStack</code> custom resource (CR) to create a namespace or new project.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to <strong>Ecosystem</strong> → <strong>Installed Operators</strong>, viewing <strong>All projects</strong> from the <strong>Project</strong> dropdown.</li>
      <li>Look for <strong>Loki Operator</strong>. In the details, under <strong>Provided APIs</strong>, select <strong>LokiStack</strong>.</li>
      <li>Click <strong>Create LokiStack</strong>.</li>
      <li>
      Ensure the following fields are specified in either <strong>Form View</strong> or <strong>YAML view</strong>:
      <CodeBlock code={"apiVersion: loki.grafana.com/v1\nkind: LokiStack\nmetadata:\n  name: loki\n  namespace: netobserv-loki\nspec:\n  size: 1x.small\n  storage:\n    schemas:\n    - version: v13\n      effectiveDate: '2022-06-01'\n    secret:\n      name: loki-s3\n      type: s3\n  storageClassName: gp3\n  tenants:\n    mode: openshift-network"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>metadata.namespace</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the namespace for the <code>LokiStack</code> resource. While this example uses <code>netobserv-loki</code>, you can use a different namespace for different components.</p></dd>
      <dt class="font-semibold"><code>spec.size</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the deployment size. In Loki Operator 5.8 and later versions, the supported size options for production instances of Loki are <code>1x.extra-small</code>, <code>1x.small</code>, or <code>1x.medium</code>.</p></dd>
      </dl>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>It is not possible to change the number <code>1x</code> for the deployment size.</p>
        </AlertDescription>
      </Alert>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.storageClassName</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a storage class name that is available on the cluster for <code>ReadWriteOnce</code> access mode. For best performance, specify a storage class that allocates block storage. Use the <code>oc get storageclasses</code> command to see available storage classes on your cluster.</p></dd>
      </dl>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>You must not reuse the same <code>LokiStack</code> custom resource that is used for logging.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>Click <strong>Create</strong>.</li>
      </ol>
      <h3 id="network-observability-role-based-access-control-for-loki-logs_network_observability" class="group scroll-mt-20">Role-based access control for Loki logs<a href="#network-observability-role-based-access-control-for-loki-logs_network_observability" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">Configure role-based access control to grant users permission to view application, infrastructure, or audit logs in Loki.</p>
      <p>By default, logging 5.8 and later does not grant users access to logs. You must configure role-based access control to grant users permission to view specific log types.</p>
      <p>For more information on access control for Loki logs, see: "Fine grained access for Loki logs" in the Red Hat OpenShift Logging Operator documentation.</p>
      <h4 id="grant-non-admin-users-cluster-wide-log-access_network_observability" class="group scroll-mt-20">Grant non-admin users cluster-wide log access<a href="#grant-non-admin-users-cluster-wide-log-access_network_observability" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h4>
      <p>Add users to a custom admin group to grant cluster-wide log access without making them cluster administrators. This is useful for senior engineers who need full log visibility but should not have cluster modification privileges.</p>
      <p>Users who are members of any group specified in the <code>adminGroups</code> field of the <code>LokiStack</code> custom resource (CR) have the same read access to logs as administrators.</p>
      <CodeBlock code={"apiVersion: loki.grafana.com/v1\nkind: LokiStack\nmetadata:\n  name: loki\n  namespace: netobserv-loki\nspec:\n  tenants:\n    mode: openshift-network\n    openshift:\n      adminGroups:\n      - cluster-admin\n      - custom-admin-group"} lang="yaml" title="Example LokiStack CR" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.tenants.mode</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the tenant mode. Must be <code>openshift-network</code> for network observability.</p></dd>
      <dt class="font-semibold"><code>spec.tenants.openshift.adminGroups</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the list of groups whose members have cluster-wide log access. Defaults to <code>system:cluster-admins</code>, <code>cluster-admin</code>, and <code>dedicated-admin</code>. Set to <code>[]</code> to disable.</p></dd>
      </dl>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="https://docs.redhat.com/en/documentation/red_hat_openshift_logging/6.5/html/configuring_logging/configuring-lokistack-storage#logging-loki-log-access_configuring-the-log-store">Fine grained access for Loki logs</a></li></ul>
      </section>
      <h3 id="network-observability-lokistack-configuring-ingestion_network_observability" class="group scroll-mt-20">LokiStack ingestion limits and health alerts<a href="#network-observability-lokistack-configuring-ingestion_network_observability" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">The <code>LokiStack</code> instance includes default ingestion and query limits that can be overridden by administrators to manage performance and prevent system alerts or errors.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>You might want to update the ingestion and query limits if you get Loki errors showing up in the Console plugin, or in <code>flowlogs-pipeline</code> logs.</p>
        </AlertDescription>
      </Alert>
      <p>Here is an example of configured limits:</p>
      <CodeBlock code={"spec:\n  limits:\n    global:\n      ingestion:\n        ingestionBurstSize: 40\n        ingestionRate: 20\n        maxGlobalStreamsPerTenant: 25000\n      queries:\n        maxChunksPerQuery: 2000000\n        maxEntriesLimitPerQuery: 10000\n        maxQuerySeries: 3000"} lang="yaml" />
      <p>For more information about these settings, see "LokiStack API reference".</p>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/observability/network_observability/installing-operators#network-observability-lokistack-create_network_observability">Creating a LokiStack custom resource</a></li>
      <li><a href="https://docs.redhat.com/en/documentation/red_hat_openshift_logging/latest/html/configuring_logging/configuring-lokistack-storage#logging-loki-storage_configuring-the-log-store">Loki object storage</a></li>
      <li><a href="https://docs.redhat.com/en/documentation/red_hat_openshift_logging/latest/html/configuring_logging/configuring-lokistack-storage.html#loki-sizing_configuring-the-log-store">Loki deployment sizing</a></li>
      <li><a href="https://loki-operator.dev/docs/api.md/#loki-grafana-com-v1-IngestionLimitSpec">LokiStack API reference</a></li>
      <li><a href="/openshift-docs-markdown/observability/network_observability/flowcollector-api#network-observability-flowcollector-api-specifications_network_observability">Flow Collector API Reference</a></li>
      <li><a href="/openshift-docs-markdown/observability/network_observability/configuring-operator#network-observability-flowcollector-view_network_observability">Flow Collector sample resource</a></li>
      </ul>
      </section>
      <h2 id="network-observability-operator-installation_network_observability" class="group scroll-mt-20">Installing the Network Observability Operator<a href="#network-observability-operator-installation_network_observability" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Install the Network Observability Operator and use the setup wizard to create the <code>FlowCollector</code> custom resource definition (CRD) to complete the initial configuration.</p>
      <p>You can set specifications in the web console when you create the <code>FlowCollector</code>.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>The actual memory consumption of the Operator depends on your cluster size and the number of resources deployed. Memory consumption might need to be adjusted accordingly. For more information refer to "Network Observability controller manager pod runs out of memory" in the "Important Flow Collector configuration considerations" section.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>If you choose to use Loki, install the <a href="https://catalog.redhat.com/software/containers/openshift-logging/loki-rhel8-operator/622b46bcae289285d6fcda39">Loki Operator version 5.7+</a>.</li>
      <li>You must have <code>cluster-admin</code> privileges.</li>
      <li>One of the following supported architectures is required: <code>amd64</code>, <code>ppc64le</code>, <code>arm64</code>, or <code>s390x</code>.</li>
      <li>Any CPU supported by Red Hat Enterprise Linux (RHEL) 9.</li>
      <li>Must be configured with OVN-Kubernetes as the main network plugin, and optionally using secondary interfaces with Multus and SR-IOV.</li>
      </ul>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Additionally, this installation example uses the <code>netobserv</code> namespace, which is used across all components. You can optionally use a different namespace.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, click <strong>Ecosystem</strong> → <strong>Software Catalog</strong>.</li>
      <li>Choose  <strong>Network Observability Operator</strong> from the list of available Operators in the software catalog, and click <strong>Install</strong>.</li>
      <li>Select the checkbox <code>Enable Operator recommended cluster monitoring on this Namespace</code>.</li>
      <li>Navigate to <strong>Operators</strong> → <strong>Installed Operators</strong>. Under Provided APIs for Network Observability, select the <strong>Flow Collector</strong> link.</li>
      <li>Follow the <strong>Network Observability FlowCollector setup</strong> wizard.</li>
      <li>Click <strong>Create</strong>.</li>
      </ol>
      <p><strong>Verification</strong></p>
      <p>To confirm this was successful, when you navigate to <strong>Observe</strong> you should see <strong>Network Traffic</strong> listed in the options.</p>
      <p>In the absence of <strong>Application Traffic</strong> within the OpenShift Container Platform cluster, default filters might show that there are "No results", which results in no visual flow. Beside the filter selections, select <strong>Clear all filters</strong> to see the flow.</p>
      <h3 id="network-observability-important-flowcollector-configuration-considerations_network_observability" class="group scroll-mt-20">Important FlowCollector configuration considerations<a href="#network-observability-important-flowcollector-configuration-considerations_network_observability" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">Review essential <code>FlowCollector</code> configuration options before initial deployment to avoid pod disruptions caused by later reconfiguration. Key settings include Kafka integration, enriched flow data exports, SR-IOV traffic monitoring, and advanced tracking for DNS and packet drops.</p>
      <p>Once you create the <code>FlowCollector</code> instance, you can reconfigure it, but the pods are terminated and recreated again, which can be disruptive.</p>
      <p>Therefore, you can consider configuring the following options when creating the <code>FlowCollector</code> for the first time.</p>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/observability/network_observability/configuring-operator#network-observability-flowcollector-kafka-config_network_observability">Configuring the Flow Collector resource with Kafka</a></li>
      <li><a href="/openshift-docs-markdown/observability/network_observability/configuring-operator#network-observability-enriched-flows_network_observability">Export enriched network flow data to Kafka or IPFIX</a></li>
      <li><a href="/openshift-docs-markdown/observability/network_observability/network-observability-secondary-networks#network-observability-SR-IOV-config_network-observability-secondary-networks">Configuring monitoring for SR-IOV interface traffic</a></li>
      <li><a href="/openshift-docs-markdown/observability/network_observability/observing-network-traffic#network-observability-working-with-conversations_nw-observe-network-traffic">Working with conversation tracking</a></li>
      <li><a href="/openshift-docs-markdown/observability/network_observability/observing-network-traffic#network-observability-dns-tracking_nw-observe-network-traffic">Working with DNS tracking</a></li>
      <li><a href="/openshift-docs-markdown/observability/network_observability/observing-network-traffic#network-observability-packet-drops_nw-observe-network-traffic">Working with packet drops</a></li>
      <li><a href="/openshift-docs-markdown/observability/network_observability/flowcollector-api#network-observability-flowcollector-api-specifications_network_observability">Flow Collector API Reference</a></li>
      <li><a href="/openshift-docs-markdown/observability/network_observability/configuring-operator#network-observability-flowcollector-view_network_observability">Flow Collector sample resource</a></li>
      <li><a href="/openshift-docs-markdown/observability/network_observability/configuring-operator#network-observability-resources-table_network_observability">Resource considerations</a></li>
      <li><a href="/openshift-docs-markdown/observability/network_observability/troubleshooting-network-observability#controller-manager-pod-runs-out-of-memory_network-observability-troubleshooting">Troubleshooting network observability controller manager pod runs out of memory</a></li>
      <li><a href="/openshift-docs-markdown/observability/network_observability/understanding-network-observability-operator#network-observability-architecture_nw-network-observability-operator">Network observability architecture</a></li>
      </ul>
      </section>
      <h2 id="network-observability-updating-migrating_network_observability" class="group scroll-mt-20">Migrating removed stored versions of the FlowCollector CRD<a href="#network-observability-updating-migrating_network_observability" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Manually remove the deprecated <code>v1alpha1</code> version from the <code>FlowCollector</code> custom resource definition (CRD) <code>storedVersion</code> list to prevent upgrade errors and successfully migrate to Network Observability Operator 1.6.</p>
      <p>There are two options to remove stored versions:</p>
      <ol>
      <li>Use the Storage Version Migrator Operator.</li>
      <li>Uninstall and reinstall the Network Observability Operator, ensuring that the installation is in a clean state.</li>
      </ol>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have an older version of the Operator installed, and you want to prepare your cluster to install the latest version of the Operator. Or you have attempted to install the Network Observability Operator 1.6 and run into the error: <code>Failed risk of data loss updating "flowcollectors.flows.netobserv.io": new CRD removes version v1alpha1 that is listed as a stored version on the existing CRD</code>.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Verify that the old <code>FlowCollector</code> CRD version is still referenced in the <code>storedVersion</code>:
      <CodeBlock code={"$ oc get crd flowcollectors.flows.netobserv.io -ojsonpath='{.status.storedVersions}'"} lang="terminal" />
      </li>
      <li>
      If <code>v1alpha1</code> appears in the list of results, proceed with <strong>Step a</strong> to use the Kubernetes Storage Version Migrator or <strong>Step b</strong> to uninstall and reinstall the CRD and the Operator.
      <ol>
      <li>
      <strong>Option 1: Kubernetes Storage Version Migrator</strong>: Create a YAML to define the <code>StorageVersionMigration</code> object, for example <code>migrate-flowcollector-v1alpha1.yaml</code>:
      <CodeBlock code={"apiVersion: migration.k8s.io/v1alpha1\nkind: StorageVersionMigration\nmetadata:\n  name: migrate-flowcollector-v1alpha1\nspec:\n  resource:\n    group: flows.netobserv.io\n    resource: flowcollectors\n    version: v1alpha1"} lang="yaml" />
      <ol>
      <li>Save the file.</li>
      <li>
      Apply the <code>StorageVersionMigration</code> by running the following command:
      <CodeBlock code={"$ oc apply -f migrate-flowcollector-v1alpha1.yaml"} lang="terminal" />
      </li>
      <li>
      Update the <code>FlowCollector</code> CRD to manually remove <code>v1alpha1</code> from the <code>storedVersion</code>:
      <CodeBlock code={"$ oc edit crd flowcollectors.flows.netobserv.io"} lang="terminal" />
      </li>
      </ol>
      </li>
      <li>
      <strong>Option 2: Reinstall</strong>: Save the Network Observability Operator 1.5 version of the <code>FlowCollector</code> CR to a file, for example <code>flowcollector-1.5.yaml</code>.
      <CodeBlock code={"$ oc get flowcollector cluster -o yaml > flowcollector-1.5.yaml"} lang="terminal" />
      <ol>
      <li>Follow the steps in "Uninstalling the Network Observability Operator", which uninstalls the Operator and removes the existing <code>FlowCollector</code> CRD.</li>
      <li>Install the Network Observability Operator latest version, 1.6.0.</li>
      <li>Create the <code>FlowCollector</code> using backup that was saved in Step b.</li>
      </ol>
      </li>
      </ol>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      Run the following command:
      <CodeBlock code={"$ oc get crd flowcollectors.flows.netobserv.io -ojsonpath='{.status.storedVersions}'"} lang="terminal" />
      <p>The list of results should no longer show <code>v1alpha1</code> and only show the latest version, <code>v1beta1</code>.</p>
      </li>
      </ul>
      <h2 id="network-observability-multi-tenancy_network_observability" class="group scroll-mt-20">Enabling multi-tenancy in network observability<a href="#network-observability-multi-tenancy_network_observability" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Enable multi-tenancy in network observability by configuring cluster roles and namespace roles to grant project administrators and developers granular, restricted access to flows and metrics in Loki and Prometheus.</p>
      <p>Access is enabled for project administrators. Project administrators who have limited access to some namespaces can access flows for only those namespaces.</p>
      <p>For Developers, multi-tenancy is available for both Loki and Prometheus but requires different access rights.</p>
      <p><strong>Prerequisite</strong></p>
      <ul>
      <li>If you are using Loki, you have installed at least <a href="https://catalog.redhat.com/software/containers/openshift-logging/loki-rhel8-operator/622b46bcae289285d6fcda39">Loki Operator version 5.7</a>.</li>
      <li>You must be logged in as a project administrator.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      For per-tenant access, you must have the <code>netobserv-loki-reader</code> cluster role and the <code>netobserv-metrics-reader</code> namespace role to use the developer perspective. Run the following commands for this level of access:
      <CodeBlock code={"$ oc adm policy add-cluster-role-to-user netobserv-loki-reader <user_group_or_name>"} lang="terminal" />
      <CodeBlock code={"$ oc adm policy add-role-to-user netobserv-metrics-reader <user_group_or_name> -n <namespace>"} lang="terminal" />
      </li>
      <li>
      For cluster-wide access, non-cluster-administrators must have the <code>netobserv-loki-reader</code>, <code>cluster-monitoring-view</code>, and <code>netobserv-metrics-reader</code> cluster roles. In this scenario, you can use either the admin perspective or the developer perspective. Run the following commands for this level of access:
      <CodeBlock code={"$ oc adm policy add-cluster-role-to-user netobserv-loki-reader <user_group_or_name>"} lang="terminal" />
      <CodeBlock code={"$ oc adm policy add-cluster-role-to-user cluster-monitoring-view <user_group_or_name>"} lang="terminal" />
      <CodeBlock code={"$ oc adm policy add-cluster-role-to-user netobserv-metrics-reader <user_group_or_name>"} lang="terminal" />
      </li>
      </ul>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="/openshift-docs-markdown/operators/operator-reference#cluster-kube-storage-version-migrator-operator_operator-reference">Kubernetes Storage Version Migrator Operator</a></li></ul>
      </section>
      <h2 id="network-observability-operator-uninstall_network_observability" class="group scroll-mt-20">Uninstalling the Network Observability Operator<a href="#network-observability-operator-uninstall_network_observability" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Uninstall the Network Observability Operator using the OpenShift Container Platform web console Operator Hub, working in the <strong>Ecosystem</strong> → <strong>Installed Operators</strong> area.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Remove the <code>FlowCollector</code> custom resource.
      <ol>
      <li>Click <strong>Flow Collector</strong>, which is next to the <strong>Network Observability Operator</strong> in the <strong>Provided APIs</strong> column.</li>
      <li>Click the Options menu <img src="/openshift-docs-markdown/images/kebab.png" alt="" loading="lazy" /> for the <strong>cluster</strong> and select <strong>Delete FlowCollector</strong>.</li>
      </ol>
      </li>
      <li>
      Uninstall the Network Observability Operator.
      <ol>
      <li>Navigate back to the <strong>Ecosystem</strong> → <strong>Installed Operators</strong> area.</li>
      <li>Click the Options menu <img src="/openshift-docs-markdown/images/kebab.png" alt="" loading="lazy" /> next to the  <strong>Network Observability Operator</strong> and select <strong>Uninstall Operator</strong>.</li>
      <li><strong>Home</strong> → <strong>Projects</strong> and select <code>openshift-netobserv-operator</code></li>
      <li>Navigate to <strong>Actions</strong> and select <strong>Delete Project</strong></li>
      </ol>
      </li>
      <li>
      Remove the <code>FlowCollector</code> custom resource definition (CRD).
      <ol>
      <li>Navigate to <strong>Administration</strong> → <strong>CustomResourceDefinitions</strong>.</li>
      <li>Look for <strong>FlowCollector</strong> and click the Options menu <img src="/openshift-docs-markdown/images/kebab.png" alt="" loading="lazy" />.</li>
      <li>
      Select <strong>Delete CustomResourceDefinition</strong>.
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>The Loki Operator and Kafka remain if they were installed and must be removed separately. Additionally, you might have remaining data stored in an object store, and a persistent volume that must be removed.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
