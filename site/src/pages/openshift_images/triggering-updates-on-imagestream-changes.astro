---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: openshift_images/triggering-updates-on-imagestream-changes.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"triggering-updates-on-imagestream-changes","text":"Triggering updates on image stream changes"},{"depth":2,"slug":"openshift-resources","text":"Resources"},{"depth":2,"slug":"images-triggering-updates-imagestream-changes-kubernetes-about_triggering-updates-on-imagestream-changes","text":"Triggering Kubernetes resources"},{"depth":2,"slug":"images-triggering-updates-imagestream-changes-kubernetes-cli_triggering-updates-on-imagestream-changes","text":"Setting the image trigger on Kubernetes resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/openshift_images/triggering-updates-on-imagestream-changes";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Triggering updates on image stream changes";
const description = "When image stream tags update in OpenShift Container Platform, the platform automatically rolls out new images to deployments and builds that reference…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["openshift_images"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Triggering updates on image stream changes {#triggering-updates-on-imagestream-changes}\n\nWhen image stream tags update in OpenShift Container Platform, the platform automatically rolls out new images to deployments and builds that reference those tags. You configure this automatic triggering behavior differently depending on the type of resource that uses the image stream.\n\n## Resources {#openshift-resources}\n\nOpenShift Container Platform `DeploymentConfig` and `BuildConfig` resources can be automatically triggered by changes to image stream tags. When triggered, these resources use the new image value referenced by the updated image stream tag.\n\n## Triggering Kubernetes resources {#images-triggering-updates-imagestream-changes-kubernetes-about_triggering-updates-on-imagestream-changes}\n\nTo enable Kubernetes resources, such as `Deployments` and `StatefulSets`, to seamlessly consume new image versions, configure image stream change triggers in OpenShift Container Platform. This ensures your application deployments are automatically updated when the associated image stream detects a change.\n\nKubernetes resources do not have fields for triggering, unlike deployment and build configurations, which include as part of their API definition a set of fields for controlling triggers. Instead, you can use annotations in OpenShift Container Platform to request triggering.\n\nThe annotation is defined as follows:\n\n```yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  annotations:\n    image.openshift.io/triggers:\n      [\n       {\n         \"from\": {\n           \"kind\": \"ImageStreamTag\",\n           \"name\": \"example:latest\",\n           \"namespace\": \"myapp\"\n         },\n         \"fieldPath\": \"spec.template.spec.containers[?(@.name==\\\"web\\\")].image\",\n         \"paused\": false\n       },\n      # ...\n      ]\n# ...\n```\n\nwhere:\n\n`kind`\n:   Specifies the resource to trigger from, and must have the value `ImageStreamTag`.\n\n`name`\n:   Specifies the name of an image stream tag.\n\n`namespace`\n:   Specifies the namespace of the object. This field is optional.\n\n`fieldPath`\n:   Specifies the JSON path to change. This field is limited and accepts only a JSON path expression that precisely matches a container by ID or index. For pods, the JSON path is `spec.containers[?(@.name='web')].image`.\n\n`paused`\n:   Specifies whether or not the trigger is paused. This field is optional, and defaults to the value `false`. Set the value to `true` to temporarily disable this trigger.\n\nWhen one of the core Kubernetes resources contains both a pod template and this annotation, OpenShift Container Platform attempts to update the object by using the image currently associated with the image stream tag that is referenced by trigger. The update is performed against the `fieldPath` specified.\n\nExamples of core Kubernetes resources that can contain both a pod template and annotation include:\n\n- `CronJobs`\n- `Deployments`\n- `StatefulSets`\n- `DaemonSets`\n- `Jobs`\n- `ReplicationControllers`\n- `Pods`\n\n## Setting the image trigger on Kubernetes resources {#images-triggering-updates-imagestream-changes-kubernetes-cli_triggering-updates-on-imagestream-changes}\n\nTo enable automatic updates for your deployed applications managed by Kubernetes, use the command-line interface (CLI) to set an image stream change trigger on Kubernetes resources. This ensures that resources, like `Deployments` and `StatefulSets`, are automatically invoked when a new version of an upstream image is available.\n\nWhen adding an image trigger to deployments, you can use the `oc set triggers` command. For example, the sample command in this procedure adds an image change trigger to the deployment named `example` so that when the `example:latest` image stream tag is updated, the `web` container inside the deployment updates with the new image value. This command sets the correct `image.openshift.io/triggers` annotation on the deployment resource.\n\n**Procedure**\n\n- Trigger Kubernetes resources by entering the `oc set triggers` command:\n\n  ```terminal\n  $ oc set triggers deploy/example --from-image=example:latest -c web\n  ```\n\n  ```yaml {title=\"Example deployment with trigger annotation\"}\n  apiVersion: apps/v1\n  kind: Deployment\n  metadata:\n    annotations:\n      image.openshift.io/triggers: '[{\"from\":{\"kind\":\"ImageStreamTag\",\"name\":\"example:latest\"},\"fieldPath\":\"spec.template.spec.containers[?(@.name==\\\"container\\\")].image\"}]'\n  # ...\n  ```\n\n  Unless the deployment is paused, this pod template update automatically causes a deployment to occur with the new image value.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/openshift_images/triggering-updates-on-imagestream-changes.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="triggering-updates-on-imagestream-changes" class="group scroll-mt-20">Triggering updates on image stream changes<a href="#triggering-updates-on-imagestream-changes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>When image stream tags update in OpenShift Container Platform, the platform automatically rolls out new images to deployments and builds that reference those tags. You configure this automatic triggering behavior differently depending on the type of resource that uses the image stream.</p>
      <h2 id="openshift-resources" class="group scroll-mt-20">Resources<a href="#openshift-resources" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>OpenShift Container Platform <code>DeploymentConfig</code> and <code>BuildConfig</code> resources can be automatically triggered by changes to image stream tags. When triggered, these resources use the new image value referenced by the updated image stream tag.</p>
      <h2 id="images-triggering-updates-imagestream-changes-kubernetes-about_triggering-updates-on-imagestream-changes" class="group scroll-mt-20">Triggering Kubernetes resources<a href="#images-triggering-updates-imagestream-changes-kubernetes-about_triggering-updates-on-imagestream-changes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To enable Kubernetes resources, such as <code>Deployments</code> and <code>StatefulSets</code>, to seamlessly consume new image versions, configure image stream change triggers in OpenShift Container Platform. This ensures your application deployments are automatically updated when the associated image stream detects a change.</p>
      <p>Kubernetes resources do not have fields for triggering, unlike deployment and build configurations, which include as part of their API definition a set of fields for controlling triggers. Instead, you can use annotations in OpenShift Container Platform to request triggering.</p>
      <p>The annotation is defined as follows:</p>
      <CodeBlock code={"apiVersion: v1\nkind: Pod\nmetadata:\n  annotations:\n    image.openshift.io/triggers:\n      [\n       {\n         \"from\": {\n           \"kind\": \"ImageStreamTag\",\n           \"name\": \"example:latest\",\n           \"namespace\": \"myapp\"\n         },\n         \"fieldPath\": \"spec.template.spec.containers[?(@.name==\\\"web\\\")].image\",\n         \"paused\": false\n       },\n      # ...\n      ]\n# ..."} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>kind</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the resource to trigger from, and must have the value <code>ImageStreamTag</code>.</p></dd>
      <dt class="font-semibold"><code>name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of an image stream tag.</p></dd>
      <dt class="font-semibold"><code>namespace</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the namespace of the object. This field is optional.</p></dd>
      <dt class="font-semibold"><code>fieldPath</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the JSON path to change. This field is limited and accepts only a JSON path expression that precisely matches a container by ID or index. For pods, the JSON path is <code>spec.containers[?(@.name='web')].image</code>.</p></dd>
      <dt class="font-semibold"><code>paused</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies whether or not the trigger is paused. This field is optional, and defaults to the value <code>false</code>. Set the value to <code>true</code> to temporarily disable this trigger.</p></dd>
      </dl>
      <p>When one of the core Kubernetes resources contains both a pod template and this annotation, OpenShift Container Platform attempts to update the object by using the image currently associated with the image stream tag that is referenced by trigger. The update is performed against the <code>fieldPath</code> specified.</p>
      <p>Examples of core Kubernetes resources that can contain both a pod template and annotation include:</p>
      <ul>
      <li><code>CronJobs</code></li>
      <li><code>Deployments</code></li>
      <li><code>StatefulSets</code></li>
      <li><code>DaemonSets</code></li>
      <li><code>Jobs</code></li>
      <li><code>ReplicationControllers</code></li>
      <li><code>Pods</code></li>
      </ul>
      <h2 id="images-triggering-updates-imagestream-changes-kubernetes-cli_triggering-updates-on-imagestream-changes" class="group scroll-mt-20">Setting the image trigger on Kubernetes resources<a href="#images-triggering-updates-imagestream-changes-kubernetes-cli_triggering-updates-on-imagestream-changes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To enable automatic updates for your deployed applications managed by Kubernetes, use the command-line interface (CLI) to set an image stream change trigger on Kubernetes resources. This ensures that resources, like <code>Deployments</code> and <code>StatefulSets</code>, are automatically invoked when a new version of an upstream image is available.</p>
      <p>When adding an image trigger to deployments, you can use the <code>oc set triggers</code> command. For example, the sample command in this procedure adds an image change trigger to the deployment named <code>example</code> so that when the <code>example:latest</code> image stream tag is updated, the <code>web</code> container inside the deployment updates with the new image value. This command sets the correct <code>image.openshift.io/triggers</code> annotation on the deployment resource.</p>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Trigger Kubernetes resources by entering the <code>oc set triggers</code> command:
      <CodeBlock code={"$ oc set triggers deploy/example --from-image=example:latest -c web"} lang="terminal" />
      <CodeBlock code={"apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  annotations:\n    image.openshift.io/triggers: '[{\"from\":{\"kind\":\"ImageStreamTag\",\"name\":\"example:latest\"},\"fieldPath\":\"spec.template.spec.containers[?(@.name==\\\"container\\\")].image\"}]'\n# ..."} lang="yaml" title="Example deployment with trigger annotation" />
      <p>Unless the deployment is paused, this pod template update automatically causes a deployment to occur with the new image value.</p>
      </li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
