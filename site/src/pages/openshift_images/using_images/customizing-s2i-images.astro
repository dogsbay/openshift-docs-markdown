---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: openshift_images/using_images/customizing-s2i-images.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"customizing-s2i-images","text":"Customizing source-to-image images"},{"depth":2,"slug":"images-using-customizing-s2i-images-scripts-embedded_customizing-s2i-images","text":"Invoking scripts embedded in an image"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/openshift_images/using_images/customizing-s2i-images";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Customizing source-to-image images";
const description = "To modify the default assemble and run script behavior in OpenShift Container Platform, you can customize source-to-image (S2I) builder images. You can…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["openshift_images","openshift_images/using_images"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Customizing source-to-image images {#customizing-s2i-images}\n\nTo modify the default assemble and run script behavior in OpenShift Container Platform, you can customize source-to-image (S2I) builder images. You can adapt S2I builders to meet your specific application requirements when the default scripts are not suitable.\n\n## Invoking scripts embedded in an image {#images-using-customizing-s2i-images-scripts-embedded_customizing-s2i-images}\n\nTo extend builder image behavior while preserving supported script logic and upgrade compatibility in OpenShift Container Platform, you can start embedded S2I image scripts by creating wrapper scripts. These wrapper scripts run custom logic and then call the default scripts from the image.\n\n**Procedure**\n\n1. Inspect the value of the `io.openshift.s2i.scripts-url` label to determine the location of the scripts inside of the builder image:\n\n   ```terminal\n   $ podman inspect --format='{{ index .Config.Labels \"io.openshift.s2i.scripts-url\" }}' wildfly/wildfly-centos7\n   ```\n\n   ```terminal {title=\"Example output\"}\n   image:///usr/libexec/s2i\n   ```\n2. Create a script that includes an invocation of one of the standard scripts wrapped in other commands:\n\n   ```bash {title=\".s2i/bin/assemble script\"}\n   #!/bin/bash\n   echo \"Before assembling\"\n\n   /usr/libexec/s2i/assemble\n   rc=$?\n\n   if [ $rc -eq 0 ]; then\n       echo \"After successful assembling\"\n   else\n       echo \"After failed assembling\"\n   fi\n\n   exit $rc\n   ```\n\n   This example shows a custom assemble script that prints the message, runs the standard assemble script from the image, and prints another message depending on the exit code of the assemble script.\n\n   > [!IMPORTANT]\n   > When wrapping the run script, you must use `exec` for invoking it to ensure signals are handled properly. The use of `exec` also precludes the ability to run additional commands after invoking the default image run script.\n\n   ```bash {title=\".s2i/bin/run script\"}\n   #!/bin/bash\n   echo \"Before running application\"\n   exec /usr/libexec/s2i/run\n   ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/openshift_images/using_images/customizing-s2i-images.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="customizing-s2i-images" class="group scroll-mt-20">Customizing source-to-image images<a href="#customizing-s2i-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>To modify the default assemble and run script behavior in OpenShift Container Platform, you can customize source-to-image (S2I) builder images. You can adapt S2I builders to meet your specific application requirements when the default scripts are not suitable.</p>
      <h2 id="images-using-customizing-s2i-images-scripts-embedded_customizing-s2i-images" class="group scroll-mt-20">Invoking scripts embedded in an image<a href="#images-using-customizing-s2i-images-scripts-embedded_customizing-s2i-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To extend builder image behavior while preserving supported script logic and upgrade compatibility in OpenShift Container Platform, you can start embedded S2I image scripts by creating wrapper scripts. These wrapper scripts run custom logic and then call the default scripts from the image.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Inspect the value of the <code>io.openshift.s2i.scripts-url</code> label to determine the location of the scripts inside of the builder image:
      <CodeBlock code={"$ podman inspect --format='{{ index .Config.Labels \"io.openshift.s2i.scripts-url\" }}' wildfly/wildfly-centos7"} lang="terminal" />
      <CodeBlock code={"image:///usr/libexec/s2i"} lang="terminal" title="Example output" />
      </li>
      <li>
      Create a script that includes an invocation of one of the standard scripts wrapped in other commands:
      <CodeBlock code={"#!/bin/bash\necho \"Before assembling\"\n\n/usr/libexec/s2i/assemble\nrc=$?\n\nif [ $rc -eq 0 ]; then\n    echo \"After successful assembling\"\nelse\n    echo \"After failed assembling\"\nfi\n\nexit $rc"} lang="bash" title=".s2i/bin/assemble script" />
      <p>This example shows a custom assemble script that prints the message, runs the standard assemble script from the image, and prints another message depending on the exit code of the assemble script.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>When wrapping the run script, you must use <code>exec</code> for invoking it to ensure signals are handled properly. The use of <code>exec</code> also precludes the ability to run additional commands after invoking the default image run script.</p>
        </AlertDescription>
      </Alert>
      <CodeBlock code={"#!/bin/bash\necho \"Before running application\"\nexec /usr/libexec/s2i/run"} lang="bash" title=".s2i/bin/run script" />
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
