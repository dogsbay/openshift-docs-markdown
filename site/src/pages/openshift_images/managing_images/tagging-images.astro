---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: openshift_images/managing_images/tagging-images.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"tagging-images","text":"Tagging images"},{"depth":2,"slug":"images-tag_tagging-images","text":"Understanding image tags in image streams"},{"depth":2,"slug":"images-tagging-conventions_tagging-images","text":"Image tag conventions"},{"depth":2,"slug":"images-add-tags-to-imagestreams_tagging-images","text":"Adding tags to image streams"},{"depth":2,"slug":"images-remove-tag-imagestream_tagging-images","text":"Removing tags from image streams"},{"depth":2,"slug":"images-referencing-images-imagestreams_tagging-images","text":"Using image stream reference syntax"},{"depth":2,"slug":"images-imagestream-reference-types_tagging-images","text":"Understanding image stream reference types"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/openshift_images/managing_images/tagging-images";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Tagging images";
const description = "Image tags identify specific versions of container images in image streams. You can use image tags to organize images and control which versions your…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["openshift_images","openshift_images/managing_images"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Tagging images {#tagging-images}\n\nImage tags identify specific versions of container images in image streams. You can use image tags to organize images and control which versions your builds and deployments use.\n\n## Understanding image tags in image streams {#images-tag_tagging-images}\n\nImage tags in OpenShift Container Platform help you organize, identify, and reference specific versions of container images in image streams. Tags are human-readable labels that act as pointers to particular image layers and digests.\n\nTags function as mutable pointers within an image stream. When a new image is imported or tagged into the stream, the tag is updated to point to the new image’s immutable SHA digest. A single image digest can have multiple tags simultaneously assigned to it. For example, the `:v3.11.59-2` and `:latest` tags are assigned to the same image digest.\n\nTags offer two main benefits:\n\n- Tags serve as the primary mechanism for builds and deployments to request a specific version of an image from an image stream.\n- Tags help maintain clarity and allow for easy promotion of images between environments. For example, you can promote an image from the `:test` tag to the `:prod` tag.\n\nWhile image tags are primarily used for referencing images in configurations, OpenShift Container Platform provides the `oc tag` command for managing tags directly within image streams. This command is similar to the `podman tag` or `docker tag` commands, but it operates on image streams instead of directly on local images. It is used to create a new tag pointer or update an existing tag pointer within an image stream to point to a new image.\n\nImage tags are appended to the image name or image stream name by using a colon (`:`) as a separator.\n\n| Context | Syntax Format | Example |\n| --- | --- | --- |\n| ***External Registry*** | `<registry_path>:<tag>` | `registry.access.redhat.com/openshift3/jenkins-2-rhel7:v3.11.59-2` |\n| ***Local Image Stream*** | `<image_stream_name>:<tag>` | `jenkins:latest` |\n\n## Image tag conventions {#images-tagging-conventions_tagging-images}\n\nImage tag naming conventions in OpenShift Container Platform provide guidelines for creating tags that enable effective image pruning and maintain manageable image streams. Use consistent naming patterns to avoid tags that point to single revisions and never update.\n\nTags that are too specific effectively pin the tag to a single image revision that is never updated. For example, if you create a tag named `v2.0.1-may-2019`, the tag points to just one revision of an image and is never updated. If you use default image pruning options, such an image is never removed.\n\nIn very large clusters, the schema of creating new tags for every revised image could eventually fill up the etcd datastore with excess tag metadata for images that are long outdated. If the tag is named `v2.0`, image revisions are more likely. This results in longer tag history and, therefore, the image pruner is more likely to remove old and unused images.\n\nTo ensure proper garbage collection, use broader, more generic tags that are designed to be updated when a new image revision is built. The following table provides some recommended tagging conventions using the format `<image_name>:<image_tag>`.\n\n**Image tag naming conventions**\n\n| Description | Example |\n| --- | --- |\n| ***Major/Minor Version*** (Ideal for mutable pointers) | `myimage:v2.0` |\n| ***Full Revision*** (Often used for tracking, but requires manual pruning) | `myimage:v2.0.1` |\n| ***Architecture*** | `myimage:v2.0-x86_64` |\n| ***Base image*** | `myimage:v1.2-centos7` |\n| ***Latest*** | `myimage:latest` |\n| ***Latest stable*** | `myimage:stable` |\n\n> [!NOTE]\n> If your team requires the use of unique, date-specific, or highly revisioned tags like `v2.0.1-may-2019`, you must periodically inspect old and unsupported images and `istags` and remove them. Otherwise, you can experience increasing resource usage caused by retaining old images.\n\n## Adding tags to image streams {#images-add-tags-to-imagestreams_tagging-images}\n\nTo organize images and create aliases for specific versions or automatically track changes to source tags in OpenShift Container Platform, you can add tags to image streams with the `oc tag` command.\n\nThere are two types of tags available in OpenShift Container Platform:\n\n- Permanent tags: A permanent tag points to a specific image in time. If the permanent tag is in use and the source changes, the tag does not change for the destination.\n- Tracking tags: A tracking tag means that the destination tag’s metadata is updated during the import of the source tag.\n\nThe default behavior creates a permanent tag that is pinned to an image ID.\n\n**Procedure**\n\n- Optional: Add a tag to an image stream by entering the following command. The default behavior creates a permanent tag that is pinned to an image ID:\n\n  ```terminal\n  $ oc tag <source_reference> <destination_image_stream>:<destination_tag>\n  ```\n\n  For example, to configure the `ruby` image stream `static-2.0` tag to always refer to the specific image that the `ruby:2.0` tag points to now, enter the following command:\n\n  ```terminal\n  $ oc tag ruby:2.0 ruby:static-2.0\n  ```\n\n  This creates a new image stream tag named `static-2.0` in the `ruby` image stream. The new tag directly references the image ID that the `ruby:2.0` image stream tag pointed to at the time `oc tag` was run, and the image it points to never changes.\n- Optional: Use the `--alias=true` flag to create a tracking tag. This ensures the destination tag automatically updates (tracks) when the source tag changes to point to a new image. For example, to ensure that the `ruby:latest` tag always reflects whatever image is currently tagged as `ruby:2.0`, enter the following command:\n\n  ```terminal\n  $ oc tag --alias=true ruby:2.0 ruby:latest\n  ```\n\n  > [!NOTE]\n  > A Tracking Tag created with `--alias=true` automatically updates its image ID whenever the source tag changes. Use the `latest` or `stable` tracking tags for creating common, long-lived aliases. This tracking behavior only works correctly within a single image stream. Trying to create a cross-image stream alias produces an error.\n- Optional: Use the `--scheduled=true` flag to have the destination tag be refreshed, or re-imported, periodically. The period is configured globally at the system level. For example:\n\n  ```terminal\n  $ oc tag <source_reference> <destination_image_stream>:<destination_tag> --scheduled=true\n  ```\n- Optional: Use the `--reference` flag to create an image stream tag that is not imported. The tag permanently points to the source location, regardless of changes to the source image. For example:\n\n  ```terminal\n  $ oc tag <source_reference> <destination_image_stream>:<destination_tag> --reference\n  ```\n- Optional. Use the `--insecure` flag if the source registry is not secured with a valid HTTPS certificate. This flag tells the image stream to skip certificate verification during the import progress. For example:\n\n  ```terminal\n  $ oc tag <source_reference> <destination_image_stream>:<destination_tag> --insecure\n  ```\n- Optional: Use the `--reference-policy=local` flag to instruct OpenShift Container Platform to always fetch the tagged image from the integrated registry. The registry uses the pull-through feature to serve the image to the client. By default, the image blobs are mirrored locally by the registry. As a result, they can be pulled more quickly the next time they are needed. The  `--reference-policy=local` flag also allows for pulling from insecure registries without a need to supply the `--insecure` flag to the container runtime provided that the image stream has an insecure annotation or the tag has an insecure import policy. For example:\n\n  ```terminal\n  $ oc tag <source_reference> <destination_image_stream>:<destination_tag> --reference-policy=local\n  ```\n\n## Removing tags from image streams {#images-remove-tag-imagestream_tagging-images}\n\nTo keep your image streams clean and maintain organized image references in OpenShift Container Platform, you can remove unused or outdated image stream tags. Remove tags by using the `oc delete istag` or `oc tag -d` commands.\n\n**Procedure**\n\n- Remove a tag from an image stream by entering the following command:\n\n  ```terminal\n  $ oc delete istag/<name>:<tag>\n  ```\n\n  For example, to remove the `ruby:latest` tag from the `ruby` image stream, enter the following command:\n\n  ```terminal\n  $ oc delete istag/ruby:latest\n  ```\n- Alternatively, you can remove a tag using the `oc tag -d` command:\n\n  ```terminal\n  $ oc tag -d <name>:<tag>\n  ```\n\n  For example, to remove the `ruby:latest` tag from the `ruby` image stream, enter the following command:\n\n  ```terminal\n  $ oc tag -d ruby:latest\n  ```\n\n## Using image stream reference syntax {#images-referencing-images-imagestreams_tagging-images}\n\nTo ensure that your builds and deployments use the intended image version in OpenShift Container Platform, you must use the correct reference syntax format.\n\n**Procedure**\n\n- To reference an image by a mutable tag (`ImageStreamTag`) from an image stream within your cluster, use the `<image_stream_name>:<tag>` format in your build or deployment. For example:\n\n  ```yaml\n  # ...\n  spec:\n    containers:\n      - name: my-app\n        image: <image_stream_name>:<tag>\n  ```\n\n  where:\n\n  `spec.containers.image`\n  :   Specifies the image to use from the image stream. For example, `ruby:2.0`.\n- To reference a specific, immutable image ID, or digest, within an image stream, use the `<image_stream_name>@<image_id>` format in your build or deployment. For example:\n\n  ```yaml\n  # ...\n  spec:\n    containers:\n      - name: my-app\n        image: <image_stream_name>@<image_id>\n  ```\n\n  where:\n\n  `spec.containers.image`\n  :   Specifies the image to use from the image stream. For example, `ruby@sha256:3a335d7d8a452970c5b4054ad7118ff134b3a6b50a2bb6d0c07c746e8986b28e`.\n\n  > [!NOTE]\n  > Using the image ID with the `@id` syntax ensures your configuration always uses the exact same image, even if the tag is later updated to point to a different image.\n- To reference an image from an external registry by using the DockerImage format, use the standard Docker pull specification: `<registry>/<namespace>/<image_name>:<tag>`. For example:\n\n  ```yaml\n  # ...\n  spec:\n    source:\n      type: Dockerfile\n    strategy:\n      type: Docker\n      dockerStrategy:\n        from:\n          kind: DockerImage\n          name: <registry>/<namespace>/<image_name>:<tag>\n  ```\n\n  where:\n\n  `spec.strategy.dockerStrategy.from.name`\n  :   Specifies the image to use from the external registry. For example, `registry.redhat.io/rhel7:latest`.\n\n  > [!NOTE]\n  > When no tag is specified in a `DockerImage` reference, the `latest` tag is assumed.\n\n## Understanding image stream reference types {#images-imagestream-reference-types_tagging-images}\n\nBy using image streams in OpenShift Container Platform, you can reference container images by using different reference types. These reference types define which specific image version your builds and deployments use.\n\n`ImageStreamImage` objects are automatically created in OpenShift Container Platform when you import or tag an image into the image stream. You never have to explicitly define an `ImageStreamImage` object in any image stream definition that you use to create image streams.\n\n> [!NOTE]\n> Example image stream definitions often contain definitions of `ImageStreamTag` and references to `DockerImage`, but never contain definitions of `ImageStreamImage`.\n\n**Imagestream reference types**\n\n| Reference Type | Description | Syntax Examples |\n| --- | --- | --- |\n| `ImageStreamTag` | References or retrieves an image for a given image stream and human-readable tag. | `image_stream_name:tag` |\n| `ImageStreamImage` | References or retrieves an image for a given image stream and immutable SHA ID (digest). | `image_stream_name@id` |\n| `DockerImage` | References or retrieves an image from an external registry. Uses the standard `docker pull` specification. | `openshift/ruby-20-centos7:2.0`, `registry.redhat.io/rhel7:latest`, `centos/ruby-22-centos7@sha256:3a335d7d...` |\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/openshift_images/managing_images/tagging-images.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="tagging-images" class="group scroll-mt-20">Tagging images<a href="#tagging-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Image tags identify specific versions of container images in image streams. You can use image tags to organize images and control which versions your builds and deployments use.</p>
      <h2 id="images-tag_tagging-images" class="group scroll-mt-20">Understanding image tags in image streams<a href="#images-tag_tagging-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Image tags in OpenShift Container Platform help you organize, identify, and reference specific versions of container images in image streams. Tags are human-readable labels that act as pointers to particular image layers and digests.</p>
      <p>Tags function as mutable pointers within an image stream. When a new image is imported or tagged into the stream, the tag is updated to point to the new image’s immutable SHA digest. A single image digest can have multiple tags simultaneously assigned to it. For example, the <code>:v3.11.59-2</code> and <code>:latest</code> tags are assigned to the same image digest.</p>
      <p>Tags offer two main benefits:</p>
      <ul>
      <li>Tags serve as the primary mechanism for builds and deployments to request a specific version of an image from an image stream.</li>
      <li>Tags help maintain clarity and allow for easy promotion of images between environments. For example, you can promote an image from the <code>:test</code> tag to the <code>:prod</code> tag.</li>
      </ul>
      <p>While image tags are primarily used for referencing images in configurations, OpenShift Container Platform provides the <code>oc tag</code> command for managing tags directly within image streams. This command is similar to the <code>podman tag</code> or <code>docker tag</code> commands, but it operates on image streams instead of directly on local images. It is used to create a new tag pointer or update an existing tag pointer within an image stream to point to a new image.</p>
      <p>Image tags are appended to the image name or image stream name by using a colon (<code>:</code>) as a separator.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Context</TableHead>
      <TableHead>Syntax Format</TableHead>
      <TableHead>Example</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><em></em><em><strong>External Registry</strong></em><em></em></TableCell>
      <TableCell><code>&lt;registry_path&gt;:&lt;tag&gt;</code></TableCell>
      <TableCell><code>registry.access.redhat.com/openshift3/jenkins-2-rhel7:v3.11.59-2</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><em></em><em><strong>Local Image Stream</strong></em><em></em></TableCell>
      <TableCell><code>&lt;image_stream_name&gt;:&lt;tag&gt;</code></TableCell>
      <TableCell><code>jenkins:latest</code></TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="images-tagging-conventions_tagging-images" class="group scroll-mt-20">Image tag conventions<a href="#images-tagging-conventions_tagging-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Image tag naming conventions in OpenShift Container Platform provide guidelines for creating tags that enable effective image pruning and maintain manageable image streams. Use consistent naming patterns to avoid tags that point to single revisions and never update.</p>
      <p>Tags that are too specific effectively pin the tag to a single image revision that is never updated. For example, if you create a tag named <code>v2.0.1-may-2019</code>, the tag points to just one revision of an image and is never updated. If you use default image pruning options, such an image is never removed.</p>
      <p>In very large clusters, the schema of creating new tags for every revised image could eventually fill up the etcd datastore with excess tag metadata for images that are long outdated. If the tag is named <code>v2.0</code>, image revisions are more likely. This results in longer tag history and, therefore, the image pruner is more likely to remove old and unused images.</p>
      <p>To ensure proper garbage collection, use broader, more generic tags that are designed to be updated when a new image revision is built. The following table provides some recommended tagging conventions using the format <code>&lt;image_name&gt;:&lt;image_tag&gt;</code>.</p>
      <p><strong>Image tag naming conventions</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Description</TableHead>
      <TableHead>Example</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><em></em><em><strong>Major/Minor Version</strong></em><em></em> (Ideal for mutable pointers)</TableCell>
      <TableCell><code>myimage:v2.0</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><em></em><em><strong>Full Revision</strong></em><em></em> (Often used for tracking, but requires manual pruning)</TableCell>
      <TableCell><code>myimage:v2.0.1</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><em></em><em><strong>Architecture</strong></em><em></em></TableCell>
      <TableCell><code>myimage:v2.0-x86_64</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><em></em><em><strong>Base image</strong></em><em></em></TableCell>
      <TableCell><code>myimage:v1.2-centos7</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><em></em><em><strong>Latest</strong></em><em></em></TableCell>
      <TableCell><code>myimage:latest</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><em></em><em><strong>Latest stable</strong></em><em></em></TableCell>
      <TableCell><code>myimage:stable</code></TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If your team requires the use of unique, date-specific, or highly revisioned tags like <code>v2.0.1-may-2019</code>, you must periodically inspect old and unsupported images and <code>istags</code> and remove them. Otherwise, you can experience increasing resource usage caused by retaining old images.</p>
        </AlertDescription>
      </Alert>
      <h2 id="images-add-tags-to-imagestreams_tagging-images" class="group scroll-mt-20">Adding tags to image streams<a href="#images-add-tags-to-imagestreams_tagging-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To organize images and create aliases for specific versions or automatically track changes to source tags in OpenShift Container Platform, you can add tags to image streams with the <code>oc tag</code> command.</p>
      <p>There are two types of tags available in OpenShift Container Platform:</p>
      <ul>
      <li>Permanent tags: A permanent tag points to a specific image in time. If the permanent tag is in use and the source changes, the tag does not change for the destination.</li>
      <li>Tracking tags: A tracking tag means that the destination tag’s metadata is updated during the import of the source tag.</li>
      </ul>
      <p>The default behavior creates a permanent tag that is pinned to an image ID.</p>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Optional: Add a tag to an image stream by entering the following command. The default behavior creates a permanent tag that is pinned to an image ID:
      <CodeBlock code={"$ oc tag <source_reference> <destination_image_stream>:<destination_tag>"} lang="terminal" />
      <p>For example, to configure the <code>ruby</code> image stream <code>static-2.0</code> tag to always refer to the specific image that the <code>ruby:2.0</code> tag points to now, enter the following command:</p>
      <CodeBlock code={"$ oc tag ruby:2.0 ruby:static-2.0"} lang="terminal" />
      <p>This creates a new image stream tag named <code>static-2.0</code> in the <code>ruby</code> image stream. The new tag directly references the image ID that the <code>ruby:2.0</code> image stream tag pointed to at the time <code>oc tag</code> was run, and the image it points to never changes.</p>
      </li>
      <li>
      Optional: Use the <code>--alias=true</code> flag to create a tracking tag. This ensures the destination tag automatically updates (tracks) when the source tag changes to point to a new image. For example, to ensure that the <code>ruby:latest</code> tag always reflects whatever image is currently tagged as <code>ruby:2.0</code>, enter the following command:
      <CodeBlock code={"$ oc tag --alias=true ruby:2.0 ruby:latest"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>A Tracking Tag created with <code>--alias=true</code> automatically updates its image ID whenever the source tag changes. Use the <code>latest</code> or <code>stable</code> tracking tags for creating common, long-lived aliases. This tracking behavior only works correctly within a single image stream. Trying to create a cross-image stream alias produces an error.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      Optional: Use the <code>--scheduled=true</code> flag to have the destination tag be refreshed, or re-imported, periodically. The period is configured globally at the system level. For example:
      <CodeBlock code={"$ oc tag <source_reference> <destination_image_stream>:<destination_tag> --scheduled=true"} lang="terminal" />
      </li>
      <li>
      Optional: Use the <code>--reference</code> flag to create an image stream tag that is not imported. The tag permanently points to the source location, regardless of changes to the source image. For example:
      <CodeBlock code={"$ oc tag <source_reference> <destination_image_stream>:<destination_tag> --reference"} lang="terminal" />
      </li>
      <li>
      Optional. Use the <code>--insecure</code> flag if the source registry is not secured with a valid HTTPS certificate. This flag tells the image stream to skip certificate verification during the import progress. For example:
      <CodeBlock code={"$ oc tag <source_reference> <destination_image_stream>:<destination_tag> --insecure"} lang="terminal" />
      </li>
      <li>
      Optional: Use the <code>--reference-policy=local</code> flag to instruct OpenShift Container Platform to always fetch the tagged image from the integrated registry. The registry uses the pull-through feature to serve the image to the client. By default, the image blobs are mirrored locally by the registry. As a result, they can be pulled more quickly the next time they are needed. The  <code>--reference-policy=local</code> flag also allows for pulling from insecure registries without a need to supply the <code>--insecure</code> flag to the container runtime provided that the image stream has an insecure annotation or the tag has an insecure import policy. For example:
      <CodeBlock code={"$ oc tag <source_reference> <destination_image_stream>:<destination_tag> --reference-policy=local"} lang="terminal" />
      </li>
      </ul>
      <h2 id="images-remove-tag-imagestream_tagging-images" class="group scroll-mt-20">Removing tags from image streams<a href="#images-remove-tag-imagestream_tagging-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To keep your image streams clean and maintain organized image references in OpenShift Container Platform, you can remove unused or outdated image stream tags. Remove tags by using the <code>oc delete istag</code> or <code>oc tag -d</code> commands. </p>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Remove a tag from an image stream by entering the following command:
      <CodeBlock code={"$ oc delete istag/<name>:<tag>"} lang="terminal" />
      <p>For example, to remove the <code>ruby:latest</code> tag from the <code>ruby</code> image stream, enter the following command:</p>
      <CodeBlock code={"$ oc delete istag/ruby:latest"} lang="terminal" />
      </li>
      <li>
      Alternatively, you can remove a tag using the <code>oc tag -d</code> command:
      <CodeBlock code={"$ oc tag -d <name>:<tag>"} lang="terminal" />
      <p>For example, to remove the <code>ruby:latest</code> tag from the <code>ruby</code> image stream, enter the following command:</p>
      <CodeBlock code={"$ oc tag -d ruby:latest"} lang="terminal" />
      </li>
      </ul>
      <h2 id="images-referencing-images-imagestreams_tagging-images" class="group scroll-mt-20">Using image stream reference syntax<a href="#images-referencing-images-imagestreams_tagging-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To ensure that your builds and deployments use the intended image version in OpenShift Container Platform, you must use the correct reference syntax format.</p>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      To reference an image by a mutable tag (<code>ImageStreamTag</code>) from an image stream within your cluster, use the <code>&lt;image_stream_name&gt;:&lt;tag&gt;</code> format in your build or deployment. For example:
      <CodeBlock code={"# ...\nspec:\n  containers:\n    - name: my-app\n      image: <image_stream_name>:<tag>"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.containers.image</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the image to use from the image stream. For example, <code>ruby:2.0</code>.</p></dd>
      </dl>
      </li>
      <li>
      To reference a specific, immutable image ID, or digest, within an image stream, use the <code>&lt;image_stream_name&gt;@&lt;image_id&gt;</code> format in your build or deployment. For example:
      <CodeBlock code={"# ...\nspec:\n  containers:\n    - name: my-app\n      image: <image_stream_name>@<image_id>"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.containers.image</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the image to use from the image stream. For example, <code>ruby@sha256:3a335d7d8a452970c5b4054ad7118ff134b3a6b50a2bb6d0c07c746e8986b28e</code>.</p></dd>
      </dl>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Using the image ID with the <code>@id</code> syntax ensures your configuration always uses the exact same image, even if the tag is later updated to point to a different image.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      To reference an image from an external registry by using the DockerImage format, use the standard Docker pull specification: <code>&lt;registry&gt;/&lt;namespace&gt;/&lt;image_name&gt;:&lt;tag&gt;</code>. For example:
      <CodeBlock code={"# ...\nspec:\n  source:\n    type: Dockerfile\n  strategy:\n    type: Docker\n    dockerStrategy:\n      from:\n        kind: DockerImage\n        name: <registry>/<namespace>/<image_name>:<tag>"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.strategy.dockerStrategy.from.name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the image to use from the external registry. For example, <code>registry.redhat.io/rhel7:latest</code>.</p></dd>
      </dl>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>When no tag is specified in a <code>DockerImage</code> reference, the <code>latest</code> tag is assumed.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      <h2 id="images-imagestream-reference-types_tagging-images" class="group scroll-mt-20">Understanding image stream reference types<a href="#images-imagestream-reference-types_tagging-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">By using image streams in OpenShift Container Platform, you can reference container images by using different reference types. These reference types define which specific image version your builds and deployments use.</p>
      <p><code>ImageStreamImage</code> objects are automatically created in OpenShift Container Platform when you import or tag an image into the image stream. You never have to explicitly define an <code>ImageStreamImage</code> object in any image stream definition that you use to create image streams.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Example image stream definitions often contain definitions of <code>ImageStreamTag</code> and references to <code>DockerImage</code>, but never contain definitions of <code>ImageStreamImage</code>.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Imagestream reference types</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Reference Type</TableHead>
      <TableHead>Description</TableHead>
      <TableHead>Syntax Examples</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>ImageStreamTag</code></TableCell>
      <TableCell>References or retrieves an image for a given image stream and human-readable tag.</TableCell>
      <TableCell><code>image_stream_name:tag</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ImageStreamImage</code></TableCell>
      <TableCell>References or retrieves an image for a given image stream and immutable SHA ID (digest).</TableCell>
      <TableCell><code>image_stream_name@id</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>DockerImage</code></TableCell>
      <TableCell>References or retrieves an image from an external registry. Uses the standard <code>docker pull</code> specification.</TableCell>
      <TableCell><code>openshift/ruby-20-centos7:2.0</code>, <code>registry.redhat.io/rhel7:latest</code>, <code>centos/ruby-22-centos7@sha256:3a335d7d...</code></TableCell>
      </TableRow>
      </TableBody>
      </Table>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
