---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: web_console/web_terminal/installing-web-terminal.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";

const headings = [{"depth":1,"slug":"installing-web-terminal","text":"Installing the web terminal"},{"depth":2,"slug":"prerequisites_installing-web-terminal","text":"Prerequisites"},{"depth":2,"slug":"installing-web-terminal-procedure","text":"Procedure"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/web_console/web_terminal/installing-web-terminal";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Installing the web terminal";
const description = "You can install the web terminal by using the {{ web_terminal_op }} listed in the OpenShift Container Platform software catalog. When you install the {{…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["web_console","web_console/web_terminal"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Installing the web terminal {#installing-web-terminal}\n\nYou can install the web terminal by using the {{ web_terminal_op }} listed in the OpenShift Container Platform software catalog. When you install the {{ web_terminal_op }}, the custom resource definitions (CRDs) that are required for the command line configuration, such as the `DevWorkspace` CRD, are automatically installed. The web console creates the required resources when you open the web terminal.\n\n## Prerequisites {#prerequisites_installing-web-terminal}\n\n- You are logged into the OpenShift Container Platform web console.\n- You have cluster administrator permissions.\n\n## Procedure {#installing-web-terminal-procedure}\n\n1. In the **Administrator** perspective of the web console, navigate to **Ecosystem** -> **Software Catalog**.\n2. Use the **Filter by keyword** box to search for the {{ web_terminal_op }} in the catalog, and then click the **Web Terminal** tile.\n3. Read the brief description about the Operator on the **Web Terminal**  page, and then click **Install**.\n4. On the **Install Operator** page, retain the default values for all fields.\n\n   - The **fast** option in the **Update Channel** menu enables installation of the latest release of the {{ web_terminal_op }}.\n   - The **All namespaces on the cluster** option in the **Installation Mode** menu  enables the Operator to watch and be available to all namespaces in the cluster.\n   - The **openshift-operators** option in the **Installed Namespace** menu installs the Operator in the default `openshift-operators` namespace.\n   - The **Automatic** option in the **Approval Strategy** menu ensures that the future upgrades to the Operator are handled automatically by the Operator Lifecycle Manager.\n5. Click **Install**.\n6. In the **Installed Operators** page, click the **View Operator** to verify that the Operator is listed on the **Installed Operators** page.\n\n   > [!NOTE]\n   > The {{ web_terminal_op }} installs the DevWorkspace Operator as a dependency.\n7. After the Operator is installed, refresh your page to see the command-line terminal icon (![title=\"web terminal icon\"](/openshift-docs-markdown/_assets/images/odc-wto-icon.png)) in the masthead of the console.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/web_console/web_terminal/installing-web-terminal.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="installing-web-terminal" class="group scroll-mt-20">Installing the web terminal<a href="#installing-web-terminal" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="/_attributes/attributes-openshift-dedicated.md /_attributes/common-attributes.md"></p>
      <p>You can install the web terminal by using the {"{"}{"{"} web_terminal_op {"}"}{"}"} listed in the OpenShift Container Platform software catalog. When you install the {"{"}{"{"} web_terminal_op {"}"}{"}"}, the custom resource definitions (CRDs) that are required for the command line configuration, such as the <code>DevWorkspace</code> CRD, are automatically installed. The web console creates the required resources when you open the web terminal.</p>
      <h2 id="prerequisites_installing-web-terminal" class="group scroll-mt-20">Prerequisites<a href="#prerequisites_installing-web-terminal" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li>You are logged into the OpenShift Container Platform web console.</li>
      <li>You have cluster administrator permissions.</li>
      </ul>
      <h2 id="installing-web-terminal-procedure" class="group scroll-mt-20">Procedure<a href="#installing-web-terminal-procedure" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ol>
      <li>In the <strong>Administrator</strong> perspective of the web console, navigate to <strong>Ecosystem</strong> -&gt; <strong>Software Catalog</strong>.</li>
      <li>Use the <strong>Filter by keyword</strong> box to search for the {"{"}{"{"} web_terminal_op {"}"}{"}"} in the catalog, and then click the <strong>Web Terminal</strong> tile.</li>
      <li>Read the brief description about the Operator on the <strong>Web Terminal</strong>  page, and then click <strong>Install</strong>.</li>
      <li>
      On the <strong>Install Operator</strong> page, retain the default values for all fields.
      <ul>
      <li>The <strong>fast</strong> option in the <strong>Update Channel</strong> menu enables installation of the latest release of the {"{"}{"{"} web_terminal_op {"}"}{"}"}.</li>
      <li>The <strong>All namespaces on the cluster</strong> option in the <strong>Installation Mode</strong> menu  enables the Operator to watch and be available to all namespaces in the cluster.</li>
      <li>The <strong>openshift-operators</strong> option in the <strong>Installed Namespace</strong> menu installs the Operator in the default <code>openshift-operators</code> namespace.</li>
      <li>The <strong>Automatic</strong> option in the <strong>Approval Strategy</strong> menu ensures that the future upgrades to the Operator are handled automatically by the Operator Lifecycle Manager.</li>
      </ul>
      </li>
      <li>Click <strong>Install</strong>.</li>
      <li>
      In the <strong>Installed Operators</strong> page, click the <strong>View Operator</strong> to verify that the Operator is listed on the <strong>Installed Operators</strong> page.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The {"{"}{"{"} web_terminal_op {"}"}{"}"} installs the DevWorkspace Operator as a dependency.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>After the Operator is installed, refresh your page to see the command-line terminal icon (<img src="/openshift-docs-markdown/_assets/images/odc-wto-icon.png" alt="title=&quot;web terminal icon&quot;" loading="lazy" />) in the masthead of the console.</li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
