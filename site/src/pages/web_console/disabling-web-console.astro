---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: web_console/disabling-web-console.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"disabling-web-console","text":"Disabling the web console in OpenShift Container Platform"},{"depth":2,"slug":"_prerequisites","text":"Prerequisites"},{"depth":2,"slug":"web-console-disable_disabling-web-console","text":"Disabling the web console"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/web_console/disabling-web-console";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Disabling the web console in OpenShift Container Platform";
const description = "You can disable the OpenShift Container Platform web console." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["web_console"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Disabling the web console in OpenShift Container Platform {#disabling-web-console}\n\nYou can disable the OpenShift Container Platform web console.\n\n## Prerequisites {#_prerequisites}\n\n- Deploy an OpenShift Container Platform\n\ncluster.\n\n## Disabling the web console {#web-console-disable_disabling-web-console}\n\nYou can disable the web console by editing the `consoles.operator.openshift.io` resource.\n\n**Procedure**\n\n- Edit the `consoles.operator.openshift.io` resource:\n\n  ```terminal\n  $ oc edit consoles.operator.openshift.io cluster\n  ```\n\n  The following example displays the parameters from this resource that you can modify:\n\n  ```yaml\n  apiVersion: operator.openshift.io/v1\n  kind: Console\n  metadata:\n    name: cluster\n  spec:\n    managementState: Removed\n  ```\n\n  where:\n\n  `spec.managementState.Removed`\n  :   Set the `managementState` parameter value to `Removed` to disable the web console. The other valid values for this parameter are `Managed`, which enables the console under the cluster’s control, and `Unmanaged`, which means that you are taking control of web console management.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/web_console/disabling-web-console.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="disabling-web-console" class="group scroll-mt-20">Disabling the web console in OpenShift Container Platform<a href="#disabling-web-console" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can disable the OpenShift Container Platform web console.</p>
      <h2 id="_prerequisites" class="group scroll-mt-20">Prerequisites<a href="#_prerequisites" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul><li>Deploy an OpenShift Container Platform</li></ul>
      <p>cluster.</p>
      <h2 id="web-console-disable_disabling-web-console" class="group scroll-mt-20">Disabling the web console<a href="#web-console-disable_disabling-web-console" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can disable the web console by editing the <code>consoles.operator.openshift.io</code> resource.</p>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Edit the <code>consoles.operator.openshift.io</code> resource:
      <CodeBlock code={"$ oc edit consoles.operator.openshift.io cluster"} lang="terminal" />
      <p>The following example displays the parameters from this resource that you can modify:</p>
      <CodeBlock code={"apiVersion: operator.openshift.io/v1\nkind: Console\nmetadata:\n  name: cluster\nspec:\n  managementState: Removed"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.managementState.Removed</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Set the <code>managementState</code> parameter value to <code>Removed</code> to disable the web console. The other valid values for this parameter are <code>Managed</code>, which enables the console under the cluster’s control, and <code>Unmanaged</code>, which means that you are taking control of web console management.</p></dd>
      </dl>
      </li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
