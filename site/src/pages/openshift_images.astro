---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: openshift_images.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"overview-of-images","text":"Overview of images"},{"depth":2,"slug":"images-about_overview-of-images","text":"Images"},{"depth":2,"slug":"images-image-registry-about_overview-of-images","text":"Image registry"},{"depth":2,"slug":"images-container-repository-about_overview-of-images","text":"Image repository"},{"depth":2,"slug":"images-tag_overview-of-images","text":"Understanding image tags in image streams"},{"depth":2,"slug":"images-id_overview-of-images","text":"Image IDs"},{"depth":2,"slug":"containers-about_overview-of-images","text":"Containers"},{"depth":2,"slug":"images-imagestream-use_overview-of-images","text":"Using image streams"},{"depth":2,"slug":"images-imagestream-tag_overview-of-images","text":"Image stream tags"},{"depth":2,"slug":"images-imagestream-image_overview-of-images","text":"Image stream images"},{"depth":2,"slug":"images-imagestream-trigger_overview-of-images","text":"Image stream triggers"},{"depth":2,"slug":"how-you-can-use-the-cluster-samples-operator_overview-of-images","text":"How you can use the Cluster Samples Operator"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/openshift_images";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Overview of images";
const description = "To understand how containerized applications work in OpenShift Container Platform, you need to know about containers, images, and image streams. This…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = null;
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Overview of images {#overview-of-images}\n\nTo understand how containerized applications work in OpenShift Container Platform, you need to know about containers, images, and image streams. This overview explains these core concepts and how they work together in your cluster.\n\n## Images {#images-about_overview-of-images}\n\nContainer images are binaries that include all requirements for running a single container. You can use images to package applications and deploy them consistently across multiple containers and hosts in OpenShift Container Platform.\n\nContainers only have access to resources defined in the image unless you give the container additional access when creating it. By deploying the same image in multiple containers across multiple hosts and load balancing between them, OpenShift Container Platform can provide redundancy and horizontal scaling for a service packaged into an image.\n\nYou can use the podman or `docker` CLI directly to build images, but OpenShift Container Platform also supplies builder images that assist with creating new images by adding your code or configuration to existing images.\n\nBecause applications develop over time, a single image name can actually refer to many different versions of the same image. Each different image is referred to uniquely by its hash, a long hexadecimal number such as `fd44297e2ddb050ec4f...`, which is usually shortened to 12 characters, such as `fd44297e2ddb`.\n\n**Additional resources**\n{._additional-resources}\n\n- [podman](https://access.redhat.com/documentation/en-us/red_hat_enterprise_linux_atomic_host/7/html-single/managing_containers/#using_podman_to_work_with_containers)\n- [Creating images](/openshift-docs-markdown/openshift_images/create-images#creating-images)\n- [Managing images](/openshift-docs-markdown/openshift_images/managing_images/managing-images-overview#managing-images-overview)\n- [Using images](/openshift-docs-markdown/openshift_images/using_images/using-images-overview#using-images-overview)\n\n## Image registry {#images-image-registry-about_overview-of-images}\n\nAn image registry is a content server that stores and serves container images in OpenShift Container Platform. You can use registries to access container images from external sources or the integrated registry in OpenShift Container Platform.\n\nRegistries contain a collection of one or more image repositories, which contain one or more tagged images. Red Hat provides a registry at registry.redhat.io for subscribers. OpenShift Container Platform can also supply its own OpenShift image registry for managing custom container images.\n\n## Image repository {#images-container-repository-about_overview-of-images}\n\nAn image repository is a collection of related container images and tags that identify them. You can use image repositories to organize and manage related container images in OpenShift Container Platform.\n\nFor example, the OpenShift Container Platform Jenkins images are in the following repository:\n\n```text\ndocker.io/openshift/jenkins-2-centos7\n```\n\n## Understanding image tags in image streams {#images-tag_overview-of-images}\n\nImage tags in OpenShift Container Platform help you organize, identify, and reference specific versions of container images in image streams. Tags are human-readable labels that act as pointers to particular image layers and digests.\n\nTags function as mutable pointers within an image stream. When a new image is imported or tagged into the stream, the tag is updated to point to the new image’s immutable SHA digest. A single image digest can have multiple tags simultaneously assigned to it. For example, the `:v3.11.59-2` and `:latest` tags are assigned to the same image digest.\n\nTags offer two main benefits:\n\n- Tags serve as the primary mechanism for builds and deployments to request a specific version of an image from an image stream.\n- Tags help maintain clarity and allow for easy promotion of images between environments. For example, you can promote an image from the `:test` tag to the `:prod` tag.\n\nWhile image tags are primarily used for referencing images in configurations, OpenShift Container Platform provides the `oc tag` command for managing tags directly within image streams. This command is similar to the `podman tag` or `docker tag` commands, but it operates on image streams instead of directly on local images. It is used to create a new tag pointer or update an existing tag pointer within an image stream to point to a new image.\n\nImage tags are appended to the image name or image stream name by using a colon (`:`) as a separator.\n\n| Context | Syntax Format | Example |\n| --- | --- | --- |\n| **External Registry** | `<registry_path>:<tag>` | `registry.access.redhat.com/openshift3/jenkins-2-rhel7:v3.11.59-2` |\n| **Local Image Stream** | `<image_stream_name>:<tag>` | `jenkins:latest` |\n\n## Image IDs {#images-id_overview-of-images}\n\nImage IDs are Secure Hash Algorithm (SHA) codes that uniquely identify container images in OpenShift Container Platform. You can use image IDs to pull specific versions of images that never change.\n\nFor example, the following image ID is for the `docker.io/openshift/jenkins-2-centos7` image:\n\n```text\ndocker.io/openshift/jenkins-2-centos7@sha256:ab312bda324\n```\n\n## Containers {#containers-about_overview-of-images}\n\nContainers are isolated running instances of container images that serve as the basic units of OpenShift Container Platform applications. By understanding containers, you can work with containerized applications and manage how they run in your cluster.\n\nMany application instances can be running in containers on a single host without visibility into each others' processes, files, network, and so on. Typically, each container provides a single service, often called a micro-service, such as a web server or a database, though containers can be used for arbitrary workloads.\n\nThe Linux kernel has been incorporating capabilities for container technologies for years. The Docker project developed a convenient management interface for Linux containers on a host. More recently, the Open Container Initiative has developed open standards for container formats and container runtimes. OpenShift Container Platform and Kubernetes add the ability to orchestrate OCI- and Docker-formatted containers across multi-host installations.\n\nThough you do not directly interact with container runtimes when using OpenShift Container Platform, understanding their capabilities and terminology is important for understanding their role in OpenShift Container Platform and how your applications function inside of containers.\n\nTools such as Podman can be used to replace Docker command-line tools for running and managing containers directly. By using the `podman` CLI, you can experiment with containers separately from OpenShift Container Platform.\n\n## Using image streams {#images-imagestream-use_overview-of-images}\n\nImage streams provide an abstraction for referencing container images from within OpenShift Container Platform. You can use image streams to manage image versions and automate builds and deployments in your cluster.\n\nImage streams do not contain actual image data, but present a single virtual view of related images, similar to an image repository.\n\nYou can configure builds and deployments to watch an image stream for notifications when new images are added and react by performing a build or deployment, respectively.\n\nFor example, if a deployment is using a certain image and a new version of that image is created, a deployment could be automatically performed to pick up the new version of the image.\n\nHowever, if the image stream tag used by the deployment or build is not updated, then even if the container image in the container image registry is updated, the build or deployment continues using the previous, presumably known good image.\n\nThe source images can be stored in any of the following:\n\n- OpenShift Container Platform's integrated registry.\n- An external registry, for example registry.redhat.io or quay.io.\n- Other image streams in the OpenShift Container Platform cluster.\n\nWhen you define an object that references an image stream tag, such as a build or deployment configuration, you point to an image stream tag and not the repository. When you build or deploy your application, OpenShift Container Platform queries the repository using the image stream tag to locate the associated ID of the image and uses that exact image.\n\nThe image stream metadata is stored in the etcd instance along with other cluster information.\n\nUsing image streams has several significant benefits:\n\n- You can tag, rollback a tag, and quickly deal with images, without having to re-push using the command line.\n- You can trigger builds and deployments when a new image is pushed to the registry. Also, OpenShift Container Platform has generic triggers for other resources, such as Kubernetes objects.\n- You can mark a tag for periodic re-import. If the source image has changed, that change is picked up and reflected in the image stream, which triggers the build or deployment flow, depending upon the build or deployment configuration.\n- You can share images using fine-grained access control and quickly distribute images across your teams.\n- If the source image changes, the image stream tag still points to a known-good version of the image, ensuring that your application does not break unexpectedly.\n- You can configure security around who can view and use the images through permissions on the image stream objects.\n- Users that lack permission to read or list images on the cluster level can still retrieve the images tagged in a project using image streams.\n\n**Additional resources**\n{._additional-resources}\n\n- [Open Container Initiative](https://github.com/opencontainers/)\n- [Podman](https://access.redhat.com/documentation/en-us/red_hat_enterprise_linux_atomic_host/7/html-single/managing_containers/#using_podman_to_work_with_containers)\n- [Managing image streams](/openshift-docs-markdown/openshift_images/image-streams-manage#managing-image-streams)\n- [Using image streams with Kubernetes resources](/openshift-docs-markdown/openshift_images/using-imagestreams-with-kube-resources#using-imagestreams-with-kube-resources)\n- [Triggering updates on image stream updates](/openshift-docs-markdown/openshift_images/triggering-updates-on-imagestream-changes#triggering-updates-on-imagestream-changes)\n\n## Image stream tags {#images-imagestream-tag_overview-of-images}\n\nImage stream tags are named pointers to images in image streams in OpenShift Container Platform. You can configure image stream tags to reference specific versions of container images.\n\n## Image stream images {#images-imagestream-image_overview-of-images}\n\nImage stream images are API resource objects in OpenShift Container Platform that retrieve specific container images from image streams. You can use image stream images to access metadata about particular image SHA identifiers.\n\n## Image stream triggers {#images-imagestream-trigger_overview-of-images}\n\nImage stream triggers in OpenShift Container Platform cause specific actions when image stream tags change. You can configure triggers to automatically start builds or deployments when new images are imported.\n\nFor example, importing a new image can cause the value of the tag to change, which causes a trigger to fire when there are deployments, builds, or other resources listening for those.\n\n## How you can use the Cluster Samples Operator {#how-you-can-use-the-cluster-samples-operator_overview-of-images}\n\nTo manage sample image streams and templates in OpenShift Container Platform, you can use the Cluster Samples Operator. The Cluster Samples Operator creates default samples during initial startup to initiate image streams and templates in the `openshift` namespace.\n\n**Additional resources**\n{._additional-resources}\n\n- [Configuring the Cluster Samples Operator](/openshift-docs-markdown/openshift_images/configuring-samples-operator#configuring-samples-operator)\n- [Use the Operator with an alternate registry](/openshift-docs-markdown/openshift_images/samples-operator-alt-registry#samples-operator-alt-registry)\n- [Understanding templates](/openshift-docs-markdown/applications/creating_applications/using-templates#templates-overview)\n- [Creating applications using Ruby on Rails](/openshift-docs-markdown/applications/creating_applications/templates-using-ruby-on-rails#templates-using-ruby-on-rails)\n- [registry.redhat.io](https://catalog.redhat.com/en)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/openshift_images.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="overview-of-images" class="group scroll-mt-20">Overview of images<a href="#overview-of-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">To understand how containerized applications work in OpenShift Container Platform, you need to know about containers, images, and image streams. This overview explains these core concepts and how they work together in your cluster.</p>
      <h2 id="images-about_overview-of-images" class="group scroll-mt-20">Images<a href="#images-about_overview-of-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Container images are binaries that include all requirements for running a single container. You can use images to package applications and deploy them consistently across multiple containers and hosts in OpenShift Container Platform.</p>
      <p>Containers only have access to resources defined in the image unless you give the container additional access when creating it. By deploying the same image in multiple containers across multiple hosts and load balancing between them, OpenShift Container Platform can provide redundancy and horizontal scaling for a service packaged into an image.</p>
      <p>You can use the podman or <code>docker</code> CLI directly to build images, but OpenShift Container Platform also supplies builder images that assist with creating new images by adding your code or configuration to existing images.</p>
      <p>Because applications develop over time, a single image name can actually refer to many different versions of the same image. Each different image is referred to uniquely by its hash, a long hexadecimal number such as <code>fd44297e2ddb050ec4f...</code>, which is usually shortened to 12 characters, such as <code>fd44297e2ddb</code>.</p>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="https://access.redhat.com/documentation/en-us/red_hat_enterprise_linux_atomic_host/7/html-single/managing_containers/#using_podman_to_work_with_containers">podman</a></li>
      <li><a href="/openshift-docs-markdown/openshift_images/create-images#creating-images">Creating images</a></li>
      <li><a href="/openshift-docs-markdown/openshift_images/managing_images/managing-images-overview#managing-images-overview">Managing images</a></li>
      <li><a href="/openshift-docs-markdown/openshift_images/using_images/using-images-overview#using-images-overview">Using images</a></li>
      </ul>
      </section>
      <h2 id="images-image-registry-about_overview-of-images" class="group scroll-mt-20">Image registry<a href="#images-image-registry-about_overview-of-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">An image registry is a content server that stores and serves container images in OpenShift Container Platform. You can use registries to access container images from external sources or the integrated registry in OpenShift Container Platform.</p>
      <p>Registries contain a collection of one or more image repositories, which contain one or more tagged images. Red Hat provides a registry at registry.redhat.io for subscribers. OpenShift Container Platform can also supply its own OpenShift image registry for managing custom container images.</p>
      <h2 id="images-container-repository-about_overview-of-images" class="group scroll-mt-20">Image repository<a href="#images-container-repository-about_overview-of-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">An image repository is a collection of related container images and tags that identify them. You can use image repositories to organize and manage related container images in OpenShift Container Platform.</p>
      <p>For example, the OpenShift Container Platform Jenkins images are in the following repository:</p>
      <CodeBlock code={"docker.io/openshift/jenkins-2-centos7"} lang="text" />
      <h2 id="images-tag_overview-of-images" class="group scroll-mt-20">Understanding image tags in image streams<a href="#images-tag_overview-of-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Image tags in OpenShift Container Platform help you organize, identify, and reference specific versions of container images in image streams. Tags are human-readable labels that act as pointers to particular image layers and digests.</p>
      <p>Tags function as mutable pointers within an image stream. When a new image is imported or tagged into the stream, the tag is updated to point to the new image’s immutable SHA digest. A single image digest can have multiple tags simultaneously assigned to it. For example, the <code>:v3.11.59-2</code> and <code>:latest</code> tags are assigned to the same image digest.</p>
      <p>Tags offer two main benefits:</p>
      <ul>
      <li>Tags serve as the primary mechanism for builds and deployments to request a specific version of an image from an image stream.</li>
      <li>Tags help maintain clarity and allow for easy promotion of images between environments. For example, you can promote an image from the <code>:test</code> tag to the <code>:prod</code> tag.</li>
      </ul>
      <p>While image tags are primarily used for referencing images in configurations, OpenShift Container Platform provides the <code>oc tag</code> command for managing tags directly within image streams. This command is similar to the <code>podman tag</code> or <code>docker tag</code> commands, but it operates on image streams instead of directly on local images. It is used to create a new tag pointer or update an existing tag pointer within an image stream to point to a new image.</p>
      <p>Image tags are appended to the image name or image stream name by using a colon (<code>:</code>) as a separator.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Context</TableHead>
      <TableHead>Syntax Format</TableHead>
      <TableHead>Example</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><strong>External Registry</strong></TableCell>
      <TableCell><code>&lt;registry_path&gt;:&lt;tag&gt;</code></TableCell>
      <TableCell><code>registry.access.redhat.com/openshift3/jenkins-2-rhel7:v3.11.59-2</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Local Image Stream</strong></TableCell>
      <TableCell><code>&lt;image_stream_name&gt;:&lt;tag&gt;</code></TableCell>
      <TableCell><code>jenkins:latest</code></TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="images-id_overview-of-images" class="group scroll-mt-20">Image IDs<a href="#images-id_overview-of-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Image IDs are Secure Hash Algorithm (SHA) codes that uniquely identify container images in OpenShift Container Platform. You can use image IDs to pull specific versions of images that never change. </p>
      <p>For example, the following image ID is for the <code>docker.io/openshift/jenkins-2-centos7</code> image:</p>
      <CodeBlock code={"docker.io/openshift/jenkins-2-centos7@sha256:ab312bda324"} lang="text" />
      <h2 id="containers-about_overview-of-images" class="group scroll-mt-20">Containers<a href="#containers-about_overview-of-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Containers are isolated running instances of container images that serve as the basic units of OpenShift Container Platform applications. By understanding containers, you can work with containerized applications and manage how they run in your cluster.</p>
      <p>Many application instances can be running in containers on a single host without visibility into each others' processes, files, network, and so on. Typically, each container provides a single service, often called a micro-service, such as a web server or a database, though containers can be used for arbitrary workloads.</p>
      <p>The Linux kernel has been incorporating capabilities for container technologies for years. The Docker project developed a convenient management interface for Linux containers on a host. More recently, the Open Container Initiative has developed open standards for container formats and container runtimes. OpenShift Container Platform and Kubernetes add the ability to orchestrate OCI- and Docker-formatted containers across multi-host installations.</p>
      <p>Though you do not directly interact with container runtimes when using OpenShift Container Platform, understanding their capabilities and terminology is important for understanding their role in OpenShift Container Platform and how your applications function inside of containers.</p>
      <p>Tools such as Podman can be used to replace Docker command-line tools for running and managing containers directly. By using the <code>podman</code> CLI, you can experiment with containers separately from OpenShift Container Platform.</p>
      <h2 id="images-imagestream-use_overview-of-images" class="group scroll-mt-20">Using image streams<a href="#images-imagestream-use_overview-of-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Image streams provide an abstraction for referencing container images from within OpenShift Container Platform. You can use image streams to manage image versions and automate builds and deployments in your cluster.</p>
      <p>Image streams do not contain actual image data, but present a single virtual view of related images, similar to an image repository.</p>
      <p>You can configure builds and deployments to watch an image stream for notifications when new images are added and react by performing a build or deployment, respectively.</p>
      <p>For example, if a deployment is using a certain image and a new version of that image is created, a deployment could be automatically performed to pick up the new version of the image.</p>
      <p>However, if the image stream tag used by the deployment or build is not updated, then even if the container image in the container image registry is updated, the build or deployment continues using the previous, presumably known good image.</p>
      <p>The source images can be stored in any of the following:</p>
      <ul>
      <li>OpenShift Container Platform's integrated registry.</li>
      <li>An external registry, for example registry.redhat.io or quay.io.</li>
      <li>Other image streams in the OpenShift Container Platform cluster.</li>
      </ul>
      <p>When you define an object that references an image stream tag, such as a build or deployment configuration, you point to an image stream tag and not the repository. When you build or deploy your application, OpenShift Container Platform queries the repository using the image stream tag to locate the associated ID of the image and uses that exact image.</p>
      <p>The image stream metadata is stored in the etcd instance along with other cluster information.</p>
      <p>Using image streams has several significant benefits:</p>
      <ul>
      <li>You can tag, rollback a tag, and quickly deal with images, without having to re-push using the command line.</li>
      <li>You can trigger builds and deployments when a new image is pushed to the registry. Also, OpenShift Container Platform has generic triggers for other resources, such as Kubernetes objects.</li>
      <li>You can mark a tag for periodic re-import. If the source image has changed, that change is picked up and reflected in the image stream, which triggers the build or deployment flow, depending upon the build or deployment configuration.</li>
      <li>You can share images using fine-grained access control and quickly distribute images across your teams.</li>
      <li>If the source image changes, the image stream tag still points to a known-good version of the image, ensuring that your application does not break unexpectedly.</li>
      <li>You can configure security around who can view and use the images through permissions on the image stream objects.</li>
      <li>Users that lack permission to read or list images on the cluster level can still retrieve the images tagged in a project using image streams.</li>
      </ul>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="https://github.com/opencontainers/">Open Container Initiative</a></li>
      <li><a href="https://access.redhat.com/documentation/en-us/red_hat_enterprise_linux_atomic_host/7/html-single/managing_containers/#using_podman_to_work_with_containers">Podman</a></li>
      <li><a href="/openshift-docs-markdown/openshift_images/image-streams-manage#managing-image-streams">Managing image streams</a></li>
      <li><a href="/openshift-docs-markdown/openshift_images/using-imagestreams-with-kube-resources#using-imagestreams-with-kube-resources">Using image streams with Kubernetes resources</a></li>
      <li><a href="/openshift-docs-markdown/openshift_images/triggering-updates-on-imagestream-changes#triggering-updates-on-imagestream-changes">Triggering updates on image stream updates</a></li>
      </ul>
      </section>
      <h2 id="images-imagestream-tag_overview-of-images" class="group scroll-mt-20">Image stream tags<a href="#images-imagestream-tag_overview-of-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Image stream tags are named pointers to images in image streams in OpenShift Container Platform. You can configure image stream tags to reference specific versions of container images.</p>
      <h2 id="images-imagestream-image_overview-of-images" class="group scroll-mt-20">Image stream images<a href="#images-imagestream-image_overview-of-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Image stream images are API resource objects in OpenShift Container Platform that retrieve specific container images from image streams. You can use image stream images to access metadata about particular image SHA identifiers.</p>
      <h2 id="images-imagestream-trigger_overview-of-images" class="group scroll-mt-20">Image stream triggers<a href="#images-imagestream-trigger_overview-of-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Image stream triggers in OpenShift Container Platform cause specific actions when image stream tags change. You can configure triggers to automatically start builds or deployments when new images are imported.</p>
      <p>For example, importing a new image can cause the value of the tag to change, which causes a trigger to fire when there are deployments, builds, or other resources listening for those.</p>
      <h2 id="how-you-can-use-the-cluster-samples-operator_overview-of-images" class="group scroll-mt-20">How you can use the Cluster Samples Operator<a href="#how-you-can-use-the-cluster-samples-operator_overview-of-images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>To manage sample image streams and templates in OpenShift Container Platform, you can use the Cluster Samples Operator. The Cluster Samples Operator creates default samples during initial startup to initiate image streams and templates in the <code>openshift</code> namespace.</p>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/openshift_images/configuring-samples-operator#configuring-samples-operator">Configuring the Cluster Samples Operator</a></li>
      <li><a href="/openshift-docs-markdown/openshift_images/samples-operator-alt-registry#samples-operator-alt-registry">Use the Operator with an alternate registry</a></li>
      <li><a href="/openshift-docs-markdown/applications/creating_applications/using-templates#templates-overview">Understanding templates</a></li>
      <li><a href="/openshift-docs-markdown/applications/creating_applications/templates-using-ruby-on-rails#templates-using-ruby-on-rails">Creating applications using Ruby on Rails</a></li>
      <li><a href="https://catalog.redhat.com/en">registry.redhat.io</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
