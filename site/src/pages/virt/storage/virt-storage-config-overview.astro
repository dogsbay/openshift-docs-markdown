---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/storage/virt-storage-config-overview.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;

const headings = [{"depth":1,"slug":"virt-storage-config-overview","text":"Storage configuration overview"},{"depth":2,"slug":"storage-configuration-tasks","text":"Storage"},{"depth":2,"slug":"cdi-configuration-tasks","text":"Containerized Data Importer"},{"depth":2,"slug":"dv-configuration-tasks","text":"Data volumes"},{"depth":2,"slug":"boot-source-configuration","text":"Boot source updates"},{"depth":2,"slug":"additional-resources_virt-storage-config-overview","text":"Additional resources"}];
const nav = navData;
const currentPath = "/virt/storage/virt-storage-config-overview";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Storage configuration overview";
const description = "You can configure a default storage class, storage profiles, Containerized Data Importer (CDI), data volumes (DVs), and automatic boot source updates." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/storage"];
const pageTaxonomies = null;
const tagsIndexPath = "/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Storage configuration overview {#virt-storage-config-overview}\n\nYou can configure a default storage class, storage profiles, Containerized Data Importer (CDI), data volumes (DVs), and automatic boot source updates.\n\n## Storage {#storage-configuration-tasks}\n\nThe following storage configuration tasks are mandatory:\n\nConfigure a default storage class\n:   You must configure a default storage class for the cluster. Otherwise, {{ VirtProductName }} cannot automatically import boot source images. `DataVolume` objects (DVs) and `PersistentVolumeClaim` objects (PVCs) that do not explicitly specify a storage class remain in the `Pending` state until you set a default storage class.\n\nConfigure storage profiles\n:   You must configure storage profiles if your storage provider is not recognized by CDI. A storage profile provides recommended storage settings based on the associated storage class.\n\nThe following storage configuration tasks are optional:\n\nReserve additional PVC space for file system overhead\n:   By default, 5.5% of a file system PVC is reserved for overhead, reducing the space available for VM disks by that amount. You can configure a different overhead value.\n\nConfigure local storage by using the hostpath provisioner\n:   You can configure local storage for virtual machines by using the hostpath provisioner (HPP). When you install the {{ VirtProductName }} Operator, the HPP Operator is automatically installed.\n\nConfigure user permissions to clone data volumes between namespaces\n:   You can configure RBAC roles to enable users to clone data volumes between namespaces.\n\n## Containerized Data Importer {#cdi-configuration-tasks}\n\nYou can perform the following Containerized Data Importer (CDI) configuration tasks:\n\nOverride the resource request limits of a namespace\n:   You can configure CDI to import, upload, and clone VM disks into namespaces that are subject to CPU and memory resource restrictions.\n\nConfigure CDI scratch space\n:   CDI requires scratch space (temporary storage) to complete some operations, such as importing and uploading VM images. During this process, CDI provisions a scratch space PVC equal to the size of the PVC backing the destination data volume (DV).\n\n## Data volumes {#dv-configuration-tasks}\n\nYou can perform the following data volume configuration tasks:\n\nEnable preallocation for data volumes\n:   CDI can preallocate disk space to improve write performance when creating data volumes. You can enable preallocation for specific data volumes.\n\nManage data volume annotations\n:   Data volume annotations allow you to manage pod behavior. You can add one or more annotations to a data volume, which then propagates to the created importer pods.\n\n## Boot source updates {#boot-source-configuration}\n\nYou can perform the following boot source update configuration task:\n\nManage automatic boot source updates\n:   Boot sources can make virtual machine (VM) creation more accessible and efficient for users. If automatic boot source updates are enabled, CDI imports, polls, and updates the images so that they are ready to be cloned for new VMs. By default, CDI automatically updates Red Hat boot sources. You can enable automatic updates for custom boot sources.\n\n## Additional resources {#additional-resources_virt-storage-config-overview}\n\n- [Defining a storage class](/storage/dynamic-provisioning#dynamic-provisioning-defining-storage-class_dynamic-provisioning)\n- [Configure storage profiles](/virt/storage/virt-configuring-storage-profile#virt-configuring-storage-profile)\n- [Reserve additional PVC space for file system overhead](/virt/storage/virt-reserving-pvc-space-fs-overhead#virt-reserving-pvc-space-fs-overhead)\n- [Configure local storage by using the hostpath provisioner](/virt/storage/virt-configuring-local-storage-with-hpp#virt-configuring-local-storage-with-hpp)\n- [Configure user permissions to clone data volumes between namespaces](/virt/storage/virt-enabling-user-permissions-to-clone-datavolumes#virt-enabling-user-permissions-to-clone-datavolumes)\n- [Override the resource request limits of a namespace](/virt/storage/virt-configuring-cdi-for-namespace-resourcequota#virt-configuring-cdi-for-namespace-resourcequota)\n- [Configure CDI scratch space](/virt/storage/virt-preparing-cdi-scratch-space#virt-preparing-cdi-scratch-space)\n- [Enable preallocation for data volumes](/virt/storage/virt-using-preallocation-for-datavolumes#virt-using-preallocation-for-datavolumes)\n- [Manage data volume annotations](/virt/storage/virt-managing-data-volume-annotations#virt-managing-data-volume-annotations)\n- [Manage automatic boot source updates](/virt/storage/virt-automatic-bootsource-updates#virt-automatic-bootsource-updates)\n","mdUrl":"/virt/storage/virt-storage-config-overview.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={""}
  navMode={"client"}
  pagefindUrl={"/pagefind/"}
  favicon={"/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-storage-config-overview" class="group scroll-mt-20">Storage configuration overview<a href="#virt-storage-config-overview" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="/_attributes/common-attributes.md"></p>
      <p>You can configure a default storage class, storage profiles, Containerized Data Importer (CDI), data volumes (DVs), and automatic boot source updates.</p>
      <h2 id="storage-configuration-tasks" class="group scroll-mt-20">Storage<a href="#storage-configuration-tasks" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>The following storage configuration tasks are mandatory:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Configure a default storage class</dt>
      <dd class="ml-6 text-muted-foreground"><p>You must configure a default storage class for the cluster. Otherwise, {"{"}{"{"} VirtProductName {"}"}{"}"} cannot automatically import boot source images. <code>DataVolume</code> objects (DVs) and <code>PersistentVolumeClaim</code> objects (PVCs) that do not explicitly specify a storage class remain in the <code>Pending</code> state until you set a default storage class.</p></dd>
      <dt class="font-semibold">Configure storage profiles</dt>
      <dd class="ml-6 text-muted-foreground"><p>You must configure storage profiles if your storage provider is not recognized by CDI. A storage profile provides recommended storage settings based on the associated storage class.</p></dd>
      </dl>
      <p>The following storage configuration tasks are optional:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Reserve additional PVC space for file system overhead</dt>
      <dd class="ml-6 text-muted-foreground"><p>By default, 5.5% of a file system PVC is reserved for overhead, reducing the space available for VM disks by that amount. You can configure a different overhead value.</p></dd>
      <dt class="font-semibold">Configure local storage by using the hostpath provisioner</dt>
      <dd class="ml-6 text-muted-foreground"><p>You can configure local storage for virtual machines by using the hostpath provisioner (HPP). When you install the {"{"}{"{"} VirtProductName {"}"}{"}"} Operator, the HPP Operator is automatically installed.</p></dd>
      <dt class="font-semibold">Configure user permissions to clone data volumes between namespaces</dt>
      <dd class="ml-6 text-muted-foreground"><p>You can configure RBAC roles to enable users to clone data volumes between namespaces.</p></dd>
      </dl>
      <h2 id="cdi-configuration-tasks" class="group scroll-mt-20">Containerized Data Importer<a href="#cdi-configuration-tasks" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can perform the following Containerized Data Importer (CDI) configuration tasks:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Override the resource request limits of a namespace</dt>
      <dd class="ml-6 text-muted-foreground"><p>You can configure CDI to import, upload, and clone VM disks into namespaces that are subject to CPU and memory resource restrictions.</p></dd>
      <dt class="font-semibold">Configure CDI scratch space</dt>
      <dd class="ml-6 text-muted-foreground"><p>CDI requires scratch space (temporary storage) to complete some operations, such as importing and uploading VM images. During this process, CDI provisions a scratch space PVC equal to the size of the PVC backing the destination data volume (DV).</p></dd>
      </dl>
      <h2 id="dv-configuration-tasks" class="group scroll-mt-20">Data volumes<a href="#dv-configuration-tasks" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can perform the following data volume configuration tasks:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Enable preallocation for data volumes</dt>
      <dd class="ml-6 text-muted-foreground"><p>CDI can preallocate disk space to improve write performance when creating data volumes. You can enable preallocation for specific data volumes.</p></dd>
      <dt class="font-semibold">Manage data volume annotations</dt>
      <dd class="ml-6 text-muted-foreground"><p>Data volume annotations allow you to manage pod behavior. You can add one or more annotations to a data volume, which then propagates to the created importer pods.</p></dd>
      </dl>
      <h2 id="boot-source-configuration" class="group scroll-mt-20">Boot source updates<a href="#boot-source-configuration" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can perform the following boot source update configuration task:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Manage automatic boot source updates</dt>
      <dd class="ml-6 text-muted-foreground"><p>Boot sources can make virtual machine (VM) creation more accessible and efficient for users. If automatic boot source updates are enabled, CDI imports, polls, and updates the images so that they are ready to be cloned for new VMs. By default, CDI automatically updates Red Hat boot sources. You can enable automatic updates for custom boot sources.</p></dd>
      </dl>
      <h2 id="additional-resources_virt-storage-config-overview" class="group scroll-mt-20">Additional resources<a href="#additional-resources_virt-storage-config-overview" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/storage/dynamic-provisioning#dynamic-provisioning-defining-storage-class_dynamic-provisioning">Defining a storage class</a></li>
      <li><a href="/virt/storage/virt-configuring-storage-profile#virt-configuring-storage-profile">Configure storage profiles</a></li>
      <li><a href="/virt/storage/virt-reserving-pvc-space-fs-overhead#virt-reserving-pvc-space-fs-overhead">Reserve additional PVC space for file system overhead</a></li>
      <li><a href="/virt/storage/virt-configuring-local-storage-with-hpp#virt-configuring-local-storage-with-hpp">Configure local storage by using the hostpath provisioner</a></li>
      <li><a href="/virt/storage/virt-enabling-user-permissions-to-clone-datavolumes#virt-enabling-user-permissions-to-clone-datavolumes">Configure user permissions to clone data volumes between namespaces</a></li>
      <li><a href="/virt/storage/virt-configuring-cdi-for-namespace-resourcequota#virt-configuring-cdi-for-namespace-resourcequota">Override the resource request limits of a namespace</a></li>
      <li><a href="/virt/storage/virt-preparing-cdi-scratch-space#virt-preparing-cdi-scratch-space">Configure CDI scratch space</a></li>
      <li><a href="/virt/storage/virt-using-preallocation-for-datavolumes#virt-using-preallocation-for-datavolumes">Enable preallocation for data volumes</a></li>
      <li><a href="/virt/storage/virt-managing-data-volume-annotations#virt-managing-data-volume-annotations">Manage data volume annotations</a></li>
      <li><a href="/virt/storage/virt-automatic-bootsource-updates#virt-automatic-bootsource-updates">Manage automatic boot source updates</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
