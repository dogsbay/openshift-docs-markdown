---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/storage/virt-reserving-pvc-space-fs-overhead.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"virt-reserving-pvc-space-fs-overhead","text":"Reserving PVC space for file system overhead"}];
const nav = navData;
const currentPath = "/virt/storage/virt-reserving-pvc-space-fs-overhead";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Reserving PVC space for file system overhead";
const description = "When you create a DataVolume custom resource (CR) for a virtual machine (VM) by setting the spec.storage.volumeMode attribute to Filesystem, {{…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/storage"];
const pageTaxonomies = null;
const tagsIndexPath = "/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Reserving PVC space for file system overhead {#virt-reserving-pvc-space-fs-overhead}\n\nWhen you create a `DataVolume` custom resource (CR) for a virtual machine (VM) by setting the `spec.storage.volumeMode`  attribute to `Filesystem`, {{ VirtProductName }} automatically accounts for file system overhead.\n\nIf you specify the storage type by using the `spec.pvc` attribute in the `DataVolume` CR, {{ VirtProductName }} does not add any file system overhead and the requested size is passed directly to Kubernetes.\n\nThe default file system overhead value is 6%. For example, if you request a 10 GiB disk and the `spec.storage.volumeMode` attribute is set to `FileSystem`, Kubernetes provisions a PVC of approximately 10.6 GiB so that the VM has the full 10 GiB of space available.\n\n**Example file system overhead for data volumes**\n\n| Requested virtual disk size | Calculated overhead (6%) | Total PVC space provisioned |\n| --- | --- | --- |\n| 10 GiB | 0.6 GiB | 10.6 GiB |\n| 100 GiB | 6 GiB | 106 GiB |\n\n> [!NOTE]\n> You can change the default file system overhead value by editing the `HyperConverged` CR.\n","mdUrl":"/virt/storage/virt-reserving-pvc-space-fs-overhead.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={""}
  navMode={"client"}
  pagefindUrl={"/pagefind/"}
  favicon={"/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-reserving-pvc-space-fs-overhead" class="group scroll-mt-20">Reserving PVC space for file system overhead<a href="#virt-reserving-pvc-space-fs-overhead" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="/_attributes/common-attributes.md"></p>
      <p>When you create a <code>DataVolume</code> custom resource (CR) for a virtual machine (VM) by setting the <code>spec.storage.volumeMode</code>  attribute to <code>Filesystem</code>, {"{"}{"{"} VirtProductName {"}"}{"}"} automatically accounts for file system overhead.</p>
      <p>If you specify the storage type by using the <code>spec.pvc</code> attribute in the <code>DataVolume</code> CR, {"{"}{"{"} VirtProductName {"}"}{"}"} does not add any file system overhead and the requested size is passed directly to Kubernetes.</p>
      <p>The default file system overhead value is 6%. For example, if you request a 10 GiB disk and the <code>spec.storage.volumeMode</code> attribute is set to <code>FileSystem</code>, Kubernetes provisions a PVC of approximately 10.6 GiB so that the VM has the full 10 GiB of space available.</p>
      <p><strong>Example file system overhead for data volumes</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Requested virtual disk size</TableHead>
      <TableHead>Calculated overhead (6%)</TableHead>
      <TableHead>Total PVC space provisioned</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>10 GiB</TableCell>
      <TableCell>0.6 GiB</TableCell>
      <TableCell>10.6 GiB</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>100 GiB</TableCell>
      <TableCell>6 GiB</TableCell>
      <TableCell>106 GiB</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>You can change the default file system overhead value by editing the <code>HyperConverged</code> CR.</p>
        </AlertDescription>
      </Alert>
      <p class="/modules/virt-overriding-default-fs-overhead-value.md"></p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
