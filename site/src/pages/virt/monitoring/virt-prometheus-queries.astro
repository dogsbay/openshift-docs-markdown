---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/monitoring/virt-prometheus-queries.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"virt-prometheus-queries","text":"Prometheus queries for virtual resources"},{"depth":2,"slug":"querying-metrics-for-all-projects-with-mon-dashboard_virt-prometheus-queries","text":"Querying metrics for all projects with the OpenShift Container Platform web console"},{"depth":2,"slug":"querying-metrics-for-user-defined-projects-with-mon-dashboard_virt-prometheus-queries","text":"Querying metrics for user-defined projects with the OpenShift Container Platform web console"},{"depth":2,"slug":"virt-querying-metrics_virt-prometheus-queries","text":"Virtualization metrics"},{"depth":3,"slug":"virt-promql-vcpu-metrics_virt-prometheus-queries","text":"vCPU metrics"},{"depth":3,"slug":"virt-promql-network-metrics_virt-prometheus-queries","text":"Network metrics"},{"depth":3,"slug":"virt-promql-storage-metrics_virt-prometheus-queries","text":"Storage metrics"},{"depth":3,"slug":"virt-promql-guest-memory-metrics_virt-prometheus-queries","text":"Guest memory swapping metrics"},{"depth":3,"slug":"virt-promql-aaq-metrics_virt-prometheus-queries","text":"Monitoring AAQ operator metrics"},{"depth":3,"slug":"virt-vm-label-metrics_virt-prometheus-queries","text":"VM label metrics"},{"depth":3,"slug":"virt-live-migration-metrics_virt-prometheus-queries","text":"Live migration metrics"},{"depth":2,"slug":"virt-node-memory-overcommit-dashboard_virt-prometheus-queries","text":"Node Memory Overcommit dashboard"},{"depth":3,"slug":"_dashboard_filters","text":"Dashboard filters"},{"depth":3,"slug":"_summary_section","text":"Summary section"},{"depth":3,"slug":"_cluster_section","text":"Cluster section"},{"depth":3,"slug":"_nodes_section","text":"Nodes section"},{"depth":3,"slug":"_system_reserved_section","text":"System Reserved section"},{"depth":3,"slug":"_workloads_section","text":"Workloads section"},{"depth":3,"slug":"_interpreting_the_dashboard","text":"Interpreting the dashboard"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/monitoring/virt-prometheus-queries";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Prometheus queries for virtual resources";
const description = "Monitor the consumption of cluster infrastructure resources by using the metrics provided by OpenShift Virtualization. These metrics are also used to…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/monitoring"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Prometheus queries for virtual resources {#virt-prometheus-queries}\n\nMonitor the consumption of cluster infrastructure resources by using the metrics provided by OpenShift Virtualization. These metrics are also used to query live migration status.\n\n> [!NOTE]\n> - To use the vCPU metric, apply the `schedstats=enable` kernel argument to the `MachineConfig` object. This kernel argument enables scheduler statistics used for debugging and performance tuning and adds a minor additional load to the scheduler.\n> - For guest memory swapping queries to return data, enable memory swapping on the virtual guests.\n\n## Querying metrics for all projects with the OpenShift Container Platform web console {#querying-metrics-for-all-projects-with-mon-dashboard_virt-prometheus-queries}\n\nMonitor the state of a cluster and any user-defined workloads by using the OpenShift Container Platform metrics query browser. The query browser uses Prometheus Query Language (PromQL) queries to examine metrics visualized on a plot.\n\nAs a cluster administrator or as a user with view permissions for all projects, you can access metrics for all default OpenShift Container Platform and user-defined projects in the Metrics UI.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` cluster role or with view permissions for all projects.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, click **Observe** → **Metrics**.\n2. To add one or more queries, perform any of the following actions:\n\n   | Option | Description |\n   | --- | --- |\n   | Select an existing query. | From the **Select query** drop-down list, select an existing query. |\n   | Create a custom query. | Add your Prometheus Query Language (PromQL) query to the **Expression** field.<br>As you type a PromQL expression, autocomplete suggestions appear in a drop-down list. These suggestions include functions, metrics, labels, and time tokens. Use the keyboard arrows to select one of these suggested items and then press Enter to add the item to your expression. Move your mouse pointer over a suggested item to view a brief description of that item. |\n   | Add multiple queries. | Click **Add query**. |\n   | Duplicate an existing query. | Click the options menu ![](/openshift-docs-markdown/_assets/images/kebab.png \"Options menu\") next to the query, then choose **Duplicate query**. |\n   | Disable a query from being run. | Click the options menu ![](/openshift-docs-markdown/_assets/images/kebab.png \"Options menu\") next to the query and choose **Disable query**. |\n3. To run queries that you created, click **Run queries**. The metrics from the queries are visualized on the plot. If a query is invalid, the UI shows an error message.\n\n   > [!NOTE]\n   > - When drawing time series graphs, queries that operate on large amounts of data might time out or overload the browser. To avoid this, click **Hide graph** and calibrate your query by using only the metrics table. Then, after finding a feasible query, enable the plot to draw the graphs.\n   > - By default, the query table shows an expanded view that lists every metric and its current value. Click the **˅** down arrowhead to minimize the expanded view for a query.\n4. Optional: Save the page URL to use this set of queries again in the future.\n5. Explore the visualized metrics. Initially, all metrics from all enabled queries are shown on the plot. Select which metrics are shown by performing any of the following actions:\n\n<table>\n<thead>\n<tr>\n  <th>Option</th>\n  <th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td>Hide all metrics from a query.</td>\n  <td>Click the options menu <img src=\"/openshift-docs-markdown/_assets/images/kebab.png\" alt=\"\" title=\"Options menu\"> for the query and click <strong>Hide all series</strong>.</td>\n</tr>\n<tr>\n  <td>Hide a specific metric.</td>\n  <td>Go to the query table and click the colored square near the metric name.</td>\n</tr>\n<tr>\n  <td>Zoom into the plot and change the time range.</td>\n  <td>Perform one of the following actions:<br><br><ul><li>Visually select the time range by clicking and dragging on the plot horizontally.</li><li>Use the menu to select the time range.</li></ul></td>\n</tr>\n<tr>\n  <td>Reset the time range.</td>\n  <td>Click <strong>Reset zoom</strong>.</td>\n</tr>\n<tr>\n  <td>Display outputs for all queries at a specific point in time.</td>\n  <td>Hover over the plot at the point you are interested in. The query outputs appear in a pop-up box.</td>\n</tr>\n<tr>\n  <td>Hide the plot.</td>\n  <td>Click <strong>Hide graph</strong>.</td>\n</tr>\n</tbody>\n</table>\n\n## Querying metrics for user-defined projects with the OpenShift Container Platform web console {#querying-metrics-for-user-defined-projects-with-mon-dashboard_virt-prometheus-queries}\n\nMonitor user-defined workloads by using the OpenShift Container Platform metrics query browser. The query browser uses Prometheus Query Language (PromQL) queries to examine metrics visualized on a plot.\n\nAs a developer, you must specify a project name when querying metrics. You must have the required privileges to view metrics for the selected project.\n\n**Prerequisites**\n\n- You have access to the cluster as a developer or as a user with view permissions for the project that you are viewing metrics for.\n- You have enabled monitoring for user-defined projects.\n- You have deployed a service in a user-defined project.\n- You have created a `ServiceMonitor` custom resource definition (CRD) for the service to define how the service is monitored.\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, click **Observe** → **Metrics**.\n2. To add one or more queries, perform any of the following actions:\n\n   | Option | Description |\n   | --- | --- |\n   | Select an existing query. | From the **Select query** drop-down list, select an existing query. |\n   | Create a custom query. | Add your Prometheus Query Language (PromQL) query to the **Expression** field.<br>As you type a PromQL expression, autocomplete suggestions appear in a drop-down list. These suggestions include functions, metrics, labels, and time tokens. Use the keyboard arrows to select one of these suggested items and then press Enter to add the item to your expression. Move your mouse pointer over a suggested item to view a brief description of that item. |\n   | Add multiple queries. | Click **Add query**. |\n   | Duplicate an existing query. | Click the options menu ![](/openshift-docs-markdown/_assets/images/kebab.png \"Options menu\") next to the query, then choose **Duplicate query**. |\n   | Disable a query from being run. | Click the options menu ![](/openshift-docs-markdown/_assets/images/kebab.png \"Options menu\") next to the query and choose **Disable query**. |\n3. To run queries that you created, click **Run queries**. The metrics from the queries are visualized on the plot. If a query is invalid, the UI shows an error message.\n\n   > [!NOTE]\n   > - When drawing time series graphs, queries that operate on large amounts of data might time out or overload the browser. To avoid this, click **Hide graph** and calibrate your query by using only the metrics table. Then, after finding a feasible query, enable the plot to draw the graphs.\n   > - By default, the query table shows an expanded view that lists every metric and its current value. Click the **˅** down arrowhead to minimize the expanded view for a query.\n4. Optional: Save the page URL to use this set of queries again in the future.\n5. Explore the visualized metrics. Initially, all metrics from all enabled queries are shown on the plot. Select which metrics are shown by performing any of the following actions:\n\n<table>\n<thead>\n<tr>\n  <th>Option</th>\n  <th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td>Hide all metrics from a query.</td>\n  <td>Click the options menu <img src=\"/openshift-docs-markdown/_assets/images/kebab.png\" alt=\"\" title=\"Options menu\"> for the query and click <strong>Hide all series</strong>.</td>\n</tr>\n<tr>\n  <td>Hide a specific metric.</td>\n  <td>Go to the query table and click the colored square near the metric name.</td>\n</tr>\n<tr>\n  <td>Zoom into the plot and change the time range.</td>\n  <td>Perform one of the following actions:<br><br><ul><li>Visually select the time range by clicking and dragging on the plot horizontally.</li><li>Use the menu to select the time range.</li></ul></td>\n</tr>\n<tr>\n  <td>Reset the time range.</td>\n  <td>Click <strong>Reset zoom</strong>.</td>\n</tr>\n<tr>\n  <td>Display outputs for all queries at a specific point in time.</td>\n  <td>Hover over the plot at the point you are interested in. The query outputs appear in a pop-up box.</td>\n</tr>\n<tr>\n  <td>Hide the plot.</td>\n  <td>Click <strong>Hide graph</strong>.</td>\n</tr>\n</tbody>\n</table>\n\n## Virtualization metrics {#virt-querying-metrics_virt-prometheus-queries}\n\nMetric descriptions including example Prometheus Query Language (PromQL) queries. These metrics are not an API and might change between versions.\n\n> [!NOTE]\n> The following examples use `topk` queries that specify a time period. If virtual machines (VMs) are deleted during that time period, they can still appear in the query output.\n\n### vCPU metrics {#virt-promql-vcpu-metrics_virt-prometheus-queries}\n\nThe following query can identify virtual machines that are waiting for Input/Output (I/O):\n\n`kubevirt_vmi_vcpu_wait_seconds_total`\n:   Returns the wait time (in seconds) on I/O for vCPUs of a virtual machine. Type: Counter.\n\nA value above '0' means that the vCPU wants to run, but the host scheduler cannot run it yet. This inability to run indicates that there is an issue with I/O.\n\n> [!NOTE]\n> To query the vCPU metric, the `schedstats=enable` kernel argument must first be applied to the `MachineConfig` object. This kernel argument enables scheduler statistics used for debugging and performance tuning and adds a minor additional load to the scheduler.\n\n`kubevirt_vmi_vcpu_delay_seconds_total`\n:   Returns the cumulative time, in seconds, that a vCPU was enqueued by the host scheduler but could not run immediately. This delay appears to the virtual machine as *steal time*, which is CPU time lost when the host runs other workloads. Steal time can impact performance and often indicates CPU overcommitment or contention on the host. Type: Counter.\n\nExample vCPU delay query:\n\nThe following query returns the average per-second delay over a 5-minute period. A high value may indicate CPU overcommitment or contention on the node:\n\n```promql\nirate(kubevirt_vmi_vcpu_delay_seconds_total[5m]) > 0.05\n```\n\nExample vCPU wait time query:\n\nThe following query returns the top 3 VMs waiting for I/O at every given moment over a six-minute time period:\n\n```promql\ntopk(3, sum by (name, namespace) (rate(kubevirt_vmi_vcpu_wait_seconds_total[6m]))) > 0\n```\n\n### Network metrics {#virt-promql-network-metrics_virt-prometheus-queries}\n\nThe following queries can identify virtual machines that are saturating the network:\n\n`kubevirt_vmi_network_receive_bytes_total`\n:   Returns the total amount of traffic received (in bytes) on the virtual machine’s network. Type: Counter.\n\n`kubevirt_vmi_network_transmit_bytes_total`\n:   Returns the total amount of traffic transmitted (in bytes) on the virtual machine’s network. Type: Counter.\n\nExample network traffic query:\n\nThe following query returns the top 3 VMs transmitting the most network traffic at every given moment over a six-minute time period:\n\n```promql\ntopk(3, sum by (name, namespace) (rate(kubevirt_vmi_network_receive_bytes_total[6m])) + sum by (name, namespace) (rate(kubevirt_vmi_network_transmit_bytes_total[6m]))) > 0\n```\n\n### Storage metrics {#virt-promql-storage-metrics_virt-prometheus-queries}\n\nYou can monitor virtual machine storage traffic and identify high-traffic VMs by using Prometheus queries.\n\nThe following queries can identify VMs that are writing large amounts of data:\n\n`kubevirt_vmi_storage_read_traffic_bytes_total`\n:   Returns the total amount (in bytes) of the virtual machine’s storage-related traffic. Type: Counter.\n\n`kubevirt_vmi_storage_write_traffic_bytes_total`\n:   Returns the total amount of storage writes (in bytes) of the virtual machine’s storage-related traffic. Type: Counter.\n\nExample storage-related traffic queries:\n\n- The following query returns the top 3 VMs performing the most storage traffic at every given moment over a six-minute time period:\n\n  ```promql\n  topk(3, sum by (name, namespace) (rate(kubevirt_vmi_storage_read_traffic_bytes_total[6m])) + sum by (name, namespace) (rate(kubevirt_vmi_storage_write_traffic_bytes_total[6m]))) > 0\n  ```\n- The following query returns the top 3 VMs with the highest average read latency at every given moment over a six-minute time period:\n\n  ```promql\n  topk(3, sum by (name, namespace) (rate(kubevirt_vmi_storage_read_times_seconds_total{name='${name}',namespace='${namespace}'${clusterFilter}}[6m]) / rate(kubevirt_vmi_storage_iops_read_total{name='${name}',namespace='${namespace}'${clusterFilter}}[6m]) > 0)) > 0\n  ```\n\nThe following queries can track data restored from storage snapshots:\n\n`vm:kubevirt_vmsnapshot_disks_restored:sum`\n:   Returns the total number of virtual machine disks restored from the source virtual machine. Type: Gauge.\n\n`vm:kubevirt_vmsnapshot_restored_bytes:sum`\n:   Returns the amount of space in bytes restored from the source virtual machine. Type: Gauge.\n\nExamples of storage snapshot data queries:\n\n- The following query returns the total number of virtual machine disks restored from the source virtual machine:\n\n  ```promql\n  vm:kubevirt_vmsnapshot_disks_restored:sum{vm_name=\"simple-vm\", vm_namespace=\"default\"}\n  ```\n- The following query returns the amount of space in bytes restored from the source virtual machine:\n\n  ```promql\n  vm:kubevirt_vmsnapshot_restored_bytes:sum{vm_name=\"simple-vm\", vm_namespace=\"default\"}\n  ```\n\nThe following queries can determine the I/O performance of storage devices:\n\n`kubevirt_vmi_storage_iops_read_total`\n:   Returns the amount of read I/O operations the virtual machine is performing per second. Type: Counter.\n\n`kubevirt_vmi_storage_iops_write_total`\n:   Returns the amount of write I/O operations the virtual machine is performing per second. Type: Counter.\n\nExample I/O performance query:\n\nThe following query returns the top 3 VMs performing the most I/O operations per second at every given moment over a six-minute time period:\n\n```promql\ntopk(3, sum by (name, namespace) (rate(kubevirt_vmi_storage_iops_read_total[6m])) + sum by (name, namespace) (rate(kubevirt_vmi_storage_iops_write_total[6m]))) > 0\n```\n\n### Guest memory swapping metrics {#virt-promql-guest-memory-metrics_virt-prometheus-queries}\n\nThe following queries can identify which swap-enabled guests are performing the most memory swapping:\n\n`kubevirt_vmi_memory_swap_in_traffic_bytes`\n:   Returns the total amount (in bytes) of memory the virtual guest is swapping in. Type: Gauge.\n\n`kubevirt_vmi_memory_swap_out_traffic_bytes`\n:   Returns the total amount (in bytes) of memory the virtual guest is swapping out. Type: Gauge.\n\nExample memory swapping query:\n\nThe following query returns the top 3 VMs where the guest is performing the most memory swapping at every given moment over a six-minute time period:\n\n```promql\ntopk(3, sum by (name, namespace) (rate(kubevirt_vmi_memory_swap_in_traffic_bytes[6m])) + sum by (name, namespace) (rate(kubevirt_vmi_memory_swap_out_traffic_bytes[6m]))) > 0\n```\n\n> [!NOTE]\n> Memory swapping indicates that the virtual machine is under memory pressure. Increasing the memory allocation of the virtual machine can mitigate this issue.\n\n### Monitoring AAQ operator metrics {#virt-promql-aaq-metrics_virt-prometheus-queries}\n\nThe following metrics are exposed by the Application Aware Quota (AAQ) controller for monitoring resource quotas:\n\n`kube_application_aware_resourcequota`\n:   Returns the current quota usage and the CPU and memory limits enforced by the AAQ Operator resources. Type: Gauge.\n\n`kube_application_aware_resourcequota_creation_timestamp`\n:   Returns the time, in UNIX timestamp format, when the AAQ Operator resource is created. Type: Gauge.\n\n### VM label metrics {#virt-vm-label-metrics_virt-prometheus-queries}\n\n`kubevirt_vm_labels`\n:   Returns virtual machine labels as Prometheus labels. Type: Gauge.\n\n    You can expose and ignore specific labels by editing the `kubevirt-vm-labels-config` config map. After you apply the config map to your cluster, the configuration is loaded dynamically.\n\n    Example config map:\n\n    ```yaml\n    apiVersion: v1\n    kind: ConfigMap\n    metadata:\n      name: kubevirt-vm-labels-config\n      namespace: openshift-cnv\n    data:\n      allowlist: \"*\"\n      ignorelist: \"\"\n    ```\n\n    - `data.allowlist` specifies labels to expose.\n\n      - If `data.allowlist` has a value of `\"*\"`, all labels are included.\n      - If `data.allowlist` has a value of `\"\"`, the metric does not return any labels.\n      - If `data.allowlist` contains a list of label keys, only the explicitly named labels are exposed. For example: `allowlist: \"example.io/name,example.io/version\"`.\n    - `data.ignorelist` specifies labels to ignore. The ignore list overrides the allow list.\n\n      - The `data.ignorelist` field does not support wildcard patterns. It can be empty or include a list of specific labels to ignore.\n      - If `data.ignorelist` has a value of `\"\"`, no labels are ignored.\n\n### Live migration metrics {#virt-live-migration-metrics_virt-prometheus-queries}\n\nYou can query metrics to show live migration status.\n\n`kubevirt_vmi_migration_data_processed_bytes`\n:   The amount of guest operating system data that has migrated to the new virtual machine (VM). Type: Gauge.\n\n`kubevirt_vmi_migration_data_remaining_bytes`\n:   The amount of guest operating system data that remains to be migrated. Type: Gauge.\n\n`kubevirt_vmi_migration_memory_transfer_rate_bytes`\n:   The rate at which memory is becoming dirty in the guest operating system. Dirty memory is data that has been changed but not yet written to disk. Type: Gauge.\n\n`kubevirt_vmi_migrations_in_pending_phase`\n:   The number of pending migrations. Type: Gauge.\n\n`kubevirt_vmi_migrations_in_scheduling_phase`\n:   The number of scheduling migrations. Type: Gauge.\n\n`kubevirt_vmi_migrations_in_running_phase`\n:   The number of running migrations. Type: Gauge.\n\n`kubevirt_vmi_migration_succeeded`\n:   The number of successfully completed migrations. Type: Gauge.\n\n`kubevirt_vmi_migration_failed`\n:   The number of failed migrations. Type: Gauge.\n\n## Node Memory Overcommit dashboard {#virt-node-memory-overcommit-dashboard_virt-prometheus-queries}\n\nThe Node Memory Overcommit dashboard displays physical and virtual memory utilization across the cluster, focusing on how virtual machines (VMs) affect node memory.\n\nUse this dashboard to monitor memory capacity, detect memory pressure, identify overcommitment risks, and validate that system processes do not exceed their reserved memory.\n\nYou can access this dashboard from the web console in **Observe** → **Dashboards (Perses)**.\n\n### Dashboard filters {#_dashboard_filters}\n\nThe dashboard provides two filter variables in the top toolbar:\n\n| Filter | Description | Default |\n| --- | --- | --- |\n| **Node** | Filters all panels to display information about one or more nodes in the cluster. | **All** |\n| **role** | Filters all panels to display information about nodes with a specific Kubernetes role, populated from `kube_node_role`. | **worker** |\n\nYou can also change the time range displayed in the different panels in the top toolbar of the dashboard.\n\n### Summary section {#_summary_section}\n\nThe **Summary** section provides a high-level overview of cluster memory health. The panels in this section provide general memory utilization data for the cluster and the nodes. Most of the panels in this section appear in other sections of the dashboard as well. However, the following panels are unique to the **Summary** section:\n\n| Panel | Type | Description |\n| --- | --- | --- |\n| **Node Utilization - max** | Gauge | Displays the highest node utilization in the cluster. Use this panel together with the **Node Utilization - min** panel to assess scheduling balance. A large gap between minimum and maximum node utilization indicates an imbalanced workload distribution. |\n| **Cluster Utilization** | Time series | Displays the trend over time of several memory metrics. Use this panel to compare actual utilization and virtual memory plans against total capacity. |\n\n### Cluster section {#_cluster_section}\n\nThe **Cluster** section provides a more detailed view of cluster-wide memory behavior over a range of time. This section includes the following panels:\n\n| Panel | Type | Description |\n| --- | --- | --- |\n| **Physical Memory Utilization & Requests** | Time series | Displays the total node memory capacity alongside actual memory utilization split into virtualization and non-virtualization workloads. This panel also shows the memory request plan (system-reserved requests and pod requests) so that you can compare actual usage to the scheduler expectations. |\n| **Cluster Utilization** | Gauge | Displays the aggregated memory utilization of the nodes in the cluster. This panel also appears in the **Summary** section. |\n| **Virtual Memory Assignment** | Time series | This panel displays the worst-case scenario if all virtual memory is used at present. It shows total node capacity, utilization without virtualization, and non-virtualization utilization combined with the total assigned VM memory. If the **VM assigned virtual memory** line nears or exceeds **Node capacity**, the cluster risks out-of-memory conditions under full VM memory pressure. |\n| **Cluster Virtual Committed** | Gauge | Displays the percentage of committed virtual memory out of all of the allocatable physical memory. This panel also appears in the **Summary** section. |\n| **Cluster - Memory Pressure** | Time series | Shows cluster-level memory PSI rates for `Waiting` (processes delayed by memory) and `Stalled` (processes completely blocked). |\n| **Cluster - Aggregated Swap** | Time series | Shows total swap capacity and usage across all the nodes that you select. Rising swap usage indicates memory pressure that has not yet caused PSI stalls. |\n\n### Nodes section {#_nodes_section}\n\nThe **Nodes** section breaks down memory information per node to help identify imbalances. This section includes the following panels:\n\n| Panel | Type | Description |\n| --- | --- | --- |\n| **Utilization - Actual Overcommit Level** | Time series | Displays the per-node allocatable memory utilization minus the system-reserved memory. |\n| **Node Utilization - min** | Gauge | Displays the lowest node utilization in the cluster. This panel also appears in the **Summary** section. |\n| **Plan - Pod Requests per Node** | Time series | Shows the memory request fill level per node: the sum of all pod memory requests on a node divided by the node’s allocatable memory. |\n| **Node Requests - min/max** | Stat | Displays the minimum and maximum pod request ratios per node. Use this panel to quickly assess the cluster’s remaining capacity to host new workloads. |\n| **Plan - Virtual Memory Commit Level** | Time series | Displays the virtual memory commit ratio per node. The panel compares total active and assigned VM memory against the node’s available memory. |\n| **Node Virtual - min/max** | Stat | Displays the minimum and maximum virtual commit levels per node. |\n| **Node - Pressure** | Time series | Displays the memory PSI waiting rate per node. Each line represents one node to help you identify which nodes experience memory contention. |\n| **Node PSI - max** | Gauge | Displays the highest PSI value across all nodes. PSI shows the amount of time applications are stalled or delayed waiting for memory resources. This panel also appears in the **Summary** section. |\n\n### System Reserved section {#_system_reserved_section}\n\nThe **System Reserved** section monitors whether the system (hypervisor) processes stay within their reserved memory budget. This section includes the following panels:\n\n| Panel | Type | Description |\n| --- | --- | --- |\n| **Utilization - Reserved System Memory** | Time series | Displays the top 5 nodes by system-reserved memory utilization. This metric compares active system process memory against the reserved memory budget. |\n| **Utilization - min/max** | Time series | Displays the minimum and maximum system-reserved memory utilization across all nodes over time. |\n| **System Exceeding Reservation** | Stat | Displays the percentage or number of nodes out of all monitored nodes that are currently triggering the `SystemMemoryExceedsReservation` alert. This panel also appears in the **Summary** section. |\n\n### Workloads section {#_workloads_section}\n\nThe **Workloads** section, which is collapsed by default, focuses on individual VM memory behavior.\n\n| Panel | Type | Description |\n| --- | --- | --- |\n| **VM Overcommit Ratio** | Time series | Displays the ratio of assigned virtual memory to pod memory requests for each VM. |\n| **VM Virtual Committed** | Gauge | Displays the average VM overcommit ratio, which includes launcher overhead. This panel also appears in the **Summary** section. |\n| **VM Virtual Memory Utilization vs Host VM Utilization** | Time series | Displays the 10 VMs with the highest ratio between guest-reported memory usage and host-side container memory usage. Use this panel to identify VMs where guest-reported usage differs significantly from host-side accounting. This difference indicates balloon driver effectiveness or memory accounting discrepancies. |\n| **Number of Running VMs** | Time series | Displays the total number of running virtual machine instances (VMIs) on the cluster to provide context for the other workload panels. |\n\n### Interpreting the dashboard {#_interpreting_the_dashboard}\n\nMonitor the dashboard indicators in the **Summary** section to identify early warning signs of memory pressure and prevent critical out-of-memory events.\n\nHealthy state\n:   The **Cluster Utilization** panel gauge is green (below 70%), the **Cluster Virtual Committed** panel gauge is below 120%, the **Node PSI - max** values are near zero, and no nodes exceed their system reservation.\n\nWarning signs\n:   Utilization gauges turn amber (80% to 90%), the virtual commit approaches 150%, or individual nodes diverge significantly from the cluster average, which suggests imbalanced scheduling.\n\nCritical state\n:   Utilization gauges turn red (above 90%), PSI values exceed 0.5, system reservation is exceeded on any node, or virtual commit ratios per node exceed 200%. These conditions indicate that the cluster is at risk of out-of-memory events and VM eviction.\n\n**Additional resources**\n{._additional-resources}\n\n- [KubeVirt components metrics](https://github.com/kubevirt/monitoring/blob/main/docs/metrics.md)\n- [Adding kernel arguments to nodes](/openshift-docs-markdown/machine_configuration/machine-configs-configure#nodes-nodes-kernel-arguments_machine-configs-configure)\n- [About OpenShift Container Platform monitoring](https://docs.redhat.com/en/documentation/monitoring_stack_for_red_hat_openshift/latest/html/about_monitoring/about-ocp-monitoring)\n- [Querying Prometheus](https://prometheus.io/docs/prometheus/latest/querying/basics/)\n- [Prometheus query examples](https://prometheus.io/docs/prometheus/latest/querying/examples/)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/monitoring/virt-prometheus-queries.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-prometheus-queries" class="group scroll-mt-20">Prometheus queries for virtual resources<a href="#virt-prometheus-queries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Monitor the consumption of cluster infrastructure resources by using the metrics provided by OpenShift Virtualization. These metrics are also used to query live migration status.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <ul>
          <li>To use the vCPU metric, apply the <code>schedstats=enable</code> kernel argument to the <code>MachineConfig</code> object. This kernel argument enables scheduler statistics used for debugging and performance tuning and adds a minor additional load to the scheduler.</li>
          <li>For guest memory swapping queries to return data, enable memory swapping on the virtual guests.</li>
          </ul>
        </AlertDescription>
      </Alert>
      <h2 id="querying-metrics-for-all-projects-with-mon-dashboard_virt-prometheus-queries" class="group scroll-mt-20">Querying metrics for all projects with the OpenShift Container Platform web console<a href="#querying-metrics-for-all-projects-with-mon-dashboard_virt-prometheus-queries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Monitor the state of a cluster and any user-defined workloads by using the OpenShift Container Platform metrics query browser. The query browser uses Prometheus Query Language (PromQL) queries to examine metrics visualized on a plot.</p>
      <p>As a cluster administrator or as a user with view permissions for all projects, you can access metrics for all default OpenShift Container Platform and user-defined projects in the Metrics UI.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> cluster role or with view permissions for all projects.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, click <strong>Observe</strong> → <strong>Metrics</strong>.</li>
      <li>
      To add one or more queries, perform any of the following actions:
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Option</TableHead>
      <TableHead>Description</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>Select an existing query.</TableCell>
      <TableCell>From the <strong>Select query</strong> drop-down list, select an existing query.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Create a custom query.</TableCell>
      <TableCell>Add your Prometheus Query Language (PromQL) query to the <strong>Expression</strong> field.<Fragment set:html={"<br>"} />As you type a PromQL expression, autocomplete suggestions appear in a drop-down list. These suggestions include functions, metrics, labels, and time tokens. Use the keyboard arrows to select one of these suggested items and then press Enter to add the item to your expression. Move your mouse pointer over a suggested item to view a brief description of that item.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Add multiple queries.</TableCell>
      <TableCell>Click <strong>Add query</strong>.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Duplicate an existing query.</TableCell>
      <TableCell>Click the options menu <img src="/openshift-docs-markdown/_assets/images/kebab.png" alt="" loading="lazy" /> next to the query, then choose <strong>Duplicate query</strong>.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Disable a query from being run.</TableCell>
      <TableCell>Click the options menu <img src="/openshift-docs-markdown/_assets/images/kebab.png" alt="" loading="lazy" /> next to the query and choose <strong>Disable query</strong>.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      </li>
      <li>
      To run queries that you created, click <strong>Run queries</strong>. The metrics from the queries are visualized on the plot. If a query is invalid, the UI shows an error message.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <ul>
          <li>When drawing time series graphs, queries that operate on large amounts of data might time out or overload the browser. To avoid this, click <strong>Hide graph</strong> and calibrate your query by using only the metrics table. Then, after finding a feasible query, enable the plot to draw the graphs.</li>
          <li>By default, the query table shows an expanded view that lists every metric and its current value. Click the <strong>˅</strong> down arrowhead to minimize the expanded view for a query.</li>
          </ul>
        </AlertDescription>
      </Alert>
      </li>
      <li>Optional: Save the page URL to use this set of queries again in the future.</li>
      <li>Explore the visualized metrics. Initially, all metrics from all enabled queries are shown on the plot. Select which metrics are shown by performing any of the following actions:</li>
      </ol>
      <Fragment set:html={"<table>\n<thead>\n<tr>\n  <th>Option</th>\n  <th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td>Hide all metrics from a query.</td>\n  <td>Click the options menu <img src=\"/openshift-docs-markdown/_assets/images/kebab.png\" alt=\"\" title=\"Options menu\"> for the query and click <strong>Hide all series</strong>.</td>\n</tr>\n<tr>\n  <td>Hide a specific metric.</td>\n  <td>Go to the query table and click the colored square near the metric name.</td>\n</tr>\n<tr>\n  <td>Zoom into the plot and change the time range.</td>\n  <td>Perform one of the following actions:<br><br><ul><li>Visually select the time range by clicking and dragging on the plot horizontally.</li><li>Use the menu to select the time range.</li></ul></td>\n</tr>\n<tr>\n  <td>Reset the time range.</td>\n  <td>Click <strong>Reset zoom</strong>.</td>\n</tr>\n<tr>\n  <td>Display outputs for all queries at a specific point in time.</td>\n  <td>Hover over the plot at the point you are interested in. The query outputs appear in a pop-up box.</td>\n</tr>\n<tr>\n  <td>Hide the plot.</td>\n  <td>Click <strong>Hide graph</strong>.</td>\n</tr>\n</tbody>\n</table>\n"} />
      <h2 id="querying-metrics-for-user-defined-projects-with-mon-dashboard_virt-prometheus-queries" class="group scroll-mt-20">Querying metrics for user-defined projects with the OpenShift Container Platform web console<a href="#querying-metrics-for-user-defined-projects-with-mon-dashboard_virt-prometheus-queries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Monitor user-defined workloads by using the OpenShift Container Platform metrics query browser. The query browser uses Prometheus Query Language (PromQL) queries to examine metrics visualized on a plot.</p>
      <p>As a developer, you must specify a project name when querying metrics. You must have the required privileges to view metrics for the selected project.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a developer or as a user with view permissions for the project that you are viewing metrics for.</li>
      <li>You have enabled monitoring for user-defined projects.</li>
      <li>You have deployed a service in a user-defined project.</li>
      <li>You have created a <code>ServiceMonitor</code> custom resource definition (CRD) for the service to define how the service is monitored.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, click <strong>Observe</strong> → <strong>Metrics</strong>.</li>
      <li>
      To add one or more queries, perform any of the following actions:
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Option</TableHead>
      <TableHead>Description</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>Select an existing query.</TableCell>
      <TableCell>From the <strong>Select query</strong> drop-down list, select an existing query.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Create a custom query.</TableCell>
      <TableCell>Add your Prometheus Query Language (PromQL) query to the <strong>Expression</strong> field.<Fragment set:html={"<br>"} />As you type a PromQL expression, autocomplete suggestions appear in a drop-down list. These suggestions include functions, metrics, labels, and time tokens. Use the keyboard arrows to select one of these suggested items and then press Enter to add the item to your expression. Move your mouse pointer over a suggested item to view a brief description of that item.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Add multiple queries.</TableCell>
      <TableCell>Click <strong>Add query</strong>.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Duplicate an existing query.</TableCell>
      <TableCell>Click the options menu <img src="/openshift-docs-markdown/_assets/images/kebab.png" alt="" loading="lazy" /> next to the query, then choose <strong>Duplicate query</strong>.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Disable a query from being run.</TableCell>
      <TableCell>Click the options menu <img src="/openshift-docs-markdown/_assets/images/kebab.png" alt="" loading="lazy" /> next to the query and choose <strong>Disable query</strong>.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      </li>
      <li>
      To run queries that you created, click <strong>Run queries</strong>. The metrics from the queries are visualized on the plot. If a query is invalid, the UI shows an error message.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <ul>
          <li>When drawing time series graphs, queries that operate on large amounts of data might time out or overload the browser. To avoid this, click <strong>Hide graph</strong> and calibrate your query by using only the metrics table. Then, after finding a feasible query, enable the plot to draw the graphs.</li>
          <li>By default, the query table shows an expanded view that lists every metric and its current value. Click the <strong>˅</strong> down arrowhead to minimize the expanded view for a query.</li>
          </ul>
        </AlertDescription>
      </Alert>
      </li>
      <li>Optional: Save the page URL to use this set of queries again in the future.</li>
      <li>Explore the visualized metrics. Initially, all metrics from all enabled queries are shown on the plot. Select which metrics are shown by performing any of the following actions:</li>
      </ol>
      <Fragment set:html={"<table>\n<thead>\n<tr>\n  <th>Option</th>\n  <th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td>Hide all metrics from a query.</td>\n  <td>Click the options menu <img src=\"/openshift-docs-markdown/_assets/images/kebab.png\" alt=\"\" title=\"Options menu\"> for the query and click <strong>Hide all series</strong>.</td>\n</tr>\n<tr>\n  <td>Hide a specific metric.</td>\n  <td>Go to the query table and click the colored square near the metric name.</td>\n</tr>\n<tr>\n  <td>Zoom into the plot and change the time range.</td>\n  <td>Perform one of the following actions:<br><br><ul><li>Visually select the time range by clicking and dragging on the plot horizontally.</li><li>Use the menu to select the time range.</li></ul></td>\n</tr>\n<tr>\n  <td>Reset the time range.</td>\n  <td>Click <strong>Reset zoom</strong>.</td>\n</tr>\n<tr>\n  <td>Display outputs for all queries at a specific point in time.</td>\n  <td>Hover over the plot at the point you are interested in. The query outputs appear in a pop-up box.</td>\n</tr>\n<tr>\n  <td>Hide the plot.</td>\n  <td>Click <strong>Hide graph</strong>.</td>\n</tr>\n</tbody>\n</table>\n"} />
      <h2 id="virt-querying-metrics_virt-prometheus-queries" class="group scroll-mt-20">Virtualization metrics<a href="#virt-querying-metrics_virt-prometheus-queries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Metric descriptions including example Prometheus Query Language (PromQL) queries. These metrics are not an API and might change between versions.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The following examples use <code>topk</code> queries that specify a time period. If virtual machines (VMs) are deleted during that time period, they can still appear in the query output.</p>
        </AlertDescription>
      </Alert>
      <h3 id="virt-promql-vcpu-metrics_virt-prometheus-queries" class="group scroll-mt-20">vCPU metrics<a href="#virt-promql-vcpu-metrics_virt-prometheus-queries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The following query can identify virtual machines that are waiting for Input/Output (I/O):</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>kubevirt_vmi_vcpu_wait_seconds_total</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Returns the wait time (in seconds) on I/O for vCPUs of a virtual machine. Type: Counter.</p></dd>
      </dl>
      <p>A value above '0' means that the vCPU wants to run, but the host scheduler cannot run it yet. This inability to run indicates that there is an issue with I/O.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>To query the vCPU metric, the <code>schedstats=enable</code> kernel argument must first be applied to the <code>MachineConfig</code> object. This kernel argument enables scheduler statistics used for debugging and performance tuning and adds a minor additional load to the scheduler.</p>
        </AlertDescription>
      </Alert>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>kubevirt_vmi_vcpu_delay_seconds_total</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Returns the cumulative time, in seconds, that a vCPU was enqueued by the host scheduler but could not run immediately. This delay appears to the virtual machine as <em>steal time</em>, which is CPU time lost when the host runs other workloads. Steal time can impact performance and often indicates CPU overcommitment or contention on the host. Type: Counter.</p></dd>
      </dl>
      <p>Example vCPU delay query:</p>
      <p>The following query returns the average per-second delay over a 5-minute period. A high value may indicate CPU overcommitment or contention on the node:</p>
      <CodeBlock code={"irate(kubevirt_vmi_vcpu_delay_seconds_total[5m]) > 0.05"} lang="promql" />
      <p>Example vCPU wait time query:</p>
      <p>The following query returns the top 3 VMs waiting for I/O at every given moment over a six-minute time period:</p>
      <CodeBlock code={"topk(3, sum by (name, namespace) (rate(kubevirt_vmi_vcpu_wait_seconds_total[6m]))) > 0"} lang="promql" />
      <h3 id="virt-promql-network-metrics_virt-prometheus-queries" class="group scroll-mt-20">Network metrics<a href="#virt-promql-network-metrics_virt-prometheus-queries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The following queries can identify virtual machines that are saturating the network:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>kubevirt_vmi_network_receive_bytes_total</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Returns the total amount of traffic received (in bytes) on the virtual machine’s network. Type: Counter.</p></dd>
      <dt class="font-semibold"><code>kubevirt_vmi_network_transmit_bytes_total</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Returns the total amount of traffic transmitted (in bytes) on the virtual machine’s network. Type: Counter.</p></dd>
      </dl>
      <p>Example network traffic query:</p>
      <p>The following query returns the top 3 VMs transmitting the most network traffic at every given moment over a six-minute time period:</p>
      <CodeBlock code={"topk(3, sum by (name, namespace) (rate(kubevirt_vmi_network_receive_bytes_total[6m])) + sum by (name, namespace) (rate(kubevirt_vmi_network_transmit_bytes_total[6m]))) > 0"} lang="promql" />
      <h3 id="virt-promql-storage-metrics_virt-prometheus-queries" class="group scroll-mt-20">Storage metrics<a href="#virt-promql-storage-metrics_virt-prometheus-queries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>You can monitor virtual machine storage traffic and identify high-traffic VMs by using Prometheus queries.</p>
      <p>The following queries can identify VMs that are writing large amounts of data:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>kubevirt_vmi_storage_read_traffic_bytes_total</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Returns the total amount (in bytes) of the virtual machine’s storage-related traffic. Type: Counter.</p></dd>
      <dt class="font-semibold"><code>kubevirt_vmi_storage_write_traffic_bytes_total</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Returns the total amount of storage writes (in bytes) of the virtual machine’s storage-related traffic. Type: Counter.</p></dd>
      </dl>
      <p>Example storage-related traffic queries:</p>
      <ul>
      <li>
      The following query returns the top 3 VMs performing the most storage traffic at every given moment over a six-minute time period:
      <CodeBlock code={"topk(3, sum by (name, namespace) (rate(kubevirt_vmi_storage_read_traffic_bytes_total[6m])) + sum by (name, namespace) (rate(kubevirt_vmi_storage_write_traffic_bytes_total[6m]))) > 0"} lang="promql" />
      </li>
      <li>
      The following query returns the top 3 VMs with the highest average read latency at every given moment over a six-minute time period:
      <CodeBlock code={"topk(3, sum by (name, namespace) (rate(kubevirt_vmi_storage_read_times_seconds_total{name='${name}',namespace='${namespace}'${clusterFilter}}[6m]) / rate(kubevirt_vmi_storage_iops_read_total{name='${name}',namespace='${namespace}'${clusterFilter}}[6m]) > 0)) > 0"} lang="promql" />
      </li>
      </ul>
      <p>The following queries can track data restored from storage snapshots:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>vm:kubevirt_vmsnapshot_disks_restored:sum</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Returns the total number of virtual machine disks restored from the source virtual machine. Type: Gauge.</p></dd>
      <dt class="font-semibold"><code>vm:kubevirt_vmsnapshot_restored_bytes:sum</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Returns the amount of space in bytes restored from the source virtual machine. Type: Gauge.</p></dd>
      </dl>
      <p>Examples of storage snapshot data queries:</p>
      <ul>
      <li>
      The following query returns the total number of virtual machine disks restored from the source virtual machine:
      <CodeBlock code={"vm:kubevirt_vmsnapshot_disks_restored:sum{vm_name=\"simple-vm\", vm_namespace=\"default\"}"} lang="promql" />
      </li>
      <li>
      The following query returns the amount of space in bytes restored from the source virtual machine:
      <CodeBlock code={"vm:kubevirt_vmsnapshot_restored_bytes:sum{vm_name=\"simple-vm\", vm_namespace=\"default\"}"} lang="promql" />
      </li>
      </ul>
      <p>The following queries can determine the I/O performance of storage devices:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>kubevirt_vmi_storage_iops_read_total</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Returns the amount of read I/O operations the virtual machine is performing per second. Type: Counter.</p></dd>
      <dt class="font-semibold"><code>kubevirt_vmi_storage_iops_write_total</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Returns the amount of write I/O operations the virtual machine is performing per second. Type: Counter.</p></dd>
      </dl>
      <p>Example I/O performance query:</p>
      <p>The following query returns the top 3 VMs performing the most I/O operations per second at every given moment over a six-minute time period:</p>
      <CodeBlock code={"topk(3, sum by (name, namespace) (rate(kubevirt_vmi_storage_iops_read_total[6m])) + sum by (name, namespace) (rate(kubevirt_vmi_storage_iops_write_total[6m]))) > 0"} lang="promql" />
      <h3 id="virt-promql-guest-memory-metrics_virt-prometheus-queries" class="group scroll-mt-20">Guest memory swapping metrics<a href="#virt-promql-guest-memory-metrics_virt-prometheus-queries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The following queries can identify which swap-enabled guests are performing the most memory swapping:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>kubevirt_vmi_memory_swap_in_traffic_bytes</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Returns the total amount (in bytes) of memory the virtual guest is swapping in. Type: Gauge.</p></dd>
      <dt class="font-semibold"><code>kubevirt_vmi_memory_swap_out_traffic_bytes</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Returns the total amount (in bytes) of memory the virtual guest is swapping out. Type: Gauge.</p></dd>
      </dl>
      <p>Example memory swapping query:</p>
      <p>The following query returns the top 3 VMs where the guest is performing the most memory swapping at every given moment over a six-minute time period:</p>
      <CodeBlock code={"topk(3, sum by (name, namespace) (rate(kubevirt_vmi_memory_swap_in_traffic_bytes[6m])) + sum by (name, namespace) (rate(kubevirt_vmi_memory_swap_out_traffic_bytes[6m]))) > 0"} lang="promql" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Memory swapping indicates that the virtual machine is under memory pressure. Increasing the memory allocation of the virtual machine can mitigate this issue.</p>
        </AlertDescription>
      </Alert>
      <h3 id="virt-promql-aaq-metrics_virt-prometheus-queries" class="group scroll-mt-20">Monitoring AAQ operator metrics<a href="#virt-promql-aaq-metrics_virt-prometheus-queries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The following metrics are exposed by the Application Aware Quota (AAQ) controller for monitoring resource quotas:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>kube_application_aware_resourcequota</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Returns the current quota usage and the CPU and memory limits enforced by the AAQ Operator resources. Type: Gauge.</p></dd>
      <dt class="font-semibold"><code>kube_application_aware_resourcequota_creation_timestamp</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Returns the time, in UNIX timestamp format, when the AAQ Operator resource is created. Type: Gauge.</p></dd>
      </dl>
      <h3 id="virt-vm-label-metrics_virt-prometheus-queries" class="group scroll-mt-20">VM label metrics<a href="#virt-vm-label-metrics_virt-prometheus-queries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>kubevirt_vm_labels</code></dt>
      <dd class="ml-6 text-muted-foreground">
      <p>Returns virtual machine labels as Prometheus labels. Type: Gauge.</p>
      <p>You can expose and ignore specific labels by editing the <code>kubevirt-vm-labels-config</code> config map. After you apply the config map to your cluster, the configuration is loaded dynamically.</p>
      <p>Example config map:</p>
      <CodeBlock code={"apiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kubevirt-vm-labels-config\n  namespace: openshift-cnv\ndata:\n  allowlist: \"*\"\n  ignorelist: \"\""} lang="yaml" />
      <ul>
      <li>
      <code>data.allowlist</code> specifies labels to expose.
      <ul>
      <li>If <code>data.allowlist</code> has a value of <code>"*"</code>, all labels are included.</li>
      <li>If <code>data.allowlist</code> has a value of <code>""</code>, the metric does not return any labels.</li>
      <li>If <code>data.allowlist</code> contains a list of label keys, only the explicitly named labels are exposed. For example: <code>allowlist: "example.io/name,example.io/version"</code>.</li>
      </ul>
      </li>
      <li>
      <code>data.ignorelist</code> specifies labels to ignore. The ignore list overrides the allow list.
      <ul>
      <li>The <code>data.ignorelist</code> field does not support wildcard patterns. It can be empty or include a list of specific labels to ignore.</li>
      <li>If <code>data.ignorelist</code> has a value of <code>""</code>, no labels are ignored.</li>
      </ul>
      </li>
      </ul>
      </dd>
      </dl>
      <h3 id="virt-live-migration-metrics_virt-prometheus-queries" class="group scroll-mt-20">Live migration metrics<a href="#virt-live-migration-metrics_virt-prometheus-queries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can query metrics to show live migration status.</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>kubevirt_vmi_migration_data_processed_bytes</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The amount of guest operating system data that has migrated to the new virtual machine (VM). Type: Gauge.</p></dd>
      <dt class="font-semibold"><code>kubevirt_vmi_migration_data_remaining_bytes</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The amount of guest operating system data that remains to be migrated. Type: Gauge.</p></dd>
      <dt class="font-semibold"><code>kubevirt_vmi_migration_memory_transfer_rate_bytes</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The rate at which memory is becoming dirty in the guest operating system. Dirty memory is data that has been changed but not yet written to disk. Type: Gauge.</p></dd>
      <dt class="font-semibold"><code>kubevirt_vmi_migrations_in_pending_phase</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The number of pending migrations. Type: Gauge.</p></dd>
      <dt class="font-semibold"><code>kubevirt_vmi_migrations_in_scheduling_phase</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The number of scheduling migrations. Type: Gauge.</p></dd>
      <dt class="font-semibold"><code>kubevirt_vmi_migrations_in_running_phase</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The number of running migrations. Type: Gauge.</p></dd>
      <dt class="font-semibold"><code>kubevirt_vmi_migration_succeeded</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The number of successfully completed migrations. Type: Gauge.</p></dd>
      <dt class="font-semibold"><code>kubevirt_vmi_migration_failed</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The number of failed migrations. Type: Gauge.</p></dd>
      </dl>
      <h2 id="virt-node-memory-overcommit-dashboard_virt-prometheus-queries" class="group scroll-mt-20">Node Memory Overcommit dashboard<a href="#virt-node-memory-overcommit-dashboard_virt-prometheus-queries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The Node Memory Overcommit dashboard displays physical and virtual memory utilization across the cluster, focusing on how virtual machines (VMs) affect node memory.</p>
      <p>Use this dashboard to monitor memory capacity, detect memory pressure, identify overcommitment risks, and validate that system processes do not exceed their reserved memory.</p>
      <p>You can access this dashboard from the web console in <strong>Observe</strong> → <strong>Dashboards (Perses)</strong>.</p>
      <h3 id="_dashboard_filters" class="group scroll-mt-20">Dashboard filters<a href="#_dashboard_filters" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The dashboard provides two filter variables in the top toolbar:</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Filter</TableHead>
      <TableHead>Description</TableHead>
      <TableHead>Default</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><strong>Node</strong></TableCell>
      <TableCell>Filters all panels to display information about one or more nodes in the cluster.</TableCell>
      <TableCell><strong>All</strong></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>role</strong></TableCell>
      <TableCell>Filters all panels to display information about nodes with a specific Kubernetes role, populated from <code>kube_node_role</code>.</TableCell>
      <TableCell><strong>worker</strong></TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>You can also change the time range displayed in the different panels in the top toolbar of the dashboard.</p>
      <h3 id="_summary_section" class="group scroll-mt-20">Summary section<a href="#_summary_section" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The <strong>Summary</strong> section provides a high-level overview of cluster memory health. The panels in this section provide general memory utilization data for the cluster and the nodes. Most of the panels in this section appear in other sections of the dashboard as well. However, the following panels are unique to the <strong>Summary</strong> section:</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Panel</TableHead>
      <TableHead>Type</TableHead>
      <TableHead>Description</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><strong>Node Utilization - max</strong></TableCell>
      <TableCell>Gauge</TableCell>
      <TableCell>Displays the highest node utilization in the cluster. Use this panel together with the <strong>Node Utilization - min</strong> panel to assess scheduling balance. A large gap between minimum and maximum node utilization indicates an imbalanced workload distribution.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Cluster Utilization</strong></TableCell>
      <TableCell>Time series</TableCell>
      <TableCell>Displays the trend over time of several memory metrics. Use this panel to compare actual utilization and virtual memory plans against total capacity.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h3 id="_cluster_section" class="group scroll-mt-20">Cluster section<a href="#_cluster_section" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The <strong>Cluster</strong> section provides a more detailed view of cluster-wide memory behavior over a range of time. This section includes the following panels:</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Panel</TableHead>
      <TableHead>Type</TableHead>
      <TableHead>Description</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><strong>Physical Memory Utilization &amp; Requests</strong></TableCell>
      <TableCell>Time series</TableCell>
      <TableCell>Displays the total node memory capacity alongside actual memory utilization split into virtualization and non-virtualization workloads. This panel also shows the memory request plan (system-reserved requests and pod requests) so that you can compare actual usage to the scheduler expectations.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Cluster Utilization</strong></TableCell>
      <TableCell>Gauge</TableCell>
      <TableCell>Displays the aggregated memory utilization of the nodes in the cluster. This panel also appears in the <strong>Summary</strong> section.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Virtual Memory Assignment</strong></TableCell>
      <TableCell>Time series</TableCell>
      <TableCell>This panel displays the worst-case scenario if all virtual memory is used at present. It shows total node capacity, utilization without virtualization, and non-virtualization utilization combined with the total assigned VM memory. If the <strong>VM assigned virtual memory</strong> line nears or exceeds <strong>Node capacity</strong>, the cluster risks out-of-memory conditions under full VM memory pressure.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Cluster Virtual Committed</strong></TableCell>
      <TableCell>Gauge</TableCell>
      <TableCell>Displays the percentage of committed virtual memory out of all of the allocatable physical memory. This panel also appears in the <strong>Summary</strong> section.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Cluster - Memory Pressure</strong></TableCell>
      <TableCell>Time series</TableCell>
      <TableCell>Shows cluster-level memory PSI rates for <code>Waiting</code> (processes delayed by memory) and <code>Stalled</code> (processes completely blocked).</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Cluster - Aggregated Swap</strong></TableCell>
      <TableCell>Time series</TableCell>
      <TableCell>Shows total swap capacity and usage across all the nodes that you select. Rising swap usage indicates memory pressure that has not yet caused PSI stalls.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h3 id="_nodes_section" class="group scroll-mt-20">Nodes section<a href="#_nodes_section" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The <strong>Nodes</strong> section breaks down memory information per node to help identify imbalances. This section includes the following panels:</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Panel</TableHead>
      <TableHead>Type</TableHead>
      <TableHead>Description</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><strong>Utilization - Actual Overcommit Level</strong></TableCell>
      <TableCell>Time series</TableCell>
      <TableCell>Displays the per-node allocatable memory utilization minus the system-reserved memory.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Node Utilization - min</strong></TableCell>
      <TableCell>Gauge</TableCell>
      <TableCell>Displays the lowest node utilization in the cluster. This panel also appears in the <strong>Summary</strong> section.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Plan - Pod Requests per Node</strong></TableCell>
      <TableCell>Time series</TableCell>
      <TableCell>Shows the memory request fill level per node: the sum of all pod memory requests on a node divided by the node’s allocatable memory.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Node Requests - min/max</strong></TableCell>
      <TableCell>Stat</TableCell>
      <TableCell>Displays the minimum and maximum pod request ratios per node. Use this panel to quickly assess the cluster’s remaining capacity to host new workloads.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Plan - Virtual Memory Commit Level</strong></TableCell>
      <TableCell>Time series</TableCell>
      <TableCell>Displays the virtual memory commit ratio per node. The panel compares total active and assigned VM memory against the node’s available memory.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Node Virtual - min/max</strong></TableCell>
      <TableCell>Stat</TableCell>
      <TableCell>Displays the minimum and maximum virtual commit levels per node.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Node - Pressure</strong></TableCell>
      <TableCell>Time series</TableCell>
      <TableCell>Displays the memory PSI waiting rate per node. Each line represents one node to help you identify which nodes experience memory contention.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Node PSI - max</strong></TableCell>
      <TableCell>Gauge</TableCell>
      <TableCell>Displays the highest PSI value across all nodes. PSI shows the amount of time applications are stalled or delayed waiting for memory resources. This panel also appears in the <strong>Summary</strong> section.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h3 id="_system_reserved_section" class="group scroll-mt-20">System Reserved section<a href="#_system_reserved_section" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The <strong>System Reserved</strong> section monitors whether the system (hypervisor) processes stay within their reserved memory budget. This section includes the following panels:</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Panel</TableHead>
      <TableHead>Type</TableHead>
      <TableHead>Description</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><strong>Utilization - Reserved System Memory</strong></TableCell>
      <TableCell>Time series</TableCell>
      <TableCell>Displays the top 5 nodes by system-reserved memory utilization. This metric compares active system process memory against the reserved memory budget.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Utilization - min/max</strong></TableCell>
      <TableCell>Time series</TableCell>
      <TableCell>Displays the minimum and maximum system-reserved memory utilization across all nodes over time.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>System Exceeding Reservation</strong></TableCell>
      <TableCell>Stat</TableCell>
      <TableCell>Displays the percentage or number of nodes out of all monitored nodes that are currently triggering the <code>SystemMemoryExceedsReservation</code> alert. This panel also appears in the <strong>Summary</strong> section.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h3 id="_workloads_section" class="group scroll-mt-20">Workloads section<a href="#_workloads_section" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The <strong>Workloads</strong> section, which is collapsed by default, focuses on individual VM memory behavior.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Panel</TableHead>
      <TableHead>Type</TableHead>
      <TableHead>Description</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><strong>VM Overcommit Ratio</strong></TableCell>
      <TableCell>Time series</TableCell>
      <TableCell>Displays the ratio of assigned virtual memory to pod memory requests for each VM.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>VM Virtual Committed</strong></TableCell>
      <TableCell>Gauge</TableCell>
      <TableCell>Displays the average VM overcommit ratio, which includes launcher overhead. This panel also appears in the <strong>Summary</strong> section.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>VM Virtual Memory Utilization vs Host VM Utilization</strong></TableCell>
      <TableCell>Time series</TableCell>
      <TableCell>Displays the 10 VMs with the highest ratio between guest-reported memory usage and host-side container memory usage. Use this panel to identify VMs where guest-reported usage differs significantly from host-side accounting. This difference indicates balloon driver effectiveness or memory accounting discrepancies.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Number of Running VMs</strong></TableCell>
      <TableCell>Time series</TableCell>
      <TableCell>Displays the total number of running virtual machine instances (VMIs) on the cluster to provide context for the other workload panels.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h3 id="_interpreting_the_dashboard" class="group scroll-mt-20">Interpreting the dashboard<a href="#_interpreting_the_dashboard" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Monitor the dashboard indicators in the <strong>Summary</strong> section to identify early warning signs of memory pressure and prevent critical out-of-memory events.</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Healthy state</dt>
      <dd class="ml-6 text-muted-foreground"><p>The <strong>Cluster Utilization</strong> panel gauge is green (below 70%), the <strong>Cluster Virtual Committed</strong> panel gauge is below 120%, the <strong>Node PSI - max</strong> values are near zero, and no nodes exceed their system reservation.</p></dd>
      <dt class="font-semibold">Warning signs</dt>
      <dd class="ml-6 text-muted-foreground"><p>Utilization gauges turn amber (80% to 90%), the virtual commit approaches 150%, or individual nodes diverge significantly from the cluster average, which suggests imbalanced scheduling.</p></dd>
      <dt class="font-semibold">Critical state</dt>
      <dd class="ml-6 text-muted-foreground"><p>Utilization gauges turn red (above 90%), PSI values exceed 0.5, system reservation is exceeded on any node, or virtual commit ratios per node exceed 200%. These conditions indicate that the cluster is at risk of out-of-memory events and VM eviction.</p></dd>
      </dl>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="https://github.com/kubevirt/monitoring/blob/main/docs/metrics.md">KubeVirt components metrics</a></li>
      <li><a href="/openshift-docs-markdown/machine_configuration/machine-configs-configure#nodes-nodes-kernel-arguments_machine-configs-configure">Adding kernel arguments to nodes</a></li>
      <li><a href="https://docs.redhat.com/en/documentation/monitoring_stack_for_red_hat_openshift/latest/html/about_monitoring/about-ocp-monitoring">About OpenShift Container Platform monitoring</a></li>
      <li><a href="https://prometheus.io/docs/prometheus/latest/querying/basics/">Querying Prometheus</a></li>
      <li><a href="https://prometheus.io/docs/prometheus/latest/querying/examples/">Prometheus query examples</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
