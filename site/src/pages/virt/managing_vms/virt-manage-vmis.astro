---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/managing_vms/virt-manage-vmis.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-manage-vmis","text":"Manage virtual machine instances"},{"depth":2,"slug":"virt-about-vmis_virt-manage-vmis","text":"About virtual machine instances"},{"depth":2,"slug":"virt-listing-vmis-cli_virt-manage-vmis","text":"Listing all virtual machine instances using the CLI"},{"depth":2,"slug":"virt-listing-vmis-web_virt-manage-vmis","text":"Listing standalone virtual machine instances using the web console"},{"depth":2,"slug":"virt-searching-vmis-web_virt-manage-vmis","text":"Searching for standalone virtual machine instances by using the web console"},{"depth":2,"slug":"virt-editing-vmis-web_virt-manage-vmis","text":"Editing a standalone virtual machine instance using the web console"},{"depth":2,"slug":"virt-deleting-vmis-cli_virt-manage-vmis","text":"Deleting a standalone virtual machine instance using the CLI"},{"depth":2,"slug":"virt-deleting-vmis-web_virt-manage-vmis","text":"Deleting a standalone virtual machine instance using the web console"},{"depth":2,"slug":"additional-resources_virt-manage-vmis","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/managing_vms/virt-manage-vmis";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Manage virtual machine instances";
const description = "Manage standalone virtual machine instances (VMIs) that were created independently outside of the OpenShift Virtualization environment through the web…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/managing_vms"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Manage virtual machine instances {#virt-manage-vmis}\n\nManage standalone virtual machine instances (VMIs) that were created independently outside of the OpenShift Virtualization environment through the web console by using `oc` or `virtctl` commands from the command-line interface (CLI).\n\nThe `virtctl` command provides more virtualization options than the `oc` command. For example, you can use `virtctl` to pause a VM or expose a port.\n\n## About virtual machine instances {#virt-about-vmis_virt-manage-vmis}\n\nA virtual machine instance (VMI) is a representation of a running virtual machine (VM). When a VMI is owned by a VM or by another object, you manage it through its owner in the web console or by using the `oc` command-line interface (CLI).\n\nA standalone VMI is created and started independently with a script, through automation, or by using other methods in the CLI. In your environment, you might have standalone VMIs that were developed and started outside of the OpenShift Virtualization environment. You can continue to manage those standalone VMIs by using the CLI. You can also use the web console for specific tasks associated with standalone VMIs:\n\n- List standalone VMIs and their details.\n- Edit labels and annotations for a standalone VMI.\n- Delete a standalone VMI.\n\nWhen you delete a VM, the associated VMI is automatically deleted. You delete a standalone VMI directly because it is not owned by VMs or other objects.\n\n> [!NOTE]\n> Before you uninstall OpenShift Virtualization, list and view the standalone VMIs by using the CLI or the web console. Then, delete any outstanding VMIs.\n\nWhen you edit a VM, some settings might be applied to the VMIs dynamically and without the need for a restart. Any change made to a VM object that cannot be applied to the VMIs dynamically will trigger the `RestartRequired` VM condition. Changes are effective on the next reboot, and the condition is removed.\n\n## Listing all virtual machine instances using the CLI {#virt-listing-vmis-cli_virt-manage-vmis}\n\nYou can list all virtual machine instances (VMIs) in your cluster, including standalone VMIs and those owned by virtual machines, by using the OpenShift CLI (`oc`).\n\n**Prerequisites**\n\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n- List all VMIs by running the following command:\n\n  ```terminal\n  $ oc get vmis -A\n  ```\n\n## Listing standalone virtual machine instances using the web console {#virt-listing-vmis-web_virt-manage-vmis}\n\nUsing the web console, you can list and view standalone virtual machine instances (VMIs) in your cluster that are not owned by virtual machines (VMs).\n\n> [!NOTE]\n> VMIs that are owned by VMs or other objects are not displayed in the web console. The web console displays only standalone VMIs. If you want to list all VMIs in your cluster, you must use the CLI.\n\n**Procedure**\n\n- Click **Virtualization** -> **VirtualMachines** from the side menu.\n\n  You can identify a standalone VMI by a dark colored badge next to its name.\n\n## Searching for standalone virtual machine instances by using the web console {#virt-searching-vmis-web_virt-manage-vmis}\n\nYou can search for virtual machine instances (VMIs) by using the search bar on the **VirtualMachines** page. Use the advanced search to apply additional filters.\n\n**Procedure**\n\n1. In the OpenShift Container Platform console, click **Virtualization** → **VirtualMachines** from the side menu.\n2. In the search bar at the top of the page, type a VM name, label, or IP address.\n3. In the suggestions list, choose one of the following options:\n\n   - Click a VM name to open its details page.\n   - Click **All search results found for ...** to view results on a dedicated page.\n   - Click a related suggestion to prefill search filters.\n4. Optional: To open advanced search options, click the sliders icon next to the search bar. Expand the ***Details*** section and specify one or more of the available filters: **Name**, **Project**, **Description**, **Labels**, **Date created**, **vCPU**, and **Memory**.\n5. Optional: Expand the ***Network*** section and enter an IP address to filter by.\n6. Click **Search**.\n7. Optional: If Advanced Cluster Management (ACM) is installed, use the **Cluster** dropdown to search across multiple clusters.\n8. Optional: Click the **Save search** icon to store your search in the `kubevirt-user-settings` ConfigMap.\n\n## Editing a standalone virtual machine instance using the web console {#virt-editing-vmis-web_virt-manage-vmis}\n\nYou can edit the annotations and labels of a standalone virtual machine instance (VMI) using the web console. Other fields are not editable.\n\n**Procedure**\n\n1. In the OpenShift Container Platform console, click **Virtualization** -> **VirtualMachines** from the side menu.\n2. Select a standalone VMI to open the **VirtualMachineInstance details** page.\n3. On the **Details** tab, click the pencil icon beside **Annotations** or **Labels**.\n4. Make the relevant changes and click **Save**.\n\n## Deleting a standalone virtual machine instance using the CLI {#virt-deleting-vmis-cli_virt-manage-vmis}\n\nYou can delete a standalone virtual machine instance (VMI) by using the `oc` command-line interface (CLI).\n\n**Prerequisites**\n\n- Identify the name of the VMI that you want to delete.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n- Delete the VMI by running the following command:\n\n  ```terminal\n  $ oc delete vmi <vmi_name>\n  ```\n\n## Deleting a standalone virtual machine instance using the web console {#virt-deleting-vmis-web_virt-manage-vmis}\n\nYou can delete a standalone virtual machine instance (VMI) from the web console.\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, click **Virtualization** -> **VirtualMachines** from the side menu.\n2. Click **Actions** -> **Delete VirtualMachineInstance**.\n3. In the confirmation pop-up window, click **Delete** to permanently delete the standalone VMI.\n\n## Additional resources {#additional-resources_virt-manage-vmis}\n\n- [Using the CLI tools](/openshift-docs-markdown/virt/getting_started/virt-using-the-cli-tools#virt-using-the-cli-tools)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/managing_vms/virt-manage-vmis.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-manage-vmis" class="group scroll-mt-20">Manage virtual machine instances<a href="#virt-manage-vmis" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Manage standalone virtual machine instances (VMIs) that were created independently outside of the OpenShift Virtualization environment through the web console by using <code>oc</code> or <code>virtctl</code> commands from the command-line interface (CLI).</p>
      <p>The <code>virtctl</code> command provides more virtualization options than the <code>oc</code> command. For example, you can use <code>virtctl</code> to pause a VM or expose a port.</p>
      <h2 id="virt-about-vmis_virt-manage-vmis" class="group scroll-mt-20">About virtual machine instances<a href="#virt-about-vmis_virt-manage-vmis" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">A virtual machine instance (VMI) is a representation of a running virtual machine (VM). When a VMI is owned by a VM or by another object, you manage it through its owner in the web console or by using the <code>oc</code> command-line interface (CLI).</p>
      <p>A standalone VMI is created and started independently with a script, through automation, or by using other methods in the CLI. In your environment, you might have standalone VMIs that were developed and started outside of the OpenShift Virtualization environment. You can continue to manage those standalone VMIs by using the CLI. You can also use the web console for specific tasks associated with standalone VMIs:</p>
      <ul>
      <li>List standalone VMIs and their details.</li>
      <li>Edit labels and annotations for a standalone VMI.</li>
      <li>Delete a standalone VMI.</li>
      </ul>
      <p>When you delete a VM, the associated VMI is automatically deleted. You delete a standalone VMI directly because it is not owned by VMs or other objects.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Before you uninstall OpenShift Virtualization, list and view the standalone VMIs by using the CLI or the web console. Then, delete any outstanding VMIs.</p>
        </AlertDescription>
      </Alert>
      <p>When you edit a VM, some settings might be applied to the VMIs dynamically and without the need for a restart. Any change made to a VM object that cannot be applied to the VMIs dynamically will trigger the <code>RestartRequired</code> VM condition. Changes are effective on the next reboot, and the condition is removed.</p>
      <h2 id="virt-listing-vmis-cli_virt-manage-vmis" class="group scroll-mt-20">Listing all virtual machine instances using the CLI<a href="#virt-listing-vmis-cli_virt-manage-vmis" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can list all virtual machine instances (VMIs) in your cluster, including standalone VMIs and those owned by virtual machines, by using the OpenShift CLI (<code>oc</code>).</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have installed the OpenShift CLI (<code>oc</code>).</li></ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      List all VMIs by running the following command:
      <CodeBlock code={"$ oc get vmis -A"} lang="terminal" />
      </li>
      </ul>
      <h2 id="virt-listing-vmis-web_virt-manage-vmis" class="group scroll-mt-20">Listing standalone virtual machine instances using the web console<a href="#virt-listing-vmis-web_virt-manage-vmis" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Using the web console, you can list and view standalone virtual machine instances (VMIs) in your cluster that are not owned by virtual machines (VMs).</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>VMIs that are owned by VMs or other objects are not displayed in the web console. The web console displays only standalone VMIs. If you want to list all VMIs in your cluster, you must use the CLI.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Click <strong>Virtualization</strong> -&gt; <strong>VirtualMachines</strong> from the side menu.
      <p>You can identify a standalone VMI by a dark colored badge next to its name.</p>
      </li>
      </ul>
      <h2 id="virt-searching-vmis-web_virt-manage-vmis" class="group scroll-mt-20">Searching for standalone virtual machine instances by using the web console<a href="#virt-searching-vmis-web_virt-manage-vmis" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can search for virtual machine instances (VMIs) by using the search bar on the <strong>VirtualMachines</strong> page. Use the advanced search to apply additional filters.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform console, click <strong>Virtualization</strong> → <strong>VirtualMachines</strong> from the side menu.</li>
      <li>In the search bar at the top of the page, type a VM name, label, or IP address.</li>
      <li>
      In the suggestions list, choose one of the following options:
      <ul>
      <li>Click a VM name to open its details page.</li>
      <li>Click <strong>All search results found for ...</strong> to view results on a dedicated page.</li>
      <li>Click a related suggestion to prefill search filters.</li>
      </ul>
      </li>
      <li>Optional: To open advanced search options, click the sliders icon next to the search bar. Expand the <em></em><em><strong>Details</strong></em><em></em> section and specify one or more of the available filters: <strong>Name</strong>, <strong>Project</strong>, <strong>Description</strong>, <strong>Labels</strong>, <strong>Date created</strong>, <strong>vCPU</strong>, and <strong>Memory</strong>.</li>
      <li>Optional: Expand the <em></em><em><strong>Network</strong></em><em></em> section and enter an IP address to filter by.</li>
      <li>Click <strong>Search</strong>.</li>
      <li>Optional: If Advanced Cluster Management (ACM) is installed, use the <strong>Cluster</strong> dropdown to search across multiple clusters.</li>
      <li>Optional: Click the <strong>Save search</strong> icon to store your search in the <code>kubevirt-user-settings</code> ConfigMap.</li>
      </ol>
      <h2 id="virt-editing-vmis-web_virt-manage-vmis" class="group scroll-mt-20">Editing a standalone virtual machine instance using the web console<a href="#virt-editing-vmis-web_virt-manage-vmis" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can edit the annotations and labels of a standalone virtual machine instance (VMI) using the web console. Other fields are not editable.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform console, click <strong>Virtualization</strong> -&gt; <strong>VirtualMachines</strong> from the side menu.</li>
      <li>Select a standalone VMI to open the <strong>VirtualMachineInstance details</strong> page.</li>
      <li>On the <strong>Details</strong> tab, click the pencil icon beside <strong>Annotations</strong> or <strong>Labels</strong>.</li>
      <li>Make the relevant changes and click <strong>Save</strong>.</li>
      </ol>
      <h2 id="virt-deleting-vmis-cli_virt-manage-vmis" class="group scroll-mt-20">Deleting a standalone virtual machine instance using the CLI<a href="#virt-deleting-vmis-cli_virt-manage-vmis" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can delete a standalone virtual machine instance (VMI) by using the <code>oc</code> command-line interface (CLI).</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>Identify the name of the VMI that you want to delete.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Delete the VMI by running the following command:
      <CodeBlock code={"$ oc delete vmi <vmi_name>"} lang="terminal" />
      </li>
      </ul>
      <h2 id="virt-deleting-vmis-web_virt-manage-vmis" class="group scroll-mt-20">Deleting a standalone virtual machine instance using the web console<a href="#virt-deleting-vmis-web_virt-manage-vmis" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can delete a standalone virtual machine instance (VMI) from the web console.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, click <strong>Virtualization</strong> -&gt; <strong>VirtualMachines</strong> from the side menu.</li>
      <li>Click <strong>Actions</strong> -&gt; <strong>Delete VirtualMachineInstance</strong>.</li>
      <li>In the confirmation pop-up window, click <strong>Delete</strong> to permanently delete the standalone VMI.</li>
      </ol>
      <h2 id="additional-resources_virt-manage-vmis" class="group scroll-mt-20">Additional resources<a href="#additional-resources_virt-manage-vmis" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul><li><a href="/openshift-docs-markdown/virt/getting_started/virt-using-the-cli-tools#virt-using-the-cli-tools">Using the CLI tools</a></li></ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
