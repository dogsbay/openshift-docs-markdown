---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/managing_vms/virt-edit-boot-order.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-edit-boot-order","text":"Edit the boot order of a virtual machine"},{"depth":2,"slug":"virt-add-boot-order-web_virt-edit-boot-order","text":"Adding items to a boot order list in the web console"},{"depth":2,"slug":"virt-edit-boot-order-web_virt-edit-boot-order","text":"Editing a boot order list in the web console"},{"depth":2,"slug":"virt-edit-boot-order-yaml-web_virt-edit-boot-order","text":"Editing a boot order list in the YAML configuration file"},{"depth":2,"slug":"virt-remove-boot-order-item-web_virt-edit-boot-order","text":"Removing items from a boot order list in the web console"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/managing_vms/virt-edit-boot-order";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Edit the boot order of a virtual machine";
const description = "You can configure the boot order of disks and network devices on your virtual machine (VM) by using the web console or the CLI." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/managing_vms"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Edit the boot order of a virtual machine {#virt-edit-boot-order}\n\nYou can configure the boot order of disks and network devices on your virtual machine (VM) by using the web console or the CLI.\n\nWith **Boot Order** in the **VirtualMachine details** page, you can:\n\n- Select a disk or network interface controller (NIC) and add it to the boot order list.\n- Edit the order of the disks or NICs in the boot order list.\n- Remove a disk or NIC from the boot order list, and return it back to the inventory of bootable sources.\n\n## Adding items to a boot order list in the web console {#virt-add-boot-order-web_virt-edit-boot-order}\n\nYou can add items to a boot order list by using the web console.\n\n**Procedure**\n\n1. Click **Virtualization** → **VirtualMachines** from the side menu.\n2. Click the **Virtual machines** tab.\n3. Select a virtual machine to open the **VirtualMachine details** page.\n4. Click the **Configuration** tab.\n5. Expand **Boot management**.\n6. Click the pencil icon that is located on the right side of **Boot Order**. If a YAML configuration does not exist, or if this is the first time that you are creating a boot order list, the following message displays: **No resource selected. VM will attempt to boot from disks by order of appearance in YAML file.**\n7. Click **Add Source** and select a bootable disk or network interface controller (NIC) for the virtual machine.\n8. Add any additional disks or NICs to the boot order list.\n9. Click **Save**.\n\n   > [!NOTE]\n   > If the virtual machine is running, changes to **Boot Order** will not take effect until you restart the virtual machine.\n   >\n   > You can view pending changes by clicking **View Pending Changes** on the right side of the **Boot Order** field. The **Pending Changes** banner at the top of the page displays a list of all changes that will be applied when the virtual machine restarts.\n\n## Editing a boot order list in the web console {#virt-edit-boot-order-web_virt-edit-boot-order}\n\nYou can edit the boot order list in the web console.\n\n**Procedure**\n\n1. Click **Virtualization** → **VirtualMachines** from the side menu.\n2. Click the **Virtual machines** tab.\n3. Select a virtual machine to open the **VirtualMachine details** page.\n4. Click the **Configuration** tab.\n5. Expand **Boot management**.\n6. Click the pencil icon that is located on the right side of **Boot Order**.\n7. Choose the appropriate method to move the item in the boot order list:\n\n   - If you do not use a screen reader, hover over the arrow icon next to the item that you want to move, drag the item up or down, and drop it in a location of your choice.\n   - If you use a screen reader, press the Up Arrow key or Down Arrow key to move the item in the boot order list. Then, press the **Tab** key to drop the item in a location of your choice.\n8. Click **Save**.\n\n   > [!NOTE]\n   > If the virtual machine is running, changes to the boot order list will not take effect until you restart the virtual machine.\n   >\n   > You can view pending changes by clicking **View Pending Changes** on the right side of the **Boot Order** field. The **Pending Changes** banner at the top of the page displays a list of all changes that will be applied when the virtual machine restarts.\n\n## Editing a boot order list in the YAML configuration file {#virt-edit-boot-order-yaml-web_virt-edit-boot-order}\n\nYou can edit the boot order list in a YAML configuration file by using the CLI.\n\n**Prerequisites**\n\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Open the YAML configuration file for the virtual machine by running the following command:\n\n   ```terminal\n   $ oc edit vm <vm_name> -n <namespace>\n   ```\n2. Edit the YAML file and modify the values for the boot order associated with a disk or network interface controller (NIC). For example:\n\n   ```yaml\n   disks:\n     - bootOrder: 1\n       disk:\n         bus: virtio\n       name: containerdisk\n     - disk:\n         bus: virtio\n       name: cloudinitdisk\n     - cdrom:\n         bus: virtio\n       name: cd-drive-1\n   interfaces:\n     - boot Order: 2\n       macAddress: '02:96:c4:00:00'\n       masquerade: {}\n       name: default\n   ```\n\n   - `disks.bootOrder` defines the boot order value specified for the disk.\n   - `interfaces.bootOrder` defines the boot order value specified for the network interface controller.\n3. Save the YAML file.\n\n## Removing items from a boot order list in the web console {#virt-remove-boot-order-item-web_virt-edit-boot-order}\n\nRemove items from a boot order list by using the web console.\n\n**Procedure**\n\n1. Click **Virtualization** → **VirtualMachines** from the side menu.\n2. Click the **Virtual machines** tab.\n3. Select a virtual machine to open the **VirtualMachine details** page.\n4. Click the **Configuration** tab.\n5. Expand **Boot management**.\n6. Click the pencil icon that is located on the right side of **Boot Order**.\n7. Click the **Remove** icon ![](/openshift-docs-markdown/_assets/images/delete.png \"Delete\") next to the item. The item is removed from the boot order list and saved in the list of available boot sources. If you remove all items from the boot order list, the following message displays: **No resource selected. VM will attempt to boot from disks by order of appearance in YAML file.**\n\n   > [!NOTE]\n   > If the virtual machine is running, changes to **Boot Order** will not take effect until you restart the virtual machine.\n   >\n   > You can view pending changes by clicking **View Pending Changes** on the right side of the **Boot Order** field. The **Pending Changes** banner at the top of the page displays a list of all changes that will be applied when the virtual machine restarts.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/managing_vms/virt-edit-boot-order.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-edit-boot-order" class="group scroll-mt-20">Edit the boot order of a virtual machine<a href="#virt-edit-boot-order" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">You can configure the boot order of disks and network devices on your virtual machine (VM) by using the web console or the CLI.</p>
      <p>With <strong>Boot Order</strong> in the <strong>VirtualMachine details</strong> page, you can:</p>
      <ul>
      <li>Select a disk or network interface controller (NIC) and add it to the boot order list.</li>
      <li>Edit the order of the disks or NICs in the boot order list.</li>
      <li>Remove a disk or NIC from the boot order list, and return it back to the inventory of bootable sources.</li>
      </ul>
      <h2 id="virt-add-boot-order-web_virt-edit-boot-order" class="group scroll-mt-20">Adding items to a boot order list in the web console<a href="#virt-add-boot-order-web_virt-edit-boot-order" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can add items to a boot order list by using the web console.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Click <strong>Virtualization</strong> → <strong>VirtualMachines</strong> from the side menu.</li>
      <li>Click the <strong>Virtual machines</strong> tab.</li>
      <li>Select a virtual machine to open the <strong>VirtualMachine details</strong> page.</li>
      <li>Click the <strong>Configuration</strong> tab.</li>
      <li>Expand <strong>Boot management</strong>.</li>
      <li>Click the pencil icon that is located on the right side of <strong>Boot Order</strong>. If a YAML configuration does not exist, or if this is the first time that you are creating a boot order list, the following message displays: <strong>No resource selected. VM will attempt to boot from disks by order of appearance in YAML file.</strong></li>
      <li>Click <strong>Add Source</strong> and select a bootable disk or network interface controller (NIC) for the virtual machine.</li>
      <li>Add any additional disks or NICs to the boot order list.</li>
      <li>
      Click <strong>Save</strong>.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If the virtual machine is running, changes to <strong>Boot Order</strong> will not take effect until you restart the virtual machine.</p>
          <p>You can view pending changes by clicking <strong>View Pending Changes</strong> on the right side of the <strong>Boot Order</strong> field. The <strong>Pending Changes</strong> banner at the top of the page displays a list of all changes that will be applied when the virtual machine restarts.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <h2 id="virt-edit-boot-order-web_virt-edit-boot-order" class="group scroll-mt-20">Editing a boot order list in the web console<a href="#virt-edit-boot-order-web_virt-edit-boot-order" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can edit the boot order list in the web console.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Click <strong>Virtualization</strong> → <strong>VirtualMachines</strong> from the side menu.</li>
      <li>Click the <strong>Virtual machines</strong> tab.</li>
      <li>Select a virtual machine to open the <strong>VirtualMachine details</strong> page.</li>
      <li>Click the <strong>Configuration</strong> tab.</li>
      <li>Expand <strong>Boot management</strong>.</li>
      <li>Click the pencil icon that is located on the right side of <strong>Boot Order</strong>.</li>
      <li>
      Choose the appropriate method to move the item in the boot order list:
      <ul>
      <li>If you do not use a screen reader, hover over the arrow icon next to the item that you want to move, drag the item up or down, and drop it in a location of your choice.</li>
      <li>If you use a screen reader, press the Up Arrow key or Down Arrow key to move the item in the boot order list. Then, press the <strong>Tab</strong> key to drop the item in a location of your choice.</li>
      </ul>
      </li>
      <li>
      Click <strong>Save</strong>.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If the virtual machine is running, changes to the boot order list will not take effect until you restart the virtual machine.</p>
          <p>You can view pending changes by clicking <strong>View Pending Changes</strong> on the right side of the <strong>Boot Order</strong> field. The <strong>Pending Changes</strong> banner at the top of the page displays a list of all changes that will be applied when the virtual machine restarts.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <h2 id="virt-edit-boot-order-yaml-web_virt-edit-boot-order" class="group scroll-mt-20">Editing a boot order list in the YAML configuration file<a href="#virt-edit-boot-order-yaml-web_virt-edit-boot-order" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can edit the boot order list in a YAML configuration file by using the CLI.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have installed the OpenShift CLI (<code>oc</code>).</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Open the YAML configuration file for the virtual machine by running the following command:
      <CodeBlock code={"$ oc edit vm <vm_name> -n <namespace>"} lang="terminal" />
      </li>
      <li>
      Edit the YAML file and modify the values for the boot order associated with a disk or network interface controller (NIC). For example:
      <CodeBlock code={"disks:\n  - bootOrder: 1\n    disk:\n      bus: virtio\n    name: containerdisk\n  - disk:\n      bus: virtio\n    name: cloudinitdisk\n  - cdrom:\n      bus: virtio\n    name: cd-drive-1\ninterfaces:\n  - boot Order: 2\n    macAddress: '02:96:c4:00:00'\n    masquerade: {}\n    name: default"} lang="yaml" />
      <ul>
      <li><code>disks.bootOrder</code> defines the boot order value specified for the disk.</li>
      <li><code>interfaces.bootOrder</code> defines the boot order value specified for the network interface controller.</li>
      </ul>
      </li>
      <li>Save the YAML file.</li>
      </ol>
      <h2 id="virt-remove-boot-order-item-web_virt-edit-boot-order" class="group scroll-mt-20">Removing items from a boot order list in the web console<a href="#virt-remove-boot-order-item-web_virt-edit-boot-order" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Remove items from a boot order list by using the web console.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Click <strong>Virtualization</strong> → <strong>VirtualMachines</strong> from the side menu.</li>
      <li>Click the <strong>Virtual machines</strong> tab.</li>
      <li>Select a virtual machine to open the <strong>VirtualMachine details</strong> page.</li>
      <li>Click the <strong>Configuration</strong> tab.</li>
      <li>Expand <strong>Boot management</strong>.</li>
      <li>Click the pencil icon that is located on the right side of <strong>Boot Order</strong>.</li>
      <li>
      Click the <strong>Remove</strong> icon <img src="/openshift-docs-markdown/_assets/images/delete.png" alt="" loading="lazy" /> next to the item. The item is removed from the boot order list and saved in the list of available boot sources. If you remove all items from the boot order list, the following message displays: <strong>No resource selected. VM will attempt to boot from disks by order of appearance in YAML file.</strong>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If the virtual machine is running, changes to <strong>Boot Order</strong> will not take effect until you restart the virtual machine.</p>
          <p>You can view pending changes by clicking <strong>View Pending Changes</strong> on the right side of the <strong>Boot Order</strong> field. The <strong>Pending Changes</strong> banner at the top of the page displays a list of all changes that will be applied when the virtual machine restarts.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
