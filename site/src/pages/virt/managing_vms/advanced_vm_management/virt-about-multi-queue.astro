---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/managing_vms/advanced_vm_management/virt-about-multi-queue.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-about-multi-queue","text":"About multi-queue functionality"},{"depth":2,"slug":"known-limitations_virt-about-multi-queue","text":"Known limitations"},{"depth":2,"slug":"virt-enabling-multi-queue_virt-about-multi-queue","text":"Enabling multi-queue functionality"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/managing_vms/advanced_vm_management/virt-about-multi-queue";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "About multi-queue functionality";
const description = "Use multi-queue functionality to scale network throughput and performance on virtual machines (VMs) with multiple vCPUs." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/managing_vms","virt/managing_vms/advanced_vm_management"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# About multi-queue functionality {#virt-about-multi-queue}\n\nUse multi-queue functionality to scale network throughput and performance on virtual machines (VMs) with multiple vCPUs.\n\nBy default, the `queueCount` value, which is derived from the domain XML, is determined by the number of vCPUs allocated to a VM. Network performance does not scale as the number of vCPUs increases. Additionally, because `virtio-net` has only one transmit and receive queue, guests cannot send or receive packs in parallel.\n\n> [!NOTE]\n> Enabling `virtio-net` multi-queue does not offer significant improvements when the number of vNICs in a guest instance is proportional to the number of vCPUs.\n\n## Known limitations {#known-limitations_virt-about-multi-queue}\n\n- Message signaled interrupt (MSI) vectors are still consumed if `virtio-net` multi-queue is enabled in the host but not enabled in the guest operating system by the administrator.\n- Each `virtio-net` queue consumes 64 KiB of kernel memory for the `vhost` driver.\n\n## Enabling multi-queue functionality {#virt-enabling-multi-queue_virt-about-multi-queue}\n\nYou can enable multi-queue functionality for interfaces configured with a VirtIO model.\n\n**Procedure**\n\n1. Set the `networkInterfaceMultiqueue` value to `true` in the `VirtualMachine` manifest file of your VM to enable multi-queue functionality:\n\n   ```yaml\n   apiVersion: kubevirt.io/v1\n   kind: VM\n   spec:\n     domain:\n       devices:\n         networkInterfaceMultiqueue: true\n   ```\n2. Save the `VirtualMachine` manifest file to apply your changes.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/managing_vms/advanced_vm_management/virt-about-multi-queue.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-about-multi-queue" class="group scroll-mt-20">About multi-queue functionality<a href="#virt-about-multi-queue" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Use multi-queue functionality to scale network throughput and performance on virtual machines (VMs) with multiple vCPUs.</p>
      <p>By default, the <code>queueCount</code> value, which is derived from the domain XML, is determined by the number of vCPUs allocated to a VM. Network performance does not scale as the number of vCPUs increases. Additionally, because <code>virtio-net</code> has only one transmit and receive queue, guests cannot send or receive packs in parallel.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Enabling <code>virtio-net</code> multi-queue does not offer significant improvements when the number of vNICs in a guest instance is proportional to the number of vCPUs.</p>
        </AlertDescription>
      </Alert>
      <h2 id="known-limitations_virt-about-multi-queue" class="group scroll-mt-20">Known limitations<a href="#known-limitations_virt-about-multi-queue" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li>Message signaled interrupt (MSI) vectors are still consumed if <code>virtio-net</code> multi-queue is enabled in the host but not enabled in the guest operating system by the administrator.</li>
      <li>Each <code>virtio-net</code> queue consumes 64 KiB of kernel memory for the <code>vhost</code> driver.</li>
      </ul>
      <h2 id="virt-enabling-multi-queue_virt-about-multi-queue" class="group scroll-mt-20">Enabling multi-queue functionality<a href="#virt-enabling-multi-queue_virt-about-multi-queue" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can enable multi-queue functionality for interfaces configured with a VirtIO model.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Set the <code>networkInterfaceMultiqueue</code> value to <code>true</code> in the <code>VirtualMachine</code> manifest file of your VM to enable multi-queue functionality:
      <CodeBlock code={"apiVersion: kubevirt.io/v1\nkind: VM\nspec:\n  domain:\n    devices:\n      networkInterfaceMultiqueue: true"} lang="yaml" />
      </li>
      <li>Save the <code>VirtualMachine</code> manifest file to apply your changes.</li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
