---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/managing_vms/advanced_vm_management/virt-working-with-resource-quotas-for-vms.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-working-with-resource-quotas-for-vms","text":"Working with resource quotas for virtual machines"},{"depth":2,"slug":"virt-setting-resource-quota-limits-for-vms_virt-working-with-resource-quotas-for-vms","text":"Setting resource quota limits for virtual machines by using the CLI"},{"depth":2,"slug":"virt-understanding-aaq-operator_virt-working-with-resource-quotas-for-vms","text":"Understanding the Application Aware Quota Operator"},{"depth":2,"slug":"virt-enabling-aaq-operator_virt-working-with-resource-quotas-for-vms","text":"Enabling the Application Aware Quota Operator"},{"depth":2,"slug":"virt-creating-vm-quotas-web_virt-working-with-resource-quotas-for-vms","text":"Creating virtual machine quotas using the web console"},{"depth":2,"slug":"virt-managing-quotas-list-web_virt-working-with-resource-quotas-for-vms","text":"Managing virtual machine quotas using the list view"},{"depth":2,"slug":"virt-managing-quota-details-web_virt-working-with-resource-quotas-for-vms","text":"Managing virtual machine quota details using the web console"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/managing_vms/advanced_vm_management/virt-working-with-resource-quotas-for-vms";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Working with resource quotas for virtual machines";
const description = "Manage and control virtual machine resource consumption to prevent overcommitment and ensure stable workload performance." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/managing_vms","virt/managing_vms/advanced_vm_management"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Working with resource quotas for virtual machines {#virt-working-with-resource-quotas-for-vms}\n\nManage and control virtual machine resource consumption to prevent overcommitment and ensure stable workload performance.\n\nCreate and manage resource quotas for virtual machines.\n\n## Setting resource quota limits for virtual machines by using the CLI {#virt-setting-resource-quota-limits-for-vms_virt-working-with-resource-quotas-for-vms}\n\nBy default, OpenShift Virtualization automatically manages CPU and memory limits for virtual machines (VMs) if a namespace enforces resource quotas that require limits to be set. The memory limit is automatically set to twice the requested memory and the CPU limit is set to one per vCPU.\n\nYou can customize the memory limit ratio for a specific namespace by adding the `alpha.kubevirt.io/auto-memory-limits-ratio` label to the namespace. For example, the following command sets the memory limit ratio to 1.2:\n\n```terminal\n$ oc label ns/my-virtualization-project  alpha.kubevirt.io/auto-memory-limits-ratio=1.2\n```\n\n> [!WARNING]\n> Avoid managing resource quota limits manually. To prevent misconfigurations or scheduling issues, rely on the automatic resource limit management provided by OpenShift Virtualization unless you have a specific need to override the defaults.\n\nResource quotas that only use requests automatically work with VMs. If your resource quota uses limits, you must manually set resource limits on VMs. Memory resource limits, defined by the `spec.template.spec.domain.resources.limits.memory` value, must be at least 500 MiB, or 2% larger than the `spec.template.spec.domain.memory.guest` value.\n\n**Procedure**\n\n1. Set limits for a VM by editing the `VirtualMachine` manifest. For example:\n\n   ```yaml\n   apiVersion: kubevirt.io/v1\n   kind: VirtualMachine\n   metadata:\n     name: with-limits\n   spec:\n     runStrategy: Halted\n     template:\n       spec:\n         domain:\n           memory:\n             guest: 128Mi\n           resources:\n             limits:\n               memory: 256Mi\n   ```\n\n   where\n\n   spec.template.spec.domain.memory.guest\n   :   Specifies the actual amount of RAM that is shown to the guest operating system in the VM.\n\n   spec.template.spec.domain.resources.limits.memory\n   :   Specifies the hard limit for total memory consumption by the `virt-launcher` pod that hosts the VM. This limit must account for the guest operating system RAM plus the hypervisor runtime resource consumption. This example configuration is supported because the `spec.template.spec.domain.resources.limits.memory` value is at least `100Mi` larger than the `spec.template.spec.domain.memory.guest` value.\n2. Save the `VirtualMachine` manifest.\n\n## Understanding the Application Aware Quota Operator {#virt-understanding-aaq-operator_virt-working-with-resource-quotas-for-vms}\n\nThe Application Aware Quota (AAQ) operator extends Kubernetes native `ResourceQuota` to measure virtual machine (VM) resource consumption in mixed pod and VM environments.\n\nAAQ provides quota enforcement designed for virtualization workloads:\n\nKubernetes ResourceQuota\n:   Measures and enforces resource limits at the pod level. When applied to VMs, it measures the entire VM pod’s resource consumption, including hypervisor overhead and pod infrastructure, which does not reflect actual VM resource allocation.\n\nApplication Aware Quota (AAQ)\n:   Extends ResourceQuota functionality to accurately measure and enforce VM resource consumption. AAQ understands the virtualization layer and can measure actual virtual CPU and memory allocated to VMs, separate from pod overhead.\n\nAAQ supports two types of quotas:\n\nProject-scoped quotas\n:   Apply resource limits to a specific namespace or project. These are the most common use case for virtualization workloads.\n\nCluster-scoped quotas\n:   Apply resource limits across the entire cluster. These are useful for advanced scenarios where you need cluster-wide resource enforcement.\n\nWhen you enable AAQ, you must configure a quota calculation method. This setting determines how AAQ measures VM resource consumption across your cluster:\n\nVirtual resources\n:   Measures only the virtual CPU and memory allocated to virtual machines (VMs), excluding pod runtime overhead. This is the default option for virtualization workloads.\n\nVirtual Machine Instance (VMI) pod usage\n:   Measures the total virtual CPU and memory consumption of the VM pod, including both the virtual machine and pod runtime overhead.\n\nDedicated virtual resources\n:   Measures the virtual CPU and memory resources assigned to virtual machines and their associated pods, tracking quota usage separately for each type.\n\n## Enabling the Application Aware Quota Operator {#virt-enabling-aaq-operator_virt-working-with-resource-quotas-for-vms}\n\nEnable the Application Aware Quota (AAQ) operator to manage resource quotas to extend native resource management capabilities. Enabling AAQ from the web console eliminates the need to manually edit the HyperConverged custom resource by using the CLI.\n\n**Prerequisites**\n\n- You have cluster administrator privileges.\n- The OpenShift Virtualization operator is installed and running.\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, click **Virtualization** → **Settings**.\n2. Under **Resource Management**, toggle the **Application Aware Quotas (AAQ)** to On.\n\n   The operator deployment begins automatically. The status changes from **Disabled** to **Enabled** after the operator pods are running.\n3. Optional: Click the **Edit** icon next to the **Quota calculation method** field to change the calculation method.\n\n   A modal dialog is displayed with the following options:\n\n   - **Virtual resources**: Measures only the virtual CPU and memory allocated to virtual machines (VMs), excluding pod runtime overhead. This is the default option for virtualization workloads.\n   - **Virtual Machine Instance (VMI) pod usage**: Measures the total virtual CPU and memory consumption of the VM pod, including both the virtual machine and pod runtime overhead.\n   - **Dedicated virtual resources**: Measures the virtual CPU and memory resources assigned to virtual machines and their associated pods, tracking quota usage separately for each type.\n4. Select your preferred quota calculation method and click **Save**.\n\n**Verification**\n\n1. Verify that a new **Quotas** option is displayed in the left navigation menu under **Virtualization**. This indicates that AAQ is ready for quota creation and management.\n\n## Creating virtual machine quotas using the web console {#virt-creating-vm-quotas-web_virt-working-with-resource-quotas-for-vms}\n\nYou can create Application Aware Quota (AAQ) quotas that limit virtual machine resource consumption by using the guided form in the OpenShift Container Platform web console. The form is the recommended approach for simple virtualization quota use cases.\n\n**Prerequisites**\n\n- You have cluster administrator privileges.\n- The Application Aware Quota operator is enabled in your cluster. See [Enabling the Application Aware Quota operator](#virt-enabling-aaq-operator_virt-working-with-resource-quotas-for-vms).\n- The project where you want to create the quota exists.\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, click **Virtualization** → **Quotas**.\n2. Click **Create quota**.\n3. In the dropdown menu, select **With form**.\n\n   The create quota form is displayed.\n4. Enter a name for your quota in the **Name** field.\n\n   The name must be unique within the project.\n5. Select the project where you want to apply this quota.\n\n   By default, the current project is selected. You can click the dropdown to select a different project if needed.\n\n   > [!NOTE]\n   > If a Kubernetes `ResourceQuota` already exists in the selected project, an alert is displayed with a link to view the existing ResourceQuota. You can still create the AAQ quota, but both quotas will apply to the project.\n6. Configure CPU, memory, and VMI limits for the quota:\n\n   The fields displayed vary based on the selected quota calculation method. For example:\n\n   - For the  `Virtual resources` or `Dedicated virtual resources` quota calculation method, the quotas exclude pod overhead:\n\n     - Use the **vCPU allocation** field to set the maximum number of virtual CPUs permitted for all VMs in the project, excluding infrastructure overhead.\n     - Use the **Virtual memory allocation** field to set the maximum memory capacity, in GiB, excluding infrastructure overhead.\n     - Use the **VMI limits** field to set the maximum number of virtual machine instances.\n   - For the `Virtual Machine Instance (VMI) pod usage` quota calculation method, the quotas include pod overhead:\n\n     - Use the **CPU allocation** field to set the maximum number of CPU cores for pods that run VM workloads\n     - Use the **Memory allocation** field to set the maximum memory capacity, in GiB, for pods that run VM workloads.\n     - Use the **VMI limits** field to set the maximum number of virtual machine instances.\n\n       > [!NOTE]\n       > Live migration overhead is excluded from the `Virtual Machine Instance (VMI) pod usage`  quota calculation method.\n7. Review the configuration and click **Create**.\n\n   The quota is created and you are redirected to the quota details page. The new quota is now active and enforcing resource limits on virtual machines in the selected project.\n\n**Verification**\n\n1. In the OpenShift Container Platform web console, click **Virtualization** → **Quotas**.\n2. In the quotas list, locate your newly created quota.\n3. Verify that the quota name is displayed in the list with the correct project and resource limits configured.\n4. Click the quota name to open its details page and confirm all settings are correct.\n\n## Managing virtual machine quotas using the list view {#virt-managing-quotas-list-web_virt-working-with-resource-quotas-for-vms}\n\nThe Application Aware Quota (AAQ) quotas page in the OpenShift Container Platform web console provides a centralized view of all quotas managed by the AAQ operator. From this page, you can view quota status, usage, and perform management actions such as editing and deleting quotas.\n\n**Prerequisites**\n\n- You have cluster administrator privileges.\n- The Application Aware Quota operator is enabled in your cluster.\n- At least one Application Aware Quota exists in a namespace that matches the label selector configured in the HyperConverged Operator (HCO).\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, click **Virtualization** → **Quotas**.\n\n   The quotas list view is displayed, showing all project-scoped AAQ quotas by default.\n2. Click the **Cluster-scoped quotas** tab to view cluster scoped quotas.\n\n   This tab is displayed only if cluster-scoped quotas have been defined in your cluster.\n\n   > [!NOTE]\n   > Cluster-scoped quotas can only be created or edited using YAML. The form-based creation option is not available for cluster-scoped quotas due to their complexity.\n3. To view detailed information about a specific quota, click the quota name to open the quota details page.\n4. To edit a quota, click the Options menu ![](/openshift-docs-markdown/images/kebab.png \"Options menu\") at the end of the quota row\n\n   1. Select **Edit quota**.\n\n      The edit form is displayed with the current quota configuration.\n   2. Click **Save** after you have completed modifying the resource limits.\n\n      > [!NOTE]\n      > If the quota was initially created with advanced settings via YAML, clicking **Edit quota** opens the YAML editor instead of the form.\n5. To delete a quota, click the Options menu ![](/openshift-docs-markdown/images/kebab.png \"Options menu\") at the end of the quota row and select **Delete quota**. A confirmation dialog is displayed.\n\n   1. Click **Delete** to remove the quota.\n\n      > [!WARNING]\n      > Deleting a quota removes all resource enforcement for the quota. Virtual machines in the affected project are no longer subject to the quota limits.\n6. To create a new quota from the list view, click **Create quota**.\n\n   1. Select an option from the dropdown menu:\n\n      - **With form**: Opens the form-based creation experience for simple VM quota use cases.\n      - **With YAML**: Opens the YAML editor for advanced quota configurations that include pod limits or other resource types.\n\n## Managing virtual machine quota details using the web console {#virt-managing-quota-details-web_virt-working-with-resource-quotas-for-vms}\n\nThe quota details page in the OpenShift Container Platform web console provides information about an Application Aware Quota (AAQ), including resource limits, current usage, and visualizations of how quota capacity is being consumed.\n\n> [!NOTE]\n> Cluster scoped quotas show only the YAML definition due to the complexity that these quotas require.\n\n**Prerequisites**\n\n- You have cluster administrator privileges.\n- The Application Aware Quota operator is enabled in your cluster.\n- An Application Aware Quota exists in your cluster.\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, click **Virtualization** → **Quotas**.\n2. Click the quota name to open the **Quota details** view.\n\n   The **Quota details** view displays visual indicators, quota metadata, and tabulated data:\n\n   - Circular utilization charts visually represent resource consumption in your project\n   - The **Details** element provides metadata such as the Quota name, scope, applied labels and project.\n   - The **Quota details** element provides a tabulated views that display the current and maximum utilization of each resource type.\n\n     > [!NOTE]\n     > If a Kubernetes `ResourceQuota` also exists in the same project as this AAQ quota, an alert is displayed at the top of the details page with a link to view the ResourceQuota. Both quotas apply simultaneously to the project.\n3. Optional: Click the **YAML** tab to review the resource configuration of the quota.\n4. Optional: Use the **Actions** panel to edit or delete the quota.\n\n**Additional resources**\n{._additional-resources}\n\n- [Resource quotas per project](/openshift-docs-markdown/applications/quotas/quotas-setting-per-project#quotas-setting-per-project)\n- [Resource quotas across many projects](/openshift-docs-markdown/applications/quotas/quotas-setting-across-multiple-projects#quotas-setting-across-multiple-projects)\n- [Understanding the Application Aware Quota operator](/openshift-docs-markdown/virt/managing_vms/advanced_vm_management/virt-working-with-resource-quotas-for-vms#virt-understanding-aaq-operator_virt-working-with-resource-quotas-for-vms)\n- [Enabling the Application Aware Quota operator](/openshift-docs-markdown/virt/managing_vms/advanced_vm_management/virt-working-with-resource-quotas-for-vms#virt-enabling-aaq-operator_virt-working-with-resource-quotas-for-vms)\n- [Creating virtual machine quotas using the web console](/openshift-docs-markdown/virt/managing_vms/advanced_vm_management/virt-working-with-resource-quotas-for-vms#virt-creating-vm-quotas-web_virt-working-with-resource-quotas-for-vms)\n- [Managing virtual machine quotas using the list view](/openshift-docs-markdown/virt/managing_vms/advanced_vm_management/virt-working-with-resource-quotas-for-vms#virt-managing-quotas-list-web_virt-working-with-resource-quotas-for-vms)\n- [Managing virtual machine quota details using the web console](/openshift-docs-markdown/virt/managing_vms/advanced_vm_management/virt-working-with-resource-quotas-for-vms#virt-managing-quota-details-web_virt-working-with-resource-quotas-for-vms)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/managing_vms/advanced_vm_management/virt-working-with-resource-quotas-for-vms.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-working-with-resource-quotas-for-vms" class="group scroll-mt-20">Working with resource quotas for virtual machines<a href="#virt-working-with-resource-quotas-for-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Manage and control virtual machine resource consumption to prevent overcommitment and ensure stable workload performance.</p>
      <p>Create and manage resource quotas for virtual machines.</p>
      <h2 id="virt-setting-resource-quota-limits-for-vms_virt-working-with-resource-quotas-for-vms" class="group scroll-mt-20">Setting resource quota limits for virtual machines by using the CLI<a href="#virt-setting-resource-quota-limits-for-vms_virt-working-with-resource-quotas-for-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">By default, OpenShift Virtualization automatically manages CPU and memory limits for virtual machines (VMs) if a namespace enforces resource quotas that require limits to be set. The memory limit is automatically set to twice the requested memory and the CPU limit is set to one per vCPU.</p>
      <p>You can customize the memory limit ratio for a specific namespace by adding the <code>alpha.kubevirt.io/auto-memory-limits-ratio</code> label to the namespace. For example, the following command sets the memory limit ratio to 1.2:</p>
      <CodeBlock code={"$ oc label ns/my-virtualization-project  alpha.kubevirt.io/auto-memory-limits-ratio=1.2"} lang="terminal" />
      <Alert variant="warning" class="my-4">
        <AlertTitle>Warning</AlertTitle>
        <AlertDescription>
          <p>Avoid managing resource quota limits manually. To prevent misconfigurations or scheduling issues, rely on the automatic resource limit management provided by OpenShift Virtualization unless you have a specific need to override the defaults.</p>
        </AlertDescription>
      </Alert>
      <p>Resource quotas that only use requests automatically work with VMs. If your resource quota uses limits, you must manually set resource limits on VMs. Memory resource limits, defined by the <code>spec.template.spec.domain.resources.limits.memory</code> value, must be at least 500 MiB, or 2% larger than the <code>spec.template.spec.domain.memory.guest</code> value.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Set limits for a VM by editing the <code>VirtualMachine</code> manifest. For example:
      <CodeBlock code={"apiVersion: kubevirt.io/v1\nkind: VirtualMachine\nmetadata:\n  name: with-limits\nspec:\n  runStrategy: Halted\n  template:\n    spec:\n      domain:\n        memory:\n          guest: 128Mi\n        resources:\n          limits:\n            memory: 256Mi"} lang="yaml" />
      <p>where</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">spec.template.spec.domain.memory.guest</dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the actual amount of RAM that is shown to the guest operating system in the VM.</p></dd>
      <dt class="font-semibold">spec.template.spec.domain.resources.limits.memory</dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the hard limit for total memory consumption by the <code>virt-launcher</code> pod that hosts the VM. This limit must account for the guest operating system RAM plus the hypervisor runtime resource consumption. This example configuration is supported because the <code>spec.template.spec.domain.resources.limits.memory</code> value is at least <code>100Mi</code> larger than the <code>spec.template.spec.domain.memory.guest</code> value.</p></dd>
      </dl>
      </li>
      <li>Save the <code>VirtualMachine</code> manifest.</li>
      </ol>
      <h2 id="virt-understanding-aaq-operator_virt-working-with-resource-quotas-for-vms" class="group scroll-mt-20">Understanding the Application Aware Quota Operator<a href="#virt-understanding-aaq-operator_virt-working-with-resource-quotas-for-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The Application Aware Quota (AAQ) operator extends Kubernetes native <code>ResourceQuota</code> to measure virtual machine (VM) resource consumption in mixed pod and VM environments.</p>
      <p>AAQ provides quota enforcement designed for virtualization workloads:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Kubernetes ResourceQuota</dt>
      <dd class="ml-6 text-muted-foreground"><p>Measures and enforces resource limits at the pod level. When applied to VMs, it measures the entire VM pod’s resource consumption, including hypervisor overhead and pod infrastructure, which does not reflect actual VM resource allocation.</p></dd>
      <dt class="font-semibold">Application Aware Quota (AAQ)</dt>
      <dd class="ml-6 text-muted-foreground"><p>Extends ResourceQuota functionality to accurately measure and enforce VM resource consumption. AAQ understands the virtualization layer and can measure actual virtual CPU and memory allocated to VMs, separate from pod overhead.</p></dd>
      </dl>
      <p>AAQ supports two types of quotas:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Project-scoped quotas</dt>
      <dd class="ml-6 text-muted-foreground"><p>Apply resource limits to a specific namespace or project. These are the most common use case for virtualization workloads.</p></dd>
      <dt class="font-semibold">Cluster-scoped quotas</dt>
      <dd class="ml-6 text-muted-foreground"><p>Apply resource limits across the entire cluster. These are useful for advanced scenarios where you need cluster-wide resource enforcement.</p></dd>
      </dl>
      <p>When you enable AAQ, you must configure a quota calculation method. This setting determines how AAQ measures VM resource consumption across your cluster:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Virtual resources</dt>
      <dd class="ml-6 text-muted-foreground"><p>Measures only the virtual CPU and memory allocated to virtual machines (VMs), excluding pod runtime overhead. This is the default option for virtualization workloads.</p></dd>
      <dt class="font-semibold">Virtual Machine Instance (VMI) pod usage</dt>
      <dd class="ml-6 text-muted-foreground"><p>Measures the total virtual CPU and memory consumption of the VM pod, including both the virtual machine and pod runtime overhead.</p></dd>
      <dt class="font-semibold">Dedicated virtual resources</dt>
      <dd class="ml-6 text-muted-foreground"><p>Measures the virtual CPU and memory resources assigned to virtual machines and their associated pods, tracking quota usage separately for each type.</p></dd>
      </dl>
      <h2 id="virt-enabling-aaq-operator_virt-working-with-resource-quotas-for-vms" class="group scroll-mt-20">Enabling the Application Aware Quota Operator<a href="#virt-enabling-aaq-operator_virt-working-with-resource-quotas-for-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Enable the Application Aware Quota (AAQ) operator to manage resource quotas to extend native resource management capabilities. Enabling AAQ from the web console eliminates the need to manually edit the HyperConverged custom resource by using the CLI.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have cluster administrator privileges.</li>
      <li>The OpenShift Virtualization operator is installed and running.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, click <strong>Virtualization</strong> → <strong>Settings</strong>.</li>
      <li>
      Under <strong>Resource Management</strong>, toggle the <strong>Application Aware Quotas (AAQ)</strong> to On.
      <p>The operator deployment begins automatically. The status changes from <strong>Disabled</strong> to <strong>Enabled</strong> after the operator pods are running.</p>
      </li>
      <li>
      Optional: Click the <strong>Edit</strong> icon next to the <strong>Quota calculation method</strong> field to change the calculation method.
      <p>A modal dialog is displayed with the following options:</p>
      <ul>
      <li><strong>Virtual resources</strong>: Measures only the virtual CPU and memory allocated to virtual machines (VMs), excluding pod runtime overhead. This is the default option for virtualization workloads.</li>
      <li><strong>Virtual Machine Instance (VMI) pod usage</strong>: Measures the total virtual CPU and memory consumption of the VM pod, including both the virtual machine and pod runtime overhead.</li>
      <li><strong>Dedicated virtual resources</strong>: Measures the virtual CPU and memory resources assigned to virtual machines and their associated pods, tracking quota usage separately for each type.</li>
      </ul>
      </li>
      <li>Select your preferred quota calculation method and click <strong>Save</strong>.</li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol><li>Verify that a new <strong>Quotas</strong> option is displayed in the left navigation menu under <strong>Virtualization</strong>. This indicates that AAQ is ready for quota creation and management.</li></ol>
      <h2 id="virt-creating-vm-quotas-web_virt-working-with-resource-quotas-for-vms" class="group scroll-mt-20">Creating virtual machine quotas using the web console<a href="#virt-creating-vm-quotas-web_virt-working-with-resource-quotas-for-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can create Application Aware Quota (AAQ) quotas that limit virtual machine resource consumption by using the guided form in the OpenShift Container Platform web console. The form is the recommended approach for simple virtualization quota use cases.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have cluster administrator privileges.</li>
      <li>The Application Aware Quota operator is enabled in your cluster. See <a href="#virt-enabling-aaq-operator_virt-working-with-resource-quotas-for-vms">Enabling the Application Aware Quota operator</a>.</li>
      <li>The project where you want to create the quota exists.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, click <strong>Virtualization</strong> → <strong>Quotas</strong>.</li>
      <li>Click <strong>Create quota</strong>.</li>
      <li>
      In the dropdown menu, select <strong>With form</strong>.
      <p>The create quota form is displayed.</p>
      </li>
      <li>
      Enter a name for your quota in the <strong>Name</strong> field.
      <p>The name must be unique within the project.</p>
      </li>
      <li>
      Select the project where you want to apply this quota.
      <p>By default, the current project is selected. You can click the dropdown to select a different project if needed.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If a Kubernetes <code>ResourceQuota</code> already exists in the selected project, an alert is displayed with a link to view the existing ResourceQuota. You can still create the AAQ quota, but both quotas will apply to the project.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      Configure CPU, memory, and VMI limits for the quota:
      <p>The fields displayed vary based on the selected quota calculation method. For example:</p>
      <ul>
      <li>
      For the  <code>Virtual resources</code> or <code>Dedicated virtual resources</code> quota calculation method, the quotas exclude pod overhead:
      <ul>
      <li>Use the <strong>vCPU allocation</strong> field to set the maximum number of virtual CPUs permitted for all VMs in the project, excluding infrastructure overhead.</li>
      <li>Use the <strong>Virtual memory allocation</strong> field to set the maximum memory capacity, in GiB, excluding infrastructure overhead.</li>
      <li>Use the <strong>VMI limits</strong> field to set the maximum number of virtual machine instances.</li>
      </ul>
      </li>
      <li>
      For the <code>Virtual Machine Instance (VMI) pod usage</code> quota calculation method, the quotas include pod overhead:
      <ul>
      <li>Use the <strong>CPU allocation</strong> field to set the maximum number of CPU cores for pods that run VM workloads</li>
      <li>Use the <strong>Memory allocation</strong> field to set the maximum memory capacity, in GiB, for pods that run VM workloads.</li>
      <li>
      Use the <strong>VMI limits</strong> field to set the maximum number of virtual machine instances.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Live migration overhead is excluded from the <code>Virtual Machine Instance (VMI) pod usage</code>  quota calculation method.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      </li>
      </ul>
      </li>
      <li>
      Review the configuration and click <strong>Create</strong>.
      <p>The quota is created and you are redirected to the quota details page. The new quota is now active and enforcing resource limits on virtual machines in the selected project.</p>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, click <strong>Virtualization</strong> → <strong>Quotas</strong>.</li>
      <li>In the quotas list, locate your newly created quota.</li>
      <li>Verify that the quota name is displayed in the list with the correct project and resource limits configured.</li>
      <li>Click the quota name to open its details page and confirm all settings are correct.</li>
      </ol>
      <h2 id="virt-managing-quotas-list-web_virt-working-with-resource-quotas-for-vms" class="group scroll-mt-20">Managing virtual machine quotas using the list view<a href="#virt-managing-quotas-list-web_virt-working-with-resource-quotas-for-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The Application Aware Quota (AAQ) quotas page in the OpenShift Container Platform web console provides a centralized view of all quotas managed by the AAQ operator. From this page, you can view quota status, usage, and perform management actions such as editing and deleting quotas.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have cluster administrator privileges.</li>
      <li>The Application Aware Quota operator is enabled in your cluster.</li>
      <li>At least one Application Aware Quota exists in a namespace that matches the label selector configured in the HyperConverged Operator (HCO).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      In the OpenShift Container Platform web console, click <strong>Virtualization</strong> → <strong>Quotas</strong>.
      <p>The quotas list view is displayed, showing all project-scoped AAQ quotas by default.</p>
      </li>
      <li>
      Click the <strong>Cluster-scoped quotas</strong> tab to view cluster scoped quotas.
      <p>This tab is displayed only if cluster-scoped quotas have been defined in your cluster.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Cluster-scoped quotas can only be created or edited using YAML. The form-based creation option is not available for cluster-scoped quotas due to their complexity.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>To view detailed information about a specific quota, click the quota name to open the quota details page.</li>
      <li>
      To edit a quota, click the Options menu <img src="/openshift-docs-markdown/images/kebab.png" alt="" loading="lazy" /> at the end of the quota row
      <ol>
      <li>
      Select <strong>Edit quota</strong>.
      <p>The edit form is displayed with the current quota configuration.</p>
      </li>
      <li>
      Click <strong>Save</strong> after you have completed modifying the resource limits.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If the quota was initially created with advanced settings via YAML, clicking <strong>Edit quota</strong> opens the YAML editor instead of the form.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      </li>
      <li>
      To delete a quota, click the Options menu <img src="/openshift-docs-markdown/images/kebab.png" alt="" loading="lazy" /> at the end of the quota row and select <strong>Delete quota</strong>. A confirmation dialog is displayed.
      <ol>
      <li>
      Click <strong>Delete</strong> to remove the quota.
      <Alert variant="warning" class="my-4">
        <AlertTitle>Warning</AlertTitle>
        <AlertDescription>
          <p>Deleting a quota removes all resource enforcement for the quota. Virtual machines in the affected project are no longer subject to the quota limits.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      </li>
      <li>
      To create a new quota from the list view, click <strong>Create quota</strong>.
      <ol>
      <li>
      Select an option from the dropdown menu:
      <ul>
      <li><strong>With form</strong>: Opens the form-based creation experience for simple VM quota use cases.</li>
      <li><strong>With YAML</strong>: Opens the YAML editor for advanced quota configurations that include pod limits or other resource types.</li>
      </ul>
      </li>
      </ol>
      </li>
      </ol>
      <h2 id="virt-managing-quota-details-web_virt-working-with-resource-quotas-for-vms" class="group scroll-mt-20">Managing virtual machine quota details using the web console<a href="#virt-managing-quota-details-web_virt-working-with-resource-quotas-for-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The quota details page in the OpenShift Container Platform web console provides information about an Application Aware Quota (AAQ), including resource limits, current usage, and visualizations of how quota capacity is being consumed.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Cluster scoped quotas show only the YAML definition due to the complexity that these quotas require.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have cluster administrator privileges.</li>
      <li>The Application Aware Quota operator is enabled in your cluster.</li>
      <li>An Application Aware Quota exists in your cluster.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, click <strong>Virtualization</strong> → <strong>Quotas</strong>.</li>
      <li>
      Click the quota name to open the <strong>Quota details</strong> view.
      <p>The <strong>Quota details</strong> view displays visual indicators, quota metadata, and tabulated data:</p>
      <ul>
      <li>Circular utilization charts visually represent resource consumption in your project</li>
      <li>The <strong>Details</strong> element provides metadata such as the Quota name, scope, applied labels and project.</li>
      <li>
      The <strong>Quota details</strong> element provides a tabulated views that display the current and maximum utilization of each resource type.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If a Kubernetes <code>ResourceQuota</code> also exists in the same project as this AAQ quota, an alert is displayed at the top of the details page with a link to view the ResourceQuota. Both quotas apply simultaneously to the project.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      </li>
      <li>Optional: Click the <strong>YAML</strong> tab to review the resource configuration of the quota.</li>
      <li>Optional: Use the <strong>Actions</strong> panel to edit or delete the quota.</li>
      </ol>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/applications/quotas/quotas-setting-per-project#quotas-setting-per-project">Resource quotas per project</a></li>
      <li><a href="/openshift-docs-markdown/applications/quotas/quotas-setting-across-multiple-projects#quotas-setting-across-multiple-projects">Resource quotas across many projects</a></li>
      <li><a href="/openshift-docs-markdown/virt/managing_vms/advanced_vm_management/virt-working-with-resource-quotas-for-vms#virt-understanding-aaq-operator_virt-working-with-resource-quotas-for-vms">Understanding the Application Aware Quota operator</a></li>
      <li><a href="/openshift-docs-markdown/virt/managing_vms/advanced_vm_management/virt-working-with-resource-quotas-for-vms#virt-enabling-aaq-operator_virt-working-with-resource-quotas-for-vms">Enabling the Application Aware Quota operator</a></li>
      <li><a href="/openshift-docs-markdown/virt/managing_vms/advanced_vm_management/virt-working-with-resource-quotas-for-vms#virt-creating-vm-quotas-web_virt-working-with-resource-quotas-for-vms">Creating virtual machine quotas using the web console</a></li>
      <li><a href="/openshift-docs-markdown/virt/managing_vms/advanced_vm_management/virt-working-with-resource-quotas-for-vms#virt-managing-quotas-list-web_virt-working-with-resource-quotas-for-vms">Managing virtual machine quotas using the list view</a></li>
      <li><a href="/openshift-docs-markdown/virt/managing_vms/advanced_vm_management/virt-working-with-resource-quotas-for-vms#virt-managing-quota-details-web_virt-working-with-resource-quotas-for-vms">Managing virtual machine quota details using the web console</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
