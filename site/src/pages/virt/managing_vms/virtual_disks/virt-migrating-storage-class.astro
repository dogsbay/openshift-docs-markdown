---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/managing_vms/virtual_disks/virt-migrating-storage-class.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-migrating-storage-class","text":"Migrate a VM disk to a different storage class"},{"depth":2,"slug":"virt-migrating-storage-permissions_virt-migrating-storage-class","text":"Assign storage migration permissions"},{"depth":2,"slug":"virt-migrating-storage-class-ui_virt-migrating-storage-class","text":"Migrating VM disks to a different storage class by using the web console"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/managing_vms/virtual_disks/virt-migrating-storage-class";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Migrate a VM disk to a different storage class";
const description = "You can migrate one or more virtual disks to a different storage class to optimize storage performance or reduce costs without stopping your virtual…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/managing_vms","virt/managing_vms/virtual_disks"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Migrate a VM disk to a different storage class {#virt-migrating-storage-class}\n\nYou can migrate one or more virtual disks to a different storage class to optimize storage performance or reduce costs without stopping your virtual machine (VM) or virtual machine instance (VMI).\n\n## Assign storage migration permissions {#virt-migrating-storage-permissions_virt-migrating-storage-class}\n\nCluster administrators must grant users permission to perform storage migrations. Permissions to perform storage migrations are not part of the administrative or editing roles in the cluster by default.\n\n**Prerequisites**\n\n- You have cluster administrator privileges.\n\n**Procedure**\n\n1. (Optional) To assign the user single namespace storage migration permissions, run the following command:\n\n   ```terminal\n   $ kubectl create rolebinding <role_binding_name> \\\n       --clusterrole=migrations.kubevirt.io:storagemigrate \\\n       --user=<user_name> -n <namespace>\n   ```\n\n   where:\n\n   <role_binding_name>\n   :   The name to assign to this role binding instance.\n\n   <user_name>\n   :   The user to assign the storage migration permission.\n\n   <namespace>\n   :   The applicable namespace for this role binding instance.\n2. (Optional) To assign the user multiple namespace storage migration permissions, run the following command:\n\n   ```terminal\n   $ kubectl create clusterrolebinding <role_binding_name> \\\n       --clusterrole=migrations.kubevirt.io:storagemigrate-multins \\\n       --user=<user_name>\n   ```\n\n   where:\n\n   <role_binding_name>\n   :   The name to assign to this role binding instance.\n\n   <user_name>\n   :   The user to assign the storage migration permission.\n\n## Migrating VM disks to a different storage class by using the web console {#virt-migrating-storage-class-ui_virt-migrating-storage-class}\n\nYou can migrate one or more disks attached to a virtual machine (VM) to a different storage class by using the OpenShift Container Platform web console. When performing this action on a running VM, the operation of the VM is not interrupted and the data on the migrated disks remains accessible.\n\n**Prerequisites**\n\n- You must have a data volume or a persistent volume claim (PVC) available for storage class migration.\n- The cluster must have a node available for live migration. As part of the storage class migration, the VM is live migrated to a different node.\n- The VM must be running.\n\n**Procedure**\n\n1. Navigate to **Virtualization** -> **VirtualMachines** in the web console.\n2. Click the Options menu ![](kebab.png \"Options menu\") beside the virtual machine and select **Migration** -> **Storage**.\n\n   You can also access this option from the **VirtualMachine details** page by selecting **Actions** -> **Migration** -> **Storage**.\n\n   Alternatively, right-click the VM in the tree view and select **Migration** from the menu.\n3. On the **Migration details** page, choose whether to migrate the entire VM storage or selected volumes only. If you click **Selected volumes**, select any disks that you intend to migrate. Click **Next** to proceed.\n4. From the list of available options on the **Destination StorageClass** page, select the storage class to migrate to. Click **Next** to proceed.\n5. On the **Review** page, review the list of affected disks and the target storage class. To start the migration, click **Migrate VirtualMachine storage**.\n6. Stay on the **Migrate VirtualMachine storage** page to watch the progress and wait for the confirmation that the migration completed successfully.\n\n**Verification**\n\n1. From the **VirtualMachine details** page, navigate to **Configuration** -> **Storage**.\n2. Verify that all disks have the expected storage class listed in the **Storage class** column.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/managing_vms/virtual_disks/virt-migrating-storage-class.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-migrating-storage-class" class="group scroll-mt-20">Migrate a VM disk to a different storage class<a href="#virt-migrating-storage-class" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can migrate one or more virtual disks to a different storage class to optimize storage performance or reduce costs without stopping your virtual machine (VM) or virtual machine instance (VMI).</p>
      <h2 id="virt-migrating-storage-permissions_virt-migrating-storage-class" class="group scroll-mt-20">Assign storage migration permissions<a href="#virt-migrating-storage-permissions_virt-migrating-storage-class" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Cluster administrators must grant users permission to perform storage migrations. Permissions to perform storage migrations are not part of the administrative or editing roles in the cluster by default.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have cluster administrator privileges.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      (Optional) To assign the user single namespace storage migration permissions, run the following command:
      <CodeBlock code={"$ kubectl create rolebinding <role_binding_name> \\\n    --clusterrole=migrations.kubevirt.io:storagemigrate \\\n    --user=<user_name> -n <namespace>"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">&lt;role_binding_name&gt;</dt>
      <dd class="ml-6 text-muted-foreground"><p>The name to assign to this role binding instance.</p></dd>
      <dt class="font-semibold">&lt;user_name&gt;</dt>
      <dd class="ml-6 text-muted-foreground"><p>The user to assign the storage migration permission.</p></dd>
      <dt class="font-semibold">&lt;namespace&gt;</dt>
      <dd class="ml-6 text-muted-foreground"><p>The applicable namespace for this role binding instance.</p></dd>
      </dl>
      </li>
      <li>
      (Optional) To assign the user multiple namespace storage migration permissions, run the following command:
      <CodeBlock code={"$ kubectl create clusterrolebinding <role_binding_name> \\\n    --clusterrole=migrations.kubevirt.io:storagemigrate-multins \\\n    --user=<user_name>"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">&lt;role_binding_name&gt;</dt>
      <dd class="ml-6 text-muted-foreground"><p>The name to assign to this role binding instance.</p></dd>
      <dt class="font-semibold">&lt;user_name&gt;</dt>
      <dd class="ml-6 text-muted-foreground"><p>The user to assign the storage migration permission.</p></dd>
      </dl>
      </li>
      </ol>
      <h2 id="virt-migrating-storage-class-ui_virt-migrating-storage-class" class="group scroll-mt-20">Migrating VM disks to a different storage class by using the web console<a href="#virt-migrating-storage-class-ui_virt-migrating-storage-class" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can migrate one or more disks attached to a virtual machine (VM) to a different storage class by using the OpenShift Container Platform web console. When performing this action on a running VM, the operation of the VM is not interrupted and the data on the migrated disks remains accessible.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You must have a data volume or a persistent volume claim (PVC) available for storage class migration.</li>
      <li>The cluster must have a node available for live migration. As part of the storage class migration, the VM is live migrated to a different node.</li>
      <li>The VM must be running.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to <strong>Virtualization</strong> -&gt; <strong>VirtualMachines</strong> in the web console.</li>
      <li>
      Click the Options menu <img src="kebab.png" alt="" loading="lazy" /> beside the virtual machine and select <strong>Migration</strong> -&gt; <strong>Storage</strong>.
      <p>You can also access this option from the <strong>VirtualMachine details</strong> page by selecting <strong>Actions</strong> -&gt; <strong>Migration</strong> -&gt; <strong>Storage</strong>.</p>
      <p>Alternatively, right-click the VM in the tree view and select <strong>Migration</strong> from the menu.</p>
      </li>
      <li>On the <strong>Migration details</strong> page, choose whether to migrate the entire VM storage or selected volumes only. If you click <strong>Selected volumes</strong>, select any disks that you intend to migrate. Click <strong>Next</strong> to proceed.</li>
      <li>From the list of available options on the <strong>Destination StorageClass</strong> page, select the storage class to migrate to. Click <strong>Next</strong> to proceed.</li>
      <li>On the <strong>Review</strong> page, review the list of affected disks and the target storage class. To start the migration, click <strong>Migrate VirtualMachine storage</strong>.</li>
      <li>Stay on the <strong>Migrate VirtualMachine storage</strong> page to watch the progress and wait for the confirmation that the migration completed successfully.</li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>From the <strong>VirtualMachine details</strong> page, navigate to <strong>Configuration</strong> -&gt; <strong>Storage</strong>.</li>
      <li>Verify that all disks have the expected storage class listed in the <strong>Storage class</strong> column.</li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
