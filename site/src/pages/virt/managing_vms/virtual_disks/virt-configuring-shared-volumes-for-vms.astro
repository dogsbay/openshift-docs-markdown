---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/managing_vms/virtual_disks/virt-configuring-shared-volumes-for-vms.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-configuring-shared-volumes-for-vms","text":"Configure shared volumes for virtual machines"},{"depth":2,"slug":"virt-configuring-vm-disk-sharing_virt-configuring-shared-volumes-for-vms","text":"Configuring disk sharing by using virtual machine disks"},{"depth":2,"slug":"virt-configuring-disk-sharing-lun_virt-configuring-shared-volumes-for-vms","text":"Configuring disk sharing by using LUN"},{"depth":3,"slug":"virt-configuring-disk-sharing-lun-web_virt-configuring-shared-volumes-for-vms","text":"Configuring disk sharing by using LUN and the web console"},{"depth":3,"slug":"virt-configuring-disk-sharing-lun-cli_virt-configuring-shared-volumes-for-vms","text":"Configuring disk sharing by using LUN and the CLI"},{"depth":2,"slug":"virt-enabling-persistentreservation-feature-gate_virt-configuring-shared-volumes-for-vms","text":"Enabling the PersistentReservation feature gate"},{"depth":3,"slug":"virt-enabling-persistentreservation-feature-gate-prereqs_virt-configuring-shared-volumes-for-vms","text":"Prerequisites"},{"depth":3,"slug":"virt-enabling-persistentreservation-feature-gate-web_virt-configuring-shared-volumes-for-vms","text":"Enabling the PersistentReservation feature gate by using the web console"},{"depth":3,"slug":"virt-enabling-persistentreservation-feature-gate-cli_virt-configuring-shared-volumes-for-vms","text":"Enabling the PersistentReservation feature gate by using the CLI"},{"depth":2,"slug":"additional-resources_virt-configuring-shared-volumes-for-vms","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/managing_vms/virtual_disks/virt-configuring-shared-volumes-for-vms";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configure shared volumes for virtual machines";
const description = "Enable high-availability scenarios like Windows Failover Clustering by configuring shared disks to allow multiple virtual machines to access the same…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/managing_vms","virt/managing_vms/virtual_disks"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configure shared volumes for virtual machines {#virt-configuring-shared-volumes-for-vms}\n\nEnable high-availability scenarios like Windows Failover Clustering by configuring shared disks to allow multiple virtual machines to access the same storage volume. A shared disk’s volume must be block mode.\n\nYou configure disk sharing by exposing the storage as either of these types:\n\n- An ordinary VM disk\n- A logical unit number (LUN) disk with an SCSI connection and raw device mapping, as required for Windows Failover Clustering for shared volumes\n\nIn addition to configuring disk sharing, you can also set an error policy for each ordinary VM disk or LUN disk. The error policy controls how the hypervisor behaves when an input/output error occurs on a disk Read or Write.\n\n## Configuring disk sharing by using virtual machine disks {#virt-configuring-vm-disk-sharing_virt-configuring-shared-volumes-for-vms}\n\nYou can configure block volumes so that multiple virtual machines (VMs) can share storage.\n\nThe application running on the guest operating system determines the storage option you must configure for the VM. A disk of type `disk` exposes the volume as an ordinary disk to the VM.\n\nYou can set an error policy for each disk. The error policy controls how the hypervisor behaves when an input/output error occurs while a disk is being written to or read. The default behavior stops the VM and generates a Kubernetes event.\n\nYou can accept the default behavior, or you can set the error policy to one of the following options:\n\n- `report`, which reports the error in the guest.\n- `ignore`, which ignores the error. The Read or Write failure is undetected.\n- `enospace`, which produces an error indicating that there is not enough disk space.\n\n**Prerequisites**\n\n- The volume access mode must be `ReadWriteMany` (RWX) if the VMs that are sharing disks are running on different nodes.\n\n  If the VMs that are sharing disks are running on the same node, `ReadWriteOnce` (RWO) volume access mode is sufficient.\n\n  > [!NOTE]\n  > Not all filesystem and format combinations support RWX mode. For example, XFS format does not support RWX mode. When multiple VMs write to an XFS-formatted volume, data corruption occurs and only the writing VM can access the data.\n- The storage provider must support the required Container Storage Interface (CSI) driver.\n\n**Procedure**\n\n1. Create the `VirtualMachine` manifest for your VM to set the required values, as shown in the following example:\n\n   ```yaml\n   apiVersion: kubevirt.io/v1\n   kind: VirtualMachine\n   metadata:\n     name: <vm_name>\n   spec:\n     template:\n   # ...\n       spec:\n         domain:\n           devices:\n             disks:\n             - disk:\n                 bus: virtio\n               name: rootdisk\n               errorPolicy: report\n             - disk:\n                 bus: virtio\n               name: cluster\n               shareable: true\n             interfaces:\n             - masquerade: {}\n               name: default\n   ```\n\n   - `spec.template.spec.domain.devices.disks.errorPolicy` defines how the hypervisor should behave when an IO error occurs.\n   - `spec.template.spec.domain.devices.disks.shareable` defines whether multiple virtual machines (VMs) can use the same underlying disk.\n2. Save the `VirtualMachine` manifest file to apply your changes.\n\n## Configuring disk sharing by using LUN {#virt-configuring-disk-sharing-lun_virt-configuring-shared-volumes-for-vms}\n\nTo secure data on your VM from outside access, you can enable SCSI persistent reservation and configure a LUN-backed virtual machine disk to be shared among multiple virtual machines.\n\nBy enabling the shared option, you can use advanced SCSI commands, such as those required for a Windows failover clustering implementation, for managing the underlying storage.\n\nWhen a storage volume is configured as the `LUN` disk type, a VM can use the volume as a logical unit number (LUN) device. As a result, the VM can deploy and manage the disk by using SCSI commands.\n\nYou reserve a LUN through the SCSI persistent reserve options. To enable the reservation:\n\n1. Configure the feature gate option\n2. Activate the feature gate option on the LUN disk to issue SCSI device-specific input and output controls (IOCTLs) that the VM requires.\n\nYou can set an error policy for each LUN disk. The error policy controls how the hypervisor behaves when an input/output error occurs on a disk Read or Write. The default behavior stops the guest and generates a Kubernetes event.\n\nFor a LUN disk with an SCSi connection and a persistent reservation, as required for Windows Failover Clustering for shared volumes, you set the error policy to `report`.\n\n**Prerequisites**\n\n- You must have cluster administrator privileges to configure the feature gate option.\n- The volume access mode must be `ReadWriteMany` (RWX) if the VMs that are sharing disks are running on different nodes.\n\n  If the VMs that are sharing disks are running on the same node, `ReadWriteOnce` (RWO) volume access mode is sufficient.\n- The storage provider must support a Container Storage Interface (CSI) driver that uses Fibre Channel (FC), Fibre Channel over Ethernet (FCoE), or iSCSI storage protocols.\n- If you are a cluster administrator and intend to configure disk sharing by using LUN, you must enable the cluster’s feature gate on the `HyperConverged` custom resource (CR).\n- Disks that you want to share must be in block mode.\n\n**Procedure**\n\n1. Edit or create the `VirtualMachine` manifest for your VM to set the required values, as shown in the following example:\n\n   ```yaml\n   apiVersion: kubevirt.io/v1\n   kind: VirtualMachine\n   metadata:\n     name: vm-0\n   spec:\n     template:\n       spec:\n         domain:\n           devices:\n             disks:\n             - disk:\n                 bus: sata\n               name: rootdisk\n             - errorPolicy: report\n               lun:\n                 bus: scsi\n                 reservation: true\n               name: na-shared\n               serial: shared1234\n         volumes:\n         - dataVolume:\n             name: vm-0\n           name: rootdisk\n         - name: na-shared\n           persistentVolumeClaim:\n             claimName: pvc-na-share\n   ```\n\n   - `spec.template.spec.domain.devices.disks.errorPolicy` defines how the hypervisor should behave when an IO error occurs.\n   - `spec.template.spec.domain.devices.disks.lun` defines a volume exposed as a LUN device.\n   - `spec.template.spec.domain.devices.disks.lun.reservation` defines whether the persistent reservation is enabled.\n2. Save the `VirtualMachine` manifest file to apply your changes.\n\n### Configuring disk sharing by using LUN and the web console {#virt-configuring-disk-sharing-lun-web_virt-configuring-shared-volumes-for-vms}\n\nYou can use the OpenShift Container Platform web console to configure disk sharing by using LUN.\n\n**Prerequisites**\n\n- The cluster administrator must enable the `persistentreservation` feature gate setting.\n\n**Procedure**\n\n1. Click **Virtualization** -> **VirtualMachines** in the web console.\n2. Select a VM to open the **VirtualMachine details** page.\n3. Expand **Storage**.\n4. On the **Disks** tab, click **Add disk**.\n5. Specify the **Name**, **Source**, **Size**, **Interface**, and **Storage Class**.\n6. Select **LUN** as the **Type**.\n7. Select **Shared access (RWX)** as the **Access Mode**.\n8. Select **Block** as the **Volume Mode**.\n9. Expand **Advanced Settings**, and select both checkboxes.\n10. Click **Save**.\n\n### Configuring disk sharing by using LUN and the CLI {#virt-configuring-disk-sharing-lun-cli_virt-configuring-shared-volumes-for-vms}\n\nYou can use the command line to configure disk sharing by using LUN.\n\n**Procedure**\n\n1. Edit or create the `VirtualMachine` manifest for your VM to set the required values, as shown in the following example:\n\n   ```yaml\n   apiVersion: kubevirt.io/v1\n   kind: VirtualMachine\n   metadata:\n     name: vm-0\n   spec:\n     template:\n       spec:\n         domain:\n           devices:\n             disks:\n             - disk:\n                 bus: sata\n               name: rootdisk\n             - errorPolicy: report\n               lun:\n                 bus: scsi\n                 reservation: true\n               name: na-shared\n               serial: shared1234\n         volumes:\n         - dataVolume:\n             name: vm-0\n           name: rootdisk\n         - name: na-shared\n           persistentVolumeClaim:\n             claimName: pvc-na-share\n   ```\n\n   - `spec.template.spec.domain.devices.disks.errorPolicy` defines how the hypervisor should behave when an IO error occurs.\n   - `spec.template.spec.domain.devices.disks.lun` defines a volume exposed as a LUN device.\n   - `spec.template.spec.domain.devices.disks.lun.reservation` defines whether the persistent reservation is enabled.\n2. Save the `VirtualMachine` manifest file to apply your changes.\n\n## Enabling the PersistentReservation feature gate {#virt-enabling-persistentreservation-feature-gate_virt-configuring-shared-volumes-for-vms}\n\nYou can enable the SCSI `persistentReservation` feature gate and allow a LUN-backed block mode virtual machine (VM) disk to be shared among multiple virtual machines.\n\nThe `persistentReservation` feature gate is disabled by default. You can enable the `persistentReservation` feature gate by using the web console or the command line.\n\n### Prerequisites {#virt-enabling-persistentreservation-feature-gate-prereqs_virt-configuring-shared-volumes-for-vms}\n\n- Cluster administrator privileges are required.\n- The volume access mode `ReadWriteMany` (RWX) is required if the VMs that are sharing disks are running on different nodes. If the VMs that are sharing disks are running on the same node, the `ReadWriteOnce` (RWO) volume access mode is sufficient.\n- The storage provider must support a Container Storage Interface (CSI) driver that uses Fibre Channel (FC), Fibre Channel over Ethernet (FCoE), or iSCSI storage protocols.\n\n### Enabling the PersistentReservation feature gate by using the web console {#virt-enabling-persistentreservation-feature-gate-web_virt-configuring-shared-volumes-for-vms}\n\nYou must enable the PersistentReservation feature gate to allow a LUN-backed block mode virtual machine (VM) disk to be shared among multiple virtual machines. Enabling the feature gate requires cluster administrator privileges.\n\n**Procedure**\n\n1. Click **Virtualization** -> **Settings** in the web console.\n2. Select **Cluster**.\n3. Expand **SCSI persistent reservation** and set **Enable persistent reservation** to on.\n\n### Enabling the PersistentReservation feature gate by using the CLI {#virt-enabling-persistentreservation-feature-gate-cli_virt-configuring-shared-volumes-for-vms}\n\nYou can enable the `persistentReservation` feature gate by using the command line. Enabling the feature gate requires cluster administrator privileges.\n\n**Prerequisites**\n\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Enable the `persistentReservation` feature gate by running the following command:\n\n   ```terminal\n   $ oc patch {{ HCOCliKind }} kubevirt-hyperconverged -n {{ CNVNamespace }} --type json -p \\\n   '[{\"op\":\"replace\",\"path\":\"/spec/featureGates/persistentReservation\", \"value\": true}]'\n   ```\n\n## Additional resources {#additional-resources_virt-configuring-shared-volumes-for-vms}\n\n- [Persistent reservation helper protocol](https://www.qemu.org/docs/master/interop/pr-helper.html)\n- [Failover Clustering in Windows Server and Azure Stack HCI](https://learn.microsoft.com/en-us/windows-server/failover-clustering/failover-clustering-overview)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/managing_vms/virtual_disks/virt-configuring-shared-volumes-for-vms.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-configuring-shared-volumes-for-vms" class="group scroll-mt-20">Configure shared volumes for virtual machines<a href="#virt-configuring-shared-volumes-for-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Enable high-availability scenarios like Windows Failover Clustering by configuring shared disks to allow multiple virtual machines to access the same storage volume. A shared disk’s volume must be block mode.</p>
      <p>You configure disk sharing by exposing the storage as either of these types:</p>
      <ul>
      <li>An ordinary VM disk</li>
      <li>A logical unit number (LUN) disk with an SCSI connection and raw device mapping, as required for Windows Failover Clustering for shared volumes</li>
      </ul>
      <p>In addition to configuring disk sharing, you can also set an error policy for each ordinary VM disk or LUN disk. The error policy controls how the hypervisor behaves when an input/output error occurs on a disk Read or Write.</p>
      <h2 id="virt-configuring-vm-disk-sharing_virt-configuring-shared-volumes-for-vms" class="group scroll-mt-20">Configuring disk sharing by using virtual machine disks<a href="#virt-configuring-vm-disk-sharing_virt-configuring-shared-volumes-for-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can configure block volumes so that multiple virtual machines (VMs) can share storage.</p>
      <p>The application running on the guest operating system determines the storage option you must configure for the VM. A disk of type <code>disk</code> exposes the volume as an ordinary disk to the VM.</p>
      <p>You can set an error policy for each disk. The error policy controls how the hypervisor behaves when an input/output error occurs while a disk is being written to or read. The default behavior stops the VM and generates a Kubernetes event.</p>
      <p>You can accept the default behavior, or you can set the error policy to one of the following options:</p>
      <ul>
      <li><code>report</code>, which reports the error in the guest.</li>
      <li><code>ignore</code>, which ignores the error. The Read or Write failure is undetected.</li>
      <li><code>enospace</code>, which produces an error indicating that there is not enough disk space.</li>
      </ul>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>
      The volume access mode must be <code>ReadWriteMany</code> (RWX) if the VMs that are sharing disks are running on different nodes.
      <p>If the VMs that are sharing disks are running on the same node, <code>ReadWriteOnce</code> (RWO) volume access mode is sufficient.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Not all filesystem and format combinations support RWX mode. For example, XFS format does not support RWX mode. When multiple VMs write to an XFS-formatted volume, data corruption occurs and only the writing VM can access the data.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>The storage provider must support the required Container Storage Interface (CSI) driver.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create the <code>VirtualMachine</code> manifest for your VM to set the required values, as shown in the following example:
      <CodeBlock code={"apiVersion: kubevirt.io/v1\nkind: VirtualMachine\nmetadata:\n  name: <vm_name>\nspec:\n  template:\n# ...\n    spec:\n      domain:\n        devices:\n          disks:\n          - disk:\n              bus: virtio\n            name: rootdisk\n            errorPolicy: report\n          - disk:\n              bus: virtio\n            name: cluster\n            shareable: true\n          interfaces:\n          - masquerade: {}\n            name: default"} lang="yaml" />
      <ul>
      <li><code>spec.template.spec.domain.devices.disks.errorPolicy</code> defines how the hypervisor should behave when an IO error occurs.</li>
      <li><code>spec.template.spec.domain.devices.disks.shareable</code> defines whether multiple virtual machines (VMs) can use the same underlying disk.</li>
      </ul>
      </li>
      <li>Save the <code>VirtualMachine</code> manifest file to apply your changes.</li>
      </ol>
      <h2 id="virt-configuring-disk-sharing-lun_virt-configuring-shared-volumes-for-vms" class="group scroll-mt-20">Configuring disk sharing by using LUN<a href="#virt-configuring-disk-sharing-lun_virt-configuring-shared-volumes-for-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To secure data on your VM from outside access, you can enable SCSI persistent reservation and configure a LUN-backed virtual machine disk to be shared among multiple virtual machines.</p>
      <p>By enabling the shared option, you can use advanced SCSI commands, such as those required for a Windows failover clustering implementation, for managing the underlying storage.</p>
      <p>When a storage volume is configured as the <code>LUN</code> disk type, a VM can use the volume as a logical unit number (LUN) device. As a result, the VM can deploy and manage the disk by using SCSI commands.</p>
      <p>You reserve a LUN through the SCSI persistent reserve options. To enable the reservation:</p>
      <ol>
      <li>Configure the feature gate option</li>
      <li>Activate the feature gate option on the LUN disk to issue SCSI device-specific input and output controls (IOCTLs) that the VM requires.</li>
      </ol>
      <p>You can set an error policy for each LUN disk. The error policy controls how the hypervisor behaves when an input/output error occurs on a disk Read or Write. The default behavior stops the guest and generates a Kubernetes event.</p>
      <p>For a LUN disk with an SCSi connection and a persistent reservation, as required for Windows Failover Clustering for shared volumes, you set the error policy to <code>report</code>.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You must have cluster administrator privileges to configure the feature gate option.</li>
      <li>
      The volume access mode must be <code>ReadWriteMany</code> (RWX) if the VMs that are sharing disks are running on different nodes.
      <p>If the VMs that are sharing disks are running on the same node, <code>ReadWriteOnce</code> (RWO) volume access mode is sufficient.</p>
      </li>
      <li>The storage provider must support a Container Storage Interface (CSI) driver that uses Fibre Channel (FC), Fibre Channel over Ethernet (FCoE), or iSCSI storage protocols.</li>
      <li>If you are a cluster administrator and intend to configure disk sharing by using LUN, you must enable the cluster’s feature gate on the <code>HyperConverged</code> custom resource (CR).</li>
      <li>Disks that you want to share must be in block mode.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Edit or create the <code>VirtualMachine</code> manifest for your VM to set the required values, as shown in the following example:
      <CodeBlock code={"apiVersion: kubevirt.io/v1\nkind: VirtualMachine\nmetadata:\n  name: vm-0\nspec:\n  template:\n    spec:\n      domain:\n        devices:\n          disks:\n          - disk:\n              bus: sata\n            name: rootdisk\n          - errorPolicy: report\n            lun:\n              bus: scsi\n              reservation: true\n            name: na-shared\n            serial: shared1234\n      volumes:\n      - dataVolume:\n          name: vm-0\n        name: rootdisk\n      - name: na-shared\n        persistentVolumeClaim:\n          claimName: pvc-na-share"} lang="yaml" />
      <ul>
      <li><code>spec.template.spec.domain.devices.disks.errorPolicy</code> defines how the hypervisor should behave when an IO error occurs.</li>
      <li><code>spec.template.spec.domain.devices.disks.lun</code> defines a volume exposed as a LUN device.</li>
      <li><code>spec.template.spec.domain.devices.disks.lun.reservation</code> defines whether the persistent reservation is enabled.</li>
      </ul>
      </li>
      <li>Save the <code>VirtualMachine</code> manifest file to apply your changes.</li>
      </ol>
      <h3 id="virt-configuring-disk-sharing-lun-web_virt-configuring-shared-volumes-for-vms" class="group scroll-mt-20">Configuring disk sharing by using LUN and the web console<a href="#virt-configuring-disk-sharing-lun-web_virt-configuring-shared-volumes-for-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can use the OpenShift Container Platform web console to configure disk sharing by using LUN.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>The cluster administrator must enable the <code>persistentreservation</code> feature gate setting.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Click <strong>Virtualization</strong> -&gt; <strong>VirtualMachines</strong> in the web console.</li>
      <li>Select a VM to open the <strong>VirtualMachine details</strong> page.</li>
      <li>Expand <strong>Storage</strong>.</li>
      <li>On the <strong>Disks</strong> tab, click <strong>Add disk</strong>.</li>
      <li>Specify the <strong>Name</strong>, <strong>Source</strong>, <strong>Size</strong>, <strong>Interface</strong>, and <strong>Storage Class</strong>.</li>
      <li>Select <strong>LUN</strong> as the <strong>Type</strong>.</li>
      <li>Select <strong>Shared access (RWX)</strong> as the <strong>Access Mode</strong>.</li>
      <li>Select <strong>Block</strong> as the <strong>Volume Mode</strong>.</li>
      <li>Expand <strong>Advanced Settings</strong>, and select both checkboxes.</li>
      <li>Click <strong>Save</strong>.</li>
      </ol>
      <h3 id="virt-configuring-disk-sharing-lun-cli_virt-configuring-shared-volumes-for-vms" class="group scroll-mt-20">Configuring disk sharing by using LUN and the CLI<a href="#virt-configuring-disk-sharing-lun-cli_virt-configuring-shared-volumes-for-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can use the command line to configure disk sharing by using LUN.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Edit or create the <code>VirtualMachine</code> manifest for your VM to set the required values, as shown in the following example:
      <CodeBlock code={"apiVersion: kubevirt.io/v1\nkind: VirtualMachine\nmetadata:\n  name: vm-0\nspec:\n  template:\n    spec:\n      domain:\n        devices:\n          disks:\n          - disk:\n              bus: sata\n            name: rootdisk\n          - errorPolicy: report\n            lun:\n              bus: scsi\n              reservation: true\n            name: na-shared\n            serial: shared1234\n      volumes:\n      - dataVolume:\n          name: vm-0\n        name: rootdisk\n      - name: na-shared\n        persistentVolumeClaim:\n          claimName: pvc-na-share"} lang="yaml" />
      <ul>
      <li><code>spec.template.spec.domain.devices.disks.errorPolicy</code> defines how the hypervisor should behave when an IO error occurs.</li>
      <li><code>spec.template.spec.domain.devices.disks.lun</code> defines a volume exposed as a LUN device.</li>
      <li><code>spec.template.spec.domain.devices.disks.lun.reservation</code> defines whether the persistent reservation is enabled.</li>
      </ul>
      </li>
      <li>Save the <code>VirtualMachine</code> manifest file to apply your changes.</li>
      </ol>
      <h2 id="virt-enabling-persistentreservation-feature-gate_virt-configuring-shared-volumes-for-vms" class="group scroll-mt-20">Enabling the PersistentReservation feature gate<a href="#virt-enabling-persistentreservation-feature-gate_virt-configuring-shared-volumes-for-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can enable the SCSI <code>persistentReservation</code> feature gate and allow a LUN-backed block mode virtual machine (VM) disk to be shared among multiple virtual machines.</p>
      <p>The <code>persistentReservation</code> feature gate is disabled by default. You can enable the <code>persistentReservation</code> feature gate by using the web console or the command line.</p>
      <h3 id="virt-enabling-persistentreservation-feature-gate-prereqs_virt-configuring-shared-volumes-for-vms" class="group scroll-mt-20">Prerequisites<a href="#virt-enabling-persistentreservation-feature-gate-prereqs_virt-configuring-shared-volumes-for-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <ul>
      <li>Cluster administrator privileges are required.</li>
      <li>The volume access mode <code>ReadWriteMany</code> (RWX) is required if the VMs that are sharing disks are running on different nodes. If the VMs that are sharing disks are running on the same node, the <code>ReadWriteOnce</code> (RWO) volume access mode is sufficient.</li>
      <li>The storage provider must support a Container Storage Interface (CSI) driver that uses Fibre Channel (FC), Fibre Channel over Ethernet (FCoE), or iSCSI storage protocols.</li>
      </ul>
      <h3 id="virt-enabling-persistentreservation-feature-gate-web_virt-configuring-shared-volumes-for-vms" class="group scroll-mt-20">Enabling the PersistentReservation feature gate by using the web console<a href="#virt-enabling-persistentreservation-feature-gate-web_virt-configuring-shared-volumes-for-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You must enable the PersistentReservation feature gate to allow a LUN-backed block mode virtual machine (VM) disk to be shared among multiple virtual machines. Enabling the feature gate requires cluster administrator privileges.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Click <strong>Virtualization</strong> -&gt; <strong>Settings</strong> in the web console.</li>
      <li>Select <strong>Cluster</strong>.</li>
      <li>Expand <strong>SCSI persistent reservation</strong> and set <strong>Enable persistent reservation</strong> to on.</li>
      </ol>
      <h3 id="virt-enabling-persistentreservation-feature-gate-cli_virt-configuring-shared-volumes-for-vms" class="group scroll-mt-20">Enabling the PersistentReservation feature gate by using the CLI<a href="#virt-enabling-persistentreservation-feature-gate-cli_virt-configuring-shared-volumes-for-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can enable the <code>persistentReservation</code> feature gate by using the command line. Enabling the feature gate requires cluster administrator privileges.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have installed the OpenShift CLI (<code>oc</code>).</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Enable the <code>persistentReservation</code> feature gate by running the following command:
      <CodeBlock code={"$ oc patch {{ HCOCliKind }} kubevirt-hyperconverged -n {{ CNVNamespace }} --type json -p \\\n'[{\"op\":\"replace\",\"path\":\"/spec/featureGates/persistentReservation\", \"value\": true}]'"} lang="terminal" />
      </li>
      </ol>
      <h2 id="additional-resources_virt-configuring-shared-volumes-for-vms" class="group scroll-mt-20">Additional resources<a href="#additional-resources_virt-configuring-shared-volumes-for-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="https://www.qemu.org/docs/master/interop/pr-helper.html">Persistent reservation helper protocol</a></li>
      <li><a href="https://learn.microsoft.com/en-us/windows-server/failover-clustering/failover-clustering-overview">Failover Clustering in Windows Server and Azure Stack HCI</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
