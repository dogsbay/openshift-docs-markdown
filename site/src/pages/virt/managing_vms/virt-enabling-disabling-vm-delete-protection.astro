---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/managing_vms/virt-enabling-disabling-vm-delete-protection.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-enabling-disabling-vm-delete-protection","text":"Enable or disable virtual machine delete protection"},{"depth":2,"slug":"virt-enabling-disabling-vm-delete-protection-web_virt-enabling-disabling-vm-delete-protection","text":"Enabling or disabling virtual machine delete protection by using the web console"},{"depth":2,"slug":"virt-enabling-disabling-vm-delete-protection-cli_virt-enabling-disabling-vm-delete-protection","text":"Enabling or disabling VM delete protection by using the CLI"},{"depth":2,"slug":"virt-removing-vm-delete-protection_virt-enabling-disabling-vm-delete-protection","text":"Removing the VM delete protection option"},{"depth":2,"slug":"additional-resources_virt-enabling-disabling-vm-delete-protection","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/managing_vms/virt-enabling-disabling-vm-delete-protection";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Enable or disable virtual machine delete protection";
const description = "You can prevent accidental deletion of a virtual machine (VM) by enabling delete protection. If delete protection is enabled, you must disable it before…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/managing_vms"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Enable or disable virtual machine delete protection {#virt-enabling-disabling-vm-delete-protection}\n\nYou can prevent accidental deletion of a virtual machine (VM) by enabling delete protection. If delete protection is enabled, you must disable it before you can delete that VM.\n\nYou enable or disable delete protection from either the command line or the VM’s **VirtualMachine details** page in the OpenShift Container Platform web console. The option is disabled by default.\n\nYou can also choose to remove availability of the delete protection option for any VMs in a cluster you administer. In this case, VMs with the feature already enabled retain the protection, while the option is unavailable for any newly created VMs.\n\n## Enabling or disabling virtual machine delete protection by using the web console {#virt-enabling-disabling-vm-delete-protection-web_virt-enabling-disabling-vm-delete-protection}\n\nTo prevent the inadvertent deletion of a virtual machine (VM), you can enable VM delete protection by using the OpenShift Container Platform web console. You can also disable delete protection for a VM.\n\nBy default, delete protection is not enabled for VMs. You must set the option for each individual VM.\n\n**Procedure**\n\n1. From the OpenShift Container Platform web console, choose your view:\n\n   - For a virtualization-focused view, select **Administrator** → **Virtualization** → **VirtualMachines**.\n   - For a general view, navigate to **Virtualization** → **VirtualMachines**.\n2. From the **VirtualMachines** list, select the VM whose delete protection you want to enable or disable.\n3. Click the **Configuration** tab.\n4. In the **VirtualMachines details**, choose to enable or disable the protection as follows:\n\n   - To enable the protection:\n\n     1. Set the **Deletion protection** switch to **On**.\n     2. Click **Enable** to confirm the protection.\n   - To disable the protection:\n\n     1. Set the **Deletion protection** switch to **Off**.\n     2. Click **Disable** to disable the protection.\n\n## Enabling or disabling VM delete protection by using the CLI {#virt-enabling-disabling-vm-delete-protection-cli_virt-enabling-disabling-vm-delete-protection}\n\nTo prevent the inadvertent deletion of a virtual machine (VM), you can enable VM delete protection by using the command line. You can also disable delete protection for a VM.\n\nBy default, delete protection is not enabled for VMs. You must set the option for each individual VM.\n\n**Prerequisites**\n\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n- Enable delete protection for a VM by running the following command:\n\n  ```terminal\n  $ oc patch vm <vm_name> --type merge -p '{\"metadata\":{\"labels\":{\"kubevirt.io/vm-delete-protection\":\"True\"}}}' -n <namespace>\n  ```\n- Disable delete protection for a VM by running the following command:\n\n  ```terminal\n  $ oc patch vm <vm_name> --type json -p '[{\"op\": \"remove\", \"path\": \"/metadata/labels/kubevirt.io~1vm-delete-protection\"}]' -n <namespace>\n  ```\n\n## Removing the VM delete protection option {#virt-removing-vm-delete-protection_virt-enabling-disabling-vm-delete-protection}\n\nWhen you enable delete protection on a virtual machine (VM), you ensure that the VM cannot be inadvertently deleted. You can also disable the protection for a VM.\n\nAs a cluster administrator, you can choose not to make the VM delete protection option available. VMs with delete protection already enabled retain that setting; for any new VMs that are created, enabling the option is not allowed.\n\nYou can remove the delete protection option by establishing a validation admission policy for the cluster and then creating the necessary binding to use the policy in the cluster.\n\n**Prerequisites**\n\n- You must have cluster administrator privileges.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Create the validation admission policy, as shown in the following example:\n\n   ```yaml\n   apiVersion: admissionregistration.k8s.io/v1\n   kind: ValidatingAdmissionPolicy\n   metadata:\n     name: \"disable-vm-delete-protection\"\n   spec:\n     failurePolicy: Fail\n     matchConstraints:\n       resourceRules:\n       - apiGroups:   [\"kubevirt.io\"]\n         apiVersions: [\"*\"]\n         operations:  [\"UPDATE\", \"CREATE\"]\n         resources:   [\"virtualmachines\"]\n     variables:\n       - expression: string('kubevirt.io/vm-delete-protection')\n         name: vmDeleteProtectionLabel\n     validations:\n     - expression: >-\n         !has(object.metadata.labels) ||\n         !object.metadata.labels.exists(label, label == variables.vmDeleteProtectionLabel) ||\n         has(oldObject.metadata.labels) &&\n         oldObject.metadata.labels.exists(label, label == variables.vmDeleteProtectionLabel)\n       message: \"Virtual Machine delete protection feature is disabled\"\n   ```\n2. Apply the validation admission policy to the cluster:\n\n   ```terminal\n   $ oc apply -f disable-vm-delete-protection.yaml\n   ```\n3. Create the validation admission policy binding, as shown in the following example:\n\n   ```yaml\n   apiVersion: admissionregistration.k8s.io/v1\n   kind: ValidatingAdmissionPolicyBinding\n   metadata:\n     name: \"disable-vm-delete-protection-binding\"\n   spec:\n     policyName: \"disable-vm-delete-protection\"\n     validationActions: [Deny]\n     matchResources:\n   ```\n4. Apply the validation admission policy binding to the cluster:\n\n   ```terminal\n   $ oc apply -f disable-vm-delete-protection-binding.yaml\n   ```\n\n## Additional resources {#additional-resources_virt-enabling-disabling-vm-delete-protection}\n\n- [Enabling or disabling virtual machine delete protection by using the web console](/openshift-docs-markdown/virt/managing_vms/virt-enabling-disabling-vm-delete-protection#virt-enabling-disabling-vm-delete-protection-web_virt-enabling-disabling-vm-delete-protection)\n- [Enabling or disabling virtual machine delete protection by using the CLI](/openshift-docs-markdown/virt/managing_vms/virt-enabling-disabling-vm-delete-protection#virt-enabling-disabling-vm-delete-protection-cli_virt-enabling-disabling-vm-delete-protection)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/managing_vms/virt-enabling-disabling-vm-delete-protection.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-enabling-disabling-vm-delete-protection" class="group scroll-mt-20">Enable or disable virtual machine delete protection<a href="#virt-enabling-disabling-vm-delete-protection" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can prevent accidental deletion of a virtual machine (VM) by enabling delete protection. If delete protection is enabled, you must disable it before you can delete that VM.</p>
      <p>You enable or disable delete protection from either the command line or the VM’s <strong>VirtualMachine details</strong> page in the OpenShift Container Platform web console. The option is disabled by default.</p>
      <p>You can also choose to remove availability of the delete protection option for any VMs in a cluster you administer. In this case, VMs with the feature already enabled retain the protection, while the option is unavailable for any newly created VMs.</p>
      <h2 id="virt-enabling-disabling-vm-delete-protection-web_virt-enabling-disabling-vm-delete-protection" class="group scroll-mt-20">Enabling or disabling virtual machine delete protection by using the web console<a href="#virt-enabling-disabling-vm-delete-protection-web_virt-enabling-disabling-vm-delete-protection" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To prevent the inadvertent deletion of a virtual machine (VM), you can enable VM delete protection by using the OpenShift Container Platform web console. You can also disable delete protection for a VM.</p>
      <p>By default, delete protection is not enabled for VMs. You must set the option for each individual VM.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      From the OpenShift Container Platform web console, choose your view:
      <ul>
      <li>For a virtualization-focused view, select <strong>Administrator</strong> → <strong>Virtualization</strong> → <strong>VirtualMachines</strong>.</li>
      <li>For a general view, navigate to <strong>Virtualization</strong> → <strong>VirtualMachines</strong>.</li>
      </ul>
      </li>
      <li>From the <strong>VirtualMachines</strong> list, select the VM whose delete protection you want to enable or disable.</li>
      <li>Click the <strong>Configuration</strong> tab.</li>
      <li>
      In the <strong>VirtualMachines details</strong>, choose to enable or disable the protection as follows:
      <ul>
      <li>
      To enable the protection:
      <ol>
      <li>Set the <strong>Deletion protection</strong> switch to <strong>On</strong>.</li>
      <li>Click <strong>Enable</strong> to confirm the protection.</li>
      </ol>
      </li>
      <li>
      To disable the protection:
      <ol>
      <li>Set the <strong>Deletion protection</strong> switch to <strong>Off</strong>.</li>
      <li>Click <strong>Disable</strong> to disable the protection.</li>
      </ol>
      </li>
      </ul>
      </li>
      </ol>
      <h2 id="virt-enabling-disabling-vm-delete-protection-cli_virt-enabling-disabling-vm-delete-protection" class="group scroll-mt-20">Enabling or disabling VM delete protection by using the CLI<a href="#virt-enabling-disabling-vm-delete-protection-cli_virt-enabling-disabling-vm-delete-protection" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To prevent the inadvertent deletion of a virtual machine (VM), you can enable VM delete protection by using the command line. You can also disable delete protection for a VM.</p>
      <p>By default, delete protection is not enabled for VMs. You must set the option for each individual VM.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have installed the OpenShift CLI (<code>oc</code>).</li></ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Enable delete protection for a VM by running the following command:
      <CodeBlock code={"$ oc patch vm <vm_name> --type merge -p '{\"metadata\":{\"labels\":{\"kubevirt.io/vm-delete-protection\":\"True\"}}}' -n <namespace>"} lang="terminal" />
      </li>
      <li>
      Disable delete protection for a VM by running the following command:
      <CodeBlock code={"$ oc patch vm <vm_name> --type json -p '[{\"op\": \"remove\", \"path\": \"/metadata/labels/kubevirt.io~1vm-delete-protection\"}]' -n <namespace>"} lang="terminal" />
      </li>
      </ul>
      <h2 id="virt-removing-vm-delete-protection_virt-enabling-disabling-vm-delete-protection" class="group scroll-mt-20">Removing the VM delete protection option<a href="#virt-removing-vm-delete-protection_virt-enabling-disabling-vm-delete-protection" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">When you enable delete protection on a virtual machine (VM), you ensure that the VM cannot be inadvertently deleted. You can also disable the protection for a VM.</p>
      <p>As a cluster administrator, you can choose not to make the VM delete protection option available. VMs with delete protection already enabled retain that setting; for any new VMs that are created, enabling the option is not allowed.</p>
      <p>You can remove the delete protection option by establishing a validation admission policy for the cluster and then creating the necessary binding to use the policy in the cluster.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You must have cluster administrator privileges.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create the validation admission policy, as shown in the following example:
      <CodeBlock code={"apiVersion: admissionregistration.k8s.io/v1\nkind: ValidatingAdmissionPolicy\nmetadata:\n  name: \"disable-vm-delete-protection\"\nspec:\n  failurePolicy: Fail\n  matchConstraints:\n    resourceRules:\n    - apiGroups:   [\"kubevirt.io\"]\n      apiVersions: [\"*\"]\n      operations:  [\"UPDATE\", \"CREATE\"]\n      resources:   [\"virtualmachines\"]\n  variables:\n    - expression: string('kubevirt.io/vm-delete-protection')\n      name: vmDeleteProtectionLabel\n  validations:\n  - expression: >-\n      !has(object.metadata.labels) ||\n      !object.metadata.labels.exists(label, label == variables.vmDeleteProtectionLabel) ||\n      has(oldObject.metadata.labels) &&\n      oldObject.metadata.labels.exists(label, label == variables.vmDeleteProtectionLabel)\n    message: \"Virtual Machine delete protection feature is disabled\""} lang="yaml" />
      </li>
      <li>
      Apply the validation admission policy to the cluster:
      <CodeBlock code={"$ oc apply -f disable-vm-delete-protection.yaml"} lang="terminal" />
      </li>
      <li>
      Create the validation admission policy binding, as shown in the following example:
      <CodeBlock code={"apiVersion: admissionregistration.k8s.io/v1\nkind: ValidatingAdmissionPolicyBinding\nmetadata:\n  name: \"disable-vm-delete-protection-binding\"\nspec:\n  policyName: \"disable-vm-delete-protection\"\n  validationActions: [Deny]\n  matchResources:"} lang="yaml" />
      </li>
      <li>
      Apply the validation admission policy binding to the cluster:
      <CodeBlock code={"$ oc apply -f disable-vm-delete-protection-binding.yaml"} lang="terminal" />
      </li>
      </ol>
      <h2 id="additional-resources_virt-enabling-disabling-vm-delete-protection" class="group scroll-mt-20">Additional resources<a href="#additional-resources_virt-enabling-disabling-vm-delete-protection" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/virt/managing_vms/virt-enabling-disabling-vm-delete-protection#virt-enabling-disabling-vm-delete-protection-web_virt-enabling-disabling-vm-delete-protection">Enabling or disabling virtual machine delete protection by using the web console</a></li>
      <li><a href="/openshift-docs-markdown/virt/managing_vms/virt-enabling-disabling-vm-delete-protection#virt-enabling-disabling-vm-delete-protection-cli_virt-enabling-disabling-vm-delete-protection">Enabling or disabling virtual machine delete protection by using the CLI</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
