---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/managing_vms/virt-install-virtio-drivers-on-windows-vms.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"virt-install-virtio-drivers-on-windows-vms","text":"Install VirtIO drivers on Windows VMs"},{"depth":2,"slug":"virt-attaching-virtio-disk-to-windows_virt-install-virtio-drivers-on-windows-vms","text":"Attaching VirtIO container disk to Windows VMs during installation"},{"depth":2,"slug":"virt-attaching-virtio-disk-to-windows-existing_virt-install-virtio-drivers-on-windows-vms","text":"Attaching VirtIO container disk to an existing Windows VM"},{"depth":2,"slug":"virt-adding-container-disk-as-cd_virt-install-virtio-drivers-on-windows-vms","text":"Installing VirtIO drivers from a container disk added as a SATA CD drive"},{"depth":2,"slug":"virt-installing-virtio-drivers-installing-windows_virt-install-virtio-drivers-on-windows-vms","text":"Installing VirtIO drivers during Windows installation"},{"depth":2,"slug":"virt-installing-virtio-drivers-existing-windows_virt-install-virtio-drivers-on-windows-vms","text":"Installing VirtIO drivers from a SATA CD drive on an existing Windows VM"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/managing_vms/virt-install-virtio-drivers-on-windows-vms";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Install VirtIO drivers on Windows VMs";
const description = "VirtIO drivers are paravirtualized device drivers required for Microsoft Windows virtual machines (VMs) to run in OpenShift Virtualization. The drivers…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/managing_vms"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Install VirtIO drivers on Windows VMs {#virt-install-virtio-drivers-on-windows-vms}\n\nVirtIO drivers are paravirtualized device drivers required for Microsoft Windows virtual machines (VMs) to run in OpenShift Virtualization. The drivers are shipped with the rest of the images and do not require a separate download.\n\nThe `container-native-virtualization/virtio-win` container disk must be attached to the VM as a SATA CD drive to enable driver installation. You can install VirtIO drivers during Windows installation or add them to an existing Windows installation.\n\nAfter the drivers are installed, the `container-native-virtualization/virtio-win` container disk can be removed from the VM.\n\n**Supported drivers**\n\n| Driver name | Hardware ID | Description |\n| --- | --- | --- |\n| **viostor** | VEN_1AF4&DEV_1001, VEN_1AF4&DEV_1042 | The block driver. Sometimes labeled as an **SCSI Controller** in the **Other devices** group. |\n| **viorng** | VEN_1AF4&DEV_1005, VEN_1AF4&DEV_1044 | The entropy source driver. Sometimes labeled as a **PCI Device** in the **Other devices** group. |\n| **NetKVM** | VEN_1AF4&DEV_1000, VEN_1AF4&DEV_1041 | The network driver. Sometimes labeled as an **Ethernet Controller** in the **Other devices** group. Available only if a VirtIO NIC is configured. |\n\n## Attaching VirtIO container disk to Windows VMs during installation {#virt-attaching-virtio-disk-to-windows_virt-install-virtio-drivers-on-windows-vms}\n\nYou must attach the VirtIO container disk to the Windows VM to install the necessary Windows drivers. This can be done during creation of the VM.\n\n**Procedure**\n\n1. When creating a Windows VM from a template, click **Customize VirtualMachine**.\n2. Select **Mount Windows drivers disk**.\n3. Click the **Customize VirtualMachine parameters**.\n4. Click **Create VirtualMachine**.\n\n**Result**\n\nAfter the VM is created, the `virtio-win` SATA CD disk will be attached to the VM.\n\n## Attaching VirtIO container disk to an existing Windows VM {#virt-attaching-virtio-disk-to-windows-existing_virt-install-virtio-drivers-on-windows-vms}\n\nYou must attach the VirtIO container disk to the Windows VM to install the necessary Windows drivers. This can be done to an existing VM.\n\n**Procedure**\n\n1. Navigate to the existing Windows VM, and click **Actions** → **Stop**.\n2. Go to **VM Details** → **Configuration** → **Storage**.\n3. Select the **Mount Windows drivers disk** checkbox.\n4. Click **Save**.\n5. Start the VM, and connect to a graphical console.\n\n## Installing VirtIO drivers from a container disk added as a SATA CD drive {#virt-adding-container-disk-as-cd_virt-install-virtio-drivers-on-windows-vms}\n\nYou can install VirtIO drivers from a container disk that you add to a Windows virtual machine (VM) as a SATA CD drive.\n\n> [!TIP]\n> You do not need to download the `container-native-virtualization/virtio-win-rhel9` container disk. If the container disk is not present, the cluster downloads it from the Red Hat registry. However, to reduce the installation time, you can download the container disk in advance from the [Red Hat Ecosystem Catalog](https://catalog.redhat.com/en/software/containers/container-native-virtualization/virtio-win-rhel9/633ffae83be061d37b2770f6).\n\n**Prerequisites**\n\n- You must have access to the Red Hat registry or to the downloaded `container-native-virtualization/virtio-win` container disk in a restricted environment.\n- You have installed the `virtctl` CLI.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Add the `container-native-virtualization/virtio-win` container disk as a CD drive by editing the `VirtualMachine` manifest:\n\n   ```yaml\n   # ...\n   spec:\n     domain:\n       devices:\n         disks:\n           - name: virtiocontainerdisk\n             bootOrder: 2\n             cdrom:\n               bus: sata\n   volumes:\n     - containerDisk:\n         image: registry.redhat.io/container-native-virtualization/virtio-win-rhel9:v4.22\n       name: virtiocontainerdisk\n   ```\n\n   OpenShift Virtualization boots the VM disks in the order defined in the `VirtualMachine` manifest. You can either define other VM disks that boot before the `container-native-virtualization/virtio-win` container disk, or use the optional `bootOrder` parameter to ensure the VM boots from the correct disk. If you configure the boot order for a disk, you must configure the boot order for the other disks.\n2. Apply the changes:\n\n   - If the VM is not running, run the following command:\n\n     ```terminal\n     $ virtctl start <vm> -n <namespace>\n     ```\n   - If the VM is running, reboot the VM or run the following command:\n\n     ```terminal\n     $ oc apply -f <vm.yaml>\n     ```\n3. After the VM has started, install the VirtIO drivers from the SATA CD drive.\n\n## Installing VirtIO drivers during Windows installation {#virt-installing-virtio-drivers-installing-windows_virt-install-virtio-drivers-on-windows-vms}\n\nYou can install the VirtIO drivers while installing Windows on a virtual machine (VM).\n\n> [!NOTE]\n> This procedure uses a generic approach to the Windows installation and the installation method might differ between versions of Windows. See the documentation for the version of Windows that you are installing.\n\n**Prerequisites**\n\n- A storage device containing the `virtio` drivers must be attached to the VM.\n\n**Procedure**\n\n1. In the Windows operating system, use the `File Explorer` to navigate to the `virtio-win` CD drive.\n2. Double-click the drive to run the appropriate installer for your VM.\n\n   For a 64-bit vCPU, select the `virtio-win-gt-x64` installer. 32-bit vCPUs are no longer supported.\n3. Optional: During the **Custom Setup** step of the installer, select the device drivers you want to install. The recommended driver set is selected by default.\n4. After the installation is complete, select **Finish**.\n5. Reboot the VM.\n\n**Verification**\n\n1. Open the system disk on the PC. This is typically `C:`.\n2. Navigate to **Program Files** → **Virtio-Win**.\n\nIf the **Virtio-Win** directory is present and contains a sub-directory for each driver, the installation was successful.\n\n## Installing VirtIO drivers from a SATA CD drive on an existing Windows VM {#virt-installing-virtio-drivers-existing-windows_virt-install-virtio-drivers-on-windows-vms}\n\nYou can install the VirtIO drivers from a SATA CD drive on an existing Windows virtual machine (VM).\n\n> [!NOTE]\n> This procedure uses a generic approach to adding drivers to Windows. See the installation documentation for your version of Windows for specific installation steps.\n\n**Prerequisites**\n\n- A storage device containing the virtio drivers must be attached to the VM as a SATA CD drive.\n\n**Procedure**\n\n1. Start the VM and connect to a graphical console.\n2. Log in to a Windows user session.\n3. Open **Device Manager** and expand **Other devices** to list any **Unknown device**.\n\n   1. Open the **Device Properties** to identify the unknown device.\n   2. Right-click the device and select **Properties**.\n   3. Click the **Details** tab and select **Hardware Ids** in the **Property** list.\n   4. Compare the **Value** for the **Hardware Ids** with the supported VirtIO drivers.\n4. Right-click the device and select **Update Driver Software**.\n5. Click **Browse my computer for driver software** and browse to the attached SATA CD drive, where the VirtIO drivers are located. The drivers are arranged hierarchically according to their driver type, operating system, and CPU architecture.\n6. Click **Next** to install the driver.\n7. Repeat this process for all the necessary VirtIO drivers.\n8. After the driver installs, click **Close** to close the window.\n9. Reboot the VM to complete the driver installation.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/managing_vms/virt-install-virtio-drivers-on-windows-vms.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-install-virtio-drivers-on-windows-vms" class="group scroll-mt-20">Install VirtIO drivers on Windows VMs<a href="#virt-install-virtio-drivers-on-windows-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">VirtIO drivers are paravirtualized device drivers required for Microsoft Windows virtual machines (VMs) to run in OpenShift Virtualization. The drivers are shipped with the rest of the images and do not require a separate download.</p>
      <p>The <code>container-native-virtualization/virtio-win</code> container disk must be attached to the VM as a SATA CD drive to enable driver installation. You can install VirtIO drivers during Windows installation or add them to an existing Windows installation.</p>
      <p>After the drivers are installed, the <code>container-native-virtualization/virtio-win</code> container disk can be removed from the VM.</p>
      <p><strong>Supported drivers</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Driver name</TableHead>
      <TableHead>Hardware ID</TableHead>
      <TableHead>Description</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><strong>viostor</strong></TableCell>
      <TableCell>VEN_1AF4&amp;DEV_1001, VEN_1AF4&amp;DEV_1042</TableCell>
      <TableCell>The block driver. Sometimes labeled as an <strong>SCSI Controller</strong> in the <strong>Other devices</strong> group.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>viorng</strong></TableCell>
      <TableCell>VEN_1AF4&amp;DEV_1005, VEN_1AF4&amp;DEV_1044</TableCell>
      <TableCell>The entropy source driver. Sometimes labeled as a <strong>PCI Device</strong> in the <strong>Other devices</strong> group.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>NetKVM</strong></TableCell>
      <TableCell>VEN_1AF4&amp;DEV_1000, VEN_1AF4&amp;DEV_1041</TableCell>
      <TableCell>The network driver. Sometimes labeled as an <strong>Ethernet Controller</strong> in the <strong>Other devices</strong> group. Available only if a VirtIO NIC is configured.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="virt-attaching-virtio-disk-to-windows_virt-install-virtio-drivers-on-windows-vms" class="group scroll-mt-20">Attaching VirtIO container disk to Windows VMs during installation<a href="#virt-attaching-virtio-disk-to-windows_virt-install-virtio-drivers-on-windows-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You must attach the VirtIO container disk to the Windows VM to install the necessary Windows drivers. This can be done during creation of the VM.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>When creating a Windows VM from a template, click <strong>Customize VirtualMachine</strong>.</li>
      <li>Select <strong>Mount Windows drivers disk</strong>.</li>
      <li>Click the <strong>Customize VirtualMachine parameters</strong>.</li>
      <li>Click <strong>Create VirtualMachine</strong>.</li>
      </ol>
      <p><strong>Result</strong></p>
      <p>After the VM is created, the <code>virtio-win</code> SATA CD disk will be attached to the VM.</p>
      <h2 id="virt-attaching-virtio-disk-to-windows-existing_virt-install-virtio-drivers-on-windows-vms" class="group scroll-mt-20">Attaching VirtIO container disk to an existing Windows VM<a href="#virt-attaching-virtio-disk-to-windows-existing_virt-install-virtio-drivers-on-windows-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You must attach the VirtIO container disk to the Windows VM to install the necessary Windows drivers. This can be done to an existing VM.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to the existing Windows VM, and click <strong>Actions</strong> → <strong>Stop</strong>.</li>
      <li>Go to <strong>VM Details</strong> → <strong>Configuration</strong> → <strong>Storage</strong>.</li>
      <li>Select the <strong>Mount Windows drivers disk</strong> checkbox.</li>
      <li>Click <strong>Save</strong>.</li>
      <li>Start the VM, and connect to a graphical console.</li>
      </ol>
      <h2 id="virt-adding-container-disk-as-cd_virt-install-virtio-drivers-on-windows-vms" class="group scroll-mt-20">Installing VirtIO drivers from a container disk added as a SATA CD drive<a href="#virt-adding-container-disk-as-cd_virt-install-virtio-drivers-on-windows-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can install VirtIO drivers from a container disk that you add to a Windows virtual machine (VM) as a SATA CD drive.</p>
      <Alert variant="tip" class="my-4">
        <AlertTitle>Tip</AlertTitle>
        <AlertDescription>
          <p>You do not need to download the <code>container-native-virtualization/virtio-win-rhel9</code> container disk. If the container disk is not present, the cluster downloads it from the Red Hat registry. However, to reduce the installation time, you can download the container disk in advance from the <a href="https://catalog.redhat.com/en/software/containers/container-native-virtualization/virtio-win-rhel9/633ffae83be061d37b2770f6">Red Hat Ecosystem Catalog</a>.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You must have access to the Red Hat registry or to the downloaded <code>container-native-virtualization/virtio-win</code> container disk in a restricted environment.</li>
      <li>You have installed the <code>virtctl</code> CLI.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Add the <code>container-native-virtualization/virtio-win</code> container disk as a CD drive by editing the <code>VirtualMachine</code> manifest:
      <CodeBlock code={"# ...\nspec:\n  domain:\n    devices:\n      disks:\n        - name: virtiocontainerdisk\n          bootOrder: 2\n          cdrom:\n            bus: sata\nvolumes:\n  - containerDisk:\n      image: registry.redhat.io/container-native-virtualization/virtio-win-rhel9:v4.22\n    name: virtiocontainerdisk"} lang="yaml" />
      <p>OpenShift Virtualization boots the VM disks in the order defined in the <code>VirtualMachine</code> manifest. You can either define other VM disks that boot before the <code>container-native-virtualization/virtio-win</code> container disk, or use the optional <code>bootOrder</code> parameter to ensure the VM boots from the correct disk. If you configure the boot order for a disk, you must configure the boot order for the other disks.</p>
      </li>
      <li>
      Apply the changes:
      <ul>
      <li>
      If the VM is not running, run the following command:
      <CodeBlock code={"$ virtctl start <vm> -n <namespace>"} lang="terminal" />
      </li>
      <li>
      If the VM is running, reboot the VM or run the following command:
      <CodeBlock code={"$ oc apply -f <vm.yaml>"} lang="terminal" />
      </li>
      </ul>
      </li>
      <li>After the VM has started, install the VirtIO drivers from the SATA CD drive.</li>
      </ol>
      <h2 id="virt-installing-virtio-drivers-installing-windows_virt-install-virtio-drivers-on-windows-vms" class="group scroll-mt-20">Installing VirtIO drivers during Windows installation<a href="#virt-installing-virtio-drivers-installing-windows_virt-install-virtio-drivers-on-windows-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can install the VirtIO drivers while installing Windows on a virtual machine (VM).</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>This procedure uses a generic approach to the Windows installation and the installation method might differ between versions of Windows. See the documentation for the version of Windows that you are installing.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul><li>A storage device containing the <code>virtio</code> drivers must be attached to the VM.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the Windows operating system, use the <code>File Explorer</code> to navigate to the <code>virtio-win</code> CD drive.</li>
      <li>
      Double-click the drive to run the appropriate installer for your VM.
      <p>For a 64-bit vCPU, select the <code>virtio-win-gt-x64</code> installer. 32-bit vCPUs are no longer supported.</p>
      </li>
      <li>Optional: During the <strong>Custom Setup</strong> step of the installer, select the device drivers you want to install. The recommended driver set is selected by default.</li>
      <li>After the installation is complete, select <strong>Finish</strong>.</li>
      <li>Reboot the VM.</li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>Open the system disk on the PC. This is typically <code>C:</code>.</li>
      <li>Navigate to <strong>Program Files</strong> → <strong>Virtio-Win</strong>.</li>
      </ol>
      <p>If the <strong>Virtio-Win</strong> directory is present and contains a sub-directory for each driver, the installation was successful.</p>
      <h2 id="virt-installing-virtio-drivers-existing-windows_virt-install-virtio-drivers-on-windows-vms" class="group scroll-mt-20">Installing VirtIO drivers from a SATA CD drive on an existing Windows VM<a href="#virt-installing-virtio-drivers-existing-windows_virt-install-virtio-drivers-on-windows-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can install the VirtIO drivers from a SATA CD drive on an existing Windows virtual machine (VM).</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>This procedure uses a generic approach to adding drivers to Windows. See the installation documentation for your version of Windows for specific installation steps.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul><li>A storage device containing the virtio drivers must be attached to the VM as a SATA CD drive.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Start the VM and connect to a graphical console.</li>
      <li>Log in to a Windows user session.</li>
      <li>
      Open <strong>Device Manager</strong> and expand <strong>Other devices</strong> to list any <strong>Unknown device</strong>.
      <ol>
      <li>Open the <strong>Device Properties</strong> to identify the unknown device.</li>
      <li>Right-click the device and select <strong>Properties</strong>.</li>
      <li>Click the <strong>Details</strong> tab and select <strong>Hardware Ids</strong> in the <strong>Property</strong> list.</li>
      <li>Compare the <strong>Value</strong> for the <strong>Hardware Ids</strong> with the supported VirtIO drivers.</li>
      </ol>
      </li>
      <li>Right-click the device and select <strong>Update Driver Software</strong>.</li>
      <li>Click <strong>Browse my computer for driver software</strong> and browse to the attached SATA CD drive, where the VirtIO drivers are located. The drivers are arranged hierarchically according to their driver type, operating system, and CPU architecture.</li>
      <li>Click <strong>Next</strong> to install the driver.</li>
      <li>Repeat this process for all the necessary VirtIO drivers.</li>
      <li>After the driver installs, click <strong>Close</strong> to close the window.</li>
      <li>Reboot the VM to complete the driver installation.</li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
