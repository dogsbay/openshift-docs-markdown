---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/managing_vms/virt-using-vtpm-devices.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-using-vtpm-devices","text":"Use a virtual Trusted Platform Module (vTPM) device"},{"depth":2,"slug":"virt-about-vtpm-devices_virt-using-vtpm-devices","text":"About vTPM devices"},{"depth":2,"slug":"virt-adding-vtpm-to-vm_virt-using-vtpm-devices","text":"Adding a vTPM device to a virtual machine"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/managing_vms/virt-using-vtpm-devices";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Use a virtual Trusted Platform Module (vTPM) device";
const description = "To run Windows 11 or other workloads that require a Trusted Platform Module, you can add a virtual TPM (vTPM) to a new or existing virtual machine. Enable…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/managing_vms"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Use a virtual Trusted Platform Module (vTPM) device {#virt-using-vtpm-devices}\n\nTo run Windows 11 or other workloads that require a Trusted Platform Module, you can add a virtual TPM (vTPM) to a new or existing virtual machine. Enable this by editing the `VirtualMachine` or `VirtualMachineInstance` manifest.\n\n> [!IMPORTANT]\n> With OpenShift Virtualization 4.18 and newer, you can export virtual machines (VMs) with attached vTPM devices, create snapshots of these VMs, and restore VMs from these snapshots. However, cloning a VM with a vTPM device attached to it or creating a new VM from its snapshot is not supported.\n\n## About vTPM devices {#virt-about-vtpm-devices_virt-using-vtpm-devices}\n\nA virtual Trusted Platform Module (vTPM) device functions like a physical Trusted Platform Module (TPM) hardware chip. You can use a vTPM device with any operating system, but Windows 11 requires the presence of a TPM chip to install or boot.\n\nA vTPM device allows VMs created from a Windows 11 image to function without a physical TPM chip.\n\nOpenShift Virtualization supports persisting vTPM device state by using Persistent Volume Claims (PVCs) for VMs. If you do not specify the storage class for this PVC, OpenShift Virtualization uses the default storage class for virtualization workloads. If the default storage class for virtualization workloads is not set, OpenShift Virtualization uses the default storage class for the cluster.\n\n> [!NOTE]\n> The storage class that is marked as default for virtualization workloads has the annotation `storageclass.kubevirt.io/is-default-virt-class` set to \"true\". You can find this storage class by running the following command:\n>\n> ```terminal\n> $ oc get sc -o jsonpath='{range .items[?(.metadata.annotations.storageclass\\.kubevirt\\.io/is-default-virt-class==\"true\")]}{.metadata.name}{\"\\n\"}{end}'\n> ```\n>\n> Similarly, the default storage class for the cluster has the annotation `storageclass.kubernetes.io/is-default-class` set to \"true\". To find this storage class, run the following command:\n>\n> ```terminal\n> $ oc get sc -o jsonpath='{range .items[?(.metadata.annotations.storageclass\\.kubernetes\\.io/is-default-class==\"true\")]}{.metadata.name}{\"\\n\"}{end}'\n> ```\n>\n> To ensure consistent behavior, configure only one storage class as the default for virtualization workloads and for the cluster respectively.\n\nIt is recommended that you specify the storage class explicitly by setting the `vmStateStorageClass` attribute in the `HyperConverged` custom resource (CR):\n\n```yaml\nkind: HyperConverged\nmetadata:\n  name: kubevirt-hyperconverged\nspec:\n  vmStateStorageClass: <storage_class_name>\n\n# ...\n```\n\nIf you do not enable vTPM, then the VM does not recognize a TPM device, even if the node has one.\n\n## Adding a vTPM device to a virtual machine {#virt-adding-vtpm-to-vm_virt-using-vtpm-devices}\n\nAdding a virtual Trusted Platform Module (vTPM) device to a virtual machine (VM) allows you to run a VM created from a Windows 11 image without a physical TPM device. A vTPM device also stores secrets for that VM.\n\n> [!IMPORTANT]\n> When you add a virtual Trusted Platform Module (vTPM) device to a Windows VM, it is important to make the vTPM device persistent. The BitLocker Drive is encrypted successfully and the encryption system check passes even if the vTPM device is not persistent. If the vTPM device is not persistent, it is discarded on shutdown.\n\n**Prerequisites**\n\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Run the following command to update the VM configuration:\n\n   ```terminal\n   $ oc edit vm <vm_name> -n <namespace>\n   ```\n2. Edit the VM specification to add the vTPM device. For example:\n\n   ```yaml\n   apiVersion: kubevirt.io/v1\n   kind: VirtualMachine\n   metadata:\n       name: example-vm\n   spec:\n     template:\n       spec:\n         domain:\n           devices:\n             tpm:  (1)\n               persistent: true (2)\n   # ...\n   ```\n\n   - `spec.template.spec.domain.devices.tpm` specifies the vTPM device to add to the VM.\n   - `spec.template.spec.domain.devices.tpm.persistent` specifies that the vTPM device state persists after the VM is shut down. The default value is `false`.\n3. To apply your changes, save and exit the editor.\n4. Optional: If you edited a running virtual machine, you must restart it for the changes to take effect.\n\n**Additional resources**\n{._additional-resources}\n\n- [Exporting virtual machines](/openshift-docs-markdown/virt/managing_vms/virt-exporting-vms#virt-exporting-vms)\n- [Creating a snapshot by using the CLI](/openshift-docs-markdown/virt/backup_restore/virt-backup-restore-snapshots#virt-creating-vm-snapshot-cli_virt-backup-restore-snapshots)\n- [Restoring a virtual machine from a snapshot by using the CLI](/openshift-docs-markdown/virt/backup_restore/virt-backup-restore-snapshots#virt-restoring-vm-from-snapshot-cli_virt-backup-restore-snapshots)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/managing_vms/virt-using-vtpm-devices.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-using-vtpm-devices" class="group scroll-mt-20">Use a virtual Trusted Platform Module (vTPM) device<a href="#virt-using-vtpm-devices" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">To run Windows 11 or other workloads that require a Trusted Platform Module, you can add a virtual TPM (vTPM) to a new or existing virtual machine. Enable this by editing the <code>VirtualMachine</code> or <code>VirtualMachineInstance</code> manifest.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>With OpenShift Virtualization 4.18 and newer, you can export virtual machines (VMs) with attached vTPM devices, create snapshots of these VMs, and restore VMs from these snapshots. However, cloning a VM with a vTPM device attached to it or creating a new VM from its snapshot is not supported.</p>
        </AlertDescription>
      </Alert>
      <h2 id="virt-about-vtpm-devices_virt-using-vtpm-devices" class="group scroll-mt-20">About vTPM devices<a href="#virt-about-vtpm-devices_virt-using-vtpm-devices" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">A virtual Trusted Platform Module (vTPM) device functions like a physical Trusted Platform Module (TPM) hardware chip. You can use a vTPM device with any operating system, but Windows 11 requires the presence of a TPM chip to install or boot.</p>
      <p>A vTPM device allows VMs created from a Windows 11 image to function without a physical TPM chip.</p>
      <p>OpenShift Virtualization supports persisting vTPM device state by using Persistent Volume Claims (PVCs) for VMs. If you do not specify the storage class for this PVC, OpenShift Virtualization uses the default storage class for virtualization workloads. If the default storage class for virtualization workloads is not set, OpenShift Virtualization uses the default storage class for the cluster.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The storage class that is marked as default for virtualization workloads has the annotation <code>storageclass.kubevirt.io/is-default-virt-class</code> set to "true". You can find this storage class by running the following command:</p>
          <CodeBlock code={"$ oc get sc -o jsonpath='{range .items[?(.metadata.annotations.storageclass\\.kubevirt\\.io/is-default-virt-class==\"true\")]}{.metadata.name}{\"\\n\"}{end}'"} lang="terminal" />
          <p>Similarly, the default storage class for the cluster has the annotation <code>storageclass.kubernetes.io/is-default-class</code> set to "true". To find this storage class, run the following command:</p>
          <CodeBlock code={"$ oc get sc -o jsonpath='{range .items[?(.metadata.annotations.storageclass\\.kubernetes\\.io/is-default-class==\"true\")]}{.metadata.name}{\"\\n\"}{end}'"} lang="terminal" />
          <p>To ensure consistent behavior, configure only one storage class as the default for virtualization workloads and for the cluster respectively.</p>
        </AlertDescription>
      </Alert>
      <p>It is recommended that you specify the storage class explicitly by setting the <code>vmStateStorageClass</code> attribute in the <code>HyperConverged</code> custom resource (CR):</p>
      <CodeBlock code={"kind: HyperConverged\nmetadata:\n  name: kubevirt-hyperconverged\nspec:\n  vmStateStorageClass: <storage_class_name>\n\n# ..."} lang="yaml" />
      <p>If you do not enable vTPM, then the VM does not recognize a TPM device, even if the node has one.</p>
      <h2 id="virt-adding-vtpm-to-vm_virt-using-vtpm-devices" class="group scroll-mt-20">Adding a vTPM device to a virtual machine<a href="#virt-adding-vtpm-to-vm_virt-using-vtpm-devices" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Adding a virtual Trusted Platform Module (vTPM) device to a virtual machine (VM) allows you to run a VM created from a Windows 11 image without a physical TPM device. A vTPM device also stores secrets for that VM.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>When you add a virtual Trusted Platform Module (vTPM) device to a Windows VM, it is important to make the vTPM device persistent. The BitLocker Drive is encrypted successfully and the encryption system check passes even if the vTPM device is not persistent. If the vTPM device is not persistent, it is discarded on shutdown.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have installed the OpenShift CLI (<code>oc</code>).</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Run the following command to update the VM configuration:
      <CodeBlock code={"$ oc edit vm <vm_name> -n <namespace>"} lang="terminal" />
      </li>
      <li>
      Edit the VM specification to add the vTPM device. For example:
      <CodeBlock code={"apiVersion: kubevirt.io/v1\nkind: VirtualMachine\nmetadata:\n    name: example-vm\nspec:\n  template:\n    spec:\n      domain:\n        devices:\n          tpm:  (1)\n            persistent: true (2)\n# ..."} lang="yaml" />
      <ul>
      <li><code>spec.template.spec.domain.devices.tpm</code> specifies the vTPM device to add to the VM.</li>
      <li><code>spec.template.spec.domain.devices.tpm.persistent</code> specifies that the vTPM device state persists after the VM is shut down. The default value is <code>false</code>.</li>
      </ul>
      </li>
      <li>To apply your changes, save and exit the editor.</li>
      <li>Optional: If you edited a running virtual machine, you must restart it for the changes to take effect.</li>
      </ol>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/virt/managing_vms/virt-exporting-vms#virt-exporting-vms">Exporting virtual machines</a></li>
      <li><a href="/openshift-docs-markdown/virt/backup_restore/virt-backup-restore-snapshots#virt-creating-vm-snapshot-cli_virt-backup-restore-snapshots">Creating a snapshot by using the CLI</a></li>
      <li><a href="/openshift-docs-markdown/virt/backup_restore/virt-backup-restore-snapshots#virt-restoring-vm-from-snapshot-cli_virt-backup-restore-snapshots">Restoring a virtual machine from a snapshot by using the CLI</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
