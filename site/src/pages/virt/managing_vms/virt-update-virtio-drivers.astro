---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/managing_vms/virt-update-virtio-drivers.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-update-virtio-drivers","text":"Update VirtIO drivers"},{"depth":2,"slug":"virt-updating-red-hat-virtio-drivers-windows_virt-update-virtio-drivers","text":"Enable automatic updates for Red Hat virtio-win drivers"},{"depth":2,"slug":"virt-updating-virtio-drivers-windows_virt-update-virtio-drivers","text":"Update VirtIO drivers on a Windows VM"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/managing_vms/virt-update-virtio-drivers";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Update VirtIO drivers";
const description = "Update VirtIO drivers in guest operating systems. Using the latest VirtIO drivers increases performance and stability." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/managing_vms"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Update VirtIO drivers {#virt-update-virtio-drivers}\n\nUpdate VirtIO drivers in guest operating systems. Using the latest VirtIO drivers increases performance and stability.\n\n## Enable automatic updates for Red Hat virtio-win drivers {#virt-updating-red-hat-virtio-drivers-windows_virt-update-virtio-drivers}\n\nIf the Windows Update service (WUS) is restricted to allow only drivers explicitly signed and published by Microsoft, you must manually configure automatic Red Hat `virtio-win` driver updates. Otherwise, automatic updates are disabled.\n\n**Prerequisites**\n\n- The cluster must have internet connectivity. Disconnected clusters cannot reach the WUS.\n\n**Procedure**\n\n1. Import the Red Hat Release Certificate into the Trusted Publishers store.\n\n   Example command:\n\n   ```powershell\n   Import-Certificate -FilePath \"redhat-driver-cert.cer\" -CertStoreLocation Cert:\\LocalMachine\\TrustedPublisher\n   ```\n2. In the Group Policy Management Console (GPMC):\n\n   1. Set the `Allow signed updates from an intranet Microsoft update service location` policy to `Enabled`.\n\n      If a driver is signed by a certificate in the Trusted Publishers store, it is now accepted, even if it didn’t come from Microsoft directly.\n   2. Set the `Do not include drivers with Windows Updates` policy to `Disabled`.\n\n## Update VirtIO drivers on a Windows VM {#virt-updating-virtio-drivers-windows_virt-update-virtio-drivers}\n\nYou can update the VirtIO drivers on a Windows virtual machine (VM) by using the Windows Update service (WUS).\n\n> [!IMPORTANT]\n> If you restrict the WUS to only allow drivers explicitly signed and published by Microsoft, automatic Red Hat `virtio-win` driver updates are disabled. For information about enabling automatic Red Hat VirtIO driver updates, see \"Enable automatic updates for Red Hat virtio-win drivers\".\n\n**Prerequisites**\n\n- The cluster must have internet connectivity. Disconnected clusters cannot reach the WUS.\n\n**Procedure**\n\n1. In the Windows Guest operating system, click the **Windows** key and select **Settings**.\n2. Navigate to **Windows Update** → **Advanced Options** → **Optional Updates**.\n3. Install all updates from **Red Hat, Inc.**.\n4. Reboot the VM.\n\n**Verification**\n\n1. On the Windows VM, navigate to the **Device Manager**.\n2. Select a device.\n3. Select the **Driver** tab.\n4. Click **Driver Details** and confirm that the `virtio` driver details displays the correct version.\n\n> [!TIP]\n> To view the individual driver versions included in the `virtio-win` container disk, open the `release-drivers-versions.txt` file at the root of the `virtio-win` CD drive.\n\n**Additional resources**\n{._additional-resources}\n\n- [Allow signed updates from an intranet Microsoft update service location](https://learn.microsoft.com/en-us/windows/deployment/update/waas-wu-settings#allow-signed-updates-from-an-intranet-microsoft-update-service-location)\n- [Do not include drivers with Windows Updates](https://learn.microsoft.com/en-us/windows/deployment/update/waas-wu-settings#do-not-include-drivers-with-windows-updates)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/managing_vms/virt-update-virtio-drivers.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-update-virtio-drivers" class="group scroll-mt-20">Update VirtIO drivers<a href="#virt-update-virtio-drivers" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Update VirtIO drivers in guest operating systems. Using the latest VirtIO drivers increases performance and stability.</p>
      <h2 id="virt-updating-red-hat-virtio-drivers-windows_virt-update-virtio-drivers" class="group scroll-mt-20">Enable automatic updates for Red Hat virtio-win drivers<a href="#virt-updating-red-hat-virtio-drivers-windows_virt-update-virtio-drivers" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">If the Windows Update service (WUS) is restricted to allow only drivers explicitly signed and published by Microsoft, you must manually configure automatic Red Hat <code>virtio-win</code> driver updates. Otherwise, automatic updates are disabled.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>The cluster must have internet connectivity. Disconnected clusters cannot reach the WUS.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Import the Red Hat Release Certificate into the Trusted Publishers store.
      <p>Example command:</p>
      <CodeBlock code={"Import-Certificate -FilePath \"redhat-driver-cert.cer\" -CertStoreLocation Cert:\\LocalMachine\\TrustedPublisher"} lang="powershell" />
      </li>
      <li>
      In the Group Policy Management Console (GPMC):
      <ol>
      <li>
      Set the <code>Allow signed updates from an intranet Microsoft update service location</code> policy to <code>Enabled</code>.
      <p>If a driver is signed by a certificate in the Trusted Publishers store, it is now accepted, even if it didn’t come from Microsoft directly.</p>
      </li>
      <li>Set the <code>Do not include drivers with Windows Updates</code> policy to <code>Disabled</code>.</li>
      </ol>
      </li>
      </ol>
      <h2 id="virt-updating-virtio-drivers-windows_virt-update-virtio-drivers" class="group scroll-mt-20">Update VirtIO drivers on a Windows VM<a href="#virt-updating-virtio-drivers-windows_virt-update-virtio-drivers" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can update the VirtIO drivers on a Windows virtual machine (VM) by using the Windows Update service (WUS).</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>If you restrict the WUS to only allow drivers explicitly signed and published by Microsoft, automatic Red Hat <code>virtio-win</code> driver updates are disabled. For information about enabling automatic Red Hat VirtIO driver updates, see "Enable automatic updates for Red Hat virtio-win drivers".</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul><li>The cluster must have internet connectivity. Disconnected clusters cannot reach the WUS.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the Windows Guest operating system, click the <strong>Windows</strong> key and select <strong>Settings</strong>.</li>
      <li>Navigate to <strong>Windows Update</strong> → <strong>Advanced Options</strong> → <strong>Optional Updates</strong>.</li>
      <li>Install all updates from <strong>Red Hat, Inc.</strong>.</li>
      <li>Reboot the VM.</li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>On the Windows VM, navigate to the <strong>Device Manager</strong>.</li>
      <li>Select a device.</li>
      <li>Select the <strong>Driver</strong> tab.</li>
      <li>Click <strong>Driver Details</strong> and confirm that the <code>virtio</code> driver details displays the correct version.</li>
      </ol>
      <Alert variant="tip" class="my-4">
        <AlertTitle>Tip</AlertTitle>
        <AlertDescription>
          <p>To view the individual driver versions included in the <code>virtio-win</code> container disk, open the <code>release-drivers-versions.txt</code> file at the root of the <code>virtio-win</code> CD drive.</p>
        </AlertDescription>
      </Alert>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="https://learn.microsoft.com/en-us/windows/deployment/update/waas-wu-settings#allow-signed-updates-from-an-intranet-microsoft-update-service-location">Allow signed updates from an intranet Microsoft update service location</a></li>
      <li><a href="https://learn.microsoft.com/en-us/windows/deployment/update/waas-wu-settings#do-not-include-drivers-with-windows-updates">Do not include drivers with Windows Updates</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
