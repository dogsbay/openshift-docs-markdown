---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/managing_vms/ssh/virt-using-virtctl-ssh.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";

const headings = [{"depth":1,"slug":"virt-using-virtctl-ssh","text":"Configure SSH by using the virtctl CLI tool"}];
const nav = navData;
const currentPath = "/virt/managing_vms/ssh/virt-using-virtctl-ssh";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configure SSH by using the virtctl CLI tool";
const description = "You can add a public SSH key to a virtual machine (VM) and connect to the VM by running the virtctl ssh command, or add the virtctl port-foward command to…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/managing_vms","virt/managing_vms/ssh"];
const pageTaxonomies = null;
const tagsIndexPath = "/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configure SSH by using the virtctl CLI tool {#virt-using-virtctl-ssh}\n\nYou can add a public SSH key to a virtual machine (VM) and connect to the VM by running the `virtctl ssh` command, or add the `virtctl port-foward` command to your `.ssh/config` file and connect to the VM by using OpenSSH.\n\n> [!IMPORTANT]\n> The `virtctl ssh` command method is not recommended for high traffic loads because it places a burden on the API server.\n\nYou can add public SSH keys to {{ op_system_base_full }} 9 VMs at runtime or at first boot to VMs with guest operating systems that can be configured by using a cloud-init data source.\n\n> [!TIP]\n> You can copy the `virtctl ssh` command in the web console by selecting **Copy SSH command** from the options {{ kebab }} menu beside a VM on the **VirtualMachines** page.\n>\n> Alternatively, right-click the VM in the tree view and select **Copy SSH command** from the menu to copy the `virtctl ssh` command.\n","mdUrl":"/virt/managing_vms/ssh/virt-using-virtctl-ssh.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={true}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={""}
  navMode={"client"}
  pagefindUrl={"/pagefind/"}
  favicon={"/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <p class="/_attributes/common-attributes.md"></p>
      <h1 id="virt-using-virtctl-ssh" class="group scroll-mt-20">Configure SSH by using the virtctl CLI tool<a href="#virt-using-virtctl-ssh" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can add a public SSH key to a virtual machine (VM) and connect to the VM by running the <code>virtctl ssh</code> command, or add the <code>virtctl port-foward</code> command to your <code>.ssh/config</code> file and connect to the VM by using OpenSSH.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>The <code>virtctl ssh</code> command method is not recommended for high traffic loads because it places a burden on the API server.</p>
        </AlertDescription>
      </Alert>
      <p>You can add public SSH keys to {"{"}{"{"} op_system_base_full {"}"}{"}"} 9 VMs at runtime or at first boot to VMs with guest operating systems that can be configured by using a cloud-init data source.</p>
      <Alert variant="tip" class="my-4">
        <AlertTitle>Tip</AlertTitle>
        <AlertDescription>
          <p>You can copy the <code>virtctl ssh</code> command in the web console by selecting <strong>Copy SSH command</strong> from the options {"{"}{"{"} kebab {"}"}{"}"} menu beside a VM on the <strong>VirtualMachines</strong> page.</p>
          <p>Alternatively, right-click the VM in the tree view and select <strong>Copy SSH command</strong> from the menu to copy the <code>virtctl ssh</code> command.</p>
        </AlertDescription>
      </Alert>
      <p class="/modules/virt-about-static-and-dynamic-ssh-keys.md"></p>
      <p class="/modules/virt-adding-key-creating-vm-template.md /modules/virt-static-key-management-vm.md"></p>
      <p class="/modules/virt-creating-vm-instancetype.md"></p>
      <p class="/modules/virt-adding-public-key-vm-cli.md"></p>
      <p class="/modules/virt-adding-key-creating-vm-template.md /modules/virt-adding-dynamic-key-vm.md"></p>
      <p class="/modules/virt-creating-vm-instancetype.md"></p>
      <p class="/modules/virt-editing-vm-dynamic-key-injection.md"></p>
      <p class="/modules/virt-enabling-dynamic-key-injection-cli.md"></p>
      <p class="/modules/virt-using-virtctl-ssh-command.md"></p>
      <p class="/modules/virt-using-virtctl-port-forward-command.md"></p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
