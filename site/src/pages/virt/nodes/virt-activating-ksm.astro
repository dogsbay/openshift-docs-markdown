---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/nodes/virt-activating-ksm.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-activating-ksm","text":"Activating kernel samepage merging (KSM)"},{"depth":2,"slug":"prerequisites_virt-activating-ksm","text":"Prerequisites"},{"depth":2,"slug":"virt-about-ksm_virt-activating-ksm","text":"About using OpenShift Virtualization to activate KSM"},{"depth":3,"slug":"virt-ksm-configuration-methods","text":"Configuration methods"},{"depth":3,"slug":"virt-ksm-node-labels","text":"KSM node labels"},{"depth":2,"slug":"virt-configure-ksm-web_virt-activating-ksm","text":"Configuring KSM activation by using the web console"},{"depth":2,"slug":"virt-configure-ksm-cli_virt-activating-ksm","text":"Configuring KSM activation by using the CLI"},{"depth":2,"slug":"additional-resources_virt-activating-ksm","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/nodes/virt-activating-ksm";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Activating kernel samepage merging (KSM)";
const description = "OpenShift Virtualization can activate kernel samepage merging (KSM) when nodes are overloaded. KSM deduplicates identical data found in the memory pages…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/nodes"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Activating kernel samepage merging (KSM) {#virt-activating-ksm}\n\nOpenShift Virtualization can activate kernel samepage merging (KSM) when nodes are overloaded. KSM deduplicates identical data found in the memory pages of virtual machines (VMs). If you have very similar VMs, KSM can make it possible to schedule more VMs on a single node.\n\n> [!IMPORTANT]\n> You must only use KSM with trusted workloads.\n\n## Prerequisites {#prerequisites_virt-activating-ksm}\n\n- Ensure that an administrator has configured KSM support on any nodes where you want OpenShift Virtualization to activate KSM.\n\n## About using OpenShift Virtualization to activate KSM {#virt-about-ksm_virt-activating-ksm}\n\nYou can configure OpenShift Virtualization to activate kernel samepage merging (KSM) when nodes experience memory overload.\n\n### Configuration methods {#virt-ksm-configuration-methods}\n\nYou can enable or disable the KSM activation feature for all nodes by using the OpenShift Container Platform web console or by editing the `HyperConverged` custom resource (CR). The `HyperConverged` CR supports more granular configuration.\n\nCR configuration\n:   <a name=\"virt-ksm-cr-configuration\"></a>\n\n    You can configure the KSM activation feature by editing the `spec.ksmConfiguration` stanza of the `HyperConverged` CR.\n\n    - You enable the feature and configure settings by editing the `ksmConfiguration` stanza.\n    - You disable the feature by deleting the `ksmConfiguration` stanza.\n    - You can allow OpenShift Virtualization to enable KSM on only a subset of nodes by adding node selection syntax to the `ksmConfiguration.nodeLabelSelector` field.\n\n    > [!NOTE]\n    > Even if the KSM activation feature is disabled in OpenShift Virtualization, an administrator can still enable KSM on nodes that support it.\n\n### KSM node labels {#virt-ksm-node-labels}\n\nOpenShift Virtualization identifies nodes that are configured to support KSM and applies the following node labels:\n\n`kubevirt.io/ksm-handler-managed: \"false\"`\n:   This label is set to `\"true\"` when OpenShift Virtualization activates KSM on a node that is experiencing memory overload. This label is not set to `\"true\"` if an administrator activates KSM.\n\n`kubevirt.io/ksm-enabled: \"false\"`\n:   This label is set to `\"true\"` when KSM is activated on a node, even if OpenShift Virtualization did not activate KSM.\n\nThese labels are not applied to nodes that do not support KSM.\n\n## Configuring KSM activation by using the web console {#virt-configure-ksm-web_virt-activating-ksm}\n\nYou can allow OpenShift Virtualization to activate kernel samepage merging (KSM) on all nodes in your cluster by using the OpenShift Container Platform web console.\n\n**Procedure**\n\n1. From the side menu, click **Virtualization** -> **Settings**.\n2. Select the **Cluster** tab.\n3. Expand **Resource management**.\n4. Enable or disable the feature for all nodes using the **Kernel Samepage Merging (KSM)** toggle button.\n\n## Configuring KSM activation by using the CLI {#virt-configure-ksm-cli_virt-activating-ksm}\n\nYou can enable or disable OpenShift Virtualization's kernel samepage merging (KSM) activation feature by editing the `HyperConverged` custom resource (CR). Use this method if you want OpenShift Virtualization to activate KSM on only a subset of nodes.\n\n**Prerequisites**\n\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Open the `HyperConverged` CR in your default editor by running the following command:\n\n   ```terminal\n   $ oc edit {{ HCOCliKind }} kubevirt-hyperconverged -n {{ CNVNamespace }}\n   ```\n2. Edit the `ksmConfiguration` stanza:\n\n   - To enable the KSM activation feature for all nodes, set the `nodeLabelSelector` value to `{}`. For example:\n\n     ```yaml\n     apiVersion: hco.kubevirt.io/v1beta1\n     kind: HyperConverged\n     metadata:\n       name: kubevirt-hyperconverged\n       namespace: {{ CNVNamespace }}\n     spec:\n       ksmConfiguration:\n         nodeLabelSelector: {}\n     # ...\n     ```\n   - To enable the KSM activation feature on a subset of nodes, edit the `nodeLabelSelector` field. Add syntax that matches the nodes where you want OpenShift Virtualization to enable KSM. For example, the following configuration allows OpenShift Virtualization to enable KSM on nodes where both `<first_example_key>` and `<second_example_key>` are set to `\"true\"`:\n\n     ```yaml\n     apiVersion: hco.kubevirt.io/v1beta1\n     kind: HyperConverged\n     metadata:\n       name: kubevirt-hyperconverged\n       namespace: {{ CNVNamespace }}\n     spec:\n       ksmConfiguration:\n         nodeLabelSelector:\n           matchLabels:\n             <first_example_key>: \"true\"\n             <second_example_key>: \"true\"\n     # ...\n     ```\n   - To disable the KSM activation feature, delete the `ksmConfiguration` stanza. For example:\n\n     ```yaml\n     apiVersion: hco.kubevirt.io/v1beta1\n     kind: HyperConverged\n     metadata:\n       name: kubevirt-hyperconverged\n       namespace: {{ CNVNamespace }}\n     spec:\n     # ...\n     ```\n3. Save the file.\n\n## Additional resources {#additional-resources_virt-activating-ksm}\n\n- [Specifying nodes for virtual machines](/openshift-docs-markdown/virt/managing_vms/advanced_vm_management/virt-specifying-nodes-for-vms#virt-specifying-nodes-for-vms)\n- [Placing pods on specific nodes using node selectors](/openshift-docs-markdown/nodes/scheduling/nodes-scheduler-node-selectors#nodes-scheduler-node-selectors)\n- [Managing kernel samepage merging](https://access.redhat.com/documentation/en-us/red_hat_enterprise_linux/9/html-single/configuring_and_managing_virtualization/index#proc_managing-ksm_optimizing-virtual-machine-cpu-performance)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/nodes/virt-activating-ksm.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-activating-ksm" class="group scroll-mt-20">Activating kernel samepage merging (KSM)<a href="#virt-activating-ksm" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>OpenShift Virtualization can activate kernel samepage merging (KSM) when nodes are overloaded. KSM deduplicates identical data found in the memory pages of virtual machines (VMs). If you have very similar VMs, KSM can make it possible to schedule more VMs on a single node.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>You must only use KSM with trusted workloads.</p>
        </AlertDescription>
      </Alert>
      <h2 id="prerequisites_virt-activating-ksm" class="group scroll-mt-20">Prerequisites<a href="#prerequisites_virt-activating-ksm" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul><li>Ensure that an administrator has configured KSM support on any nodes where you want OpenShift Virtualization to activate KSM.</li></ul>
      <h2 id="virt-about-ksm_virt-activating-ksm" class="group scroll-mt-20">About using OpenShift Virtualization to activate KSM<a href="#virt-about-ksm_virt-activating-ksm" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can configure OpenShift Virtualization to activate kernel samepage merging (KSM) when nodes experience memory overload.</p>
      <h3 id="virt-ksm-configuration-methods" class="group scroll-mt-20">Configuration methods<a href="#virt-ksm-configuration-methods" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>You can enable or disable the KSM activation feature for all nodes by using the OpenShift Container Platform web console or by editing the <code>HyperConverged</code> custom resource (CR). The <code>HyperConverged</code> CR supports more granular configuration.</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">CR configuration</dt>
      <dd class="ml-6 text-muted-foreground">
      <p><Fragment set:html={"<a name=\"virt-ksm-cr-configuration\">"} /><Fragment set:html={"</a>"} /></p>
      <p>You can configure the KSM activation feature by editing the <code>spec.ksmConfiguration</code> stanza of the <code>HyperConverged</code> CR.</p>
      <ul>
      <li>You enable the feature and configure settings by editing the <code>ksmConfiguration</code> stanza.</li>
      <li>You disable the feature by deleting the <code>ksmConfiguration</code> stanza.</li>
      <li>You can allow OpenShift Virtualization to enable KSM on only a subset of nodes by adding node selection syntax to the <code>ksmConfiguration.nodeLabelSelector</code> field.</li>
      </ul>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Even if the KSM activation feature is disabled in OpenShift Virtualization, an administrator can still enable KSM on nodes that support it.</p>
        </AlertDescription>
      </Alert>
      </dd>
      </dl>
      <h3 id="virt-ksm-node-labels" class="group scroll-mt-20">KSM node labels<a href="#virt-ksm-node-labels" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>OpenShift Virtualization identifies nodes that are configured to support KSM and applies the following node labels:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>kubevirt.io/ksm-handler-managed: "false"</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>This label is set to <code>"true"</code> when OpenShift Virtualization activates KSM on a node that is experiencing memory overload. This label is not set to <code>"true"</code> if an administrator activates KSM.</p></dd>
      <dt class="font-semibold"><code>kubevirt.io/ksm-enabled: "false"</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>This label is set to <code>"true"</code> when KSM is activated on a node, even if OpenShift Virtualization did not activate KSM.</p></dd>
      </dl>
      <p>These labels are not applied to nodes that do not support KSM.</p>
      <h2 id="virt-configure-ksm-web_virt-activating-ksm" class="group scroll-mt-20">Configuring KSM activation by using the web console<a href="#virt-configure-ksm-web_virt-activating-ksm" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can allow OpenShift Virtualization to activate kernel samepage merging (KSM) on all nodes in your cluster by using the OpenShift Container Platform web console.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>From the side menu, click <strong>Virtualization</strong> -&gt; <strong>Settings</strong>.</li>
      <li>Select the <strong>Cluster</strong> tab.</li>
      <li>Expand <strong>Resource management</strong>.</li>
      <li>Enable or disable the feature for all nodes using the <strong>Kernel Samepage Merging (KSM)</strong> toggle button.</li>
      </ol>
      <h2 id="virt-configure-ksm-cli_virt-activating-ksm" class="group scroll-mt-20">Configuring KSM activation by using the CLI<a href="#virt-configure-ksm-cli_virt-activating-ksm" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can enable or disable OpenShift Virtualization's kernel samepage merging (KSM) activation feature by editing the <code>HyperConverged</code> custom resource (CR). Use this method if you want OpenShift Virtualization to activate KSM on only a subset of nodes.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have installed the OpenShift CLI (<code>oc</code>).</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Open the <code>HyperConverged</code> CR in your default editor by running the following command:
      <CodeBlock code={"$ oc edit {{ HCOCliKind }} kubevirt-hyperconverged -n {{ CNVNamespace }}"} lang="terminal" />
      </li>
      <li>
      Edit the <code>ksmConfiguration</code> stanza:
      <ul>
      <li>
      To enable the KSM activation feature for all nodes, set the <code>nodeLabelSelector</code> value to <code>{"{"}{"}"}</code>. For example:
      <CodeBlock code={"apiVersion: hco.kubevirt.io/v1beta1\nkind: HyperConverged\nmetadata:\n  name: kubevirt-hyperconverged\n  namespace: {{ CNVNamespace }}\nspec:\n  ksmConfiguration:\n    nodeLabelSelector: {}\n# ..."} lang="yaml" />
      </li>
      <li>
      To enable the KSM activation feature on a subset of nodes, edit the <code>nodeLabelSelector</code> field. Add syntax that matches the nodes where you want OpenShift Virtualization to enable KSM. For example, the following configuration allows OpenShift Virtualization to enable KSM on nodes where both <code>&lt;first_example_key&gt;</code> and <code>&lt;second_example_key&gt;</code> are set to <code>"true"</code>:
      <CodeBlock code={"apiVersion: hco.kubevirt.io/v1beta1\nkind: HyperConverged\nmetadata:\n  name: kubevirt-hyperconverged\n  namespace: {{ CNVNamespace }}\nspec:\n  ksmConfiguration:\n    nodeLabelSelector:\n      matchLabels:\n        <first_example_key>: \"true\"\n        <second_example_key>: \"true\"\n# ..."} lang="yaml" />
      </li>
      <li>
      To disable the KSM activation feature, delete the <code>ksmConfiguration</code> stanza. For example:
      <CodeBlock code={"apiVersion: hco.kubevirt.io/v1beta1\nkind: HyperConverged\nmetadata:\n  name: kubevirt-hyperconverged\n  namespace: {{ CNVNamespace }}\nspec:\n# ..."} lang="yaml" />
      </li>
      </ul>
      </li>
      <li>Save the file.</li>
      </ol>
      <h2 id="additional-resources_virt-activating-ksm" class="group scroll-mt-20">Additional resources<a href="#additional-resources_virt-activating-ksm" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/virt/managing_vms/advanced_vm_management/virt-specifying-nodes-for-vms#virt-specifying-nodes-for-vms">Specifying nodes for virtual machines</a></li>
      <li><a href="/openshift-docs-markdown/nodes/scheduling/nodes-scheduler-node-selectors#nodes-scheduler-node-selectors">Placing pods on specific nodes using node selectors</a></li>
      <li><a href="https://access.redhat.com/documentation/en-us/red_hat_enterprise_linux/9/html-single/configuring_and_managing_virtualization/index#proc_managing-ksm_optimizing-virtual-machine-cpu-performance">Managing kernel samepage merging</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
