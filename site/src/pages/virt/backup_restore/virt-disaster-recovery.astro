---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/backup_restore/virt-disaster-recovery.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";

const headings = [{"depth":1,"slug":"virt-disaster-recovery","text":"Disaster recovery"},{"depth":2,"slug":"virt-about-dr-methods_virt-disaster-recovery","text":"About disaster recovery methods"},{"depth":3,"slug":"metro-dr_virt-disaster-recovery","text":"Metro-DR"},{"depth":3,"slug":"regional-dr_virt-disaster-recovery","text":"Regional-DR"},{"depth":2,"slug":"virt-disaster-recovery-defining-apps_virt-disaster-recovery","text":"Defining applications for disaster recovery"},{"depth":3,"slug":"best-practices-rhacm-managed-vm_virt-disaster-recovery","text":"Best practices when defining an RHACM-managed VM"},{"depth":3,"slug":"best-practices-rhacm-discovered-vm_virt-disaster-recovery","text":"Best practices when defining an RHACM-discovered VM"},{"depth":2,"slug":"virt-vm-behavior-dr_virt-disaster-recovery","text":"VM behavior during disaster recovery scenarios"},{"depth":3,"slug":"dr-relocate_virt-disaster-recovery","text":"Relocate"},{"depth":3,"slug":"dr-failover_virt-disaster-recovery","text":"Failover"},{"depth":2,"slug":"virt-dr-solutions-rh-managed-clusters_virt-disaster-recovery","text":"Disaster recovery solutions for Red Hat managed clusters"},{"depth":3,"slug":"metro-dr-odf_virt-disaster-recovery","text":"Metro-DR for Red Hat OpenShift Data Foundation"},{"depth":3,"slug":"regional-dr-odf_virt-disaster-recovery","text":"Regional-DR for Red Hat OpenShift Data Foundation"},{"depth":2,"slug":"additional-resources_virt-disaster-recovery","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/backup_restore/virt-disaster-recovery";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Disaster recovery";
const description = "OpenShift Virtualization supports using disaster recovery (DR) solutions to ensure that your environment can recover after a site outage. To use these…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/backup_restore"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Disaster recovery {#virt-disaster-recovery}\n\nOpenShift Virtualization supports using disaster recovery (DR) solutions to ensure that your environment can recover after a site outage. To use these methods, you must plan your OpenShift Virtualization deployment in advance.\n\n## About disaster recovery methods {#virt-about-dr-methods_virt-disaster-recovery}\n\nThe two primary DR methods for OpenShift Virtualization are Metropolitan Disaster Recovery (Metro-DR) and Regional-DR.\n\nFor an overview of disaster recovery (DR) concepts, architecture, and planning considerations, see the \"Red Hat OpenShift Virtualization disaster recovery guide\" in the Red Hat Knowledgebase.\n\n### Metro-DR {#metro-dr_virt-disaster-recovery}\n\nMetro-DR uses synchronous replication. It writes to storage at both the primary and secondary sites so that the data is always synchronized between sites. Because the storage provider is responsible for ensuring that the synchronization succeeds, the environment must meet the throughput and latency requirements of the storage provider.\n\n### Regional-DR {#regional-dr_virt-disaster-recovery}\n\nRegional-DR uses asynchronous replication. The data in the primary site is synchronized with the secondary site at regular intervals. For this type of replication, you can have a higher latency connection between the primary and secondary sites.\n\n## Defining applications for disaster recovery {#virt-disaster-recovery-defining-apps_virt-disaster-recovery}\n\nDefine applications for disaster recovery by using VMs that Red Hat Advanced Cluster Management (RHACM) manages or discovers.\n\n### Best practices when defining an RHACM-managed VM {#best-practices-rhacm-managed-vm_virt-disaster-recovery}\n\nWhen creating an RHACM-managed application that includes a VM, you must use a GitOps workflow and create an RHACM application or `ApplicationSet` resource.\n\nYou can take several actions to improve your experience and chance of success when defining an RHACM-managed VM.\n\nUse a PVC and populator to define storage for the VM\n:   Because data volumes create persistent volume claims (PVCs) implicitly, data volumes and VMs with data volume templates do not fit as neatly into the GitOps model.\n\nUse the import method when choosing a population source for your VM disk\n:   Select a RHEL image from the software catalog to use the import method. Red Hat recommends using a specific version of the image rather than a floating tag for consistent results. The KubeVirt community maintains container disks for other operating systems in a Quay repository.\n\nUse `pullMethod: node`\n:   Use the pod `pullMethod: node` when creating a data volume from a registry source to take advantage of the OpenShift Container Platform pull secret, which is required to pull container images from the Red Hat registry.\n\n### Best practices when defining an RHACM-discovered VM {#best-practices-rhacm-discovered-vm_virt-disaster-recovery}\n\nYou can configure any VM in the cluster that is not an RHACM-managed application as an RHACM-discovered application. This includes VMs imported by using the Migration Toolkit for Virtualization (MTV), VMs created by using the OpenShift Container Platform web console, or VMs created by any other means, such as the CLI.\n\nYou can take several actions to improve your experience and chance of success when defining an RHACM-discovered VM.\n\nProtecting the VM when using MTV, the OpenShift Container Platform web console, or a custom VM\n:   Because automatic labeling is not currently available, the application owner must manually label the components of the VM application when using MTV, the OpenShift Container Platform web console, or a custom VM.\n\n    After creating the VM, apply a common label to the following resources associated with the VM: `VirtualMachine`, `DataVolume`, `PersistentVolumeClaim`, `Service`, `Route`, `Secret` and `ConfigMap`. If the VM uses an instance type or preference, you must also label the `ControllerRevision` copy of these objects referenced by the spec or status of the VM. Do not label virtual machine instances (VMIs) or pods; OpenShift Virtualization creates and manages these automatically.\n\n    > [!IMPORTANT]\n    > You must apply the common label to everything in the namespace that you want to protect, including objects that you added to the VM that are not listed here.\n\nIncluding more than the `VirtualMachine` object in the VM\n:   Working VMs typically also contain data volumes, persistent volume claims (PVCs), services, routes, secrets, `ConfigMap` objects, and `VirtualMachineSnapshot` objects.\n\nIncluding the VM as part of a larger logical application\n:   This includes other pod-based workloads and VMs.\n\n## VM behavior during disaster recovery scenarios {#virt-vm-behavior-dr_virt-disaster-recovery}\n\nVMs typically act similarly to pod-based workloads during both relocate and failover disaster recovery flows.\n\n### Relocate {#dr-relocate_virt-disaster-recovery}\n\nUse relocate to move an application from the primary environment to the secondary environment when the primary environment is still accessible. During relocate, the VM is gracefully terminated, any unreplicated data is synchronized to the secondary environment, and the VM starts in the secondary environment.\n\nBecause the VM terminates gracefully, there is no data loss. Therefore, the VM operating system will not perform crash recovery.\n\n### Failover {#dr-failover_virt-disaster-recovery}\n\nUse failover when there is a critical failure in the primary environment that makes it impractical or impossible to use relocation to move the workload to a secondary environment. When failover is executed, the storage is fenced from the primary environment, the I/O to the VM disks is abruptly halted, and the VM restarts in the secondary environment using the replicated data.\n\nYou should expect data loss due to failover. The extent of loss depends on whether you use Metro-DR, which uses synchronous replication, or Regional-DR, which uses asynchronous replication. Because Regional-DR uses snapshot-based replication intervals, the window of data loss is proportional to the replication interval length. When the VM restarts, the operating system might perform crash recovery.\n\n## Disaster recovery solutions for Red Hat managed clusters {#virt-dr-solutions-rh-managed-clusters_virt-disaster-recovery}\n\nYou can use disaster recovery (DR) solutions that combine Red Hat Advanced Cluster Management (RHACM), Red Hat Ceph Storage, and OpenShift Data Foundation components to failover applications between sites.\n\n### Metro-DR for Red Hat OpenShift Data Foundation {#metro-dr-odf_virt-disaster-recovery}\n\nOpenShift Virtualization supports the Metro-DR solution for OpenShift Data Foundation, which provides two-way synchronous data replication between managed OpenShift Virtualization clusters installed on primary and secondary sites.\n\n**Metro-DR differences**\n\n- This synchronous solution is only available to metropolitan distance data centers with a network round-trip latency of 10 milliseconds or less.\n- Multiple disk VMs are supported.\n- To prevent data corruption, you must ensure that storage is fenced during failover.\n\n  > [!TIP]\n  > Fencing means isolating a node so that workloads do not run on it.\n\nFor more information about using the Metro-DR solution for OpenShift Data Foundation with OpenShift Virtualization, see IBM's OpenShift Data Foundation Metro-DR documentation.\n\n### Regional-DR for Red Hat OpenShift Data Foundation {#regional-dr-odf_virt-disaster-recovery}\n\nOpenShift Virtualization supports the Regional-DR solution for OpenShift Data Foundation, which provides asynchronous data replication at regular intervals between managed OpenShift Virtualization clusters installed on primary and secondary sites.\n\n**Regional-DR differences**\n\n- Regional-DR supports higher network latency between the primary and secondary sites.\n- Regional-DR uses RBD snapshots to replicate data asynchronously. Currently, your applications must be resilient to small variances between VM disks. You can prevent these variances by using single disk VMs.\n- Using the import method when selecting a population source for your VM disk is recommended. However, you can protect VMs that use cloned PVCs if you select a `VolumeReplicationClass` that enables image flattening. For more information, see the OpenShift Data Foundation documentation.\n\nFor more information about using the Regional-DR solution for OpenShift Data Foundation with OpenShift Virtualization, see IBM's OpenShift Data Foundation Regional-DR documentation.\n\n## Additional resources {#additional-resources_virt-disaster-recovery}\n\n- [Red Hat OpenShift Virtualization disaster recovery guide](https://access.redhat.com/articles/7041594)\n- [Configuring OpenShift Data Foundation Disaster Recovery for OpenShift Workloads](https://docs.redhat.com/en/documentation/red_hat_openshift_data_foundation/latest/html/configuring_openshift_data_foundation_disaster_recovery_for_openshift_workloads/index)\n- [Metro-DR solution for OpenShift Data Foundation](https://access.redhat.com/documentation/en-us/red_hat_openshift_data_foundation/latest/html-single/configuring_openshift_data_foundation_disaster_recovery_for_openshift_workloads/index#metro-dr-solution)\n- [Regional-DR solution for OpenShift Data Foundation](https://docs.redhat.com/en/documentation/red_hat_openshift_data_foundation/latest/html-single/configuring_openshift_data_foundation_disaster_recovery_for_openshift_workloads/index#rdr-solution)\n- [Use OpenShift Data Foundation Disaster Recovery to Protect Virtual Machines (in the Red Hat Knowledgebase)](https://access.redhat.com/articles/7053115)\n- [Red Hat Advanced Cluster Management for Kubernetes 2.10](https://docs.redhat.com/documentation/en-us/red_hat_advanced_cluster_management_for_kubernetes/2.10)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/backup_restore/virt-disaster-recovery.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-disaster-recovery" class="group scroll-mt-20">Disaster recovery<a href="#virt-disaster-recovery" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>OpenShift Virtualization supports using disaster recovery (DR) solutions to ensure that your environment can recover after a site outage. To use these methods, you must plan your OpenShift Virtualization deployment in advance.</p>
      <h2 id="virt-about-dr-methods_virt-disaster-recovery" class="group scroll-mt-20">About disaster recovery methods<a href="#virt-about-dr-methods_virt-disaster-recovery" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The two primary DR methods for OpenShift Virtualization are Metropolitan Disaster Recovery (Metro-DR) and Regional-DR.</p>
      <p>For an overview of disaster recovery (DR) concepts, architecture, and planning considerations, see the "Red Hat OpenShift Virtualization disaster recovery guide" in the Red Hat Knowledgebase.</p>
      <h3 id="metro-dr_virt-disaster-recovery" class="group scroll-mt-20">Metro-DR<a href="#metro-dr_virt-disaster-recovery" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Metro-DR uses synchronous replication. It writes to storage at both the primary and secondary sites so that the data is always synchronized between sites. Because the storage provider is responsible for ensuring that the synchronization succeeds, the environment must meet the throughput and latency requirements of the storage provider.</p>
      <h3 id="regional-dr_virt-disaster-recovery" class="group scroll-mt-20">Regional-DR<a href="#regional-dr_virt-disaster-recovery" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Regional-DR uses asynchronous replication. The data in the primary site is synchronized with the secondary site at regular intervals. For this type of replication, you can have a higher latency connection between the primary and secondary sites.</p>
      <h2 id="virt-disaster-recovery-defining-apps_virt-disaster-recovery" class="group scroll-mt-20">Defining applications for disaster recovery<a href="#virt-disaster-recovery-defining-apps_virt-disaster-recovery" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Define applications for disaster recovery by using VMs that Red Hat Advanced Cluster Management (RHACM) manages or discovers.</p>
      <h3 id="best-practices-rhacm-managed-vm_virt-disaster-recovery" class="group scroll-mt-20">Best practices when defining an RHACM-managed VM<a href="#best-practices-rhacm-managed-vm_virt-disaster-recovery" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>When creating an RHACM-managed application that includes a VM, you must use a GitOps workflow and create an RHACM application or <code>ApplicationSet</code> resource.</p>
      <p>You can take several actions to improve your experience and chance of success when defining an RHACM-managed VM.</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Use a PVC and populator to define storage for the VM</dt>
      <dd class="ml-6 text-muted-foreground"><p>Because data volumes create persistent volume claims (PVCs) implicitly, data volumes and VMs with data volume templates do not fit as neatly into the GitOps model.</p></dd>
      <dt class="font-semibold">Use the import method when choosing a population source for your VM disk</dt>
      <dd class="ml-6 text-muted-foreground"><p>Select a RHEL image from the software catalog to use the import method. Red Hat recommends using a specific version of the image rather than a floating tag for consistent results. The KubeVirt community maintains container disks for other operating systems in a Quay repository.</p></dd>
      <dt class="font-semibold">Use <code>pullMethod: node</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Use the pod <code>pullMethod: node</code> when creating a data volume from a registry source to take advantage of the OpenShift Container Platform pull secret, which is required to pull container images from the Red Hat registry.</p></dd>
      </dl>
      <h3 id="best-practices-rhacm-discovered-vm_virt-disaster-recovery" class="group scroll-mt-20">Best practices when defining an RHACM-discovered VM<a href="#best-practices-rhacm-discovered-vm_virt-disaster-recovery" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>You can configure any VM in the cluster that is not an RHACM-managed application as an RHACM-discovered application. This includes VMs imported by using the Migration Toolkit for Virtualization (MTV), VMs created by using the OpenShift Container Platform web console, or VMs created by any other means, such as the CLI.</p>
      <p>You can take several actions to improve your experience and chance of success when defining an RHACM-discovered VM.</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Protecting the VM when using MTV, the OpenShift Container Platform web console, or a custom VM</dt>
      <dd class="ml-6 text-muted-foreground">
      <p>Because automatic labeling is not currently available, the application owner must manually label the components of the VM application when using MTV, the OpenShift Container Platform web console, or a custom VM.</p>
      <p>After creating the VM, apply a common label to the following resources associated with the VM: <code>VirtualMachine</code>, <code>DataVolume</code>, <code>PersistentVolumeClaim</code>, <code>Service</code>, <code>Route</code>, <code>Secret</code> and <code>ConfigMap</code>. If the VM uses an instance type or preference, you must also label the <code>ControllerRevision</code> copy of these objects referenced by the spec or status of the VM. Do not label virtual machine instances (VMIs) or pods; OpenShift Virtualization creates and manages these automatically.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>You must apply the common label to everything in the namespace that you want to protect, including objects that you added to the VM that are not listed here.</p>
        </AlertDescription>
      </Alert>
      </dd>
      <dt class="font-semibold">Including more than the <code>VirtualMachine</code> object in the VM</dt>
      <dd class="ml-6 text-muted-foreground"><p>Working VMs typically also contain data volumes, persistent volume claims (PVCs), services, routes, secrets, <code>ConfigMap</code> objects, and <code>VirtualMachineSnapshot</code> objects.</p></dd>
      <dt class="font-semibold">Including the VM as part of a larger logical application</dt>
      <dd class="ml-6 text-muted-foreground"><p>This includes other pod-based workloads and VMs.</p></dd>
      </dl>
      <h2 id="virt-vm-behavior-dr_virt-disaster-recovery" class="group scroll-mt-20">VM behavior during disaster recovery scenarios<a href="#virt-vm-behavior-dr_virt-disaster-recovery" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">VMs typically act similarly to pod-based workloads during both relocate and failover disaster recovery flows.</p>
      <h3 id="dr-relocate_virt-disaster-recovery" class="group scroll-mt-20">Relocate<a href="#dr-relocate_virt-disaster-recovery" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Use relocate to move an application from the primary environment to the secondary environment when the primary environment is still accessible. During relocate, the VM is gracefully terminated, any unreplicated data is synchronized to the secondary environment, and the VM starts in the secondary environment.</p>
      <p>Because the VM terminates gracefully, there is no data loss. Therefore, the VM operating system will not perform crash recovery.</p>
      <h3 id="dr-failover_virt-disaster-recovery" class="group scroll-mt-20">Failover<a href="#dr-failover_virt-disaster-recovery" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Use failover when there is a critical failure in the primary environment that makes it impractical or impossible to use relocation to move the workload to a secondary environment. When failover is executed, the storage is fenced from the primary environment, the I/O to the VM disks is abruptly halted, and the VM restarts in the secondary environment using the replicated data.</p>
      <p>You should expect data loss due to failover. The extent of loss depends on whether you use Metro-DR, which uses synchronous replication, or Regional-DR, which uses asynchronous replication. Because Regional-DR uses snapshot-based replication intervals, the window of data loss is proportional to the replication interval length. When the VM restarts, the operating system might perform crash recovery.</p>
      <h2 id="virt-dr-solutions-rh-managed-clusters_virt-disaster-recovery" class="group scroll-mt-20">Disaster recovery solutions for Red Hat managed clusters<a href="#virt-dr-solutions-rh-managed-clusters_virt-disaster-recovery" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can use disaster recovery (DR) solutions that combine Red Hat Advanced Cluster Management (RHACM), Red Hat Ceph Storage, and OpenShift Data Foundation components to failover applications between sites.</p>
      <h3 id="metro-dr-odf_virt-disaster-recovery" class="group scroll-mt-20">Metro-DR for Red Hat OpenShift Data Foundation<a href="#metro-dr-odf_virt-disaster-recovery" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>OpenShift Virtualization supports the Metro-DR solution for OpenShift Data Foundation, which provides two-way synchronous data replication between managed OpenShift Virtualization clusters installed on primary and secondary sites.</p>
      <p><strong>Metro-DR differences</strong></p>
      <ul>
      <li>This synchronous solution is only available to metropolitan distance data centers with a network round-trip latency of 10 milliseconds or less.</li>
      <li>Multiple disk VMs are supported.</li>
      <li>
      To prevent data corruption, you must ensure that storage is fenced during failover.
      <Alert variant="tip" class="my-4">
        <AlertTitle>Tip</AlertTitle>
        <AlertDescription>
          <p>Fencing means isolating a node so that workloads do not run on it.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      <p>For more information about using the Metro-DR solution for OpenShift Data Foundation with OpenShift Virtualization, see IBM's OpenShift Data Foundation Metro-DR documentation.</p>
      <h3 id="regional-dr-odf_virt-disaster-recovery" class="group scroll-mt-20">Regional-DR for Red Hat OpenShift Data Foundation<a href="#regional-dr-odf_virt-disaster-recovery" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>OpenShift Virtualization supports the Regional-DR solution for OpenShift Data Foundation, which provides asynchronous data replication at regular intervals between managed OpenShift Virtualization clusters installed on primary and secondary sites.</p>
      <p><strong>Regional-DR differences</strong></p>
      <ul>
      <li>Regional-DR supports higher network latency between the primary and secondary sites.</li>
      <li>Regional-DR uses RBD snapshots to replicate data asynchronously. Currently, your applications must be resilient to small variances between VM disks. You can prevent these variances by using single disk VMs.</li>
      <li>Using the import method when selecting a population source for your VM disk is recommended. However, you can protect VMs that use cloned PVCs if you select a <code>VolumeReplicationClass</code> that enables image flattening. For more information, see the OpenShift Data Foundation documentation.</li>
      </ul>
      <p>For more information about using the Regional-DR solution for OpenShift Data Foundation with OpenShift Virtualization, see IBM's OpenShift Data Foundation Regional-DR documentation.</p>
      <h2 id="additional-resources_virt-disaster-recovery" class="group scroll-mt-20">Additional resources<a href="#additional-resources_virt-disaster-recovery" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="https://access.redhat.com/articles/7041594">Red Hat OpenShift Virtualization disaster recovery guide</a></li>
      <li><a href="https://docs.redhat.com/en/documentation/red_hat_openshift_data_foundation/latest/html/configuring_openshift_data_foundation_disaster_recovery_for_openshift_workloads/index">Configuring OpenShift Data Foundation Disaster Recovery for OpenShift Workloads</a></li>
      <li><a href="https://access.redhat.com/documentation/en-us/red_hat_openshift_data_foundation/latest/html-single/configuring_openshift_data_foundation_disaster_recovery_for_openshift_workloads/index#metro-dr-solution">Metro-DR solution for OpenShift Data Foundation</a></li>
      <li><a href="https://docs.redhat.com/en/documentation/red_hat_openshift_data_foundation/latest/html-single/configuring_openshift_data_foundation_disaster_recovery_for_openshift_workloads/index#rdr-solution">Regional-DR solution for OpenShift Data Foundation</a></li>
      <li><a href="https://access.redhat.com/articles/7053115">Use OpenShift Data Foundation Disaster Recovery to Protect Virtual Machines (in the Red Hat Knowledgebase)</a></li>
      <li><a href="https://docs.redhat.com/documentation/en-us/red_hat_advanced_cluster_management_for_kubernetes/2.10">Red Hat Advanced Cluster Management for Kubernetes 2.10</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
