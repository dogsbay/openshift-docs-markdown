---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/creating_vm/virt-cloning-vms.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";

const headings = [{"depth":1,"slug":"virt-cloning-vms","text":"Cloning VMs"},{"depth":2,"slug":"virt-cloning-vm-snapshot_virt-cloning-vms","text":"Cloning a VM by using the web console"},{"depth":2,"slug":"virt-creating-vm-from-snapshot-web_virt-cloning-vms","text":"Creating a VM from an existing snapshot by using the web console"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/creating_vm/virt-cloning-vms";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Cloning VMs";
const description = "You can clone virtual machines (VMs) or create new VMs from snapshots." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/creating_vm"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Cloning VMs {#virt-cloning-vms}\n\nYou can clone virtual machines (VMs) or create new VMs from snapshots.\n\n> [!IMPORTANT]\n> Cloning a VM with a vTPM device attached to it or creating a new VM from its snapshot is not supported.\n\n## Cloning a VM by using the web console {#virt-cloning-vm-snapshot_virt-cloning-vms}\n\nYou can clone an existing VM by using the web console.\n\n**Procedure**\n\n1. Navigate to **Virtualization** → **VirtualMachines** in the web console.\n2. Select a VM to open the **VirtualMachine details** page.\n3. Click **Actions**.\n\n   Alternatively, access the same menu in the tree view by right-clicking the VM.\n4. Select **Clone**.\n5. On the **Clone VirtualMachine** page, enter the name of the new VM.\n6. Optional: Select the **Start cloned VM** checkbox to start the cloned VM.\n7. Optional: In the **Volume name policy** section, select how cloned persistent volume claims (PVCs) are named:\n\n   - **Randomize names** - The cloned PVC names are randomly generated. This is the default setting.\n   - **Prefix target name** - The cloned PVC names use the target VM name as a prefix.\n8. Click **Clone**.\n\n## Creating a VM from an existing snapshot by using the web console {#virt-creating-vm-from-snapshot-web_virt-cloning-vms}\n\nYou can create a new VM by copying an existing snapshot.\n\n**Procedure**\n\n1. Navigate to **Virtualization** → **VirtualMachines** in the web console.\n2. Select a VM to open the **VirtualMachine details** page.\n3. Click the **Snapshots** tab.\n4. Click the Options menu ![](/openshift-docs-markdown/images/kebab.png \"Options menu\") for the snapshot you want to copy.\n5. Select **Create VirtualMachine**.\n6. Enter the name of the VM.\n7. Optional: Select the **Start this VM after creation** checkbox to start the new VM.\n8. Optional: In the **Volume name policy** section, select how cloned persistent volume claims (PVCs) are named:\n\n   - **Randomize names** - The cloned PVC names are randomly generated. This is the default setting.\n   - **Prefix target name** - The cloned PVC names use the target VM name as a prefix.\n9. Click **Create**.\n\n**Additional resources**\n{._additional-resources}\n\n- [Creating VMs by cloning PVCs](/openshift-docs-markdown/virt/creating_vm/virt-creating-vms-by-cloning-pvcs#virt-creating-vms-by-cloning-pvcs)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/creating_vm/virt-cloning-vms.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-cloning-vms" class="group scroll-mt-20">Cloning VMs<a href="#virt-cloning-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">You can clone virtual machines (VMs) or create new VMs from snapshots.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Cloning a VM with a vTPM device attached to it or creating a new VM from its snapshot is not supported.</p>
        </AlertDescription>
      </Alert>
      <h2 id="virt-cloning-vm-snapshot_virt-cloning-vms" class="group scroll-mt-20">Cloning a VM by using the web console<a href="#virt-cloning-vm-snapshot_virt-cloning-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can clone an existing VM by using the web console.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to <strong>Virtualization</strong> → <strong>VirtualMachines</strong> in the web console.</li>
      <li>Select a VM to open the <strong>VirtualMachine details</strong> page.</li>
      <li>
      Click <strong>Actions</strong>.
      <p>Alternatively, access the same menu in the tree view by right-clicking the VM.</p>
      </li>
      <li>Select <strong>Clone</strong>.</li>
      <li>On the <strong>Clone VirtualMachine</strong> page, enter the name of the new VM.</li>
      <li>Optional: Select the <strong>Start cloned VM</strong> checkbox to start the cloned VM.</li>
      <li>
      Optional: In the <strong>Volume name policy</strong> section, select how cloned persistent volume claims (PVCs) are named:
      <ul>
      <li><strong>Randomize names</strong> - The cloned PVC names are randomly generated. This is the default setting.</li>
      <li><strong>Prefix target name</strong> - The cloned PVC names use the target VM name as a prefix.</li>
      </ul>
      </li>
      <li>Click <strong>Clone</strong>.</li>
      </ol>
      <h2 id="virt-creating-vm-from-snapshot-web_virt-cloning-vms" class="group scroll-mt-20">Creating a VM from an existing snapshot by using the web console<a href="#virt-creating-vm-from-snapshot-web_virt-cloning-vms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can create a new VM by copying an existing snapshot.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to <strong>Virtualization</strong> → <strong>VirtualMachines</strong> in the web console.</li>
      <li>Select a VM to open the <strong>VirtualMachine details</strong> page.</li>
      <li>Click the <strong>Snapshots</strong> tab.</li>
      <li>Click the Options menu <img src="/openshift-docs-markdown/images/kebab.png" alt="" loading="lazy" /> for the snapshot you want to copy.</li>
      <li>Select <strong>Create VirtualMachine</strong>.</li>
      <li>Enter the name of the VM.</li>
      <li>Optional: Select the <strong>Start this VM after creation</strong> checkbox to start the new VM.</li>
      <li>
      Optional: In the <strong>Volume name policy</strong> section, select how cloned persistent volume claims (PVCs) are named:
      <ul>
      <li><strong>Randomize names</strong> - The cloned PVC names are randomly generated. This is the default setting.</li>
      <li><strong>Prefix target name</strong> - The cloned PVC names use the target VM name as a prefix.</li>
      </ul>
      </li>
      <li>Click <strong>Create</strong>.</li>
      </ol>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="/openshift-docs-markdown/virt/creating_vm/virt-creating-vms-by-cloning-pvcs#virt-creating-vms-by-cloning-pvcs">Creating VMs by cloning PVCs</a></li></ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
