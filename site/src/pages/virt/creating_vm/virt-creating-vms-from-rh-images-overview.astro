---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/creating_vm/virt-creating-vms-from-rh-images-overview.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-creating-vms-from-rh-images-overview","text":"Creating virtual machines from Red Hat images"},{"depth":2,"slug":"virt-about-golden-images_virt-creating-vms-from-rh-images-overview","text":"About golden images"},{"depth":3,"slug":"virt-how-golden-images-work_virt-creating-vms-from-rh-images-overview","text":"How do golden images work?"},{"depth":3,"slug":"virt-golden-images-implementation_virt-creating-vms-from-rh-images-overview","text":"Red Hat implementation of golden images"},{"depth":2,"slug":"virt-about-vms-and-boot-sources_virt-creating-vms-from-rh-images-overview","text":"About VM boot sources"},{"depth":2,"slug":"virt-boot-source-images-namespace-web_virt-creating-vms-from-rh-images-overview","text":"Configuring a custom namespace for boot source images by using the web console"},{"depth":2,"slug":"virt-boot-source-images-namespace-cli_virt-creating-vms-from-rh-images-overview","text":"Configuring a custom namespace for boot source images by using the CLI"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/creating_vm/virt-creating-vms-from-rh-images-overview";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Creating virtual machines from Red Hat images";
const description = "RHEL golden images are published as container disks in a secure registry. The Containerized Data Importer (CDI) polls imports golden images into your…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/creating_vm"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Creating virtual machines from Red Hat images {#virt-creating-vms-from-rh-images-overview}\n\nRHEL golden images are published as container disks in a secure registry. The Containerized Data Importer (CDI) polls imports golden images into your cluster and stores them in the `openshift-virtualization-os-images` project as snapshots or persistent volume claims (PVCs).\n\nRHEL images are automatically updated. You can disable and re-enable automatic updates for these images. For more information, see \"Additional resources\".\n\nCluster administrators can enable automatic subscription for RHEL virtual machines in the OpenShift Container Platform web console.\n\nYou can create virtual machines (VMs) from operating system images provided by Red Hat by using one of the following methods:\n\n- Create a VM from a template by using the web console.\n- Create a VM from an instance type by using the web console.\n- Create a VM from a `VirtualMachine` manifest by using the command line.\n\n> [!IMPORTANT]\n> Do not create VMs in the default `openshift-*` namespaces. Instead, create a new namespace or use an existing namespace without the `openshift` prefix.\n\n## About golden images {#virt-about-golden-images_virt-creating-vms-from-rh-images-overview}\n\nA golden image is a preconfigured snapshot of a virtual machine (VM) that you can use as a resource to deploy new VMs. For example, you can use golden images to provision the same system environment consistently and deploy systems more quickly and efficiently.\n\n### How do golden images work? {#virt-how-golden-images-work_virt-creating-vms-from-rh-images-overview}\n\nGolden images are created by installing and configuring an operating system and software applications on a reference machine or virtual machine. This includes setting up the system, installing required drivers, applying patches and updates, and configuring specific options and preferences.\n\nAfter the golden image is created, it is saved as a template or image file that can be replicated and deployed across multiple clusters. The golden image can be updated by its maintainer periodically to incorporate necessary software updates and patches, ensuring that the image remains up to date and secure, and newly created VMs are based on this updated image.\n\n### Red Hat implementation of golden images {#virt-golden-images-implementation_virt-creating-vms-from-rh-images-overview}\n\nRed Hat publishes golden images as container disks in the registry for versions of Red Hat Enterprise Linux (RHEL). Container disks are virtual machine images that are stored as a container image in a container image registry. Any published image will automatically be made available in connected clusters after the installation of OpenShift Virtualization. After the images are available in a cluster, they are ready to use to create VMs.\n\n## About VM boot sources {#virt-about-vms-and-boot-sources_virt-creating-vms-from-rh-images-overview}\n\nVirtual machines (VMs) consist of a VM definition and one or more disks that are backed by data volumes. VM templates enable you to create VMs using predefined specifications.\n\nEvery template requires a boot source, which is a fully configured disk image including configured drivers. Each template contains a VM definition with a pointer to the boot source. Each boot source has a predefined name and namespace. For some operating systems, a boot source is automatically provided. If it is not provided, then an administrator must prepare a custom boot source.\n\nProvided boot sources are updated automatically to the latest version of the operating system. For auto-updated boot sources, persistent volume claims (PVCs) and volume snapshots are created with the cluster’s default storage class. If you select a different default storage class after configuration, you must delete the existing boot sources in the cluster namespace that are configured with the previous default storage class.\n\n## Configuring a custom namespace for boot source images by using the web console {#virt-boot-source-images-namespace-web_virt-creating-vms-from-rh-images-overview}\n\nYou can configure a custom namespace for boot source images in your cluster by using the OpenShift Container Platform web console.\n\n**Procedure**\n\n1. In the web console, select **Virtualization** → **Settings**.\n2. On the **Cluster** tab, select **General settings** → **Templates and images management**.\n3. Click **Bootable volumes project**.\n4. Select a namespace to use for boot source images.\n\n   1. If you already created a namespace, select it from the **Project** list.\n   2. If you did not create a namespace, scroll to the bottom of the list and click **Create project**.\n\n      1. Enter a name for your new namespace in the **Name** field of the **Create project** dialog.\n      2. Click **Create**.\n\n## Configuring a custom namespace for boot source images by using the CLI {#virt-boot-source-images-namespace-cli_virt-creating-vms-from-rh-images-overview}\n\nYou can configure a custom namespace for boot source images in your cluster by setting the `spec.commonBootImageNamespace` field in the `HyperConverged` custom resource (CR).\n\n**Prerequisites**\n\n- You installed the OpenShift CLI (`oc`).\n- You created a namespace to use for boot source images.\n\n**Procedure**\n\n1. Open the `HyperConverged` CR in your default editor by running the following command:\n\n   ```terminal\n   $ oc edit hyperconvergeds.v1beta1.hco.kubevirt.io kubevirt-hyperconverged -n openshift-cnv\n   ```\n2. Configure the custom namespace by updating the value of the `spec.commonBootImageNamespace` field.\n\n   Example configuration file:\n\n   ```yaml\n   apiVersion: hco.kubevirt.io/v1beta1\n   kind: HyperConverged\n   metadata:\n     name: kubevirt-hyperconverged\n     namespace: openshift-cnv\n   spec:\n     commonBootImageNamespace: <custom_namespace>\n   # ...\n   ```\n\n   where:\n\n   `spec.commonBootImageNamespace`\n   :   Specifies the namespace to use for boot source images.\n3. Save your changes and exit the editor.\n\n**Additional resources**\n{._additional-resources}\n\n- [Managing Red Hat boot source updates](/openshift-docs-markdown/virt/storage/virt-automatic-bootsource-updates#virt-managing-auto-update-all-system-boot-sources_virt-automatic-bootsource-updates)\n- [Creating a VM from a template by using the web console](/openshift-docs-markdown/virt/creating_vm/virt-creating-vms-from-templates#virt-creating-vms-from-templates)\n- [Creating a VM from an instance type by using the web console](/openshift-docs-markdown/virt/creating_vm/virt-creating-vms-from-instance-types#virt-creating-vms-from-instance-types)\n- [Creating a VM from a `VirtualMachine` manifest by using the command line](/openshift-docs-markdown/virt/creating_vm/virt-creating-vms-from-cli#virt-creating-vms-from-cli)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/creating_vm/virt-creating-vms-from-rh-images-overview.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-creating-vms-from-rh-images-overview" class="group scroll-mt-20">Creating virtual machines from Red Hat images<a href="#virt-creating-vms-from-rh-images-overview" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">RHEL golden images are published as container disks in a secure registry. The Containerized Data Importer (CDI) polls imports golden images into your cluster and stores them in the <code>openshift-virtualization-os-images</code> project as snapshots or persistent volume claims (PVCs).</p>
      <p>RHEL images are automatically updated. You can disable and re-enable automatic updates for these images. For more information, see "Additional resources".</p>
      <p>Cluster administrators can enable automatic subscription for RHEL virtual machines in the OpenShift Container Platform web console.</p>
      <p>You can create virtual machines (VMs) from operating system images provided by Red Hat by using one of the following methods:</p>
      <ul>
      <li>Create a VM from a template by using the web console.</li>
      <li>Create a VM from an instance type by using the web console.</li>
      <li>Create a VM from a <code>VirtualMachine</code> manifest by using the command line.</li>
      </ul>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Do not create VMs in the default <code>openshift-*</code> namespaces. Instead, create a new namespace or use an existing namespace without the <code>openshift</code> prefix.</p>
        </AlertDescription>
      </Alert>
      <h2 id="virt-about-golden-images_virt-creating-vms-from-rh-images-overview" class="group scroll-mt-20">About golden images<a href="#virt-about-golden-images_virt-creating-vms-from-rh-images-overview" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">A golden image is a preconfigured snapshot of a virtual machine (VM) that you can use as a resource to deploy new VMs. For example, you can use golden images to provision the same system environment consistently and deploy systems more quickly and efficiently.</p>
      <h3 id="virt-how-golden-images-work_virt-creating-vms-from-rh-images-overview" class="group scroll-mt-20">How do golden images work?<a href="#virt-how-golden-images-work_virt-creating-vms-from-rh-images-overview" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Golden images are created by installing and configuring an operating system and software applications on a reference machine or virtual machine. This includes setting up the system, installing required drivers, applying patches and updates, and configuring specific options and preferences.</p>
      <p>After the golden image is created, it is saved as a template or image file that can be replicated and deployed across multiple clusters. The golden image can be updated by its maintainer periodically to incorporate necessary software updates and patches, ensuring that the image remains up to date and secure, and newly created VMs are based on this updated image.</p>
      <h3 id="virt-golden-images-implementation_virt-creating-vms-from-rh-images-overview" class="group scroll-mt-20">Red Hat implementation of golden images<a href="#virt-golden-images-implementation_virt-creating-vms-from-rh-images-overview" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Red Hat publishes golden images as container disks in the registry for versions of Red Hat Enterprise Linux (RHEL). Container disks are virtual machine images that are stored as a container image in a container image registry. Any published image will automatically be made available in connected clusters after the installation of OpenShift Virtualization. After the images are available in a cluster, they are ready to use to create VMs.</p>
      <h2 id="virt-about-vms-and-boot-sources_virt-creating-vms-from-rh-images-overview" class="group scroll-mt-20">About VM boot sources<a href="#virt-about-vms-and-boot-sources_virt-creating-vms-from-rh-images-overview" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Virtual machines (VMs) consist of a VM definition and one or more disks that are backed by data volumes. VM templates enable you to create VMs using predefined specifications.</p>
      <p>Every template requires a boot source, which is a fully configured disk image including configured drivers. Each template contains a VM definition with a pointer to the boot source. Each boot source has a predefined name and namespace. For some operating systems, a boot source is automatically provided. If it is not provided, then an administrator must prepare a custom boot source.</p>
      <p>Provided boot sources are updated automatically to the latest version of the operating system. For auto-updated boot sources, persistent volume claims (PVCs) and volume snapshots are created with the cluster’s default storage class. If you select a different default storage class after configuration, you must delete the existing boot sources in the cluster namespace that are configured with the previous default storage class.</p>
      <h2 id="virt-boot-source-images-namespace-web_virt-creating-vms-from-rh-images-overview" class="group scroll-mt-20">Configuring a custom namespace for boot source images by using the web console<a href="#virt-boot-source-images-namespace-web_virt-creating-vms-from-rh-images-overview" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can configure a custom namespace for boot source images in your cluster by using the OpenShift Container Platform web console.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the web console, select <strong>Virtualization</strong> → <strong>Settings</strong>.</li>
      <li>On the <strong>Cluster</strong> tab, select <strong>General settings</strong> → <strong>Templates and images management</strong>.</li>
      <li>Click <strong>Bootable volumes project</strong>.</li>
      <li>
      Select a namespace to use for boot source images.
      <ol>
      <li>If you already created a namespace, select it from the <strong>Project</strong> list.</li>
      <li>
      If you did not create a namespace, scroll to the bottom of the list and click <strong>Create project</strong>.
      <ol>
      <li>Enter a name for your new namespace in the <strong>Name</strong> field of the <strong>Create project</strong> dialog.</li>
      <li>Click <strong>Create</strong>.</li>
      </ol>
      </li>
      </ol>
      </li>
      </ol>
      <h2 id="virt-boot-source-images-namespace-cli_virt-creating-vms-from-rh-images-overview" class="group scroll-mt-20">Configuring a custom namespace for boot source images by using the CLI<a href="#virt-boot-source-images-namespace-cli_virt-creating-vms-from-rh-images-overview" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can configure a custom namespace for boot source images in your cluster by setting the <code>spec.commonBootImageNamespace</code> field in the <code>HyperConverged</code> custom resource (CR).</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You installed the OpenShift CLI (<code>oc</code>).</li>
      <li>You created a namespace to use for boot source images.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Open the <code>HyperConverged</code> CR in your default editor by running the following command:
      <CodeBlock code={"$ oc edit hyperconvergeds.v1beta1.hco.kubevirt.io kubevirt-hyperconverged -n openshift-cnv"} lang="terminal" />
      </li>
      <li>
      Configure the custom namespace by updating the value of the <code>spec.commonBootImageNamespace</code> field.
      <p>Example configuration file:</p>
      <CodeBlock code={"apiVersion: hco.kubevirt.io/v1beta1\nkind: HyperConverged\nmetadata:\n  name: kubevirt-hyperconverged\n  namespace: openshift-cnv\nspec:\n  commonBootImageNamespace: <custom_namespace>\n# ..."} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.commonBootImageNamespace</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the namespace to use for boot source images.</p></dd>
      </dl>
      </li>
      <li>Save your changes and exit the editor.</li>
      </ol>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/virt/storage/virt-automatic-bootsource-updates#virt-managing-auto-update-all-system-boot-sources_virt-automatic-bootsource-updates">Managing Red Hat boot source updates</a></li>
      <li><a href="/openshift-docs-markdown/virt/creating_vm/virt-creating-vms-from-templates#virt-creating-vms-from-templates">Creating a VM from a template by using the web console</a></li>
      <li><a href="/openshift-docs-markdown/virt/creating_vm/virt-creating-vms-from-instance-types#virt-creating-vms-from-instance-types">Creating a VM from an instance type by using the web console</a></li>
      <li><a href="/openshift-docs-markdown/virt/creating_vm/virt-creating-vms-from-cli#virt-creating-vms-from-cli">Creating a VM from a <code>VirtualMachine</code> manifest by using the command line</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
