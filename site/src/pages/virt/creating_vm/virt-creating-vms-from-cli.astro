---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/creating_vm/virt-creating-vms-from-cli.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"virt-creating-vms-from-cli","text":"Creating virtual machines by using the CLI"},{"depth":2,"slug":"virt-creating-vm-cli_virt-creating-vms-cli","text":"Creating a VM from a VirtualMachine manifest"},{"depth":2,"slug":"virt-uploading-image-virtctl_virt-creating-vms-cli","text":"Uploading a virtual machine image by using the CLI"},{"depth":3,"slug":"virt-supported-custom-video-devices_virt-creating-vms-cli","text":"Supported custom video device types"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/creating_vm/virt-creating-vms-from-cli";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Creating virtual machines by using the CLI";
const description = "You can create virtual machines (VMs) from the command line by editing or creating a VirtualMachine manifest. You can simplify VM configuration by using…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/creating_vm"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Creating virtual machines by using the CLI {#virt-creating-vms-from-cli}\n\nYou can create virtual machines (VMs) from the command line by editing or creating a `VirtualMachine` manifest. You can simplify VM configuration by using an instance type in your VM manifest.\n\n> [!NOTE]\n> You can also create VMs from instance types by using the OpenShift Container Platform web console.\n\n## Creating a VM from a VirtualMachine manifest {#virt-creating-vm-cli_virt-creating-vms-cli}\n\nYou can create a virtual machine (VM) from a `VirtualMachine` manifest. To simplify the creation of these manifests, you can use the `virtctl` command-line tool.\n\n**Prerequisites**\n\n- You have installed the `virtctl` CLI.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Create a `VirtualMachine` manifest for your VM and save it as a YAML file. For example, to create a minimal Red Hat Enterprise Linux (RHEL) VM, run the following command:\n\n   ```terminal\n   $ virtctl create vm --name rhel-9-minimal --volume-import type:ds,src:openshift-virtualization-os-images/rhel9\n   ```\n2. Review the `VirtualMachine` manifest for your VM:\n\n   > [!NOTE]\n   > This example manifest does not configure VM authentication.\n\n   ```yaml {title=\"Example manifest for a RHEL VM\"}\n   apiVersion: kubevirt.io/v1\n   kind: VirtualMachine\n   metadata:\n     name: rhel-9-minimal\n   spec:\n     dataVolumeTemplates:\n     - metadata:\n         name: imported-volume-mk4lj\n       spec:\n         sourceRef:\n           kind: DataSource\n           name: rhel9\n           namespace: openshift-virtualization-os-images\n         storage:\n           resources: {}\n     instancetype:\n       inferFromVolume: imported-volume-mk4lj\n       inferFromVolumeFailurePolicy: Ignore\n     preference:\n       inferFromVolume: imported-volume-mk4lj\n       inferFromVolumeFailurePolicy: Ignore\n     runStrategy: Always\n     template:\n       spec:\n         domain:\n           devices:\n             video:\n               type: virtio\n           memory:\n             guest: 512Mi\n           resources: {}\n         terminationGracePeriodSeconds: 180\n         volumes:\n         - dataVolume:\n             name: imported-volume-mk4lj\n           name: imported-volume-mk4lj\n   ```\n\n   - `name: rhel-9-minimal` specifies the name of the VM.\n   - `name: rhel9` specifies the boot source for the guest operating system in the `sourceRef` section.\n   - `namespace: openshift-virtualization-os-images` specifies the namespace for the boot source. Golden images are stored in the `openshift-virtualization-os-images` namespace.\n   - `instancetype: inferFromVolume: imported-volume-mk4lj` specifies the instance type inferred from the selected `DataSource` object.\n   - `preference: inferFromVolume: imported-volume-mk4lj` specifies that the preference is inferred from the selected `DataSource` object.\n   - `type: virtio` specifies the use of a custom video device (a VirtIO device in this example) to enable hardware graphics acceleration. Enabling a custom video device is in Technology Preview for OpenShift Virtualization 4.21.\n3. Create a virtual machine by using the manifest file:\n\n   ```terminal\n   $ oc create -f <vm_manifest_file>.yaml\n   ```\n4. Optional: Start the virtual machine:\n\n   ```terminal\n   $ virtctl start <vm_name>\n   ```\n\n## Uploading a virtual machine image by using the CLI {#virt-uploading-image-virtctl_virt-creating-vms-cli}\n\nYou can upload an operating system image by using the `virtctl` command-line tool. You can use an existing data volume or create a new data volume for the image.\n\n**Prerequisites**\n\n- You must have an `ISO`, `IMG`, or `QCOW2` operating system image file.\n- For best performance, compress the image file by using the [virt-sparsify](https://libguestfs.org/virt-sparsify.1.html) tool or the `xz` or `gzip` utilities.\n- The client machine must be configured to trust the OpenShift Container Platform router’s certificate.\n- You have installed the `virtctl` CLI.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Upload the image by running the `virtctl image-upload` command:\n\n   ```terminal\n   $ virtctl image-upload dv <datavolume_name> \\\n     --size=<datavolume_size> \\\n     --image-path=</path/to/image>\n   ```\n\n   `<datavolume_name>`\n   :   The name of the data volume.\n\n   `<datavolume_size>`\n   :   The size of the data volume. For example: `--size=500Mi`, `--size=1G`\n\n   `</path/to/image>`\n   :   The file path of the image.\n\n   > [!NOTE]\n   > - If you do not want to create a new data volume, omit the `--size` parameter and include the `--no-create` flag.\n   > - When uploading a disk image to a PVC, the PVC size must be larger than the size of the uncompressed virtual disk.\n   > - To allow insecure server connections when using HTTPS, use the `--insecure` parameter. When you use the `--insecure` flag, the authenticity of the upload endpoint is **not** verified.\n2. Optional. To verify that a data volume was created, view all data volumes by running the following command:\n\n   ```terminal\n   $ oc get dvs\n   ```\n\n### Supported custom video device types {#virt-supported-custom-video-devices_virt-creating-vms-cli}\n\nWhen creating a virtual machine (VM), you can configure a custom video device type to override the default video configuration.\n\nConfiguring a custom video device allows you to specify different video devices, based on your guest operating system requirements and performance needs.\n\n> [!IMPORTANT]\n> Custom video device support is a Technology Preview feature only. Technology Preview features are not supported with Red Hat production service level agreements (SLAs) and might not be functionally complete. Red Hat does not recommend using them in production. These features provide early access to upcoming product features, enabling customers to test functionality and provide feedback during the development process.\n>\n> For more information about the support scope of Red Hat Technology Preview features, see [Technology Preview Features Support Scope](https://access.redhat.com/support/offerings/techpreview/).\n\nUsing a custom video device  provides several advantages:\n\nPerformance\n:   Certain video devices provide better performance than the default configuration. For example, VirtIO is a more efficient video device on AMD/x86_64 architecture than legacy VGA.\n\nResolution flexibility\n:   With some video device types, you can set custom display resolutions.\n\nMemory efficiency\n:   Some video types are more memory efficient for headless or console-only operations.\n\nYou can configure the following video device types:\n\n- VirtIO: provides improved performance, and hardware-accelerated video decoding and encoding by offloading tasks to the host. Recommended for modern guest operating systems with available `VirtIO` drivers.\n- VGA: the standard for analog video display (default on AMD/x86_64 with BIOS).\n- Bochs: an emulated graphics adapter that provides a simple interface for guest operating systems to manage display settings (default on AMD/x86_64 with EFI).\n- Cirrus: a legacy video device that provides stable video output.\n- ramfb:  a simple, unaccelerated virtual display device primarily used in the QEMU emulator, and useful for ARM architecture.\n\n**Video device support by architecture**\n\n| Architecture | Boot mode | Default type | Supported types |\n| --- | --- | --- | --- |\n| AMD/x86_64 | BIOS | `vga` | `virtio`, `vga`, `bochs`, `cirrus`, \\`ramfb\\`\\` |\n| AMD/x86_64 | EFI | `bochs` | `virtio`, `vga`, `bochs`, `cirrus`, \\`ramfb\\`\\` |\n| ARM64 | BIOS/EFI | `virtio` | `virtio`, `ramfb` |\n| s390x | BIOS/EFI | `virtio` | `virtio` |\n\n**Additional resources**\n{._additional-resources}\n\n- [SSH access for virtual machines](/openshift-docs-markdown/virt/managing_vms/ssh/virt-accessing-vm-ssh#virt-accessing-vm-ssh)\n- [Creating virtual machines from instance types](/openshift-docs-markdown/virt/creating_vm/virt-creating-vms-from-instance-types#virt-creating-vms-from-instance-types)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/creating_vm/virt-creating-vms-from-cli.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-creating-vms-from-cli" class="group scroll-mt-20">Creating virtual machines by using the CLI<a href="#virt-creating-vms-from-cli" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">You can create virtual machines (VMs) from the command line by editing or creating a <code>VirtualMachine</code> manifest. You can simplify VM configuration by using an instance type in your VM manifest.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>You can also create VMs from instance types by using the OpenShift Container Platform web console.</p>
        </AlertDescription>
      </Alert>
      <h2 id="virt-creating-vm-cli_virt-creating-vms-cli" class="group scroll-mt-20">Creating a VM from a VirtualMachine manifest<a href="#virt-creating-vm-cli_virt-creating-vms-cli" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can create a virtual machine (VM) from a <code>VirtualMachine</code> manifest. To simplify the creation of these manifests, you can use the <code>virtctl</code> command-line tool.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have installed the <code>virtctl</code> CLI.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a <code>VirtualMachine</code> manifest for your VM and save it as a YAML file. For example, to create a minimal Red Hat Enterprise Linux (RHEL) VM, run the following command:
      <CodeBlock code={"$ virtctl create vm --name rhel-9-minimal --volume-import type:ds,src:openshift-virtualization-os-images/rhel9"} lang="terminal" />
      </li>
      <li>
      Review the <code>VirtualMachine</code> manifest for your VM:
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>This example manifest does not configure VM authentication.</p>
        </AlertDescription>
      </Alert>
      <CodeBlock code={"apiVersion: kubevirt.io/v1\nkind: VirtualMachine\nmetadata:\n  name: rhel-9-minimal\nspec:\n  dataVolumeTemplates:\n  - metadata:\n      name: imported-volume-mk4lj\n    spec:\n      sourceRef:\n        kind: DataSource\n        name: rhel9\n        namespace: openshift-virtualization-os-images\n      storage:\n        resources: {}\n  instancetype:\n    inferFromVolume: imported-volume-mk4lj\n    inferFromVolumeFailurePolicy: Ignore\n  preference:\n    inferFromVolume: imported-volume-mk4lj\n    inferFromVolumeFailurePolicy: Ignore\n  runStrategy: Always\n  template:\n    spec:\n      domain:\n        devices:\n          video:\n            type: virtio\n        memory:\n          guest: 512Mi\n        resources: {}\n      terminationGracePeriodSeconds: 180\n      volumes:\n      - dataVolume:\n          name: imported-volume-mk4lj\n        name: imported-volume-mk4lj"} lang="yaml" title="Example manifest for a RHEL VM" />
      <ul>
      <li><code>name: rhel-9-minimal</code> specifies the name of the VM.</li>
      <li><code>name: rhel9</code> specifies the boot source for the guest operating system in the <code>sourceRef</code> section.</li>
      <li><code>namespace: openshift-virtualization-os-images</code> specifies the namespace for the boot source. Golden images are stored in the <code>openshift-virtualization-os-images</code> namespace.</li>
      <li><code>instancetype: inferFromVolume: imported-volume-mk4lj</code> specifies the instance type inferred from the selected <code>DataSource</code> object.</li>
      <li><code>preference: inferFromVolume: imported-volume-mk4lj</code> specifies that the preference is inferred from the selected <code>DataSource</code> object.</li>
      <li><code>type: virtio</code> specifies the use of a custom video device (a VirtIO device in this example) to enable hardware graphics acceleration. Enabling a custom video device is in Technology Preview for OpenShift Virtualization 4.21.</li>
      </ul>
      </li>
      <li>
      Create a virtual machine by using the manifest file:
      <CodeBlock code={"$ oc create -f <vm_manifest_file>.yaml"} lang="terminal" />
      </li>
      <li>
      Optional: Start the virtual machine:
      <CodeBlock code={"$ virtctl start <vm_name>"} lang="terminal" />
      </li>
      </ol>
      <h2 id="virt-uploading-image-virtctl_virt-creating-vms-cli" class="group scroll-mt-20">Uploading a virtual machine image by using the CLI<a href="#virt-uploading-image-virtctl_virt-creating-vms-cli" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can upload an operating system image by using the <code>virtctl</code> command-line tool. You can use an existing data volume or create a new data volume for the image.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You must have an <code>ISO</code>, <code>IMG</code>, or <code>QCOW2</code> operating system image file.</li>
      <li>For best performance, compress the image file by using the <a href="https://libguestfs.org/virt-sparsify.1.html">virt-sparsify</a> tool or the <code>xz</code> or <code>gzip</code> utilities.</li>
      <li>The client machine must be configured to trust the OpenShift Container Platform router’s certificate.</li>
      <li>You have installed the <code>virtctl</code> CLI.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Upload the image by running the <code>virtctl image-upload</code> command:
      <CodeBlock code={"$ virtctl image-upload dv <datavolume_name> \\\n  --size=<datavolume_size> \\\n  --image-path=</path/to/image>"} lang="terminal" />
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;datavolume_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The name of the data volume.</p></dd>
      <dt class="font-semibold"><code>&lt;datavolume_size&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The size of the data volume. For example: <code>--size=500Mi</code>, <code>--size=1G</code></p></dd>
      <dt class="font-semibold"><code>&lt;/path/to/image&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The file path of the image.</p></dd>
      </dl>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <ul>
          <li>If you do not want to create a new data volume, omit the <code>--size</code> parameter and include the <code>--no-create</code> flag.</li>
          <li>When uploading a disk image to a PVC, the PVC size must be larger than the size of the uncompressed virtual disk.</li>
          <li>To allow insecure server connections when using HTTPS, use the <code>--insecure</code> parameter. When you use the <code>--insecure</code> flag, the authenticity of the upload endpoint is <strong>not</strong> verified.</li>
          </ul>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      Optional. To verify that a data volume was created, view all data volumes by running the following command:
      <CodeBlock code={"$ oc get dvs"} lang="terminal" />
      </li>
      </ol>
      <h3 id="virt-supported-custom-video-devices_virt-creating-vms-cli" class="group scroll-mt-20">Supported custom video device types<a href="#virt-supported-custom-video-devices_virt-creating-vms-cli" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">When creating a virtual machine (VM), you can configure a custom video device type to override the default video configuration.</p>
      <p>Configuring a custom video device allows you to specify different video devices, based on your guest operating system requirements and performance needs.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Custom video device support is a Technology Preview feature only. Technology Preview features are not supported with Red Hat production service level agreements (SLAs) and might not be functionally complete. Red Hat does not recommend using them in production. These features provide early access to upcoming product features, enabling customers to test functionality and provide feedback during the development process.</p>
          <p>For more information about the support scope of Red Hat Technology Preview features, see <a href="https://access.redhat.com/support/offerings/techpreview/">Technology Preview Features Support Scope</a>.</p>
        </AlertDescription>
      </Alert>
      <p>Using a custom video device  provides several advantages:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Performance</dt>
      <dd class="ml-6 text-muted-foreground"><p>Certain video devices provide better performance than the default configuration. For example, VirtIO is a more efficient video device on AMD/x86_64 architecture than legacy VGA.</p></dd>
      <dt class="font-semibold">Resolution flexibility</dt>
      <dd class="ml-6 text-muted-foreground"><p>With some video device types, you can set custom display resolutions.</p></dd>
      <dt class="font-semibold">Memory efficiency</dt>
      <dd class="ml-6 text-muted-foreground"><p>Some video types are more memory efficient for headless or console-only operations.</p></dd>
      </dl>
      <p>You can configure the following video device types:</p>
      <ul>
      <li>VirtIO: provides improved performance, and hardware-accelerated video decoding and encoding by offloading tasks to the host. Recommended for modern guest operating systems with available <code>VirtIO</code> drivers.</li>
      <li>VGA: the standard for analog video display (default on AMD/x86_64 with BIOS).</li>
      <li>Bochs: an emulated graphics adapter that provides a simple interface for guest operating systems to manage display settings (default on AMD/x86_64 with EFI).</li>
      <li>Cirrus: a legacy video device that provides stable video output.</li>
      <li>ramfb:  a simple, unaccelerated virtual display device primarily used in the QEMU emulator, and useful for ARM architecture.</li>
      </ul>
      <p><strong>Video device support by architecture</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Architecture</TableHead>
      <TableHead>Boot mode</TableHead>
      <TableHead>Default type</TableHead>
      <TableHead>Supported types</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>AMD/x86_64</TableCell>
      <TableCell>BIOS</TableCell>
      <TableCell><code>vga</code></TableCell>
      <TableCell><code>virtio</code>, <code>vga</code>, <code>bochs</code>, <code>cirrus</code>, `ramfb``</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>AMD/x86_64</TableCell>
      <TableCell>EFI</TableCell>
      <TableCell><code>bochs</code></TableCell>
      <TableCell><code>virtio</code>, <code>vga</code>, <code>bochs</code>, <code>cirrus</code>, `ramfb``</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>ARM64</TableCell>
      <TableCell>BIOS/EFI</TableCell>
      <TableCell><code>virtio</code></TableCell>
      <TableCell><code>virtio</code>, <code>ramfb</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell>s390x</TableCell>
      <TableCell>BIOS/EFI</TableCell>
      <TableCell><code>virtio</code></TableCell>
      <TableCell><code>virtio</code></TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/virt/managing_vms/ssh/virt-accessing-vm-ssh#virt-accessing-vm-ssh">SSH access for virtual machines</a></li>
      <li><a href="/openshift-docs-markdown/virt/creating_vm/virt-creating-vms-from-instance-types#virt-creating-vms-from-instance-types">Creating virtual machines from instance types</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
