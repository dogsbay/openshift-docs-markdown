---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/vm_networking/virt-configuring-physical-networks.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";

const headings = [{"depth":1,"slug":"configuring-physical-networks","text":"Configuring physical networks"},{"depth":2,"slug":"creating-a-physical-network_configuring-physical-networks","text":"Creating a physical network by using the OpenShift Container Platform web console"},{"depth":2,"slug":"expanding-physical-network_configuring-physical-networks","text":"Expanding a OpenShift Container Platform physical network to include new nodes"},{"depth":2,"slug":"creating-vm-network-localnet_configuring-physical-networks","text":"Creating a virtual machine network from a physical network"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/vm_networking/virt-configuring-physical-networks";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring physical networks";
const description = "As an OpenShift Container Platform administrator, you can create or configure a physical network in the OpenShift Container Platform web console without…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/vm_networking"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring physical networks {#configuring-physical-networks}\n\nAs an OpenShift Container Platform administrator, you can create or configure a physical network in the OpenShift Container Platform web console without using the node network configuration policy (NNCP) page.\n\n> [!NOTE]\n> The **Physical networks** page is available when the NMState console plugin is installed. When you use the **Physical networks** page in the web console, the NNCP is generated automatically. If you need more flexibility or require complex settings, use the NNCP page.\n\n## Creating a physical network by using the OpenShift Container Platform web console {#creating-a-physical-network_configuring-physical-networks}\n\nYou can create physical networks for OpenShift Virtualization using the OpenShift Container Platform web console to create a network with direct layer 2 connectivity to your data center.\n\n**Prerequisites**\n\n- You are logged in to the OpenShift Container Platform web console as a user with `cluster-admin` permissions.\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, go to **Networking** → **Physical networks**.\n2. Click **Create network**. The **Network configuration wizard** is displayed.\n3. On the **Network identity** page, enter a name for your network.\n4. On the **Nodes configuration** page, select either **Apply to all nodes on the cluster** or **Apply to specific subsets of nodes using the nodes selector**.\n\n   > [!NOTE]\n   > If you select specific nodes to apply the network to, you can view the matching nodes list to ensure the selection is correct. A validation error is displayed if the selected nodes overlap with another configuration associated with the same network.\n5. On the **Uplink connection** page, select the network interface that you want to connect to the physical network:\n\nDefault node network\n:   Uses the default node network to access the outside physical network.\n\nA single interface\n:   Select a specific physical network interface from the list.\n\n    > [!WARNING]\n    > If the selected secondary interface has an IP address on some of the nodes, using it removes the IP address and might disrupt network services.\n\n    Bonding interface\n    :   Configures bonded network interfaces to achieve resilience and higher throughput.\n\n1. Enter a **Bonding name**.\n2. Select the **Network interfaces** to bond.\n3. Select the **Aggregation mode** from the drop-down menu.\n4. On the **Settings** page, enter a **Bridge name** and set the **Maximum Transmission Unit (MTU)**.\n5. Review the configuration details.\n6. Click **Create**.\n\n**Verification**\n\n1. In the OpenShift Container Platform web console, go to **Networking** → **Physical networks**.\n2. Locate your new network in the list.\n3. Expand the network row to view the associated configurations. Verify that the **Enactment state** is **Available** and that the **Nodes** count matches your expectation.\n\n## Expanding a OpenShift Container Platform physical network to include new nodes {#expanding-physical-network_configuring-physical-networks}\n\nYou can add one or more OpenShift Container Platform worker nodes to an existing physical network if you want to expand access to that network. Expanding a physical network creates a new configuration under the same logical physical network.\n\n**Prerequisites**\n\n- You are logged in to the OpenShift Container Platform web console as a user with `cluster-admin` permissions.\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, go to **Networking** → **Physical networks**.\n2. Click the Options menu ![](/openshift-docs-markdown/images/kebab.png \"Options menu\") next to the network that you want to edit.\n3. Click **Configure nodes**. The **Network configuration wizard** is displayed.\n\n   > [!NOTE]\n   > The **Physical network name** is predefined. You cannot edit it during this process.\n4. Click **Next**.\n5. On the **Nodes configuration** page, select either **Apply to all nodes on the cluster** or **Apply to specific subsets of nodes using the nodes selector**.\n\n   > [!NOTE]\n   > If you select specific nodes, you can view the matching nodes list to ensure the selection is correct. A validation error is displayed if the selected nodes overlap with another configuration associated with the same network.\n6. On the **Uplink connection** page, select the network interface to connect to the physical network:\n\nDefault node network\n:   Uses the default node network to access the outside physical network.\n\nA single interface\n:   Select a specific physical network interface from the list.\n\n    > [!WARNING]\n    > If the selected secondary interface has an IP address on some of the nodes, using removes the IP address and might disrupt network services.\n\n    Bonding interface\n    :   Configures bonded network interfaces to achieve resilience and higher throughput.\n\n    1. Enter a **Bonding name**.\n    2. Select the **Network interfaces** to bond.\n\n    > [!NOTE]\n    > The system displays only the interfaces that all nodes have in common.\n\n    1. Select the **Aggregation mode** from the drop down options.\n\n1. On the **Settings** page, enter a **Bridge name** and set the **Maximum Transmission Unit (MTU)**.\n2. Review the configuration details.\n3. Click **Create**.\n\n## Creating a virtual machine network from a physical network {#creating-vm-network-localnet_configuring-physical-networks}\n\nIf your use case does not permit the use of network address translation (NAT), you can give VMs direct layer 2 access by creating a VM network that uses a physical network.\n\n**Prerequisites**\n\n- You are logged in to the OpenShift Container Platform web console as a user with `cluster-admin` permissions.\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, go to **Networking** → **Physical networks**.\n2. Click the Options menu ![](/openshift-docs-markdown/images/kebab.png \"Options menu\") next to the network that you want to edit.\n3. Click **Create a virtual machines network using this physical network**. The **Create virtual machine network** wizard is displayed with the network name populated.\n4. Select a **Physical network**.\n5. Optional: Select **VLAN tagging** and enter a **VLAN ID**.\n6. On the **Project mapping** page, define which projects can access this network.\n7. Click **Create**.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/vm_networking/virt-configuring-physical-networks.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="configuring-physical-networks" class="group scroll-mt-20">Configuring physical networks<a href="#configuring-physical-networks" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">As an OpenShift Container Platform administrator, you can create or configure a physical network in the OpenShift Container Platform web console without using the node network configuration policy (NNCP) page.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The <strong>Physical networks</strong> page is available when the NMState console plugin is installed. When you use the <strong>Physical networks</strong> page in the web console, the NNCP is generated automatically. If you need more flexibility or require complex settings, use the NNCP page.</p>
        </AlertDescription>
      </Alert>
      <h2 id="creating-a-physical-network_configuring-physical-networks" class="group scroll-mt-20">Creating a physical network by using the OpenShift Container Platform web console<a href="#creating-a-physical-network_configuring-physical-networks" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can create physical networks for OpenShift Virtualization using the OpenShift Container Platform web console to create a network with direct layer 2 connectivity to your data center.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You are logged in to the OpenShift Container Platform web console as a user with <code>cluster-admin</code> permissions.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, go to <strong>Networking</strong> → <strong>Physical networks</strong>.</li>
      <li>Click <strong>Create network</strong>. The <strong>Network configuration wizard</strong> is displayed.</li>
      <li>On the <strong>Network identity</strong> page, enter a name for your network.</li>
      <li>
      On the <strong>Nodes configuration</strong> page, select either <strong>Apply to all nodes on the cluster</strong> or <strong>Apply to specific subsets of nodes using the nodes selector</strong>.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If you select specific nodes to apply the network to, you can view the matching nodes list to ensure the selection is correct. A validation error is displayed if the selected nodes overlap with another configuration associated with the same network.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>On the <strong>Uplink connection</strong> page, select the network interface that you want to connect to the physical network:</li>
      </ol>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Default node network</dt>
      <dd class="ml-6 text-muted-foreground"><p>Uses the default node network to access the outside physical network.</p></dd>
      <dt class="font-semibold">A single interface</dt>
      <dd class="ml-6 text-muted-foreground">
      <p>Select a specific physical network interface from the list.</p>
      <Alert variant="warning" class="my-4">
        <AlertTitle>Warning</AlertTitle>
        <AlertDescription>
          <p>If the selected secondary interface has an IP address on some of the nodes, using it removes the IP address and might disrupt network services.</p>
        </AlertDescription>
      </Alert>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Bonding interface</dt>
      <dd class="ml-6 text-muted-foreground"><p>Configures bonded network interfaces to achieve resilience and higher throughput.</p></dd>
      </dl>
      </dd>
      </dl>
      <ol>
      <li>Enter a <strong>Bonding name</strong>.</li>
      <li>Select the <strong>Network interfaces</strong> to bond.</li>
      <li>Select the <strong>Aggregation mode</strong> from the drop-down menu.</li>
      <li>On the <strong>Settings</strong> page, enter a <strong>Bridge name</strong> and set the <strong>Maximum Transmission Unit (MTU)</strong>.</li>
      <li>Review the configuration details.</li>
      <li>Click <strong>Create</strong>.</li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, go to <strong>Networking</strong> → <strong>Physical networks</strong>.</li>
      <li>Locate your new network in the list.</li>
      <li>Expand the network row to view the associated configurations. Verify that the <strong>Enactment state</strong> is <strong>Available</strong> and that the <strong>Nodes</strong> count matches your expectation.</li>
      </ol>
      <h2 id="expanding-physical-network_configuring-physical-networks" class="group scroll-mt-20">Expanding a OpenShift Container Platform physical network to include new nodes<a href="#expanding-physical-network_configuring-physical-networks" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can add one or more OpenShift Container Platform worker nodes to an existing physical network if you want to expand access to that network. Expanding a physical network creates a new configuration under the same logical physical network.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You are logged in to the OpenShift Container Platform web console as a user with <code>cluster-admin</code> permissions.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, go to <strong>Networking</strong> → <strong>Physical networks</strong>.</li>
      <li>Click the Options menu <img src="/openshift-docs-markdown/images/kebab.png" alt="" loading="lazy" /> next to the network that you want to edit.</li>
      <li>
      Click <strong>Configure nodes</strong>. The <strong>Network configuration wizard</strong> is displayed.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The <strong>Physical network name</strong> is predefined. You cannot edit it during this process.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>Click <strong>Next</strong>.</li>
      <li>
      On the <strong>Nodes configuration</strong> page, select either <strong>Apply to all nodes on the cluster</strong> or <strong>Apply to specific subsets of nodes using the nodes selector</strong>.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If you select specific nodes, you can view the matching nodes list to ensure the selection is correct. A validation error is displayed if the selected nodes overlap with another configuration associated with the same network.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>On the <strong>Uplink connection</strong> page, select the network interface to connect to the physical network:</li>
      </ol>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Default node network</dt>
      <dd class="ml-6 text-muted-foreground"><p>Uses the default node network to access the outside physical network.</p></dd>
      <dt class="font-semibold">A single interface</dt>
      <dd class="ml-6 text-muted-foreground">
      <p>Select a specific physical network interface from the list.</p>
      <Alert variant="warning" class="my-4">
        <AlertTitle>Warning</AlertTitle>
        <AlertDescription>
          <p>If the selected secondary interface has an IP address on some of the nodes, using removes the IP address and might disrupt network services.</p>
        </AlertDescription>
      </Alert>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Bonding interface</dt>
      <dd class="ml-6 text-muted-foreground"><p>Configures bonded network interfaces to achieve resilience and higher throughput.</p></dd>
      </dl>
      <ol>
      <li>Enter a <strong>Bonding name</strong>.</li>
      <li>Select the <strong>Network interfaces</strong> to bond.</li>
      </ol>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The system displays only the interfaces that all nodes have in common.</p>
        </AlertDescription>
      </Alert>
      <ol><li>Select the <strong>Aggregation mode</strong> from the drop down options.</li></ol>
      </dd>
      </dl>
      <ol>
      <li>On the <strong>Settings</strong> page, enter a <strong>Bridge name</strong> and set the <strong>Maximum Transmission Unit (MTU)</strong>.</li>
      <li>Review the configuration details.</li>
      <li>Click <strong>Create</strong>.</li>
      </ol>
      <h2 id="creating-vm-network-localnet_configuring-physical-networks" class="group scroll-mt-20">Creating a virtual machine network from a physical network<a href="#creating-vm-network-localnet_configuring-physical-networks" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">If your use case does not permit the use of network address translation (NAT), you can give VMs direct layer 2 access by creating a VM network that uses a physical network.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You are logged in to the OpenShift Container Platform web console as a user with <code>cluster-admin</code> permissions.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, go to <strong>Networking</strong> → <strong>Physical networks</strong>.</li>
      <li>Click the Options menu <img src="/openshift-docs-markdown/images/kebab.png" alt="" loading="lazy" /> next to the network that you want to edit.</li>
      <li>Click <strong>Create a virtual machines network using this physical network</strong>. The <strong>Create virtual machine network</strong> wizard is displayed with the network name populated.</li>
      <li>Select a <strong>Physical network</strong>.</li>
      <li>Optional: Select <strong>VLAN tagging</strong> and enter a <strong>VLAN ID</strong>.</li>
      <li>On the <strong>Project mapping</strong> page, define which projects can access this network.</li>
      <li>Click <strong>Create</strong>.</li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
