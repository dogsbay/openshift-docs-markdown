---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/vm_networking/virt-hot-swap-vm-secondary-network.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-hot-swap-vm-secondary-network","text":"Hot swapping a virtual machine secondary network"},{"depth":2,"slug":"virt-vm-nw-hot-swap-limitations_virt-hot-swap-vm-secondary-network","text":"Hot swap limitations"},{"depth":2,"slug":"virt-live-updating-vm-nad-udn_virt-hot-swap-vm-secondary-network","text":"Hot swapping a virtual machine secondary network by using the command line"},{"depth":2,"slug":"additional-resources_virt-hot-swap-vm-secondary-network","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/vm_networking/virt-hot-swap-vm-secondary-network";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Hot swapping a virtual machine secondary network";
const description = "toc::[]" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/vm_networking"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Hot swapping a virtual machine secondary network {#virt-hot-swap-vm-secondary-network}\n\ntoc::\\[\\]\n\nYou can change the secondary network of a virtual machine (VM) without rebooting your VM. The change is transparent to the guest operating system, preserving properties like the MAC address.\n\nBy hot swapping the secondary network, you can move a running VM to a different network segment or VLAN and apply new network policies or reconfigure network topology without interrupting the workload. OpenShift Virtualization supports hot swapping for VMs that are connected to an OVN-Kubernetes localnet and a Linux bridge secondary network.\n\nTo hot swap a VM secondary network, you must edit the network configuration of the running VM to refer to a new `NetworkAttachmentDefinition` or `ClusterUserDefinedNetwork` manifest. This action triggers a live migration, connecting the VM to the new network without a reboot.\n\n## Hot swap limitations {#virt-vm-nw-hot-swap-limitations_virt-hot-swap-vm-secondary-network}\n\nOpenShift Virtualization supports hot swapping for VMs that are connected to an OVN-Kubernetes localnet and a Linux bridge secondary network.\n\nConsider the following limitations before hot swapping a VM secondary network:\n\n- Hot swapping only works for VMs that are live migratable.\n- Network connectivity might be interrupted during the live migration process.\n- If you update network references for multiple VMs, the updates might be queued because only a limited number of live migrations can run in parallel across the cluster.\n- You cannot hot swap to a new network binding type or a Container Network Interface (CNI) plugin. For example, you cannot change from bridge binding to SR-IOV binding.\n- The target `NetworkAttachmentDefinition` and `ClusterUserDefinedNetwork` objects must be valid and all referenced resources such as bridges, VLANs, and network resources must exist. Migration completes even if the network configuration is invalid, but the VM will lose network connectivity.\n- This feature applies only to secondary networks attached by using `NetworkAttachmentDefinition` or `ClusterUserDefinedNetwork` manifests. You cannot hot swap the primary pod network, regardless of whether it uses the default cluster network or a custom primary user-defined network.\n- If the new network requires a different IP configuration, such as a different subnet or gateway, you must reconfigure the guest operating system network settings. The hot swap does not automatically update the guest network configuration.\n\n## Hot swapping a virtual machine secondary network by using the command line {#virt-live-updating-vm-nad-udn_virt-hot-swap-vm-secondary-network}\n\nYou can hot swap a virtual machine (VM) secondary network by using the command line.\n\n**Prerequisites**\n\n- The VM to which you want to hot swap the network is running and is live migratable.\n- You have installed the OpenShift CLI (`oc`).\n- The target `NetworkAttachmentDefinition` object exists in the same namespace as the VM. If you created a `ClusterUserDefinedNetwork` object, verify that the cluster user-defined network controller has created the corresponding `NetworkAttachmentDefinition` object.\n\n  Example `NetworkAttachmentDefinition` manifest:\n\n  ```yaml\n  apiVersion: k8s.cni.cncf.io/v1\n  kind: NetworkAttachmentDefinition\n  metadata:\n    name: nad-with-vlan20\n  spec:\n    config: '{\n      \"cniVersion\": \"0.3.1\",\n      \"name\": \"nad-with-vlan20\",\n      \"type\": \"bridge\",\n      \"bridge\": \"br2\",\n      \"vlan\": 20\n    }'\n  ```\n\n**Procedure**\n\n1. Use your preferred text editor to edit the `VirtualMachine` manifest, as shown in the following example:\n\n   ```yaml\n   apiVersion: kubevirt.io/v1\n   kind: VirtualMachine\n   ...\n     template:\n       spec:\n         domain:\n           devices:\n             interfaces:\n             - bridge: {}\n               name: bridge-net\n         networks:\n         - name: bridge-net\n           multus:\n             networkName: nad-with-vlan20\n   #...\n   ```\n\n   - `spec.networks.name` specifies the name of the network. This must be the same as the `name` of the new network interface that you defined in the `template.spec.domain.devices.interfaces` list.\n   - `spec.networks.multus.networkName` specifies the name of the target `NetworkAttachmentDefinition` object.\n2. Save your changes and exit the editor.\n3. For the new configuration to take effect, apply the changes by running the following command. If your OpenShift Container Platform cluster has live migration enabled, applying the changes triggers automatic VM live migration and connects the new network to the running VM.\n\n   ```terminal\n   $ oc apply -f <filename>.yaml\n   ```\n\n   where:\n\n   `<filename>`\n   :   Specifies the name of your `VirtualMachine` manifest YAML file.\n\n**Verification**\n\n1. Verify that the VM live migration is progressing successfully by using the following command.\n\n   ```terminal\n   $ oc get vmi vm-fedora -w -o jsonpath='{.status.conditions[?(@.type==\"MigrationRequired\")]}{\"\\n\"}'\n   ```\n\n   Example output:\n\n   ```terminal\n   {\"type\":\"MigrationRequired\",\"status\":\"True\",\"lastProbeTime\":null,\"lastTransitionTime\":\"2024-05-27T10:15:30Z\",\"reason\":\"AutoMigrationDueToLiveUpdate\",\"message\":\"\"}\n   ```\n2. Use the following command to connect to the VM console and to devices on the new network:\n\n   ```terminal\n   $ virtctl console vm-fedora\n   ```\n\n## Additional resources {#additional-resources_virt-hot-swap-vm-secondary-network}\n\n- [About live migration](/openshift-docs-markdown/virt/live_migration/virt-about-live-migration#virt-about-live-migration-permissions_virt-about-live-migration)\n- [Connecting a virtual machine to a secondary localnet user-defined network](/openshift-docs-markdown/virt/vm_networking/virt-connecting-vm-to-secondary-udn#virt-connecting-vm-to-secondary-udn)\n- [Creating a Linux bridge network attachment definition](/openshift-docs-markdown/virt/vm_networking/virt-connecting-vm-to-linux-bridge#virt-connecting-vm-to-linux-bridge)\n- [Creating an SR-IOV network attachment definition](/openshift-docs-markdown/virt/vm_networking/virt-connecting-vm-to-sriov#nw-sriov-additional-network_virt-connecting-vm-to-sriov)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/vm_networking/virt-hot-swap-vm-secondary-network.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-hot-swap-vm-secondary-network" class="group scroll-mt-20">Hot swapping a virtual machine secondary network<a href="#virt-hot-swap-vm-secondary-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>toc::[]</p>
      <p>You can change the secondary network of a virtual machine (VM) without rebooting your VM. The change is transparent to the guest operating system, preserving properties like the MAC address.</p>
      <p>By hot swapping the secondary network, you can move a running VM to a different network segment or VLAN and apply new network policies or reconfigure network topology without interrupting the workload. OpenShift Virtualization supports hot swapping for VMs that are connected to an OVN-Kubernetes localnet and a Linux bridge secondary network.</p>
      <p>To hot swap a VM secondary network, you must edit the network configuration of the running VM to refer to a new <code>NetworkAttachmentDefinition</code> or <code>ClusterUserDefinedNetwork</code> manifest. This action triggers a live migration, connecting the VM to the new network without a reboot.</p>
      <h2 id="virt-vm-nw-hot-swap-limitations_virt-hot-swap-vm-secondary-network" class="group scroll-mt-20">Hot swap limitations<a href="#virt-vm-nw-hot-swap-limitations_virt-hot-swap-vm-secondary-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">OpenShift Virtualization supports hot swapping for VMs that are connected to an OVN-Kubernetes localnet and a Linux bridge secondary network.</p>
      <p>Consider the following limitations before hot swapping a VM secondary network:</p>
      <ul>
      <li>Hot swapping only works for VMs that are live migratable.</li>
      <li>Network connectivity might be interrupted during the live migration process.</li>
      <li>If you update network references for multiple VMs, the updates might be queued because only a limited number of live migrations can run in parallel across the cluster.</li>
      <li>You cannot hot swap to a new network binding type or a Container Network Interface (CNI) plugin. For example, you cannot change from bridge binding to SR-IOV binding.</li>
      <li>The target <code>NetworkAttachmentDefinition</code> and <code>ClusterUserDefinedNetwork</code> objects must be valid and all referenced resources such as bridges, VLANs, and network resources must exist. Migration completes even if the network configuration is invalid, but the VM will lose network connectivity.</li>
      <li>This feature applies only to secondary networks attached by using <code>NetworkAttachmentDefinition</code> or <code>ClusterUserDefinedNetwork</code> manifests. You cannot hot swap the primary pod network, regardless of whether it uses the default cluster network or a custom primary user-defined network.</li>
      <li>If the new network requires a different IP configuration, such as a different subnet or gateway, you must reconfigure the guest operating system network settings. The hot swap does not automatically update the guest network configuration.</li>
      </ul>
      <h2 id="virt-live-updating-vm-nad-udn_virt-hot-swap-vm-secondary-network" class="group scroll-mt-20">Hot swapping a virtual machine secondary network by using the command line<a href="#virt-live-updating-vm-nad-udn_virt-hot-swap-vm-secondary-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can hot swap a virtual machine (VM) secondary network by using the command line.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>The VM to which you want to hot swap the network is running and is live migratable.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      <li>
      The target <code>NetworkAttachmentDefinition</code> object exists in the same namespace as the VM. If you created a <code>ClusterUserDefinedNetwork</code> object, verify that the cluster user-defined network controller has created the corresponding <code>NetworkAttachmentDefinition</code> object.
      <p>Example <code>NetworkAttachmentDefinition</code> manifest:</p>
      <CodeBlock code={"apiVersion: k8s.cni.cncf.io/v1\nkind: NetworkAttachmentDefinition\nmetadata:\n  name: nad-with-vlan20\nspec:\n  config: '{\n    \"cniVersion\": \"0.3.1\",\n    \"name\": \"nad-with-vlan20\",\n    \"type\": \"bridge\",\n    \"bridge\": \"br2\",\n    \"vlan\": 20\n  }'"} lang="yaml" />
      </li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Use your preferred text editor to edit the <code>VirtualMachine</code> manifest, as shown in the following example:
      <CodeBlock code={"apiVersion: kubevirt.io/v1\nkind: VirtualMachine\n...\n  template:\n    spec:\n      domain:\n        devices:\n          interfaces:\n          - bridge: {}\n            name: bridge-net\n      networks:\n      - name: bridge-net\n        multus:\n          networkName: nad-with-vlan20\n#..."} lang="yaml" />
      <ul>
      <li><code>spec.networks.name</code> specifies the name of the network. This must be the same as the <code>name</code> of the new network interface that you defined in the <code>template.spec.domain.devices.interfaces</code> list.</li>
      <li><code>spec.networks.multus.networkName</code> specifies the name of the target <code>NetworkAttachmentDefinition</code> object.</li>
      </ul>
      </li>
      <li>Save your changes and exit the editor.</li>
      <li>
      For the new configuration to take effect, apply the changes by running the following command. If your OpenShift Container Platform cluster has live migration enabled, applying the changes triggers automatic VM live migration and connects the new network to the running VM.
      <CodeBlock code={"$ oc apply -f <filename>.yaml"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;filename&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of your <code>VirtualMachine</code> manifest YAML file.</p></dd>
      </dl>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>
      Verify that the VM live migration is progressing successfully by using the following command.
      <CodeBlock code={"$ oc get vmi vm-fedora -w -o jsonpath='{.status.conditions[?(@.type==\"MigrationRequired\")]}{\"\\n\"}'"} lang="terminal" />
      <p>Example output:</p>
      <CodeBlock code={"{\"type\":\"MigrationRequired\",\"status\":\"True\",\"lastProbeTime\":null,\"lastTransitionTime\":\"2024-05-27T10:15:30Z\",\"reason\":\"AutoMigrationDueToLiveUpdate\",\"message\":\"\"}"} lang="terminal" />
      </li>
      <li>
      Use the following command to connect to the VM console and to devices on the new network:
      <CodeBlock code={"$ virtctl console vm-fedora"} lang="terminal" />
      </li>
      </ol>
      <h2 id="additional-resources_virt-hot-swap-vm-secondary-network" class="group scroll-mt-20">Additional resources<a href="#additional-resources_virt-hot-swap-vm-secondary-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/virt/live_migration/virt-about-live-migration#virt-about-live-migration-permissions_virt-about-live-migration">About live migration</a></li>
      <li><a href="/openshift-docs-markdown/virt/vm_networking/virt-connecting-vm-to-secondary-udn#virt-connecting-vm-to-secondary-udn">Connecting a virtual machine to a secondary localnet user-defined network</a></li>
      <li><a href="/openshift-docs-markdown/virt/vm_networking/virt-connecting-vm-to-linux-bridge#virt-connecting-vm-to-linux-bridge">Creating a Linux bridge network attachment definition</a></li>
      <li><a href="/openshift-docs-markdown/virt/vm_networking/virt-connecting-vm-to-sriov#nw-sriov-additional-network_virt-connecting-vm-to-sriov">Creating an SR-IOV network attachment definition</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
