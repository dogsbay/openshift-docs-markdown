---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/vm_networking/virt-connecting-vm-to-service-mesh.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-connecting-vm-to-service-mesh","text":"Connecting a virtual machine to a service mesh"},{"depth":2,"slug":"virt-adding-vm-to-service-mesh_virt-connecting-vm-to-service-mesh","text":"Adding a virtual machine to a service mesh"},{"depth":2,"slug":"additional-resources_virt-connecting-vm-to-service-mesh","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/vm_networking/virt-connecting-vm-to-service-mesh";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Connecting a virtual machine to a service mesh";
const description = "OpenShift Virtualization is now integrated with Red Hat OpenShift Service Mesh. You can monitor, visualize, and control traffic between pods that run…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/vm_networking"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Connecting a virtual machine to a service mesh {#virt-connecting-vm-to-service-mesh}\n\nOpenShift Virtualization is now integrated with Red Hat OpenShift Service Mesh. You can monitor, visualize, and control traffic between pods that run virtual machine (VM) workloads on the default pod network with IPv4.\n\n## Adding a virtual machine to a service mesh {#virt-adding-vm-to-service-mesh_virt-connecting-vm-to-service-mesh}\n\nTo add a virtual machine (VM) workload to a service mesh, enable automatic sidecar injection in the VM configuration file by setting the `sidecar.istio.io/inject` annotation to `true`. Then expose your VM as a service to view your application in the mesh.\n\n> [!IMPORTANT]\n> To avoid port conflicts, do not use ports used by the Istio sidecar proxy. These include ports 15000, 15001, 15006, 15008, 15020, 15021, and 15090.\n\n**Prerequisites**\n\n- You have installed the OpenShift CLI (`oc`).\n- You have installed the Service Mesh Operator.\n\n**Procedure**\n\n1. Edit the VM configuration file to add the `sidecar.istio.io/inject: \"true\"` annotation.\n\n   Example configuration file:\n\n   ```yaml\n   apiVersion: kubevirt.io/v1\n   kind: VirtualMachine\n   metadata:\n     labels:\n       kubevirt.io/vm: vm-istio\n     name: vm-istio\n   spec:\n     runStrategy: Always\n     template:\n       metadata:\n         labels:\n           kubevirt.io/vm: vm-istio\n           app: vm-istio\n         annotations:\n           sidecar.istio.io/inject: \"true\"\n       spec:\n         domain:\n           devices:\n             interfaces:\n             - name: default\n               masquerade: {}\n             disks:\n             - disk:\n                 bus: virtio\n               name: containerdisk\n             - disk:\n                 bus: virtio\n               name: cloudinitdisk\n           resources:\n             requests:\n               memory: 1024M\n         networks:\n         - name: default\n           pod: {}\n         terminationGracePeriodSeconds: 180\n         volumes:\n         - containerDisk:\n             image: registry:5000/kubevirt/fedora-cloud-container-disk-demo:devel\n           name: containerdisk\n   ```\n\n   - `spec.template.metadata.labels.app` specifies the key/value pair (label) that must be matched to the service selector attribute.\n   - `spec.template.metadata.annotations.sidecar.istio.io/inject` is the annotation to enable automatic sidecar injection.\n   - `spec.template.spec.domain.devices.interfaces.masquerade` is the binding method (masquerade mode) for use with the default pod network.\n2. Run the following command to apply the VM configuration:\n\n   ```terminal\n   $ oc apply -f <vm_name>.yaml\n   ```\n\n   where:\n\n   `<vm_name>`\n   :   Specifies the name of the virtual machine YAML file.\n3. Create a `Service` object to expose your VM to the service mesh:\n\n   ```yaml\n   apiVersion: v1\n   kind: Service\n   metadata:\n     name: vm-istio\n   spec:\n     selector:\n       app: vm-istio\n     ports:\n       - port: 8080\n         name: http\n         protocol: TCP\n   ```\n\n   - `spec.selector.app` specifies the service selector that determines the set of pods targeted by a service. This attribute corresponds to the `spec.metadata.labels` field in the VM configuration file. In the above example, the `Service` object named `vm-istio` targets TCP port 8080 on any pod with the label `app=vm-istio`.\n4. Run the following command to create the service:\n\n   ```terminal\n   $ oc create -f <service_name>.yaml\n   ```\n\n   where:\n\n   `<service_name>`\n   :   Specifies the name of the service YAML file.\n\n## Additional resources {#additional-resources_virt-connecting-vm-to-service-mesh}\n\n- [Installing the Service Mesh Operator](https://docs.redhat.com/en/documentation/red_hat_openshift_service_mesh/3.0/html/installing/ossm-installing-service-mesh)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/vm_networking/virt-connecting-vm-to-service-mesh.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-connecting-vm-to-service-mesh" class="group scroll-mt-20">Connecting a virtual machine to a service mesh<a href="#virt-connecting-vm-to-service-mesh" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>OpenShift Virtualization is now integrated with Red Hat OpenShift Service Mesh. You can monitor, visualize, and control traffic between pods that run virtual machine (VM) workloads on the default pod network with IPv4.</p>
      <h2 id="virt-adding-vm-to-service-mesh_virt-connecting-vm-to-service-mesh" class="group scroll-mt-20">Adding a virtual machine to a service mesh<a href="#virt-adding-vm-to-service-mesh_virt-connecting-vm-to-service-mesh" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To add a virtual machine (VM) workload to a service mesh, enable automatic sidecar injection in the VM configuration file by setting the <code>sidecar.istio.io/inject</code> annotation to <code>true</code>. Then expose your VM as a service to view your application in the mesh.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>To avoid port conflicts, do not use ports used by the Istio sidecar proxy. These include ports 15000, 15001, 15006, 15008, 15020, 15021, and 15090.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      <li>You have installed the Service Mesh Operator.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Edit the VM configuration file to add the <code>sidecar.istio.io/inject: "true"</code> annotation.
      <p>Example configuration file:</p>
      <CodeBlock code={"apiVersion: kubevirt.io/v1\nkind: VirtualMachine\nmetadata:\n  labels:\n    kubevirt.io/vm: vm-istio\n  name: vm-istio\nspec:\n  runStrategy: Always\n  template:\n    metadata:\n      labels:\n        kubevirt.io/vm: vm-istio\n        app: vm-istio\n      annotations:\n        sidecar.istio.io/inject: \"true\"\n    spec:\n      domain:\n        devices:\n          interfaces:\n          - name: default\n            masquerade: {}\n          disks:\n          - disk:\n              bus: virtio\n            name: containerdisk\n          - disk:\n              bus: virtio\n            name: cloudinitdisk\n        resources:\n          requests:\n            memory: 1024M\n      networks:\n      - name: default\n        pod: {}\n      terminationGracePeriodSeconds: 180\n      volumes:\n      - containerDisk:\n          image: registry:5000/kubevirt/fedora-cloud-container-disk-demo:devel\n        name: containerdisk"} lang="yaml" />
      <ul>
      <li><code>spec.template.metadata.labels.app</code> specifies the key/value pair (label) that must be matched to the service selector attribute.</li>
      <li><code>spec.template.metadata.annotations.sidecar.istio.io/inject</code> is the annotation to enable automatic sidecar injection.</li>
      <li><code>spec.template.spec.domain.devices.interfaces.masquerade</code> is the binding method (masquerade mode) for use with the default pod network.</li>
      </ul>
      </li>
      <li>
      Run the following command to apply the VM configuration:
      <CodeBlock code={"$ oc apply -f <vm_name>.yaml"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;vm_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the virtual machine YAML file.</p></dd>
      </dl>
      </li>
      <li>
      Create a <code>Service</code> object to expose your VM to the service mesh:
      <CodeBlock code={"apiVersion: v1\nkind: Service\nmetadata:\n  name: vm-istio\nspec:\n  selector:\n    app: vm-istio\n  ports:\n    - port: 8080\n      name: http\n      protocol: TCP"} lang="yaml" />
      <ul><li><code>spec.selector.app</code> specifies the service selector that determines the set of pods targeted by a service. This attribute corresponds to the <code>spec.metadata.labels</code> field in the VM configuration file. In the above example, the <code>Service</code> object named <code>vm-istio</code> targets TCP port 8080 on any pod with the label <code>app=vm-istio</code>.</li></ul>
      </li>
      <li>
      Run the following command to create the service:
      <CodeBlock code={"$ oc create -f <service_name>.yaml"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;service_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the service YAML file.</p></dd>
      </dl>
      </li>
      </ol>
      <h2 id="additional-resources_virt-connecting-vm-to-service-mesh" class="group scroll-mt-20">Additional resources<a href="#additional-resources_virt-connecting-vm-to-service-mesh" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul><li><a href="https://docs.redhat.com/en/documentation/red_hat_openshift_service_mesh/3.0/html/installing/ossm-installing-service-mesh">Installing the Service Mesh Operator</a></li></ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
