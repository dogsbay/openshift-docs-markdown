---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/vm_networking/virt-connecting-vm-to-ovn-secondary-network.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-connecting-vm-to-ovn-secondary-network","text":"Connecting a virtual machine to an OVN-Kubernetes layer 2 secondary network"},{"depth":2,"slug":"virt-creating-layer2-nad-cli_virt-connecting-vm-to-ovn-secondary-network","text":"Creating a NAD for layer 2 topology by using the CLI"},{"depth":2,"slug":"virt-creating-nad-l2-overlay-console_virt-connecting-vm-to-ovn-secondary-network","text":"Creating a NAD for layer 2 topology by using the web console"},{"depth":2,"slug":"virt-attaching-vm-to-ovn-secondary-nw-cli_virt-connecting-vm-to-ovn-secondary-network","text":"Attaching a virtual machine to an OVN-Kubernetes secondary network using the CLI"},{"depth":2,"slug":"additional-resources_virt-connecting-vm-to-ovn-secondary-network","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/vm_networking/virt-connecting-vm-to-ovn-secondary-network";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Connecting a virtual machine to an OVN-Kubernetes layer 2 secondary network";
const description = "You can connect a virtual machine (VM) to an OVN-Kubernetes custom secondary overlay network. You can use this overlay network to connect VMs on different…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/vm_networking"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Connecting a virtual machine to an OVN-Kubernetes layer 2 secondary network {#virt-connecting-vm-to-ovn-secondary-network}\n\nYou can connect a virtual machine (VM) to an OVN-Kubernetes custom secondary overlay network. You can use this overlay network to connect VMs on different nodes, without configuring any additional physical networking infrastructure.\n\n> [!NOTE]\n> An OVN-Kubernetes secondary network is compatible with the multi-network policy API which provides the `MultiNetworkPolicy` custom resource definition (CRD) to control traffic flow to and from VMs. You must use the `ipBlock` attribute to define network policy ingress and egress rules for specific CIDR blocks. You cannot use pod or namespace selectors for virtualization workloads.\n\nA layer 2 topology connects workloads by a cluster-wide logical switch. The OVN-Kubernetes Container Network Interface (CNI) plugin uses the Geneve (Generic Network Virtualization Encapsulation) protocol to create an overlay network between nodes.\n\nTo configure an OVN-Kubernetes layer 2 secondary network and attach a VM to that network, perform the following steps:\n\n1. Define the secondary network\n2. Attach the VM to the secondary network\n\n> [!NOTE]\n> Configuring IP address management (IPAM) by specifying the `spec.config.ipam.subnet` attribute in a network attachment definition for virtual machines is not supported.\n\n## Creating a NAD for layer 2 topology by using the CLI {#virt-creating-layer2-nad-cli_virt-connecting-vm-to-ovn-secondary-network}\n\nYou can create a network attachment definition (NAD) which describes how to attach a pod to the layer 2 overlay network.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with `cluster-admin` privileges.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Create a `NetworkAttachmentDefinition` object:\n\n   ```yaml\n   apiVersion: k8s.cni.cncf.io/v1\n   kind: NetworkAttachmentDefinition\n   metadata:\n     name: l2-network\n     namespace: my-namespace\n   spec:\n     config: |-\n       {\n               \"cniVersion\": \"0.3.1\",\n               \"name\": \"my-namespace-l2-network\",\n               \"type\": \"ovn-k8s-cni-overlay\",\n               \"topology\":\"layer2\",\n               \"mtu\": 1400,\n               \"netAttachDefName\": \"my-namespace/l2-network\"\n       }\n   ```\n\n   - `spec.config.cniVersion` defines the Container Network Interface (CNI) specification version. The required value is `0.3.1`.\n   - `spec.config.name` defines the name of the network. This attribute is not namespaced. For example, you can have a network named `l2-network` referenced from two different `NetworkAttachmentDefinition` objects that exist in two different namespaces. This feature is useful to connect VMs in different namespaces.\n   - `spec.config.type` defines the name of the CNI plugin. The required value is `ovn-k8s-cni-overlay`.\n   - `spec.config.topology` defines the topological configuration for the network. The required value is `layer2`.\n   - `spec.config.mtu` is optional and defines the maximum transmission unit (MTU) value. If you do not set a value, the Cluster Network Operator (CNO) sets a default MTU value by calculating the difference among the underlay MTU of the primary network interface, the overlay MTU of the pod network, such as the Geneve (Generic Network Virtualization Encapsulation), and byte capacity of any enabled features, such as IPsec.\n   - `spec.config.netAttachDefName` defines the value of the `namespace` and `name` fields in the `metadata` stanza of the `NetworkAttachmentDefinition` object.\n\n     > [!NOTE]\n     > The previous example configures a cluster-wide overlay without a subnet defined. This means that the logical switch implementing the network only provides layer 2 communication. You must configure an IP address when you create the virtual machine by either setting a static IP address or by deploying a DHCP server on the network for a dynamic IP address.\n2. Apply the manifest by running the following command:\n\n   ```terminal\n   $ oc apply -f <filename>.yaml\n   ```\n\n## Creating a NAD for layer 2 topology by using the web console {#virt-creating-nad-l2-overlay-console_virt-connecting-vm-to-ovn-secondary-network}\n\nYou can create a network attachment definition (NAD) that describes how to attach a pod to the layer 2 overlay network.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with `cluster-admin` privileges.\n\n**Procedure**\n\n1. Go to **Networking** -> **NetworkAttachmentDefinitions** in the web console.\n2. Click **Create Network Attachment Definition**. The network attachment definition must be in the same namespace as the pod or virtual machine using it.\n3. Enter a unique **Name** and optional **Description**.\n4. Select **OVN Kubernetes L2 overlay network** from the **Network Type** list.\n5. Click **Create**.\n\n## Attaching a virtual machine to an OVN-Kubernetes secondary network using the CLI {#virt-attaching-vm-to-ovn-secondary-nw-cli_virt-connecting-vm-to-ovn-secondary-network}\n\nYou can connect a virtual machine (VM) to the OVN-Kubernetes secondary network by including the network details in the VM configuration.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with `cluster-admin` privileges.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Edit the `VirtualMachine` manifest to add the OVN-Kubernetes secondary network interface details, as in the following example:\n\n   ```yaml\n   apiVersion: kubevirt.io/v1\n   kind: VirtualMachine\n   metadata:\n     name: vm-server\n   spec:\n     runStrategy: Always\n     template:\n       spec:\n         domain:\n           devices:\n             interfaces:\n             - name: secondary\n               bridge: {}\n           resources:\n             requests:\n               memory: 1024Mi\n         networks:\n         - name: secondary\n           multus:\n             networkName: <nad_name>\n         nodeSelector:\n           node-role.kubernetes.io/worker: ''\n   # ...\n   ```\n\n   - `spec.template.spec.domain.devices.interfaces.name` specifies the name of the OVN-Kubernetes secondary interface.\n   - `spec.template.spec.networks.name` specifies the name of the network. This must match the value of the `spec.template.spec.domain.devices.interfaces.name` field.\n   - `spec.template.spec.networks.multus.networkName` specifies the name of the `NetworkAttachmentDefinition` object.\n   - `spec.template.spec.nodeSelector` specifies the nodes on which the VM can be scheduled. The recommended node selector value is `node-role.kubernetes.io/worker: ''`.\n2. Apply the `VirtualMachine` manifest:\n\n   ```terminal\n   $ oc apply -f <filename>.yaml\n   ```\n3. Optional: If you edited a running virtual machine, you must restart it for the changes to take effect.\n\n## Additional resources {#additional-resources_virt-connecting-vm-to-ovn-secondary-network}\n\n- [Creating secondary networks on OVN-Kubernetes](/openshift-docs-markdown/networking/multiple_networks/secondary_networks/creating-secondary-nwt-ovnk#configuration-ovnk-additional-networks_configuring-additional-network-ovnk)\n- [About the Kubernetes NMState Operator](/openshift-docs-markdown/networking/networking_operators/k8s-nmstate-about-the-k8s-nmstate-operator#k8s-nmstate-about-the-k8s-nmstate-operator)\n- [Multi-network policy API](/openshift-docs-markdown/networking/multiple_networks/secondary_networks/configuring-multi-network-policy#configuring-multi-network-policy)\n- [Creating primary networks by using a network attachment definition](/openshift-docs-markdown/networking/multiple_networks/primary_networks/about-primary-nwt-nad#about-primary-nwt-nad)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/vm_networking/virt-connecting-vm-to-ovn-secondary-network.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-connecting-vm-to-ovn-secondary-network" class="group scroll-mt-20">Connecting a virtual machine to an OVN-Kubernetes layer 2 secondary network<a href="#virt-connecting-vm-to-ovn-secondary-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can connect a virtual machine (VM) to an OVN-Kubernetes custom secondary overlay network. You can use this overlay network to connect VMs on different nodes, without configuring any additional physical networking infrastructure.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>An OVN-Kubernetes secondary network is compatible with the multi-network policy API which provides the <code>MultiNetworkPolicy</code> custom resource definition (CRD) to control traffic flow to and from VMs. You must use the <code>ipBlock</code> attribute to define network policy ingress and egress rules for specific CIDR blocks. You cannot use pod or namespace selectors for virtualization workloads.</p>
        </AlertDescription>
      </Alert>
      <p>A layer 2 topology connects workloads by a cluster-wide logical switch. The OVN-Kubernetes Container Network Interface (CNI) plugin uses the Geneve (Generic Network Virtualization Encapsulation) protocol to create an overlay network between nodes.</p>
      <p>To configure an OVN-Kubernetes layer 2 secondary network and attach a VM to that network, perform the following steps:</p>
      <ol>
      <li>Define the secondary network</li>
      <li>Attach the VM to the secondary network</li>
      </ol>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Configuring IP address management (IPAM) by specifying the <code>spec.config.ipam.subnet</code> attribute in a network attachment definition for virtual machines is not supported.</p>
        </AlertDescription>
      </Alert>
      <h2 id="virt-creating-layer2-nad-cli_virt-connecting-vm-to-ovn-secondary-network" class="group scroll-mt-20">Creating a NAD for layer 2 topology by using the CLI<a href="#virt-creating-layer2-nad-cli_virt-connecting-vm-to-ovn-secondary-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can create a network attachment definition (NAD) which describes how to attach a pod to the layer 2 overlay network.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with <code>cluster-admin</code> privileges.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a <code>NetworkAttachmentDefinition</code> object:
      <CodeBlock code={"apiVersion: k8s.cni.cncf.io/v1\nkind: NetworkAttachmentDefinition\nmetadata:\n  name: l2-network\n  namespace: my-namespace\nspec:\n  config: |-\n    {\n            \"cniVersion\": \"0.3.1\",\n            \"name\": \"my-namespace-l2-network\",\n            \"type\": \"ovn-k8s-cni-overlay\",\n            \"topology\":\"layer2\",\n            \"mtu\": 1400,\n            \"netAttachDefName\": \"my-namespace/l2-network\"\n    }"} lang="yaml" />
      <ul>
      <li><code>spec.config.cniVersion</code> defines the Container Network Interface (CNI) specification version. The required value is <code>0.3.1</code>.</li>
      <li><code>spec.config.name</code> defines the name of the network. This attribute is not namespaced. For example, you can have a network named <code>l2-network</code> referenced from two different <code>NetworkAttachmentDefinition</code> objects that exist in two different namespaces. This feature is useful to connect VMs in different namespaces.</li>
      <li><code>spec.config.type</code> defines the name of the CNI plugin. The required value is <code>ovn-k8s-cni-overlay</code>.</li>
      <li><code>spec.config.topology</code> defines the topological configuration for the network. The required value is <code>layer2</code>.</li>
      <li><code>spec.config.mtu</code> is optional and defines the maximum transmission unit (MTU) value. If you do not set a value, the Cluster Network Operator (CNO) sets a default MTU value by calculating the difference among the underlay MTU of the primary network interface, the overlay MTU of the pod network, such as the Geneve (Generic Network Virtualization Encapsulation), and byte capacity of any enabled features, such as IPsec.</li>
      <li>
      <code>spec.config.netAttachDefName</code> defines the value of the <code>namespace</code> and <code>name</code> fields in the <code>metadata</code> stanza of the <code>NetworkAttachmentDefinition</code> object.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The previous example configures a cluster-wide overlay without a subnet defined. This means that the logical switch implementing the network only provides layer 2 communication. You must configure an IP address when you create the virtual machine by either setting a static IP address or by deploying a DHCP server on the network for a dynamic IP address.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      </li>
      <li>
      Apply the manifest by running the following command:
      <CodeBlock code={"$ oc apply -f <filename>.yaml"} lang="terminal" />
      </li>
      </ol>
      <h2 id="virt-creating-nad-l2-overlay-console_virt-connecting-vm-to-ovn-secondary-network" class="group scroll-mt-20">Creating a NAD for layer 2 topology by using the web console<a href="#virt-creating-nad-l2-overlay-console_virt-connecting-vm-to-ovn-secondary-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can create a network attachment definition (NAD) that describes how to attach a pod to the layer 2 overlay network.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have access to the cluster as a user with <code>cluster-admin</code> privileges.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Go to <strong>Networking</strong> -&gt; <strong>NetworkAttachmentDefinitions</strong> in the web console.</li>
      <li>Click <strong>Create Network Attachment Definition</strong>. The network attachment definition must be in the same namespace as the pod or virtual machine using it.</li>
      <li>Enter a unique <strong>Name</strong> and optional <strong>Description</strong>.</li>
      <li>Select <strong>OVN Kubernetes L2 overlay network</strong> from the <strong>Network Type</strong> list.</li>
      <li>Click <strong>Create</strong>.</li>
      </ol>
      <h2 id="virt-attaching-vm-to-ovn-secondary-nw-cli_virt-connecting-vm-to-ovn-secondary-network" class="group scroll-mt-20">Attaching a virtual machine to an OVN-Kubernetes secondary network using the CLI<a href="#virt-attaching-vm-to-ovn-secondary-nw-cli_virt-connecting-vm-to-ovn-secondary-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can connect a virtual machine (VM) to the OVN-Kubernetes secondary network by including the network details in the VM configuration.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with <code>cluster-admin</code> privileges.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Edit the <code>VirtualMachine</code> manifest to add the OVN-Kubernetes secondary network interface details, as in the following example:
      <CodeBlock code={"apiVersion: kubevirt.io/v1\nkind: VirtualMachine\nmetadata:\n  name: vm-server\nspec:\n  runStrategy: Always\n  template:\n    spec:\n      domain:\n        devices:\n          interfaces:\n          - name: secondary\n            bridge: {}\n        resources:\n          requests:\n            memory: 1024Mi\n      networks:\n      - name: secondary\n        multus:\n          networkName: <nad_name>\n      nodeSelector:\n        node-role.kubernetes.io/worker: ''\n# ..."} lang="yaml" />
      <ul>
      <li><code>spec.template.spec.domain.devices.interfaces.name</code> specifies the name of the OVN-Kubernetes secondary interface.</li>
      <li><code>spec.template.spec.networks.name</code> specifies the name of the network. This must match the value of the <code>spec.template.spec.domain.devices.interfaces.name</code> field.</li>
      <li><code>spec.template.spec.networks.multus.networkName</code> specifies the name of the <code>NetworkAttachmentDefinition</code> object.</li>
      <li><code>spec.template.spec.nodeSelector</code> specifies the nodes on which the VM can be scheduled. The recommended node selector value is <code>node-role.kubernetes.io/worker: ''</code>.</li>
      </ul>
      </li>
      <li>
      Apply the <code>VirtualMachine</code> manifest:
      <CodeBlock code={"$ oc apply -f <filename>.yaml"} lang="terminal" />
      </li>
      <li>Optional: If you edited a running virtual machine, you must restart it for the changes to take effect.</li>
      </ol>
      <h2 id="additional-resources_virt-connecting-vm-to-ovn-secondary-network" class="group scroll-mt-20">Additional resources<a href="#additional-resources_virt-connecting-vm-to-ovn-secondary-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/networking/multiple_networks/secondary_networks/creating-secondary-nwt-ovnk#configuration-ovnk-additional-networks_configuring-additional-network-ovnk">Creating secondary networks on OVN-Kubernetes</a></li>
      <li><a href="/openshift-docs-markdown/networking/networking_operators/k8s-nmstate-about-the-k8s-nmstate-operator#k8s-nmstate-about-the-k8s-nmstate-operator">About the Kubernetes NMState Operator</a></li>
      <li><a href="/openshift-docs-markdown/networking/multiple_networks/secondary_networks/configuring-multi-network-policy#configuring-multi-network-policy">Multi-network policy API</a></li>
      <li><a href="/openshift-docs-markdown/networking/multiple_networks/primary_networks/about-primary-nwt-nad#about-primary-nwt-nad">Creating primary networks by using a network attachment definition</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
