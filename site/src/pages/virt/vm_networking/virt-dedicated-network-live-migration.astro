---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/vm_networking/virt-dedicated-network-live-migration.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-dedicated-network-live-migration","text":"Configuring a dedicated network for live migration"},{"depth":2,"slug":"virt-configuring-secondary-network-vm-live-migration_virt-dedicated-network-live-migration","text":"Configuring a dedicated secondary network for live migration"},{"depth":2,"slug":"virt-selecting-migration-network-ui_virt-dedicated-network-live-migration","text":"Selecting a dedicated network by using the web console"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/vm_networking/virt-dedicated-network-live-migration";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring a dedicated network for live migration";
const description = "You can configure a dedicated secondary network for live migration. A dedicated network minimizes the effects of network saturation on tenant workloads…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/vm_networking"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring a dedicated network for live migration {#virt-dedicated-network-live-migration}\n\nYou can configure a dedicated secondary network for live migration. A dedicated network minimizes the effects of network saturation on tenant workloads during live migration.\n\n## Configuring a dedicated secondary network for live migration {#virt-configuring-secondary-network-vm-live-migration_virt-dedicated-network-live-migration}\n\nAfter you have configured a Linux bridge network, you can configure a dedicated network for live migration. A dedicated network minimizes the effects of network saturation on tenant workloads during live migration.\n\nTo configure a dedicated secondary network for live migration, you must first create a bridge network attachment definition (NAD) by using the CLI. You can then add the name of the `NetworkAttachmentDefinition` object to the `HyperConverged` custom resource (CR).\n\n**Prerequisites**\n\n- You installed the OpenShift CLI (`oc`).\n- You logged in to the cluster as a user with the `cluster-admin` role.\n- Each node has at least two Network Interface Cards (NICs).\n- The NICs for live migration are connected to the same VLAN.\n\n**Procedure**\n\n1. Create a `NetworkAttachmentDefinition` manifest according to the following example:\n\n   ```yaml\n   apiVersion: \"k8s.cni.cncf.io/v1\"\n   kind: NetworkAttachmentDefinition\n   metadata:\n     name: my-secondary-network\n     namespace: openshift-cnv\n   spec:\n     config: '{\n       \"cniVersion\": \"0.3.1\",\n       \"name\": \"migration-bridge\",\n       \"type\": \"macvlan\",\n       \"master\": \"eth1\",\n       \"mode\": \"bridge\",\n       \"ipam\": {\n         \"type\": \"whereabouts\",\n         \"range\": \"10.200.5.0/24\"\n       }\n     }'\n   ```\n\n   - `metadata.name` defines the name of the `NetworkAttachmentDefinition` object.\n   - `config.master` defines the name of the NIC to be used for live migration.\n   - `config.type` defines the name of the CNI plugin that provides the network for the NAD.\n   - `config.range` defines an IP address range for the secondary network. This range must not overlap the IP addresses of the main network.\n2. Open the `HyperConverged` CR in your default editor by running the following command:\n\n   ```terminal\n   $ oc edit hyperconvergeds.v1beta1.hco.kubevirt.io kubevirt-hyperconverged -n openshift-cnv\n   ```\n3. Add the name of the `NetworkAttachmentDefinition` object to the `spec.liveMigrationConfig` stanza of the `HyperConverged` CR.\n\n   Example `HyperConverged` manifest:\n\n   ```yaml\n   apiVersion: hco.kubevirt.io/v1beta1\n   kind: HyperConverged\n   metadata:\n     name: kubevirt-hyperconverged\n     namespace: openshift-cnv\n   spec:\n     liveMigrationConfig:\n       completionTimeoutPerGiB: 800\n       network: <network>\n       parallelMigrationsPerCluster: 5\n       parallelOutboundMigrationsPerNode: 2\n       progressTimeout: 150\n   # ...\n   ```\n\n   - `spec.liveMigrationConfig.network` defines the name of the Multus `NetworkAttachmentDefinition` object to be used for live migrations.\n4. Save your changes and exit the editor. The `virt-handler` pods restart and connect to the secondary network.\n\n**Verification**\n\n- When the node that the virtual machine runs on is placed into maintenance mode, the VM automatically migrates to another node in the cluster. You can verify that the migration occurred over the secondary network and not the default pod network by checking the target IP address in the virtual machine instance (VMI) metadata.\n\n  ```terminal\n  $ oc get vmi <vmi_name> -o jsonpath='{.status.migrationState.targetNodeAddress}'\n  ```\n\n## Selecting a dedicated network by using the web console {#virt-selecting-migration-network-ui_virt-dedicated-network-live-migration}\n\nYou can select a dedicated network for live migration by using the OpenShift Container Platform web console.\n\n**Prerequisites**\n\n- You configured a Multus network for live migration.\n- You created a network attachment definition for the network.\n\n**Procedure**\n\n1. Go to **Virtualization → Settings** in the OpenShift Container Platform web console.\n2. On the **Cluster** tab, click **General settiings**.\n3. Click **Live Migration**.\n4. Select the network from the **Live migration network** list.\n\n**Additional resources**\n{._additional-resources}\n\n- [Configuring live migration limits and timeouts](/openshift-docs-markdown/virt/live_migration/virt-configuring-live-migration#virt-configuring-live-migration)\n- [Connecting a VM to a Linux bridge network](/openshift-docs-markdown/virt/vm_networking/virt-connecting-vm-to-linux-bridge#virt-connecting-vm-to-linux-bridge)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/vm_networking/virt-dedicated-network-live-migration.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-dedicated-network-live-migration" class="group scroll-mt-20">Configuring a dedicated network for live migration<a href="#virt-dedicated-network-live-migration" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">You can configure a dedicated secondary network for live migration. A dedicated network minimizes the effects of network saturation on tenant workloads during live migration.</p>
      <h2 id="virt-configuring-secondary-network-vm-live-migration_virt-dedicated-network-live-migration" class="group scroll-mt-20">Configuring a dedicated secondary network for live migration<a href="#virt-configuring-secondary-network-vm-live-migration_virt-dedicated-network-live-migration" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">After you have configured a Linux bridge network, you can configure a dedicated network for live migration. A dedicated network minimizes the effects of network saturation on tenant workloads during live migration.</p>
      <p>To configure a dedicated secondary network for live migration, you must first create a bridge network attachment definition (NAD) by using the CLI. You can then add the name of the <code>NetworkAttachmentDefinition</code> object to the <code>HyperConverged</code> custom resource (CR).</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You installed the OpenShift CLI (<code>oc</code>).</li>
      <li>You logged in to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>Each node has at least two Network Interface Cards (NICs).</li>
      <li>The NICs for live migration are connected to the same VLAN.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a <code>NetworkAttachmentDefinition</code> manifest according to the following example:
      <CodeBlock code={"apiVersion: \"k8s.cni.cncf.io/v1\"\nkind: NetworkAttachmentDefinition\nmetadata:\n  name: my-secondary-network\n  namespace: openshift-cnv\nspec:\n  config: '{\n    \"cniVersion\": \"0.3.1\",\n    \"name\": \"migration-bridge\",\n    \"type\": \"macvlan\",\n    \"master\": \"eth1\",\n    \"mode\": \"bridge\",\n    \"ipam\": {\n      \"type\": \"whereabouts\",\n      \"range\": \"10.200.5.0/24\"\n    }\n  }'"} lang="yaml" />
      <ul>
      <li><code>metadata.name</code> defines the name of the <code>NetworkAttachmentDefinition</code> object.</li>
      <li><code>config.master</code> defines the name of the NIC to be used for live migration.</li>
      <li><code>config.type</code> defines the name of the CNI plugin that provides the network for the NAD.</li>
      <li><code>config.range</code> defines an IP address range for the secondary network. This range must not overlap the IP addresses of the main network.</li>
      </ul>
      </li>
      <li>
      Open the <code>HyperConverged</code> CR in your default editor by running the following command:
      <CodeBlock code={"$ oc edit hyperconvergeds.v1beta1.hco.kubevirt.io kubevirt-hyperconverged -n openshift-cnv"} lang="terminal" />
      </li>
      <li>
      Add the name of the <code>NetworkAttachmentDefinition</code> object to the <code>spec.liveMigrationConfig</code> stanza of the <code>HyperConverged</code> CR.
      <p>Example <code>HyperConverged</code> manifest:</p>
      <CodeBlock code={"apiVersion: hco.kubevirt.io/v1beta1\nkind: HyperConverged\nmetadata:\n  name: kubevirt-hyperconverged\n  namespace: openshift-cnv\nspec:\n  liveMigrationConfig:\n    completionTimeoutPerGiB: 800\n    network: <network>\n    parallelMigrationsPerCluster: 5\n    parallelOutboundMigrationsPerNode: 2\n    progressTimeout: 150\n# ..."} lang="yaml" />
      <ul><li><code>spec.liveMigrationConfig.network</code> defines the name of the Multus <code>NetworkAttachmentDefinition</code> object to be used for live migrations.</li></ul>
      </li>
      <li>Save your changes and exit the editor. The <code>virt-handler</code> pods restart and connect to the secondary network.</li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      When the node that the virtual machine runs on is placed into maintenance mode, the VM automatically migrates to another node in the cluster. You can verify that the migration occurred over the secondary network and not the default pod network by checking the target IP address in the virtual machine instance (VMI) metadata.
      <CodeBlock code={"$ oc get vmi <vmi_name> -o jsonpath='{.status.migrationState.targetNodeAddress}'"} lang="terminal" />
      </li>
      </ul>
      <h2 id="virt-selecting-migration-network-ui_virt-dedicated-network-live-migration" class="group scroll-mt-20">Selecting a dedicated network by using the web console<a href="#virt-selecting-migration-network-ui_virt-dedicated-network-live-migration" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can select a dedicated network for live migration by using the OpenShift Container Platform web console.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You configured a Multus network for live migration.</li>
      <li>You created a network attachment definition for the network.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Go to <strong>Virtualization → Settings</strong> in the OpenShift Container Platform web console.</li>
      <li>On the <strong>Cluster</strong> tab, click <strong>General settiings</strong>.</li>
      <li>Click <strong>Live Migration</strong>.</li>
      <li>Select the network from the <strong>Live migration network</strong> list.</li>
      </ol>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/virt/live_migration/virt-configuring-live-migration#virt-configuring-live-migration">Configuring live migration limits and timeouts</a></li>
      <li><a href="/openshift-docs-markdown/virt/vm_networking/virt-connecting-vm-to-linux-bridge#virt-connecting-vm-to-linux-bridge">Connecting a VM to a Linux bridge network</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
