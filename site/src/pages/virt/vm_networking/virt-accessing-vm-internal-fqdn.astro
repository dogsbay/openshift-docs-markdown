---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/vm_networking/virt-accessing-vm-internal-fqdn.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-accessing-vm-internal-fqdn","text":"Accessing a virtual machine  by using its internal FQDN"},{"depth":2,"slug":"virt-creating-headless-services_virt-accessing-vm-internal-fqdn","text":"Creating a headless service in a project by using the CLI"},{"depth":2,"slug":"virt-discovering-vm-internal-fqdn_virt-accessing-vm-internal-fqdn","text":"Mapping a virtual machine to a headless service by using the CLI"},{"depth":2,"slug":"virt-connecting-vm-internal-fqdn_virt-accessing-vm-internal-fqdn","text":"Connecting to a virtual machine by using its internal FQDN"},{"depth":2,"slug":"additional-resources_virt-accessing-vm-internal-fqdn","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/vm_networking/virt-accessing-vm-internal-fqdn";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Accessing a virtual machine  by using its internal FQDN";
const description = "You can access a virtual machine on a stable, fully qualified domain name (FQDN) by using headless services. A headless service creates DNS records for…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/vm_networking"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Accessing a virtual machine  by using its internal FQDN {#virt-accessing-vm-internal-fqdn}\n\nYou can access a virtual machine on a stable, fully qualified domain name (FQDN) by using headless services. A headless service creates DNS records for each pod instead of a virtual IP, enabling FQDN access without exposing specific ports.\n\n> [!IMPORTANT]\n> If you created a VM by using the OpenShift Container Platform web console, you can find its internal FQDN listed in the **Network** tile on the **Overview** tab of the **VirtualMachine details** page.\n\n## Creating a headless service in a project by using the CLI {#virt-creating-headless-services_virt-accessing-vm-internal-fqdn}\n\nTo create a headless service in a namespace, add the `clusterIP: None` parameter to the service YAML definition.\n\n**Prerequisites**\n\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Create a `Service` manifest to expose the VM, such as the following example:\n\n   ```yaml\n   apiVersion: v1\n   kind: Service\n   metadata:\n     name: mysubdomain\n   spec:\n     selector:\n       expose: me\n     clusterIP: None\n     ports:\n     - protocol: TCP\n       port: 1234\n       targetPort: 1234\n   ```\n\n   - `metadata.name` defines the name of the service. This must match the `spec.subdomain` attribute in the `VirtualMachine` manifest file.\n   - `spec.selector` defines the service selector that must match the `expose:me` label in the `VirtualMachine` manifest file.\n   - `spec.clusterIP` defines a headless service.\n   - `spec.ports` defines the list of ports that are exposed by the service. You must define at least one port. This can be any arbitrary value as it does not affect the headless service.\n2. Save the `Service` manifest file.\n3. Create the service by running the following command:\n\n   ```terminal\n   $ oc create -f headless_service.yaml\n   ```\n\n## Mapping a virtual machine to a headless service by using the CLI {#virt-discovering-vm-internal-fqdn_virt-accessing-vm-internal-fqdn}\n\nTo connect to a virtual machine (VM) from within the cluster by using its internal fully qualified domain name (FQDN), you must first map the VM to a headless service. Set the `spec.hostname` and `spec.subdomain` parameters in the VM configuration file.\n\nIf a headless service exists with a name that matches the subdomain, a unique DNS A record is created for the VM in the form of `<vm.spec.hostname>.<vm.spec.subdomain>.<vm.metadata.namespace>.svc.cluster.local`.\n\n**Prerequisites**\n\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Edit the `VirtualMachine` manifest to add the service selector label and subdomain by running the following command:\n\n   ```terminal\n   $ oc edit vm <vm_name>\n   ```\n\n   Example `VirtualMachine` manifest file:\n\n   ```yaml\n   apiVersion: kubevirt.io/v1\n   kind: VirtualMachine\n   metadata:\n     name: vm-fedora\n   spec:\n     template:\n       metadata:\n         labels:\n           expose: me\n       spec:\n         hostname: \"myvm\"\n         subdomain: \"mysubdomain\"\n   # ...\n   ```\n\n   - `spec.template.metadata.labels.expose` defines a label that must match the `spec.selector` attribute of the `Service` manifest that you previously created.\n   - `spec.template.spec.hostname` defines the hostname. If this attribute is not specified, the resulting DNS A record takes the form of `<vm.metadata.name>.<vm.spec.subdomain>.<vm.metadata.namespace>.svc.cluster.local`.\n   - `spec.template.spec.subdomain` defines the subdomain. The `spec.subdomain` attribute must match the `metadata.name` value of the `Service` object.\n2. Save your changes and exit the editor.\n3. Restart the VM to apply the changes.\n\n## Connecting to a virtual machine by using its internal FQDN {#virt-connecting-vm-internal-fqdn_virt-accessing-vm-internal-fqdn}\n\nYou can connect to a virtual machine (VM) by using its internal fully qualified domain name (FQDN).\n\n**Prerequisites**\n\n- You have installed the `virtctl` tool.\n- You have identified the internal FQDN of the VM from the web console or by mapping the VM to a headless service. The internal FQDN has the format `<vm.spec.hostname>.<vm.spec.subdomain>.<vm.metadata.namespace>.svc.cluster.local`.\n\n**Procedure**\n\n1. Connect to the VM console by entering the following command:\n\n   ```terminal\n   $ virtctl console vm-fedora\n   ```\n2. To connect to the VM by using the requested FQDN, run the following command:\n\n   ```terminal\n   $ ping myvm.mysubdomain.<namespace>.svc.cluster.local\n   ```\n\n   Example output:\n\n   ```terminal\n   PING myvm.mysubdomain.default.svc.cluster.local (10.244.0.57) 56(84) bytes of data.\n   64 bytes from myvm.mysubdomain.default.svc.cluster.local (10.244.0.57): icmp_seq=1 ttl=64 time=0.029 ms\n   ```\n\n   In the preceding example, the DNS entry for `myvm.mysubdomain.default.svc.cluster.local` points to `10.244.0.57`, which is the cluster IP address that is currently assigned to the VM.\n\n## Additional resources {#additional-resources_virt-accessing-vm-internal-fqdn}\n\n- [Exposing a VM by using a service](/openshift-docs-markdown/virt/vm_networking/virt-exposing-vm-with-service#virt-exposing-vm-with-service)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/vm_networking/virt-accessing-vm-internal-fqdn.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-accessing-vm-internal-fqdn" class="group scroll-mt-20">Accessing a virtual machine  by using its internal FQDN<a href="#virt-accessing-vm-internal-fqdn" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can access a virtual machine on a stable, fully qualified domain name (FQDN) by using headless services. A headless service creates DNS records for each pod instead of a virtual IP, enabling FQDN access without exposing specific ports.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>If you created a VM by using the OpenShift Container Platform web console, you can find its internal FQDN listed in the <strong>Network</strong> tile on the <strong>Overview</strong> tab of the <strong>VirtualMachine details</strong> page.</p>
        </AlertDescription>
      </Alert>
      <h2 id="virt-creating-headless-services_virt-accessing-vm-internal-fqdn" class="group scroll-mt-20">Creating a headless service in a project by using the CLI<a href="#virt-creating-headless-services_virt-accessing-vm-internal-fqdn" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To create a headless service in a namespace, add the <code>clusterIP: None</code> parameter to the service YAML definition.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have installed the OpenShift CLI (<code>oc</code>).</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a <code>Service</code> manifest to expose the VM, such as the following example:
      <CodeBlock code={"apiVersion: v1\nkind: Service\nmetadata:\n  name: mysubdomain\nspec:\n  selector:\n    expose: me\n  clusterIP: None\n  ports:\n  - protocol: TCP\n    port: 1234\n    targetPort: 1234"} lang="yaml" />
      <ul>
      <li><code>metadata.name</code> defines the name of the service. This must match the <code>spec.subdomain</code> attribute in the <code>VirtualMachine</code> manifest file.</li>
      <li><code>spec.selector</code> defines the service selector that must match the <code>expose:me</code> label in the <code>VirtualMachine</code> manifest file.</li>
      <li><code>spec.clusterIP</code> defines a headless service.</li>
      <li><code>spec.ports</code> defines the list of ports that are exposed by the service. You must define at least one port. This can be any arbitrary value as it does not affect the headless service.</li>
      </ul>
      </li>
      <li>Save the <code>Service</code> manifest file.</li>
      <li>
      Create the service by running the following command:
      <CodeBlock code={"$ oc create -f headless_service.yaml"} lang="terminal" />
      </li>
      </ol>
      <h2 id="virt-discovering-vm-internal-fqdn_virt-accessing-vm-internal-fqdn" class="group scroll-mt-20">Mapping a virtual machine to a headless service by using the CLI<a href="#virt-discovering-vm-internal-fqdn_virt-accessing-vm-internal-fqdn" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To connect to a virtual machine (VM) from within the cluster by using its internal fully qualified domain name (FQDN), you must first map the VM to a headless service. Set the <code>spec.hostname</code> and <code>spec.subdomain</code> parameters in the VM configuration file.</p>
      <p>If a headless service exists with a name that matches the subdomain, a unique DNS A record is created for the VM in the form of <code>&lt;vm.spec.hostname&gt;.&lt;vm.spec.subdomain&gt;.&lt;vm.metadata.namespace&gt;.svc.cluster.local</code>.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have installed the OpenShift CLI (<code>oc</code>).</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Edit the <code>VirtualMachine</code> manifest to add the service selector label and subdomain by running the following command:
      <CodeBlock code={"$ oc edit vm <vm_name>"} lang="terminal" />
      <p>Example <code>VirtualMachine</code> manifest file:</p>
      <CodeBlock code={"apiVersion: kubevirt.io/v1\nkind: VirtualMachine\nmetadata:\n  name: vm-fedora\nspec:\n  template:\n    metadata:\n      labels:\n        expose: me\n    spec:\n      hostname: \"myvm\"\n      subdomain: \"mysubdomain\"\n# ..."} lang="yaml" />
      <ul>
      <li><code>spec.template.metadata.labels.expose</code> defines a label that must match the <code>spec.selector</code> attribute of the <code>Service</code> manifest that you previously created.</li>
      <li><code>spec.template.spec.hostname</code> defines the hostname. If this attribute is not specified, the resulting DNS A record takes the form of <code>&lt;vm.metadata.name&gt;.&lt;vm.spec.subdomain&gt;.&lt;vm.metadata.namespace&gt;.svc.cluster.local</code>.</li>
      <li><code>spec.template.spec.subdomain</code> defines the subdomain. The <code>spec.subdomain</code> attribute must match the <code>metadata.name</code> value of the <code>Service</code> object.</li>
      </ul>
      </li>
      <li>Save your changes and exit the editor.</li>
      <li>Restart the VM to apply the changes.</li>
      </ol>
      <h2 id="virt-connecting-vm-internal-fqdn_virt-accessing-vm-internal-fqdn" class="group scroll-mt-20">Connecting to a virtual machine by using its internal FQDN<a href="#virt-connecting-vm-internal-fqdn_virt-accessing-vm-internal-fqdn" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can connect to a virtual machine (VM) by using its internal fully qualified domain name (FQDN).</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have installed the <code>virtctl</code> tool.</li>
      <li>You have identified the internal FQDN of the VM from the web console or by mapping the VM to a headless service. The internal FQDN has the format <code>&lt;vm.spec.hostname&gt;.&lt;vm.spec.subdomain&gt;.&lt;vm.metadata.namespace&gt;.svc.cluster.local</code>.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Connect to the VM console by entering the following command:
      <CodeBlock code={"$ virtctl console vm-fedora"} lang="terminal" />
      </li>
      <li>
      To connect to the VM by using the requested FQDN, run the following command:
      <CodeBlock code={"$ ping myvm.mysubdomain.<namespace>.svc.cluster.local"} lang="terminal" />
      <p>Example output:</p>
      <CodeBlock code={"PING myvm.mysubdomain.default.svc.cluster.local (10.244.0.57) 56(84) bytes of data.\n64 bytes from myvm.mysubdomain.default.svc.cluster.local (10.244.0.57): icmp_seq=1 ttl=64 time=0.029 ms"} lang="terminal" />
      <p>In the preceding example, the DNS entry for <code>myvm.mysubdomain.default.svc.cluster.local</code> points to <code>10.244.0.57</code>, which is the cluster IP address that is currently assigned to the VM.</p>
      </li>
      </ol>
      <h2 id="additional-resources_virt-accessing-vm-internal-fqdn" class="group scroll-mt-20">Additional resources<a href="#additional-resources_virt-accessing-vm-internal-fqdn" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul><li><a href="/openshift-docs-markdown/virt/vm_networking/virt-exposing-vm-with-service#virt-exposing-vm-with-service">Exposing a VM by using a service</a></li></ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
