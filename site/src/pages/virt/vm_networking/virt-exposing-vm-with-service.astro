---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/vm_networking/virt-exposing-vm-with-service.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-exposing-vm-with-service","text":"Exposing a virtual machine by using a service"},{"depth":2,"slug":"virt-about-services_virt-exposing-vm-with-service","text":"About services"},{"depth":2,"slug":"virt-dual-stack-support-services_virt-exposing-vm-with-service","text":"Dual-stack support"},{"depth":2,"slug":"virt-creating-service-cli_virt-exposing-vm-with-service","text":"Creating a service by using the CLI"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/vm_networking/virt-exposing-vm-with-service";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Exposing a virtual machine by using a service";
const description = "You can expose a virtual machine (VM) within or outside the cluster by exposing a VM as a Kubernetes service. You can leverage native load balancing and…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/vm_networking"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Exposing a virtual machine by using a service {#virt-exposing-vm-with-service}\n\nYou can expose a virtual machine (VM) within or outside the cluster by exposing a VM as a Kubernetes service. You can leverage native load balancing and observability tools that provide unified traffic management, consistent SSL termination, and centralized security policies across hybrid workloads.\n\n## About services {#virt-about-services_virt-exposing-vm-with-service}\n\nA Kubernetes service exposes network access for clients to an application running on a set of pods. Services offer abstraction, load balancing, and, in the case of the `NodePort` and `LoadBalancer` types, exposure to the outside world.\n\n`ClusterIP`\n:   Exposes the service on an internal IP address and as a DNS name to other applications within the cluster. A single service can map to multiple virtual machines. When a client tries to connect to the service, the client’s request is load balanced among available backends. `ClusterIP` is the default service type.\n\n`NodePort`\n:   Exposes the service on the same port of each selected node in the cluster. `NodePort` makes a port accessible from outside the cluster, provided that the node itself is externally accessible to the client.\n\n`LoadBalancer`\n:   Creates an external load balancer in the current cloud (if supported) and assigns a fixed, external IP address to the service.\n\n> [!NOTE]\n> For on-premise clusters, you can configure a load balancing service by deploying the MetalLB Operator.\n\n## Dual-stack support {#virt-dual-stack-support-services_virt-exposing-vm-with-service}\n\nIf IPv4 and IPv6 dual-stack networking is enabled for your cluster, you can create a service that uses IPv4, IPv6, or both, by defining the `spec.ipFamilyPolicy` and the `spec.ipFamilies` fields in the `Service` object.\n\nThe `spec.ipFamilyPolicy` field can be set to one of the following values:\n\nSingleStack\n:   The control plane assigns a cluster IP address for the service based on the first configured service cluster IP range.\n\nPreferDualStack\n:   The control plane assigns both IPv4 and IPv6 cluster IP addresses for the service on clusters that have dual-stack configured.\n\nRequireDualStack\n:   This option fails for clusters that do not have dual-stack networking enabled. For clusters that have dual-stack configured, the behavior is the same as when the value is set to `PreferDualStack`. The control plane allocates cluster IP addresses from both IPv4 and IPv6 address ranges.\n\nYou can define which IP family to use for single-stack or define the order of IP families for dual-stack by setting the `spec.ipFamilies` field to one of the following array values:\n\n- `[IPv4]`\n- `[IPv6]`\n- `[IPv4, IPv6]`\n- `[IPv6, IPv4]`\n\n> [!NOTE]\n> IPv6 single stack and the `[IPv6, IPv4]` array are not supported in IBM Z(R) and IBM(R) LinuxONE.\n\n## Creating a service by using the CLI {#virt-creating-service-cli_virt-exposing-vm-with-service}\n\nYou can create a service and associate it with a virtual machine (VM) by using the command line.\n\n**Prerequisites**\n\n- You configured the cluster network to support the service.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Edit the `VirtualMachine` manifest to add the label for service creation. Add `special: key` to the `spec.template.metadata.labels` stanza:\n\n   ```yaml\n   apiVersion: kubevirt.io/v1\n   kind: VirtualMachine\n   metadata:\n     name: example-vm\n     namespace: example-namespace\n   spec:\n     runStrategy: Halted\n     template:\n       metadata:\n         labels:\n           special: key\n   # ...\n   ```\n\n   > [!NOTE]\n   > Labels on a virtual machine pass through to the pod. The `special: key` label must match the label in the `spec.selector` attribute of the `Service` manifest.\n2. Save the `VirtualMachine` manifest file to apply your changes.\n3. Create a `Service` manifest to expose the VM:\n\n   ```yaml\n   apiVersion: v1\n   kind: Service\n   metadata:\n     name: example-service\n     namespace: example-namespace\n   spec:\n   # ...\n     selector:\n       special: key\n     type: NodePort\n     ports:\n       protocol: TCP\n       port: 80\n       targetPort: 9376\n       nodePort: 30000\n   ```\n\n   - `spec.selector` defines the label that you added to the `spec.template.metadata.labels` stanza of the `VirtualMachine` manifest.\n   - `spec.type` defines the type of service by the way it is exposed. Choose one of `ClusterIP`, `NodePort`, or `LoadBalancer`.\n   - `spec.ports` defines a collection of network ports and protocols to expose from the virtual machine.\n4. Save the `Service` manifest file.\n5. Create the service by running the following command:\n\n   ```terminal\n   $ oc create -f example-service.yaml\n   ```\n6. Restart the VM to apply the changes.\n\n**Verification**\n\n- Query the `Service` object to verify that it is available:\n\n  ```terminal\n  $ oc get service -n example-namespace\n  ```\n\n**Additional resources**\n{._additional-resources}\n\n- [Configuring ingress cluster traffic by using a `NodePort`](/openshift-docs-markdown/networking/ingress_load_balancing/configuring_ingress_cluster_traffic/configuring-ingress-cluster-traffic-nodeport#configuring-ingress-cluster-traffic-nodeport)\n- [Configuring ingress cluster traffic by using a load balancer](/openshift-docs-markdown/networking/ingress_load_balancing/configuring_ingress_cluster_traffic/configuring-ingress-cluster-traffic-load-balancer#configuring-ingress-cluster-traffic-load-balancer)\n- [Installing the MetalLB Operator](/openshift-docs-markdown/networking/networking_operators/metallb-operator/metallb-operator-install#metallb-operator-install)\n- [Configuring services to use MetalLB](/openshift-docs-markdown/networking/ingress_load_balancing/metallb/metallb-configure-services#metallb-configure-services)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/vm_networking/virt-exposing-vm-with-service.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-exposing-vm-with-service" class="group scroll-mt-20">Exposing a virtual machine by using a service<a href="#virt-exposing-vm-with-service" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">You can expose a virtual machine (VM) within or outside the cluster by exposing a VM as a Kubernetes service. You can leverage native load balancing and observability tools that provide unified traffic management, consistent SSL termination, and centralized security policies across hybrid workloads.</p>
      <h2 id="virt-about-services_virt-exposing-vm-with-service" class="group scroll-mt-20">About services<a href="#virt-about-services_virt-exposing-vm-with-service" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">A Kubernetes service exposes network access for clients to an application running on a set of pods. Services offer abstraction, load balancing, and, in the case of the <code>NodePort</code> and <code>LoadBalancer</code> types, exposure to the outside world.</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>ClusterIP</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Exposes the service on an internal IP address and as a DNS name to other applications within the cluster. A single service can map to multiple virtual machines. When a client tries to connect to the service, the client’s request is load balanced among available backends. <code>ClusterIP</code> is the default service type.</p></dd>
      <dt class="font-semibold"><code>NodePort</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Exposes the service on the same port of each selected node in the cluster. <code>NodePort</code> makes a port accessible from outside the cluster, provided that the node itself is externally accessible to the client.</p></dd>
      <dt class="font-semibold"><code>LoadBalancer</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Creates an external load balancer in the current cloud (if supported) and assigns a fixed, external IP address to the service.</p></dd>
      </dl>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>For on-premise clusters, you can configure a load balancing service by deploying the MetalLB Operator.</p>
        </AlertDescription>
      </Alert>
      <h2 id="virt-dual-stack-support-services_virt-exposing-vm-with-service" class="group scroll-mt-20">Dual-stack support<a href="#virt-dual-stack-support-services_virt-exposing-vm-with-service" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">If IPv4 and IPv6 dual-stack networking is enabled for your cluster, you can create a service that uses IPv4, IPv6, or both, by defining the <code>spec.ipFamilyPolicy</code> and the <code>spec.ipFamilies</code> fields in the <code>Service</code> object.</p>
      <p>The <code>spec.ipFamilyPolicy</code> field can be set to one of the following values:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">SingleStack</dt>
      <dd class="ml-6 text-muted-foreground"><p>The control plane assigns a cluster IP address for the service based on the first configured service cluster IP range.</p></dd>
      <dt class="font-semibold">PreferDualStack</dt>
      <dd class="ml-6 text-muted-foreground"><p>The control plane assigns both IPv4 and IPv6 cluster IP addresses for the service on clusters that have dual-stack configured.</p></dd>
      <dt class="font-semibold">RequireDualStack</dt>
      <dd class="ml-6 text-muted-foreground"><p>This option fails for clusters that do not have dual-stack networking enabled. For clusters that have dual-stack configured, the behavior is the same as when the value is set to <code>PreferDualStack</code>. The control plane allocates cluster IP addresses from both IPv4 and IPv6 address ranges.</p></dd>
      </dl>
      <p>You can define which IP family to use for single-stack or define the order of IP families for dual-stack by setting the <code>spec.ipFamilies</code> field to one of the following array values:</p>
      <ul>
      <li><code>[IPv4]</code></li>
      <li><code>[IPv6]</code></li>
      <li><code>[IPv4, IPv6]</code></li>
      <li><code>[IPv6, IPv4]</code></li>
      </ul>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>IPv6 single stack and the <code>[IPv6, IPv4]</code> array are not supported in IBM Z(R) and IBM(R) LinuxONE.</p>
        </AlertDescription>
      </Alert>
      <h2 id="virt-creating-service-cli_virt-exposing-vm-with-service" class="group scroll-mt-20">Creating a service by using the CLI<a href="#virt-creating-service-cli_virt-exposing-vm-with-service" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can create a service and associate it with a virtual machine (VM) by using the command line.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You configured the cluster network to support the service.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Edit the <code>VirtualMachine</code> manifest to add the label for service creation. Add <code>special: key</code> to the <code>spec.template.metadata.labels</code> stanza:
      <CodeBlock code={"apiVersion: kubevirt.io/v1\nkind: VirtualMachine\nmetadata:\n  name: example-vm\n  namespace: example-namespace\nspec:\n  runStrategy: Halted\n  template:\n    metadata:\n      labels:\n        special: key\n# ..."} lang="yaml" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Labels on a virtual machine pass through to the pod. The <code>special: key</code> label must match the label in the <code>spec.selector</code> attribute of the <code>Service</code> manifest.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>Save the <code>VirtualMachine</code> manifest file to apply your changes.</li>
      <li>
      Create a <code>Service</code> manifest to expose the VM:
      <CodeBlock code={"apiVersion: v1\nkind: Service\nmetadata:\n  name: example-service\n  namespace: example-namespace\nspec:\n# ...\n  selector:\n    special: key\n  type: NodePort\n  ports:\n    protocol: TCP\n    port: 80\n    targetPort: 9376\n    nodePort: 30000"} lang="yaml" />
      <ul>
      <li><code>spec.selector</code> defines the label that you added to the <code>spec.template.metadata.labels</code> stanza of the <code>VirtualMachine</code> manifest.</li>
      <li><code>spec.type</code> defines the type of service by the way it is exposed. Choose one of <code>ClusterIP</code>, <code>NodePort</code>, or <code>LoadBalancer</code>.</li>
      <li><code>spec.ports</code> defines a collection of network ports and protocols to expose from the virtual machine.</li>
      </ul>
      </li>
      <li>Save the <code>Service</code> manifest file.</li>
      <li>
      Create the service by running the following command:
      <CodeBlock code={"$ oc create -f example-service.yaml"} lang="terminal" />
      </li>
      <li>Restart the VM to apply the changes.</li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      Query the <code>Service</code> object to verify that it is available:
      <CodeBlock code={"$ oc get service -n example-namespace"} lang="terminal" />
      </li>
      </ul>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/networking/ingress_load_balancing/configuring_ingress_cluster_traffic/configuring-ingress-cluster-traffic-nodeport#configuring-ingress-cluster-traffic-nodeport">Configuring ingress cluster traffic by using a <code>NodePort</code></a></li>
      <li><a href="/openshift-docs-markdown/networking/ingress_load_balancing/configuring_ingress_cluster_traffic/configuring-ingress-cluster-traffic-load-balancer#configuring-ingress-cluster-traffic-load-balancer">Configuring ingress cluster traffic by using a load balancer</a></li>
      <li><a href="/openshift-docs-markdown/networking/networking_operators/metallb-operator/metallb-operator-install#metallb-operator-install">Installing the MetalLB Operator</a></li>
      <li><a href="/openshift-docs-markdown/networking/ingress_load_balancing/metallb/metallb-configure-services#metallb-configure-services">Configuring services to use MetalLB</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
