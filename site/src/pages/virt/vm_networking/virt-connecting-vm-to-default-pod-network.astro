---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/vm_networking/virt-connecting-vm-to-default-pod-network.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-connecting-vm-to-default-pod-network","text":"Connecting a virtual machine to the default pod network"},{"depth":2,"slug":"virt-configuring-masquerade-mode-cli_virt-connecting-vm-to-default-pod-network","text":"Configuring masquerade mode from the CLI"},{"depth":2,"slug":"virt-configuring-masquerade-mode-dual-stack_virt-connecting-vm-to-default-pod-network","text":"Configuring masquerade mode with dual-stack (IPv4 and IPv6)"},{"depth":2,"slug":"virt-jumbo-frames-vm-pod-nw_virt-connecting-vm-to-default-pod-network","text":"About jumbo frames support"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/vm_networking/virt-connecting-vm-to-default-pod-network";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Connecting a virtual machine to the default pod network";
const description = "You can connect a virtual machine to the default internal pod network by configuring its network interface to use the masquerade binding mode." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/vm_networking"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Connecting a virtual machine to the default pod network {#virt-connecting-vm-to-default-pod-network}\n\nYou can connect a virtual machine to the default internal pod network by configuring its network interface to use the `masquerade` binding mode.\n\n> [!NOTE]\n> Traffic passing through network interfaces to the default pod network is interrupted during live migration.\n\n## Configuring masquerade mode from the CLI {#virt-configuring-masquerade-mode-cli_virt-connecting-vm-to-default-pod-network}\n\nYou can use masquerade mode to hide a virtual machine’s outgoing traffic behind the pod IP address. Masquerade mode uses Network Address Translation (NAT) to connect virtual machines to the pod network backend through a Linux bridge.\n\nEnable masquerade mode and allow traffic to enter the virtual machine by editing your virtual machine configuration file.\n\n**Prerequisites**\n\n- You have installed the OpenShift CLI (`oc`).\n- The virtual machine must be configured to use DHCP to acquire IPv4 addresses.\n\n**Procedure**\n\n1. Edit the `interfaces` spec of your virtual machine configuration file:\n\n   ```yaml\n   apiVersion: kubevirt.io/v1\n   kind: VirtualMachine\n   metadata:\n     name: example-vm\n   spec:\n     template:\n       spec:\n         domain:\n           devices:\n             interfaces:\n               - name: default\n                 masquerade: {}\n                 ports:\n                   - port: 80\n   # ...\n         networks:\n         - name: default\n           pod: {}\n   ```\n\n   - `spec.template.spec.domain.devices.interfaces.masquerade` connects the virtual machine using masquerade mode.\n   - `spec.template.spec.domain.devices.interfaces.ports` is optional and defines the list of ports that you want to expose from the virtual machine, each specified by the `port` field. The `port` value must be a number between 0 and 65536. When the `ports` array is not used, all ports in the valid range are open to incoming traffic. In this example, incoming traffic is allowed on port `80`.\n\n     > [!NOTE]\n     > Ports 49152 and 49153 are reserved for use by the libvirt platform and all other incoming traffic to these ports is dropped.\n2. Create the virtual machine:\n\n   ```terminal\n   $ oc create -f <vm-name>.yaml\n   ```\n\n## Configuring masquerade mode with dual-stack (IPv4 and IPv6) {#virt-configuring-masquerade-mode-dual-stack_virt-connecting-vm-to-default-pod-network}\n\nYou can configure a new virtual machine (VM) to use both IPv6 and IPv4 on the default pod network by using cloud-init.\n\nThe `Network.pod.vmIPv6NetworkCIDR` field in the virtual machine instance configuration determines the static IPv6 address of the VM and the gateway IP address. These are used by the virt-launcher pod to route IPv6 traffic to the virtual machine and are not used externally. The `Network.pod.vmIPv6NetworkCIDR` field specifies an IPv6 address block in Classless Inter-Domain Routing (CIDR) notation. The default value is `fd10:0:2::2/120`. You can edit this value based on your network requirements.\n\nWhen the virtual machine is running, incoming and outgoing traffic for the virtual machine is routed to both the IPv4 address and the unique IPv6 address of the virt-launcher pod. The virt-launcher pod then routes the IPv4 traffic to the DHCP address of the virtual machine, and the IPv6 traffic to the statically set IPv6 address of the virtual machine.\n\n**Prerequisites**\n\n- The OpenShift Container Platform cluster must use the OVN-Kubernetes Container Network Interface (CNI) network plugin configured for dual-stack.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. In a new virtual machine configuration, include an interface with `masquerade` and configure the IPv6 address and default gateway by using cloud-init.\n\n   ```yaml\n   apiVersion: kubevirt.io/v1\n   kind: VirtualMachine\n   metadata:\n     name: example-vm-ipv6\n   spec:\n     template:\n       spec:\n         domain:\n           devices:\n             interfaces:\n               - name: default\n                 masquerade: {}\n                 ports:\n                   - port: 80\n   # ...\n         networks:\n         - name: default\n           pod: {}\n         volumes:\n         - cloudInitNoCloud:\n             networkData: |\n               version: 2\n               ethernets:\n                 eth0:\n                   dhcp4: true\n                   addresses: [ fd10:0:2::2/120 ]\n                   gateway6: fd10:0:2::1\n   ```\n\n   - `spec.template.spec.domain.devices.interfaces.masquerade` connects the virtual machine using masquerade mode.\n   - `spec.template.spec.domain.devices.interfaces.ports.port` allows incoming traffic on port 80 to the virtual machine.\n   - `spec.template.spec.volumes.cloudInitNoCloud.networkData.ethernets.eth0.addresses` defines the static IPv6 address as determined by the `Network.pod.vmIPv6NetworkCIDR` field in the virtual machine instance configuration. The default value is `fd10:0:2::2/120`.\n   - `spec.template.spec.volumes.cloudInitNoCloud.networkData.ethernets.eth0.gateway6` defines the gateway IP address as determined by the `Network.pod.vmIPv6NetworkCIDR` field in the virtual machine instance configuration. The default value is `fd10:0:2::1`.\n2. Create the virtual machine in the namespace:\n\n   ```terminal\n   $ oc create -f example-vm-ipv6.yaml\n   ```\n\n**Verification**\n\n- To verify that IPv6 has been configured, start the virtual machine and view the interface status of the virtual machine instance to ensure it has an IPv6 address:\n\n```terminal\n$ oc get vmi <vmi-name> -o jsonpath=\"{.status.interfaces[*].ipAddresses}\"\n```\n\n## About jumbo frames support {#virt-jumbo-frames-vm-pod-nw_virt-connecting-vm-to-default-pod-network}\n\nWhen using the OVN-Kubernetes CNI plugin, you can send unfragmented jumbo frame packets between two virtual machines (VMs) that are connected on the default pod network. Jumbo frames have a maximum transmission unit (MTU) value greater than 1500 bytes.\n\nThe VM automatically gets the MTU value of the cluster network, set by the cluster administrator, in one of the following ways:\n\n- `libvirt`: If the guest OS has the latest version of the VirtIO driver that can interpret incoming data via a Peripheral Component Interconnect (PCI) config register in the emulated device.\n- DHCP: If the guest DHCP client can read the MTU value from the DHCP server response.\n\n> [!NOTE]\n> For Windows VMs that do not have a VirtIO driver, you must set the MTU manually by using `netsh` or a similar tool. This is because the Windows DHCP client does not read the MTU value.\n\n**Additional resources**\n{._additional-resources}\n\n- [Changing the MTU for the cluster network](/openshift-docs-markdown/networking/advanced_networking/changing-cluster-network-mtu#changing-cluster-network-mtu)\n- [Optimizing the MTU for your network](/openshift-docs-markdown/scalability_and_performance/optimization/optimizing-networking#optimizing-mtu_optimizing-networking)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/vm_networking/virt-connecting-vm-to-default-pod-network.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-connecting-vm-to-default-pod-network" class="group scroll-mt-20">Connecting a virtual machine to the default pod network<a href="#virt-connecting-vm-to-default-pod-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">You can connect a virtual machine to the default internal pod network by configuring its network interface to use the <code>masquerade</code> binding mode.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Traffic passing through network interfaces to the default pod network is interrupted during live migration.</p>
        </AlertDescription>
      </Alert>
      <h2 id="virt-configuring-masquerade-mode-cli_virt-connecting-vm-to-default-pod-network" class="group scroll-mt-20">Configuring masquerade mode from the CLI<a href="#virt-configuring-masquerade-mode-cli_virt-connecting-vm-to-default-pod-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can use masquerade mode to hide a virtual machine’s outgoing traffic behind the pod IP address. Masquerade mode uses Network Address Translation (NAT) to connect virtual machines to the pod network backend through a Linux bridge.</p>
      <p>Enable masquerade mode and allow traffic to enter the virtual machine by editing your virtual machine configuration file.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      <li>The virtual machine must be configured to use DHCP to acquire IPv4 addresses.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Edit the <code>interfaces</code> spec of your virtual machine configuration file:
      <CodeBlock code={"apiVersion: kubevirt.io/v1\nkind: VirtualMachine\nmetadata:\n  name: example-vm\nspec:\n  template:\n    spec:\n      domain:\n        devices:\n          interfaces:\n            - name: default\n              masquerade: {}\n              ports:\n                - port: 80\n# ...\n      networks:\n      - name: default\n        pod: {}"} lang="yaml" />
      <ul>
      <li><code>spec.template.spec.domain.devices.interfaces.masquerade</code> connects the virtual machine using masquerade mode.</li>
      <li>
      <code>spec.template.spec.domain.devices.interfaces.ports</code> is optional and defines the list of ports that you want to expose from the virtual machine, each specified by the <code>port</code> field. The <code>port</code> value must be a number between 0 and 65536. When the <code>ports</code> array is not used, all ports in the valid range are open to incoming traffic. In this example, incoming traffic is allowed on port <code>80</code>.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Ports 49152 and 49153 are reserved for use by the libvirt platform and all other incoming traffic to these ports is dropped.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      </li>
      <li>
      Create the virtual machine:
      <CodeBlock code={"$ oc create -f <vm-name>.yaml"} lang="terminal" />
      </li>
      </ol>
      <h2 id="virt-configuring-masquerade-mode-dual-stack_virt-connecting-vm-to-default-pod-network" class="group scroll-mt-20">Configuring masquerade mode with dual-stack (IPv4 and IPv6)<a href="#virt-configuring-masquerade-mode-dual-stack_virt-connecting-vm-to-default-pod-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can configure a new virtual machine (VM) to use both IPv6 and IPv4 on the default pod network by using cloud-init.</p>
      <p>The <code>Network.pod.vmIPv6NetworkCIDR</code> field in the virtual machine instance configuration determines the static IPv6 address of the VM and the gateway IP address. These are used by the virt-launcher pod to route IPv6 traffic to the virtual machine and are not used externally. The <code>Network.pod.vmIPv6NetworkCIDR</code> field specifies an IPv6 address block in Classless Inter-Domain Routing (CIDR) notation. The default value is <code>fd10:0:2::2/120</code>. You can edit this value based on your network requirements.</p>
      <p>When the virtual machine is running, incoming and outgoing traffic for the virtual machine is routed to both the IPv4 address and the unique IPv6 address of the virt-launcher pod. The virt-launcher pod then routes the IPv4 traffic to the DHCP address of the virtual machine, and the IPv6 traffic to the statically set IPv6 address of the virtual machine.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>The OpenShift Container Platform cluster must use the OVN-Kubernetes Container Network Interface (CNI) network plugin configured for dual-stack.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      In a new virtual machine configuration, include an interface with <code>masquerade</code> and configure the IPv6 address and default gateway by using cloud-init.
      <CodeBlock code={"apiVersion: kubevirt.io/v1\nkind: VirtualMachine\nmetadata:\n  name: example-vm-ipv6\nspec:\n  template:\n    spec:\n      domain:\n        devices:\n          interfaces:\n            - name: default\n              masquerade: {}\n              ports:\n                - port: 80\n# ...\n      networks:\n      - name: default\n        pod: {}\n      volumes:\n      - cloudInitNoCloud:\n          networkData: |\n            version: 2\n            ethernets:\n              eth0:\n                dhcp4: true\n                addresses: [ fd10:0:2::2/120 ]\n                gateway6: fd10:0:2::1"} lang="yaml" />
      <ul>
      <li><code>spec.template.spec.domain.devices.interfaces.masquerade</code> connects the virtual machine using masquerade mode.</li>
      <li><code>spec.template.spec.domain.devices.interfaces.ports.port</code> allows incoming traffic on port 80 to the virtual machine.</li>
      <li><code>spec.template.spec.volumes.cloudInitNoCloud.networkData.ethernets.eth0.addresses</code> defines the static IPv6 address as determined by the <code>Network.pod.vmIPv6NetworkCIDR</code> field in the virtual machine instance configuration. The default value is <code>fd10:0:2::2/120</code>.</li>
      <li><code>spec.template.spec.volumes.cloudInitNoCloud.networkData.ethernets.eth0.gateway6</code> defines the gateway IP address as determined by the <code>Network.pod.vmIPv6NetworkCIDR</code> field in the virtual machine instance configuration. The default value is <code>fd10:0:2::1</code>.</li>
      </ul>
      </li>
      <li>
      Create the virtual machine in the namespace:
      <CodeBlock code={"$ oc create -f example-vm-ipv6.yaml"} lang="terminal" />
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul><li>To verify that IPv6 has been configured, start the virtual machine and view the interface status of the virtual machine instance to ensure it has an IPv6 address:</li></ul>
      <CodeBlock code={"$ oc get vmi <vmi-name> -o jsonpath=\"{.status.interfaces[*].ipAddresses}\""} lang="terminal" />
      <h2 id="virt-jumbo-frames-vm-pod-nw_virt-connecting-vm-to-default-pod-network" class="group scroll-mt-20">About jumbo frames support<a href="#virt-jumbo-frames-vm-pod-nw_virt-connecting-vm-to-default-pod-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">When using the OVN-Kubernetes CNI plugin, you can send unfragmented jumbo frame packets between two virtual machines (VMs) that are connected on the default pod network. Jumbo frames have a maximum transmission unit (MTU) value greater than 1500 bytes.</p>
      <p>The VM automatically gets the MTU value of the cluster network, set by the cluster administrator, in one of the following ways:</p>
      <ul>
      <li><code>libvirt</code>: If the guest OS has the latest version of the VirtIO driver that can interpret incoming data via a Peripheral Component Interconnect (PCI) config register in the emulated device.</li>
      <li>DHCP: If the guest DHCP client can read the MTU value from the DHCP server response.</li>
      </ul>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>For Windows VMs that do not have a VirtIO driver, you must set the MTU manually by using <code>netsh</code> or a similar tool. This is because the Windows DHCP client does not read the MTU value.</p>
        </AlertDescription>
      </Alert>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/networking/advanced_networking/changing-cluster-network-mtu#changing-cluster-network-mtu">Changing the MTU for the cluster network</a></li>
      <li><a href="/openshift-docs-markdown/scalability_and_performance/optimization/optimizing-networking#optimizing-mtu_optimizing-networking">Optimizing the MTU for your network</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
