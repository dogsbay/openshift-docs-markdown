---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: virt/live_migration/virt-about-mtv-providers.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"virt-about-mtv-providers","text":"About Migration Toolkit for Virtualization (MTV) providers"},{"depth":2,"slug":"virt-configuring-root-ca-for-providers_virt-about-mtv-providers","text":"Configuring the root certificate authority for providers"},{"depth":2,"slug":"virt-creating-long-lived-account-and-token_virt-about-mtv-providers","text":"Creating the long-lived service account and token to use with MTV providers"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/virt/live_migration/virt-about-mtv-providers";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "About Migration Toolkit for Virtualization (MTV) providers";
const description = "To migrate a virtual machine (VM) across OpenShift Container Platform clusters, you must configure an OpenShift Container Platform provider for each…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["virt","virt/live_migration"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# About Migration Toolkit for Virtualization (MTV) providers {#virt-about-mtv-providers}\n\nTo migrate a virtual machine (VM) across OpenShift Container Platform clusters, you must configure an OpenShift Container Platform provider for each cluster that you are including in the migration. If MTV is already installed on a cluster, a local provider already exists.\n\n## Configuring the root certificate authority for providers {#virt-configuring-root-ca-for-providers_virt-about-mtv-providers}\n\nYou must configure an OpenShift Container Platform provider for each cluster that you are including in a migration, and each provider requires a certificate authority (CA) for the cluster. Configure the root CA for the entire cluster to avoid CA expiration, which causes the provider to fail.\n\n**Procedure**\n\n1. Run the following command against the cluster for which you are creating the provider:\n\n   ```terminal\n   $ oc get cm kube-root-ca.crt -o=jsonpath={.data.ca\\\\.crt}\n   ```\n2. Copy the printed certificate.\n3. In the Migration Toolkit for Virtualization (MTV) web console, create a provider and select **OpenShift Virtualization**.\n4. Paste the certificate into the **CA certificate** field, as shown in the following example:\n\n   ```terminal\n   -----BEGIN CERTIFICATE-----\n   <CA_certificate_content>\n   -----END CERTIFICATE-----\n   ```\n\n## Creating the long-lived service account and token to use with MTV providers {#virt-creating-long-lived-account-and-token_virt-about-mtv-providers}\n\nWhen you register an OpenShift Virtualization provider in the Migration Toolkit for Virtualization (MTV) web console, you must create a service account and cluster role binding, which gives MTV persistent permissions to read and create virtual machine resources during migration.\n\n**Procedure**\n\n1. Create the cluster role as shown in the following example:\n\n   ```yaml\n   apiVersion: rbac.authorization.k8s.io/v1\n   kind: ClusterRole\n   metadata:\n     name: live-migration-role\n   rules:\n     - apiGroups:\n         - forklift.konveyor.io\n       resources:\n         - '*'\n       verbs:\n         - get\n         - list\n         - watch\n     - apiGroups:\n         - \"\"\n       resources:\n         - secrets\n         - namespaces\n         - configmaps\n         - persistentvolumes\n         - persistentvolumeclaims\n       verbs:\n         - get\n         - list\n         - watch\n         - create\n         - update\n         - patch\n         - delete\n     - apiGroups:\n         - k8s.cni.cncf.io\n       resources:\n         - network-attachment-definitions\n       verbs:\n         - get\n         - list\n         - watch\n     - apiGroups:\n         - storage.k8s.io\n       resources:\n         - storageclasses\n       verbs:\n         - get\n         - list\n         - watch\n     - apiGroups:\n         - kubevirt.io\n       resources:\n         - virtualmachines\n         - virtualmachines/finalizers\n         - virtualmachineinstancemigrations\n       verbs:\n         - get\n         - list\n         - watch\n         - create\n         - update\n         - patch\n         - delete\n     - apiGroups:\n         - kubevirt.io\n       resources:\n         - kubevirts\n         - virtualmachineinstances\n       verbs:\n         - get\n         - list\n         - watch\n     - apiGroups:\n         - cdi.kubevirt.io\n       resources:\n         - datavolumes\n         - datavolumes/finalizers\n       verbs:\n         - get\n         - list\n         - watch\n         - create\n         - update\n         - patch\n         - delete\n     - apiGroups:\n         - apps\n       resources:\n         - deployments\n       verbs:\n         - get\n         - list\n         - watch\n         - create\n         - update\n         - patch\n         - delete\n     - apiGroups:\n         - instancetype.kubevirt.io\n       resources:\n         - virtualmachineclusterpreferences\n         - virtualmachineclusterinstancetypes\n       verbs:\n         - get\n         - list\n         - watch\n     - apiGroups:\n         - instancetype.kubevirt.io\n       resources:\n         - virtualmachinepreferences\n         - virtualmachineinstancetypes\n       verbs:\n         - get\n         - list\n         - watch\n         - create\n         - update\n         - patch\n         - delete\n   ```\n2. Create the cluster role by running the following command:\n\n   ```terminal\n   $ oc create -f <filename>.yaml\n   ```\n3. Create a service account by running the following command:\n\n   ```terminal\n   $ oc create serviceaccount <service_account_name> -n <service_account_namespace>\n   ```\n4. Create a cluster role binding that links the service account to the cluster role, by running the following command:\n\n   ```terminal\n   $ oc create clusterrolebinding <service_account_name> --clusterrole=<cluster_role_name> --serviceaccount=<service_account_namespace>:<service_account_name>\n   ```\n5. Create a secret to hold the token by saving the following manifest as a YAML file:\n\n   ```yaml\n   apiVersion: v1\n   kind: Secret\n   metadata:\n     name: <name_of_secret>\n     namespace: <namespace_for_service_account>\n     annotations:\n       kubernetes.io/service-account.name: <service_account_name>\n   type: kubernetes.io/service-account-token\n   ```\n6. Apply the manifest by running the following command:\n\n   ```terminal\n   $ oc apply -f <filename>.yaml\n   ```\n7. After the secret is populated, run the following command to get the service account bearer token:\n\n   ```terminal\n   $ TOKEN_BASE64=$(oc get secret \"<name_of_secret>\" -n \"<namespace_bound_to_service_account>\" -o jsonpath='{.data.token}')\n     TOKEN=$(echo \"$TOKEN_BASE64\" | base64 --decode)\n     echo \"$TOKEN\"\n   ```\n8. Copy the printed token.\n9. In the Migration Toolkit for Virtualization (MTV) web console, when you create a provider and select **OpenShift Virtualization**, paste the token into the **Service account bearer token** field.\n\n**Additional resources**\n{._additional-resources}\n\n- [Adding a Red Hat OpenShift Virtualization source provider](https://docs.redhat.com/en/documentation/migration_toolkit_for_virtualization/2.9/html-single/installing_and_using_the_migration_toolkit_for_virtualization/index#adding-source-provider_cnv)\n- [Adding an OpenShift Virtualization destination provider](https://docs.redhat.com/en/documentation/migration_toolkit_for_virtualization/2.9/html-single/installing_and_using_the_migration_toolkit_for_virtualization/index#adding-source-provider_dest_cnv)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/virt/live_migration/virt-about-mtv-providers.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="virt-about-mtv-providers" class="group scroll-mt-20">About Migration Toolkit for Virtualization (MTV) providers<a href="#virt-about-mtv-providers" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">To migrate a virtual machine (VM) across OpenShift Container Platform clusters, you must configure an OpenShift Container Platform provider for each cluster that you are including in the migration. If MTV is already installed on a cluster, a local provider already exists.</p>
      <h2 id="virt-configuring-root-ca-for-providers_virt-about-mtv-providers" class="group scroll-mt-20">Configuring the root certificate authority for providers<a href="#virt-configuring-root-ca-for-providers_virt-about-mtv-providers" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You must configure an OpenShift Container Platform provider for each cluster that you are including in a migration, and each provider requires a certificate authority (CA) for the cluster. Configure the root CA for the entire cluster to avoid CA expiration, which causes the provider to fail.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Run the following command against the cluster for which you are creating the provider:
      <CodeBlock code={"$ oc get cm kube-root-ca.crt -o=jsonpath={.data.ca\\\\.crt}"} lang="terminal" />
      </li>
      <li>Copy the printed certificate.</li>
      <li>In the Migration Toolkit for Virtualization (MTV) web console, create a provider and select <strong>OpenShift Virtualization</strong>.</li>
      <li>
      Paste the certificate into the <strong>CA certificate</strong> field, as shown in the following example:
      <CodeBlock code={"-----BEGIN CERTIFICATE-----\n<CA_certificate_content>\n-----END CERTIFICATE-----"} lang="terminal" />
      </li>
      </ol>
      <h2 id="virt-creating-long-lived-account-and-token_virt-about-mtv-providers" class="group scroll-mt-20">Creating the long-lived service account and token to use with MTV providers<a href="#virt-creating-long-lived-account-and-token_virt-about-mtv-providers" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">When you register an OpenShift Virtualization provider in the Migration Toolkit for Virtualization (MTV) web console, you must create a service account and cluster role binding, which gives MTV persistent permissions to read and create virtual machine resources during migration.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create the cluster role as shown in the following example:
      <CodeBlock code={"apiVersion: rbac.authorization.k8s.io/v1\nkind: ClusterRole\nmetadata:\n  name: live-migration-role\nrules:\n  - apiGroups:\n      - forklift.konveyor.io\n    resources:\n      - '*'\n    verbs:\n      - get\n      - list\n      - watch\n  - apiGroups:\n      - \"\"\n    resources:\n      - secrets\n      - namespaces\n      - configmaps\n      - persistentvolumes\n      - persistentvolumeclaims\n    verbs:\n      - get\n      - list\n      - watch\n      - create\n      - update\n      - patch\n      - delete\n  - apiGroups:\n      - k8s.cni.cncf.io\n    resources:\n      - network-attachment-definitions\n    verbs:\n      - get\n      - list\n      - watch\n  - apiGroups:\n      - storage.k8s.io\n    resources:\n      - storageclasses\n    verbs:\n      - get\n      - list\n      - watch\n  - apiGroups:\n      - kubevirt.io\n    resources:\n      - virtualmachines\n      - virtualmachines/finalizers\n      - virtualmachineinstancemigrations\n    verbs:\n      - get\n      - list\n      - watch\n      - create\n      - update\n      - patch\n      - delete\n  - apiGroups:\n      - kubevirt.io\n    resources:\n      - kubevirts\n      - virtualmachineinstances\n    verbs:\n      - get\n      - list\n      - watch\n  - apiGroups:\n      - cdi.kubevirt.io\n    resources:\n      - datavolumes\n      - datavolumes/finalizers\n    verbs:\n      - get\n      - list\n      - watch\n      - create\n      - update\n      - patch\n      - delete\n  - apiGroups:\n      - apps\n    resources:\n      - deployments\n    verbs:\n      - get\n      - list\n      - watch\n      - create\n      - update\n      - patch\n      - delete\n  - apiGroups:\n      - instancetype.kubevirt.io\n    resources:\n      - virtualmachineclusterpreferences\n      - virtualmachineclusterinstancetypes\n    verbs:\n      - get\n      - list\n      - watch\n  - apiGroups:\n      - instancetype.kubevirt.io\n    resources:\n      - virtualmachinepreferences\n      - virtualmachineinstancetypes\n    verbs:\n      - get\n      - list\n      - watch\n      - create\n      - update\n      - patch\n      - delete"} lang="yaml" />
      </li>
      <li>
      Create the cluster role by running the following command:
      <CodeBlock code={"$ oc create -f <filename>.yaml"} lang="terminal" />
      </li>
      <li>
      Create a service account by running the following command:
      <CodeBlock code={"$ oc create serviceaccount <service_account_name> -n <service_account_namespace>"} lang="terminal" />
      </li>
      <li>
      Create a cluster role binding that links the service account to the cluster role, by running the following command:
      <CodeBlock code={"$ oc create clusterrolebinding <service_account_name> --clusterrole=<cluster_role_name> --serviceaccount=<service_account_namespace>:<service_account_name>"} lang="terminal" />
      </li>
      <li>
      Create a secret to hold the token by saving the following manifest as a YAML file:
      <CodeBlock code={"apiVersion: v1\nkind: Secret\nmetadata:\n  name: <name_of_secret>\n  namespace: <namespace_for_service_account>\n  annotations:\n    kubernetes.io/service-account.name: <service_account_name>\ntype: kubernetes.io/service-account-token"} lang="yaml" />
      </li>
      <li>
      Apply the manifest by running the following command:
      <CodeBlock code={"$ oc apply -f <filename>.yaml"} lang="terminal" />
      </li>
      <li>
      After the secret is populated, run the following command to get the service account bearer token:
      <CodeBlock code={"$ TOKEN_BASE64=$(oc get secret \"<name_of_secret>\" -n \"<namespace_bound_to_service_account>\" -o jsonpath='{.data.token}')\n  TOKEN=$(echo \"$TOKEN_BASE64\" | base64 --decode)\n  echo \"$TOKEN\""} lang="terminal" />
      </li>
      <li>Copy the printed token.</li>
      <li>In the Migration Toolkit for Virtualization (MTV) web console, when you create a provider and select <strong>OpenShift Virtualization</strong>, paste the token into the <strong>Service account bearer token</strong> field.</li>
      </ol>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="https://docs.redhat.com/en/documentation/migration_toolkit_for_virtualization/2.9/html-single/installing_and_using_the_migration_toolkit_for_virtualization/index#adding-source-provider_cnv">Adding a Red Hat OpenShift Virtualization source provider</a></li>
      <li><a href="https://docs.redhat.com/en/documentation/migration_toolkit_for_virtualization/2.9/html-single/installing_and_using_the_migration_toolkit_for_virtualization/index#adding-source-provider_dest_cnv">Adding an OpenShift Virtualization destination provider</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
