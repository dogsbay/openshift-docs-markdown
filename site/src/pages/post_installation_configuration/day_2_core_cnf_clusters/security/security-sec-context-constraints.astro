---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: post_installation_configuration/day_2_core_cnf_clusters/security/security-sec-context-constraints.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"security-sec-context-constraints","text":"Security context constraints"}];
const nav = navData;
const currentPath = "/post_installation_configuration/day_2_core_cnf_clusters/security/security-sec-context-constraints";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Security context constraints";
const description = "Similar to the way that RBAC resources control user access, administrators can use security context constraints (SCCs) to control permissions for pods…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["post_installation_configuration","post_installation_configuration/day_2_core_cnf_clusters","post_installation_configuration/day_2_core_cnf_clusters/security"];
const pageTaxonomies = null;
const tagsIndexPath = "/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Security context constraints {#security-sec-context-constraints}\n\nSimilar to the way that RBAC resources control user access, administrators can use security context constraints (SCCs) to control permissions for pods. These permissions determine the actions that a pod can perform and what resources it can access. You can use SCCs to define a set of conditions that a pod must run.\n\nSecurity context constraints allow an administrator to control the following security constraints:\n\n- Whether a pod can run privileged containers with the `allowPrivilegedContainer` flag\n- Whether a pod is constrained with the `allowPrivilegeEscalation` flag\n- The capabilities that a container can request\n- The use of host directories as volumes\n- The SELinux context of the container\n- The container user ID\n- The use of host namespaces and networking\n- The allocation of an `FSGroup` that owns the pod volumes\n- The configuration of allowable supplemental groups\n- Whether a container requires write access to its root file system\n- The usage of volume types\n- The configuration of allowable `seccomp` profiles\n\nDefault SCCs are created during installation and when you install some Operators or other components. As a cluster administrator, you can also create your own SCCs by using the OpenShift CLI (`oc`).\n\nFor information about default security context constraints, see [Default security context constraints](/authentication/managing-security-context-constraints#default-sccs_configuring-internal-oauth).\n\n> [!IMPORTANT]\n> Do not modify the default SCCs. Customizing the default SCCs can lead to issues when some of the platform pods deploy or OpenShift Container Platform is upgraded. Additionally, the default SCC values are reset to the defaults during some cluster upgrades, which discards all customizations to those SCCs.\n>\n> Instead of modifying the default SCCs, create and modify your own SCCs as needed. For detailed steps, see [Creating security context constraints](/authentication/managing-security-context-constraints#security-context-constraints-creating_configuring-internal-oauth).\n\nYou can use the following basic SCCs:\n\n- `restricted`\n- `restricted-v2`\n\nThe `restricted-v2` SCC is the most restrictive SCC provided by a new installation and is used by default for authenticated users. It aligns with Pod Security Admission (PSA) restrictions and improves security, as the original `restricted` SCC is less restrictive. It also helps transition from the original SCCs to v2 across multiple releases. Eventually, the original SCCs get deprecated. Therefore, it is recommended to use the `restricted-v2` SCC.\n\nYou can examine the `restricted-v2` SCC by running the following command:\n\n```terminal\n$ oc describe scc restricted-v2\n```\n\n```terminal {title=\"Example output\"}\nName:                                           restricted-v2\nPriority:                                       <none>\nAccess:\n  Users:                                        <none>\n  Groups:                                       <none>\nSettings:\n  Allow Privileged:                             false\n  Allow Privilege Escalation:                   false\n  Default Add Capabilities:                     <none>\n  Required Drop Capabilities:                   ALL\n  Allowed Capabilities:                         NET_BIND_SERVICE\n  Allowed Seccomp Profiles:                     runtime/default\n  Allowed Volume Types:                         configMap,downwardAPI,emptyDir,ephemeral,persistentVolumeClaim,projected,secret\n  Allowed Flexvolumes:                          <all>\n  Allowed Unsafe Sysctls:                       <none>\n  Forbidden Sysctls:                            <none>\n  Allow Host Network:                           false\n  Allow Host Ports:                             false\n  Allow Host PID:                               false\n  Allow Host IPC:                               false\n  Read Only Root Filesystem:                    false\n  Run As User Strategy: MustRunAsRange\n    UID:                                        <none>\n    UID Range Min:                              <none>\n    UID Range Max:                              <none>\n  SELinux Context Strategy: MustRunAs\n    User:                                       <none>\n    Role:                                       <none>\n    Type:                                       <none>\n    Level:                                      <none>\n  FSGroup Strategy: MustRunAs\n    Ranges:                                     <none>\n  Supplemental Groups Strategy: RunAsAny\n    Ranges:                                     <none>\n```\n\nThe `restricted-v2` SCC explicitly denies everything except what it explicitly allows. The following settings define the allowed capabilities and security restrictions:\n\n- Default add capabilities: Set to `<none>`. It means that no capabilities are added to a pod by default.\n- Required drop capabilities: Set to `ALL`. This drops all the default Linux capabilities of a pod.\n- Allowed capabilities: `NET_BIND_SERVICE`. A pod can request this capability, but it is not added by default.\n- Allowed `seccomp` profiles: `runtime/default`.\n\nFor more information, see [Managing security context constraints](/authentication/managing-security-context-constraints).\n","mdUrl":"/post_installation_configuration/day_2_core_cnf_clusters/security/security-sec-context-constraints.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={""}
  navMode={"client"}
  pagefindUrl={"/pagefind/"}
  favicon={"/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="security-sec-context-constraints" class="group scroll-mt-20">Security context constraints<a href="#security-sec-context-constraints" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="/_attributes/common-attributes.md"></p>
      <p>Similar to the way that RBAC resources control user access, administrators can use security context constraints (SCCs) to control permissions for pods. These permissions determine the actions that a pod can perform and what resources it can access. You can use SCCs to define a set of conditions that a pod must run.</p>
      <p>Security context constraints allow an administrator to control the following security constraints:</p>
      <ul>
      <li>Whether a pod can run privileged containers with the <code>allowPrivilegedContainer</code> flag</li>
      <li>Whether a pod is constrained with the <code>allowPrivilegeEscalation</code> flag</li>
      <li>The capabilities that a container can request</li>
      <li>The use of host directories as volumes</li>
      <li>The SELinux context of the container</li>
      <li>The container user ID</li>
      <li>The use of host namespaces and networking</li>
      <li>The allocation of an <code>FSGroup</code> that owns the pod volumes</li>
      <li>The configuration of allowable supplemental groups</li>
      <li>Whether a container requires write access to its root file system</li>
      <li>The usage of volume types</li>
      <li>The configuration of allowable <code>seccomp</code> profiles</li>
      </ul>
      <p>Default SCCs are created during installation and when you install some Operators or other components. As a cluster administrator, you can also create your own SCCs by using the OpenShift CLI (<code>oc</code>).</p>
      <p>For information about default security context constraints, see <a href="/authentication/managing-security-context-constraints#default-sccs_configuring-internal-oauth">Default security context constraints</a>.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Do not modify the default SCCs. Customizing the default SCCs can lead to issues when some of the platform pods deploy or OpenShift Container Platform is upgraded. Additionally, the default SCC values are reset to the defaults during some cluster upgrades, which discards all customizations to those SCCs.</p>
          <p>Instead of modifying the default SCCs, create and modify your own SCCs as needed. For detailed steps, see <a href="/authentication/managing-security-context-constraints#security-context-constraints-creating_configuring-internal-oauth">Creating security context constraints</a>.</p>
        </AlertDescription>
      </Alert>
      <p>You can use the following basic SCCs:</p>
      <ul>
      <li><code>restricted</code></li>
      <li><code>restricted-v2</code></li>
      </ul>
      <p>The <code>restricted-v2</code> SCC is the most restrictive SCC provided by a new installation and is used by default for authenticated users. It aligns with Pod Security Admission (PSA) restrictions and improves security, as the original <code>restricted</code> SCC is less restrictive. It also helps transition from the original SCCs to v2 across multiple releases. Eventually, the original SCCs get deprecated. Therefore, it is recommended to use the <code>restricted-v2</code> SCC.</p>
      <p>You can examine the <code>restricted-v2</code> SCC by running the following command:</p>
      <CodeBlock code={"$ oc describe scc restricted-v2"} lang="terminal" />
      <CodeBlock code={"Name:                                           restricted-v2\nPriority:                                       <none>\nAccess:\n  Users:                                        <none>\n  Groups:                                       <none>\nSettings:\n  Allow Privileged:                             false\n  Allow Privilege Escalation:                   false\n  Default Add Capabilities:                     <none>\n  Required Drop Capabilities:                   ALL\n  Allowed Capabilities:                         NET_BIND_SERVICE\n  Allowed Seccomp Profiles:                     runtime/default\n  Allowed Volume Types:                         configMap,downwardAPI,emptyDir,ephemeral,persistentVolumeClaim,projected,secret\n  Allowed Flexvolumes:                          <all>\n  Allowed Unsafe Sysctls:                       <none>\n  Forbidden Sysctls:                            <none>\n  Allow Host Network:                           false\n  Allow Host Ports:                             false\n  Allow Host PID:                               false\n  Allow Host IPC:                               false\n  Read Only Root Filesystem:                    false\n  Run As User Strategy: MustRunAsRange\n    UID:                                        <none>\n    UID Range Min:                              <none>\n    UID Range Max:                              <none>\n  SELinux Context Strategy: MustRunAs\n    User:                                       <none>\n    Role:                                       <none>\n    Type:                                       <none>\n    Level:                                      <none>\n  FSGroup Strategy: MustRunAs\n    Ranges:                                     <none>\n  Supplemental Groups Strategy: RunAsAny\n    Ranges:                                     <none>"} lang="terminal" title="Example output" />
      <p>The <code>restricted-v2</code> SCC explicitly denies everything except what it explicitly allows. The following settings define the allowed capabilities and security restrictions:</p>
      <ul>
      <li>Default add capabilities: Set to <code>&lt;none&gt;</code>. It means that no capabilities are added to a pod by default.</li>
      <li>Required drop capabilities: Set to <code>ALL</code>. This drops all the default Linux capabilities of a pod.</li>
      <li>Allowed capabilities: <code>NET_BIND_SERVICE</code>. A pod can request this capability, but it is not added by default.</li>
      <li>Allowed <code>seccomp</code> profiles: <code>runtime/default</code>.</li>
      </ul>
      <p>For more information, see <a href="/authentication/managing-security-context-constraints">Managing security context constraints</a>.</p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
