---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: post_installation_configuration/day_2_core_cnf_clusters/troubleshooting/troubleshooting-mco.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"troubleshooting-mco","text":"Machine Config Operator"},{"depth":2,"slug":"troubleshooting-mco-purpose_troubleshooting-mco","text":"Purpose of the Machine Config Operator"},{"depth":2,"slug":"troubleshooting-mco-apply-several-mcs_troubleshooting-mco","text":"Applying several machine config files at the same time"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/post_installation_configuration/day_2_core_cnf_clusters/troubleshooting/troubleshooting-mco";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Machine Config Operator";
const description = "The Machine Config Operator provides useful information to cluster administrators and controls what is running directly on the bare-metal host." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["post_installation_configuration","post_installation_configuration/day_2_core_cnf_clusters","post_installation_configuration/day_2_core_cnf_clusters/troubleshooting"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Machine Config Operator {#troubleshooting-mco}\n\nThe Machine Config Operator provides useful information to cluster administrators and controls what is running directly on the bare-metal host.\n\nThe Machine Config Operator differentiates between groups of nodes in the cluster, allowing control plane nodes and worker nodes to run with different configurations. These groups of nodes run worker or application pods, which are called `MachineConfigPool` (`mcp`) groups. The same machine config is applied to all nodes or only to one MCP in the cluster.\n\nFor more information about the Machine Config Operator, see [Machine Config Operator](/openshift-docs-markdown/operators/operator-reference#machine-config-operator_cluster-operators-ref).\n\n## Purpose of the Machine Config Operator {#troubleshooting-mco-purpose_troubleshooting-mco}\n\nThe Machine Config Operator (MCO) manages and applies configuration and updates of Red Hat Enterprise Linux CoreOS (RHCOS) and container runtime, including everything between the kernel and kubelet. Managing RHCOS is important since most telecommunications companies run on bare-metal hardware and use some sort of hardware accelerator or kernel modification. Applying machine configuration to RHCOS manually can cause problems because the MCO monitors each node and what is applied to it.\n\nYou must consider these minor components and how the MCO can help you manage your clusters effectively.\n\n> [!IMPORTANT]\n> You must use the MCO to perform all changes on worker or control plane nodes. Do not manually make changes to RHCOS or node files.\n\n## Applying several machine config files at the same time {#troubleshooting-mco-apply-several-mcs_troubleshooting-mco}\n\nWhen you need to change the machine config for a group of nodes in the cluster, also known as machine config pools (MCPs), sometimes the changes must be applied with several different machine config files. The nodes need to restart for the machine config file to be applied. After each machine config file is applied to the cluster, all nodes restart that are affected by the machine config file.\n\nTo prevent the nodes from restarting for each machine config file, you can apply all of the changes at the same time by pausing each MCP that is updated by the new machine config file.\n\n**Procedure**\n\n1. Pause the affected MCP by running the following command:\n\n   ```terminal\n   $ oc patch mcp/<mcp_name> --type merge --patch '{\"spec\":{\"paused\":true}}'\n   ```\n2. After you apply all machine config changes to the cluster, run the following command:\n\n   ```terminal\n   $ oc patch mcp/<mcp_name> --type merge --patch '{\"spec\":{\"paused\":false}}'\n   ```\n\n   This allows the nodes in your MCP to reboot into the new configurations.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/post_installation_configuration/day_2_core_cnf_clusters/troubleshooting/troubleshooting-mco.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="troubleshooting-mco" class="group scroll-mt-20">Machine Config Operator<a href="#troubleshooting-mco" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>The Machine Config Operator provides useful information to cluster administrators and controls what is running directly on the bare-metal host.</p>
      <p>The Machine Config Operator differentiates between groups of nodes in the cluster, allowing control plane nodes and worker nodes to run with different configurations. These groups of nodes run worker or application pods, which are called <code>MachineConfigPool</code> (<code>mcp</code>) groups. The same machine config is applied to all nodes or only to one MCP in the cluster.</p>
      <p>For more information about the Machine Config Operator, see <a href="/openshift-docs-markdown/operators/operator-reference#machine-config-operator_cluster-operators-ref">Machine Config Operator</a>.</p>
      <h2 id="troubleshooting-mco-purpose_troubleshooting-mco" class="group scroll-mt-20">Purpose of the Machine Config Operator<a href="#troubleshooting-mco-purpose_troubleshooting-mco" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The Machine Config Operator (MCO) manages and applies configuration and updates of Red Hat Enterprise Linux CoreOS (RHCOS) and container runtime, including everything between the kernel and kubelet. Managing RHCOS is important since most telecommunications companies run on bare-metal hardware and use some sort of hardware accelerator or kernel modification. Applying machine configuration to RHCOS manually can cause problems because the MCO monitors each node and what is applied to it.</p>
      <p>You must consider these minor components and how the MCO can help you manage your clusters effectively.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>You must use the MCO to perform all changes on worker or control plane nodes. Do not manually make changes to RHCOS or node files.</p>
        </AlertDescription>
      </Alert>
      <h2 id="troubleshooting-mco-apply-several-mcs_troubleshooting-mco" class="group scroll-mt-20">Applying several machine config files at the same time<a href="#troubleshooting-mco-apply-several-mcs_troubleshooting-mco" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">When you need to change the machine config for a group of nodes in the cluster, also known as machine config pools (MCPs), sometimes the changes must be applied with several different machine config files. The nodes need to restart for the machine config file to be applied. After each machine config file is applied to the cluster, all nodes restart that are affected by the machine config file.</p>
      <p>To prevent the nodes from restarting for each machine config file, you can apply all of the changes at the same time by pausing each MCP that is updated by the new machine config file.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Pause the affected MCP by running the following command:
      <CodeBlock code={"$ oc patch mcp/<mcp_name> --type merge --patch '{\"spec\":{\"paused\":true}}'"} lang="terminal" />
      </li>
      <li>
      After you apply all machine config changes to the cluster, run the following command:
      <CodeBlock code={"$ oc patch mcp/<mcp_name> --type merge --patch '{\"spec\":{\"paused\":false}}'"} lang="terminal" />
      <p>This allows the nodes in your MCP to reboot into the new configurations.</p>
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
