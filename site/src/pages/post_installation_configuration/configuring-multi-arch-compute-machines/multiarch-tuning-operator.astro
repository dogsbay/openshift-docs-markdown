---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: post_installation_configuration/configuring-multi-arch-compute-machines/multiarch-tuning-operator.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";

const headings = [{"depth":1,"slug":"multiarch-tuning-operator","text":"Managing workloads on multi-architecture clusters by using the Multiarch Tuning Operator"}];
const nav = navData;
const currentPath = "/post_installation_configuration/configuring-multi-arch-compute-machines/multiarch-tuning-operator";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Managing workloads on multi-architecture clusters by using the Multiarch Tuning Operator";
const description = "The Multiarch Tuning Operator optimizes workload management within multi-architecture clusters and in single-architecture clusters transitioning to…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["post_installation_configuration","post_installation_configuration/configuring-multi-arch-compute-machines"];
const pageTaxonomies = null;
const tagsIndexPath = "/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Managing workloads on multi-architecture clusters by using the Multiarch Tuning Operator {#multiarch-tuning-operator}\n\nThe Multiarch Tuning Operator optimizes workload management within multi-architecture clusters and in single-architecture clusters transitioning to multi-architecture environments.\n\nArchitecture-aware workload scheduling allows the scheduler to place pods onto nodes that match the architecture of the pod images.\n\nBy default, the scheduler does not consider the architecture of a pod’s container images when determining the placement of new pods onto nodes.\n\nTo enable architecture-aware workload scheduling, you must create the `ClusterPodPlacementConfig` object. When you create the `ClusterPodPlacementConfig` object, the Multiarch Tuning Operator deploys the necessary operands to support architecture-aware workload scheduling. You can also use the `nodeAffinityScoring` plugin in the `ClusterPodPlacementConfig` object to set cluster-wide scores for node architectures. If you enable the `nodeAffinityScoring` plugin, the scheduler first filters nodes with compatible architectures and then places the pod on the node with the highest score.\n\nWhen a pod is created, the operands perform the following actions:\n\n1. Add the `multiarch.openshift.io/scheduling-gate` scheduling gate that prevents the scheduling of the pod.\n2. Compute a scheduling predicate that includes the supported architecture values for the `kubernetes.io/arch` label.\n3. Integrate the scheduling predicate as a `nodeAffinity` requirement in the pod specification.\n4. Remove the scheduling gate from the pod.\n\n> [!IMPORTANT]\n> Note the following operand behaviors:\n>\n> - If the `nodeSelector` field is already configured with the `kubernetes.io/arch` label for a workload, the operand does not update the `nodeAffinity` field for that workload.\n> - If the `nodeSelector` field is not configured with the `kubernetes.io/arch` label for a workload, the operand updates the `nodeAffinity` field for that workload. For the `nodeAffinity` field, the operand updates only the node selector terms that are not configured with the `kubernetes.io/arch` label.\n> - If the `nodeName` field is already set, the Multiarch Tuning Operator does not process the pod.\n> - If the pod is owned by a DaemonSet, the operand does not update the `nodeAffinity` field.\n> - If `nodeSelector` or `nodeAffinity` and `preferredAffinity` fields are set for the `kubernetes.io/arch` label, the operand does not update the `nodeAffinity` field.\n> - If only the `nodeSelector` or the `nodeAffinity` field is set for the `kubernetes.io/arch` label and the `nodeAffinityScoring` plugin is disabled, the operand does not update the `nodeAffinity` field.\n> - If the `nodeAffinity.preferredDuringSchedulingIgnoredDuringExecution` field already contains terms that score nodes based on the `kubernetes.io/arch` label, the operand ignores the configuration in the `nodeAffinityScoring` plugin.\n\n**Additional resources**\n\n- [Installing from the software catalog by using the CLI](/operators/user/olm-installing-operators-in-namespace#olm-installing-operator-from-software-catalog-using-cli_olm-installing-operators-in-namespace)\n","mdUrl":"/post_installation_configuration/configuring-multi-arch-compute-machines/multiarch-tuning-operator.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={""}
  navMode={"client"}
  pagefindUrl={"/pagefind/"}
  favicon={"/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="multiarch-tuning-operator" class="group scroll-mt-20">Managing workloads on multi-architecture clusters by using the Multiarch Tuning Operator<a href="#multiarch-tuning-operator" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="/_attributes/common-attributes.md"></p>
      <p>The Multiarch Tuning Operator optimizes workload management within multi-architecture clusters and in single-architecture clusters transitioning to multi-architecture environments.</p>
      <p>Architecture-aware workload scheduling allows the scheduler to place pods onto nodes that match the architecture of the pod images.</p>
      <p>By default, the scheduler does not consider the architecture of a pod’s container images when determining the placement of new pods onto nodes.</p>
      <p>To enable architecture-aware workload scheduling, you must create the <code>ClusterPodPlacementConfig</code> object. When you create the <code>ClusterPodPlacementConfig</code> object, the Multiarch Tuning Operator deploys the necessary operands to support architecture-aware workload scheduling. You can also use the <code>nodeAffinityScoring</code> plugin in the <code>ClusterPodPlacementConfig</code> object to set cluster-wide scores for node architectures. If you enable the <code>nodeAffinityScoring</code> plugin, the scheduler first filters nodes with compatible architectures and then places the pod on the node with the highest score.</p>
      <p>When a pod is created, the operands perform the following actions:</p>
      <ol>
      <li>Add the <code>multiarch.openshift.io/scheduling-gate</code> scheduling gate that prevents the scheduling of the pod.</li>
      <li>Compute a scheduling predicate that includes the supported architecture values for the <code>kubernetes.io/arch</code> label.</li>
      <li>Integrate the scheduling predicate as a <code>nodeAffinity</code> requirement in the pod specification.</li>
      <li>Remove the scheduling gate from the pod.</li>
      </ol>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Note the following operand behaviors:</p>
          <ul>
          <li>If the <code>nodeSelector</code> field is already configured with the <code>kubernetes.io/arch</code> label for a workload, the operand does not update the <code>nodeAffinity</code> field for that workload.</li>
          <li>If the <code>nodeSelector</code> field is not configured with the <code>kubernetes.io/arch</code> label for a workload, the operand updates the <code>nodeAffinity</code> field for that workload. For the <code>nodeAffinity</code> field, the operand updates only the node selector terms that are not configured with the <code>kubernetes.io/arch</code> label.</li>
          <li>If the <code>nodeName</code> field is already set, the Multiarch Tuning Operator does not process the pod.</li>
          <li>If the pod is owned by a DaemonSet, the operand does not update the <code>nodeAffinity</code> field.</li>
          <li>If <code>nodeSelector</code> or <code>nodeAffinity</code> and <code>preferredAffinity</code> fields are set for the <code>kubernetes.io/arch</code> label, the operand does not update the <code>nodeAffinity</code> field.</li>
          <li>If only the <code>nodeSelector</code> or the <code>nodeAffinity</code> field is set for the <code>kubernetes.io/arch</code> label and the <code>nodeAffinityScoring</code> plugin is disabled, the operand does not update the <code>nodeAffinity</code> field.</li>
          <li>If the <code>nodeAffinity.preferredDuringSchedulingIgnoredDuringExecution</code> field already contains terms that score nodes based on the <code>kubernetes.io/arch</code> label, the operand ignores the configuration in the <code>nodeAffinityScoring</code> plugin.</li>
          </ul>
        </AlertDescription>
      </Alert>
      <p class="/modules/multi-arch-installing-using-cli.md"></p>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/operators/user/olm-installing-operators-in-namespace#olm-installing-operator-from-software-catalog-using-cli_olm-installing-operators-in-namespace">Installing from the software catalog by using the CLI</a></li></ul>
      <p class="/modules/multi-arch-gather-info-about-workloads.md"></p>
      <p class="/modules/multi-arch-creating-podplacment-config.md"></p>
      <p class="/modules/multi-arch-creating-podplacment-config-using-cli.md"></p>
      <p class="/modules/multi-arch-creating-podplacment-config-using-web-console.md"></p>
      <p class="/modules/multi-arch-creating-namespace-podplacement-config.md"></p>
      <p class="/modules/multi-arch-deleting-podplacment-config-using-cli.md"></p>
      <p class="/modules/multi-arch-deleting-podplacment-config-using-web-console.md"></p>
      <p class="/modules/multi-arch-uninstalling-using-cli.md"></p>
      <p class="/modules/multi-arch-uninstalling-using-web-console.md"></p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
