---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: extensions/of-terms.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;

const headings = [{"depth":1,"slug":"of-terms","text":"Operator Framework glossary of common terms"},{"depth":2,"slug":"olm-common-terms-bundle_of-terms","text":"Bundle"},{"depth":2,"slug":"olm-common-terms-bundle-image_of-terms","text":"Bundle image"},{"depth":2,"slug":"olm-common-terms-catalogsource_of-terms","text":"Catalog source"},{"depth":2,"slug":"olm-common-terms-channel_of-terms","text":"Channel"},{"depth":2,"slug":"olm-common-terms-channel-head_of-terms","text":"Channel head"},{"depth":2,"slug":"olm-common-terms-csv_of-terms","text":"Cluster service version"},{"depth":2,"slug":"olm-common-terms-dependency_of-terms","text":"Dependency"},{"depth":2,"slug":"olm-common-terms-extension_of-terms","text":"Extension"},{"depth":2,"slug":"olm-common-terms-index-image_of-terms","text":"Index image"},{"depth":2,"slug":"olm-common-terms-installplan_of-terms","text":"Install plan"},{"depth":2,"slug":"olm-common-terms-multitenancy_of-terms","text":"Multitenancy"},{"depth":2,"slug":"olm-common-terms-operator_of-terms","text":"Operator"},{"depth":2,"slug":"olm-common-terms-operatorgroup_of-terms","text":"Operator group"},{"depth":2,"slug":"olm-common-terms-package_of-terms","text":"Package"},{"depth":2,"slug":"olm-common-terms-registry_of-terms","text":"Registry"},{"depth":2,"slug":"olm-common-terms-subscription_of-terms","text":"Subscription"},{"depth":2,"slug":"olm-common-terms-update-graph_of-terms","text":"Update graph"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/extensions/of-terms";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Operator Framework glossary of common terms";
const description = "Review the Operator Framework terminology, including Operator Lifecycle Manager (OLM) v1 terms, used throughout the documentation." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["extensions"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Operator Framework glossary of common terms {#of-terms}\n\nReview the Operator Framework terminology, including Operator Lifecycle Manager (OLM) v1 terms, used throughout the documentation.\n\n## Bundle {#olm-common-terms-bundle_of-terms}\n\nIn the bundle format, a *bundle* is a collection of an Operator CSV, manifests, and metadata. Together, they form a unique version of an Operator that can be installed onto the cluster.\n\n## Bundle image {#olm-common-terms-bundle-image_of-terms}\n\nIn the bundle format, a *bundle image* is a container image that is built from Operator manifests and that contains one bundle. Bundle images are stored and distributed by Open Container Initiative (OCI) spec container registries, such as Quay.io or DockerHub.\n\n## Catalog source {#olm-common-terms-catalogsource_of-terms}\n\nA *catalog source* represents a store of metadata that OLM can query to discover and install Operators and their dependencies.\n\n## Channel {#olm-common-terms-channel_of-terms}\n\nA *channel* defines a stream of updates for an Operator and is used to roll out updates for subscribers. The head points to the latest version of that channel. For example, a `stable` channel would have all stable versions of an Operator arranged from the earliest to the latest.\n\nAn Operator can have several channels, and a subscription binding to a certain channel would only look for updates in that channel.\n\n## Channel head {#olm-common-terms-channel-head_of-terms}\n\nA *channel head* refers to the latest known update in a particular channel.\n\n## Cluster service version {#olm-common-terms-csv_of-terms}\n\nA *cluster service version (CSV)* is a YAML manifest created from Operator metadata that assists OLM in running the Operator in a cluster. It is the metadata that accompanies an Operator container image, used to populate user interfaces with information such as its logo, description, and version.\n\nIt is also a source of technical information that is required to run the Operator, like the RBAC rules it requires and which custom resources (CRs) it manages or depends on.\n\n## Dependency {#olm-common-terms-dependency_of-terms}\n\nAn Operator may have a *dependency* on another Operator being present in the cluster. For example, the Vault Operator has a dependency on the etcd Operator for its data persistence layer.\n\nOLM resolves dependencies by ensuring that all specified versions of Operators and CRDs are installed on the cluster during the installation phase. This dependency is resolved by finding and installing an Operator in a catalog that satisfies the required CRD API, and is not related to packages or bundles.\n\n## Extension {#olm-common-terms-extension_of-terms}\n\nExtensions enable cluster administrators to extend capabilities for users on their OpenShift Container Platform cluster. Extensions are managed by Operator Lifecycle Manager (OLM) v1.\n\nThe `ClusterExtension` API streamlines management of installed extensions, which includes Operators via the `registry+v1` bundle format, by consolidating user-facing APIs into a single object. Administrators and SREs can use the API to automate processes and define desired states by using GitOps principles.\n\n## Index image {#olm-common-terms-index-image_of-terms}\n\nIn the bundle format, an *index image* refers to an image of a database (a database snapshot) that contains information about Operator bundles including CSVs and CRDs of all versions. This index can host a history of Operators on a cluster and be maintained by adding or removing Operators using the `opm` CLI tool.\n\n## Install plan {#olm-common-terms-installplan_of-terms}\n\nAn *install plan* is a calculated list of resources to be created to automatically install or upgrade a CSV.\n\n## Multitenancy {#olm-common-terms-multitenancy_of-terms}\n\nA *tenant* in OpenShift Container Platform is a user or group of users that share common access and privileges for a set of deployed workloads, typically represented by a namespace or project. You can use tenants to provide a level of isolation between different groups or teams.\n\nWhen a cluster is shared by multiple users or groups, it is considered a *multitenant* cluster.\n\n## Operator {#olm-common-terms-operator_of-terms}\n\nOperators are a method of packaging, deploying, and managing a Kubernetes application. A Kubernetes application is an app that is both deployed on Kubernetes and managed using the Kubernetes APIs and `kubectl` or `oc` tooling.\n\nIn Operator Lifecycle Manager (OLM) v1, the `ClusterExtension` API streamlines management of installed extensions, which includes Operators via the `registry+v1` bundle format.\n\n## Operator group {#olm-common-terms-operatorgroup_of-terms}\n\nAn *Operator group* configures all Operators deployed in the same namespace as the `OperatorGroup` object to watch for their CR in a list of namespaces or cluster-wide.\n\n## Package {#olm-common-terms-package_of-terms}\n\nIn the bundle format, a *package* is a directory that encloses all released history of an Operator with each version. A released version of an Operator is described in a CSV manifest alongside the CRDs.\n\n## Registry {#olm-common-terms-registry_of-terms}\n\nA *registry* is a database that stores bundle images of Operators, each with all of its latest and historical versions in all channels.\n\n## Subscription {#olm-common-terms-subscription_of-terms}\n\nA *subscription* keeps CSVs up to date by tracking a channel in a package.\n\n## Update graph {#olm-common-terms-update-graph_of-terms}\n\nAn *update graph* links versions of CSVs together, similar to the update graph of any other packaged software. Operators can be installed sequentially, or certain versions can be skipped. The update graph is expected to grow only at the head with newer versions being added.\n\nAlso known as *update edges* or *update paths*.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/extensions/of-terms.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="of-terms" class="group scroll-mt-20">Operator Framework glossary of common terms<a href="#of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Review the Operator Framework terminology, including Operator Lifecycle Manager (OLM) v1 terms, used throughout the documentation.</p>
      <h2 id="olm-common-terms-bundle_of-terms" class="group scroll-mt-20">Bundle<a href="#olm-common-terms-bundle_of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>In the bundle format, a <em>bundle</em> is a collection of an Operator CSV, manifests, and metadata. Together, they form a unique version of an Operator that can be installed onto the cluster.</p>
      <h2 id="olm-common-terms-bundle-image_of-terms" class="group scroll-mt-20">Bundle image<a href="#olm-common-terms-bundle-image_of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>In the bundle format, a <em>bundle image</em> is a container image that is built from Operator manifests and that contains one bundle. Bundle images are stored and distributed by Open Container Initiative (OCI) spec container registries, such as Quay.io or DockerHub.</p>
      <h2 id="olm-common-terms-catalogsource_of-terms" class="group scroll-mt-20">Catalog source<a href="#olm-common-terms-catalogsource_of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>A <em>catalog source</em> represents a store of metadata that OLM can query to discover and install Operators and their dependencies.</p>
      <h2 id="olm-common-terms-channel_of-terms" class="group scroll-mt-20">Channel<a href="#olm-common-terms-channel_of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>A <em>channel</em> defines a stream of updates for an Operator and is used to roll out updates for subscribers. The head points to the latest version of that channel. For example, a <code>stable</code> channel would have all stable versions of an Operator arranged from the earliest to the latest.</p>
      <p>An Operator can have several channels, and a subscription binding to a certain channel would only look for updates in that channel.</p>
      <h2 id="olm-common-terms-channel-head_of-terms" class="group scroll-mt-20">Channel head<a href="#olm-common-terms-channel-head_of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>A <em>channel head</em> refers to the latest known update in a particular channel.</p>
      <h2 id="olm-common-terms-csv_of-terms" class="group scroll-mt-20">Cluster service version<a href="#olm-common-terms-csv_of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>A <em>cluster service version (CSV)</em> is a YAML manifest created from Operator metadata that assists OLM in running the Operator in a cluster. It is the metadata that accompanies an Operator container image, used to populate user interfaces with information such as its logo, description, and version.</p>
      <p>It is also a source of technical information that is required to run the Operator, like the RBAC rules it requires and which custom resources (CRs) it manages or depends on.</p>
      <h2 id="olm-common-terms-dependency_of-terms" class="group scroll-mt-20">Dependency<a href="#olm-common-terms-dependency_of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>An Operator may have a <em>dependency</em> on another Operator being present in the cluster. For example, the Vault Operator has a dependency on the etcd Operator for its data persistence layer.</p>
      <p>OLM resolves dependencies by ensuring that all specified versions of Operators and CRDs are installed on the cluster during the installation phase. This dependency is resolved by finding and installing an Operator in a catalog that satisfies the required CRD API, and is not related to packages or bundles.</p>
      <h2 id="olm-common-terms-extension_of-terms" class="group scroll-mt-20">Extension<a href="#olm-common-terms-extension_of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Extensions enable cluster administrators to extend capabilities for users on their OpenShift Container Platform cluster. Extensions are managed by Operator Lifecycle Manager (OLM) v1.</p>
      <p>The <code>ClusterExtension</code> API streamlines management of installed extensions, which includes Operators via the <code>registry+v1</code> bundle format, by consolidating user-facing APIs into a single object. Administrators and SREs can use the API to automate processes and define desired states by using GitOps principles.</p>
      <h2 id="olm-common-terms-index-image_of-terms" class="group scroll-mt-20">Index image<a href="#olm-common-terms-index-image_of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>In the bundle format, an <em>index image</em> refers to an image of a database (a database snapshot) that contains information about Operator bundles including CSVs and CRDs of all versions. This index can host a history of Operators on a cluster and be maintained by adding or removing Operators using the <code>opm</code> CLI tool.</p>
      <h2 id="olm-common-terms-installplan_of-terms" class="group scroll-mt-20">Install plan<a href="#olm-common-terms-installplan_of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>An <em>install plan</em> is a calculated list of resources to be created to automatically install or upgrade a CSV.</p>
      <h2 id="olm-common-terms-multitenancy_of-terms" class="group scroll-mt-20">Multitenancy<a href="#olm-common-terms-multitenancy_of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>A <em>tenant</em> in OpenShift Container Platform is a user or group of users that share common access and privileges for a set of deployed workloads, typically represented by a namespace or project. You can use tenants to provide a level of isolation between different groups or teams.</p>
      <p>When a cluster is shared by multiple users or groups, it is considered a <em>multitenant</em> cluster.</p>
      <h2 id="olm-common-terms-operator_of-terms" class="group scroll-mt-20">Operator<a href="#olm-common-terms-operator_of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Operators are a method of packaging, deploying, and managing a Kubernetes application. A Kubernetes application is an app that is both deployed on Kubernetes and managed using the Kubernetes APIs and <code>kubectl</code> or <code>oc</code> tooling.</p>
      <p>In Operator Lifecycle Manager (OLM) v1, the <code>ClusterExtension</code> API streamlines management of installed extensions, which includes Operators via the <code>registry+v1</code> bundle format.</p>
      <h2 id="olm-common-terms-operatorgroup_of-terms" class="group scroll-mt-20">Operator group<a href="#olm-common-terms-operatorgroup_of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>An <em>Operator group</em> configures all Operators deployed in the same namespace as the <code>OperatorGroup</code> object to watch for their CR in a list of namespaces or cluster-wide.</p>
      <h2 id="olm-common-terms-package_of-terms" class="group scroll-mt-20">Package<a href="#olm-common-terms-package_of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>In the bundle format, a <em>package</em> is a directory that encloses all released history of an Operator with each version. A released version of an Operator is described in a CSV manifest alongside the CRDs.</p>
      <h2 id="olm-common-terms-registry_of-terms" class="group scroll-mt-20">Registry<a href="#olm-common-terms-registry_of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>A <em>registry</em> is a database that stores bundle images of Operators, each with all of its latest and historical versions in all channels.</p>
      <h2 id="olm-common-terms-subscription_of-terms" class="group scroll-mt-20">Subscription<a href="#olm-common-terms-subscription_of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>A <em>subscription</em> keeps CSVs up to date by tracking a channel in a package.</p>
      <h2 id="olm-common-terms-update-graph_of-terms" class="group scroll-mt-20">Update graph<a href="#olm-common-terms-update-graph_of-terms" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>An <em>update graph</em> links versions of CSVs together, similar to the update graph of any other packaged software. Operators can be installed sequentially, or certain versions can be skipped. The update graph is expected to grow only at the head with newer versions being added.</p>
      <p>Also known as <em>update edges</em> or <em>update paths</em>.</p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
