---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: registry/configuring_registry_storage/configuring-registry-storage-gcp-user-infrastructure.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"configuring-registry-storage-gcp-user-infrastructure","text":"Configuring the registry for Google Cloud user-provisioned infrastructure"},{"depth":2,"slug":"registry-operator-config-resources-secret-gcp_configuring-registry-storage-gcp-user-infrastructure","text":"Configuring a secret for the Image Registry Operator"},{"depth":2,"slug":"registry-configuring-storage-gcp-user-infra_configuring-registry-storage-gcp-user-infrastructure","text":"Configuring the registry storage for Google Cloud with user-provisioned infrastructure"},{"depth":2,"slug":"registry-operator-configuration-resource-overview-gcp-gcs_configuring-registry-storage-gcp-user-infrastructure","text":"Image Registry Operator configuration parameters for Google Cloud GCS"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/registry/configuring_registry_storage/configuring-registry-storage-gcp-user-infrastructure";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring the registry for Google Cloud user-provisioned infrastructure";
const description = "Save your container images to a durable storage location by configuring the built-in image registry to use dedicated Google Cloud storage. This setup…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["registry","registry/configuring_registry_storage"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring the registry for Google Cloud user-provisioned infrastructure {#configuring-registry-storage-gcp-user-infrastructure}\n\nSave your container images to a durable storage location by configuring the built-in image registry to use dedicated Google Cloud storage. This setup provides persistent scalable storage for your registry, separate from ephemeral cluster storage.\n\n## Configuring a secret for the Image Registry Operator {#registry-operator-config-resources-secret-gcp_configuring-registry-storage-gcp-user-infrastructure}\n\nIn addition to the `configs.imageregistry.operator.openshift.io` and ConfigMap resources, configuration is provided to the Operator by a separate secret resource located within the `openshift-image-registry` namespace.\n\nThe `image-registry-private-configuration-user` secret provides credentials needed for storage access and management. It overrides the default credentials used by the Operator, if default credentials were found.\n\nFor GCS on Google Cloud storage, the secret is expected to contain one key whose value is the contents of a credentials file provided by Google Cloud:\n\n- `REGISTRY_STORAGE_GCS_KEYFILE`\n\n**Procedure**\n\n- Create an OpenShift Container Platform secret that contains the required keys.\n\n  ```terminal\n  $ oc create secret generic image-registry-private-configuration-user --from-file=REGISTRY_STORAGE_GCS_KEYFILE=<path_to_keyfile> --namespace openshift-image-registry\n  ```\n\n## Configuring the registry storage for Google Cloud with user-provisioned infrastructure {#registry-configuring-storage-gcp-user-infra_configuring-registry-storage-gcp-user-infrastructure}\n\nIf the Registry Operator cannot create a Google Cloud bucket, you must set up the storage medium manually and configure the settings in the registry custom resource (CR).\n\n**Prerequisites**\n\n- A cluster on Google Cloud with user-provisioned infrastructure.\n- To configure registry storage for Google Cloud, you need to provide Registry Operator cloud credentials.\n- For GCS on Google Cloud storage, the secret is expected to contain one key whose value is the contents of a credentials file provided by Google Cloud:\n\n  - `REGISTRY_STORAGE_GCS_KEYFILE`\n\n  > [!WARNING]\n  > You can secure your registry images that use a Google Cloud Storage bucket by setting [public access prevention](https://cloud.google.com/storage/docs/using-public-access-prevention).\n\n**Procedure**\n\n1. Set up an [Object Lifecycle Management policy](https://cloud.google.com/storage/docs/lifecycle) to abort incomplete multipart uploads that are one day old.\n2. Fill in the storage configuration in `configs.imageregistry.operator.openshift.io/cluster`:\n\n   ```terminal\n   $ oc edit configs.imageregistry.operator.openshift.io/cluster\n   ```\n\n   ```yaml {title=\"Example configuration\"}\n   apiVersion: imageregistry.operator.openshift.io/v1\n   kind: Config\n   metadata:\n     name: cluster\n   spec:\n     storage:\n       gcs:\n         bucket: <bucket_name>\n         projectID: <project_id>\n         region: <region_name>\n   ```\n\n## Image Registry Operator configuration parameters for Google Cloud GCS {#registry-operator-configuration-resource-overview-gcp-gcs_configuring-registry-storage-gcp-user-infrastructure}\n\nThe following parameters configure are available to configure your Google Cloud GCS registry storage.\n\n<table>\n<thead>\n<tr>\n  <th>Parameter</th>\n  <th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td><code>bucket</code></td>\n  <td>Bucket is the bucket name in which you want to store the registry's data. It is optional and is generated if not provided.</td>\n</tr>\n<tr>\n  <td><code>region</code></td>\n  <td>Region is the GCS location in which your bucket exists. It is optional and is set based on the installed GCS Region.</td>\n</tr>\n<tr>\n  <td><code>projectID</code></td>\n  <td>ProjectID is the Project ID of the Google Cloud project that this bucket should be associated with. It is optional.</td>\n</tr>\n<tr>\n  <td><code>keyID</code></td>\n  <td>KeyID is the KMS key ID to use for encryption. It is optional because buckets are encrypted by default on Google Cloud. This allows for the use of a custom encryption key.</td>\n</tr>\n</tbody>\n</table>\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/registry/configuring_registry_storage/configuring-registry-storage-gcp-user-infrastructure.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="configuring-registry-storage-gcp-user-infrastructure" class="group scroll-mt-20">Configuring the registry for Google Cloud user-provisioned infrastructure<a href="#configuring-registry-storage-gcp-user-infrastructure" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Save your container images to a durable storage location by configuring the built-in image registry to use dedicated Google Cloud storage. This setup provides persistent scalable storage for your registry, separate from ephemeral cluster storage.</p>
      <h2 id="registry-operator-config-resources-secret-gcp_configuring-registry-storage-gcp-user-infrastructure" class="group scroll-mt-20">Configuring a secret for the Image Registry Operator<a href="#registry-operator-config-resources-secret-gcp_configuring-registry-storage-gcp-user-infrastructure" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">In addition to the <code>configs.imageregistry.operator.openshift.io</code> and ConfigMap resources, configuration is provided to the Operator by a separate secret resource located within the <code>openshift-image-registry</code> namespace.</p>
      <p>The <code>image-registry-private-configuration-user</code> secret provides credentials needed for storage access and management. It overrides the default credentials used by the Operator, if default credentials were found.</p>
      <p>For GCS on Google Cloud storage, the secret is expected to contain one key whose value is the contents of a credentials file provided by Google Cloud:</p>
      <ul><li><code>REGISTRY_STORAGE_GCS_KEYFILE</code></li></ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Create an OpenShift Container Platform secret that contains the required keys.
      <CodeBlock code={"$ oc create secret generic image-registry-private-configuration-user --from-file=REGISTRY_STORAGE_GCS_KEYFILE=<path_to_keyfile> --namespace openshift-image-registry"} lang="terminal" />
      </li>
      </ul>
      <h2 id="registry-configuring-storage-gcp-user-infra_configuring-registry-storage-gcp-user-infrastructure" class="group scroll-mt-20">Configuring the registry storage for Google Cloud with user-provisioned infrastructure<a href="#registry-configuring-storage-gcp-user-infra_configuring-registry-storage-gcp-user-infrastructure" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">If the Registry Operator cannot create a Google Cloud bucket, you must set up the storage medium manually and configure the settings in the registry custom resource (CR).</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>A cluster on Google Cloud with user-provisioned infrastructure.</li>
      <li>To configure registry storage for Google Cloud, you need to provide Registry Operator cloud credentials.</li>
      <li>
      For GCS on Google Cloud storage, the secret is expected to contain one key whose value is the contents of a credentials file provided by Google Cloud:
      <ul><li><code>REGISTRY_STORAGE_GCS_KEYFILE</code></li></ul>
      <Alert variant="warning" class="my-4">
        <AlertTitle>Warning</AlertTitle>
        <AlertDescription>
          <p>You can secure your registry images that use a Google Cloud Storage bucket by setting <a href="https://cloud.google.com/storage/docs/using-public-access-prevention">public access prevention</a>.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Set up an <a href="https://cloud.google.com/storage/docs/lifecycle">Object Lifecycle Management policy</a> to abort incomplete multipart uploads that are one day old.</li>
      <li>
      Fill in the storage configuration in <code>configs.imageregistry.operator.openshift.io/cluster</code>:
      <CodeBlock code={"$ oc edit configs.imageregistry.operator.openshift.io/cluster"} lang="terminal" />
      <CodeBlock code={"apiVersion: imageregistry.operator.openshift.io/v1\nkind: Config\nmetadata:\n  name: cluster\nspec:\n  storage:\n    gcs:\n      bucket: <bucket_name>\n      projectID: <project_id>\n      region: <region_name>"} lang="yaml" title="Example configuration" />
      </li>
      </ol>
      <h2 id="registry-operator-configuration-resource-overview-gcp-gcs_configuring-registry-storage-gcp-user-infrastructure" class="group scroll-mt-20">Image Registry Operator configuration parameters for Google Cloud GCS<a href="#registry-operator-configuration-resource-overview-gcp-gcs_configuring-registry-storage-gcp-user-infrastructure" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The following parameters configure are available to configure your Google Cloud GCS registry storage.</p>
      <Fragment set:html={"<table>\n<thead>\n<tr>\n  <th>Parameter</th>\n  <th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td><code>bucket</code></td>\n  <td>Bucket is the bucket name in which you want to store the registry's data. It is optional and is generated if not provided.</td>\n</tr>\n<tr>\n  <td><code>region</code></td>\n  <td>Region is the GCS location in which your bucket exists. It is optional and is set based on the installed GCS Region.</td>\n</tr>\n<tr>\n  <td><code>projectID</code></td>\n  <td>ProjectID is the Project ID of the Google Cloud project that this bucket should be associated with. It is optional.</td>\n</tr>\n<tr>\n  <td><code>keyID</code></td>\n  <td>KeyID is the KMS key ID to use for encryption. It is optional because buckets are encrypted by default on Google Cloud. This allows for the use of a custom encryption key.</td>\n</tr>\n</tbody>\n</table>"} />
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
