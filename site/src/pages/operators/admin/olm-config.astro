---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: operators/admin/olm-config.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"olm-config","text":"Configuring Operator Lifecycle Manager features"},{"depth":2,"slug":"olm-disabling-copied-csvs_olm-config","text":"Disabling copied CSVs"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/operators/admin/olm-config";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring Operator Lifecycle Manager features";
const description = "Cluster administrators can enable or disable Operator Lifecycle Manager (OLM) cluster features by editing the OLMConfig custom resource (CR) named cluster…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["operators","operators/admin"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring Operator Lifecycle Manager features {#olm-config}\n\nCluster administrators can enable or disable Operator Lifecycle Manager (OLM) cluster features by editing the `OLMConfig` custom resource (CR) named `cluster` in OpenShift Container Platform.\n\n## Disabling copied CSVs {#olm-disabling-copied-csvs_olm-config}\n\nTo reduce memory, etcd, and network usage on large OpenShift Container Platform clusters, you can disable copied cluster service versions (CSVs) for Operators installed in `AllNamespaces` mode through the cluster `OLMConfig`.\n\nWhen an Operator is installed by Operator Lifecycle Manager (OLM), a simplified copy of its cluster service version (CSV) is created by default in every namespace that the Operator is configured to watch. These CSVs are known as *copied CSVs* and communicate to users which controllers are actively reconciling resource events in a given namespace.\n\nWhen an Operator is configured to use the `AllNamespaces` install mode, versus targeting a single or specified set of namespaces, a copied CSV for the Operator is created in every namespace on the cluster. On especially large clusters, with namespaces and installed Operators potentially in the hundreds or thousands, copied CSVs consume an untenable amount of resources, such as OLM’s memory usage, cluster etcd limits, and networking.\n\n> [!NOTE]\n> If you disable copied CSVs, an Operator installed in `AllNamespaces` mode has their CSV copied only to the `openshift` namespace, instead of every namespace on the cluster. In disabled copied CSVs mode, the behavior differs between the web console and CLI:\n>\n> - In the web console, the default behavior is modified to show copied CSVs from the `openshift` namespace in every namespace, even though the CSVs are not actually copied to every namespace. This allows regular users to still be able to view the details of these Operators in their namespaces and create related custom resources (CRs).\n> - In the OpenShift CLI (`oc`), regular users can view Operators installed directly in their namespaces by using the `oc get csvs` command, but the copied CSVs from the `openshift` namespace are not visible in their namespaces. Operators affected by this limitation are still available and continue to reconcile events in the user’s namespace.\n>\n>   To view a full list of installed global Operators, similar to the web console behavior, all authenticated users can run the following command:\n>\n>   ```terminal\n>   $ oc get csvs -n openshift\n>   ```\n\n**Procedure**\n\n- Edit the `OLMConfig` object named `cluster` and set the `spec.features.disableCopiedCSVs` field to `true`:\n\n  ```terminal\n  $ oc apply -f - <<EOF\n  apiVersion: operators.coreos.com/v1\n  kind: OLMConfig\n  metadata:\n    name: cluster\n  spec:\n    features:\n      disableCopiedCSVs: true (1)\n  EOF\n  ```\n\n  1. Disabled copied CSVs for `AllNamespaces` install mode Operators\n\n**Verification**\n\n- When copied CSVs are disabled, OLM captures this information in an event in the Operator’s namespace:\n\n  ```terminal\n  $ oc get events\n  ```\n\n  ```terminal {title=\"Example output\"}\n  LAST SEEN   TYPE      REASON               OBJECT                                MESSAGE\n  85s         Warning   DisabledCopiedCSVs   clusterserviceversion/my-csv.v1.0.0   CSV copying disabled for operators/my-csv.v1.0.0\n  ```\n\n  When the `spec.features.disableCopiedCSVs` field is missing or set to `false`, OLM recreates the copied CSVs for all Operators installed with the `AllNamespaces` mode and deletes the previously mentioned events.\n\n**Additional resources**\n\n- [Install modes](/openshift-docs-markdown/operators/understanding/olm/olm-understanding-operatorgroups#olm-operatorgroups-membership_olm-understanding-operatorgroups)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/operators/admin/olm-config.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="olm-config" class="group scroll-mt-20">Configuring Operator Lifecycle Manager features<a href="#olm-config" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Cluster administrators can enable or disable Operator Lifecycle Manager (OLM) cluster features by editing the <code>OLMConfig</code> custom resource (CR) named <code>cluster</code> in OpenShift Container Platform.</p>
      <h2 id="olm-disabling-copied-csvs_olm-config" class="group scroll-mt-20">Disabling copied CSVs<a href="#olm-disabling-copied-csvs_olm-config" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To reduce memory, etcd, and network usage on large OpenShift Container Platform clusters, you can disable copied cluster service versions (CSVs) for Operators installed in <code>AllNamespaces</code> mode through the cluster <code>OLMConfig</code>.</p>
      <p>When an Operator is installed by Operator Lifecycle Manager (OLM), a simplified copy of its cluster service version (CSV) is created by default in every namespace that the Operator is configured to watch. These CSVs are known as <em>copied CSVs</em> and communicate to users which controllers are actively reconciling resource events in a given namespace.</p>
      <p>When an Operator is configured to use the <code>AllNamespaces</code> install mode, versus targeting a single or specified set of namespaces, a copied CSV for the Operator is created in every namespace on the cluster. On especially large clusters, with namespaces and installed Operators potentially in the hundreds or thousands, copied CSVs consume an untenable amount of resources, such as OLM’s memory usage, cluster etcd limits, and networking.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If you disable copied CSVs, an Operator installed in <code>AllNamespaces</code> mode has their CSV copied only to the <code>openshift</code> namespace, instead of every namespace on the cluster. In disabled copied CSVs mode, the behavior differs between the web console and CLI:</p>
          <ul>
          <li>In the web console, the default behavior is modified to show copied CSVs from the <code>openshift</code> namespace in every namespace, even though the CSVs are not actually copied to every namespace. This allows regular users to still be able to view the details of these Operators in their namespaces and create related custom resources (CRs).</li>
          <li>
          In the OpenShift CLI (<code>oc</code>), regular users can view Operators installed directly in their namespaces by using the <code>oc get csvs</code> command, but the copied CSVs from the <code>openshift</code> namespace are not visible in their namespaces. Operators affected by this limitation are still available and continue to reconcile events in the user’s namespace.
          <p>To view a full list of installed global Operators, similar to the web console behavior, all authenticated users can run the following command:</p>
          <CodeBlock code={"$ oc get csvs -n openshift"} lang="terminal" />
          </li>
          </ul>
        </AlertDescription>
      </Alert>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Edit the <code>OLMConfig</code> object named <code>cluster</code> and set the <code>spec.features.disableCopiedCSVs</code> field to <code>true</code>:
      <CodeBlock code={"$ oc apply -f - <<EOF\napiVersion: operators.coreos.com/v1\nkind: OLMConfig\nmetadata:\n  name: cluster\nspec:\n  features:\n    disableCopiedCSVs: true (1)\nEOF"} lang="terminal" />
      <ol><li>Disabled copied CSVs for <code>AllNamespaces</code> install mode Operators</li></ol>
      </li>
      </ul>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      When copied CSVs are disabled, OLM captures this information in an event in the Operator’s namespace:
      <CodeBlock code={"$ oc get events"} lang="terminal" />
      <CodeBlock code={"LAST SEEN   TYPE      REASON               OBJECT                                MESSAGE\n85s         Warning   DisabledCopiedCSVs   clusterserviceversion/my-csv.v1.0.0   CSV copying disabled for operators/my-csv.v1.0.0"} lang="terminal" title="Example output" />
      <p>When the <code>spec.features.disableCopiedCSVs</code> field is missing or set to <code>false</code>, OLM recreates the copied CSVs for all Operators installed with the <code>AllNamespaces</code> mode and deletes the previously mentioned events.</p>
      </li>
      </ul>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/operators/understanding/olm/olm-understanding-operatorgroups#olm-operatorgroups-membership_olm-understanding-operatorgroups">Install modes</a></li></ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
