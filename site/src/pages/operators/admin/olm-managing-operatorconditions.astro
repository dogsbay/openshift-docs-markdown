---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: operators/admin/olm-managing-operatorconditions.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"olm-managing-operatorconditions","text":"Managing Operator conditions"},{"depth":2,"slug":"olm-supported-operatorconditions_olm-managing-operatorconditions","text":"Overriding Operator conditions"},{"depth":2,"slug":"olm-updating-use-operatorconditions_olm-managing-operatorconditions","text":"Updating your Operator to use Operator conditions"},{"depth":3,"slug":"olm-updating-use-operatorconditions-defaults_olm-managing-operatorconditions","text":"Setting defaults"},{"depth":2,"slug":"olm-managing-operatorconditions-addtl-resources","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/operators/admin/olm-managing-operatorconditions";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Managing Operator conditions";
const description = "You can manage Operator conditions in OpenShift Container Platform by using Operator Lifecycle Manager (OLM)." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["operators","operators/admin"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Managing Operator conditions {#olm-managing-operatorconditions}\n\nYou can manage Operator conditions in OpenShift Container Platform by using Operator Lifecycle Manager (OLM).\n\n## Overriding Operator conditions {#olm-supported-operatorconditions_olm-managing-operatorconditions}\n\nAs a cluster administrator,\n\nyou might want to ignore a supported Operator condition reported by an Operator. When present, Operator conditions in the `Spec.Overrides` array override the conditions in the `Spec.Conditions` array, allowing cluster administrators\n\nto deal with situations where an Operator is incorrectly reporting a state to Operator Lifecycle Manager (OLM).\n\n> [!NOTE]\n> By default, the `Spec.Overrides` array is not present in an `OperatorCondition` object until it is added by a cluster administrator\n>\n> . The `Spec.Conditions` array is also not present until it is either added by a user or as a result of custom Operator logic.\n\nFor example, consider a known version of an Operator that always communicates that it is not upgradeable. In this instance, you might want to upgrade the Operator despite the Operator communicating that it is not upgradeable. This could be accomplished by overriding the Operator condition by adding the condition `type` and `status` to the `Spec.Overrides` array in the `OperatorCondition` object.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- An Operator with an `OperatorCondition` object, installed using OLM.\n\n**Procedure**\n\n1. Edit the `OperatorCondition` object for the Operator:\n\n   ```terminal\n   $ oc edit operatorcondition <name>\n   ```\n2. Add a `Spec.Overrides` array to the object:\n\n   ```yaml {title=\"Example Operator condition override\"}\n   apiVersion: operators.coreos.com/v2\n   kind: OperatorCondition\n   metadata:\n     name: my-operator\n     namespace: operators\n   spec:\n     overrides:\n     - type: Upgradeable\n       status: \"True\"\n       reason: \"upgradeIsSafe\"\n       message: \"This is a known issue with the Operator where it always reports that it cannot be upgraded.\"\n     conditions:\n     - type: Upgradeable\n       status: \"False\"\n       reason: \"migration\"\n       message: \"The operator is performing a migration.\"\n       lastTransitionTime: \"2020-08-24T23:15:55Z\"\n   ```\n\n   Setting the 'type' field to `Upgradeable` allows the cluster administrator to change the upgrade readiness to `True`.\n\n## Updating your Operator to use Operator conditions {#olm-updating-use-operatorconditions_olm-managing-operatorconditions}\n\nOperator Lifecycle Manager (OLM) automatically creates an `OperatorCondition` resource for each `ClusterServiceVersion` resource that it reconciles. All service accounts in the CSV are granted the RBAC to interact with the `OperatorCondition` owned by the Operator.\n\nAn Operator author can develop their Operator to use the `operator-lib` library such that, after the Operator has been deployed by OLM, it can set its own conditions. For more resources about setting Operator conditions as an Operator author, see the [Enabling Operator conditions](https://docs.openshift.com/container-platform/4.12/operators/operator_sdk/osdk-generating-csvs.html#osdk-operatorconditions_osdk-generating-csvs) page.\n\n### Setting defaults {#olm-updating-use-operatorconditions-defaults_olm-managing-operatorconditions}\n\nIn an effort to remain backwards compatible, OLM treats the absence of an `OperatorCondition` resource as opting out of the condition. Therefore, an Operator that opts in to using Operator conditions should set default conditions before the ready probe for the pod is set to `true`. This provides the Operator with a grace period to update the condition to the correct state.\n\n## Additional resources {#olm-managing-operatorconditions-addtl-resources}\n\n- [Operator conditions](/openshift-docs-markdown/operators/understanding/olm/olm-operatorconditions#olm-operatorconditions)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/operators/admin/olm-managing-operatorconditions.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="olm-managing-operatorconditions" class="group scroll-mt-20">Managing Operator conditions<a href="#olm-managing-operatorconditions" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can manage Operator conditions in OpenShift Container Platform by using Operator Lifecycle Manager (OLM).</p>
      <h2 id="olm-supported-operatorconditions_olm-managing-operatorconditions" class="group scroll-mt-20">Overriding Operator conditions<a href="#olm-supported-operatorconditions_olm-managing-operatorconditions" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>As a cluster administrator,</p>
      <p>you might want to ignore a supported Operator condition reported by an Operator. When present, Operator conditions in the <code>Spec.Overrides</code> array override the conditions in the <code>Spec.Conditions</code> array, allowing cluster administrators</p>
      <p>to deal with situations where an Operator is incorrectly reporting a state to Operator Lifecycle Manager (OLM).</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>By default, the <code>Spec.Overrides</code> array is not present in an <code>OperatorCondition</code> object until it is added by a cluster administrator</p>
          <p>. The <code>Spec.Conditions</code> array is also not present until it is either added by a user or as a result of custom Operator logic.</p>
        </AlertDescription>
      </Alert>
      <p>For example, consider a known version of an Operator that always communicates that it is not upgradeable. In this instance, you might want to upgrade the Operator despite the Operator communicating that it is not upgradeable. This could be accomplished by overriding the Operator condition by adding the condition <code>type</code> and <code>status</code> to the <code>Spec.Overrides</code> array in the <code>OperatorCondition</code> object.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>An Operator with an <code>OperatorCondition</code> object, installed using OLM.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Edit the <code>OperatorCondition</code> object for the Operator:
      <CodeBlock code={"$ oc edit operatorcondition <name>"} lang="terminal" />
      </li>
      <li>
      Add a <code>Spec.Overrides</code> array to the object:
      <CodeBlock code={"apiVersion: operators.coreos.com/v2\nkind: OperatorCondition\nmetadata:\n  name: my-operator\n  namespace: operators\nspec:\n  overrides:\n  - type: Upgradeable\n    status: \"True\"\n    reason: \"upgradeIsSafe\"\n    message: \"This is a known issue with the Operator where it always reports that it cannot be upgraded.\"\n  conditions:\n  - type: Upgradeable\n    status: \"False\"\n    reason: \"migration\"\n    message: \"The operator is performing a migration.\"\n    lastTransitionTime: \"2020-08-24T23:15:55Z\""} lang="yaml" title="Example Operator condition override" />
      <p>Setting the 'type' field to <code>Upgradeable</code> allows the cluster administrator to change the upgrade readiness to <code>True</code>.</p>
      </li>
      </ol>
      <h2 id="olm-updating-use-operatorconditions_olm-managing-operatorconditions" class="group scroll-mt-20">Updating your Operator to use Operator conditions<a href="#olm-updating-use-operatorconditions_olm-managing-operatorconditions" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Operator Lifecycle Manager (OLM) automatically creates an <code>OperatorCondition</code> resource for each <code>ClusterServiceVersion</code> resource that it reconciles. All service accounts in the CSV are granted the RBAC to interact with the <code>OperatorCondition</code> owned by the Operator.</p>
      <p>An Operator author can develop their Operator to use the <code>operator-lib</code> library such that, after the Operator has been deployed by OLM, it can set its own conditions. For more resources about setting Operator conditions as an Operator author, see the <a href="https://docs.openshift.com/container-platform/4.12/operators/operator_sdk/osdk-generating-csvs.html#osdk-operatorconditions_osdk-generating-csvs">Enabling Operator conditions</a> page.</p>
      <h3 id="olm-updating-use-operatorconditions-defaults_olm-managing-operatorconditions" class="group scroll-mt-20">Setting defaults<a href="#olm-updating-use-operatorconditions-defaults_olm-managing-operatorconditions" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>In an effort to remain backwards compatible, OLM treats the absence of an <code>OperatorCondition</code> resource as opting out of the condition. Therefore, an Operator that opts in to using Operator conditions should set default conditions before the ready probe for the pod is set to <code>true</code>. This provides the Operator with a grace period to update the condition to the correct state.</p>
      <h2 id="olm-managing-operatorconditions-addtl-resources" class="group scroll-mt-20">Additional resources<a href="#olm-managing-operatorconditions-addtl-resources" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul><li><a href="/openshift-docs-markdown/operators/understanding/olm/olm-operatorconditions#olm-operatorconditions">Operator conditions</a></li></ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
