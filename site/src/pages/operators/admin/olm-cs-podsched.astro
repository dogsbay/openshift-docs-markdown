---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: operators/admin/olm-cs-podsched.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"olm-cs-podsched","text":"Catalog source pod scheduling"},{"depth":2,"slug":"disabling-catalogsource-objects_olm-cs-podsched","text":"Disabling default CatalogSource objects at a local level"},{"depth":2,"slug":"olm-node-selector_olm-cs-podsched","text":"Overriding the node selector for catalog source pods"},{"depth":2,"slug":"olm-priority-class-name_olm-cs-podsched","text":"Overriding the priority class name for catalog source pods"},{"depth":2,"slug":"olm-tolerations_olm-cs-podsched","text":"Overriding tolerations for catalog source pods"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/operators/admin/olm-cs-podsched";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Catalog source pod scheduling";
const description = "When a grpc type catalog source defines the spec.image field, the Catalog Operator creates a pod to serve that image." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["operators","operators/admin"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Catalog source pod scheduling {#olm-cs-podsched}\n\nWhen a `grpc` type catalog source defines the `spec.image` field, the Catalog Operator creates a pod to serve that image.\n\nBy default, the pod specification configures the following default settings:\n\n- Node selector: `kubernetes.io/os=linux`\n- Priority class name: `system-cluster-critical`\n- Tolerations: None\n\nAs an administrator, you can override these defaults by configuring fields in the optional `spec.grpcPodConfig` section of the `CatalogSource` object.\n\n> [!IMPORTANT]\n> The Marketplace Operator, `openshift-marketplace`, manages the default `OperatorHub` custom resource’s (CR). This CR manages `CatalogSource` objects. If you attempt to modify fields in the `CatalogSource` object’s `spec.grpcPodConfig` section, the Marketplace Operator automatically reverts these modifications. By default, if you modify fields in the `spec.grpcPodConfig` section of the   `CatalogSource` object, the Marketplace Operator automatically reverts these changes.\n>\n> To apply persistent changes to `CatalogSource` object, you must first disable a default `CatalogSource` object.\n\n**Additional resources**\n\n- [OLM concepts and resources -> Catalog source](/openshift-docs-markdown/operators/understanding/olm/olm-understanding-olm#olm-catalogsource_olm-understanding-olm)\n\n## Disabling default CatalogSource objects at a local level {#disabling-catalogsource-objects_olm-cs-podsched}\n\nYou can make persistent local changes to a `CatalogSource` object by disabling the default `CatalogSource` object. Otherwise, the Marketplace Operator automatically reverts any manual modifications to fields in the `spec.grpcPodConfig` section.\n\nThe Marketplace Operator, `openshift-marketplace`, manages the default custom resources (CRs) of the `OperatorHub`. The `OperatorHub` manages `CatalogSource` objects.\n\nTo apply persistent changes to `CatalogSource` object, you must first disable a default `CatalogSource` object.\n\n**Procedure**\n\n- To disable all the default `CatalogSource` objects at a local level, enter the following command:\n\n  ```terminal\n  $ oc patch operatorhub cluster -p '{\"spec\": {\"disableAllDefaultSources\": true}}' --type=merge\n  ```\n\n  > [!NOTE]\n  > You can also configure the default `OperatorHub` CR to either disable all `CatalogSource` objects or disable a specific object.\n\n**Additional resources**\n\n- [OperatorHub custom resource](/openshift-docs-markdown/operators/understanding/olm-understanding-software-catalog#olm-software-catalog-arch-operatorhub-crd_olm-understanding-software-catalog)\n- [Disabling the default OperatorHub catalog sources](/openshift-docs-markdown/disconnected/using-olm#olm-restricted-networks-operatorhub_olm-restricted-networks)\n\n## Overriding the node selector for catalog source pods {#olm-node-selector_olm-cs-podsched}\n\nTo control which nodes run catalog source pods, you can override the default node selector in the `spec.grpcPodConfig` section of the `CatalogSource` object.\n\n**Prerequisites**\n\n- A `CatalogSource` object of source type `grpc` with `spec.image` is defined.\n\n**Procedure**\n\n- Edit the `CatalogSource` object and add or modify the `spec.grpcPodConfig` section to include the following:\n\n  ```yaml\n    grpcPodConfig:\n      nodeSelector:\n        custom_label: <label>\n  ```\n\n  where `<label>` is the label for the node selector that you want catalog source pods to use for scheduling.\n\n**Additional resources**\n\n- [Placing pods on specific nodes using node selectors](/openshift-docs-markdown/nodes/scheduling/nodes-scheduler-node-selectors#nodes-scheduler-node-selectors)\n\n## Overriding the priority class name for catalog source pods {#olm-priority-class-name_olm-cs-podsched}\n\nTo control the scheduling priority of catalog source pods, you can override the default priority class name in the `spec.grpcPodConfig` section of the `CatalogSource` object.\n\n**Prerequisites**\n\n- A `CatalogSource` object of source type `grpc` with a defined `spec.image`.\n\n**Procedure**\n\n- Edit the `CatalogSource` object and configure the `spec.grpcPodConfig` section, similar to the following example:\n\n  ```yaml\n    grpcPodConfig:\n      priorityClassName: <priority_class>\n  ```\n\n  where:\n\n  `<priority_class>`\n  :   Specifies one of the following priority classes: \\*   A default Kubernetes priority class, such as `system-cluster-critical` or `system-node-critical` \\*   An empty string (`\"\"`) to assign the default priority \\*   A custom, pre-existing priority class name\n\n  > [!NOTE]\n  > Previously, the only pod scheduling parameter that could be overriden was `priorityClassName`. This was done by adding the `operatorframework.io/priorityclass` annotation to the `CatalogSource` object. For example:\n  >\n  > ```yaml\n  > apiVersion: operators.coreos.com/v1alpha1\n  > kind: CatalogSource\n  > metadata:\n  >   name: example-catalog\n  >   namespace: {{ global_ns }}\n  >   annotations:\n  >     operatorframework.io/priorityclass: system-cluster-critical\n  > ```\n  >\n  > If a `CatalogSource` object defines both the annotation and `spec.grpcPodConfig.priorityClassName`, the annotation takes precedence over the configuration parameter.\n\n**Additional resources**\n\n- [Pod priority classes](/openshift-docs-markdown/nodes/pods/nodes-pods-priority#admin-guide-priority-preemption-priority-class_nodes-pods-priority)\n\n## Overriding tolerations for catalog source pods {#olm-tolerations_olm-cs-podsched}\n\nTo allow catalog source pods to schedule onto nodes with matching taints, you can override the default tolerations in the `spec.grpcPodConfig` section of the `CatalogSource` object.\n\n**Prerequisites**\n\n- A `CatalogSource` object of source type `grpc` with `spec.image` is defined.\n\n**Procedure**\n\n- Edit the `CatalogSource` object and add or modify the `spec.grpcPodConfig` section to include the following:\n\n  ```yaml\n    grpcPodConfig:\n      tolerations:\n        - key: \"<key_name>\"\n          operator: \"<operator_type>\"\n          value: \"<value>\"\n          effect: \"<effect>\"\n  ```\n\n**Additional resources**\n\n- [Understanding taints and tolerations](/openshift-docs-markdown/nodes/scheduling/nodes-scheduler-taints-tolerations#nodes-scheduler-taints-tolerations-about_nodes-scheduler-taints-tolerations)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/operators/admin/olm-cs-podsched.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="olm-cs-podsched" class="group scroll-mt-20">Catalog source pod scheduling<a href="#olm-cs-podsched" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>When a <code>grpc</code> type catalog source defines the <code>spec.image</code> field, the Catalog Operator creates a pod to serve that image.</p>
      <p>By default, the pod specification configures the following default settings:</p>
      <ul>
      <li>Node selector: <code>kubernetes.io/os=linux</code></li>
      <li>Priority class name: <code>system-cluster-critical</code></li>
      <li>Tolerations: None</li>
      </ul>
      <p>As an administrator, you can override these defaults by configuring fields in the optional <code>spec.grpcPodConfig</code> section of the <code>CatalogSource</code> object.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>The Marketplace Operator, <code>openshift-marketplace</code>, manages the default <code>OperatorHub</code> custom resource’s (CR). This CR manages <code>CatalogSource</code> objects. If you attempt to modify fields in the <code>CatalogSource</code> object’s <code>spec.grpcPodConfig</code> section, the Marketplace Operator automatically reverts these modifications. By default, if you modify fields in the <code>spec.grpcPodConfig</code> section of the   <code>CatalogSource</code> object, the Marketplace Operator automatically reverts these changes.</p>
          <p>To apply persistent changes to <code>CatalogSource</code> object, you must first disable a default <code>CatalogSource</code> object.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/operators/understanding/olm/olm-understanding-olm#olm-catalogsource_olm-understanding-olm">OLM concepts and resources -&gt; Catalog source</a></li></ul>
      <h2 id="disabling-catalogsource-objects_olm-cs-podsched" class="group scroll-mt-20">Disabling default CatalogSource objects at a local level<a href="#disabling-catalogsource-objects_olm-cs-podsched" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can make persistent local changes to a <code>CatalogSource</code> object by disabling the default <code>CatalogSource</code> object. Otherwise, the Marketplace Operator automatically reverts any manual modifications to fields in the <code>spec.grpcPodConfig</code> section.</p>
      <p>The Marketplace Operator, <code>openshift-marketplace</code>, manages the default custom resources (CRs) of the <code>OperatorHub</code>. The <code>OperatorHub</code> manages <code>CatalogSource</code> objects.</p>
      <p>To apply persistent changes to <code>CatalogSource</code> object, you must first disable a default <code>CatalogSource</code> object.</p>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      To disable all the default <code>CatalogSource</code> objects at a local level, enter the following command:
      <CodeBlock code={"$ oc patch operatorhub cluster -p '{\"spec\": {\"disableAllDefaultSources\": true}}' --type=merge"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>You can also configure the default <code>OperatorHub</code> CR to either disable all <code>CatalogSource</code> objects or disable a specific object.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      <p><strong>Additional resources</strong></p>
      <ul>
      <li><a href="/openshift-docs-markdown/operators/understanding/olm-understanding-software-catalog#olm-software-catalog-arch-operatorhub-crd_olm-understanding-software-catalog">OperatorHub custom resource</a></li>
      <li><a href="/openshift-docs-markdown/disconnected/using-olm#olm-restricted-networks-operatorhub_olm-restricted-networks">Disabling the default OperatorHub catalog sources</a></li>
      </ul>
      <h2 id="olm-node-selector_olm-cs-podsched" class="group scroll-mt-20">Overriding the node selector for catalog source pods<a href="#olm-node-selector_olm-cs-podsched" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To control which nodes run catalog source pods, you can override the default node selector in the <code>spec.grpcPodConfig</code> section of the <code>CatalogSource</code> object.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>A <code>CatalogSource</code> object of source type <code>grpc</code> with <code>spec.image</code> is defined.</li></ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Edit the <code>CatalogSource</code> object and add or modify the <code>spec.grpcPodConfig</code> section to include the following:
      <CodeBlock code={"  grpcPodConfig:\n    nodeSelector:\n      custom_label: <label>"} lang="yaml" />
      <p>where <code>&lt;label&gt;</code> is the label for the node selector that you want catalog source pods to use for scheduling.</p>
      </li>
      </ul>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/nodes/scheduling/nodes-scheduler-node-selectors#nodes-scheduler-node-selectors">Placing pods on specific nodes using node selectors</a></li></ul>
      <h2 id="olm-priority-class-name_olm-cs-podsched" class="group scroll-mt-20">Overriding the priority class name for catalog source pods<a href="#olm-priority-class-name_olm-cs-podsched" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To control the scheduling priority of catalog source pods, you can override the default priority class name in the <code>spec.grpcPodConfig</code> section of the <code>CatalogSource</code> object.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>A <code>CatalogSource</code> object of source type <code>grpc</code> with a defined <code>spec.image</code>.</li></ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Edit the <code>CatalogSource</code> object and configure the <code>spec.grpcPodConfig</code> section, similar to the following example:
      <CodeBlock code={"  grpcPodConfig:\n    priorityClassName: <priority_class>"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;priority_class&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies one of the following priority classes: *   A default Kubernetes priority class, such as <code>system-cluster-critical</code> or <code>system-node-critical</code> *   An empty string (<code>""</code>) to assign the default priority *   A custom, pre-existing priority class name</p></dd>
      </dl>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Previously, the only pod scheduling parameter that could be overriden was <code>priorityClassName</code>. This was done by adding the <code>operatorframework.io/priorityclass</code> annotation to the <code>CatalogSource</code> object. For example:</p>
          <CodeBlock code={"apiVersion: operators.coreos.com/v1alpha1\nkind: CatalogSource\nmetadata:\n  name: example-catalog\n  namespace: {{ global_ns }}\n  annotations:\n    operatorframework.io/priorityclass: system-cluster-critical"} lang="yaml" />
          <p>If a <code>CatalogSource</code> object defines both the annotation and <code>spec.grpcPodConfig.priorityClassName</code>, the annotation takes precedence over the configuration parameter.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/nodes/pods/nodes-pods-priority#admin-guide-priority-preemption-priority-class_nodes-pods-priority">Pod priority classes</a></li></ul>
      <h2 id="olm-tolerations_olm-cs-podsched" class="group scroll-mt-20">Overriding tolerations for catalog source pods<a href="#olm-tolerations_olm-cs-podsched" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To allow catalog source pods to schedule onto nodes with matching taints, you can override the default tolerations in the <code>spec.grpcPodConfig</code> section of the <code>CatalogSource</code> object.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>A <code>CatalogSource</code> object of source type <code>grpc</code> with <code>spec.image</code> is defined.</li></ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Edit the <code>CatalogSource</code> object and add or modify the <code>spec.grpcPodConfig</code> section to include the following:
      <CodeBlock code={"  grpcPodConfig:\n    tolerations:\n      - key: \"<key_name>\"\n        operator: \"<operator_type>\"\n        value: \"<value>\"\n        effect: \"<effect>\""} lang="yaml" />
      </li>
      </ul>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/nodes/scheduling/nodes-scheduler-taints-tolerations#nodes-scheduler-taints-tolerations-about_nodes-scheduler-taints-tolerations">Understanding taints and tolerations</a></li></ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
