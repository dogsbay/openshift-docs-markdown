---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: operators/understanding/olm/olm-arch.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"olm-arch","text":"Operator Lifecycle Manager architecture"},{"depth":2,"slug":"olm-architecture_olm-arch","text":"Component responsibilities"},{"depth":2,"slug":"olm-arch-olm-operator_olm-arch","text":"OLM Operator"},{"depth":2,"slug":"olm-arch-catalog-operator_olm-arch","text":"Catalog Operator"},{"depth":2,"slug":"olm-arch-catalog-registry_olm-arch","text":"Catalog Registry"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/operators/understanding/olm/olm-arch";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Operator Lifecycle Manager architecture";
const description = "You can learn how Operator Lifecycle Manager (OLM) components interact to manage Operators in OpenShift Container Platform. The architecture includes the…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["operators","operators/understanding","operators/understanding/olm"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Operator Lifecycle Manager architecture {#olm-arch}\n\nYou can learn how Operator Lifecycle Manager (OLM) components interact to manage Operators in OpenShift Container Platform. The architecture includes the OLM Operator, Catalog Operator, and Catalog Registry.\n\n## Component responsibilities {#olm-architecture_olm-arch}\n\nOperator Lifecycle Manager (OLM) and the Catalog Operator manage the following custom resource definitions (CRDs) that form the basis of the Operator Framework.\n\n***CRDs managed by OLM and Catalog Operators***\n\n<table>\n<thead>\n<tr>\n  <th>Resource</th>\n  <th>Short name</th>\n  <th>Owner</th>\n  <th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td><code>ClusterServiceVersion</code> (CSV)</td>\n  <td><code>csv</code></td>\n  <td>OLM</td>\n  <td>Application metadata: name, version, icon, required resources, installation, and so on.</td>\n</tr>\n<tr>\n  <td><code>InstallPlan</code></td>\n  <td><code>ip</code></td>\n  <td>Catalog</td>\n  <td>Calculated list of resources to be created to automatically install or upgrade a CSV.</td>\n</tr>\n<tr>\n  <td><code>CatalogSource</code></td>\n  <td><code>catsrc</code></td>\n  <td>Catalog</td>\n  <td>A repository of CSVs, CRDs, and packages that define an application.</td>\n</tr>\n<tr>\n  <td><code>Subscription</code></td>\n  <td><code>sub</code></td>\n  <td>Catalog</td>\n  <td>Used to keep CSVs up to date by tracking a channel in a package.</td>\n</tr>\n<tr>\n  <td><code>OperatorGroup</code></td>\n  <td><code>og</code></td>\n  <td>OLM</td>\n  <td>Configures all Operators deployed in the same namespace as the <code>OperatorGroup</code> object to watch for their custom resource (CR) in a list of namespaces or cluster-wide.</td>\n</tr>\n</tbody>\n</table>\n\nEach of these Operators is also responsible for creating the following resources:\n\n**Resources created by OLM and Catalog Operators**\n\n| Resource | Owner |\n| --- | --- |\n| `Deployments` .4+.^ | OLM |\n| `ServiceAccounts` | `(Cluster)Roles` |\n| `(Cluster)RoleBindings` | `CustomResourceDefinitions` (CRDs) .2+.^ |\n| Catalog | `ClusterServiceVersions` |\n\n## OLM Operator {#olm-arch-olm-operator_olm-arch}\n\nThe OLM Operator deploys applications defined by cluster service versions (CSVs) after their required resources are present in the cluster. It watches CSVs in a namespace, verifies requirements, and runs the install strategy when conditions are met.\n\nThe OLM Operator is not concerned with the creation of the required resources; you can choose to manually create these resources using the CLI or using the Catalog Operator. This separation of concern allows users incremental buy-in in terms of how much of the OLM framework they choose to leverage for their application.\n\nThe OLM Operator uses the following workflow:\n\n1. Watch for cluster service versions (CSVs) in a namespace and check that requirements are met.\n2. If requirements are met, run the install strategy for the CSV.\n\n   > [!NOTE]\n   > A CSV must be an active member of an Operator group for the install strategy to run.\n\n## Catalog Operator {#olm-arch-catalog-operator_olm-arch}\n\nThe Catalog Operator in OpenShift Container Platform resolves and installs cluster service versions (CSVs) and their required resources from catalog sources. It watches subscriptions and catalog sources to create install plans and upgrade packages in channels.\n\nTo track a package in a channel, you can create a `Subscription` object configuring the desired package, channel, and the `CatalogSource` object you want to use for pulling updates. When updates are found, an appropriate `InstallPlan` object is written into the namespace on behalf of the user.\n\nThe Catalog Operator uses the following workflow:\n\n1. Connect to each catalog source in the cluster.\n2. Watch for unresolved install plans created by a user, and if found:\n\n   1. Find the CSV matching the name requested and add the CSV as a resolved resource.\n   2. For each managed or required CRD, add the CRD as a resolved resource.\n   3. For each required CRD, find the CSV that manages it.\n3. Watch for resolved install plans and create all of the discovered resources for it, if approved by a user or automatically.\n4. Watch for catalog sources and subscriptions and create install plans based on them.\n\n## Catalog Registry {#olm-arch-catalog-registry_olm-arch}\n\nThe Catalog Registry stores cluster service versions (CSVs), custom resource definitions (CRDs), and metadata about packages and channels for Operator installation in OpenShift Container Platform. Package manifests link package identities to CSVs so the Catalog Operator can step through channel upgrade paths.\n\nA *package manifest* is an entry in the Catalog Registry that associates a package identity with sets of CSVs. Within a package, channels point to a particular CSV. Because CSVs explicitly reference the CSV that they replace, a package manifest provides the Catalog Operator with all of the information that is required to update a CSV to the latest version in a channel, stepping through each intermediate version.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/operators/understanding/olm/olm-arch.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="olm-arch" class="group scroll-mt-20">Operator Lifecycle Manager architecture<a href="#olm-arch" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can learn how Operator Lifecycle Manager (OLM) components interact to manage Operators in OpenShift Container Platform. The architecture includes the OLM Operator, Catalog Operator, and Catalog Registry.</p>
      <h2 id="olm-architecture_olm-arch" class="group scroll-mt-20">Component responsibilities<a href="#olm-architecture_olm-arch" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Operator Lifecycle Manager (OLM) and the Catalog Operator manage the following custom resource definitions (CRDs) that form the basis of the Operator Framework.</p>
      <p><em></em><em><strong>CRDs managed by OLM and Catalog Operators</strong></em><em></em></p>
      <Fragment set:html={"<table>\n<thead>\n<tr>\n  <th>Resource</th>\n  <th>Short name</th>\n  <th>Owner</th>\n  <th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td><code>ClusterServiceVersion</code> (CSV)</td>\n  <td><code>csv</code></td>\n  <td>OLM</td>\n  <td>Application metadata: name, version, icon, required resources, installation, and so on.</td>\n</tr>\n<tr>\n  <td><code>InstallPlan</code></td>\n  <td><code>ip</code></td>\n  <td>Catalog</td>\n  <td>Calculated list of resources to be created to automatically install or upgrade a CSV.</td>\n</tr>\n<tr>\n  <td><code>CatalogSource</code></td>\n  <td><code>catsrc</code></td>\n  <td>Catalog</td>\n  <td>A repository of CSVs, CRDs, and packages that define an application.</td>\n</tr>\n<tr>\n  <td><code>Subscription</code></td>\n  <td><code>sub</code></td>\n  <td>Catalog</td>\n  <td>Used to keep CSVs up to date by tracking a channel in a package.</td>\n</tr>\n<tr>\n  <td><code>OperatorGroup</code></td>\n  <td><code>og</code></td>\n  <td>OLM</td>\n  <td>Configures all Operators deployed in the same namespace as the <code>OperatorGroup</code> object to watch for their custom resource (CR) in a list of namespaces or cluster-wide.</td>\n</tr>\n</tbody>\n</table>\n"} />
      <p>Each of these Operators is also responsible for creating the following resources:</p>
      <p><strong>Resources created by OLM and Catalog Operators</strong></p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Resource</TableHead>
      <TableHead>Owner</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>Deployments</code> .4+.^</TableCell>
      <TableCell>OLM</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ServiceAccounts</code></TableCell>
      <TableCell><code>(Cluster)Roles</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>(Cluster)RoleBindings</code></TableCell>
      <TableCell><code>CustomResourceDefinitions</code> (CRDs) .2+.^</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Catalog</TableCell>
      <TableCell><code>ClusterServiceVersions</code></TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="olm-arch-olm-operator_olm-arch" class="group scroll-mt-20">OLM Operator<a href="#olm-arch-olm-operator_olm-arch" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The OLM Operator deploys applications defined by cluster service versions (CSVs) after their required resources are present in the cluster. It watches CSVs in a namespace, verifies requirements, and runs the install strategy when conditions are met.</p>
      <p>The OLM Operator is not concerned with the creation of the required resources; you can choose to manually create these resources using the CLI or using the Catalog Operator. This separation of concern allows users incremental buy-in in terms of how much of the OLM framework they choose to leverage for their application.</p>
      <p>The OLM Operator uses the following workflow:</p>
      <ol>
      <li>Watch for cluster service versions (CSVs) in a namespace and check that requirements are met.</li>
      <li>
      If requirements are met, run the install strategy for the CSV.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>A CSV must be an active member of an Operator group for the install strategy to run.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <h2 id="olm-arch-catalog-operator_olm-arch" class="group scroll-mt-20">Catalog Operator<a href="#olm-arch-catalog-operator_olm-arch" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The Catalog Operator in OpenShift Container Platform resolves and installs cluster service versions (CSVs) and their required resources from catalog sources. It watches subscriptions and catalog sources to create install plans and upgrade packages in channels.</p>
      <p>To track a package in a channel, you can create a <code>Subscription</code> object configuring the desired package, channel, and the <code>CatalogSource</code> object you want to use for pulling updates. When updates are found, an appropriate <code>InstallPlan</code> object is written into the namespace on behalf of the user.</p>
      <p>The Catalog Operator uses the following workflow:</p>
      <ol>
      <li>Connect to each catalog source in the cluster.</li>
      <li>
      Watch for unresolved install plans created by a user, and if found:
      <ol>
      <li>Find the CSV matching the name requested and add the CSV as a resolved resource.</li>
      <li>For each managed or required CRD, add the CRD as a resolved resource.</li>
      <li>For each required CRD, find the CSV that manages it.</li>
      </ol>
      </li>
      <li>Watch for resolved install plans and create all of the discovered resources for it, if approved by a user or automatically.</li>
      <li>Watch for catalog sources and subscriptions and create install plans based on them.</li>
      </ol>
      <h2 id="olm-arch-catalog-registry_olm-arch" class="group scroll-mt-20">Catalog Registry<a href="#olm-arch-catalog-registry_olm-arch" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The Catalog Registry stores cluster service versions (CSVs), custom resource definitions (CRDs), and metadata about packages and channels for Operator installation in OpenShift Container Platform. Package manifests link package identities to CSVs so the Catalog Operator can step through channel upgrade paths.</p>
      <p>A <em>package manifest</em> is an entry in the Catalog Registry that associates a package identity with sets of CSVs. Within a package, channels point to a particular CSV. Because CSVs explicitly reference the CSV that they replace, a package manifest provides the Catalog Operator with all of the information that is required to update a CSV to the latest version in a channel, stepping through each intermediate version.</p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
