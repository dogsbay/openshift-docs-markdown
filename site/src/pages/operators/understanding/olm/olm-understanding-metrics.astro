---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: operators/understanding/olm/olm-understanding-metrics.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;

const headings = [{"depth":1,"slug":"olm-understanding-metrics","text":"Operator Lifecycle Manager metrics"},{"depth":2,"slug":"olm-metrics_olm-understanding-metrics","text":"Exposed metrics"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/operators/understanding/olm/olm-understanding-metrics";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Operator Lifecycle Manager metrics";
const description = "Operator Lifecycle Manager (OLM) exposes OLM-specific metrics that the Prometheus-based OpenShift Container Platform cluster monitoring stack can use to…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["operators","operators/understanding","operators/understanding/olm"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Operator Lifecycle Manager metrics {#olm-understanding-metrics}\n\nOperator Lifecycle Manager (OLM) exposes OLM-specific metrics that the Prometheus-based OpenShift Container Platform cluster monitoring stack can use to track catalog sources, cluster service versions, install plans, and subscriptions.\n\n## Exposed metrics {#olm-metrics_olm-understanding-metrics}\n\nOperator Lifecycle Manager (OLM) exposes certain OLM-specific resources for use by the Prometheus-based OpenShift Container Platform cluster monitoring stack.\n\n**Metrics exposed by OLM**\n\n<table>\n<thead>\n<tr>\n  <th>Name</th>\n  <th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td><code>catalog_source_count</code></td>\n  <td>Number of catalog sources.</td>\n</tr>\n<tr>\n  <td><code>catalogsource_ready</code></td>\n  <td>State of a catalog source. The value <code>1</code> indicates that the catalog source is in a <code>READY</code> state. The value of <code>0</code> indicates that the catalog source is not in a <code>READY</code> state.</td>\n</tr>\n<tr>\n  <td><code>csv_abnormal</code></td>\n  <td>When reconciling a cluster service version (CSV), present whenever a CSV version is in any state other than <code>Succeeded</code>, for example when it is not installed. Includes the <code>name</code>, <code>namespace</code>, <code>phase</code>, <code>reason</code>, and <code>version</code> labels. A Prometheus alert is created when this metric is present.</td>\n</tr>\n<tr>\n  <td><code>csv_count</code></td>\n  <td>Number of CSVs successfully registered.</td>\n</tr>\n<tr>\n  <td><code>csv_succeeded</code></td>\n  <td>When reconciling a CSV, represents whether a CSV version is in a <code>Succeeded</code> state (value <code>1</code>) or not (value <code>0</code>). Includes the <code>name</code>, <code>namespace</code>, and <code>version</code> labels.</td>\n</tr>\n<tr>\n  <td><code>csv_upgrade_count</code></td>\n  <td>Monotonic count of CSV upgrades.</td>\n</tr>\n<tr>\n  <td><code>install_plan_count</code></td>\n  <td>Number of install plans.</td>\n</tr>\n<tr>\n  <td><code>installplan_warnings_total</code></td>\n  <td>Monotonic count of warnings generated by resources, such as deprecated resources, included in an install plan.</td>\n</tr>\n<tr>\n  <td><code>olm_resolution_duration_seconds</code></td>\n  <td>The duration of a dependency resolution attempt.</td>\n</tr>\n<tr>\n  <td><code>subscription_count</code></td>\n  <td>Number of subscriptions.</td>\n</tr>\n<tr>\n  <td><code>subscription_sync_total</code></td>\n  <td>Monotonic count of subscription syncs. Includes the <code>channel</code>, <code>installed</code> CSV, and subscription <code>name</code> labels.</td>\n</tr>\n</tbody>\n</table>\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/operators/understanding/olm/olm-understanding-metrics.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="olm-understanding-metrics" class="group scroll-mt-20">Operator Lifecycle Manager metrics<a href="#olm-understanding-metrics" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Operator Lifecycle Manager (OLM) exposes OLM-specific metrics that the Prometheus-based OpenShift Container Platform cluster monitoring stack can use to track catalog sources, cluster service versions, install plans, and subscriptions.</p>
      <h2 id="olm-metrics_olm-understanding-metrics" class="group scroll-mt-20">Exposed metrics<a href="#olm-metrics_olm-understanding-metrics" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Operator Lifecycle Manager (OLM) exposes certain OLM-specific resources for use by the Prometheus-based OpenShift Container Platform cluster monitoring stack.</p>
      <p><strong>Metrics exposed by OLM</strong></p>
      <Fragment set:html={"<table>\n<thead>\n<tr>\n  <th>Name</th>\n  <th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n  <td><code>catalog_source_count</code></td>\n  <td>Number of catalog sources.</td>\n</tr>\n<tr>\n  <td><code>catalogsource_ready</code></td>\n  <td>State of a catalog source. The value <code>1</code> indicates that the catalog source is in a <code>READY</code> state. The value of <code>0</code> indicates that the catalog source is not in a <code>READY</code> state.</td>\n</tr>\n<tr>\n  <td><code>csv_abnormal</code></td>\n  <td>When reconciling a cluster service version (CSV), present whenever a CSV version is in any state other than <code>Succeeded</code>, for example when it is not installed. Includes the <code>name</code>, <code>namespace</code>, <code>phase</code>, <code>reason</code>, and <code>version</code> labels. A Prometheus alert is created when this metric is present.</td>\n</tr>\n<tr>\n  <td><code>csv_count</code></td>\n  <td>Number of CSVs successfully registered.</td>\n</tr>\n<tr>\n  <td><code>csv_succeeded</code></td>\n  <td>When reconciling a CSV, represents whether a CSV version is in a <code>Succeeded</code> state (value <code>1</code>) or not (value <code>0</code>). Includes the <code>name</code>, <code>namespace</code>, and <code>version</code> labels.</td>\n</tr>\n<tr>\n  <td><code>csv_upgrade_count</code></td>\n  <td>Monotonic count of CSV upgrades.</td>\n</tr>\n<tr>\n  <td><code>install_plan_count</code></td>\n  <td>Number of install plans.</td>\n</tr>\n<tr>\n  <td><code>installplan_warnings_total</code></td>\n  <td>Monotonic count of warnings generated by resources, such as deprecated resources, included in an install plan.</td>\n</tr>\n<tr>\n  <td><code>olm_resolution_duration_seconds</code></td>\n  <td>The duration of a dependency resolution attempt.</td>\n</tr>\n<tr>\n  <td><code>subscription_count</code></td>\n  <td>Number of subscriptions.</td>\n</tr>\n<tr>\n  <td><code>subscription_sync_total</code></td>\n  <td>Monotonic count of subscription syncs. Includes the <code>channel</code>, <code>installed</code> CSV, and subscription <code>name</code> labels.</td>\n</tr>\n</tbody>\n</table>"} />
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
