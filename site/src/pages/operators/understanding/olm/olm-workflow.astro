---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: operators/understanding/olm/olm-workflow.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import BlockImage from "@ui/image/Image.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"olm-workflow","text":"Operator Lifecycle Manager workflow"},{"depth":2,"slug":"olm-upgrades_olm-workflow","text":"Operator installation and upgrade workflow in OLM"},{"depth":3,"slug":"olm-upgrades-example-upgrade-path_olm-workflow","text":"Example upgrade path"},{"depth":3,"slug":"olm-upgrades-skipping_olm-workflow","text":"Skipping upgrades"},{"depth":3,"slug":"olm-upgrades-replacing-multiple_olm-workflow","text":"Replacing multiple Operators"},{"depth":3,"slug":"olm-upgrades-z-stream_olm-workflow","text":"Z-stream support"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/operators/understanding/olm/olm-workflow";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Operator Lifecycle Manager workflow";
const description = "The Operator Lifecycle Manager (OLM) resolves Operator installs and upgrades in OpenShift Container Platform. The OLM lifecycle involves interacting with…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["operators","operators/understanding","operators/understanding/olm"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Operator Lifecycle Manager workflow {#olm-workflow}\n\nThe Operator Lifecycle Manager (OLM) resolves Operator installs and upgrades in OpenShift Container Platform. The OLM lifecycle involves interacting with catalog sources, subscriptions, and cluster service versions (CSVs).\n\n## Operator installation and upgrade workflow in OLM {#olm-upgrades_olm-workflow}\n\nIn the Operator Lifecycle Manager (OLM) ecosystem, the following resources are used to resolve Operator installations and upgrades:\n\n- `ClusterServiceVersion` (CSV)\n- `CatalogSource`\n- `Subscription`\n\nOperator metadata, defined in CSVs, can be stored in a collection called a catalog source. OLM uses catalog sources, which use the [Operator Registry API](https://github.com/operator-framework/operator-registry), to query for available Operators as well as upgrades for installed Operators.\n\n**Figure 1. Catalog source overview**\n\n![olm-catalogsource](/openshift-docs-markdown/images/olm-catalogsource.png)\n\nWithin a catalog source, Operators are organized into *packages* and streams of updates called *channels*, which should be a familiar update pattern from OpenShift Container Platform or other software on a continuous release cycle like web browsers.\n\n**Figure 2. Packages and channels in a Catalog source**\n\n![olm-channels](/openshift-docs-markdown/images/olm-channels.png)\n\nA user indicates a particular package and channel in a particular catalog source in a *subscription*, for example an `etcd` package and its `alpha` channel. If a subscription is made to a package that has not yet been installed in the namespace, the latest Operator for that package is installed.\n\n> [!NOTE]\n> OLM deliberately avoids version comparisons, so the \"latest\" or \"newest\" Operator available from a given *catalog* → *channel* → *package* path does not necessarily need to be the highest version number. It should be thought of more as the *head* reference of a channel, similar to a Git repository.\n\nEach CSV has a `replaces` parameter that indicates which Operator it replaces. This builds a graph of CSVs that can be queried by OLM, and updates can be shared between channels. Channels can be thought of as entry points into the graph of updates:\n\n**Figure 3. OLM graph of available channel updates**\n\n![olm-replaces](/openshift-docs-markdown/images/olm-replaces.png)\n\n```yaml {title=\"Example channels in a package\"}\npackageName: example\nchannels:\n- name: alpha\n  currentCSV: example.v0.1.2\n- name: beta\n  currentCSV: example.v0.1.3\ndefaultChannel: alpha\n```\n\nFor OLM to successfully query for updates, given a catalog source, package, channel, and CSV, a catalog must be able to return, unambiguously and deterministically, a single CSV that `replaces` the input CSV.\n\n### Example upgrade path {#olm-upgrades-example-upgrade-path_olm-workflow}\n\nFor an example upgrade scenario, consider an installed Operator corresponding to CSV version `0.1.1`. OLM queries the catalog source and detects an upgrade in the subscribed channel with new CSV version `0.1.3` that replaces an older but not-installed CSV version `0.1.2`, which in turn replaces the older and installed CSV version `0.1.1`.\n\nOLM walks back from the channel head to previous versions via the `replaces` field specified in the CSVs to determine the upgrade path `0.1.3` → `0.1.2` → `0.1.1`; the direction of the arrow indicates that the former replaces the latter. OLM upgrades the Operator one version at the time until it reaches the channel head.\n\nFor this given scenario, OLM installs Operator version `0.1.2` to replace the existing Operator version `0.1.1`. Then, it installs Operator version `0.1.3` to replace the previously installed Operator version `0.1.2`. At this point, the installed operator version `0.1.3` matches the channel head and the upgrade is completed.\n\n### Skipping upgrades {#olm-upgrades-skipping_olm-workflow}\n\nThe basic path for upgrades in OLM is:\n\n- A catalog source is updated with one or more updates to an Operator.\n- OLM traverses every version of the Operator until reaching the latest version the catalog source contains.\n\nHowever, sometimes this is not a safe operation to perform. There will be cases where a published version of an Operator should never be installed on a cluster if it has not already, for example because a version introduces a serious vulnerability.\n\nIn those cases, OLM must consider two cluster states and provide an update graph that supports both:\n\n- The \"bad\" intermediate Operator has been seen by the cluster and installed.\n- The \"bad\" intermediate Operator has not yet been installed onto the cluster.\n\nBy shipping a new catalog and adding a *skipped* release, OLM is ensured that it can always get a single unique update regardless of the cluster state and whether it has seen the bad update yet.\n\n```yaml {title=\"Example CSV with skipped release\"}\napiVersion: operators.coreos.com/v1alpha1\nkind: ClusterServiceVersion\nmetadata:\n  name: etcdoperator.v0.9.2\n  namespace: placeholder\n  annotations:\nspec:\n    displayName: etcd\n    description: Etcd Operator\n    replaces: etcdoperator.v0.9.0\n    skips:\n    - etcdoperator.v0.9.1\n```\n\nConsider the following example of **Old CatalogSource** and **New CatalogSource**.\n\n**Figure 4. Skipping updates**\n\n![olm-skipping-updates](/openshift-docs-markdown/images/olm-skipping-updates.png)\n\nThis graph maintains that:\n\n- Any Operator found in **Old CatalogSource** has a single replacement in **New CatalogSource**.\n- Any Operator found in **New CatalogSource** has a single replacement in **New CatalogSource**.\n- If the bad update has not yet been installed, it will never be.\n\n### Replacing multiple Operators {#olm-upgrades-replacing-multiple_olm-workflow}\n\nCreating **New CatalogSource** as described requires publishing CSVs that `replace` one Operator, but can `skip` several. This can be accomplished using the `skipRange` annotation:\n\n```yaml\nolm.skipRange: <semver_range>\n```\n\nwhere `<semver_range>` has the version range format supported by the [semver library](https://github.com/blang/semver#ranges).\n\nWhen searching catalogs for updates, if the head of a channel has a `skipRange` annotation and the currently installed Operator has a version field that falls in the range, OLM updates to the latest entry in the channel.\n\nThe order of precedence is:\n\n1. Channel head in the source specified by `sourceName` on the subscription, if the other criteria for skipping are met.\n2. The next Operator that replaces the current one, in the source specified by `sourceName`.\n3. Channel head in another source that is visible to the subscription, if the other criteria for skipping are met.\n4. The next Operator that replaces the current one in any source visible to the subscription.\n\n```yaml {title=\"Example CSV with skipRange\"}\napiVersion: operators.coreos.com/v1alpha1\nkind: ClusterServiceVersion\nmetadata:\n    name: elasticsearch-operator.v4.1.2\n    namespace: <namespace>\n    annotations:\n        olm.skipRange: '>=4.1.0 <4.1.2'\n```\n\n### Z-stream support {#olm-upgrades-z-stream_olm-workflow}\n\nA *z-stream*, or patch release, must replace all previous z-stream releases for the same minor version. OLM does not consider major, minor, or patch versions, it just needs to build the correct graph in a catalog.\n\nIn other words, OLM must be able to take a graph as in **Old CatalogSource** and, similar to before, generate a graph as in **New CatalogSource**:\n\n**Figure 5. Replacing several Operators**\n\n![olm-z-stream](/openshift-docs-markdown/images/olm-z-stream.png)\n\nThis graph maintains that:\n\n- Any Operator found in **Old CatalogSource** has a single replacement in **New CatalogSource**.\n- Any Operator found in **New CatalogSource** has a single replacement in **New CatalogSource**.\n- Any z-stream release in **Old CatalogSource** will update to the latest z-stream release in **New CatalogSource**.\n- Unavailable releases can be considered \"virtual\" graph nodes; their content does not need to exist, the registry just needs to respond as if the graph looks like this.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/operators/understanding/olm/olm-workflow.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="olm-workflow" class="group scroll-mt-20">Operator Lifecycle Manager workflow<a href="#olm-workflow" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">The Operator Lifecycle Manager (OLM) resolves Operator installs and upgrades in OpenShift Container Platform. The OLM lifecycle involves interacting with catalog sources, subscriptions, and cluster service versions (CSVs).</p>
      <h2 id="olm-upgrades_olm-workflow" class="group scroll-mt-20">Operator installation and upgrade workflow in OLM<a href="#olm-upgrades_olm-workflow" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>In the Operator Lifecycle Manager (OLM) ecosystem, the following resources are used to resolve Operator installations and upgrades:</p>
      <ul>
      <li><code>ClusterServiceVersion</code> (CSV)</li>
      <li><code>CatalogSource</code></li>
      <li><code>Subscription</code></li>
      </ul>
      <p>Operator metadata, defined in CSVs, can be stored in a collection called a catalog source. OLM uses catalog sources, which use the <a href="https://github.com/operator-framework/operator-registry">Operator Registry API</a>, to query for available Operators as well as upgrades for installed Operators.</p>
      <p><strong>Figure 1. Catalog source overview</strong></p>
      <BlockImage src="/openshift-docs-markdown/images/olm-catalogsource.png" alt="olm-catalogsource" />
      <p>Within a catalog source, Operators are organized into <em>packages</em> and streams of updates called <em>channels</em>, which should be a familiar update pattern from OpenShift Container Platform or other software on a continuous release cycle like web browsers.</p>
      <p><strong>Figure 2. Packages and channels in a Catalog source</strong></p>
      <BlockImage src="/openshift-docs-markdown/images/olm-channels.png" alt="olm-channels" />
      <p>A user indicates a particular package and channel in a particular catalog source in a <em>subscription</em>, for example an <code>etcd</code> package and its <code>alpha</code> channel. If a subscription is made to a package that has not yet been installed in the namespace, the latest Operator for that package is installed.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>OLM deliberately avoids version comparisons, so the "latest" or "newest" Operator available from a given <em>catalog</em> → <em>channel</em> → <em>package</em> path does not necessarily need to be the highest version number. It should be thought of more as the <em>head</em> reference of a channel, similar to a Git repository.</p>
        </AlertDescription>
      </Alert>
      <p>Each CSV has a <code>replaces</code> parameter that indicates which Operator it replaces. This builds a graph of CSVs that can be queried by OLM, and updates can be shared between channels. Channels can be thought of as entry points into the graph of updates:</p>
      <p><strong>Figure 3. OLM graph of available channel updates</strong></p>
      <BlockImage src="/openshift-docs-markdown/images/olm-replaces.png" alt="olm-replaces" />
      <CodeBlock code={"packageName: example\nchannels:\n- name: alpha\n  currentCSV: example.v0.1.2\n- name: beta\n  currentCSV: example.v0.1.3\ndefaultChannel: alpha"} lang="yaml" title="Example channels in a package" />
      <p>For OLM to successfully query for updates, given a catalog source, package, channel, and CSV, a catalog must be able to return, unambiguously and deterministically, a single CSV that <code>replaces</code> the input CSV.</p>
      <h3 id="olm-upgrades-example-upgrade-path_olm-workflow" class="group scroll-mt-20">Example upgrade path<a href="#olm-upgrades-example-upgrade-path_olm-workflow" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>For an example upgrade scenario, consider an installed Operator corresponding to CSV version <code>0.1.1</code>. OLM queries the catalog source and detects an upgrade in the subscribed channel with new CSV version <code>0.1.3</code> that replaces an older but not-installed CSV version <code>0.1.2</code>, which in turn replaces the older and installed CSV version <code>0.1.1</code>.</p>
      <p>OLM walks back from the channel head to previous versions via the <code>replaces</code> field specified in the CSVs to determine the upgrade path <code>0.1.3</code> → <code>0.1.2</code> → <code>0.1.1</code>; the direction of the arrow indicates that the former replaces the latter. OLM upgrades the Operator one version at the time until it reaches the channel head.</p>
      <p>For this given scenario, OLM installs Operator version <code>0.1.2</code> to replace the existing Operator version <code>0.1.1</code>. Then, it installs Operator version <code>0.1.3</code> to replace the previously installed Operator version <code>0.1.2</code>. At this point, the installed operator version <code>0.1.3</code> matches the channel head and the upgrade is completed.</p>
      <h3 id="olm-upgrades-skipping_olm-workflow" class="group scroll-mt-20">Skipping upgrades<a href="#olm-upgrades-skipping_olm-workflow" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The basic path for upgrades in OLM is:</p>
      <ul>
      <li>A catalog source is updated with one or more updates to an Operator.</li>
      <li>OLM traverses every version of the Operator until reaching the latest version the catalog source contains.</li>
      </ul>
      <p>However, sometimes this is not a safe operation to perform. There will be cases where a published version of an Operator should never be installed on a cluster if it has not already, for example because a version introduces a serious vulnerability.</p>
      <p>In those cases, OLM must consider two cluster states and provide an update graph that supports both:</p>
      <ul>
      <li>The "bad" intermediate Operator has been seen by the cluster and installed.</li>
      <li>The "bad" intermediate Operator has not yet been installed onto the cluster.</li>
      </ul>
      <p>By shipping a new catalog and adding a <em>skipped</em> release, OLM is ensured that it can always get a single unique update regardless of the cluster state and whether it has seen the bad update yet.</p>
      <CodeBlock code={"apiVersion: operators.coreos.com/v1alpha1\nkind: ClusterServiceVersion\nmetadata:\n  name: etcdoperator.v0.9.2\n  namespace: placeholder\n  annotations:\nspec:\n    displayName: etcd\n    description: Etcd Operator\n    replaces: etcdoperator.v0.9.0\n    skips:\n    - etcdoperator.v0.9.1"} lang="yaml" title="Example CSV with skipped release" />
      <p>Consider the following example of <strong>Old CatalogSource</strong> and <strong>New CatalogSource</strong>.</p>
      <p><strong>Figure 4. Skipping updates</strong></p>
      <BlockImage src="/openshift-docs-markdown/images/olm-skipping-updates.png" alt="olm-skipping-updates" />
      <p>This graph maintains that:</p>
      <ul>
      <li>Any Operator found in <strong>Old CatalogSource</strong> has a single replacement in <strong>New CatalogSource</strong>.</li>
      <li>Any Operator found in <strong>New CatalogSource</strong> has a single replacement in <strong>New CatalogSource</strong>.</li>
      <li>If the bad update has not yet been installed, it will never be.</li>
      </ul>
      <h3 id="olm-upgrades-replacing-multiple_olm-workflow" class="group scroll-mt-20">Replacing multiple Operators<a href="#olm-upgrades-replacing-multiple_olm-workflow" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Creating <strong>New CatalogSource</strong> as described requires publishing CSVs that <code>replace</code> one Operator, but can <code>skip</code> several. This can be accomplished using the <code>skipRange</code> annotation:</p>
      <CodeBlock code={"olm.skipRange: <semver_range>"} lang="yaml" />
      <p>where <code>&lt;semver_range&gt;</code> has the version range format supported by the <a href="https://github.com/blang/semver#ranges">semver library</a>.</p>
      <p>When searching catalogs for updates, if the head of a channel has a <code>skipRange</code> annotation and the currently installed Operator has a version field that falls in the range, OLM updates to the latest entry in the channel.</p>
      <p>The order of precedence is:</p>
      <ol>
      <li>Channel head in the source specified by <code>sourceName</code> on the subscription, if the other criteria for skipping are met.</li>
      <li>The next Operator that replaces the current one, in the source specified by <code>sourceName</code>.</li>
      <li>Channel head in another source that is visible to the subscription, if the other criteria for skipping are met.</li>
      <li>The next Operator that replaces the current one in any source visible to the subscription.</li>
      </ol>
      <CodeBlock code={"apiVersion: operators.coreos.com/v1alpha1\nkind: ClusterServiceVersion\nmetadata:\n    name: elasticsearch-operator.v4.1.2\n    namespace: <namespace>\n    annotations:\n        olm.skipRange: '>=4.1.0 <4.1.2'"} lang="yaml" title="Example CSV with skipRange" />
      <h3 id="olm-upgrades-z-stream_olm-workflow" class="group scroll-mt-20">Z-stream support<a href="#olm-upgrades-z-stream_olm-workflow" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>A <em>z-stream</em>, or patch release, must replace all previous z-stream releases for the same minor version. OLM does not consider major, minor, or patch versions, it just needs to build the correct graph in a catalog.</p>
      <p>In other words, OLM must be able to take a graph as in <strong>Old CatalogSource</strong> and, similar to before, generate a graph as in <strong>New CatalogSource</strong>:</p>
      <p><strong>Figure 5. Replacing several Operators</strong></p>
      <BlockImage src="/openshift-docs-markdown/images/olm-z-stream.png" alt="olm-z-stream" />
      <p>This graph maintains that:</p>
      <ul>
      <li>Any Operator found in <strong>Old CatalogSource</strong> has a single replacement in <strong>New CatalogSource</strong>.</li>
      <li>Any Operator found in <strong>New CatalogSource</strong> has a single replacement in <strong>New CatalogSource</strong>.</li>
      <li>Any z-stream release in <strong>Old CatalogSource</strong> will update to the latest z-stream release in <strong>New CatalogSource</strong>.</li>
      <li>Unavailable releases can be considered "virtual" graph nodes; their content does not need to exist, the registry just needs to respond as if the graph looks like this.</li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
