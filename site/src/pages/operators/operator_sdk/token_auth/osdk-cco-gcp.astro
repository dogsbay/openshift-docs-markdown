---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: operators/operator_sdk/token_auth/osdk-cco-gcp.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"osdk-cco-gcp","text":"CCO-based workflow for OLM-managed Operators with {{ gcp_wid_short }}"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/operators/operator_sdk/token_auth/osdk-cco-gcp";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "CCO-based workflow for OLM-managed Operators with {{ gcp_wid_short }}";
const description = "When an OpenShift Container Platform cluster running on {{ gcp_first }} is in {{ gcp_wid_short }} / Federated Identity mode, it means the cluster is…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["operators","operators/operator_sdk","operators/operator_sdk/token_auth"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# CCO-based workflow for OLM-managed Operators with {{ gcp_wid_short }} {#osdk-cco-gcp}\n\nWhen an OpenShift Container Platform cluster running on {{ gcp_first }} is in **{{ gcp_wid_short }} / Federated Identity** mode, it means the cluster is utilizing features of {{ gcp_first }} and OpenShift Container Platform to apply permissions in {{ gcp_wid_short }} at an application level.\n\nThe Cloud Credential Operator (CCO) is a cluster Operator installed by default in OpenShift Container Platform clusters running on cloud providers. Starting in OpenShift Container Platform 4.17, the CCO supports workflows for OLM-managed Operators with {{ gcp_wid_short }}.\n\nFor the purposes of {{ gcp_wid_short }}, the CCO provides the following functions:\n\n- Detects when it is running on an {{ gcp_wid_short }}-enabled cluster\n- Checks the `CredentialsRequest` object for the presence of fields that provide the required information for granting Operators access to {{ gcp_short }} resources\n\nThe CCO can semi-automate this process through an expanded use of `CredentialsRequest` objects, which can request the creation of `Secrets` that contain the information required for {{ gcp_wid_short }} workflows.\n\n> [!NOTE]\n> Subscriptions with automatic approvals for updates are not recommended because there might be permission changes to make before updating. Subscriptions with manual approvals for updates ensure that administrators have the opportunity to verify the permissions of the later version, take any necessary steps, and then update.\n\nAs an Operator author preparing an Operator for use alongside the updated CCO in OpenShift Container Platform 4.17 and later, you should instruct users and add code to handle the divergence from earlier CCO versions, in addition to handling {{ gcp_wid_short }} token authentication (if your Operator is not already enabled). The recommended method is to provide a `CredentialsRequest` object with the correctly filled {{ gcp_wid_short }} fields and let the CCO create the `Secret` object for you.\n\n> [!IMPORTANT]\n> If you plan to support OpenShift Container Platform clusters earlier than version 4.17, consider providing users with instructions on how to manually create a secret with the {{ gcp_wid_short }}-enabling information by using the CCO utility (`ccoctl`). Earlier CCO versions are unaware of {{ gcp_wid_short }} mode on the cluster and cannot create secrets for you.\n>\n> Your code should check for secrets that never appear and warn users to follow the fallback instructions you have provided.\n\nTo authenticate with {{ gcp_short }} using short-lived tokens via {{ gcp_wid_first }}, Operators must provide the following information:\n\n`AUDIENCE`\n:   Created in {{ gcp_short }} by the administrator when they set up {{ gcp_wid_short }}, the `AUDIENCE` value must be a preformatted URL in the following format:\n\n    ```text\n    //iam.googleapis.com/projects/<project_number>/locations/global/workloadIdentityPools/<pool_id>/providers/<provider_id>\n    ```\n\n`SERVICE_ACCOUNT_EMAIL`\n:   The `SERVICE_ACCOUNT_EMAIL` value is a {{ gcp_short }} service account email that is impersonated during Operator operation, for example:\n\n    ```text\n    <service_account_name>@<project_id>.iam.gserviceaccount.com\n    ```\n\nThe **Install Operator** page in the web console allows cluster administrators to provide this information at installation time. This information is then propagated to the `Subscription` object as environment variables on the Operator pod.\n\n**Additional resources**\n\n- [OLM-managed Operator support for authentication with {{ gcp_wid_short }}](/openshift-docs-markdown/authentication/managing_cloud_provider_credentials/cco-short-term-creds#cco-short-term-creds-gcp-olm_cco-short-term-creds)\n- [Installing from OperatorHub using the web console](/openshift-docs-markdown/operators/admin/olm-adding-operators-to-cluster#olm-installing-from-software-catalog-using-web-console_olm-adding-operators-to-a-cluster)\n- [Installing from OperatorHub using the CLI](/openshift-docs-markdown/operators/admin/olm-adding-operators-to-cluster#olm-installing-operator-from-software-catalog-using-cli_olm-adding-operators-to-a-cluster)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/operators/operator_sdk/token_auth/osdk-cco-gcp.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={true}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <p class="/_attributes/common-attributes.md"></p>
      <h1 id="osdk-cco-gcp" class="group scroll-mt-20">CCO-based workflow for OLM-managed Operators with {"{"}{"{"} gcp_wid_short {"}"}{"}"}<a href="#osdk-cco-gcp" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>When an OpenShift Container Platform cluster running on {"{"}{"{"} gcp_first {"}"}{"}"} is in <strong>{"{"}{"{"} gcp_wid_short {"}"}{"}"} / Federated Identity</strong> mode, it means the cluster is utilizing features of {"{"}{"{"} gcp_first {"}"}{"}"} and OpenShift Container Platform to apply permissions in {"{"}{"{"} gcp_wid_short {"}"}{"}"} at an application level.</p>
      <p>The Cloud Credential Operator (CCO) is a cluster Operator installed by default in OpenShift Container Platform clusters running on cloud providers. Starting in OpenShift Container Platform 4.17, the CCO supports workflows for OLM-managed Operators with {"{"}{"{"} gcp_wid_short {"}"}{"}"}.</p>
      <p>For the purposes of {"{"}{"{"} gcp_wid_short {"}"}{"}"}, the CCO provides the following functions:</p>
      <ul>
      <li>Detects when it is running on an {"{"}{"{"} gcp_wid_short {"}"}{"}"}-enabled cluster</li>
      <li>Checks the <code>CredentialsRequest</code> object for the presence of fields that provide the required information for granting Operators access to {"{"}{"{"} gcp_short {"}"}{"}"} resources</li>
      </ul>
      <p>The CCO can semi-automate this process through an expanded use of <code>CredentialsRequest</code> objects, which can request the creation of <code>Secrets</code> that contain the information required for {"{"}{"{"} gcp_wid_short {"}"}{"}"} workflows.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Subscriptions with automatic approvals for updates are not recommended because there might be permission changes to make before updating. Subscriptions with manual approvals for updates ensure that administrators have the opportunity to verify the permissions of the later version, take any necessary steps, and then update.</p>
        </AlertDescription>
      </Alert>
      <p>As an Operator author preparing an Operator for use alongside the updated CCO in OpenShift Container Platform 4.17 and later, you should instruct users and add code to handle the divergence from earlier CCO versions, in addition to handling {"{"}{"{"} gcp_wid_short {"}"}{"}"} token authentication (if your Operator is not already enabled). The recommended method is to provide a <code>CredentialsRequest</code> object with the correctly filled {"{"}{"{"} gcp_wid_short {"}"}{"}"} fields and let the CCO create the <code>Secret</code> object for you.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>If you plan to support OpenShift Container Platform clusters earlier than version 4.17, consider providing users with instructions on how to manually create a secret with the {"{"}{"{"} gcp_wid_short {"}"}{"}"}-enabling information by using the CCO utility (<code>ccoctl</code>). Earlier CCO versions are unaware of {"{"}{"{"} gcp_wid_short {"}"}{"}"} mode on the cluster and cannot create secrets for you.</p>
          <p>Your code should check for secrets that never appear and warn users to follow the fallback instructions you have provided.</p>
        </AlertDescription>
      </Alert>
      <p>To authenticate with {"{"}{"{"} gcp_short {"}"}{"}"} using short-lived tokens via {"{"}{"{"} gcp_wid_first {"}"}{"}"}, Operators must provide the following information:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>AUDIENCE</code></dt>
      <dd class="ml-6 text-muted-foreground">
      <p>Created in {"{"}{"{"} gcp_short {"}"}{"}"} by the administrator when they set up {"{"}{"{"} gcp_wid_short {"}"}{"}"}, the <code>AUDIENCE</code> value must be a preformatted URL in the following format:</p>
      <CodeBlock code={"//iam.googleapis.com/projects/<project_number>/locations/global/workloadIdentityPools/<pool_id>/providers/<provider_id>"} lang="text" />
      </dd>
      <dt class="font-semibold"><code>SERVICE_ACCOUNT_EMAIL</code></dt>
      <dd class="ml-6 text-muted-foreground">
      <p>The <code>SERVICE_ACCOUNT_EMAIL</code> value is a {"{"}{"{"} gcp_short {"}"}{"}"} service account email that is impersonated during Operator operation, for example:</p>
      <CodeBlock code={"<service_account_name>@<project_id>.iam.gserviceaccount.com"} lang="text" />
      </dd>
      </dl>
      <p>The <strong>Install Operator</strong> page in the web console allows cluster administrators to provide this information at installation time. This information is then propagated to the <code>Subscription</code> object as environment variables on the Operator pod.</p>
      <p><strong>Additional resources</strong></p>
      <ul>
      <li><a href="/openshift-docs-markdown/authentication/managing_cloud_provider_credentials/cco-short-term-creds#cco-short-term-creds-gcp-olm_cco-short-term-creds">OLM-managed Operator support for authentication with {"{"}{"{"} gcp_wid_short {"}"}{"}"}</a></li>
      <li><a href="/openshift-docs-markdown/operators/admin/olm-adding-operators-to-cluster#olm-installing-from-software-catalog-using-web-console_olm-adding-operators-to-a-cluster">Installing from OperatorHub using the web console</a></li>
      <li><a href="/openshift-docs-markdown/operators/admin/olm-adding-operators-to-cluster#olm-installing-operator-from-software-catalog-using-cli_olm-adding-operators-to-a-cluster">Installing from OperatorHub using the CLI</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
