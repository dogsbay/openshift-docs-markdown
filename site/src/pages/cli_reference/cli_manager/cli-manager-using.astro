---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: cli_reference/cli_manager/cli-manager-using.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"cli-manager-using","text":"Using the CLI Manager Operator"},{"depth":2,"slug":"cli-manager-adding-plugins_cli-manager-using","text":"Installing CLI plugins with the CLI Manager Operator"},{"depth":2,"slug":"cli-manager-upgrading-plugin-yamls_cli-manager-using","text":"Upgrading a plugin with the CLI Manager Operator"},{"depth":2,"slug":"cli-manager-updating-plugin_cli-manager-using","text":"Updating CLI plugins with the CLI Manager Operator"},{"depth":2,"slug":"cli-manager-remove-plugin_cli-manager-using","text":"Uninstalling a CLI plugin with the CLI Manager Operator"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/cli_reference/cli_manager/cli-manager-using";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Using the CLI Manager Operator";
const description = "To install, update, and uninstall CLI plugins in OpenShift Container Platform, you can set up and configure the CLI Manager Operator." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["cli_reference","cli_reference/cli_manager"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Using the CLI Manager Operator {#cli-manager-using}\n\nTo install, update, and uninstall CLI plugins in OpenShift Container Platform, you can set up and configure the CLI Manager Operator.\n\n> [!IMPORTANT]\n> Using the CLI Manager Operator to install and manage plugins for the OpenShift CLI is a Technology Preview feature only. Technology Preview features are not supported with Red Hat production service level agreements (SLAs) and might not be functionally complete. Red Hat does not recommend using them in production. These features provide early access to upcoming product features, enabling customers to test functionality and provide feedback during the development process.\n>\n> For more information about the support scope of Red Hat Technology Preview features, see [Technology Preview Features Support Scope](https://access.redhat.com/support/offerings/techpreview/).\n\n## Installing CLI plugins with the CLI Manager Operator {#cli-manager-adding-plugins_cli-manager-using}\n\nYou can install CLI plugins with the CLI Manager Operator to extend OpenShift CLI functionality in both connected and disconnected environments.\n\n**Prerequisites**\n\n- You have installed Krew by following the [installation procedure](https://krew.sigs.k8s.io/docs/user-guide/setup/install/) in the Krew documentation.\n- The CLI Manager Operator is installed.\n- The CLI Manager Operator custom index has been added to Krew.\n- You are using OpenShift Container Platform 4.17 or later.\n\n**Procedure**\n\n1. To list all available plugins, run the following command:\n\n   ```terminal\n   $ oc krew search\n   ```\n2. To get information about a plugin, run the following command:\n\n   ```terminal\n   $ oc krew info <plugin_name>\n   ```\n3. To install a plugin, run the following command:\n\n   ```terminal\n   $ oc krew install <plugin_name>\n   ```\n4. To list all plugins that were installed by Krew, run the following command:\n\n   ```terminal\n   $ oc krew list\n   ```\n\n## Upgrading a plugin with the CLI Manager Operator {#cli-manager-upgrading-plugin-yamls_cli-manager-using}\n\nYou can upgrade a CLI plugin to a newer version with the CLI Manager Operator by directly editing the plugin’s resource YAML file.\n\n**Prerequisites**\n\n- You are logged in to OpenShift Container Platform as a user with the `cluster-admin` role.\n- The CLI Manager Operator is installed.\n- The plugin you are upgrading is installed.\n\n**Procedure**\n\n1. Using the CLI, enter the following command:\n\n   ```terminal\n   oc edit plugin/<plugin_name>\n   ```\n2. Edit the YAML file to include the new specifications for your plugin.\n\n   ```yaml {title=\"Example YAML file to upgrade a plugin\"}\n   apiVersion: config.openshift.io/v1alpha1\n   kind: Plugin\n   metadata:\n     name: <plugin_name> (1)\n   spec:\n     description: <description_of_plugin>\n     homepage: <plugin_homepage>\n     platforms:\n     - bin: (2)\n       files:\n       - from: <plugin_file_path>\n         to: .\n       image: <plugin_image>\n       imagePullSecret: (3)\n       platform: <platform> (4)\n     shortDescription: <short_description_of_plugin>\n     version: <version> (5)\n   ```\n\n   where:\n\n   `<plugin_name>`\n   :   Specifies the name of the plugin you plan to use in commands.\n\n   `bin`\n   :   Specifies the path to the plugin executable.\n\n   `imagePullSecret`\n   :   Optional field if the registry is not public to add a pull secret to access your plugin image.\n\n   `<platform>`\n   :   Add the architecture for your system; for example, `linux/amd64`, `darwin/arm64`, `windows/amd64`, or another architecture.\n\n   `<version>`\n   :   The version must be in v0.0.0 format.\n3. Save the file.\n\n## Updating CLI plugins with the CLI Manager Operator {#cli-manager-updating-plugin_cli-manager-using}\n\nYou can update a plugin that was installed for the OpenShift CLI (`oc`) with the CLI Manager Operator and Krew to keep your plugins current with the latest features.\n\n**Prerequisites**\n\n- You have installed Krew by following the [installation procedure](https://krew.sigs.k8s.io/docs/user-guide/setup/install/) in the Krew documentation.\n- The CLI Manager Operator is installed.\n- The custom index has been added to Krew by the cluster administrator.\n- The plugin updates have been added to the CLI Manager Operator by the cluster administrator.\n- The plugin you are updating is already installed.\n\n**Procedure**\n\n- To update a single plugin, run the following command:\n\n  ```terminal\n  $ oc krew upgrade <plugin_name>\n  ```\n- To update all plugins that were installed by Krew, run the following command:\n\n  ```terminal\n  $ oc krew upgrade\n  ```\n\n## Uninstalling a CLI plugin with the CLI Manager Operator {#cli-manager-remove-plugin_cli-manager-using}\n\nYou can uninstall a plugin that was installed for the OpenShift CLI (`oc`) with the CLI Manager Operator.\n\n**Prerequisites**\n\n- You have installed Krew by following the [installation procedure](https://krew.sigs.k8s.io/docs/user-guide/setup/install/) in the Krew documentation.\n- You have installed a plugin for the OpenShift CLI with the CLI Manager Operator.\n\n**Procedure**\n\n- To uninstall a plugin, run the following command:\n\n  ```terminal\n  $ oc krew uninstall <plugin_name>\n  ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/cli_reference/cli_manager/cli-manager-using.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="cli-manager-using" class="group scroll-mt-20">Using the CLI Manager Operator<a href="#cli-manager-using" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">To install, update, and uninstall CLI plugins in OpenShift Container Platform, you can set up and configure the CLI Manager Operator.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Using the CLI Manager Operator to install and manage plugins for the OpenShift CLI is a Technology Preview feature only. Technology Preview features are not supported with Red Hat production service level agreements (SLAs) and might not be functionally complete. Red Hat does not recommend using them in production. These features provide early access to upcoming product features, enabling customers to test functionality and provide feedback during the development process.</p>
          <p>For more information about the support scope of Red Hat Technology Preview features, see <a href="https://access.redhat.com/support/offerings/techpreview/">Technology Preview Features Support Scope</a>.</p>
        </AlertDescription>
      </Alert>
      <h2 id="cli-manager-adding-plugins_cli-manager-using" class="group scroll-mt-20">Installing CLI plugins with the CLI Manager Operator<a href="#cli-manager-adding-plugins_cli-manager-using" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can install CLI plugins with the CLI Manager Operator to extend OpenShift CLI functionality in both connected and disconnected environments.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have installed Krew by following the <a href="https://krew.sigs.k8s.io/docs/user-guide/setup/install/">installation procedure</a> in the Krew documentation.</li>
      <li>The CLI Manager Operator is installed.</li>
      <li>The CLI Manager Operator custom index has been added to Krew.</li>
      <li>You are using OpenShift Container Platform 4.17 or later.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      To list all available plugins, run the following command:
      <CodeBlock code={"$ oc krew search"} lang="terminal" />
      </li>
      <li>
      To get information about a plugin, run the following command:
      <CodeBlock code={"$ oc krew info <plugin_name>"} lang="terminal" />
      </li>
      <li>
      To install a plugin, run the following command:
      <CodeBlock code={"$ oc krew install <plugin_name>"} lang="terminal" />
      </li>
      <li>
      To list all plugins that were installed by Krew, run the following command:
      <CodeBlock code={"$ oc krew list"} lang="terminal" />
      </li>
      </ol>
      <h2 id="cli-manager-upgrading-plugin-yamls_cli-manager-using" class="group scroll-mt-20">Upgrading a plugin with the CLI Manager Operator<a href="#cli-manager-upgrading-plugin-yamls_cli-manager-using" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can upgrade a CLI plugin to a newer version with the CLI Manager Operator by directly editing the plugin’s resource YAML file.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You are logged in to OpenShift Container Platform as a user with the <code>cluster-admin</code> role.</li>
      <li>The CLI Manager Operator is installed.</li>
      <li>The plugin you are upgrading is installed.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Using the CLI, enter the following command:
      <CodeBlock code={"oc edit plugin/<plugin_name>"} lang="terminal" />
      </li>
      <li>
      Edit the YAML file to include the new specifications for your plugin.
      <CodeBlock code={"apiVersion: config.openshift.io/v1alpha1\nkind: Plugin\nmetadata:\n  name: <plugin_name> (1)\nspec:\n  description: <description_of_plugin>\n  homepage: <plugin_homepage>\n  platforms:\n  - bin: (2)\n    files:\n    - from: <plugin_file_path>\n      to: .\n    image: <plugin_image>\n    imagePullSecret: (3)\n    platform: <platform> (4)\n  shortDescription: <short_description_of_plugin>\n  version: <version> (5)"} lang="yaml" title="Example YAML file to upgrade a plugin" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;plugin_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the plugin you plan to use in commands.</p></dd>
      <dt class="font-semibold"><code>bin</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the path to the plugin executable.</p></dd>
      <dt class="font-semibold"><code>imagePullSecret</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional field if the registry is not public to add a pull secret to access your plugin image.</p></dd>
      <dt class="font-semibold"><code>&lt;platform&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Add the architecture for your system; for example, <code>linux/amd64</code>, <code>darwin/arm64</code>, <code>windows/amd64</code>, or another architecture.</p></dd>
      <dt class="font-semibold"><code>&lt;version&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>The version must be in v0.0.0 format.</p></dd>
      </dl>
      </li>
      <li>Save the file.</li>
      </ol>
      <h2 id="cli-manager-updating-plugin_cli-manager-using" class="group scroll-mt-20">Updating CLI plugins with the CLI Manager Operator<a href="#cli-manager-updating-plugin_cli-manager-using" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can update a plugin that was installed for the OpenShift CLI (<code>oc</code>) with the CLI Manager Operator and Krew to keep your plugins current with the latest features.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have installed Krew by following the <a href="https://krew.sigs.k8s.io/docs/user-guide/setup/install/">installation procedure</a> in the Krew documentation.</li>
      <li>The CLI Manager Operator is installed.</li>
      <li>The custom index has been added to Krew by the cluster administrator.</li>
      <li>The plugin updates have been added to the CLI Manager Operator by the cluster administrator.</li>
      <li>The plugin you are updating is already installed.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      To update a single plugin, run the following command:
      <CodeBlock code={"$ oc krew upgrade <plugin_name>"} lang="terminal" />
      </li>
      <li>
      To update all plugins that were installed by Krew, run the following command:
      <CodeBlock code={"$ oc krew upgrade"} lang="terminal" />
      </li>
      </ul>
      <h2 id="cli-manager-remove-plugin_cli-manager-using" class="group scroll-mt-20">Uninstalling a CLI plugin with the CLI Manager Operator<a href="#cli-manager-remove-plugin_cli-manager-using" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can uninstall a plugin that was installed for the OpenShift CLI (<code>oc</code>) with the CLI Manager Operator.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have installed Krew by following the <a href="https://krew.sigs.k8s.io/docs/user-guide/setup/install/">installation procedure</a> in the Krew documentation.</li>
      <li>You have installed a plugin for the OpenShift CLI with the CLI Manager Operator.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      To uninstall a plugin, run the following command:
      <CodeBlock code={"$ oc krew uninstall <plugin_name>"} lang="terminal" />
      </li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
