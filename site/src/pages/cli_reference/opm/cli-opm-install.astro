---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: cli_reference/opm/cli-opm-install.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"cli-opm-install","text":"Installing the opm CLI"},{"depth":2,"slug":"olm-about-opm_cli-opm-install","text":"About the opm CLI"},{"depth":2,"slug":"olm-installing-opm_cli-opm-install","text":"Installing the opm CLI"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/cli_reference/opm/cli-opm-install";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Installing the opm CLI";
const description = "The opm CLI is an Operator Framework tool for creating and maintaining Operator catalogs from bundle images in OpenShift Container Platform. You can use…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["cli_reference","cli_reference/opm"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Installing the opm CLI {#cli-opm-install}\n\n## About the opm CLI {#olm-about-opm_cli-opm-install}\n\nThe `opm` CLI is an Operator Framework tool for creating and maintaining Operator catalogs from bundle images in OpenShift Container Platform. You can use it to build catalog container images that Operator Lifecycle Manager (OLM) references through catalog sources.\n\nA catalog contains a database of pointers to Operator manifest content that can be queried through an included API that is served when the container image is run. On OpenShift Container Platform, Operator Lifecycle Manager (OLM) can reference the image in a catalog source, defined by a `CatalogSource` object, which polls the image at regular intervals to enable frequent updates to installed Operators on the cluster.\n\n**Additional resources**\n{._additional-resources}\n\n- See [Operator Framework packaging format](/openshift-docs-markdown/operators/understanding/olm-packaging-format#olm-bundle-format_olm-packaging-format) for more information about the bundle format.\n\n## Installing the opm CLI {#olm-installing-opm_cli-opm-install}\n\nYou can install the `opm` CLI tool on your Linux, macOS, or Windows workstation.\n\n**Prerequisites**\n\n- For Red Hat Enterprise Linux (RHEL) 9.0 and later, you must provide the following packages:\n\n  - `podman` version 1.9.3+ (version 2.0+ recommended)\n  - `glibc` version 2.28+\n\n**Procedure**\n\n1. Navigate to the [OpenShift mirror site](https://mirror.openshift.com/pub/openshift-v4/x86_64/clients/ocp/latest-4.22/) and download the latest version of the tarball that matches your operating system.\n2. Unpack the archive.\n\n   - For Linux or macOS:\n\n     ```terminal\n     $ tar xvf <file>\n     ```\n   - For Windows, unzip the archive with a ZIP program.\n3. Place the file anywhere in your `PATH`.\n\n   - For Linux or macOS:\n\n     1. Check your `PATH`:\n\n        ```terminal\n        $ echo $PATH\n        ```\n     2. Move the file. For example:\n\n        ```terminal\n        $ sudo mv ./opm /usr/local/bin/\n        ```\n   - For Windows:\n\n     1. Check your `PATH`:\n\n        ```terminal\n        C:\\> path\n        ```\n     2. Move the file:\n\n        ```terminal\n        C:\\> move opm.exe <directory>\n        ```\n\n**Verification**\n\n- After you install the `opm` CLI, verify that it is available:\n\n  ```terminal\n  $ opm version\n  ```\n\n**Additional resources**\n{._additional-resources}\n\n- See [Managing custom catalogs](/openshift-docs-markdown/operators/admin/olm-managing-custom-catalogs#olm-managing-custom-catalogs) for `opm` procedures including creating, updating, and pruning catalogs.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/cli_reference/opm/cli-opm-install.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="cli-opm-install" class="group scroll-mt-20">Installing the opm CLI<a href="#cli-opm-install" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <h2 id="olm-about-opm_cli-opm-install" class="group scroll-mt-20">About the opm CLI<a href="#olm-about-opm_cli-opm-install" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The <code>opm</code> CLI is an Operator Framework tool for creating and maintaining Operator catalogs from bundle images in OpenShift Container Platform. You can use it to build catalog container images that Operator Lifecycle Manager (OLM) references through catalog sources.</p>
      <p>A catalog contains a database of pointers to Operator manifest content that can be queried through an included API that is served when the container image is run. On OpenShift Container Platform, Operator Lifecycle Manager (OLM) can reference the image in a catalog source, defined by a <code>CatalogSource</code> object, which polls the image at regular intervals to enable frequent updates to installed Operators on the cluster.</p>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li>See <a href="/openshift-docs-markdown/operators/understanding/olm-packaging-format#olm-bundle-format_olm-packaging-format">Operator Framework packaging format</a> for more information about the bundle format.</li></ul>
      </section>
      <h2 id="olm-installing-opm_cli-opm-install" class="group scroll-mt-20">Installing the opm CLI<a href="#olm-installing-opm_cli-opm-install" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can install the <code>opm</code> CLI tool on your Linux, macOS, or Windows workstation.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>
      For Red Hat Enterprise Linux (RHEL) 9.0 and later, you must provide the following packages:
      <ul>
      <li><code>podman</code> version 1.9.3+ (version 2.0+ recommended)</li>
      <li><code>glibc</code> version 2.28+</li>
      </ul>
      </li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to the <a href="https://mirror.openshift.com/pub/openshift-v4/x86_64/clients/ocp/latest-4.22/">OpenShift mirror site</a> and download the latest version of the tarball that matches your operating system.</li>
      <li>
      Unpack the archive.
      <ul>
      <li>
      For Linux or macOS:
      <CodeBlock code={"$ tar xvf <file>"} lang="terminal" />
      </li>
      <li>For Windows, unzip the archive with a ZIP program.</li>
      </ul>
      </li>
      <li>
      Place the file anywhere in your <code>PATH</code>.
      <ul>
      <li>
      For Linux or macOS:
      <ol>
      <li>
      Check your <code>PATH</code>:
      <CodeBlock code={"$ echo $PATH"} lang="terminal" />
      </li>
      <li>
      Move the file. For example:
      <CodeBlock code={"$ sudo mv ./opm /usr/local/bin/"} lang="terminal" />
      </li>
      </ol>
      </li>
      <li>
      For Windows:
      <ol>
      <li>
      Check your <code>PATH</code>:
      <CodeBlock code={"C:\\> path"} lang="terminal" />
      </li>
      <li>
      Move the file:
      <CodeBlock code={"C:\\> move opm.exe <directory>"} lang="terminal" />
      </li>
      </ol>
      </li>
      </ul>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      After you install the <code>opm</code> CLI, verify that it is available:
      <CodeBlock code={"$ opm version"} lang="terminal" />
      </li>
      </ul>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li>See <a href="/openshift-docs-markdown/operators/admin/olm-managing-custom-catalogs#olm-managing-custom-catalogs">Managing custom catalogs</a> for <code>opm</code> procedures including creating, updating, and pruning catalogs.</li></ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
