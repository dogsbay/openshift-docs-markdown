---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: cli_reference/openshift_cli/extending-cli-plugins.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"cli-extend-plugins","text":"Extending the OpenShift CLI with plugins"},{"depth":2,"slug":"cli-writing-plugins_cli-extend-plugins","text":"Writing CLI plugins"},{"depth":2,"slug":"cli-installing-plugins_cli-extend-plugins","text":"Installing and using CLI plugins"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/cli_reference/openshift_cli/extending-cli-plugins";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Extending the OpenShift CLI with plugins";
const description = "You can write and install plugins to build on the default oc commands, allowing you to perform new and more complex tasks with the OpenShift CLI (oc)." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["cli_reference","cli_reference/openshift_cli"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Extending the OpenShift CLI with plugins {#cli-extend-plugins}\n\nYou can write and install plugins to build on the default `oc` commands, allowing you to perform new and more complex tasks with the OpenShift CLI (`oc`).\n\n## Writing CLI plugins {#cli-writing-plugins_cli-extend-plugins}\n\nYou can write a plugin for the OpenShift CLI (`oc`) in any programming language or script that allows you to write command-line commands. Note that you cannot use a plugin to overwrite an existing `oc` command.\n\nThis procedure creates a simple Bash plugin that prints a message to the terminal when the `oc foo` command is issued.\n\n**Procedure**\n\n1. Create a file called `oc-foo`.\n\n   When naming your plugin file, keep the following in mind:\n\n   - The file must begin with `oc-` or `kubectl-` to be recognized as a plugin.\n   - The file name determines the command that invokes the plugin. For example, a plugin with the file name `oc-foo-bar` can be invoked by a command of `oc foo bar`. You can also use underscores if you want the command to contain dashes. For example, a plugin with the file name `oc-foo_bar` can be invoked by a command of `oc foo-bar`.\n2. Add the following contents to the file.\n\n   ```bash\n   #!/bin/bash\n\n   # optional argument handling\n   if [[ \"$1\" == \"version\" ]]\n   then\n       echo \"1.0.0\"\n       exit 0\n   fi\n\n   # optional argument handling\n   if [[ \"$1\" == \"config\" ]]\n   then\n       echo $KUBECONFIG\n       exit 0\n   fi\n\n   echo \"I am a plugin named kubectl-foo\"\n   ```\n\n   After you install this plugin for the OpenShift CLI (`oc`), you can invoke it by using the `oc foo` command.\n\n- [Sample plugin repository](https://github.com/kubernetes/sample-cli-plugin)\n- [CLI runtime repository](https://github.com/kubernetes/cli-runtime/)\n\n## Installing and using CLI plugins {#cli-installing-plugins_cli-extend-plugins}\n\nAfter you write a custom plugin for the OpenShift CLI (`oc`), you must install the plugin before you can use it.\n\n**Prerequisites**\n\n- You must have the `oc` CLI tool installed.\n- You must have a CLI plugin file that begins with `oc-` or `kubectl-`.\n\n**Procedure**\n\n1. If necessary, update the plugin file to be executable.\n\n   ```terminal\n   $ chmod +x <plugin_file>\n   ```\n2. Place the file anywhere in your `PATH`, such as `/usr/local/bin/`.\n\n   ```terminal\n   $ sudo mv <plugin_file> /usr/local/bin/.\n   ```\n3. Run `oc plugin list` to make sure that the plugin is listed.\n\n   ```terminal\n   $ oc plugin list\n   ```\n\n   ```terminal {title=\"Example output\"}\n   The following compatible plugins are available:\n\n   /usr/local/bin/<plugin_file>\n   ```\n\n   If your plugin is not listed here, verify that the file begins with `oc-` or `kubectl-`, is executable, and is on your `PATH`.\n4. Invoke the new command or option introduced by the plugin.\n\n   For example, if you built and installed the `kubectl-ns` plugin from the [Sample plugin repository](https://github.com/kubernetes/sample-cli-plugin), you can use the following command to view the current namespace:\n\n   ```terminal\n   $ oc ns\n   ```\n\n   Note that the command to invoke the plugin depends on the plugin file name. For example, a plugin with the file name of `oc-foo-bar` is invoked by the `oc foo bar` command.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/cli_reference/openshift_cli/extending-cli-plugins.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="cli-extend-plugins" class="group scroll-mt-20">Extending the OpenShift CLI with plugins<a href="#cli-extend-plugins" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can write and install plugins to build on the default <code>oc</code> commands, allowing you to perform new and more complex tasks with the OpenShift CLI (<code>oc</code>).</p>
      <h2 id="cli-writing-plugins_cli-extend-plugins" class="group scroll-mt-20">Writing CLI plugins<a href="#cli-writing-plugins_cli-extend-plugins" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can write a plugin for the OpenShift CLI (<code>oc</code>) in any programming language or script that allows you to write command-line commands. Note that you cannot use a plugin to overwrite an existing <code>oc</code> command.</p>
      <p>This procedure creates a simple Bash plugin that prints a message to the terminal when the <code>oc foo</code> command is issued.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a file called <code>oc-foo</code>.
      <p>When naming your plugin file, keep the following in mind:</p>
      <ul>
      <li>The file must begin with <code>oc-</code> or <code>kubectl-</code> to be recognized as a plugin.</li>
      <li>The file name determines the command that invokes the plugin. For example, a plugin with the file name <code>oc-foo-bar</code> can be invoked by a command of <code>oc foo bar</code>. You can also use underscores if you want the command to contain dashes. For example, a plugin with the file name <code>oc-foo_bar</code> can be invoked by a command of <code>oc foo-bar</code>.</li>
      </ul>
      </li>
      <li>
      Add the following contents to the file.
      <CodeBlock code={"#!/bin/bash\n\n# optional argument handling\nif [[ \"$1\" == \"version\" ]]\nthen\n    echo \"1.0.0\"\n    exit 0\nfi\n\n# optional argument handling\nif [[ \"$1\" == \"config\" ]]\nthen\n    echo $KUBECONFIG\n    exit 0\nfi\n\necho \"I am a plugin named kubectl-foo\""} lang="bash" />
      <p>After you install this plugin for the OpenShift CLI (<code>oc</code>), you can invoke it by using the <code>oc foo</code> command.</p>
      </li>
      </ol>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="https://github.com/kubernetes/sample-cli-plugin">Sample plugin repository</a></li>
      <li><a href="https://github.com/kubernetes/cli-runtime/">CLI runtime repository</a></li>
      </ul>
      </section>
      <h2 id="cli-installing-plugins_cli-extend-plugins" class="group scroll-mt-20">Installing and using CLI plugins<a href="#cli-installing-plugins_cli-extend-plugins" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">After you write a custom plugin for the OpenShift CLI (<code>oc</code>), you must install the plugin before you can use it.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You must have the <code>oc</code> CLI tool installed.</li>
      <li>You must have a CLI plugin file that begins with <code>oc-</code> or <code>kubectl-</code>.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      If necessary, update the plugin file to be executable.
      <CodeBlock code={"$ chmod +x <plugin_file>"} lang="terminal" />
      </li>
      <li>
      Place the file anywhere in your <code>PATH</code>, such as <code>/usr/local/bin/</code>.
      <CodeBlock code={"$ sudo mv <plugin_file> /usr/local/bin/."} lang="terminal" />
      </li>
      <li>
      Run <code>oc plugin list</code> to make sure that the plugin is listed.
      <CodeBlock code={"$ oc plugin list"} lang="terminal" />
      <CodeBlock code={"The following compatible plugins are available:\n\n/usr/local/bin/<plugin_file>"} lang="terminal" title="Example output" />
      <p>If your plugin is not listed here, verify that the file begins with <code>oc-</code> or <code>kubectl-</code>, is executable, and is on your <code>PATH</code>.</p>
      </li>
      <li>
      Invoke the new command or option introduced by the plugin.
      <p>For example, if you built and installed the <code>kubectl-ns</code> plugin from the <a href="https://github.com/kubernetes/sample-cli-plugin">Sample plugin repository</a>, you can use the following command to view the current namespace:</p>
      <CodeBlock code={"$ oc ns"} lang="terminal" />
      <p>Note that the command to invoke the plugin depends on the plugin file name. For example, a plugin with the file name of <code>oc-foo-bar</code> is invoked by the <code>oc foo bar</code> command.</p>
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
