---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: cli_reference/openshift_cli/administrator-cli-commands.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"cli-administrator-commands","text":"OpenShift CLI administrator command reference"},{"depth":2,"slug":"openshift-cli-admin_cli-administrator-commands","text":"OpenShift CLI (oc) administrator commands"},{"depth":3,"slug":"_oc_adm_build-chain","text":"oc adm build-chain"},{"depth":3,"slug":"_oc_adm_catalog_mirror","text":"oc adm catalog mirror"},{"depth":3,"slug":"_oc_adm_certificate_approve","text":"oc adm certificate approve"},{"depth":3,"slug":"_oc_adm_certificate_deny","text":"oc adm certificate deny"},{"depth":3,"slug":"_oc_adm_copy-to-node","text":"oc adm copy-to-node"},{"depth":3,"slug":"_oc_adm_cordon","text":"oc adm cordon"},{"depth":3,"slug":"_oc_adm_create-bootstrap-project-template","text":"oc adm create-bootstrap-project-template"},{"depth":3,"slug":"_oc_adm_create-error-template","text":"oc adm create-error-template"},{"depth":3,"slug":"_oc_adm_create-login-template","text":"oc adm create-login-template"},{"depth":3,"slug":"_oc_adm_create-provider-selection-template","text":"oc adm create-provider-selection-template"},{"depth":3,"slug":"_oc_adm_drain","text":"oc adm drain"},{"depth":3,"slug":"_oc_adm_groups_add-users","text":"oc adm groups add-users"},{"depth":3,"slug":"_oc_adm_groups_new","text":"oc adm groups new"},{"depth":3,"slug":"_oc_adm_groups_prune","text":"oc adm groups prune"},{"depth":3,"slug":"_oc_adm_groups_remove-users","text":"oc adm groups remove-users"},{"depth":3,"slug":"_oc_adm_groups_sync","text":"oc adm groups sync"},{"depth":3,"slug":"_oc_adm_inspect","text":"oc adm inspect"},{"depth":3,"slug":"_oc_adm_migrate_icsp","text":"oc adm migrate icsp"},{"depth":3,"slug":"_oc_adm_migrate_template-instances","text":"oc adm migrate template-instances"},{"depth":3,"slug":"_oc_adm_must-gather","text":"oc adm must-gather"},{"depth":3,"slug":"_oc_adm_new-project","text":"oc adm new-project"},{"depth":3,"slug":"_oc_adm_node-image_create","text":"oc adm node-image create"},{"depth":3,"slug":"_oc_adm_node-image_monitor","text":"oc adm node-image monitor"},{"depth":3,"slug":"_oc_adm_node-logs","text":"oc adm node-logs"},{"depth":3,"slug":"_oc_adm_ocp-certificates_monitor-certificates","text":"oc adm ocp-certificates monitor-certificates"},{"depth":3,"slug":"_oc_adm_ocp-certificates_regenerate-leaf","text":"oc adm ocp-certificates regenerate-leaf"},{"depth":3,"slug":"_oc_adm_ocp-certificates_regenerate-machine-config-server-serving-cert","text":"oc adm ocp-certificates regenerate-machine-config-server-serving-cert"},{"depth":3,"slug":"_oc_adm_ocp-certificates_regenerate-top-level","text":"oc adm ocp-certificates regenerate-top-level"},{"depth":3,"slug":"_oc_adm_ocp-certificates_remove-old-trust","text":"oc adm ocp-certificates remove-old-trust"},{"depth":3,"slug":"_oc_adm_ocp-certificates_update-ignition-ca-bundle-for-machine-config-server","text":"oc adm ocp-certificates update-ignition-ca-bundle-for-machine-config-server"},{"depth":3,"slug":"_oc_adm_policy_add-cluster-role-to-group","text":"oc adm policy add-cluster-role-to-group"},{"depth":3,"slug":"_oc_adm_policy_add-cluster-role-to-user","text":"oc adm policy add-cluster-role-to-user"},{"depth":3,"slug":"_oc_adm_policy_add-role-to-user","text":"oc adm policy add-role-to-user"},{"depth":3,"slug":"_oc_adm_policy_add-scc-to-group","text":"oc adm policy add-scc-to-group"},{"depth":3,"slug":"_oc_adm_policy_add-scc-to-user","text":"oc adm policy add-scc-to-user"},{"depth":3,"slug":"_oc_adm_policy_remove-cluster-role-from-group","text":"oc adm policy remove-cluster-role-from-group"},{"depth":3,"slug":"_oc_adm_policy_remove-cluster-role-from-user","text":"oc adm policy remove-cluster-role-from-user"},{"depth":3,"slug":"_oc_adm_policy_scc-review","text":"oc adm policy scc-review"},{"depth":3,"slug":"_oc_adm_policy_scc-subject-review","text":"oc adm policy scc-subject-review"},{"depth":3,"slug":"_oc_adm_prune_builds","text":"oc adm prune builds"},{"depth":3,"slug":"_oc_adm_prune_deployments","text":"oc adm prune deployments"},{"depth":3,"slug":"_oc_adm_prune_groups","text":"oc adm prune groups"},{"depth":3,"slug":"_oc_adm_prune_images","text":"oc adm prune images"},{"depth":3,"slug":"_oc_adm_prune_renderedmachineconfigs","text":"oc adm prune renderedmachineconfigs"},{"depth":3,"slug":"_oc_adm_prune_renderedmachineconfigs_list","text":"oc adm prune renderedmachineconfigs list"},{"depth":3,"slug":"_oc_adm_reboot-machine-config-pool","text":"oc adm reboot-machine-config-pool"},{"depth":3,"slug":"_oc_adm_release_extract","text":"oc adm release extract"},{"depth":3,"slug":"_oc_adm_release_info","text":"oc adm release info"},{"depth":3,"slug":"_oc_adm_release_new","text":"oc adm release new"},{"depth":3,"slug":"_oc_adm_restart-kubelet","text":"oc adm restart-kubelet"},{"depth":3,"slug":"_oc_adm_taint","text":"oc adm taint"},{"depth":3,"slug":"_oc_adm_top_images","text":"oc adm top images"},{"depth":3,"slug":"_oc_adm_top_imagestreams","text":"oc adm top imagestreams"},{"depth":3,"slug":"_oc_adm_top_node","text":"oc adm top node"},{"depth":3,"slug":"_oc_adm_top_persistentvolumeclaims","text":"oc adm top persistentvolumeclaims"},{"depth":3,"slug":"_oc_adm_top_pod","text":"oc adm top pod"},{"depth":3,"slug":"_oc_adm_uncordon","text":"oc adm uncordon"},{"depth":3,"slug":"_oc_adm_upgrade","text":"oc adm upgrade"},{"depth":3,"slug":"_oc_adm_verify-image-signature","text":"oc adm verify-image-signature"},{"depth":3,"slug":"_oc_adm_wait-for-node-reboot","text":"oc adm wait-for-node-reboot"},{"depth":3,"slug":"_oc_adm_wait-for-stable-cluster","text":"oc adm wait-for-stable-cluster"},{"depth":2,"slug":"additional-resources_cli-administrator-commands","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/cli_reference/openshift_cli/administrator-cli-commands";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "OpenShift CLI administrator command reference";
const description = "To learn more about the OpenShift CLI (oc) administrator commands, review their descriptions and example commands. You must have cluster-admin or…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["cli_reference","cli_reference/openshift_cli"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# OpenShift CLI administrator command reference {#cli-administrator-commands}\n\nTo learn more about the OpenShift CLI (`oc`) administrator commands, review their descriptions and example commands. You must have `cluster-admin` or equivalent permissions to use these commands.\n\nFor developer commands, see the \"OpenShift CLI developer command reference\".\n\nRun `oc adm -h` to list all administrator commands or run `oc <command> --help` to get additional details for a specific command.\n\n## OpenShift CLI (oc) administrator commands {#openshift-cli-admin_cli-administrator-commands}\n\nReview the commands that cluster administrators can use to configure, maintain, and troubleshoot OpenShift Container Platform clusters.\n\n### oc adm build-chain {#_oc_adm_build-chain}\n\nOutput the inputs and dependencies of your builds\n\n```bash {title=\"Example usage\"}\n  # Build the dependency tree for the 'latest' tag in <image-stream>\n  oc adm build-chain <image-stream>\n\n  # Build the dependency tree for the 'v2' tag in dot format and visualize it via the dot utility\n  oc adm build-chain <image-stream>:v2 -o dot | dot -T svg -o deps.svg\n\n  # Build the dependency tree across all namespaces for the specified image stream tag found in the 'test' namespace\n  oc adm build-chain <image-stream> -n test --all\n```\n\n### oc adm catalog mirror {#_oc_adm_catalog_mirror}\n\nMirror an operator-registry catalog\n\n```bash {title=\"Example usage\"}\n  # Mirror an operator-registry image and its contents to a registry\n  oc adm catalog mirror quay.io/my/image:latest myregistry.com\n\n  # Mirror an operator-registry image and its contents to a particular namespace in a registry\n  oc adm catalog mirror quay.io/my/image:latest myregistry.com/my-namespace\n\n  # Mirror to an airgapped registry by first mirroring to files\n  oc adm catalog mirror quay.io/my/image:latest file:///local/index\n  oc adm catalog mirror file:///local/index/my/image:latest my-airgapped-registry.com\n\n  # Configure a cluster to use a mirrored registry\n  oc apply -f manifests/imageDigestMirrorSet.yaml\n\n  # Edit the mirroring mappings and mirror with \"oc image mirror\" manually\n  oc adm catalog mirror --manifests-only quay.io/my/image:latest myregistry.com\n  oc image mirror -f manifests/mapping.txt\n\n  # Delete all ImageDigestMirrorSets generated by oc adm catalog mirror\n  oc delete imagedigestmirrorset -l operators.openshift.org/catalog=true\n```\n\n### oc adm certificate approve {#_oc_adm_certificate_approve}\n\nApprove a certificate signing request\n\n```bash {title=\"Example usage\"}\n  # Approve CSR 'csr-sqgzp'\n  oc adm certificate approve csr-sqgzp\n```\n\n### oc adm certificate deny {#_oc_adm_certificate_deny}\n\nDeny a certificate signing request\n\n```bash {title=\"Example usage\"}\n  # Deny CSR 'csr-sqgzp'\n  oc adm certificate deny csr-sqgzp\n```\n\n### oc adm copy-to-node {#_oc_adm_copy-to-node}\n\nCopy specified files to the node\n\n```bash {title=\"Example usage\"}\n  # Copy a new bootstrap kubeconfig file to node-0\n  oc adm copy-to-node --copy=new-bootstrap-kubeconfig=/etc/kubernetes/kubeconfig node/node-0\n```\n\n### oc adm cordon {#_oc_adm_cordon}\n\nMark node as unschedulable\n\n```bash {title=\"Example usage\"}\n  # Mark node \"foo\" as unschedulable\n  oc adm cordon foo\n```\n\n### oc adm create-bootstrap-project-template {#_oc_adm_create-bootstrap-project-template}\n\nCreate a bootstrap project template\n\n```bash {title=\"Example usage\"}\n  # Output a bootstrap project template in YAML format to stdout\n  oc adm create-bootstrap-project-template -o yaml\n```\n\n### oc adm create-error-template {#_oc_adm_create-error-template}\n\nCreate an error page template\n\n```bash {title=\"Example usage\"}\n  # Output a template for the error page to stdout\n  oc adm create-error-template\n```\n\n### oc adm create-login-template {#_oc_adm_create-login-template}\n\nCreate a login template\n\n```bash {title=\"Example usage\"}\n  # Output a template for the login page to stdout\n  oc adm create-login-template\n```\n\n### oc adm create-provider-selection-template {#_oc_adm_create-provider-selection-template}\n\nCreate a provider selection template\n\n```bash {title=\"Example usage\"}\n  # Output a template for the provider selection page to stdout\n  oc adm create-provider-selection-template\n```\n\n### oc adm drain {#_oc_adm_drain}\n\nDrain node in preparation for maintenance\n\n```bash {title=\"Example usage\"}\n  # Drain node \"foo\", even if there are pods not managed by a replication controller, replica set, job, daemon set, or stateful set on it\n  oc adm drain foo --force\n\n  # As above, but abort if there are pods not managed by a replication controller, replica set, job, daemon set, or stateful set, and use a grace period of 15 minutes\n  oc adm drain foo --grace-period=900\n```\n\n### oc adm groups add-users {#_oc_adm_groups_add-users}\n\nAdd users to a group\n\n```bash {title=\"Example usage\"}\n  # Add user1 and user2 to my-group\n  oc adm groups add-users my-group user1 user2\n```\n\n### oc adm groups new {#_oc_adm_groups_new}\n\nCreate a new group\n\n```bash {title=\"Example usage\"}\n  # Add a group with no users\n  oc adm groups new my-group\n\n  # Add a group with two users\n  oc adm groups new my-group user1 user2\n\n  # Add a group with one user and shorter output\n  oc adm groups new my-group user1 -o name\n```\n\n### oc adm groups prune {#_oc_adm_groups_prune}\n\nRemove old OpenShift groups referencing missing records from an external provider\n\n```bash {title=\"Example usage\"}\n  # Prune all orphaned groups\n  oc adm groups prune --sync-config=/path/to/ldap-sync-config.yaml --confirm\n\n  # Prune all orphaned groups except the ones from the denylist file\n  oc adm groups prune --blacklist=/path/to/denylist.txt --sync-config=/path/to/ldap-sync-config.yaml --confirm\n\n  # Prune all orphaned groups from a list of specific groups specified in an allowlist file\n  oc adm groups prune --whitelist=/path/to/allowlist.txt --sync-config=/path/to/ldap-sync-config.yaml --confirm\n\n  # Prune all orphaned groups from a list of specific groups specified in a list\n  oc adm groups prune groups/group_name groups/other_name --sync-config=/path/to/ldap-sync-config.yaml --confirm\n```\n\n### oc adm groups remove-users {#_oc_adm_groups_remove-users}\n\nRemove users from a group\n\n```bash {title=\"Example usage\"}\n  # Remove user1 and user2 from my-group\n  oc adm groups remove-users my-group user1 user2\n```\n\n### oc adm groups sync {#_oc_adm_groups_sync}\n\nSync OpenShift groups with records from an external provider\n\n```bash {title=\"Example usage\"}\n  # Sync all groups with an LDAP server\n  oc adm groups sync --sync-config=/path/to/ldap-sync-config.yaml --confirm\n\n  # Sync all groups except the ones from the blacklist file with an LDAP server\n  oc adm groups sync --blacklist=/path/to/blacklist.txt --sync-config=/path/to/ldap-sync-config.yaml --confirm\n\n  # Sync specific groups specified in an allowlist file with an LDAP server\n  oc adm groups sync --whitelist=/path/to/allowlist.txt --sync-config=/path/to/sync-config.yaml --confirm\n\n  # Sync all OpenShift groups that have been synced previously with an LDAP server\n  oc adm groups sync --type=openshift --sync-config=/path/to/ldap-sync-config.yaml --confirm\n\n  # Sync specific OpenShift groups if they have been synced previously with an LDAP server\n  oc adm groups sync groups/group1 groups/group2 groups/group3 --sync-config=/path/to/sync-config.yaml --confirm\n```\n\n### oc adm inspect {#_oc_adm_inspect}\n\nCollect debugging data for a given resource\n\n```bash {title=\"Example usage\"}\n  # Collect debugging data for the \"openshift-apiserver\" clusteroperator\n  oc adm inspect clusteroperator/openshift-apiserver\n\n  # Collect debugging data for the \"openshift-apiserver\" and \"kube-apiserver\" clusteroperators\n  oc adm inspect clusteroperator/openshift-apiserver clusteroperator/kube-apiserver\n\n  # Collect debugging data for all clusteroperators\n  oc adm inspect clusteroperator\n\n  # Collect debugging data for all clusteroperators and clusterversions\n  oc adm inspect clusteroperators,clusterversions\n```\n\n### oc adm migrate icsp {#_oc_adm_migrate_icsp}\n\nUpdate imagecontentsourcepolicy file(s) to imagedigestmirrorset file(s)\n\n```bash {title=\"Example usage\"}\n  # Update the imagecontentsourcepolicy.yaml file to a new imagedigestmirrorset file under the mydir directory\n  oc adm migrate icsp imagecontentsourcepolicy.yaml --dest-dir mydir\n```\n\n### oc adm migrate template-instances {#_oc_adm_migrate_template-instances}\n\nUpdate template instances to point to the latest group-version-kinds\n\n```bash {title=\"Example usage\"}\n  # Perform a dry-run of updating all objects\n  oc adm migrate template-instances\n\n  # To actually perform the update, the confirm flag must be appended\n  oc adm migrate template-instances --confirm\n```\n\n### oc adm must-gather {#_oc_adm_must-gather}\n\nLaunch a new instance of a pod for gathering debug information\n\n```bash {title=\"Example usage\"}\n  # Gather information using the default plug-in image and command, writing into ./must-gather.local.<rand>\n  oc adm must-gather\n\n  # Gather information with a specific local folder to copy to\n  oc adm must-gather --dest-dir=/local/directory\n\n  # Gather audit information\n  oc adm must-gather -- /usr/bin/gather_audit_logs\n\n  # Gather information using multiple plug-in images\n  oc adm must-gather --image=quay.io/kubevirt/must-gather --image=quay.io/openshift/origin-must-gather\n\n  # Gather information using a specific image stream plug-in\n  oc adm must-gather --image-stream=openshift/must-gather:latest\n\n  # Gather information using a specific image, command, and pod directory\n  oc adm must-gather --image=my/image:tag --source-dir=/pod/directory -- myspecial-command.sh\n```\n\n### oc adm new-project {#_oc_adm_new-project}\n\nCreate a new project\n\n```bash {title=\"Example usage\"}\n  # Create a new project using a node selector\n  oc adm new-project myproject --node-selector='type=user-node,region=east'\n```\n\n### oc adm node-image create {#_oc_adm_node-image_create}\n\nCreate an ISO image for booting the nodes to be added to the target cluster\n\n```bash {title=\"Example usage\"}\n  # Create the ISO image and download it in the current folder\n  oc adm node-image create\n\n  # Use a different assets folder\n  oc adm node-image create --dir=/tmp/assets\n\n  # Specify a custom image name\n  oc adm node-image create -o=my-node.iso\n\n  # In place of an ISO, creates files that can be used for PXE boot\n  oc adm node-image create --pxe\n\n  # Create an ISO to add a single node without using the configuration file\n  oc adm node-image create --mac-address=00:d8:e7:c7:4b:bb\n\n  # Create an ISO to add a single node with a root device hint and without\n  # using the configuration file\n  oc adm node-image create --mac-address=00:d8:e7:c7:4b:bb --root-device-hint=deviceName:/dev/sda\n```\n\n### oc adm node-image monitor {#_oc_adm_node-image_monitor}\n\nMonitor new nodes being added to an OpenShift cluster\n\n```bash {title=\"Example usage\"}\n  # Monitor a single node being added to a cluster\n  oc adm node-image monitor --ip-addresses 192.168.111.83\n\n  # Monitor multiple nodes being added to a cluster by separating each\n  # IP address with a comma\n  oc adm node-image monitor --ip-addresses 192.168.111.83,192.168.111.84\n```\n\n### oc adm node-logs {#_oc_adm_node-logs}\n\nDisplay and filter node logs\n\n```bash {title=\"Example usage\"}\n  # Show kubelet logs from all control plane nodes\n  oc adm node-logs --role master -u kubelet\n\n  # See what logs are available in control plane nodes in /var/log\n  oc adm node-logs --role master --path=/\n\n  # Display cron log file from all control plane nodes\n  oc adm node-logs --role master --path=cron\n```\n\n### oc adm ocp-certificates monitor-certificates {#_oc_adm_ocp-certificates_monitor-certificates}\n\nWatch platform certificates\n\n```bash {title=\"Example usage\"}\n  # Watch platform certificates\n  oc adm ocp-certificates monitor-certificates\n```\n\n### oc adm ocp-certificates regenerate-leaf {#_oc_adm_ocp-certificates_regenerate-leaf}\n\nRegenerate client and serving certificates of an OpenShift cluster\n\n```bash {title=\"Example usage\"}\n  # Regenerate a leaf certificate contained in a particular secret\n  oc adm ocp-certificates regenerate-leaf -n openshift-config-managed secret/kube-controller-manager-client-cert-key\n```\n\n### oc adm ocp-certificates regenerate-machine-config-server-serving-cert {#_oc_adm_ocp-certificates_regenerate-machine-config-server-serving-cert}\n\nRegenerate the machine config operator certificates in an OpenShift cluster\n\n```bash {title=\"Example usage\"}\n  # Regenerate the MCO certs without modifying user-data secrets\n  oc adm ocp-certificates regenerate-machine-config-server-serving-cert --update-ignition=false\n\n  # Update the user-data secrets to use new MCS certs\n  oc adm ocp-certificates update-ignition-ca-bundle-for-machine-config-server\n```\n\n### oc adm ocp-certificates regenerate-top-level {#_oc_adm_ocp-certificates_regenerate-top-level}\n\nRegenerate the top level certificates in an OpenShift cluster\n\n```bash {title=\"Example usage\"}\n  # Regenerate the signing certificate contained in a particular secret\n  oc adm ocp-certificates regenerate-top-level -n openshift-kube-apiserver-operator secret/loadbalancer-serving-signer-key\n```\n\n### oc adm ocp-certificates remove-old-trust {#_oc_adm_ocp-certificates_remove-old-trust}\n\nRemove old CAs from ConfigMaps representing platform trust bundles in an OpenShift cluster\n\n```bash {title=\"Example usage\"}\n  # Remove a trust bundled contained in a particular config map\n  oc adm ocp-certificates remove-old-trust -n openshift-config-managed configmaps/kube-apiserver-aggregator-client-ca --created-before 2023-06-05T14:44:06Z\n\n  #  Remove only CA certificates created before a certain date from all trust bundles\n  oc adm ocp-certificates remove-old-trust configmaps -A --all --created-before 2023-06-05T14:44:06Z\n```\n\n### oc adm ocp-certificates update-ignition-ca-bundle-for-machine-config-server {#_oc_adm_ocp-certificates_update-ignition-ca-bundle-for-machine-config-server}\n\nUpdate user-data secrets in an OpenShift cluster to use updated MCO certfs\n\n```bash {title=\"Example usage\"}\n  # Regenerate the MCO certs without modifying user-data secrets\n  oc adm ocp-certificates regenerate-machine-config-server-serving-cert --update-ignition=false\n\n  # Update the user-data secrets to use new MCS certs\n  oc adm ocp-certificates update-ignition-ca-bundle-for-machine-config-server\n```\n\n### oc adm policy add-cluster-role-to-group {#_oc_adm_policy_add-cluster-role-to-group}\n\nAdd a role to groups for all projects in the cluster\n\n```bash {title=\"Example usage\"}\n  # Add the 'cluster-admin' cluster role to the 'cluster-admins' group\n  oc adm policy add-cluster-role-to-group cluster-admin cluster-admins\n```\n\n### oc adm policy add-cluster-role-to-user {#_oc_adm_policy_add-cluster-role-to-user}\n\nAdd a role to users for all projects in the cluster\n\n```bash {title=\"Example usage\"}\n  # Add the 'system:build-strategy-docker' cluster role to the 'devuser' user\n  oc adm policy add-cluster-role-to-user system:build-strategy-docker devuser\n```\n\n### oc adm policy add-role-to-user {#_oc_adm_policy_add-role-to-user}\n\nAdd a role to users or service accounts for the current project\n\n```bash {title=\"Example usage\"}\n  # Add the 'view' role to user1 for the current project\n  oc adm policy add-role-to-user view user1\n\n  # Add the 'edit' role to serviceaccount1 for the current project\n  oc adm policy add-role-to-user edit -z serviceaccount1\n```\n\n### oc adm policy add-scc-to-group {#_oc_adm_policy_add-scc-to-group}\n\nAdd a security context constraint to groups\n\n```bash {title=\"Example usage\"}\n  # Add the 'restricted' security context constraint to group1 and group2\n  oc adm policy add-scc-to-group restricted group1 group2\n```\n\n### oc adm policy add-scc-to-user {#_oc_adm_policy_add-scc-to-user}\n\nAdd a security context constraint to users or a service account\n\n```bash {title=\"Example usage\"}\n  # Add the 'restricted' security context constraint to user1 and user2\n  oc adm policy add-scc-to-user restricted user1 user2\n\n  # Add the 'privileged' security context constraint to serviceaccount1 in the current namespace\n  oc adm policy add-scc-to-user privileged -z serviceaccount1\n```\n\n### oc adm policy remove-cluster-role-from-group {#_oc_adm_policy_remove-cluster-role-from-group}\n\nRemove a role from groups for all projects in the cluster\n\n```bash {title=\"Example usage\"}\n  # Remove the 'cluster-admin' cluster role from the 'cluster-admins' group\n  oc adm policy remove-cluster-role-from-group cluster-admin cluster-admins\n```\n\n### oc adm policy remove-cluster-role-from-user {#_oc_adm_policy_remove-cluster-role-from-user}\n\nRemove a role from users for all projects in the cluster\n\n```bash {title=\"Example usage\"}\n  # Remove the 'system:build-strategy-docker' cluster role from the 'devuser' user\n  oc adm policy remove-cluster-role-from-user system:build-strategy-docker devuser\n```\n\n### oc adm policy scc-review {#_oc_adm_policy_scc-review}\n\nCheck which service account can create a pod\n\n```bash {title=\"Example usage\"}\n  # Check whether service accounts sa1 and sa2 can admit a pod with a template pod spec specified in my_resource.yaml\n  # Service Account specified in myresource.yaml file is ignored\n  oc adm policy scc-review -z sa1,sa2 -f my_resource.yaml\n\n  # Check whether service accounts system:serviceaccount:bob:default can admit a pod with a template pod spec specified in my_resource.yaml\n  oc adm policy scc-review -z system:serviceaccount:bob:default -f my_resource.yaml\n\n  # Check whether the service account specified in my_resource_with_sa.yaml can admit the pod\n  oc adm policy scc-review -f my_resource_with_sa.yaml\n\n  # Check whether the default service account can admit the pod; default is taken since no service account is defined in myresource_with_no_sa.yaml\n  oc adm policy scc-review -f myresource_with_no_sa.yaml\n```\n\n### oc adm policy scc-subject-review {#_oc_adm_policy_scc-subject-review}\n\nCheck whether a user or a service account can create a pod\n\n```bash {title=\"Example usage\"}\n  # Check whether user bob can create a pod specified in myresource.yaml\n  oc adm policy scc-subject-review -u bob -f myresource.yaml\n\n  # Check whether user bob who belongs to projectAdmin group can create a pod specified in myresource.yaml\n  oc adm policy scc-subject-review -u bob -g projectAdmin -f myresource.yaml\n\n  # Check whether a service account specified in the pod template spec in myresourcewithsa.yaml can create the pod\n  oc adm policy scc-subject-review -f myresourcewithsa.yaml\n```\n\n### oc adm prune builds {#_oc_adm_prune_builds}\n\nRemove old completed and failed builds\n\n```bash {title=\"Example usage\"}\n  # Dry run deleting older completed and failed builds and also including\n  # all builds whose associated build config no longer exists\n  oc adm prune builds --orphans\n\n  # To actually perform the prune operation, the confirm flag must be appended\n  oc adm prune builds --orphans --confirm\n```\n\n### oc adm prune deployments {#_oc_adm_prune_deployments}\n\nRemove old completed and failed deployment configs\n\n```bash {title=\"Example usage\"}\n  # Dry run deleting all but the last complete deployment for every deployment config\n  oc adm prune deployments --keep-complete=1\n\n  # To actually perform the prune operation, the confirm flag must be appended\n  oc adm prune deployments --keep-complete=1 --confirm\n```\n\n### oc adm prune groups {#_oc_adm_prune_groups}\n\nRemove old OpenShift groups referencing missing records from an external provider\n\n```bash {title=\"Example usage\"}\n  # Prune all orphaned groups\n  oc adm prune groups --sync-config=/path/to/ldap-sync-config.yaml --confirm\n\n  # Prune all orphaned groups except the ones from the denylist file\n  oc adm prune groups --blacklist=/path/to/denylist.txt --sync-config=/path/to/ldap-sync-config.yaml --confirm\n\n  # Prune all orphaned groups from a list of specific groups specified in an allowlist file\n  oc adm prune groups --whitelist=/path/to/allowlist.txt --sync-config=/path/to/ldap-sync-config.yaml --confirm\n\n  # Prune all orphaned groups from a list of specific groups specified in a list\n  oc adm prune groups groups/group_name groups/other_name --sync-config=/path/to/ldap-sync-config.yaml --confirm\n```\n\n### oc adm prune images {#_oc_adm_prune_images}\n\nRemove unreferenced images\n\n```bash {title=\"Example usage\"}\n  # See what the prune command would delete if only images and their referrers were more than an hour old\n  # and obsoleted by 3 newer revisions under the same tag were considered\n  oc adm prune images --keep-tag-revisions=3 --keep-younger-than=60m\n\n  # To actually perform the prune operation, the confirm flag must be appended\n  oc adm prune images --keep-tag-revisions=3 --keep-younger-than=60m --confirm\n\n  # See what the prune command would delete if we are interested in removing images\n  # exceeding currently set limit ranges ('openshift.io/Image')\n  oc adm prune images --prune-over-size-limit\n\n  # To actually perform the prune operation, the confirm flag must be appended\n  oc adm prune images --prune-over-size-limit --confirm\n\n  # Force the insecure HTTP protocol with the particular registry host name\n  oc adm prune images --registry-url=http://registry.example.org --confirm\n\n  # Force a secure connection with a custom certificate authority to the particular registry host name\n  oc adm prune images --registry-url=registry.example.org --certificate-authority=/path/to/custom/ca.crt --confirm\n```\n\n### oc adm prune renderedmachineconfigs {#_oc_adm_prune_renderedmachineconfigs}\n\nPrunes rendered MachineConfigs in an OpenShift cluster\n\n```bash {title=\"Example usage\"}\n  # See what the prune command would delete if run with no options\n  oc adm prune renderedmachineconfigs\n\n  # To actually perform the prune operation, the confirm flag must be appended\n  oc adm prune renderedmachineconfigs --confirm\n\n  # See what the prune command would delete if run on the worker MachineConfigPool\n  oc adm prune renderedmachineconfigs --pool-name=worker\n\n  # Prunes 10 oldest rendered MachineConfigs in the cluster\n  oc adm prune renderedmachineconfigs --count=10 --confirm\n\n  # Prunes 10 oldest rendered MachineConfigs in the cluster for the worker MachineConfigPool\n  oc adm prune renderedmachineconfigs --count=10 --pool-name=worker --confirm\n```\n\n### oc adm prune renderedmachineconfigs list {#_oc_adm_prune_renderedmachineconfigs_list}\n\nLists rendered MachineConfigs in an OpenShift cluster\n\n```bash {title=\"Example usage\"}\n  # List all rendered MachineConfigs for the worker MachineConfigPool in the cluster\n  oc adm prune renderedmachineconfigs list --pool-name=worker\n\n  # List all rendered MachineConfigs in use by the cluster's MachineConfigPools\n  oc adm prune renderedmachineconfigs list --in-use\n```\n\n### oc adm reboot-machine-config-pool {#_oc_adm_reboot-machine-config-pool}\n\nInitiate reboot of the specified MachineConfigPool\n\n```bash {title=\"Example usage\"}\n  # Reboot all MachineConfigPools\n  oc adm reboot-machine-config-pool mcp/worker mcp/master\n\n  # Reboot all MachineConfigPools that inherit from worker.  This include all custom MachineConfigPools and infra.\n  oc adm reboot-machine-config-pool mcp/worker\n\n  # Reboot masters\n  oc adm reboot-machine-config-pool mcp/master\n```\n\n### oc adm release extract {#_oc_adm_release_extract}\n\nExtract the contents of an update payload to disk\n\n```bash {title=\"Example usage\"}\n  # Use git to check out the source code for the current cluster release to DIR\n  oc adm release extract --git=DIR\n\n  # Extract cloud credential requests for AWS\n  oc adm release extract --credentials-requests --cloud=aws\n\n  # Use git to check out the source code for the current cluster release to DIR from linux/s390x image\n  # Note: Wildcard filter is not supported; pass a single os/arch to extract\n  oc adm release extract --git=DIR quay.io/openshift-release-dev/ocp-release:4.11.2 --filter-by-os=linux/s390x\n```\n\n### oc adm release info {#_oc_adm_release_info}\n\nDisplay information about a release\n\n```bash {title=\"Example usage\"}\n  # Show information about the cluster's current release\n  oc adm release info\n\n  # Show the source code that comprises a release\n  oc adm release info 4.11.2 --commit-urls\n\n  # Show the source code difference between two releases\n  oc adm release info 4.11.0 4.11.2 --commits\n\n  # Show where the images referenced by the release are located\n  oc adm release info quay.io/openshift-release-dev/ocp-release:4.11.2 --pullspecs\n\n  # Show information about linux/s390x image\n  # Note: Wildcard filter is not supported; pass a single os/arch to extract\n  oc adm release info quay.io/openshift-release-dev/ocp-release:4.11.2 --filter-by-os=linux/s390x\n```\n\n### oc adm release new {#_oc_adm_release_new}\n\nCreate a new OpenShift release\n\n```bash {title=\"Example usage\"}\n  # Create a release from the latest origin images and push to a DockerHub repository\n  oc adm release new --from-image-stream=4.11 -n origin --to-image docker.io/mycompany/myrepo:latest\n\n  # Create a new release with updated metadata from a previous release\n  oc adm release new --from-release registry.ci.openshift.org/origin/release:v4.11 --name 4.11.1 \\\n  --previous 4.11.0 --metadata ... --to-image docker.io/mycompany/myrepo:latest\n\n  # Create a new release and override a single image\n  oc adm release new --from-release registry.ci.openshift.org/origin/release:v4.11 \\\n  cli=docker.io/mycompany/cli:latest --to-image docker.io/mycompany/myrepo:latest\n\n  # Run a verification pass to ensure the release can be reproduced\n  oc adm release new --from-release registry.ci.openshift.org/origin/release:v4.11\n```\n\n### oc adm restart-kubelet {#_oc_adm_restart-kubelet}\n\nRestart kubelet on the specified nodes\n\n```bash {title=\"Example usage\"}\n  # Restart all the nodes, 10% at a time\n  oc adm restart-kubelet nodes --all --directive=RemoveKubeletKubeconfig\n\n  # Restart all the nodes, 20 nodes at a time\n  oc adm restart-kubelet nodes --all --parallelism=20 --directive=RemoveKubeletKubeconfig\n\n  # Restart all the nodes, 15% at a time\n  oc adm restart-kubelet nodes --all --parallelism=15% --directive=RemoveKubeletKubeconfig\n\n  # Restart all the masters at the same time\n  oc adm restart-kubelet nodes -l node-role.kubernetes.io/master --parallelism=100% --directive=RemoveKubeletKubeconfig\n```\n\n### oc adm taint {#_oc_adm_taint}\n\nUpdate the taints on one or more nodes\n\n```bash {title=\"Example usage\"}\n  # Update node 'foo' with a taint with key 'dedicated' and value 'special-user' and effect 'NoSchedule'\n  # If a taint with that key and effect already exists, its value is replaced as specified\n  oc adm taint nodes foo dedicated=special-user:NoSchedule\n\n  # Remove from node 'foo' the taint with key 'dedicated' and effect 'NoSchedule' if one exists\n  oc adm taint nodes foo dedicated:NoSchedule-\n\n  # Remove from node 'foo' all the taints with key 'dedicated'\n  oc adm taint nodes foo dedicated-\n\n  # Add a taint with key 'dedicated' on nodes having label myLabel=X\n  oc adm taint node -l myLabel=X  dedicated=foo:PreferNoSchedule\n\n  # Add to node 'foo' a taint with key 'bar' and no value\n  oc adm taint nodes foo bar:NoSchedule\n```\n\n### oc adm top images {#_oc_adm_top_images}\n\nShow usage statistics for images\n\n```bash {title=\"Example usage\"}\n  # Show usage statistics for images\n  oc adm top images\n```\n\n### oc adm top imagestreams {#_oc_adm_top_imagestreams}\n\nShow usage statistics for image streams\n\n```bash {title=\"Example usage\"}\n  # Show usage statistics for image streams\n  oc adm top imagestreams\n```\n\n### oc adm top node {#_oc_adm_top_node}\n\nDisplay resource (CPU/memory) usage of nodes\n\n```bash {title=\"Example usage\"}\n  # Show metrics for all nodes\n  oc adm top node\n\n  # Show metrics for a given node\n  oc adm top node NODE_NAME\n```\n\n### oc adm top persistentvolumeclaims {#_oc_adm_top_persistentvolumeclaims}\n\nExperimental: Show usage statistics for bound persistentvolumeclaims\n\n```bash {title=\"Example usage\"}\n  # Show usage statistics for all the bound persistentvolumeclaims across the cluster\n  oc adm top persistentvolumeclaims -A\n\n  # Show usage statistics for all the bound persistentvolumeclaims in a specific namespace\n  oc adm top persistentvolumeclaims -n default\n\n  # Show usage statistics for specific bound persistentvolumeclaims\n  oc adm top persistentvolumeclaims database-pvc app-pvc -n default\n```\n\n### oc adm top pod {#_oc_adm_top_pod}\n\nDisplay resource (CPU/memory) usage of pods\n\n```bash {title=\"Example usage\"}\n  # Show metrics for all pods in the default namespace\n  oc adm top pod\n\n  # Show metrics for all pods in the given namespace\n  oc adm top pod --namespace=NAMESPACE\n\n  # Show metrics for a given pod and its containers\n  oc adm top pod POD_NAME --containers\n\n  # Show metrics for the pods defined by label name=myLabel\n  oc adm top pod -l name=myLabel\n```\n\n### oc adm uncordon {#_oc_adm_uncordon}\n\nMark node as schedulable\n\n```bash {title=\"Example usage\"}\n  # Mark node \"foo\" as schedulable\n  oc adm uncordon foo\n```\n\n### oc adm upgrade {#_oc_adm_upgrade}\n\nUpgrade a cluster or adjust the upgrade channel\n\n```bash {title=\"Example usage\"}\n  # View the update status and available cluster updates\n  oc adm upgrade\n\n  # Update to the latest version\n  oc adm upgrade --to-latest=true\n```\n\n### oc adm verify-image-signature {#_oc_adm_verify-image-signature}\n\nVerify the image identity contained in the image signature\n\n```bash {title=\"Example usage\"}\n  # Verify the image signature and identity using the local GPG keychain\n  oc adm verify-image-signature sha256:c841e9b64e4579bd56c794bdd7c36e1c257110fd2404bebbb8b613e4935228c4 \\\n  --expected-identity=registry.local:5000/foo/bar:v1\n\n  # Verify the image signature and identity using the local GPG keychain and save the status\n  oc adm verify-image-signature sha256:c841e9b64e4579bd56c794bdd7c36e1c257110fd2404bebbb8b613e4935228c4 \\\n  --expected-identity=registry.local:5000/foo/bar:v1 --save\n\n  # Verify the image signature and identity via exposed registry route\n  oc adm verify-image-signature sha256:c841e9b64e4579bd56c794bdd7c36e1c257110fd2404bebbb8b613e4935228c4 \\\n  --expected-identity=registry.local:5000/foo/bar:v1 \\\n  --registry-url=docker-registry.foo.com\n\n  # Remove all signature verifications from the image\n  oc adm verify-image-signature sha256:c841e9b64e4579bd56c794bdd7c36e1c257110fd2404bebbb8b613e4935228c4 --remove-all\n```\n\n### oc adm wait-for-node-reboot {#_oc_adm_wait-for-node-reboot}\n\nWait for nodes to reboot after running `oc adm reboot-machine-config-pool`\n\n```bash {title=\"Example usage\"}\n  # Wait for all nodes to complete a requested reboot from 'oc adm reboot-machine-config-pool mcp/worker mcp/master'\n  oc adm wait-for-node-reboot nodes --all\n\n  # Wait for masters to complete a requested reboot from 'oc adm reboot-machine-config-pool mcp/master'\n  oc adm wait-for-node-reboot nodes -l node-role.kubernetes.io/master\n\n  # Wait for masters to complete a specific reboot\n  oc adm wait-for-node-reboot nodes -l node-role.kubernetes.io/master --reboot-number=4\n```\n\n### oc adm wait-for-stable-cluster {#_oc_adm_wait-for-stable-cluster}\n\nWait for the platform operators to become stable\n\n```bash {title=\"Example usage\"}\n  # Wait for all cluster operators to become stable\n  oc adm wait-for-stable-cluster\n\n  # Consider operators to be stable if they report as such for 5 minutes straight\n  oc adm wait-for-stable-cluster --minimum-stable-period 5m\n```\n\n## Additional resources {#additional-resources_cli-administrator-commands}\n\n- [OpenShift CLI developer command reference](/openshift-docs-markdown/cli_reference/openshift_cli/developer-cli-commands#cli-developer-commands)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/cli_reference/openshift_cli/administrator-cli-commands.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="cli-administrator-commands" class="group scroll-mt-20">OpenShift CLI administrator command reference<a href="#cli-administrator-commands" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>To learn more about the OpenShift CLI (<code>oc</code>) administrator commands, review their descriptions and example commands. You must have <code>cluster-admin</code> or equivalent permissions to use these commands.</p>
      <p>For developer commands, see the "OpenShift CLI developer command reference".</p>
      <p>Run <code>oc adm -h</code> to list all administrator commands or run <code>oc &lt;command&gt; --help</code> to get additional details for a specific command.</p>
      <h2 id="openshift-cli-admin_cli-administrator-commands" class="group scroll-mt-20">OpenShift CLI (oc) administrator commands<a href="#openshift-cli-admin_cli-administrator-commands" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Review the commands that cluster administrators can use to configure, maintain, and troubleshoot OpenShift Container Platform clusters.</p>
      <h3 id="_oc_adm_build-chain" class="group scroll-mt-20">oc adm build-chain<a href="#_oc_adm_build-chain" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Output the inputs and dependencies of your builds</p>
      <CodeBlock code={"  # Build the dependency tree for the 'latest' tag in <image-stream>\n  oc adm build-chain <image-stream>\n  \n  # Build the dependency tree for the 'v2' tag in dot format and visualize it via the dot utility\n  oc adm build-chain <image-stream>:v2 -o dot | dot -T svg -o deps.svg\n  \n  # Build the dependency tree across all namespaces for the specified image stream tag found in the 'test' namespace\n  oc adm build-chain <image-stream> -n test --all"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_catalog_mirror" class="group scroll-mt-20">oc adm catalog mirror<a href="#_oc_adm_catalog_mirror" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Mirror an operator-registry catalog</p>
      <CodeBlock code={"  # Mirror an operator-registry image and its contents to a registry\n  oc adm catalog mirror quay.io/my/image:latest myregistry.com\n  \n  # Mirror an operator-registry image and its contents to a particular namespace in a registry\n  oc adm catalog mirror quay.io/my/image:latest myregistry.com/my-namespace\n  \n  # Mirror to an airgapped registry by first mirroring to files\n  oc adm catalog mirror quay.io/my/image:latest file:///local/index\n  oc adm catalog mirror file:///local/index/my/image:latest my-airgapped-registry.com\n  \n  # Configure a cluster to use a mirrored registry\n  oc apply -f manifests/imageDigestMirrorSet.yaml\n  \n  # Edit the mirroring mappings and mirror with \"oc image mirror\" manually\n  oc adm catalog mirror --manifests-only quay.io/my/image:latest myregistry.com\n  oc image mirror -f manifests/mapping.txt\n  \n  # Delete all ImageDigestMirrorSets generated by oc adm catalog mirror\n  oc delete imagedigestmirrorset -l operators.openshift.org/catalog=true"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_certificate_approve" class="group scroll-mt-20">oc adm certificate approve<a href="#_oc_adm_certificate_approve" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Approve a certificate signing request</p>
      <CodeBlock code={"  # Approve CSR 'csr-sqgzp'\n  oc adm certificate approve csr-sqgzp"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_certificate_deny" class="group scroll-mt-20">oc adm certificate deny<a href="#_oc_adm_certificate_deny" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Deny a certificate signing request</p>
      <CodeBlock code={"  # Deny CSR 'csr-sqgzp'\n  oc adm certificate deny csr-sqgzp"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_copy-to-node" class="group scroll-mt-20">oc adm copy-to-node<a href="#_oc_adm_copy-to-node" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Copy specified files to the node</p>
      <CodeBlock code={"  # Copy a new bootstrap kubeconfig file to node-0\n  oc adm copy-to-node --copy=new-bootstrap-kubeconfig=/etc/kubernetes/kubeconfig node/node-0"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_cordon" class="group scroll-mt-20">oc adm cordon<a href="#_oc_adm_cordon" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Mark node as unschedulable</p>
      <CodeBlock code={"  # Mark node \"foo\" as unschedulable\n  oc adm cordon foo"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_create-bootstrap-project-template" class="group scroll-mt-20">oc adm create-bootstrap-project-template<a href="#_oc_adm_create-bootstrap-project-template" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Create a bootstrap project template</p>
      <CodeBlock code={"  # Output a bootstrap project template in YAML format to stdout\n  oc adm create-bootstrap-project-template -o yaml"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_create-error-template" class="group scroll-mt-20">oc adm create-error-template<a href="#_oc_adm_create-error-template" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Create an error page template</p>
      <CodeBlock code={"  # Output a template for the error page to stdout\n  oc adm create-error-template"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_create-login-template" class="group scroll-mt-20">oc adm create-login-template<a href="#_oc_adm_create-login-template" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Create a login template</p>
      <CodeBlock code={"  # Output a template for the login page to stdout\n  oc adm create-login-template"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_create-provider-selection-template" class="group scroll-mt-20">oc adm create-provider-selection-template<a href="#_oc_adm_create-provider-selection-template" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Create a provider selection template</p>
      <CodeBlock code={"  # Output a template for the provider selection page to stdout\n  oc adm create-provider-selection-template"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_drain" class="group scroll-mt-20">oc adm drain<a href="#_oc_adm_drain" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Drain node in preparation for maintenance</p>
      <CodeBlock code={"  # Drain node \"foo\", even if there are pods not managed by a replication controller, replica set, job, daemon set, or stateful set on it\n  oc adm drain foo --force\n  \n  # As above, but abort if there are pods not managed by a replication controller, replica set, job, daemon set, or stateful set, and use a grace period of 15 minutes\n  oc adm drain foo --grace-period=900"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_groups_add-users" class="group scroll-mt-20">oc adm groups add-users<a href="#_oc_adm_groups_add-users" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Add users to a group</p>
      <CodeBlock code={"  # Add user1 and user2 to my-group\n  oc adm groups add-users my-group user1 user2"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_groups_new" class="group scroll-mt-20">oc adm groups new<a href="#_oc_adm_groups_new" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Create a new group</p>
      <CodeBlock code={"  # Add a group with no users\n  oc adm groups new my-group\n  \n  # Add a group with two users\n  oc adm groups new my-group user1 user2\n  \n  # Add a group with one user and shorter output\n  oc adm groups new my-group user1 -o name"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_groups_prune" class="group scroll-mt-20">oc adm groups prune<a href="#_oc_adm_groups_prune" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Remove old OpenShift groups referencing missing records from an external provider</p>
      <CodeBlock code={"  # Prune all orphaned groups\n  oc adm groups prune --sync-config=/path/to/ldap-sync-config.yaml --confirm\n  \n  # Prune all orphaned groups except the ones from the denylist file\n  oc adm groups prune --blacklist=/path/to/denylist.txt --sync-config=/path/to/ldap-sync-config.yaml --confirm\n  \n  # Prune all orphaned groups from a list of specific groups specified in an allowlist file\n  oc adm groups prune --whitelist=/path/to/allowlist.txt --sync-config=/path/to/ldap-sync-config.yaml --confirm\n  \n  # Prune all orphaned groups from a list of specific groups specified in a list\n  oc adm groups prune groups/group_name groups/other_name --sync-config=/path/to/ldap-sync-config.yaml --confirm"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_groups_remove-users" class="group scroll-mt-20">oc adm groups remove-users<a href="#_oc_adm_groups_remove-users" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Remove users from a group</p>
      <CodeBlock code={"  # Remove user1 and user2 from my-group\n  oc adm groups remove-users my-group user1 user2"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_groups_sync" class="group scroll-mt-20">oc adm groups sync<a href="#_oc_adm_groups_sync" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Sync OpenShift groups with records from an external provider</p>
      <CodeBlock code={"  # Sync all groups with an LDAP server\n  oc adm groups sync --sync-config=/path/to/ldap-sync-config.yaml --confirm\n  \n  # Sync all groups except the ones from the blacklist file with an LDAP server\n  oc adm groups sync --blacklist=/path/to/blacklist.txt --sync-config=/path/to/ldap-sync-config.yaml --confirm\n  \n  # Sync specific groups specified in an allowlist file with an LDAP server\n  oc adm groups sync --whitelist=/path/to/allowlist.txt --sync-config=/path/to/sync-config.yaml --confirm\n  \n  # Sync all OpenShift groups that have been synced previously with an LDAP server\n  oc adm groups sync --type=openshift --sync-config=/path/to/ldap-sync-config.yaml --confirm\n  \n  # Sync specific OpenShift groups if they have been synced previously with an LDAP server\n  oc adm groups sync groups/group1 groups/group2 groups/group3 --sync-config=/path/to/sync-config.yaml --confirm"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_inspect" class="group scroll-mt-20">oc adm inspect<a href="#_oc_adm_inspect" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Collect debugging data for a given resource</p>
      <CodeBlock code={"  # Collect debugging data for the \"openshift-apiserver\" clusteroperator\n  oc adm inspect clusteroperator/openshift-apiserver\n  \n  # Collect debugging data for the \"openshift-apiserver\" and \"kube-apiserver\" clusteroperators\n  oc adm inspect clusteroperator/openshift-apiserver clusteroperator/kube-apiserver\n  \n  # Collect debugging data for all clusteroperators\n  oc adm inspect clusteroperator\n  \n  # Collect debugging data for all clusteroperators and clusterversions\n  oc adm inspect clusteroperators,clusterversions"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_migrate_icsp" class="group scroll-mt-20">oc adm migrate icsp<a href="#_oc_adm_migrate_icsp" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Update imagecontentsourcepolicy file(s) to imagedigestmirrorset file(s)</p>
      <CodeBlock code={"  # Update the imagecontentsourcepolicy.yaml file to a new imagedigestmirrorset file under the mydir directory\n  oc adm migrate icsp imagecontentsourcepolicy.yaml --dest-dir mydir"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_migrate_template-instances" class="group scroll-mt-20">oc adm migrate template-instances<a href="#_oc_adm_migrate_template-instances" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Update template instances to point to the latest group-version-kinds</p>
      <CodeBlock code={"  # Perform a dry-run of updating all objects\n  oc adm migrate template-instances\n  \n  # To actually perform the update, the confirm flag must be appended\n  oc adm migrate template-instances --confirm"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_must-gather" class="group scroll-mt-20">oc adm must-gather<a href="#_oc_adm_must-gather" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Launch a new instance of a pod for gathering debug information</p>
      <CodeBlock code={"  # Gather information using the default plug-in image and command, writing into ./must-gather.local.<rand>\n  oc adm must-gather\n  \n  # Gather information with a specific local folder to copy to\n  oc adm must-gather --dest-dir=/local/directory\n  \n  # Gather audit information\n  oc adm must-gather -- /usr/bin/gather_audit_logs\n  \n  # Gather information using multiple plug-in images\n  oc adm must-gather --image=quay.io/kubevirt/must-gather --image=quay.io/openshift/origin-must-gather\n  \n  # Gather information using a specific image stream plug-in\n  oc adm must-gather --image-stream=openshift/must-gather:latest\n  \n  # Gather information using a specific image, command, and pod directory\n  oc adm must-gather --image=my/image:tag --source-dir=/pod/directory -- myspecial-command.sh"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_new-project" class="group scroll-mt-20">oc adm new-project<a href="#_oc_adm_new-project" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Create a new project</p>
      <CodeBlock code={"  # Create a new project using a node selector\n  oc adm new-project myproject --node-selector='type=user-node,region=east'"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_node-image_create" class="group scroll-mt-20">oc adm node-image create<a href="#_oc_adm_node-image_create" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Create an ISO image for booting the nodes to be added to the target cluster</p>
      <CodeBlock code={"  # Create the ISO image and download it in the current folder\n  oc adm node-image create\n  \n  # Use a different assets folder\n  oc adm node-image create --dir=/tmp/assets\n  \n  # Specify a custom image name\n  oc adm node-image create -o=my-node.iso\n  \n  # In place of an ISO, creates files that can be used for PXE boot\n  oc adm node-image create --pxe\n  \n  # Create an ISO to add a single node without using the configuration file\n  oc adm node-image create --mac-address=00:d8:e7:c7:4b:bb\n  \n  # Create an ISO to add a single node with a root device hint and without\n  # using the configuration file\n  oc adm node-image create --mac-address=00:d8:e7:c7:4b:bb --root-device-hint=deviceName:/dev/sda"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_node-image_monitor" class="group scroll-mt-20">oc adm node-image monitor<a href="#_oc_adm_node-image_monitor" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Monitor new nodes being added to an OpenShift cluster</p>
      <CodeBlock code={"  # Monitor a single node being added to a cluster\n  oc adm node-image monitor --ip-addresses 192.168.111.83\n  \n  # Monitor multiple nodes being added to a cluster by separating each\n  # IP address with a comma\n  oc adm node-image monitor --ip-addresses 192.168.111.83,192.168.111.84"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_node-logs" class="group scroll-mt-20">oc adm node-logs<a href="#_oc_adm_node-logs" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Display and filter node logs</p>
      <CodeBlock code={"  # Show kubelet logs from all control plane nodes\n  oc adm node-logs --role master -u kubelet\n  \n  # See what logs are available in control plane nodes in /var/log\n  oc adm node-logs --role master --path=/\n  \n  # Display cron log file from all control plane nodes\n  oc adm node-logs --role master --path=cron"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_ocp-certificates_monitor-certificates" class="group scroll-mt-20">oc adm ocp-certificates monitor-certificates<a href="#_oc_adm_ocp-certificates_monitor-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Watch platform certificates</p>
      <CodeBlock code={"  # Watch platform certificates\n  oc adm ocp-certificates monitor-certificates"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_ocp-certificates_regenerate-leaf" class="group scroll-mt-20">oc adm ocp-certificates regenerate-leaf<a href="#_oc_adm_ocp-certificates_regenerate-leaf" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Regenerate client and serving certificates of an OpenShift cluster</p>
      <CodeBlock code={"  # Regenerate a leaf certificate contained in a particular secret\n  oc adm ocp-certificates regenerate-leaf -n openshift-config-managed secret/kube-controller-manager-client-cert-key"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_ocp-certificates_regenerate-machine-config-server-serving-cert" class="group scroll-mt-20">oc adm ocp-certificates regenerate-machine-config-server-serving-cert<a href="#_oc_adm_ocp-certificates_regenerate-machine-config-server-serving-cert" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Regenerate the machine config operator certificates in an OpenShift cluster</p>
      <CodeBlock code={"  # Regenerate the MCO certs without modifying user-data secrets\n  oc adm ocp-certificates regenerate-machine-config-server-serving-cert --update-ignition=false\n  \n  # Update the user-data secrets to use new MCS certs\n  oc adm ocp-certificates update-ignition-ca-bundle-for-machine-config-server"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_ocp-certificates_regenerate-top-level" class="group scroll-mt-20">oc adm ocp-certificates regenerate-top-level<a href="#_oc_adm_ocp-certificates_regenerate-top-level" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Regenerate the top level certificates in an OpenShift cluster</p>
      <CodeBlock code={"  # Regenerate the signing certificate contained in a particular secret\n  oc adm ocp-certificates regenerate-top-level -n openshift-kube-apiserver-operator secret/loadbalancer-serving-signer-key"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_ocp-certificates_remove-old-trust" class="group scroll-mt-20">oc adm ocp-certificates remove-old-trust<a href="#_oc_adm_ocp-certificates_remove-old-trust" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Remove old CAs from ConfigMaps representing platform trust bundles in an OpenShift cluster</p>
      <CodeBlock code={"  # Remove a trust bundled contained in a particular config map\n  oc adm ocp-certificates remove-old-trust -n openshift-config-managed configmaps/kube-apiserver-aggregator-client-ca --created-before 2023-06-05T14:44:06Z\n  \n  #  Remove only CA certificates created before a certain date from all trust bundles\n  oc adm ocp-certificates remove-old-trust configmaps -A --all --created-before 2023-06-05T14:44:06Z"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_ocp-certificates_update-ignition-ca-bundle-for-machine-config-server" class="group scroll-mt-20">oc adm ocp-certificates update-ignition-ca-bundle-for-machine-config-server<a href="#_oc_adm_ocp-certificates_update-ignition-ca-bundle-for-machine-config-server" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Update user-data secrets in an OpenShift cluster to use updated MCO certfs</p>
      <CodeBlock code={"  # Regenerate the MCO certs without modifying user-data secrets\n  oc adm ocp-certificates regenerate-machine-config-server-serving-cert --update-ignition=false\n  \n  # Update the user-data secrets to use new MCS certs\n  oc adm ocp-certificates update-ignition-ca-bundle-for-machine-config-server"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_policy_add-cluster-role-to-group" class="group scroll-mt-20">oc adm policy add-cluster-role-to-group<a href="#_oc_adm_policy_add-cluster-role-to-group" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Add a role to groups for all projects in the cluster</p>
      <CodeBlock code={"  # Add the 'cluster-admin' cluster role to the 'cluster-admins' group\n  oc adm policy add-cluster-role-to-group cluster-admin cluster-admins"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_policy_add-cluster-role-to-user" class="group scroll-mt-20">oc adm policy add-cluster-role-to-user<a href="#_oc_adm_policy_add-cluster-role-to-user" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Add a role to users for all projects in the cluster</p>
      <CodeBlock code={"  # Add the 'system:build-strategy-docker' cluster role to the 'devuser' user\n  oc adm policy add-cluster-role-to-user system:build-strategy-docker devuser"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_policy_add-role-to-user" class="group scroll-mt-20">oc adm policy add-role-to-user<a href="#_oc_adm_policy_add-role-to-user" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Add a role to users or service accounts for the current project</p>
      <CodeBlock code={"  # Add the 'view' role to user1 for the current project\n  oc adm policy add-role-to-user view user1\n  \n  # Add the 'edit' role to serviceaccount1 for the current project\n  oc adm policy add-role-to-user edit -z serviceaccount1"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_policy_add-scc-to-group" class="group scroll-mt-20">oc adm policy add-scc-to-group<a href="#_oc_adm_policy_add-scc-to-group" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Add a security context constraint to groups</p>
      <CodeBlock code={"  # Add the 'restricted' security context constraint to group1 and group2\n  oc adm policy add-scc-to-group restricted group1 group2"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_policy_add-scc-to-user" class="group scroll-mt-20">oc adm policy add-scc-to-user<a href="#_oc_adm_policy_add-scc-to-user" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Add a security context constraint to users or a service account</p>
      <CodeBlock code={"  # Add the 'restricted' security context constraint to user1 and user2\n  oc adm policy add-scc-to-user restricted user1 user2\n  \n  # Add the 'privileged' security context constraint to serviceaccount1 in the current namespace\n  oc adm policy add-scc-to-user privileged -z serviceaccount1"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_policy_remove-cluster-role-from-group" class="group scroll-mt-20">oc adm policy remove-cluster-role-from-group<a href="#_oc_adm_policy_remove-cluster-role-from-group" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Remove a role from groups for all projects in the cluster</p>
      <CodeBlock code={"  # Remove the 'cluster-admin' cluster role from the 'cluster-admins' group\n  oc adm policy remove-cluster-role-from-group cluster-admin cluster-admins"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_policy_remove-cluster-role-from-user" class="group scroll-mt-20">oc adm policy remove-cluster-role-from-user<a href="#_oc_adm_policy_remove-cluster-role-from-user" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Remove a role from users for all projects in the cluster</p>
      <CodeBlock code={"  # Remove the 'system:build-strategy-docker' cluster role from the 'devuser' user\n  oc adm policy remove-cluster-role-from-user system:build-strategy-docker devuser"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_policy_scc-review" class="group scroll-mt-20">oc adm policy scc-review<a href="#_oc_adm_policy_scc-review" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Check which service account can create a pod</p>
      <CodeBlock code={"  # Check whether service accounts sa1 and sa2 can admit a pod with a template pod spec specified in my_resource.yaml\n  # Service Account specified in myresource.yaml file is ignored\n  oc adm policy scc-review -z sa1,sa2 -f my_resource.yaml\n  \n  # Check whether service accounts system:serviceaccount:bob:default can admit a pod with a template pod spec specified in my_resource.yaml\n  oc adm policy scc-review -z system:serviceaccount:bob:default -f my_resource.yaml\n  \n  # Check whether the service account specified in my_resource_with_sa.yaml can admit the pod\n  oc adm policy scc-review -f my_resource_with_sa.yaml\n  \n  # Check whether the default service account can admit the pod; default is taken since no service account is defined in myresource_with_no_sa.yaml\n  oc adm policy scc-review -f myresource_with_no_sa.yaml"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_policy_scc-subject-review" class="group scroll-mt-20">oc adm policy scc-subject-review<a href="#_oc_adm_policy_scc-subject-review" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Check whether a user or a service account can create a pod</p>
      <CodeBlock code={"  # Check whether user bob can create a pod specified in myresource.yaml\n  oc adm policy scc-subject-review -u bob -f myresource.yaml\n  \n  # Check whether user bob who belongs to projectAdmin group can create a pod specified in myresource.yaml\n  oc adm policy scc-subject-review -u bob -g projectAdmin -f myresource.yaml\n  \n  # Check whether a service account specified in the pod template spec in myresourcewithsa.yaml can create the pod\n  oc adm policy scc-subject-review -f myresourcewithsa.yaml"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_prune_builds" class="group scroll-mt-20">oc adm prune builds<a href="#_oc_adm_prune_builds" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Remove old completed and failed builds</p>
      <CodeBlock code={"  # Dry run deleting older completed and failed builds and also including\n  # all builds whose associated build config no longer exists\n  oc adm prune builds --orphans\n  \n  # To actually perform the prune operation, the confirm flag must be appended\n  oc adm prune builds --orphans --confirm"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_prune_deployments" class="group scroll-mt-20">oc adm prune deployments<a href="#_oc_adm_prune_deployments" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Remove old completed and failed deployment configs</p>
      <CodeBlock code={"  # Dry run deleting all but the last complete deployment for every deployment config\n  oc adm prune deployments --keep-complete=1\n  \n  # To actually perform the prune operation, the confirm flag must be appended\n  oc adm prune deployments --keep-complete=1 --confirm"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_prune_groups" class="group scroll-mt-20">oc adm prune groups<a href="#_oc_adm_prune_groups" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Remove old OpenShift groups referencing missing records from an external provider</p>
      <CodeBlock code={"  # Prune all orphaned groups\n  oc adm prune groups --sync-config=/path/to/ldap-sync-config.yaml --confirm\n  \n  # Prune all orphaned groups except the ones from the denylist file\n  oc adm prune groups --blacklist=/path/to/denylist.txt --sync-config=/path/to/ldap-sync-config.yaml --confirm\n  \n  # Prune all orphaned groups from a list of specific groups specified in an allowlist file\n  oc adm prune groups --whitelist=/path/to/allowlist.txt --sync-config=/path/to/ldap-sync-config.yaml --confirm\n  \n  # Prune all orphaned groups from a list of specific groups specified in a list\n  oc adm prune groups groups/group_name groups/other_name --sync-config=/path/to/ldap-sync-config.yaml --confirm"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_prune_images" class="group scroll-mt-20">oc adm prune images<a href="#_oc_adm_prune_images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Remove unreferenced images</p>
      <CodeBlock code={"  # See what the prune command would delete if only images and their referrers were more than an hour old\n  # and obsoleted by 3 newer revisions under the same tag were considered\n  oc adm prune images --keep-tag-revisions=3 --keep-younger-than=60m\n  \n  # To actually perform the prune operation, the confirm flag must be appended\n  oc adm prune images --keep-tag-revisions=3 --keep-younger-than=60m --confirm\n  \n  # See what the prune command would delete if we are interested in removing images\n  # exceeding currently set limit ranges ('openshift.io/Image')\n  oc adm prune images --prune-over-size-limit\n  \n  # To actually perform the prune operation, the confirm flag must be appended\n  oc adm prune images --prune-over-size-limit --confirm\n  \n  # Force the insecure HTTP protocol with the particular registry host name\n  oc adm prune images --registry-url=http://registry.example.org --confirm\n  \n  # Force a secure connection with a custom certificate authority to the particular registry host name\n  oc adm prune images --registry-url=registry.example.org --certificate-authority=/path/to/custom/ca.crt --confirm"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_prune_renderedmachineconfigs" class="group scroll-mt-20">oc adm prune renderedmachineconfigs<a href="#_oc_adm_prune_renderedmachineconfigs" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Prunes rendered MachineConfigs in an OpenShift cluster</p>
      <CodeBlock code={"  # See what the prune command would delete if run with no options\n  oc adm prune renderedmachineconfigs\n  \n  # To actually perform the prune operation, the confirm flag must be appended\n  oc adm prune renderedmachineconfigs --confirm\n  \n  # See what the prune command would delete if run on the worker MachineConfigPool\n  oc adm prune renderedmachineconfigs --pool-name=worker\n  \n  # Prunes 10 oldest rendered MachineConfigs in the cluster\n  oc adm prune renderedmachineconfigs --count=10 --confirm\n  \n  # Prunes 10 oldest rendered MachineConfigs in the cluster for the worker MachineConfigPool\n  oc adm prune renderedmachineconfigs --count=10 --pool-name=worker --confirm"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_prune_renderedmachineconfigs_list" class="group scroll-mt-20">oc adm prune renderedmachineconfigs list<a href="#_oc_adm_prune_renderedmachineconfigs_list" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Lists rendered MachineConfigs in an OpenShift cluster</p>
      <CodeBlock code={"  # List all rendered MachineConfigs for the worker MachineConfigPool in the cluster\n  oc adm prune renderedmachineconfigs list --pool-name=worker\n  \n  # List all rendered MachineConfigs in use by the cluster's MachineConfigPools\n  oc adm prune renderedmachineconfigs list --in-use"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_reboot-machine-config-pool" class="group scroll-mt-20">oc adm reboot-machine-config-pool<a href="#_oc_adm_reboot-machine-config-pool" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Initiate reboot of the specified MachineConfigPool</p>
      <CodeBlock code={"  # Reboot all MachineConfigPools\n  oc adm reboot-machine-config-pool mcp/worker mcp/master\n  \n  # Reboot all MachineConfigPools that inherit from worker.  This include all custom MachineConfigPools and infra.\n  oc adm reboot-machine-config-pool mcp/worker\n  \n  # Reboot masters\n  oc adm reboot-machine-config-pool mcp/master"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_release_extract" class="group scroll-mt-20">oc adm release extract<a href="#_oc_adm_release_extract" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Extract the contents of an update payload to disk</p>
      <CodeBlock code={"  # Use git to check out the source code for the current cluster release to DIR\n  oc adm release extract --git=DIR\n  \n  # Extract cloud credential requests for AWS\n  oc adm release extract --credentials-requests --cloud=aws\n  \n  # Use git to check out the source code for the current cluster release to DIR from linux/s390x image\n  # Note: Wildcard filter is not supported; pass a single os/arch to extract\n  oc adm release extract --git=DIR quay.io/openshift-release-dev/ocp-release:4.11.2 --filter-by-os=linux/s390x"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_release_info" class="group scroll-mt-20">oc adm release info<a href="#_oc_adm_release_info" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Display information about a release</p>
      <CodeBlock code={"  # Show information about the cluster's current release\n  oc adm release info\n  \n  # Show the source code that comprises a release\n  oc adm release info 4.11.2 --commit-urls\n  \n  # Show the source code difference between two releases\n  oc adm release info 4.11.0 4.11.2 --commits\n  \n  # Show where the images referenced by the release are located\n  oc adm release info quay.io/openshift-release-dev/ocp-release:4.11.2 --pullspecs\n  \n  # Show information about linux/s390x image\n  # Note: Wildcard filter is not supported; pass a single os/arch to extract\n  oc adm release info quay.io/openshift-release-dev/ocp-release:4.11.2 --filter-by-os=linux/s390x"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_release_new" class="group scroll-mt-20">oc adm release new<a href="#_oc_adm_release_new" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Create a new OpenShift release</p>
      <CodeBlock code={"  # Create a release from the latest origin images and push to a DockerHub repository\n  oc adm release new --from-image-stream=4.11 -n origin --to-image docker.io/mycompany/myrepo:latest\n  \n  # Create a new release with updated metadata from a previous release\n  oc adm release new --from-release registry.ci.openshift.org/origin/release:v4.11 --name 4.11.1 \\\n  --previous 4.11.0 --metadata ... --to-image docker.io/mycompany/myrepo:latest\n  \n  # Create a new release and override a single image\n  oc adm release new --from-release registry.ci.openshift.org/origin/release:v4.11 \\\n  cli=docker.io/mycompany/cli:latest --to-image docker.io/mycompany/myrepo:latest\n  \n  # Run a verification pass to ensure the release can be reproduced\n  oc adm release new --from-release registry.ci.openshift.org/origin/release:v4.11"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_restart-kubelet" class="group scroll-mt-20">oc adm restart-kubelet<a href="#_oc_adm_restart-kubelet" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Restart kubelet on the specified nodes</p>
      <CodeBlock code={"  # Restart all the nodes, 10% at a time\n  oc adm restart-kubelet nodes --all --directive=RemoveKubeletKubeconfig\n  \n  # Restart all the nodes, 20 nodes at a time\n  oc adm restart-kubelet nodes --all --parallelism=20 --directive=RemoveKubeletKubeconfig\n  \n  # Restart all the nodes, 15% at a time\n  oc adm restart-kubelet nodes --all --parallelism=15% --directive=RemoveKubeletKubeconfig\n  \n  # Restart all the masters at the same time\n  oc adm restart-kubelet nodes -l node-role.kubernetes.io/master --parallelism=100% --directive=RemoveKubeletKubeconfig"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_taint" class="group scroll-mt-20">oc adm taint<a href="#_oc_adm_taint" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Update the taints on one or more nodes</p>
      <CodeBlock code={"  # Update node 'foo' with a taint with key 'dedicated' and value 'special-user' and effect 'NoSchedule'\n  # If a taint with that key and effect already exists, its value is replaced as specified\n  oc adm taint nodes foo dedicated=special-user:NoSchedule\n  \n  # Remove from node 'foo' the taint with key 'dedicated' and effect 'NoSchedule' if one exists\n  oc adm taint nodes foo dedicated:NoSchedule-\n  \n  # Remove from node 'foo' all the taints with key 'dedicated'\n  oc adm taint nodes foo dedicated-\n  \n  # Add a taint with key 'dedicated' on nodes having label myLabel=X\n  oc adm taint node -l myLabel=X  dedicated=foo:PreferNoSchedule\n  \n  # Add to node 'foo' a taint with key 'bar' and no value\n  oc adm taint nodes foo bar:NoSchedule"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_top_images" class="group scroll-mt-20">oc adm top images<a href="#_oc_adm_top_images" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Show usage statistics for images</p>
      <CodeBlock code={"  # Show usage statistics for images\n  oc adm top images"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_top_imagestreams" class="group scroll-mt-20">oc adm top imagestreams<a href="#_oc_adm_top_imagestreams" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Show usage statistics for image streams</p>
      <CodeBlock code={"  # Show usage statistics for image streams\n  oc adm top imagestreams"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_top_node" class="group scroll-mt-20">oc adm top node<a href="#_oc_adm_top_node" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Display resource (CPU/memory) usage of nodes</p>
      <CodeBlock code={"  # Show metrics for all nodes\n  oc adm top node\n  \n  # Show metrics for a given node\n  oc adm top node NODE_NAME"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_top_persistentvolumeclaims" class="group scroll-mt-20">oc adm top persistentvolumeclaims<a href="#_oc_adm_top_persistentvolumeclaims" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Experimental: Show usage statistics for bound persistentvolumeclaims</p>
      <CodeBlock code={"  # Show usage statistics for all the bound persistentvolumeclaims across the cluster\n  oc adm top persistentvolumeclaims -A\n  \n  # Show usage statistics for all the bound persistentvolumeclaims in a specific namespace\n  oc adm top persistentvolumeclaims -n default\n  \n  # Show usage statistics for specific bound persistentvolumeclaims\n  oc adm top persistentvolumeclaims database-pvc app-pvc -n default"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_top_pod" class="group scroll-mt-20">oc adm top pod<a href="#_oc_adm_top_pod" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Display resource (CPU/memory) usage of pods</p>
      <CodeBlock code={"  # Show metrics for all pods in the default namespace\n  oc adm top pod\n  \n  # Show metrics for all pods in the given namespace\n  oc adm top pod --namespace=NAMESPACE\n  \n  # Show metrics for a given pod and its containers\n  oc adm top pod POD_NAME --containers\n  \n  # Show metrics for the pods defined by label name=myLabel\n  oc adm top pod -l name=myLabel"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_uncordon" class="group scroll-mt-20">oc adm uncordon<a href="#_oc_adm_uncordon" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Mark node as schedulable</p>
      <CodeBlock code={"  # Mark node \"foo\" as schedulable\n  oc adm uncordon foo"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_upgrade" class="group scroll-mt-20">oc adm upgrade<a href="#_oc_adm_upgrade" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Upgrade a cluster or adjust the upgrade channel</p>
      <CodeBlock code={"  # View the update status and available cluster updates\n  oc adm upgrade\n  \n  # Update to the latest version\n  oc adm upgrade --to-latest=true"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_verify-image-signature" class="group scroll-mt-20">oc adm verify-image-signature<a href="#_oc_adm_verify-image-signature" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Verify the image identity contained in the image signature</p>
      <CodeBlock code={"  # Verify the image signature and identity using the local GPG keychain\n  oc adm verify-image-signature sha256:c841e9b64e4579bd56c794bdd7c36e1c257110fd2404bebbb8b613e4935228c4 \\\n  --expected-identity=registry.local:5000/foo/bar:v1\n  \n  # Verify the image signature and identity using the local GPG keychain and save the status\n  oc adm verify-image-signature sha256:c841e9b64e4579bd56c794bdd7c36e1c257110fd2404bebbb8b613e4935228c4 \\\n  --expected-identity=registry.local:5000/foo/bar:v1 --save\n  \n  # Verify the image signature and identity via exposed registry route\n  oc adm verify-image-signature sha256:c841e9b64e4579bd56c794bdd7c36e1c257110fd2404bebbb8b613e4935228c4 \\\n  --expected-identity=registry.local:5000/foo/bar:v1 \\\n  --registry-url=docker-registry.foo.com\n  \n  # Remove all signature verifications from the image\n  oc adm verify-image-signature sha256:c841e9b64e4579bd56c794bdd7c36e1c257110fd2404bebbb8b613e4935228c4 --remove-all"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_wait-for-node-reboot" class="group scroll-mt-20">oc adm wait-for-node-reboot<a href="#_oc_adm_wait-for-node-reboot" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Wait for nodes to reboot after running <code>oc adm reboot-machine-config-pool</code></p>
      <CodeBlock code={"  # Wait for all nodes to complete a requested reboot from 'oc adm reboot-machine-config-pool mcp/worker mcp/master'\n  oc adm wait-for-node-reboot nodes --all\n  \n  # Wait for masters to complete a requested reboot from 'oc adm reboot-machine-config-pool mcp/master'\n  oc adm wait-for-node-reboot nodes -l node-role.kubernetes.io/master\n  \n  # Wait for masters to complete a specific reboot\n  oc adm wait-for-node-reboot nodes -l node-role.kubernetes.io/master --reboot-number=4"} lang="bash" title="Example usage" />
      <h3 id="_oc_adm_wait-for-stable-cluster" class="group scroll-mt-20">oc adm wait-for-stable-cluster<a href="#_oc_adm_wait-for-stable-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Wait for the platform operators to become stable</p>
      <CodeBlock code={"  # Wait for all cluster operators to become stable\n  oc adm wait-for-stable-cluster\n  \n  # Consider operators to be stable if they report as such for 5 minutes straight\n  oc adm wait-for-stable-cluster --minimum-stable-period 5m"} lang="bash" title="Example usage" />
      <h2 id="additional-resources_cli-administrator-commands" class="group scroll-mt-20">Additional resources<a href="#additional-resources_cli-administrator-commands" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul><li><a href="/openshift-docs-markdown/cli_reference/openshift_cli/developer-cli-commands#cli-developer-commands">OpenShift CLI developer command reference</a></li></ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
