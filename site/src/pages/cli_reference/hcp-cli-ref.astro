---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: cli_reference/hcp-cli-ref.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"hcp-cli-ref","text":"Installing the hosted control planes command-line interface"},{"depth":2,"slug":"hcp-cli-terminal_hcp-cli-ref","text":"Installing the hosted control planes command-line interface from the terminal"},{"depth":2,"slug":"hcp-cli-console_hcp-cli-ref","text":"Installing the hosted control planes command-line interface by using the web console"},{"depth":2,"slug":"hcp-cli-gateway_hcp-cli-ref","text":"Installing the hosted control planes command-line interface by using the content gateway"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/cli_reference/hcp-cli-ref";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Installing the hosted control planes command-line interface";
const description = "The hosted control planes command-line interface, hcp, is a tool that you can use to get started with hosted control planes. For Day 2 operations, such as…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["cli_reference"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Installing the hosted control planes command-line interface {#hcp-cli-ref}\n\nThe hosted control planes command-line interface, `hcp`, is a tool that you can use to get started with hosted control planes. For Day 2 operations, such as management and configuration, use GitOps or your own automation tool.\n\n## Installing the hosted control planes command-line interface from the terminal {#hcp-cli-terminal_hcp-cli-ref}\n\nYou can install the hosted control planes command-line interface (CLI), `hcp`, from the terminal.\n\n**Prerequisites**\n\n- On an OpenShift Container Platform cluster, you have installed multicluster engine for Kubernetes Operator 2.5 or later. The multicluster engine Operator is automatically installed when you install Red Hat Advanced Cluster Management. You can also install multicluster engine Operator without Red Hat Advanced Management as an Operator from the OpenShift Container Platform software catalog.\n\n**Procedure**\n\n1. Get the URL to download the `hcp` binary by running the following command:\n\n   ```terminal\n   $ oc get ConsoleCLIDownload hcp-cli-download -o json | jq -r \".spec\"\n   ```\n2. Download the `hcp` binary by running the following command:\n\n   ```terminal\n   $ wget <hcp_cli_download_url>\n   ```\n\n   Replace `hcp_cli_download_url` with the URL that you obtained from the previous step.\n3. Unpack the downloaded archive by running the following command:\n\n   ```terminal\n   $ tar xvzf hcp.tar.gz\n   ```\n4. Make the `hcp` binary file executable by running the following command:\n\n   ```terminal\n   $ chmod +x hcp\n   ```\n5. Move the `hcp` binary file to a directory in your path by running the following command:\n\n   ```terminal\n   $ sudo mv hcp /usr/local/bin/.\n   ```\n\n   > [!NOTE]\n   > If you download the CLI on a Mac computer, you might see a warning about the `hcp` binary file. You need to adjust your security settings to allow the binary file to be run.\n\n**Verification**\n\n- Verify that you see the list of available parameters by running the following command:\n\n  ```terminal\n  $ hcp create cluster <platform> --help\n  ```\n\n  You can use the `hcp create cluster` command to create and manage hosted clusters. The supported platforms are `agent`, `aws`, and `kubevirt`. The `azure` and `openstack` platforms are also available as Technology Preview features.\n\n## Installing the hosted control planes command-line interface by using the web console {#hcp-cli-console_hcp-cli-ref}\n\nYou can install the hosted control planes command-line interface (CLI), `hcp`, by using the OpenShift Container Platform web console.\n\n**Prerequisites**\n\n- On an OpenShift Container Platform cluster, you have installed multicluster engine for Kubernetes Operator 2.5 or later. The multicluster engine Operator is automatically installed when you install Red Hat Advanced Cluster Management. You can also install multicluster engine Operator without Red Hat Advanced Management as an Operator from the OpenShift Container Platform software catalog.\n\n**Procedure**\n\n1. From the OpenShift Container Platform web console, click the **Help icon** → **Command Line Tools**.\n2. Click **Download hcp CLI** for your platform.\n3. Unpack the downloaded archive by running the following command:\n\n   ```terminal\n   $ tar xvzf hcp.tar.gz\n   ```\n4. Run the following command to make the binary file executable:\n\n   ```terminal\n   $ chmod +x hcp\n   ```\n5. Run the following command to move the binary file to a directory in your path:\n\n   ```terminal\n   $ sudo mv hcp /usr/local/bin/.\n   ```\n\n   > [!NOTE]\n   > If you download the CLI on a Mac computer, you might see a warning about the `hcp` binary file. You need to adjust your security settings to allow the binary file to be run.\n\n**Verification**\n\n- Verify that you see the list of available parameters by running the following command:\n\n  ```terminal\n  $ hcp create cluster <platform> --help\n  ```\n\n  You can use the `hcp create cluster` command to create and manage hosted clusters. The supported platforms are `agent`, `aws`, and `kubevirt`. The `azure` and `openstack` platforms are also available as Technology Preview features.\n\n## Installing the hosted control planes command-line interface by using the content gateway {#hcp-cli-gateway_hcp-cli-ref}\n\nYou can install the hosted control planes command-line interface (CLI), `hcp`, by using the content gateway.\n\n**Prerequisites**\n\n- On an OpenShift Container Platform cluster, you have installed multicluster engine for Kubernetes Operator 2.7 or later. The multicluster engine Operator is automatically installed when you install Red Hat Advanced Cluster Management. You can also install multicluster engine Operator without Red Hat Advanced Management as an Operator from OpenShift Container Platform OperatorHub.\n\n**Procedure**\n\n1. Navigate to the [content gateway](https://developers.redhat.com/content-gateway/rest/browse/pub/mce/clients/hcp-cli/) and download the `hcp` binary.\n2. Unpack the downloaded archive by running the following command:\n\n   ```terminal\n   $ tar xvzf hcp.tar.gz\n   ```\n3. Make the `hcp` binary file executable by running the following command:\n\n   ```terminal\n   $ chmod +x hcp\n   ```\n4. Move the `hcp` binary file to a directory in your path by running the following command:\n\n   ```terminal\n   $ sudo mv hcp /usr/local/bin/.\n   ```\n\n   > [!NOTE]\n   > If you download the CLI on a Mac computer, you might see a warning about the `hcp` binary file. You need to adjust your security settings to allow the binary file to be run.\n\n**Verification**\n\n- Verify that you see the list of available parameters by running the following command:\n\n  ```terminal\n  $ hcp create cluster <platform> --help\n  ```\n\n  You can use the `hcp create cluster` command to create and manage hosted clusters. The supported platforms are `agent`, `aws`, and `kubevirt`. The `azure` and `openstack` platforms are also available as Technology Preview features.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/cli_reference/hcp-cli-ref.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="hcp-cli-ref" class="group scroll-mt-20">Installing the hosted control planes command-line interface<a href="#hcp-cli-ref" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">The hosted control planes command-line interface, <code>hcp</code>, is a tool that you can use to get started with hosted control planes. For Day 2 operations, such as management and configuration, use GitOps or your own automation tool.</p>
      <h2 id="hcp-cli-terminal_hcp-cli-ref" class="group scroll-mt-20">Installing the hosted control planes command-line interface from the terminal<a href="#hcp-cli-terminal_hcp-cli-ref" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can install the hosted control planes command-line interface (CLI), <code>hcp</code>, from the terminal.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>On an OpenShift Container Platform cluster, you have installed multicluster engine for Kubernetes Operator 2.5 or later. The multicluster engine Operator is automatically installed when you install Red Hat Advanced Cluster Management. You can also install multicluster engine Operator without Red Hat Advanced Management as an Operator from the OpenShift Container Platform software catalog.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Get the URL to download the <code>hcp</code> binary by running the following command:
      <CodeBlock code={"$ oc get ConsoleCLIDownload hcp-cli-download -o json | jq -r \".spec\""} lang="terminal" />
      </li>
      <li>
      Download the <code>hcp</code> binary by running the following command:
      <CodeBlock code={"$ wget <hcp_cli_download_url>"} lang="terminal" />
      <p>Replace <code>hcp_cli_download_url</code> with the URL that you obtained from the previous step.</p>
      </li>
      <li>
      Unpack the downloaded archive by running the following command:
      <CodeBlock code={"$ tar xvzf hcp.tar.gz"} lang="terminal" />
      </li>
      <li>
      Make the <code>hcp</code> binary file executable by running the following command:
      <CodeBlock code={"$ chmod +x hcp"} lang="terminal" />
      </li>
      <li>
      Move the <code>hcp</code> binary file to a directory in your path by running the following command:
      <CodeBlock code={"$ sudo mv hcp /usr/local/bin/."} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If you download the CLI on a Mac computer, you might see a warning about the <code>hcp</code> binary file. You need to adjust your security settings to allow the binary file to be run.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      Verify that you see the list of available parameters by running the following command:
      <CodeBlock code={"$ hcp create cluster <platform> --help"} lang="terminal" />
      <p>You can use the <code>hcp create cluster</code> command to create and manage hosted clusters. The supported platforms are <code>agent</code>, <code>aws</code>, and <code>kubevirt</code>. The <code>azure</code> and <code>openstack</code> platforms are also available as Technology Preview features.</p>
      </li>
      </ul>
      <h2 id="hcp-cli-console_hcp-cli-ref" class="group scroll-mt-20">Installing the hosted control planes command-line interface by using the web console<a href="#hcp-cli-console_hcp-cli-ref" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can install the hosted control planes command-line interface (CLI), <code>hcp</code>, by using the OpenShift Container Platform web console.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>On an OpenShift Container Platform cluster, you have installed multicluster engine for Kubernetes Operator 2.5 or later. The multicluster engine Operator is automatically installed when you install Red Hat Advanced Cluster Management. You can also install multicluster engine Operator without Red Hat Advanced Management as an Operator from the OpenShift Container Platform software catalog.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>From the OpenShift Container Platform web console, click the <strong>Help icon</strong> → <strong>Command Line Tools</strong>.</li>
      <li>Click <strong>Download hcp CLI</strong> for your platform.</li>
      <li>
      Unpack the downloaded archive by running the following command:
      <CodeBlock code={"$ tar xvzf hcp.tar.gz"} lang="terminal" />
      </li>
      <li>
      Run the following command to make the binary file executable:
      <CodeBlock code={"$ chmod +x hcp"} lang="terminal" />
      </li>
      <li>
      Run the following command to move the binary file to a directory in your path:
      <CodeBlock code={"$ sudo mv hcp /usr/local/bin/."} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If you download the CLI on a Mac computer, you might see a warning about the <code>hcp</code> binary file. You need to adjust your security settings to allow the binary file to be run.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      Verify that you see the list of available parameters by running the following command:
      <CodeBlock code={"$ hcp create cluster <platform> --help"} lang="terminal" />
      <p>You can use the <code>hcp create cluster</code> command to create and manage hosted clusters. The supported platforms are <code>agent</code>, <code>aws</code>, and <code>kubevirt</code>. The <code>azure</code> and <code>openstack</code> platforms are also available as Technology Preview features.</p>
      </li>
      </ul>
      <h2 id="hcp-cli-gateway_hcp-cli-ref" class="group scroll-mt-20">Installing the hosted control planes command-line interface by using the content gateway<a href="#hcp-cli-gateway_hcp-cli-ref" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can install the hosted control planes command-line interface (CLI), <code>hcp</code>, by using the content gateway.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>On an OpenShift Container Platform cluster, you have installed multicluster engine for Kubernetes Operator 2.7 or later. The multicluster engine Operator is automatically installed when you install Red Hat Advanced Cluster Management. You can also install multicluster engine Operator without Red Hat Advanced Management as an Operator from OpenShift Container Platform OperatorHub.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to the <a href="https://developers.redhat.com/content-gateway/rest/browse/pub/mce/clients/hcp-cli/">content gateway</a> and download the <code>hcp</code> binary.</li>
      <li>
      Unpack the downloaded archive by running the following command:
      <CodeBlock code={"$ tar xvzf hcp.tar.gz"} lang="terminal" />
      </li>
      <li>
      Make the <code>hcp</code> binary file executable by running the following command:
      <CodeBlock code={"$ chmod +x hcp"} lang="terminal" />
      </li>
      <li>
      Move the <code>hcp</code> binary file to a directory in your path by running the following command:
      <CodeBlock code={"$ sudo mv hcp /usr/local/bin/."} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If you download the CLI on a Mac computer, you might see a warning about the <code>hcp</code> binary file. You need to adjust your security settings to allow the binary file to be run.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      Verify that you see the list of available parameters by running the following command:
      <CodeBlock code={"$ hcp create cluster <platform> --help"} lang="terminal" />
      <p>You can use the <code>hcp create cluster</code> command to create and manage hosted clusters. The supported platforms are <code>agent</code>, <code>aws</code>, and <code>kubevirt</code>. The <code>azure</code> and <code>openstack</code> platforms are also available as Technology Preview features.</p>
      </li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
