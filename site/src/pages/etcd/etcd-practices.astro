---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: etcd/etcd-practices.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"etcd-practices","text":"Recommended etcd practices"},{"depth":2,"slug":"recommended-etcd-practices_etcd-practices","text":"Storage practices for etcd"},{"depth":2,"slug":"recommended-cluster-latency-etcd_etcd-practices","text":"Cluster latency requirements for etcd"},{"depth":2,"slug":"etcd-verify-hardware_etcd-practices","text":"Validating the hardware for etcd"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/etcd/etcd-practices";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Recommended etcd practices";
const description = "Follow storage, latency, and hardware validation guidance for etcd to reduce leader elections, API timeouts, and control plane instability on OpenShift…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["etcd"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Recommended etcd practices {#etcd-practices}\n\nFollow storage, latency, and hardware validation guidance for etcd to reduce leader elections, API timeouts, and control plane instability on OpenShift Container Platform.\n\n## Storage practices for etcd {#recommended-etcd-practices_etcd-practices}\n\nBecause etcd writes data to disk and persists proposals on disk, its performance depends on disk performance.\n\nAlthough etcd is not particularly I/O intensive, it requires a low latency block device for optimal performance and stability. Because the consensus protocol for etcd depends on persistently storing metadata to a log (WAL), etcd is sensitive to disk-write latency. Slow disks and disk activity from other processes can cause long fsync latencies.\n\nThose latencies can cause etcd to miss heartbeats, not commit new proposals to the disk on time, and ultimately experience request timeouts and temporary leader loss. High write latencies also lead to an OpenShift API slowness, which affects cluster performance. Because of these reasons, avoid colocating other workloads on the control-plane nodes that are I/O sensitive or intensive and share the same underlying I/O infrastructure.\n\nRun etcd on a block device that can write at least 50 IOPS of 8KB sequentially, including fdatasync, in under 10ms. For heavy loaded clusters, sequential 500 IOPS of 8000 bytes (2 ms) are recommended. To measure those numbers, you can use a benchmarking tool, such as the `fio` command.\n\nTo achieve such performance, run etcd on machines that are backed by SSD or NVMe disks with low latency and high throughput. Consider single-level cell (SLC) solid-state drives (SSDs), which provide 1 bit per memory cell, are durable and reliable, and are ideal for write-intensive workloads.\n\n> [!NOTE]\n> The load on etcd arises from static factors, such as the number of nodes and pods, and dynamic factors, including changes in endpoints due to pod autoscaling, pod restarts, job executions, and other workload-related events. To accurately size your etcd setup, you must analyze the specific requirements of your workload. Consider the number of nodes, pods, and other relevant factors that impact the load on etcd.\n\nThe following hard drive practices provide optimal etcd performance:\n\n- Use dedicated etcd drives. Avoid drives that communicate over the network, such as iSCSI. Do not place log files or other heavy workloads on etcd drives.\n- Prefer drives with low latency to support fast read and write operations.\n- Prefer high-bandwidth writes for faster compactions and defragmentation.\n- Prefer high-bandwidth reads for faster recovery from failures.\n- Use solid state drives as a minimum selection. Prefer NVMe drives for production environments.\n- Use server-grade hardware for increased reliability.\n- Avoid NAS or SAN setups and spinning drives. Ceph Rados Block Device (RBD) and other types of network-attached storage can result in unpredictable network latency. To provide fast storage to etcd nodes at scale, use PCI passthrough to pass NVM devices directly to the nodes.\n- Always benchmark by using utilities such as `fio`. You can use such utilities to continuously monitor the cluster performance as it increases.\n- Avoid using the Network File System (NFS) protocol or other network based file systems.\n\nSome key metrics to monitor on a deployed OpenShift Container Platform cluster are p99 of etcd disk write ahead log duration and the number of etcd leader changes. Use Prometheus to track these metrics.\n\n> [!NOTE]\n> The etcd member database sizes can vary in a cluster during normal operations. This difference does not affect cluster upgrades, even if the leader size is different from the other members.\n\n## Cluster latency requirements for etcd {#recommended-cluster-latency-etcd_etcd-practices}\n\netcd requires low network and disk I/O latency so that Raft consensus can commit changes quickly and recover from leader failures without destabilizing the cluster.\n\nAddress two constraints to provide a low-latency, high-availability network for etcd:\n\n- network I/O latency\n- disk I/O latency\n\netcd uses the Raft consensus algorithm, and every change must replicate to a majority of cluster members before it commits. This process is highly sensitive to network and disk performance. The minimum time for an etcd request is the round-trip time (RTT) between members, plus the time required to write data to permanent storage.\n\nTo achieve high availability, etcd must detect and recover from a leader failure quickly. This depends on two key tuning parameters:\n\nHeartbeat Interval\n:   The frequency that the leader sends a heartbeat to followers. This value should be close to the average RTT between members.\n\nElection Timeout\n:   The time a follower waits without hearing a heartbeat before it attempts to become the new leader. This should be at least 10 times the RTT value to account for network variance.\n\nIn a healthy cluster, the round-trip time between members should be less than 50 ms to eensure stability and avoid frequent leader elections. For this reason, etcd clusters are often deployed within a single data center or availability zone to minimize physical distance and network latency.\n\nTo support a low-latency, high-availability network, especially during the leader election process, an arbiter site should be located where it provides an RTT latency of less than 10 ms. The arbiter component of a network maintains consistency and availability in a distributed system.\n\n**Additional resources**\n{._additional-resources}\n\n- [Setting tuning parameters for etcd](/openshift-docs-markdown/etcd/etcd-performance#etcd-tuning-parameters_etcd-performance)\n\n## Validating the hardware for etcd {#etcd-verify-hardware_etcd-practices}\n\nValidate control plane disk performance with `fio` before or after you create the OpenShift Container Platform cluster so that you can confirm that storage meets etcd latency requirements.\n\nThe output of the following procedure reports whether the disk is fast enough to host etcd by comparing the 99th percentile of the fsync metric captured from the run to see if it is less than 10 ms.\n\nA few of the most important etcd metrics that might affected by I/O performance are as follows:\n\n- `etcd_disk_wal_fsync_duration_seconds_bucket` reports the etcd WAL fsync duration.\n- `etcd_disk_backend_commit_duration_seconds_bucket` reports the etcd backend commit latency duration.\n- `etcd_server_leader_changes_seen_total` reports the leader changes.\n\nBecause etcd replicates the requests among all the members, its performance strongly depends on network input/output (I/O) latency. High network latencies result in etcd heartbeats taking longer than the election timeout, which results in leader elections that are disruptive to the cluster. A key metric to monitor on a deployed OpenShift Container Platform cluster is the 99th percentile of etcd network peer latency on each etcd cluster member. Use Prometheus to track the metric.\n\nThe `histogram_quantile(0.99, rate(etcd_network_peer_round_trip_time_seconds_bucket[2m]))` metric reports the round trip time for etcd to finish replicating the client requests between the members. Ensure that it is less than 50 ms.\n\n**Prerequisites**\n\n- A container runtime, such as Podman or Docker, is installed on the machine that you are testing.\n- The `/var/lib/etcd` path is available for writing test data.\n\n**Procedure**\n\n- Run `fio` and analyze the results:\n\n  - If you use Podman, run the following command:\n\n    ```terminal\n    $ sudo podman run --volume /var/lib/etcd:/var/lib/etcd:Z quay.io/cloud-bulldozer/etcd-perf\n    ```\n  - If you use Docker, run the following command:\n\n    ```terminal\n    $ sudo docker run --volume /var/lib/etcd:/var/lib/etcd:Z quay.io/cloud-bulldozer/etcd-perf\n    ```\n\n**Verification**\n\n- Confirm that the 99th percentile of the fsync metric from the run is less than 10 ms.\n- Confirm that the report indicates that the disk is fast enough to host etcd.\n\n**Additional resources**\n{._additional-resources}\n\n- [How to use `fio` to check etcd disk performance in OpenShift Container Platform](https://access.redhat.com/solutions/4885641)\n- [etcd performance troubleshooting guide for OpenShift Container Platform](https://access.redhat.com/articles/6271341)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/etcd/etcd-practices.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="etcd-practices" class="group scroll-mt-20">Recommended etcd practices<a href="#etcd-practices" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Follow storage, latency, and hardware validation guidance for etcd to reduce leader elections, API timeouts, and control plane instability on OpenShift Container Platform.</p>
      <h2 id="recommended-etcd-practices_etcd-practices" class="group scroll-mt-20">Storage practices for etcd<a href="#recommended-etcd-practices_etcd-practices" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Because etcd writes data to disk and persists proposals on disk, its performance depends on disk performance.</p>
      <p>Although etcd is not particularly I/O intensive, it requires a low latency block device for optimal performance and stability. Because the consensus protocol for etcd depends on persistently storing metadata to a log (WAL), etcd is sensitive to disk-write latency. Slow disks and disk activity from other processes can cause long fsync latencies.</p>
      <p>Those latencies can cause etcd to miss heartbeats, not commit new proposals to the disk on time, and ultimately experience request timeouts and temporary leader loss. High write latencies also lead to an OpenShift API slowness, which affects cluster performance. Because of these reasons, avoid colocating other workloads on the control-plane nodes that are I/O sensitive or intensive and share the same underlying I/O infrastructure.</p>
      <p>Run etcd on a block device that can write at least 50 IOPS of 8KB sequentially, including fdatasync, in under 10ms. For heavy loaded clusters, sequential 500 IOPS of 8000 bytes (2 ms) are recommended. To measure those numbers, you can use a benchmarking tool, such as the <code>fio</code> command.</p>
      <p>To achieve such performance, run etcd on machines that are backed by SSD or NVMe disks with low latency and high throughput. Consider single-level cell (SLC) solid-state drives (SSDs), which provide 1 bit per memory cell, are durable and reliable, and are ideal for write-intensive workloads.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The load on etcd arises from static factors, such as the number of nodes and pods, and dynamic factors, including changes in endpoints due to pod autoscaling, pod restarts, job executions, and other workload-related events. To accurately size your etcd setup, you must analyze the specific requirements of your workload. Consider the number of nodes, pods, and other relevant factors that impact the load on etcd.</p>
        </AlertDescription>
      </Alert>
      <p>The following hard drive practices provide optimal etcd performance:</p>
      <ul>
      <li>Use dedicated etcd drives. Avoid drives that communicate over the network, such as iSCSI. Do not place log files or other heavy workloads on etcd drives.</li>
      <li>Prefer drives with low latency to support fast read and write operations.</li>
      <li>Prefer high-bandwidth writes for faster compactions and defragmentation.</li>
      <li>Prefer high-bandwidth reads for faster recovery from failures.</li>
      <li>Use solid state drives as a minimum selection. Prefer NVMe drives for production environments.</li>
      <li>Use server-grade hardware for increased reliability.</li>
      <li>Avoid NAS or SAN setups and spinning drives. Ceph Rados Block Device (RBD) and other types of network-attached storage can result in unpredictable network latency. To provide fast storage to etcd nodes at scale, use PCI passthrough to pass NVM devices directly to the nodes.</li>
      <li>Always benchmark by using utilities such as <code>fio</code>. You can use such utilities to continuously monitor the cluster performance as it increases.</li>
      <li>Avoid using the Network File System (NFS) protocol or other network based file systems.</li>
      </ul>
      <p>Some key metrics to monitor on a deployed OpenShift Container Platform cluster are p99 of etcd disk write ahead log duration and the number of etcd leader changes. Use Prometheus to track these metrics.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The etcd member database sizes can vary in a cluster during normal operations. This difference does not affect cluster upgrades, even if the leader size is different from the other members.</p>
        </AlertDescription>
      </Alert>
      <h2 id="recommended-cluster-latency-etcd_etcd-practices" class="group scroll-mt-20">Cluster latency requirements for etcd<a href="#recommended-cluster-latency-etcd_etcd-practices" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">etcd requires low network and disk I/O latency so that Raft consensus can commit changes quickly and recover from leader failures without destabilizing the cluster.</p>
      <p>Address two constraints to provide a low-latency, high-availability network for etcd:</p>
      <ul>
      <li>network I/O latency</li>
      <li>disk I/O latency</li>
      </ul>
      <p>etcd uses the Raft consensus algorithm, and every change must replicate to a majority of cluster members before it commits. This process is highly sensitive to network and disk performance. The minimum time for an etcd request is the round-trip time (RTT) between members, plus the time required to write data to permanent storage.</p>
      <p>To achieve high availability, etcd must detect and recover from a leader failure quickly. This depends on two key tuning parameters:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Heartbeat Interval</dt>
      <dd class="ml-6 text-muted-foreground"><p>The frequency that the leader sends a heartbeat to followers. This value should be close to the average RTT between members.</p></dd>
      <dt class="font-semibold">Election Timeout</dt>
      <dd class="ml-6 text-muted-foreground"><p>The time a follower waits without hearing a heartbeat before it attempts to become the new leader. This should be at least 10 times the RTT value to account for network variance.</p></dd>
      </dl>
      <p>In a healthy cluster, the round-trip time between members should be less than 50 ms to eensure stability and avoid frequent leader elections. For this reason, etcd clusters are often deployed within a single data center or availability zone to minimize physical distance and network latency.</p>
      <p>To support a low-latency, high-availability network, especially during the leader election process, an arbiter site should be located where it provides an RTT latency of less than 10 ms. The arbiter component of a network maintains consistency and availability in a distributed system.</p>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="/openshift-docs-markdown/etcd/etcd-performance#etcd-tuning-parameters_etcd-performance">Setting tuning parameters for etcd</a></li></ul>
      </section>
      <h2 id="etcd-verify-hardware_etcd-practices" class="group scroll-mt-20">Validating the hardware for etcd<a href="#etcd-verify-hardware_etcd-practices" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Validate control plane disk performance with <code>fio</code> before or after you create the OpenShift Container Platform cluster so that you can confirm that storage meets etcd latency requirements.</p>
      <p>The output of the following procedure reports whether the disk is fast enough to host etcd by comparing the 99th percentile of the fsync metric captured from the run to see if it is less than 10 ms.</p>
      <p>A few of the most important etcd metrics that might affected by I/O performance are as follows:</p>
      <ul>
      <li><code>etcd_disk_wal_fsync_duration_seconds_bucket</code> reports the etcd WAL fsync duration.</li>
      <li><code>etcd_disk_backend_commit_duration_seconds_bucket</code> reports the etcd backend commit latency duration.</li>
      <li><code>etcd_server_leader_changes_seen_total</code> reports the leader changes.</li>
      </ul>
      <p>Because etcd replicates the requests among all the members, its performance strongly depends on network input/output (I/O) latency. High network latencies result in etcd heartbeats taking longer than the election timeout, which results in leader elections that are disruptive to the cluster. A key metric to monitor on a deployed OpenShift Container Platform cluster is the 99th percentile of etcd network peer latency on each etcd cluster member. Use Prometheus to track the metric.</p>
      <p>The <code>histogram_quantile(0.99, rate(etcd_network_peer_round_trip_time_seconds_bucket[2m]))</code> metric reports the round trip time for etcd to finish replicating the client requests between the members. Ensure that it is less than 50 ms.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>A container runtime, such as Podman or Docker, is installed on the machine that you are testing.</li>
      <li>The <code>/var/lib/etcd</code> path is available for writing test data.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Run <code>fio</code> and analyze the results:
      <ul>
      <li>
      If you use Podman, run the following command:
      <CodeBlock code={"$ sudo podman run --volume /var/lib/etcd:/var/lib/etcd:Z quay.io/cloud-bulldozer/etcd-perf"} lang="terminal" />
      </li>
      <li>
      If you use Docker, run the following command:
      <CodeBlock code={"$ sudo docker run --volume /var/lib/etcd:/var/lib/etcd:Z quay.io/cloud-bulldozer/etcd-perf"} lang="terminal" />
      </li>
      </ul>
      </li>
      </ul>
      <p><strong>Verification</strong></p>
      <ul>
      <li>Confirm that the 99th percentile of the fsync metric from the run is less than 10 ms.</li>
      <li>Confirm that the report indicates that the disk is fast enough to host etcd.</li>
      </ul>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="https://access.redhat.com/solutions/4885641">How to use <code>fio</code> to check etcd disk performance in OpenShift Container Platform</a></li>
      <li><a href="https://access.redhat.com/articles/6271341">etcd performance troubleshooting guide for OpenShift Container Platform</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
