---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: windows_containers/creating_windows_machinesets/creating-windows-machineset-aws.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"creating-windows-machineset-aws","text":"Creating a Windows machine set on AWS"},{"depth":2,"slug":"prerequisites_creating-windows-machineset-aws","text":"Prerequisites"},{"depth":2,"slug":"_additional_resources","text":"Additional resources"}];
const nav = navData;
const currentPath = "/windows_containers/creating_windows_machinesets/creating-windows-machineset-aws";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Creating a Windows machine set on AWS";
const description = "You can use a MachineSet custom resource (CR) to add a Windows compute node to your {{ aws_full }} cluster, where you can run Windows container workloads." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["windows_containers","windows_containers/creating_windows_machinesets"];
const pageTaxonomies = null;
const tagsIndexPath = "/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Creating a Windows machine set on AWS {#creating-windows-machineset-aws}\n\nYou can use a `MachineSet` custom resource (CR) to add a Windows compute node to your {{ aws_full }} cluster, where you can run Windows container workloads.\n\nFor example, you might create infrastructure Windows machine sets and related machines so that you can move supporting Windows workloads to the new Windows machines. For more information about machine sets, see \"Overview of machine management\" in the *Additional resources* section.\n\n## Prerequisites {#prerequisites_creating-windows-machineset-aws}\n\n- You installed the Windows Machine Config Operator (WMCO) using Operator Lifecycle Manager (OLM).\n- You are using a supported Windows Server as the operating system image.\n\n  Use one of the following `aws` commands, as appropriate for your Windows Server release, to query valid AMI images:\n\n  ```terminal {title=\"Example Windows Server 2025 command\"}\n  $ aws ec2 describe-images --region <aws_region_name> --filters \"Name=name,Values=Windows_Server-2025*English*Core*Base*\" \"Name=is-public,Values=true\" --query \"reverse(sort_by(Images, &CreationDate))[*].{name: Name, id: ImageId}\" --output table\n  ```\n\n  ```terminal {title=\"Example Windows Server 2022 command\"}\n  $ aws ec2 describe-images --region <aws_region_name> --filters \"Name=name,Values=Windows_Server-2022*English*Core*Base*\" \"Name=is-public,Values=true\" --query \"reverse(sort_by(Images, &CreationDate))[*].{name: Name, id: ImageId}\" --output table\n  ```\n\n  ```terminal {title=\"Example Windows Server 2019 command\"}\n  $ aws ec2 describe-images --region <aws_region_name> --filters \"Name=name,Values=Windows_Server-2019*English*Core*Base*\" \"Name=is-public,Values=true\" --query \"reverse(sort_by(Images, &CreationDate))[*].{name: Name, id: ImageId}\" --output table\n  ```\n\n  where:\n\n  <aws_region_name>\n  :   Specifies the name of your AWS region.\n- For disconnected clusters, the Windows AMI must have the EC2LaunchV2 agent version 2.0.2107 or later installed. For more information, see \"Install the latest version of EC2Launch v2 (AWS documentation)\" in the *Additional references* section.\n\n## Additional resources {#_additional_resources}\n\n- [Overview of machine management](/machine_management/index#overview-of-machine-management)\n- [Install the latest version of EC2Launch v2 (AWS documentation)](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2launch-v2-install.html)\n","mdUrl":"/windows_containers/creating_windows_machinesets/creating-windows-machineset-aws.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={""}
  navMode={"client"}
  pagefindUrl={"/pagefind/"}
  favicon={"/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="creating-windows-machineset-aws" class="group scroll-mt-20">Creating a Windows machine set on AWS<a href="#creating-windows-machineset-aws" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="/_attributes/common-attributes.md"></p>
      <p>You can use a <code>MachineSet</code> custom resource (CR) to add a Windows compute node to your {"{"}{"{"} aws_full {"}"}{"}"} cluster, where you can run Windows container workloads.</p>
      <p>For example, you might create infrastructure Windows machine sets and related machines so that you can move supporting Windows workloads to the new Windows machines. For more information about machine sets, see "Overview of machine management" in the <em>Additional resources</em> section.</p>
      <h2 id="prerequisites_creating-windows-machineset-aws" class="group scroll-mt-20">Prerequisites<a href="#prerequisites_creating-windows-machineset-aws" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li>You installed the Windows Machine Config Operator (WMCO) using Operator Lifecycle Manager (OLM).</li>
      <li>
      You are using a supported Windows Server as the operating system image.
      <p>Use one of the following <code>aws</code> commands, as appropriate for your Windows Server release, to query valid AMI images:</p>
      <CodeBlock code={"$ aws ec2 describe-images --region <aws_region_name> --filters \"Name=name,Values=Windows_Server-2025*English*Core*Base*\" \"Name=is-public,Values=true\" --query \"reverse(sort_by(Images, &CreationDate))[*].{name: Name, id: ImageId}\" --output table"} lang="terminal" title="Example Windows Server 2025 command" />
      <CodeBlock code={"$ aws ec2 describe-images --region <aws_region_name> --filters \"Name=name,Values=Windows_Server-2022*English*Core*Base*\" \"Name=is-public,Values=true\" --query \"reverse(sort_by(Images, &CreationDate))[*].{name: Name, id: ImageId}\" --output table"} lang="terminal" title="Example Windows Server 2022 command" />
      <CodeBlock code={"$ aws ec2 describe-images --region <aws_region_name> --filters \"Name=name,Values=Windows_Server-2019*English*Core*Base*\" \"Name=is-public,Values=true\" --query \"reverse(sort_by(Images, &CreationDate))[*].{name: Name, id: ImageId}\" --output table"} lang="terminal" title="Example Windows Server 2019 command" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">&lt;aws_region_name&gt;</dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of your AWS region.</p></dd>
      </dl>
      </li>
      <li>For disconnected clusters, the Windows AMI must have the EC2LaunchV2 agent version 2.0.2107 or later installed. For more information, see "Install the latest version of EC2Launch v2 (AWS documentation)" in the <em>Additional references</em> section.</li>
      </ul>
      <p class="/modules/machineset-creating.md"></p>
      <h2 id="_additional_resources" class="group scroll-mt-20">Additional resources<a href="#_additional_resources" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/machine_management/index#overview-of-machine-management">Overview of machine management</a></li>
      <li><a href="https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2launch-v2-install.html">Install the latest version of EC2Launch v2 (AWS documentation)</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
