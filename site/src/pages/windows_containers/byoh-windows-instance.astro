---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: windows_containers/byoh-windows-instance.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"byoh-windows-instance","text":"Using Bring-Your-Own-Host (BYOH) Windows instances as nodes"},{"depth":2,"slug":"configuring-byoh-windows-instance","text":"Configuring a BYOH Windows instance"},{"depth":2,"slug":"removing-byoh-windows-instance","text":"Removing BYOH Windows instances"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/windows_containers/byoh-windows-instance";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Using Bring-Your-Own-Host (BYOH) Windows instances as nodes";
const description = "You can create Bring-Your-Own-Host (BYOH) Windows instances to bring existing Windows Server VMs into OpenShift Container Platform. By using BYOH Windows…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["windows_containers"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Using Bring-Your-Own-Host (BYOH) Windows instances as nodes {#byoh-windows-instance}\n\nYou can create Bring-Your-Own-Host (BYOH) Windows instances to bring existing Windows Server VMs into OpenShift Container Platform. By using BYOH Windows instances, you can mitigate major disruptions if a Windows server goes offline.\n\n## Configuring a BYOH Windows instance {#configuring-byoh-windows-instance}\n\nTo create a Bring-Your-Own-Host (BYOH) Windows instance, you must create a config map in the Windows Machine Config Operator (WMCO) namespace.\n\n**Prerequisites**\n\nAny Windows instances that are to be attached to the cluster as a node must fulfill the following requirements:\n\n- The instance must be on the same network as the Linux worker nodes in the cluster.\n- Port 22 must be open and running an SSH server.\n- The default shell for the SSH server must be the [Windows Command shell](https://docs.microsoft.com/en-us/windows-server/administration/openssh/openssh_server_configuration#configuring-the-default-shell-for-openssh-in-windows), or `cmd.exe`.\n- Port 10250 must be open for log collection.\n- An administrator user is present with the [private key used in the secret set as an authorized SSH key](https://learn.microsoft.com/en-us/windows-server/administration/openssh/openssh_keymanagement#configure-key-based-authentication) (Microsoft documentation).\n- If you are creating a BYOH Windows instance for an installer-provisioned infrastructure (IPI) AWS cluster, you must add a tag to the AWS instance that matches the `spec.template.spec.value.tag` value in the compute machine set for your worker nodes. For example, `kubernetes.io/cluster/<cluster_id>: owned` or `kubernetes.io/cluster/<cluster_id>: shared`.\n- If you are creating a BYOH Windows instance on vSphere, communication with the internal API server must be enabled.\n- The hostname of the instance must follow the [RFC 1123](https://datatracker.ietf.org/doc/html/rfc1123) DNS label requirements, which include the following standards:\n\n  - Contains only lowercase alphanumeric characters or '-'.\n  - Starts with an alphanumeric character.\n  - Ends with an alphanumeric character.\n\n> [!NOTE]\n> Windows instances deployed by the WMCO are configured with the containerd container runtime. Because the WMCO installs and manages the runtime, it is recommended that you not manually install containerd on nodes.\n\n**Procedure**\n\n1. Create a ConfigMap named `windows-instances` in the WMCO namespace that describes the Windows instances to be added.\n\n   > [!NOTE]\n   > Format each entry in the config map’s data section by using the address as the key while formatting the value as `username=<username>`.\n\n   ```yaml {title=\"Example config map\"}\n   kind: ConfigMap\n   apiVersion: v1\n   metadata:\n     name: windows-instances\n     namespace: openshift-windows-machine-config-operator\n   data:\n     10.1.42.1: |-\n       username=Administrator\n     instance.example.com: |-\n       username=core\n   ```\n\n   where:\n\n   `data`\n   :   Specifies the address that the WMCO uses to reach the instance over SSH, either a DNS name or an IPv4 address. A DNS PTR record must exist for this address. You should use a DNS name with your BYOH instance if your organization uses DHCP to assign IP addresses. If not, you need to update the `windows-instances` ConfigMap whenever the instance is assigned a new IP address.\n\n       Also, specify the user name of the administrator user created in the prerequisites.\n\n## Removing BYOH Windows instances {#removing-byoh-windows-instance}\n\nYou can remove a Bring-Your-Own-Host (BYOH) instance that is attached to the cluster by deleting the instance’s entry in the BYOH config map. Deleting an instance reverts that instance back to its previous state, before it was added to the cluster.\n\nThe removal process does not remove any logs or container runtime artifacts from the instances.\n\nFor an instance to be cleanly removed, it must be accessible with the current private key provided to WMCO. For example, to remove the `10.1.42.1` instance from the previous example, the config map would be changed to the following:\n\n```yaml\nkind: ConfigMap\napiVersion: v1\nmetadata:\n  name: windows-instances\n  namespace: openshift-windows-machine-config-operator\ndata:\n  instance.example.com: |-\n    username=core\n```\n\nDeleting `windows-instances` is viewed as a request to deconstruct all Windows instances added as nodes.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/windows_containers/byoh-windows-instance.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="byoh-windows-instance" class="group scroll-mt-20">Using Bring-Your-Own-Host (BYOH) Windows instances as nodes<a href="#byoh-windows-instance" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can create Bring-Your-Own-Host (BYOH) Windows instances to bring existing Windows Server VMs into OpenShift Container Platform. By using BYOH Windows instances, you can mitigate major disruptions if a Windows server goes offline.</p>
      <h2 id="configuring-byoh-windows-instance" class="group scroll-mt-20">Configuring a BYOH Windows instance<a href="#configuring-byoh-windows-instance" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To create a Bring-Your-Own-Host (BYOH) Windows instance, you must create a config map in the Windows Machine Config Operator (WMCO) namespace.</p>
      <p><strong>Prerequisites</strong></p>
      <p>Any Windows instances that are to be attached to the cluster as a node must fulfill the following requirements:</p>
      <ul>
      <li>The instance must be on the same network as the Linux worker nodes in the cluster.</li>
      <li>Port 22 must be open and running an SSH server.</li>
      <li>The default shell for the SSH server must be the <a href="https://docs.microsoft.com/en-us/windows-server/administration/openssh/openssh_server_configuration#configuring-the-default-shell-for-openssh-in-windows">Windows Command shell</a>, or <code>cmd.exe</code>.</li>
      <li>Port 10250 must be open for log collection.</li>
      <li>An administrator user is present with the <a href="https://learn.microsoft.com/en-us/windows-server/administration/openssh/openssh_keymanagement#configure-key-based-authentication">private key used in the secret set as an authorized SSH key</a> (Microsoft documentation).</li>
      <li>If you are creating a BYOH Windows instance for an installer-provisioned infrastructure (IPI) AWS cluster, you must add a tag to the AWS instance that matches the <code>spec.template.spec.value.tag</code> value in the compute machine set for your worker nodes. For example, <code>kubernetes.io/cluster/&lt;cluster_id&gt;: owned</code> or <code>kubernetes.io/cluster/&lt;cluster_id&gt;: shared</code>.</li>
      <li>If you are creating a BYOH Windows instance on vSphere, communication with the internal API server must be enabled.</li>
      <li>
      The hostname of the instance must follow the <a href="https://datatracker.ietf.org/doc/html/rfc1123">RFC 1123</a> DNS label requirements, which include the following standards:
      <ul>
      <li>Contains only lowercase alphanumeric characters or '-'.</li>
      <li>Starts with an alphanumeric character.</li>
      <li>Ends with an alphanumeric character.</li>
      </ul>
      </li>
      </ul>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Windows instances deployed by the WMCO are configured with the containerd container runtime. Because the WMCO installs and manages the runtime, it is recommended that you not manually install containerd on nodes.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a ConfigMap named <code>windows-instances</code> in the WMCO namespace that describes the Windows instances to be added.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Format each entry in the config map’s data section by using the address as the key while formatting the value as <code>username=&lt;username&gt;</code>.</p>
        </AlertDescription>
      </Alert>
      <CodeBlock code={"kind: ConfigMap\napiVersion: v1\nmetadata:\n  name: windows-instances\n  namespace: openshift-windows-machine-config-operator\ndata:\n  10.1.42.1: |-\n    username=Administrator\n  instance.example.com: |-\n    username=core"} lang="yaml" title="Example config map" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>data</code></dt>
      <dd class="ml-6 text-muted-foreground">
      <p>Specifies the address that the WMCO uses to reach the instance over SSH, either a DNS name or an IPv4 address. A DNS PTR record must exist for this address. You should use a DNS name with your BYOH instance if your organization uses DHCP to assign IP addresses. If not, you need to update the <code>windows-instances</code> ConfigMap whenever the instance is assigned a new IP address.</p>
      <p>Also, specify the user name of the administrator user created in the prerequisites.</p>
      </dd>
      </dl>
      </li>
      </ol>
      <h2 id="removing-byoh-windows-instance" class="group scroll-mt-20">Removing BYOH Windows instances<a href="#removing-byoh-windows-instance" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can remove a Bring-Your-Own-Host (BYOH) instance that is attached to the cluster by deleting the instance’s entry in the BYOH config map. Deleting an instance reverts that instance back to its previous state, before it was added to the cluster. </p>
      <p>The removal process does not remove any logs or container runtime artifacts from the instances.</p>
      <p>For an instance to be cleanly removed, it must be accessible with the current private key provided to WMCO. For example, to remove the <code>10.1.42.1</code> instance from the previous example, the config map would be changed to the following:</p>
      <CodeBlock code={"kind: ConfigMap\napiVersion: v1\nmetadata:\n  name: windows-instances\n  namespace: openshift-windows-machine-config-operator\ndata:\n  instance.example.com: |-\n    username=core"} lang="yaml" />
      <p>Deleting <code>windows-instances</code> is viewed as a request to deconstruct all Windows instances added as nodes.</p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
