---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: storage/generic-ephemeral-vols.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"generic-ephemeral-volumes","text":"Generic ephemeral volumes"},{"depth":2,"slug":"generic-ephemeral-vols-overview_generic-ephemeral-volumes","text":"Overview of generic ephemeral volumes"},{"depth":2,"slug":"generic-ephemeral-vols-lifecycle_generic-ephemeral-volumes","text":"Lifecycle and persistent volume claims"},{"depth":2,"slug":"generic-ephemeral-security_generic-ephemeral-volumes","text":"Security"},{"depth":2,"slug":"generic-ephemeral-vols-pvc-naming_generic-ephemeral-volumes","text":"Persistent volume claim naming"},{"depth":2,"slug":"generic-ephemeral-vols-procedure_generic-ephemeral-volumes","text":"Creating generic ephemeral volumes"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/storage/generic-ephemeral-vols";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Generic ephemeral volumes";
const description = "Generic ephemeral volumes provide per-pod temporary storage backed by any storage driver that supports dynamic provisioning, unlike emptyDir volumes which…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["storage"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Generic ephemeral volumes {#generic-ephemeral-volumes}\n\nGeneric ephemeral volumes provide per-pod temporary storage backed by any storage driver that supports dynamic provisioning, unlike `emptyDir` volumes which are limited to local node storage. This flexibility lets you use network storage backends, control storage classes and volume characteristics, and leverage delayed volume binding for optimal pod scheduling.\n\n## Overview of generic ephemeral volumes {#generic-ephemeral-vols-overview_generic-ephemeral-volumes}\n\nGeneric ephemeral volumes support network-attached storage, size limits, initial data population, and operations like cloning and snapshotting for temporary storage, with some driver-specific limitations.\n\nGeneric ephemeral volumes have the following features:\n\n- Storage can be local or network-attached.\n- Volumes can have a fixed size that pods cannot exceed.\n- Volumes might have some initial data, depending on the driver and parameters.\n- Typical operations on volumes are supported, assuming that the driver supports them, including snapshotting, cloning, resizing, and storage capacity tracking.\n\n> [!NOTE]\n> Generic ephemeral volumes do not support offline snapshotting and resizing.\n>\n> Due to this limitation, the following Container Storage Interface (CSI) drivers do not support the following features for generic ephemeral volumes:\n>\n> - Azure Disk CSI driver does not support resize.\n> - Cinder CSI driver does not support snapshot.\n\n## Lifecycle and persistent volume claims {#generic-ephemeral-vols-lifecycle_generic-ephemeral-volumes}\n\nGeneric ephemeral volumes follow pod lifecycle through automatically managed persistent volume claims created at pod startup and deleted at termination. Choose volume binding mode and reclaim policy based on this lifecycle behavior.\n\nGeneric ephemeral volumes are specified inline in the pod spec and follow the pod’s lifecycle. They are created and deleted along with the pod.\n\nThe parameters for a volume claim are allowed inside a volume source of a pod. Labels, annotations, and the whole set of fields for PVCs are supported. When such a pod is created, the ephemeral volume controller then creates an actual PVC object (from the template shown in the *Creating generic ephemeral volumes* procedure) in the same namespace as the pod, and ensures that the PVC is deleted when the pod is deleted.\n\nThis triggers volume binding and provisioning in one of two ways:\n\n- Either immediately, if the storage class uses immediate volume binding.\n\n  With immediate binding, the scheduler is forced to select a node that has access to the volume after it is available.\n- When the pod is tentatively scheduled onto a node (`WaitForFirstConsumer` volume binding mode).\n\n  This volume binding option is recommended for generic ephemeral volumes because then the scheduler can choose a suitable node for the pod.\n\nIn terms of resource ownership, a pod that has generic ephemeral storage is the owner of the PVCs that provide that ephemeral storage. When the pod is deleted, the Kubernetes garbage collector deletes the PVC, which then usually triggers deletion of the volume because the default reclaim policy of storage classes is to delete volumes. You can create quasi-ephemeral local storage by using a storage class with a reclaim policy of retain. The storage outlives the pod, and in this case, you must ensure that volume clean-up happens separately. While these PVCs exist, they can be used like any other PVC. In particular, they can be referenced as data sources in volume cloning or snapshotting. The PVC object also holds the current status of the volume.\n\n**Additional resources**\n{._additional-resources}\n\n- [Creating generic ephemeral volumes](/openshift-docs-markdown/storage/generic-ephemeral-vols#generic-ephemeral-vols-procedure_generic-ephemeral-volumes)\n\n## Security {#generic-ephemeral-security_generic-ephemeral-volumes}\n\nGeneric ephemeral volumes allow users who can create pods to indirectly create persistent volume claims (PVCs), even without direct PVC creation permissions. You can restrict this behavior if it conflicts with your security model.\n\nTo restrict this behavior, use an admission webhook that rejects objects such as pods that have a generic ephemeral volume.\n\nThe normal namespace quota for PVCs still applies, so even if users are allowed to use this new mechanism, they cannot use it to circumvent other policies.\n\n## Persistent volume claim naming {#generic-ephemeral-vols-pvc-naming_generic-ephemeral-volumes}\n\nAutomatically created persistent volume claims (PVCs) are named using pod name and volume name with a hyphen separator, potentially causing conflicts with other pods or manual PVCs.\n\nFor example, `pod-a` with volume `scratch` and `pod` with volume `a-scratch` both end up with the same PVC name, `pod-a-scratch`.\n\nSuch conflicts are detected, and a PVC is only used for an ephemeral volume if it was created for the pod. This check is based on the ownership relationship. An existing PVC is not overwritten or modified, but this does not resolve the conflict. Without the right PVC, a pod cannot start.\n\n> [!IMPORTANT]\n> Be careful when naming pods and volumes inside the same namespace so that naming conflicts do not occur.\n\n## Creating generic ephemeral volumes {#generic-ephemeral-vols-procedure_generic-ephemeral-volumes}\n\nTo create ephemeral volumes that are automatically provisioned and deleted with pod lifecycle, define a `volumeClaimTemplate` in your pod spec specifying storage class, size, and access modes.\n\n**Procedure**\n\n1. Create the `pod` object definition and save it to a file.\n2. Include the generic ephemeral volume information in the file.\n\n   ```yaml {title=\"my-example-pod-with-generic-vols.yaml\"}\n   kind: Pod\n   apiVersion: v1\n   metadata:\n     name: my-app\n   spec:\n     containers:\n       - name: my-frontend\n         image: busybox:1.28\n         volumeMounts:\n         - mountPath: \"/mnt/storage\"\n           name: data\n         command: [ \"sleep\", \"1000000\" ]\n     volumes:\n       - name: data\n         ephemeral:\n           volumeClaimTemplate:\n             metadata:\n               labels:\n                 type: my-app-ephvol\n             spec:\n               accessModes: [ \"ReadWriteOnce\" ]\n               storageClassName: \"gp2-csi\"\n               resources:\n                 requests:\n                   storage: 1Gi\n   ```\n\n   Where `spec.volumes.name` is the name of the generic ephemeral volume.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/storage/generic-ephemeral-vols.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="generic-ephemeral-volumes" class="group scroll-mt-20">Generic ephemeral volumes<a href="#generic-ephemeral-volumes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Generic ephemeral volumes provide per-pod temporary storage backed by any storage driver that supports dynamic provisioning, unlike <code>emptyDir</code> volumes which are limited to local node storage. This flexibility lets you use network storage backends, control storage classes and volume characteristics, and leverage delayed volume binding for optimal pod scheduling.</p>
      <h2 id="generic-ephemeral-vols-overview_generic-ephemeral-volumes" class="group scroll-mt-20">Overview of generic ephemeral volumes<a href="#generic-ephemeral-vols-overview_generic-ephemeral-volumes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Generic ephemeral volumes support network-attached storage, size limits, initial data population, and operations like cloning and snapshotting for temporary storage, with some driver-specific limitations.</p>
      <p>Generic ephemeral volumes have the following features:</p>
      <ul>
      <li>Storage can be local or network-attached.</li>
      <li>Volumes can have a fixed size that pods cannot exceed.</li>
      <li>Volumes might have some initial data, depending on the driver and parameters.</li>
      <li>Typical operations on volumes are supported, assuming that the driver supports them, including snapshotting, cloning, resizing, and storage capacity tracking.</li>
      </ul>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Generic ephemeral volumes do not support offline snapshotting and resizing.</p>
          <p>Due to this limitation, the following Container Storage Interface (CSI) drivers do not support the following features for generic ephemeral volumes:</p>
          <ul>
          <li>Azure Disk CSI driver does not support resize.</li>
          <li>Cinder CSI driver does not support snapshot.</li>
          </ul>
        </AlertDescription>
      </Alert>
      <h2 id="generic-ephemeral-vols-lifecycle_generic-ephemeral-volumes" class="group scroll-mt-20">Lifecycle and persistent volume claims<a href="#generic-ephemeral-vols-lifecycle_generic-ephemeral-volumes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Generic ephemeral volumes follow pod lifecycle through automatically managed persistent volume claims created at pod startup and deleted at termination. Choose volume binding mode and reclaim policy based on this lifecycle behavior.</p>
      <p>Generic ephemeral volumes are specified inline in the pod spec and follow the pod’s lifecycle. They are created and deleted along with the pod.</p>
      <p>The parameters for a volume claim are allowed inside a volume source of a pod. Labels, annotations, and the whole set of fields for PVCs are supported. When such a pod is created, the ephemeral volume controller then creates an actual PVC object (from the template shown in the <em>Creating generic ephemeral volumes</em> procedure) in the same namespace as the pod, and ensures that the PVC is deleted when the pod is deleted.</p>
      <p>This triggers volume binding and provisioning in one of two ways:</p>
      <ul>
      <li>
      Either immediately, if the storage class uses immediate volume binding.
      <p>With immediate binding, the scheduler is forced to select a node that has access to the volume after it is available.</p>
      </li>
      <li>
      When the pod is tentatively scheduled onto a node (<code>WaitForFirstConsumer</code> volume binding mode).
      <p>This volume binding option is recommended for generic ephemeral volumes because then the scheduler can choose a suitable node for the pod.</p>
      </li>
      </ul>
      <p>In terms of resource ownership, a pod that has generic ephemeral storage is the owner of the PVCs that provide that ephemeral storage. When the pod is deleted, the Kubernetes garbage collector deletes the PVC, which then usually triggers deletion of the volume because the default reclaim policy of storage classes is to delete volumes. You can create quasi-ephemeral local storage by using a storage class with a reclaim policy of retain. The storage outlives the pod, and in this case, you must ensure that volume clean-up happens separately. While these PVCs exist, they can be used like any other PVC. In particular, they can be referenced as data sources in volume cloning or snapshotting. The PVC object also holds the current status of the volume.</p>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="/openshift-docs-markdown/storage/generic-ephemeral-vols#generic-ephemeral-vols-procedure_generic-ephemeral-volumes">Creating generic ephemeral volumes</a></li></ul>
      </section>
      <h2 id="generic-ephemeral-security_generic-ephemeral-volumes" class="group scroll-mt-20">Security<a href="#generic-ephemeral-security_generic-ephemeral-volumes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Generic ephemeral volumes allow users who can create pods to indirectly create persistent volume claims (PVCs), even without direct PVC creation permissions. You can restrict this behavior if it conflicts with your security model.</p>
      <p>To restrict this behavior, use an admission webhook that rejects objects such as pods that have a generic ephemeral volume.</p>
      <p>The normal namespace quota for PVCs still applies, so even if users are allowed to use this new mechanism, they cannot use it to circumvent other policies.</p>
      <h2 id="generic-ephemeral-vols-pvc-naming_generic-ephemeral-volumes" class="group scroll-mt-20">Persistent volume claim naming<a href="#generic-ephemeral-vols-pvc-naming_generic-ephemeral-volumes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Automatically created persistent volume claims (PVCs) are named using pod name and volume name with a hyphen separator, potentially causing conflicts with other pods or manual PVCs.</p>
      <p>For example, <code>pod-a</code> with volume <code>scratch</code> and <code>pod</code> with volume <code>a-scratch</code> both end up with the same PVC name, <code>pod-a-scratch</code>.</p>
      <p>Such conflicts are detected, and a PVC is only used for an ephemeral volume if it was created for the pod. This check is based on the ownership relationship. An existing PVC is not overwritten or modified, but this does not resolve the conflict. Without the right PVC, a pod cannot start.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Be careful when naming pods and volumes inside the same namespace so that naming conflicts do not occur.</p>
        </AlertDescription>
      </Alert>
      <h2 id="generic-ephemeral-vols-procedure_generic-ephemeral-volumes" class="group scroll-mt-20">Creating generic ephemeral volumes<a href="#generic-ephemeral-vols-procedure_generic-ephemeral-volumes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To create ephemeral volumes that are automatically provisioned and deleted with pod lifecycle, define a <code>volumeClaimTemplate</code> in your pod spec specifying storage class, size, and access modes.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Create the <code>pod</code> object definition and save it to a file.</li>
      <li>
      Include the generic ephemeral volume information in the file.
      <CodeBlock code={"kind: Pod\napiVersion: v1\nmetadata:\n  name: my-app\nspec:\n  containers:\n    - name: my-frontend\n      image: busybox:1.28\n      volumeMounts:\n      - mountPath: \"/mnt/storage\"\n        name: data\n      command: [ \"sleep\", \"1000000\" ]\n  volumes:\n    - name: data\n      ephemeral:\n        volumeClaimTemplate:\n          metadata:\n            labels:\n              type: my-app-ephvol\n          spec:\n            accessModes: [ \"ReadWriteOnce\" ]\n            storageClassName: \"gp2-csi\"\n            resources:\n              requests:\n                storage: 1Gi\n"} lang="yaml" title="my-example-pod-with-generic-vols.yaml" />
      <p>Where <code>spec.volumes.name</code> is the name of the generic ephemeral volume.</p>
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
