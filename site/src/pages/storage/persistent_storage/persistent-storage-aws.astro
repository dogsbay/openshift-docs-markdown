---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: storage/persistent_storage/persistent-storage-aws.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"persistent-storage-aws","text":"Persistent storage using AWS Elastic Block Store"},{"depth":2,"slug":"storage-create-storage-class_persistent-storage-aws","text":"About the EBS storage class"},{"depth":2,"slug":"creating-volume-claim_persistent-storage-aws","text":"Creating the persistent volume claim"},{"depth":2,"slug":"volume-format-AWS_persistent-storage-aws","text":"Volume format"},{"depth":2,"slug":"maximum-number-of-ebs-volumes-on-a-node_persistent-storage-aws","text":"Maximum number of EBS volumes on a node"},{"depth":2,"slug":"aws-container-persistent-volumes-encrypt_persistent-storage-aws","text":"Encrypting container persistent volumes on AWS with a KMS key"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/storage/persistent_storage/persistent-storage-aws";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Persistent storage using AWS Elastic Block Store";
const description = "OpenShift Container Platform supports Amazon Elastic Block Store (EBS) volumes. You can provision your OpenShift Container Platform cluster with…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["storage","storage/persistent_storage"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Persistent storage using AWS Elastic Block Store {#persistent-storage-aws}\n\nOpenShift Container Platform supports Amazon Elastic Block Store (EBS) volumes. You can provision your OpenShift Container Platform cluster with persistent storage by using Amazon EC2.\n\nThe Kubernetes persistent volume framework allows administrators to provision a cluster with persistent storage and gives users a way to request those resources without having any knowledge of the underlying infrastructure. You can dynamically provision Amazon EBS volumes. Persistent volumes are not bound to a single project or namespace; they can be shared across the OpenShift Container Platform cluster. Persistent volume claims are specific to a project or namespace and can be requested by users. You can define a KMS key to encrypt container-persistent volumes on AWS. By default, newly created clusters by using OpenShift Container Platform version 4.10 and later use gp3 storage and the AWS EBS CSI driver.\n\n> [!IMPORTANT]\n> High-availability of storage in the infrastructure is left to the underlying storage provider.\n\n> [!IMPORTANT]\n> OpenShift Container Platform 4.12 and later provides automatic migration for the AWS Block in-tree volume plugin to its equivalent CSI driver.\n>\n> CSI automatic migration should be seamless. Migration does not change how you use all existing API objects, such as persistent volumes, persistent volume claims, and storage classes. For more information about migration, see CSI automatic migration.\n\n## About the EBS storage class {#storage-create-storage-class_persistent-storage-aws}\n\nTo enable dynamic provisioning of persistent volumes, create a storage class that defines storage characteristics and allows users to automatically provision volumes on-demand.\n\n## Creating the persistent volume claim {#creating-volume-claim_persistent-storage-aws}\n\nYou can create a persistent volume claim to dynamically provision and bind storage from a pre-configured storage class, so that your applications can consume persistent storage in OpenShift Container Platform.\n\n**Prerequisites**\n\n- Storage must exist in the underlying infrastructure before it can be mounted as a volume in OpenShift Container Platform.\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, click **Storage** → **Persistent Volume Claims**.\n2. In the persistent volume claims overview, click **Create Persistent Volume Claim**.\n3. Define the required options on the page that is displayed.\n\n   1. Select the previously-created storage class from the drop-down menu.\n   2. Enter a unique name for the storage claim.\n   3. Select the access mode. This selection determines the read and write access for the storage claim.\n   4. Define the size of the storage claim.\n4. Click **Create** to create the persistent volume claim and generate a persistent volume.\n\n## Volume format {#volume-format-AWS_persistent-storage-aws}\n\nYou can use unformatted AWS volumes as persistent volumes, because OpenShift Container Platform automatically formats the device before mounting it to a container.\n\nBefore OpenShift Container Platform mounts the volume and passes it to a container, it checks that the volume contains a file system as specified by the `fsType` parameter in the persistent volume definition. If the device is not formatted with the file system, all data from the device is erased and the device is automatically formatted with the given file system.\n\n## Maximum number of EBS volumes on a node {#maximum-number-of-ebs-volumes-on-a-node_persistent-storage-aws}\n\nBy default, you can attach a maximum of 39 EBS volumes attached to one node. This limit is consistent with the AWS volume limits. The volume limit depends on the instance type.\n\n> [!IMPORTANT]\n> As a cluster administrator, you must use either in-tree or Container Storage Interface (CSI) volumes and their respective storage classes, but never both volume types at the same time. The maximum attached EBS volume number is counted separately for in-tree and CSI volumes, which means you could have up to 39 EBS volumes of each type.\n\n## Encrypting container persistent volumes on AWS with a KMS key {#aws-container-persistent-volumes-encrypt_persistent-storage-aws}\n\nYou can define a KMS key to encrypt container-persistent volumes on AWS if you have explicit compliance and security guidelines when deploying to AWS.\n\n**Prerequisites**\n\n- Underlying infrastructure must contain storage.\n- You must create a customer KMS key on AWS.\n\n**Procedure**\n\n1. Create a storage class:\n\n   ```yaml\n   $ cat << EOF | oc create -f -\n   apiVersion: storage.k8s.io/v1\n   kind: StorageClass\n   metadata:\n     name: <storage-class-name>\n   parameters:\n     fsType: ext4\n     encrypted: \"true\"\n     kmsKeyId: keyvalue\n   provisioner: ebs.csi.aws.com\n   reclaimPolicy: Delete\n   volumeBindingMode: WaitForFirstConsumer\n   EOF\n   ```\n\n   where:\n\n   `metadata.name`\n   :   Specifies the name of the storage class.\n\n   `parameters.fsType`\n   :   Specifies the file system that is created on provisioned volumes.\n\n   `parameters.kmsKeyId`\n   :   Specifies the full Amazon Resource Name (ARN) of the key to use when encrypting the container-persistent volume. If you do not provide any key, but the `encrypted` field is set to `true`, then the default KMS key is used.\n2. Create a persistent volume claim (PVC) with the storage class specifying the KMS key:\n\n   ```yaml\n   $ cat << EOF | oc create -f -\n   apiVersion: v1\n   kind: PersistentVolumeClaim\n   metadata:\n     name: mypvc\n   spec:\n     accessModes:\n       - ReadWriteOnce\n     volumeMode: Filesystem\n     storageClassName: <storage-class-name>\n     resources:\n       requests:\n         storage: 1Gi\n   EOF\n   ```\n3. Create workload containers to consume the PVC:\n\n   ```yaml\n   $ cat << EOF | oc create -f -\n   kind: Pod\n   metadata:\n     name: mypod\n   spec:\n     containers:\n       - name: httpd\n         image: quay.io/centos7/httpd-24-centos7\n         ports:\n           - containerPort: 80\n         volumeMounts:\n           - mountPath: /mnt/storage\n             name: data\n     volumes:\n       - name: data\n         persistentVolumeClaim:\n           claimName: mypvc\n   EOF\n   ```\n\n**Additional resources**\n{._additional-resources}\n\n- [Amazon EC2 documentation](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/concepts.html)\n- [AWS EBS CSI driver](https://github.com/openshift/aws-ebs-csi-driver)\n- [CSI automatic migration](/openshift-docs-markdown/storage/container_storage_interface/persistent-storage-csi-migration#persistent-storage-csi-migration)\n- [AWS Elastic Block Store CSI Driver Operator](/openshift-docs-markdown/storage/container_storage_interface/persistent-storage-csi-ebs#persistent-storage-csi-ebs)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/storage/persistent_storage/persistent-storage-aws.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="persistent-storage-aws" class="group scroll-mt-20">Persistent storage using AWS Elastic Block Store<a href="#persistent-storage-aws" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>OpenShift Container Platform supports Amazon Elastic Block Store (EBS) volumes. You can provision your OpenShift Container Platform cluster with persistent storage by using Amazon EC2.</p>
      <p>The Kubernetes persistent volume framework allows administrators to provision a cluster with persistent storage and gives users a way to request those resources without having any knowledge of the underlying infrastructure. You can dynamically provision Amazon EBS volumes. Persistent volumes are not bound to a single project or namespace; they can be shared across the OpenShift Container Platform cluster. Persistent volume claims are specific to a project or namespace and can be requested by users. You can define a KMS key to encrypt container-persistent volumes on AWS. By default, newly created clusters by using OpenShift Container Platform version 4.10 and later use gp3 storage and the AWS EBS CSI driver.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>High-availability of storage in the infrastructure is left to the underlying storage provider.</p>
        </AlertDescription>
      </Alert>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>OpenShift Container Platform 4.12 and later provides automatic migration for the AWS Block in-tree volume plugin to its equivalent CSI driver.</p>
          <p>CSI automatic migration should be seamless. Migration does not change how you use all existing API objects, such as persistent volumes, persistent volume claims, and storage classes. For more information about migration, see CSI automatic migration.</p>
        </AlertDescription>
      </Alert>
      <h2 id="storage-create-storage-class_persistent-storage-aws" class="group scroll-mt-20">About the EBS storage class<a href="#storage-create-storage-class_persistent-storage-aws" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To enable dynamic provisioning of persistent volumes, create a storage class that defines storage characteristics and allows users to automatically provision volumes on-demand.</p>
      <h2 id="creating-volume-claim_persistent-storage-aws" class="group scroll-mt-20">Creating the persistent volume claim<a href="#creating-volume-claim_persistent-storage-aws" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can create a persistent volume claim to dynamically provision and bind storage from a pre-configured storage class, so that your applications can consume persistent storage in OpenShift Container Platform.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>Storage must exist in the underlying infrastructure before it can be mounted as a volume in OpenShift Container Platform.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, click <strong>Storage</strong> → <strong>Persistent Volume Claims</strong>.</li>
      <li>In the persistent volume claims overview, click <strong>Create Persistent Volume Claim</strong>.</li>
      <li>
      Define the required options on the page that is displayed.
      <ol>
      <li>Select the previously-created storage class from the drop-down menu.</li>
      <li>Enter a unique name for the storage claim.</li>
      <li>Select the access mode. This selection determines the read and write access for the storage claim.</li>
      <li>Define the size of the storage claim.</li>
      </ol>
      </li>
      <li>Click <strong>Create</strong> to create the persistent volume claim and generate a persistent volume.</li>
      </ol>
      <h2 id="volume-format-AWS_persistent-storage-aws" class="group scroll-mt-20">Volume format<a href="#volume-format-AWS_persistent-storage-aws" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can use unformatted AWS volumes as persistent volumes, because OpenShift Container Platform automatically formats the device before mounting it to a container.</p>
      <p>Before OpenShift Container Platform mounts the volume and passes it to a container, it checks that the volume contains a file system as specified by the <code>fsType</code> parameter in the persistent volume definition. If the device is not formatted with the file system, all data from the device is erased and the device is automatically formatted with the given file system.</p>
      <h2 id="maximum-number-of-ebs-volumes-on-a-node_persistent-storage-aws" class="group scroll-mt-20">Maximum number of EBS volumes on a node<a href="#maximum-number-of-ebs-volumes-on-a-node_persistent-storage-aws" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">By default, you can attach a maximum of 39 EBS volumes attached to one node. This limit is consistent with the AWS volume limits. The volume limit depends on the instance type.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>As a cluster administrator, you must use either in-tree or Container Storage Interface (CSI) volumes and their respective storage classes, but never both volume types at the same time. The maximum attached EBS volume number is counted separately for in-tree and CSI volumes, which means you could have up to 39 EBS volumes of each type.</p>
        </AlertDescription>
      </Alert>
      <h2 id="aws-container-persistent-volumes-encrypt_persistent-storage-aws" class="group scroll-mt-20">Encrypting container persistent volumes on AWS with a KMS key<a href="#aws-container-persistent-volumes-encrypt_persistent-storage-aws" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can define a KMS key to encrypt container-persistent volumes on AWS if you have explicit compliance and security guidelines when deploying to AWS.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>Underlying infrastructure must contain storage.</li>
      <li>You must create a customer KMS key on AWS.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a storage class:
      <CodeBlock code={"$ cat << EOF | oc create -f -\napiVersion: storage.k8s.io/v1\nkind: StorageClass\nmetadata:\n  name: <storage-class-name>\nparameters:\n  fsType: ext4\n  encrypted: \"true\"\n  kmsKeyId: keyvalue\nprovisioner: ebs.csi.aws.com\nreclaimPolicy: Delete\nvolumeBindingMode: WaitForFirstConsumer\nEOF"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>metadata.name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the storage class.</p></dd>
      <dt class="font-semibold"><code>parameters.fsType</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the file system that is created on provisioned volumes.</p></dd>
      <dt class="font-semibold"><code>parameters.kmsKeyId</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the full Amazon Resource Name (ARN) of the key to use when encrypting the container-persistent volume. If you do not provide any key, but the <code>encrypted</code> field is set to <code>true</code>, then the default KMS key is used.</p></dd>
      </dl>
      </li>
      <li>
      Create a persistent volume claim (PVC) with the storage class specifying the KMS key:
      <CodeBlock code={"$ cat << EOF | oc create -f -\napiVersion: v1\nkind: PersistentVolumeClaim\nmetadata:\n  name: mypvc\nspec:\n  accessModes:\n    - ReadWriteOnce\n  volumeMode: Filesystem\n  storageClassName: <storage-class-name>\n  resources:\n    requests:\n      storage: 1Gi\nEOF"} lang="yaml" />
      </li>
      <li>
      Create workload containers to consume the PVC:
      <CodeBlock code={"$ cat << EOF | oc create -f -\nkind: Pod\nmetadata:\n  name: mypod\nspec:\n  containers:\n    - name: httpd\n      image: quay.io/centos7/httpd-24-centos7\n      ports:\n        - containerPort: 80\n      volumeMounts:\n        - mountPath: /mnt/storage\n          name: data\n  volumes:\n    - name: data\n      persistentVolumeClaim:\n        claimName: mypvc\nEOF"} lang="yaml" />
      </li>
      </ol>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/concepts.html">Amazon EC2 documentation</a></li>
      <li><a href="https://github.com/openshift/aws-ebs-csi-driver">AWS EBS CSI driver</a></li>
      <li><a href="/openshift-docs-markdown/storage/container_storage_interface/persistent-storage-csi-migration#persistent-storage-csi-migration">CSI automatic migration</a></li>
      <li><a href="/openshift-docs-markdown/storage/container_storage_interface/persistent-storage-csi-ebs#persistent-storage-csi-ebs">AWS Elastic Block Store CSI Driver Operator</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
