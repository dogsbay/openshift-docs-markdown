---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: storage/persistent_storage/persistent-storage-fibre.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"persistent-storage-using-fibre","text":"Persistent storage using Fibre Channel"},{"depth":2,"slug":"provisioning-fibre_persistent-storage-fibre","text":"About provisioning storage using Fibre Channel"},{"depth":3,"slug":"enforcing-disk-quota_persistent-storage-fibre","text":"Enforcing disk quotas"},{"depth":3,"slug":"fibre-volume-security_persistent-storage-fibre","text":"Fibre Channel volume security"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/storage/persistent_storage/persistent-storage-fibre";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Persistent storage using Fibre Channel";
const description = "You can provision your OpenShift Container Platform cluster with persistent storage by using Fibre Channel volumes for workloads that require high-speed…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["storage","storage/persistent_storage"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Persistent storage using Fibre Channel {#persistent-storage-using-fibre}\n\nYou can provision your OpenShift Container Platform cluster with persistent storage by using Fibre Channel volumes for workloads that require high-speed, reliable block-level storage.\n\n> [!IMPORTANT]\n> Persistent storage using Fibre Channel is not supported on ARM architecture based infrastructures.\n\nThe Kubernetes persistent volume framework allows administrators to provision a cluster with persistent storage and gives users a way to request those resources without having any knowledge of the underlying infrastructure. Persistent volumes are not bound to a single project or namespace; they can be shared across the OpenShift Container Platform cluster. Persistent volume claims are specific to a project or namespace and can be requested by users.\n\n> [!IMPORTANT]\n> High availability of storage in the infrastructure is left to the underlying storage provider.\n\n**Additional resources**\n\n- [Using Fibre Channel devices](https://access.redhat.com/documentation/en-us/red_hat_enterprise_linux/8/html/managing_storage_devices/using-fibre-channel-devices_managing-storage-devices)\n\n## About provisioning storage using Fibre Channel {#provisioning-fibre_persistent-storage-fibre}\n\nYou can provision Fibre Channel volumes by using the `PersistentVolume` API.\n\nBefore provisioning the volumes, the following items must be available.\n\n- The `targetWWNs` (array of Fibre Channel target’s World Wide Names).\n- A valid LUN number.\n- The filesystem type.\n\nA persistent volume and a LUN have a one-to-one mapping between them.\n\n> [!NOTE]\n> Fibre Channel LUNs must exist in the underlying infrastructure.\n\n```yaml {title=\"PersistentVolume object definition\"}\napiVersion: v1\nkind: PersistentVolume\nmetadata:\n  name: pv0001\nspec:\n  capacity:\n    storage: 1Gi\n  accessModes:\n    - ReadWriteOnce\n  fc:\n    wwids: [3600508b400105e210000900000490000]\n    targetWWNs: ['500a0981891b8dc5', '500a0981991b8dc5']\n    lun: 2\n    fsType: ext4\n```\n\nwhere:\n\n`spec.fc.wwids`\n:   Specifies the world wide identifiers (WWIDs). Either FC `wwids` or a combination of FC `targetWWNs` and `lun` must be set, but not both simultaneously. The FC WWID identifier is recommended over the WWNs target because it is guaranteed to be unique for every storage device, and independent of the path that is used to access the device. The WWID identifier can be obtained by issuing a SCSI Inquiry to retrieve the Device Identification Vital Product Data (`page 0x83`) or Unit Serial Number (`page 0x80`). FC WWIDs are identified as `/dev/disk/by-id/` to reference the data on the disk, even if the path to the device changes and even when accessing the device from different systems.\n\n`spec.fc.targetWWNs`\n:   Specifies the Fibre Channel World Wide Names (WWNs). Fibre Channel WWNs are identified as `/dev/disk/by-path/pci-<IDENTIFIER>-fc-0x<WWN>-lun-<LUN#>`, but you do not need to provide any part of the path leading up to the `WWN`, including the `0x`, and anything after, including the `-` (hyphen).\n\n`spec.fc.lun`\n:   Specifies the LUN number.\n\n> [!IMPORTANT]\n> Changing the value of the `fstype` parameter after the volume has been formatted and provisioned can result in data loss and pod failure.\n\n### Enforcing disk quotas {#enforcing-disk-quota_persistent-storage-fibre}\n\nYou can use LUN partitions to enforce disk quotas and size constraints. Each LUN is mapped to a single persistent volume, and unique names must be used for persistent volumes.\n\nEnforcing quotas in this way allows the user to request persistent storage by a specific amount, such as 10Gi, and be matched with a corresponding volume of equal or greater capacity.\n\n### Fibre Channel volume security {#fibre-volume-security_persistent-storage-fibre}\n\nYou can request storage with a persistent volume claim. This claim only lives in the user’s namespace, and can only be referenced by a pod within that same namespace. Any attempt to access a persistent volume across a namespace causes the pod to fail.\n\nEach Fibre Channel LUN must be accessible by all nodes in the cluster.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/storage/persistent_storage/persistent-storage-fibre.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="persistent-storage-using-fibre" class="group scroll-mt-20">Persistent storage using Fibre Channel<a href="#persistent-storage-using-fibre" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can provision your OpenShift Container Platform cluster with persistent storage by using Fibre Channel volumes for workloads that require high-speed, reliable block-level storage.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Persistent storage using Fibre Channel is not supported on ARM architecture based infrastructures.</p>
        </AlertDescription>
      </Alert>
      <p>The Kubernetes persistent volume framework allows administrators to provision a cluster with persistent storage and gives users a way to request those resources without having any knowledge of the underlying infrastructure. Persistent volumes are not bound to a single project or namespace; they can be shared across the OpenShift Container Platform cluster. Persistent volume claims are specific to a project or namespace and can be requested by users.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>High availability of storage in the infrastructure is left to the underlying storage provider.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="https://access.redhat.com/documentation/en-us/red_hat_enterprise_linux/8/html/managing_storage_devices/using-fibre-channel-devices_managing-storage-devices">Using Fibre Channel devices</a></li></ul>
      <h2 id="provisioning-fibre_persistent-storage-fibre" class="group scroll-mt-20">About provisioning storage using Fibre Channel<a href="#provisioning-fibre_persistent-storage-fibre" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can provision Fibre Channel volumes by using the <code>PersistentVolume</code> API.</p>
      <p>Before provisioning the volumes, the following items must be available.</p>
      <ul>
      <li>The <code>targetWWNs</code> (array of Fibre Channel target’s World Wide Names).</li>
      <li>A valid LUN number.</li>
      <li>The filesystem type.</li>
      </ul>
      <p>A persistent volume and a LUN have a one-to-one mapping between them.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Fibre Channel LUNs must exist in the underlying infrastructure.</p>
        </AlertDescription>
      </Alert>
      <CodeBlock code={"apiVersion: v1\nkind: PersistentVolume\nmetadata:\n  name: pv0001\nspec:\n  capacity:\n    storage: 1Gi\n  accessModes:\n    - ReadWriteOnce\n  fc:\n    wwids: [3600508b400105e210000900000490000]\n    targetWWNs: ['500a0981891b8dc5', '500a0981991b8dc5']\n    lun: 2\n    fsType: ext4"} lang="yaml" title="PersistentVolume object definition" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.fc.wwids</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the world wide identifiers (WWIDs). Either FC <code>wwids</code> or a combination of FC <code>targetWWNs</code> and <code>lun</code> must be set, but not both simultaneously. The FC WWID identifier is recommended over the WWNs target because it is guaranteed to be unique for every storage device, and independent of the path that is used to access the device. The WWID identifier can be obtained by issuing a SCSI Inquiry to retrieve the Device Identification Vital Product Data (<code>page 0x83</code>) or Unit Serial Number (<code>page 0x80</code>). FC WWIDs are identified as <code>/dev/disk/by-id/</code> to reference the data on the disk, even if the path to the device changes and even when accessing the device from different systems.</p></dd>
      <dt class="font-semibold"><code>spec.fc.targetWWNs</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the Fibre Channel World Wide Names (WWNs). Fibre Channel WWNs are identified as <code>/dev/disk/by-path/pci-&lt;IDENTIFIER&gt;-fc-0x&lt;WWN&gt;-lun-&lt;LUN#&gt;</code>, but you do not need to provide any part of the path leading up to the <code>WWN</code>, including the <code>0x</code>, and anything after, including the <code>-</code> (hyphen).</p></dd>
      <dt class="font-semibold"><code>spec.fc.lun</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the LUN number.</p></dd>
      </dl>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Changing the value of the <code>fstype</code> parameter after the volume has been formatted and provisioned can result in data loss and pod failure.</p>
        </AlertDescription>
      </Alert>
      <h3 id="enforcing-disk-quota_persistent-storage-fibre" class="group scroll-mt-20">Enforcing disk quotas<a href="#enforcing-disk-quota_persistent-storage-fibre" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can use LUN partitions to enforce disk quotas and size constraints. Each LUN is mapped to a single persistent volume, and unique names must be used for persistent volumes.</p>
      <p>Enforcing quotas in this way allows the user to request persistent storage by a specific amount, such as 10Gi, and be matched with a corresponding volume of equal or greater capacity.</p>
      <h3 id="fibre-volume-security_persistent-storage-fibre" class="group scroll-mt-20">Fibre Channel volume security<a href="#fibre-volume-security_persistent-storage-fibre" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can request storage with a persistent volume claim. This claim only lives in the user’s namespace, and can only be referenced by a pod within that same namespace. Any attempt to access a persistent volume across a namespace causes the pod to fail.</p>
      <p>Each Fibre Channel LUN must be accessible by all nodes in the cluster.</p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
