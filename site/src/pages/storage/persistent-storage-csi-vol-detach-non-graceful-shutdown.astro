---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: storage/persistent-storage-csi-vol-detach-non-graceful-shutdown.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"ephemeral-storage-csi-vol-detach-non-graceful-shutdown","text":"Detach volumes after non-graceful node shutdown"},{"depth":2,"slug":"persistent-storage-csi-vol-detach-non-graceful-overview_ephemeral-storage-csi-vol-detach-non-graceful-shutdown","text":"Overview"},{"depth":2,"slug":"persistent-storage-csi-vol-detach-non-graceful-shutdown-procedure_ephemeral-storage-csi-vol-detach-non-graceful-shutdown","text":"Adding an out-of-service taint manually for automatic volume detachment"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/storage/persistent-storage-csi-vol-detach-non-graceful-shutdown";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Detach volumes after non-graceful node shutdown";
const description = "[role=\"_abstract\"] Automatic volume detachment after non-graceful node shutdowns prevents volumes from remaining attached to failed nodes, enabling faster…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["storage"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Detach volumes after non-graceful node shutdown {#ephemeral-storage-csi-vol-detach-non-graceful-shutdown}\n\n\\[role=\"\\_abstract\"\\] Automatic volume detachment after non-graceful node shutdowns prevents volumes from remaining attached to failed nodes, enabling faster workload recovery by allowing pods to reschedule and reattach volumes on healthy nodes without manual intervention.\n\n## Overview {#persistent-storage-csi-vol-detach-non-graceful-overview_ephemeral-storage-csi-vol-detach-non-graceful-shutdown}\n\n\\[role=\"\\_abstract\"\\] Non-graceful node shutdowns from hardware failures or system crashes leave volumes attached to failed nodes, blocking pod rescheduling. Applying an out-of-service taint triggers automatic volume detachment from failed nodes, enabling workload recovery without manual volume management.\n\nA graceful node shutdown occurs when the kubelet’s node shutdown manager detects the upcoming node shutdown action. Non-graceful shutdowns occur when the kubelet does not detect a node shutdown action, which can occur because of system or hardware failures. Also, the kubelet might not detect a node shutdown action when the shutdown command does not trigger the Inhibitor Locks mechanism used by the kubelet on Linux, or because of a user error, for example, if the shutdownGracePeriod and shutdownGracePeriodCriticalPods details are not configured correctly for that node.\n\n## Adding an out-of-service taint manually for automatic volume detachment {#persistent-storage-csi-vol-detach-non-graceful-shutdown-procedure_ephemeral-storage-csi-vol-detach-non-graceful-shutdown}\n\n\\[role=\"\\_abstract\"\\] After non-graceful shutdowns, to trigger automatic volume detachment and enable pod rescheduling, apply an out-of-service taint to the node. This recovers workloads faster than manually detaching volumes from failed nodes.\n\n**Prerequisites**\n\n- Access to the cluster with cluster-admin privileges.\n\n**Procedure**\n\n1. After a node is detected as unhealthy, shut down the worker node.\n2. Ensure that the node is shutdown by running the following command and checking the status:\n\n   ```terminal\n   $ oc get node <node_name>\n   ```\n\n   - Use the `<node_name>` to specify the node that shut down non-gracefully.\n\n     > [!IMPORTANT]\n     > If the node is not completely shut down, do not proceed with tainting the node. If the node is still up and the taint is applied, filesystem corruption can occur.\n3. Taint the corresponding node object by running the following command:\n\n   > [!IMPORTANT]\n   > Tainting a node this way deletes all pods on that node. This also causes any pods that are backed by statefulsets to be evicted, and replacement pods to be created on a different node.\n\n   ```terminal\n   $ oc adm taint node <node_name> node.kubernetes.io/out-of-service=nodeshutdown:NoExecute\n   ```\n\n   - Use the `<node_name>` to specify the node that shut down non-gracefully.\n\n     After the taint is applied, the volumes detach from the shutdown node allowing their disks to be attached to a different node.\n\n     The resulting YAML file resembles the following example file:\n\n     ```yaml {title=\"Example node YAML file with out-of-service taint applied\"}\n     spec:\n       taints:\n       - effect: NoExecute\n         key: node.kubernetes.io/out-of-service\n         value: nodeshutdown\n     ```\n4. Restart the node.\n5. Remove the taint from the corresponding node object by running the following command:\n\n   ```terminal\n   $ oc adm taint node <node_name> node.kubernetes.io/out-of-service=nodeshutdown:NoExecute-\n   ```\n\n   - Use the `<node_name>` to specify the node that shut down non-gracefully\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/storage/persistent-storage-csi-vol-detach-non-graceful-shutdown.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="ephemeral-storage-csi-vol-detach-non-graceful-shutdown" class="group scroll-mt-20">Detach volumes after non-graceful node shutdown<a href="#ephemeral-storage-csi-vol-detach-non-graceful-shutdown" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>[role="_abstract"] Automatic volume detachment after non-graceful node shutdowns prevents volumes from remaining attached to failed nodes, enabling faster workload recovery by allowing pods to reschedule and reattach volumes on healthy nodes without manual intervention.</p>
      <h2 id="persistent-storage-csi-vol-detach-non-graceful-overview_ephemeral-storage-csi-vol-detach-non-graceful-shutdown" class="group scroll-mt-20">Overview<a href="#persistent-storage-csi-vol-detach-non-graceful-overview_ephemeral-storage-csi-vol-detach-non-graceful-shutdown" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>[role="_abstract"] Non-graceful node shutdowns from hardware failures or system crashes leave volumes attached to failed nodes, blocking pod rescheduling. Applying an out-of-service taint triggers automatic volume detachment from failed nodes, enabling workload recovery without manual volume management.</p>
      <p>A graceful node shutdown occurs when the kubelet’s node shutdown manager detects the upcoming node shutdown action. Non-graceful shutdowns occur when the kubelet does not detect a node shutdown action, which can occur because of system or hardware failures. Also, the kubelet might not detect a node shutdown action when the shutdown command does not trigger the Inhibitor Locks mechanism used by the kubelet on Linux, or because of a user error, for example, if the shutdownGracePeriod and shutdownGracePeriodCriticalPods details are not configured correctly for that node.</p>
      <h2 id="persistent-storage-csi-vol-detach-non-graceful-shutdown-procedure_ephemeral-storage-csi-vol-detach-non-graceful-shutdown" class="group scroll-mt-20">Adding an out-of-service taint manually for automatic volume detachment<a href="#persistent-storage-csi-vol-detach-non-graceful-shutdown-procedure_ephemeral-storage-csi-vol-detach-non-graceful-shutdown" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>[role="_abstract"] After non-graceful shutdowns, to trigger automatic volume detachment and enable pod rescheduling, apply an out-of-service taint to the node. This recovers workloads faster than manually detaching volumes from failed nodes.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>Access to the cluster with cluster-admin privileges.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>After a node is detected as unhealthy, shut down the worker node.</li>
      <li>
      Ensure that the node is shutdown by running the following command and checking the status:
      <CodeBlock code={"$ oc get node <node_name>"} lang="terminal" />
      <ul>
      <li>
      Use the <code>&lt;node_name&gt;</code> to specify the node that shut down non-gracefully.
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>If the node is not completely shut down, do not proceed with tainting the node. If the node is still up and the taint is applied, filesystem corruption can occur.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      </li>
      <li>
      Taint the corresponding node object by running the following command:
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Tainting a node this way deletes all pods on that node. This also causes any pods that are backed by statefulsets to be evicted, and replacement pods to be created on a different node.</p>
        </AlertDescription>
      </Alert>
      <CodeBlock code={"$ oc adm taint node <node_name> node.kubernetes.io/out-of-service=nodeshutdown:NoExecute"} lang="terminal" />
      <ul>
      <li>
      Use the <code>&lt;node_name&gt;</code> to specify the node that shut down non-gracefully.
      <p>After the taint is applied, the volumes detach from the shutdown node allowing their disks to be attached to a different node.</p>
      <p>The resulting YAML file resembles the following example file:</p>
      <CodeBlock code={"spec:\n  taints:\n  - effect: NoExecute\n    key: node.kubernetes.io/out-of-service\n    value: nodeshutdown"} lang="yaml" title="Example node YAML file with out-of-service taint applied" />
      </li>
      </ul>
      </li>
      <li>Restart the node.</li>
      <li>
      Remove the taint from the corresponding node object by running the following command:
      <CodeBlock code={"$ oc adm taint node <node_name> node.kubernetes.io/out-of-service=nodeshutdown:NoExecute-"} lang="terminal" />
      <ul><li>Use the <code>&lt;node_name&gt;</code> to specify the node that shut down non-gracefully</li></ul>
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
