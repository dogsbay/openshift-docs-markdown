---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: backup_and_restore/control_plane_backup_and_restore/disaster_recovery/quorum-restoration.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"dr-quorum-restoration","text":"Quorum restoration"},{"depth":2,"slug":"dr-restoring-etcd-quorum-ha_dr-quorum-restoration","text":"Restoring etcd quorum for high availability clusters"},{"depth":2,"slug":"additional-resources_dr-quorum-restoration","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/backup_and_restore/control_plane_backup_and_restore/disaster_recovery/quorum-restoration";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Quorum restoration";
const description = "You can restore etcd quorum on your OpenShift Container Platform cluster by running the quorum-restore.sh script on a recovery host when quorum loss…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["backup_and_restore","backup_and_restore/control_plane_backup_and_restore","backup_and_restore/control_plane_backup_and_restore/disaster_recovery"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Quorum restoration {#dr-quorum-restoration}\n\nYou can restore etcd quorum on your OpenShift Container Platform cluster by running the `quorum-restore.sh` script on a recovery host when quorum loss leaves the API read-only. After quorum is restored, the API returns to read/write mode.\n\n## Restoring etcd quorum for high availability clusters {#dr-restoring-etcd-quorum-ha_dr-quorum-restoration}\n\nYou can restore etcd quorum on high availability (HA) clusters by running the `quorum-restore.sh` script on a recovery host. Restored quorum returns the OpenShift Container Platform API to read/write mode when quorum loss takes the cluster offline.\n\nThe `quorum-restore.sh` script creates a new single-member etcd cluster from the local data directory on the recovery host. No prior backup is required.\n\nFor high availability (HA) clusters, a three-node HA cluster requires you to shut down etcd on two hosts to avoid a cluster split. On four-node and five-node HA clusters, you must shut down three hosts. Quorum requires a majority of nodes. The minimum number of nodes required for quorum on a three-node HA cluster is two. On four-node and five-node HA clusters, the minimum number of nodes required for quorum is three. If you start a new cluster from backup on your recovery host, the other etcd members might still be able to form quorum and continue service.\n\n> [!WARNING]\n> You might experience data loss if the host that runs the restoration does not have all data replicated to it.\n\n> [!IMPORTANT]\n> Quorum restoration should not be used to decrease the number of nodes outside of the restoration process. Decreasing the number of nodes results in an unsupported cluster configuration.\n\n**Prerequisites**\n\n- You have SSH access to the node used to restore quorum.\n\n**Procedure**\n\n1. Select a control plane host to use as the recovery host. You run the restore operation on this host.\n\n   1. List the running etcd pods by running the following command:\n\n      ```terminal\n      $ oc get pods -n openshift-etcd -l app=etcd --field-selector=\"status.phase==Running\"\n      ```\n   2. Choose a pod and run the following command to obtain its IP address:\n\n      ```terminal\n      $ oc exec -n openshift-etcd <etcd-pod> -c etcdctl -- etcdctl endpoint status -w table\n      ```\n\n      Note the IP address of a member that is not a learner and has the highest Raft index.\n   3. List nodes by running the following command:\n\n      ```terminal\n      $ oc get nodes -o jsonpath='{range .items[*]}[{.metadata.name},{.status.addresses[?(@.type==\"InternalIP\")].address}]{end}'\n      ```\n\n      Note the node name that corresponds to the IP address of the chosen etcd member.\n2. Using SSH, connect to the chosen recovery node and run the following command to restore etcd quorum:\n\n   ```terminal\n   $ sudo -E /usr/local/bin/quorum-restore.sh\n   ```\n\n   After a few minutes, the nodes that went down are automatically synchronized with the node that the recovery script was run on. Any remaining online nodes automatically rejoin the new etcd cluster created by the `quorum-restore.sh` script. This process takes a few minutes.\n3. Exit the SSH session.\n4. Return to a three-node configuration if any nodes are offline. Repeat the following steps for each node that is offline to delete and re-create them. After the machines are re-created, a new revision is forced and etcd automatically scales up.\n\n   - If you use a user-provisioned bare-metal installation, you can re-create a control plane machine by using the same method that you used to originally create it. For more information, see \"Installing a user-provisioned cluster on bare metal\".\n\n     > [!WARNING]\n     > Do not delete and re-create the machine for the recovery host.\n   - If you are running installer-provisioned infrastructure, or you used the Machine API to create your machines, follow these steps:\n\n     > [!WARNING]\n     > Do not delete and re-create the machine for the recovery host.\n     >\n     > For bare-metal installations on installer-provisioned infrastructure, control plane machines are not re-created. For more information, see \"Replacing a bare-metal control plane node\".\n\n     1. In a terminal that has access to the cluster as a `cluster-admin` user, obtain the machine for one of the offline nodes by running the following command:\n\n        ```terminal\n        $ oc get machines -n openshift-machine-api -o wide\n        ```\n\n        ```terminal {title=\"Example output\"}\n        NAME                                        PHASE     TYPE        REGION      ZONE         AGE     NODE                           PROVIDERID                              STATE\n        clustername-8qw5l-master-0                  Running   m4.xlarge   us-east-1   us-east-1a   3h37m   ip-10-0-131-183.ec2.internal   aws:///us-east-1a/i-0ec2782f8287dfb7e   stopped\n        clustername-8qw5l-master-1                  Running   m4.xlarge   us-east-1   us-east-1b   3h37m   ip-10-0-143-125.ec2.internal   aws:///us-east-1b/i-096c349b700a19631   running\n        clustername-8qw5l-master-2                  Running   m4.xlarge   us-east-1   us-east-1c   3h37m   ip-10-0-154-194.ec2.internal    aws:///us-east-1c/i-02626f1dba9ed5bba  running\n        clustername-8qw5l-worker-us-east-1a-wbtgd   Running   m4.large    us-east-1   us-east-1a   3h28m   ip-10-0-129-226.ec2.internal   aws:///us-east-1a/i-010ef6279b4662ced   running\n        clustername-8qw5l-worker-us-east-1b-lrdxb   Running   m4.large    us-east-1   us-east-1b   3h28m   ip-10-0-144-248.ec2.internal   aws:///us-east-1b/i-0cb45ac45a166173b   running\n        clustername-8qw5l-worker-us-east-1c-pkg26   Running   m4.large    us-east-1   us-east-1c   3h28m   ip-10-0-170-181.ec2.internal   aws:///us-east-1c/i-06861c00007751b0a   running\n        ```\n\n        In the example output, `clustername-8qw5l-master-0` is the control plane machine for the offline node, `ip-10-0-131-183.ec2.internal`.\n     2. Delete the machine of the offline node by running the following command:\n\n        ```terminal\n        $ oc delete machine -n openshift-machine-api clustername-8qw5l-master-0\n        ```\n\n        Specify the name of the control plane machine for the offline node.\n\n        A new machine is automatically provisioned after deleting the machine of the offline node.\n5. Verify that a new machine has been created by running the following command:\n\n   ```terminal\n   $ oc get machines -n openshift-machine-api -o wide\n   ```\n\n   ```terminal {title=\"Example output\"}\n   NAME                                        PHASE          TYPE        REGION      ZONE         AGE     NODE                           PROVIDERID                              STATE\n   clustername-8qw5l-master-1                  Running        m4.xlarge   us-east-1   us-east-1b   3h37m   ip-10-0-143-125.ec2.internal   aws:///us-east-1b/i-096c349b700a19631   running\n   clustername-8qw5l-master-2                  Running        m4.xlarge   us-east-1   us-east-1c   3h37m   ip-10-0-154-194.ec2.internal    aws:///us-east-1c/i-02626f1dba9ed5bba  running\n   clustername-8qw5l-master-3                  Provisioning   m4.xlarge   us-east-1   us-east-1a   85s     ip-10-0-173-171.ec2.internal    aws:///us-east-1a/i-015b0888fe17bc2c8  running\n   clustername-8qw5l-worker-us-east-1a-wbtgd   Running        m4.large    us-east-1   us-east-1a   3h28m   ip-10-0-129-226.ec2.internal   aws:///us-east-1a/i-010ef6279b4662ced   running\n   clustername-8qw5l-worker-us-east-1b-lrdxb   Running        m4.large    us-east-1   us-east-1b   3h28m   ip-10-0-144-248.ec2.internal   aws:///us-east-1b/i-0cb45ac45a166173b   running\n   clustername-8qw5l-worker-us-east-1c-pkg26   Running        m4.large    us-east-1   us-east-1c   3h28m   ip-10-0-170-181.ec2.internal   aws:///us-east-1c/i-06861c00007751b0a   running\n   ```\n\n   In the example output, `clustername-8qw5l-master-3` is being created and is ready after the phase changes from `Provisioning` to `Running`.\n\n   It might take a few minutes for the new machine to be created. The etcd cluster Operator automatically synchronizes when the machine or node returns to a healthy state.\n6. For each node that is offline, repeat the previous steps to delete and re-create the node.\n7. Wait until the control plane recovers by running the following command:\n\n   ```terminal\n   $ oc adm wait-for-stable-cluster\n   ```\n\n   > [!NOTE]\n   > It can take up to 15 minutes for the control plane to recover.\n\n**Troubleshooting**\n\n- If you see no progress rolling out the etcd static pods, you can force redeployment from the etcd cluster Operator by running the following command:\n\n  ```terminal\n  $ oc patch etcd cluster -p='{\"spec\": {\"forceRedeploymentReason\": \"recovery-'\"$(date --rfc-3339=ns )\"'\"}}' --type=merge\n  ```\n\n## Additional resources {#additional-resources_dr-quorum-restoration}\n\n- [Installing a user-provisioned cluster on bare metal](/openshift-docs-markdown/installing/installing_bare_metal/upi/installing-bare-metal#installing-bare-metal)\n- [Replacing a bare-metal control plane node](/openshift-docs-markdown/installing/installing_bare_metal/bare-metal-expanding-the-cluster#replacing-a-bare-metal-control-plane-node_bare-metal-expanding)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/backup_and_restore/control_plane_backup_and_restore/disaster_recovery/quorum-restoration.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="dr-quorum-restoration" class="group scroll-mt-20">Quorum restoration<a href="#dr-quorum-restoration" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can restore etcd quorum on your OpenShift Container Platform cluster by running the <code>quorum-restore.sh</code> script on a recovery host when quorum loss leaves the API read-only. After quorum is restored, the API returns to read/write mode.</p>
      <h2 id="dr-restoring-etcd-quorum-ha_dr-quorum-restoration" class="group scroll-mt-20">Restoring etcd quorum for high availability clusters<a href="#dr-restoring-etcd-quorum-ha_dr-quorum-restoration" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can restore etcd quorum on high availability (HA) clusters by running the <code>quorum-restore.sh</code> script on a recovery host. Restored quorum returns the OpenShift Container Platform API to read/write mode when quorum loss takes the cluster offline.</p>
      <p>The <code>quorum-restore.sh</code> script creates a new single-member etcd cluster from the local data directory on the recovery host. No prior backup is required.</p>
      <p>For high availability (HA) clusters, a three-node HA cluster requires you to shut down etcd on two hosts to avoid a cluster split. On four-node and five-node HA clusters, you must shut down three hosts. Quorum requires a majority of nodes. The minimum number of nodes required for quorum on a three-node HA cluster is two. On four-node and five-node HA clusters, the minimum number of nodes required for quorum is three. If you start a new cluster from backup on your recovery host, the other etcd members might still be able to form quorum and continue service.</p>
      <Alert variant="warning" class="my-4">
        <AlertTitle>Warning</AlertTitle>
        <AlertDescription>
          <p>You might experience data loss if the host that runs the restoration does not have all data replicated to it.</p>
        </AlertDescription>
      </Alert>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Quorum restoration should not be used to decrease the number of nodes outside of the restoration process. Decreasing the number of nodes results in an unsupported cluster configuration.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have SSH access to the node used to restore quorum.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Select a control plane host to use as the recovery host. You run the restore operation on this host.
      <ol>
      <li>
      List the running etcd pods by running the following command:
      <CodeBlock code={"$ oc get pods -n openshift-etcd -l app=etcd --field-selector=\"status.phase==Running\""} lang="terminal" />
      </li>
      <li>
      Choose a pod and run the following command to obtain its IP address:
      <CodeBlock code={"$ oc exec -n openshift-etcd <etcd-pod> -c etcdctl -- etcdctl endpoint status -w table"} lang="terminal" />
      <p>Note the IP address of a member that is not a learner and has the highest Raft index.</p>
      </li>
      <li>
      List nodes by running the following command:
      <CodeBlock code={"$ oc get nodes -o jsonpath='{range .items[*]}[{.metadata.name},{.status.addresses[?(@.type==\"InternalIP\")].address}]{end}'"} lang="terminal" />
      <p>Note the node name that corresponds to the IP address of the chosen etcd member.</p>
      </li>
      </ol>
      </li>
      <li>
      Using SSH, connect to the chosen recovery node and run the following command to restore etcd quorum:
      <CodeBlock code={"$ sudo -E /usr/local/bin/quorum-restore.sh"} lang="terminal" />
      <p>After a few minutes, the nodes that went down are automatically synchronized with the node that the recovery script was run on. Any remaining online nodes automatically rejoin the new etcd cluster created by the <code>quorum-restore.sh</code> script. This process takes a few minutes.</p>
      </li>
      <li>Exit the SSH session.</li>
      <li>
      Return to a three-node configuration if any nodes are offline. Repeat the following steps for each node that is offline to delete and re-create them. After the machines are re-created, a new revision is forced and etcd automatically scales up.
      <ul>
      <li>
      If you use a user-provisioned bare-metal installation, you can re-create a control plane machine by using the same method that you used to originally create it. For more information, see "Installing a user-provisioned cluster on bare metal".
      <Alert variant="warning" class="my-4">
        <AlertTitle>Warning</AlertTitle>
        <AlertDescription>
          <p>Do not delete and re-create the machine for the recovery host.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      If you are running installer-provisioned infrastructure, or you used the Machine API to create your machines, follow these steps:
      <Alert variant="warning" class="my-4">
        <AlertTitle>Warning</AlertTitle>
        <AlertDescription>
          <p>Do not delete and re-create the machine for the recovery host.</p>
          <p>For bare-metal installations on installer-provisioned infrastructure, control plane machines are not re-created. For more information, see "Replacing a bare-metal control plane node".</p>
        </AlertDescription>
      </Alert>
      <ol>
      <li>
      In a terminal that has access to the cluster as a <code>cluster-admin</code> user, obtain the machine for one of the offline nodes by running the following command:
      <CodeBlock code={"$ oc get machines -n openshift-machine-api -o wide"} lang="terminal" />
      <CodeBlock code={"NAME                                        PHASE     TYPE        REGION      ZONE         AGE     NODE                           PROVIDERID                              STATE\nclustername-8qw5l-master-0                  Running   m4.xlarge   us-east-1   us-east-1a   3h37m   ip-10-0-131-183.ec2.internal   aws:///us-east-1a/i-0ec2782f8287dfb7e   stopped\nclustername-8qw5l-master-1                  Running   m4.xlarge   us-east-1   us-east-1b   3h37m   ip-10-0-143-125.ec2.internal   aws:///us-east-1b/i-096c349b700a19631   running\nclustername-8qw5l-master-2                  Running   m4.xlarge   us-east-1   us-east-1c   3h37m   ip-10-0-154-194.ec2.internal    aws:///us-east-1c/i-02626f1dba9ed5bba  running\nclustername-8qw5l-worker-us-east-1a-wbtgd   Running   m4.large    us-east-1   us-east-1a   3h28m   ip-10-0-129-226.ec2.internal   aws:///us-east-1a/i-010ef6279b4662ced   running\nclustername-8qw5l-worker-us-east-1b-lrdxb   Running   m4.large    us-east-1   us-east-1b   3h28m   ip-10-0-144-248.ec2.internal   aws:///us-east-1b/i-0cb45ac45a166173b   running\nclustername-8qw5l-worker-us-east-1c-pkg26   Running   m4.large    us-east-1   us-east-1c   3h28m   ip-10-0-170-181.ec2.internal   aws:///us-east-1c/i-06861c00007751b0a   running"} lang="terminal" title="Example output" />
      <p>In the example output, <code>clustername-8qw5l-master-0</code> is the control plane machine for the offline node, <code>ip-10-0-131-183.ec2.internal</code>.</p>
      </li>
      <li>
      Delete the machine of the offline node by running the following command:
      <CodeBlock code={"$ oc delete machine -n openshift-machine-api clustername-8qw5l-master-0"} lang="terminal" />
      <p>Specify the name of the control plane machine for the offline node.</p>
      <p>A new machine is automatically provisioned after deleting the machine of the offline node.</p>
      </li>
      </ol>
      </li>
      </ul>
      </li>
      <li>
      Verify that a new machine has been created by running the following command:
      <CodeBlock code={"$ oc get machines -n openshift-machine-api -o wide"} lang="terminal" />
      <CodeBlock code={"NAME                                        PHASE          TYPE        REGION      ZONE         AGE     NODE                           PROVIDERID                              STATE\nclustername-8qw5l-master-1                  Running        m4.xlarge   us-east-1   us-east-1b   3h37m   ip-10-0-143-125.ec2.internal   aws:///us-east-1b/i-096c349b700a19631   running\nclustername-8qw5l-master-2                  Running        m4.xlarge   us-east-1   us-east-1c   3h37m   ip-10-0-154-194.ec2.internal    aws:///us-east-1c/i-02626f1dba9ed5bba  running\nclustername-8qw5l-master-3                  Provisioning   m4.xlarge   us-east-1   us-east-1a   85s     ip-10-0-173-171.ec2.internal    aws:///us-east-1a/i-015b0888fe17bc2c8  running\nclustername-8qw5l-worker-us-east-1a-wbtgd   Running        m4.large    us-east-1   us-east-1a   3h28m   ip-10-0-129-226.ec2.internal   aws:///us-east-1a/i-010ef6279b4662ced   running\nclustername-8qw5l-worker-us-east-1b-lrdxb   Running        m4.large    us-east-1   us-east-1b   3h28m   ip-10-0-144-248.ec2.internal   aws:///us-east-1b/i-0cb45ac45a166173b   running\nclustername-8qw5l-worker-us-east-1c-pkg26   Running        m4.large    us-east-1   us-east-1c   3h28m   ip-10-0-170-181.ec2.internal   aws:///us-east-1c/i-06861c00007751b0a   running"} lang="terminal" title="Example output" />
      <p>In the example output, <code>clustername-8qw5l-master-3</code> is being created and is ready after the phase changes from <code>Provisioning</code> to <code>Running</code>.</p>
      <p>It might take a few minutes for the new machine to be created. The etcd cluster Operator automatically synchronizes when the machine or node returns to a healthy state.</p>
      </li>
      <li>For each node that is offline, repeat the previous steps to delete and re-create the node.</li>
      <li>
      Wait until the control plane recovers by running the following command:
      <CodeBlock code={"$ oc adm wait-for-stable-cluster"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>It can take up to 15 minutes for the control plane to recover.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <p><strong>Troubleshooting</strong></p>
      <ul>
      <li>
      If you see no progress rolling out the etcd static pods, you can force redeployment from the etcd cluster Operator by running the following command:
      <CodeBlock code={"$ oc patch etcd cluster -p='{\"spec\": {\"forceRedeploymentReason\": \"recovery-'\"$(date --rfc-3339=ns )\"'\"}}' --type=merge"} lang="terminal" />
      </li>
      </ul>
      <h2 id="additional-resources_dr-quorum-restoration" class="group scroll-mt-20">Additional resources<a href="#additional-resources_dr-quorum-restoration" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/installing/installing_bare_metal/upi/installing-bare-metal#installing-bare-metal">Installing a user-provisioned cluster on bare metal</a></li>
      <li><a href="/openshift-docs-markdown/installing/installing_bare_metal/bare-metal-expanding-the-cluster#replacing-a-bare-metal-control-plane-node_bare-metal-expanding">Replacing a bare-metal control plane node</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
