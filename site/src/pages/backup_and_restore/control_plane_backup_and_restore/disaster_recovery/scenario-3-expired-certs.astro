---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: backup_and_restore/control_plane_backup_and_restore/disaster_recovery/scenario-3-expired-certs.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"dr-recovering-expired-certs","text":"Recovering from expired control plane certificates"},{"depth":2,"slug":"dr-scenario-3-recovering-expired-certs_dr-recovering-expired-certs","text":"Recovering from expired control plane certificates"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/backup_and_restore/control_plane_backup_and_restore/disaster_recovery/scenario-3-expired-certs";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Recovering from expired control plane certificates";
const description = "You can restore kubelet certificates on your OpenShift Container Platform cluster by approving pending certificate signing requests (CSRs) after control…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["backup_and_restore","backup_and_restore/control_plane_backup_and_restore","backup_and_restore/control_plane_backup_and_restore/disaster_recovery"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Recovering from expired control plane certificates {#dr-recovering-expired-certs}\n\nYou can restore kubelet certificates on your OpenShift Container Platform cluster by approving pending certificate signing requests (CSRs) after control plane certificates expire. Approved CSRs return nodes to a healthy state.\n\n## Recovering from expired control plane certificates {#dr-scenario-3-recovering-expired-certs_dr-recovering-expired-certs}\n\nYou can restore kubelet certificates by manually approving pending `node-bootstrapper` certificate signing requests (CSRs) and, on user-provisioned installations, kubelet serving CSRs. Approved CSRs return nodes to a healthy state after control plane certificates expire.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- You have access to the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Get the list of current CSRs by running the following command:\n\n   ```terminal\n   $ oc get csr\n   ```\n\n   ```terminal {title=\"Example output\"}\n   NAME        AGE    SIGNERNAME                                    REQUESTOR                                                                   CONDITION\n   csr-2s94x   8m3s   kubernetes.io/kubelet-serving                 system:node:<node_name>                                                     Pending\n   csr-4bd6t   8m3s   kubernetes.io/kubelet-serving                 system:node:<node_name>                                                     Pending\n   csr-4hl85   13m    kubernetes.io/kube-apiserver-client-kubelet   system:serviceaccount:openshift-machine-config-operator:node-bootstrapper   Pending\n   csr-zhhhp   3m8s   kubernetes.io/kube-apiserver-client-kubelet   system:serviceaccount:openshift-machine-config-operator:node-bootstrapper   Pending\n   ...\n   ```\n\n   In the example output, CSRs with a `SIGNERNAME` of `kubernetes.io/kubelet-serving` are kubelet serving CSRs. You see this CSR type on user-provisioned installations. CSRs with a `SIGNERNAME` of `kubernetes.io/kube-apiserver-client-kubelet` and a `node-bootstrapper` requestor are `node-bootstrapper` CSRs that you must approve to restore kubelet certificates.\n2. Review the details of a CSR to verify that it is valid by running the following command:\n\n   ```terminal\n   $ oc describe csr <csr_name>\n   ```\n\n   `<csr_name>` is the name of a CSR from the list of current CSRs.\n3. Approve each valid `node-bootstrapper` CSR by running the following command:\n\n   ```terminal\n   $ oc adm certificate approve <csr_name>\n   ```\n4. For user-provisioned installations, approve each valid kubelet serving CSR by running the following command:\n\n   ```terminal\n   $ oc adm certificate approve <csr_name>\n   ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/backup_and_restore/control_plane_backup_and_restore/disaster_recovery/scenario-3-expired-certs.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="dr-recovering-expired-certs" class="group scroll-mt-20">Recovering from expired control plane certificates<a href="#dr-recovering-expired-certs" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">You can restore kubelet certificates on your OpenShift Container Platform cluster by approving pending certificate signing requests (CSRs) after control plane certificates expire. Approved CSRs return nodes to a healthy state.</p>
      <h2 id="dr-scenario-3-recovering-expired-certs_dr-recovering-expired-certs" class="group scroll-mt-20">Recovering from expired control plane certificates<a href="#dr-scenario-3-recovering-expired-certs_dr-recovering-expired-certs" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can restore kubelet certificates by manually approving pending <code>node-bootstrapper</code> certificate signing requests (CSRs) and, on user-provisioned installations, kubelet serving CSRs. Approved CSRs return nodes to a healthy state after control plane certificates expire.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>You have access to the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Get the list of current CSRs by running the following command:
      <CodeBlock code={"$ oc get csr"} lang="terminal" />
      <CodeBlock code={"NAME        AGE    SIGNERNAME                                    REQUESTOR                                                                   CONDITION\ncsr-2s94x   8m3s   kubernetes.io/kubelet-serving                 system:node:<node_name>                                                     Pending\ncsr-4bd6t   8m3s   kubernetes.io/kubelet-serving                 system:node:<node_name>                                                     Pending\ncsr-4hl85   13m    kubernetes.io/kube-apiserver-client-kubelet   system:serviceaccount:openshift-machine-config-operator:node-bootstrapper   Pending\ncsr-zhhhp   3m8s   kubernetes.io/kube-apiserver-client-kubelet   system:serviceaccount:openshift-machine-config-operator:node-bootstrapper   Pending\n..."} lang="terminal" title="Example output" />
      <p>In the example output, CSRs with a <code>SIGNERNAME</code> of <code>kubernetes.io/kubelet-serving</code> are kubelet serving CSRs. You see this CSR type on user-provisioned installations. CSRs with a <code>SIGNERNAME</code> of <code>kubernetes.io/kube-apiserver-client-kubelet</code> and a <code>node-bootstrapper</code> requestor are <code>node-bootstrapper</code> CSRs that you must approve to restore kubelet certificates.</p>
      </li>
      <li>
      Review the details of a CSR to verify that it is valid by running the following command:
      <CodeBlock code={"$ oc describe csr <csr_name>"} lang="terminal" />
      <p><code>&lt;csr_name&gt;</code> is the name of a CSR from the list of current CSRs.</p>
      </li>
      <li>
      Approve each valid <code>node-bootstrapper</code> CSR by running the following command:
      <CodeBlock code={"$ oc adm certificate approve <csr_name>"} lang="terminal" />
      </li>
      <li>
      For user-provisioned installations, approve each valid kubelet serving CSR by running the following command:
      <CodeBlock code={"$ oc adm certificate approve <csr_name>"} lang="terminal" />
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
