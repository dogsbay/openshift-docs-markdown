---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: backup_and_restore/graceful-cluster-shutdown.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"graceful-shutdown-cluster","text":"Shutting down the cluster gracefully"},{"depth":2,"slug":"graceful-shutdown_graceful-shutdown-cluster","text":"Shutting down the cluster"},{"depth":2,"slug":"additional-resources_restarting-restoring-cluster","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/backup_and_restore/graceful-cluster-shutdown";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Shutting down the cluster gracefully";
const description = "You can shut down your OpenShift Container Platform cluster for planned maintenance by cordoning nodes, draining workloads, and stopping nodes in order…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["backup_and_restore"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Shutting down the cluster gracefully {#graceful-shutdown-cluster}\n\nYou can shut down your OpenShift Container Platform cluster for planned maintenance by cordoning nodes, draining workloads, and stopping nodes in order. Graceful shutdown preserves cluster state so you can restart the cluster when maintenance is complete.\n\n## Shutting down the cluster {#graceful-shutdown_graceful-shutdown-cluster}\n\nYou can shut down a OpenShift Container Platform cluster gracefully by cordoning nodes, draining worker nodes, and stopping nodes in order. This preserves cluster data so you can restart the cluster after maintenance or a planned outage.\n\n> [!NOTE]\n> You can shut down a cluster until a year from the installation date and expect it to restart gracefully. After a year from the installation date, the cluster certificates expire. However, you might need to manually approve the pending certificate signing requests (CSRs) to recover kubelet certificates when the cluster restarts.\n\nTake an etcd backup before shutting down the cluster so that you can restore the cluster if you encounter issues when restarting it.\n\nYou might need to restore from the backup if any of the following conditions occur:\n\n- etcd data is corrupted during shutdown\n- A node fails because of hardware\n- Network connectivity is interrupted\n\nIf the cluster does not recover after restart, follow the steps to restore to a previous cluster state.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- You created an etcd backup before shutting down the cluster.\n\n  > [!IMPORTANT]\n  > Without a recent etcd backup, you might not be able to restore the cluster if shutdown or restart fails.\n- If you are running a single-node OpenShift cluster, you must evacuate all workload pods off of the cluster before you shut it down.\n\n**Procedure**\n\n1. If you are shutting the cluster down for an extended period, determine the date on which certificates expire by running the following command:\n\n   ```terminal\n   $ oc -n openshift-kube-apiserver-operator get secret kube-apiserver-to-kubelet-signer -o jsonpath='{.metadata.annotations.auth\\.openshift\\.io/certificate-not-after}'\n   ```\n\n   ```terminal {title=\"Example output\"}\n   2030-08-05T14:37:50Z\n   ```\n\n   Plan to restart the cluster on or before the displayed date so the cluster can restart gracefully. When the cluster restarts, you might need to manually approve pending certificate signing requests (CSRs) to recover kubelet certificates.\n2. Mark all the nodes in the cluster as unschedulable by running the following command:\n\n   ```terminal\n   $ for node in $(oc get nodes -o jsonpath='{.items[*].metadata.name}'); do echo ${node} ; oc adm cordon ${node} ; done\n   ```\n\n   ```terminal {title=\"Example output\"}\n   ci-ln-mgdnf4b-72292-n547t-master-0\n   node/ci-ln-mgdnf4b-72292-n547t-master-0 cordoned\n   ci-ln-mgdnf4b-72292-n547t-master-1\n   node/ci-ln-mgdnf4b-72292-n547t-master-1 cordoned\n   ci-ln-mgdnf4b-72292-n547t-master-2\n   node/ci-ln-mgdnf4b-72292-n547t-master-2 cordoned\n   ci-ln-mgdnf4b-72292-n547t-worker-a-s7ntl\n   node/ci-ln-mgdnf4b-72292-n547t-worker-a-s7ntl cordoned\n   ci-ln-mgdnf4b-72292-n547t-worker-b-cmc9k\n   node/ci-ln-mgdnf4b-72292-n547t-worker-b-cmc9k cordoned\n   ci-ln-mgdnf4b-72292-n547t-worker-c-vcmtn\n   node/ci-ln-mgdnf4b-72292-n547t-worker-c-vcmtn cordoned\n   ```\n3. Evacuate the pods by running the following command:\n\n   ```terminal\n   $ for node in $(oc get nodes -l node-role.kubernetes.io/worker -o jsonpath='{.items[*].metadata.name}'); do echo ${node} ; oc adm drain ${node} --delete-emptydir-data --ignore-daemonsets=true --timeout=15s --force ; done\n   ```\n\n   Draining worker nodes before shutdown allows pods to terminate gracefully, which reduces the chance of data corruption.\n4. Shut down all of the nodes in the cluster by running the following command:\n\n   ```terminal\n   $ for node in $(oc get nodes -o jsonpath='{.items[*].metadata.name}'); do oc debug node/${node} -- chroot /host shutdown -h 1; done\n   ```\n\n   > [!NOTE]\n   > Ensure that the control plane node with the API VIP assigned is the last node processed in the loop. Otherwise, the shutdown command fails.\n\n   The `-h 1` option indicates how long, in minutes, this process lasts before the control plane nodes are shut down. For large-scale clusters with 10 nodes or more, set to `-h 10` or longer to ensure all the compute nodes have time to shut down first.\n\n   ```terminal {title=\"Example output\"}\n   Starting pod/ip-10-0-130-169us-east-2computeinternal-debug ...\n   To use host binaries, run `chroot /host`\n   Shutdown scheduled for Mon 2021-09-13 09:36:17 UTC, use 'shutdown -c' to cancel.\n   Removing debug pod ...\n   Starting pod/ip-10-0-150-116us-east-2computeinternal-debug ...\n   To use host binaries, run `chroot /host`\n   Shutdown scheduled for Mon 2021-09-13 09:36:29 UTC, use 'shutdown -c' to cancel.\n   ```\n\n   > [!NOTE]\n   > It is not necessary to drain control plane nodes of the standard pods that ship with OpenShift Container Platform before shutdown. Cluster administrators are responsible for ensuring a clean restart of workloads they deploy after the cluster is restarted. If you drained control plane nodes before shutdown because of custom workloads, you must mark the control plane nodes as schedulable before the cluster is functional again after restart.\n5. Shut off any cluster dependencies that are no longer needed, such as external storage or a Lightweight Directory Access Protocol (LDAP) server. Be sure to consult documentation from the vendor before doing so.\n\n   > [!IMPORTANT]\n   > If you deployed your cluster on a cloud-provider platform, do not shut down, suspend, or delete the associated cloud resources. If you delete the cloud resources of a suspended virtual machine, OpenShift Container Platform might not restore successfully.\n\n## Additional resources {#additional-resources_restarting-restoring-cluster}\n\n- [Backing up etcd](/openshift-docs-markdown/backup_and_restore/control_plane_backup_and_restore/backing-up-etcd#backup-etcd)\n- [Restoring to an earlier cluster state](/openshift-docs-markdown/backup_and_restore/control_plane_backup_and_restore/disaster_recovery/scenario-2-restoring-cluster-state#dr-restoring-cluster-state)\n- [Restarting the cluster gracefully](/openshift-docs-markdown/backup_and_restore/graceful-cluster-restart#graceful-restart-cluster)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/backup_and_restore/graceful-cluster-shutdown.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="graceful-shutdown-cluster" class="group scroll-mt-20">Shutting down the cluster gracefully<a href="#graceful-shutdown-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can shut down your OpenShift Container Platform cluster for planned maintenance by cordoning nodes, draining workloads, and stopping nodes in order. Graceful shutdown preserves cluster state so you can restart the cluster when maintenance is complete.</p>
      <h2 id="graceful-shutdown_graceful-shutdown-cluster" class="group scroll-mt-20">Shutting down the cluster<a href="#graceful-shutdown_graceful-shutdown-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can shut down a OpenShift Container Platform cluster gracefully by cordoning nodes, draining worker nodes, and stopping nodes in order. This preserves cluster data so you can restart the cluster after maintenance or a planned outage.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>You can shut down a cluster until a year from the installation date and expect it to restart gracefully. After a year from the installation date, the cluster certificates expire. However, you might need to manually approve the pending certificate signing requests (CSRs) to recover kubelet certificates when the cluster restarts.</p>
        </AlertDescription>
      </Alert>
      <p>Take an etcd backup before shutting down the cluster so that you can restore the cluster if you encounter issues when restarting it.</p>
      <p>You might need to restore from the backup if any of the following conditions occur:</p>
      <ul>
      <li>etcd data is corrupted during shutdown</li>
      <li>A node fails because of hardware</li>
      <li>Network connectivity is interrupted</li>
      </ul>
      <p>If the cluster does not recover after restart, follow the steps to restore to a previous cluster state.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>
      You created an etcd backup before shutting down the cluster.
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Without a recent etcd backup, you might not be able to restore the cluster if shutdown or restart fails.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>If you are running a single-node OpenShift cluster, you must evacuate all workload pods off of the cluster before you shut it down.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      If you are shutting the cluster down for an extended period, determine the date on which certificates expire by running the following command:
      <CodeBlock code={"$ oc -n openshift-kube-apiserver-operator get secret kube-apiserver-to-kubelet-signer -o jsonpath='{.metadata.annotations.auth\\.openshift\\.io/certificate-not-after}'"} lang="terminal" />
      <CodeBlock code={"2030-08-05T14:37:50Z"} lang="terminal" title="Example output" />
      <p>Plan to restart the cluster on or before the displayed date so the cluster can restart gracefully. When the cluster restarts, you might need to manually approve pending certificate signing requests (CSRs) to recover kubelet certificates.</p>
      </li>
      <li>
      Mark all the nodes in the cluster as unschedulable by running the following command:
      <CodeBlock code={"$ for node in $(oc get nodes -o jsonpath='{.items[*].metadata.name}'); do echo ${node} ; oc adm cordon ${node} ; done"} lang="terminal" />
      <CodeBlock code={"ci-ln-mgdnf4b-72292-n547t-master-0\nnode/ci-ln-mgdnf4b-72292-n547t-master-0 cordoned\nci-ln-mgdnf4b-72292-n547t-master-1\nnode/ci-ln-mgdnf4b-72292-n547t-master-1 cordoned\nci-ln-mgdnf4b-72292-n547t-master-2\nnode/ci-ln-mgdnf4b-72292-n547t-master-2 cordoned\nci-ln-mgdnf4b-72292-n547t-worker-a-s7ntl\nnode/ci-ln-mgdnf4b-72292-n547t-worker-a-s7ntl cordoned\nci-ln-mgdnf4b-72292-n547t-worker-b-cmc9k\nnode/ci-ln-mgdnf4b-72292-n547t-worker-b-cmc9k cordoned\nci-ln-mgdnf4b-72292-n547t-worker-c-vcmtn\nnode/ci-ln-mgdnf4b-72292-n547t-worker-c-vcmtn cordoned"} lang="terminal" title="Example output" />
      </li>
      <li>
      Evacuate the pods by running the following command:
      <CodeBlock code={"$ for node in $(oc get nodes -l node-role.kubernetes.io/worker -o jsonpath='{.items[*].metadata.name}'); do echo ${node} ; oc adm drain ${node} --delete-emptydir-data --ignore-daemonsets=true --timeout=15s --force ; done"} lang="terminal" />
      <p>Draining worker nodes before shutdown allows pods to terminate gracefully, which reduces the chance of data corruption.</p>
      </li>
      <li>
      Shut down all of the nodes in the cluster by running the following command:
      <CodeBlock code={"$ for node in $(oc get nodes -o jsonpath='{.items[*].metadata.name}'); do oc debug node/${node} -- chroot /host shutdown -h 1; done"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Ensure that the control plane node with the API VIP assigned is the last node processed in the loop. Otherwise, the shutdown command fails.</p>
        </AlertDescription>
      </Alert>
      <p>The <code>-h 1</code> option indicates how long, in minutes, this process lasts before the control plane nodes are shut down. For large-scale clusters with 10 nodes or more, set to <code>-h 10</code> or longer to ensure all the compute nodes have time to shut down first.</p>
      <CodeBlock code={"Starting pod/ip-10-0-130-169us-east-2computeinternal-debug ...\nTo use host binaries, run `chroot /host`\nShutdown scheduled for Mon 2021-09-13 09:36:17 UTC, use 'shutdown -c' to cancel.\nRemoving debug pod ...\nStarting pod/ip-10-0-150-116us-east-2computeinternal-debug ...\nTo use host binaries, run `chroot /host`\nShutdown scheduled for Mon 2021-09-13 09:36:29 UTC, use 'shutdown -c' to cancel."} lang="terminal" title="Example output" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>It is not necessary to drain control plane nodes of the standard pods that ship with OpenShift Container Platform before shutdown. Cluster administrators are responsible for ensuring a clean restart of workloads they deploy after the cluster is restarted. If you drained control plane nodes before shutdown because of custom workloads, you must mark the control plane nodes as schedulable before the cluster is functional again after restart.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      Shut off any cluster dependencies that are no longer needed, such as external storage or a Lightweight Directory Access Protocol (LDAP) server. Be sure to consult documentation from the vendor before doing so.
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>If you deployed your cluster on a cloud-provider platform, do not shut down, suspend, or delete the associated cloud resources. If you delete the cloud resources of a suspended virtual machine, OpenShift Container Platform might not restore successfully.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <h2 id="additional-resources_restarting-restoring-cluster" class="group scroll-mt-20">Additional resources<a href="#additional-resources_restarting-restoring-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/backup_and_restore/control_plane_backup_and_restore/backing-up-etcd#backup-etcd">Backing up etcd</a></li>
      <li><a href="/openshift-docs-markdown/backup_and_restore/control_plane_backup_and_restore/disaster_recovery/scenario-2-restoring-cluster-state#dr-restoring-cluster-state">Restoring to an earlier cluster state</a></li>
      <li><a href="/openshift-docs-markdown/backup_and_restore/graceful-cluster-restart#graceful-restart-cluster">Restarting the cluster gracefully</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
