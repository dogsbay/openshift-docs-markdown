---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: backup_and_restore/application_backup_and_restore/troubleshooting/using-the-must-gather-tool.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import BlockImage from "@ui/image/Image.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"using-the-must-gather-tool","text":"Using the must-gather tool"},{"depth":2,"slug":"oadp-running-must-gather_using-the-must-gather-tool","text":"Using the must-gather tool"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/troubleshooting/using-the-must-gather-tool";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Using the must-gather tool";
const description = "Collect logs and information about OADP custom resources by using the must-gather tool. The must-gather data must be attached to all customer cases." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["backup_and_restore","backup_and_restore/application_backup_and_restore","backup_and_restore/application_backup_and_restore/troubleshooting"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Using the must-gather tool {#using-the-must-gather-tool}\n\nCollect logs and information about OADP custom resources by using the `must-gather` tool. The `must-gather` data must be attached to all customer cases.\n\nThe `must-gather` tool is a container and does not run all the time. The tool runs for a few minutes only after you start the tool by running the `must-gather` command.\n\n## Using the must-gather tool {#oadp-running-must-gather_using-the-must-gather-tool}\n\nRun the `must-gather` tool with the default configuration, timeout, and insecure TLS options. To use an option, add a flag corresponding to that option in the `must-gather` command.\n\nDefault configuration\n:   This configuration collects pod logs, OADP, and `Velero` custom resource (CR) information for all namespaces where the OADP Operator is installed.\n\nTimeout\n:   Data collection can take a long time if there are many failed `Backup` CRs. You can improve performance by setting a timeout value.\n\nInsecure TLS connections\n:   If a custom CA certificate is used, use the `must-gather` tool with insecure TLS connections.\n\nThe `must-gather` tool generates a Markdown output file with the collected information. The Markdown file is located in a cluster directory.\n\nFor more information about the supported flags, use the help flag with the `must-gather` tool as shown in the following example:\n\n```terminal\n$ oc adm must-gather --image={{ must_gather_v1_5 }} -- /usr/bin/gather -h\n```\n\n**Prerequisites**\n\n- You have logged in to the OpenShift Container Platform cluster as a user with the `cluster-admin` role.\n- You have installed the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Navigate to the directory where you want to store the `must-gather` data.\n2. Run the `oc adm must-gather` command for one of the following data collection options:\n\n   - To use the default configuration of the `must-gather` tool, run the following command:\n\n     ```terminal\n     $ oc adm must-gather --image={{ must_gather_v1_5 }}\n     ```\n   - To use the timeout flag with the `must-gather` tool, run the following command:\n\n     ```terminal\n     $ oc adm must-gather --image={{ must_gather_v1_5 }} -- /usr/bin/gather --request-timeout 1m\n     ```\n\n     In this example, the timeout is 1 minute.\n   - To use the insecure TLS connection flag with the `must-gather` tool, run the following command:\n\n     ```terminal\n     $ oc adm must-gather --image={{ must_gather_v1_5 }} -- /usr/bin/gather --skip-tls\n     ```\n   - To use a combination of the insecure TLS connection and the timeout flags with the `must-gather` tool, run the following command:\n\n     ```terminal\n     $ oc adm must-gather --image={{ must_gather_v1_5 }} -- /usr/bin/gather --request-timeout 15s --skip-tls\n     ```\n\n     In this example, the timeout is 15 seconds. By default, the `--skip-tls` flag value is `false`. Set the value to `true` to allow insecure TLS connections.\n\n**Verification**\n\n1. Verify that the Markdown output file is generated at the following location: `must-gather.local.89...054550/registry.redhat.io/oadp/oadp-mustgather-rhel9:v1.5-sha256-0...84/clusters/a4...86/oadp-must-gather-summary.md`\n2. Review the `must-gather` data in the Markdown file by opening the file in a Markdown previewer. For an example output, refer to the following image. You can upload this output file to a support case on the [Red Hat Customer Portal](https://access.redhat.com/). **Figure 1. Example markdown output of must-gather tool**\n\n   ![must-gather markdown output](/openshift-docs-markdown/_assets/images/oadp-must-gather-markdown-output.png)\n\n**Additional resources**\n\n- [Gathering cluster data](/openshift-docs-markdown/support/gathering-cluster-data#gathering-cluster-data)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/troubleshooting/using-the-must-gather-tool.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="using-the-must-gather-tool" class="group scroll-mt-20">Using the must-gather tool<a href="#using-the-must-gather-tool" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Collect logs and information about OADP custom resources by using the <code>must-gather</code> tool. The <code>must-gather</code> data must be attached to all customer cases.</p>
      <p>The <code>must-gather</code> tool is a container and does not run all the time. The tool runs for a few minutes only after you start the tool by running the <code>must-gather</code> command.</p>
      <h2 id="oadp-running-must-gather_using-the-must-gather-tool" class="group scroll-mt-20">Using the must-gather tool<a href="#oadp-running-must-gather_using-the-must-gather-tool" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Run the <code>must-gather</code> tool with the default configuration, timeout, and insecure TLS options. To use an option, add a flag corresponding to that option in the <code>must-gather</code> command.</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Default configuration</dt>
      <dd class="ml-6 text-muted-foreground"><p>This configuration collects pod logs, OADP, and <code>Velero</code> custom resource (CR) information for all namespaces where the OADP Operator is installed.</p></dd>
      <dt class="font-semibold">Timeout</dt>
      <dd class="ml-6 text-muted-foreground"><p>Data collection can take a long time if there are many failed <code>Backup</code> CRs. You can improve performance by setting a timeout value.</p></dd>
      <dt class="font-semibold">Insecure TLS connections</dt>
      <dd class="ml-6 text-muted-foreground"><p>If a custom CA certificate is used, use the <code>must-gather</code> tool with insecure TLS connections.</p></dd>
      </dl>
      <p>The <code>must-gather</code> tool generates a Markdown output file with the collected information. The Markdown file is located in a cluster directory.</p>
      <p>For more information about the supported flags, use the help flag with the <code>must-gather</code> tool as shown in the following example:</p>
      <CodeBlock code={"$ oc adm must-gather --image={{ must_gather_v1_5 }} -- /usr/bin/gather -h"} lang="terminal" />
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have logged in to the OpenShift Container Platform cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>You have installed the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to the directory where you want to store the <code>must-gather</code> data.</li>
      <li>
      Run the <code>oc adm must-gather</code> command for one of the following data collection options:
      <ul>
      <li>
      To use the default configuration of the <code>must-gather</code> tool, run the following command:
      <CodeBlock code={"$ oc adm must-gather --image={{ must_gather_v1_5 }}"} lang="terminal" />
      </li>
      <li>
      To use the timeout flag with the <code>must-gather</code> tool, run the following command:
      <CodeBlock code={"$ oc adm must-gather --image={{ must_gather_v1_5 }} -- /usr/bin/gather --request-timeout 1m"} lang="terminal" />
      <p>In this example, the timeout is 1 minute.</p>
      </li>
      <li>
      To use the insecure TLS connection flag with the <code>must-gather</code> tool, run the following command:
      <CodeBlock code={"$ oc adm must-gather --image={{ must_gather_v1_5 }} -- /usr/bin/gather --skip-tls"} lang="terminal" />
      </li>
      <li>
      To use a combination of the insecure TLS connection and the timeout flags with the <code>must-gather</code> tool, run the following command:
      <CodeBlock code={"$ oc adm must-gather --image={{ must_gather_v1_5 }} -- /usr/bin/gather --request-timeout 15s --skip-tls"} lang="terminal" />
      <p>In this example, the timeout is 15 seconds. By default, the <code>--skip-tls</code> flag value is <code>false</code>. Set the value to <code>true</code> to allow insecure TLS connections.</p>
      </li>
      </ul>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>Verify that the Markdown output file is generated at the following location: <code>must-gather.local.89...054550/registry.redhat.io/oadp/oadp-mustgather-rhel9:v1.5-sha256-0...84/clusters/a4...86/oadp-must-gather-summary.md</code></li>
      <li>
      Review the <code>must-gather</code> data in the Markdown file by opening the file in a Markdown previewer. For an example output, refer to the following image. You can upload this output file to a support case on the <a href="https://access.redhat.com/">Red Hat Customer Portal</a>. <strong>Figure 1. Example markdown output of must-gather tool</strong>
      <BlockImage src="/openshift-docs-markdown/_assets/images/oadp-must-gather-markdown-output.png" alt="must-gather markdown output" />
      </li>
      </ol>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/support/gathering-cluster-data#gathering-cluster-data">Gathering cluster data</a></li></ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
