---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: backup_and_restore/application_backup_and_restore/troubleshooting/restoring-workarounds-for-velero-backups-that-use-admission-webhooks.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"restoring-workarounds-for-velero-backups-that-use-admission-webhooks","text":"Restoring workarounds for Velero backups that use admission webhooks"},{"depth":2,"slug":"migration-debugging-velero-admission-webhooks-knative_restoring-workarounds-for-velero-backups-that-use-admission-webhooks","text":"Restoring Knative resources"},{"depth":2,"slug":"migration-debugging-velero-admission-webhooks-ibm-appconnect_restoring-workarounds-for-velero-backups-that-use-admission-webhooks","text":"Restoring IBM AppConnect resources"},{"depth":2,"slug":"avoiding-the-velero-plugin-panic-error_restoring-workarounds-for-velero-backups-that-use-admission-webhooks","text":"Avoiding the Velero plugin panic error"},{"depth":2,"slug":"workaround-for-openshift-adp-controller-segmentation-fault_restoring-workarounds-for-velero-backups-that-use-admission-webhooks","text":"Workaround for OpenShift ADP Controller segmentation fault"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/troubleshooting/restoring-workarounds-for-velero-backups-that-use-admission-webhooks";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Restoring workarounds for Velero backups that use admission webhooks";
const description = "Resolve restore failures caused by admission webhooks by applying workarounds for workloads such as Knative and IBM AppConnect resources. This helps you…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["backup_and_restore","backup_and_restore/application_backup_and_restore","backup_and_restore/application_backup_and_restore/troubleshooting"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Restoring workarounds for Velero backups that use admission webhooks {#restoring-workarounds-for-velero-backups-that-use-admission-webhooks}\n\nResolve restore failures caused by admission webhooks by applying workarounds for workloads such as Knative and IBM AppConnect resources. This helps you to successfully restore workloads that have mutating or validating admission webhooks.\n\nVelero has limited abilities to resolve admission webhook issues during a restore. If you have workloads with admission webhooks, you might need to use an additional Velero plugin or make changes to how you restore the workload. Typically, workloads with admission webhooks require you to create a resource of a specific kind first. This is especially true if your workload has child resources because admission webhooks typically block child resources.\n\nFor example, creating or restoring a top-level object such as `service.serving.knative.dev` typically creates child resources automatically. If you do this first, you will not need to use Velero to create and restore these resources. This avoids the problem of child resources being blocked by an admission webhook that Velero might use.\n\n> [!NOTE]\n> Velero plugins are started as separate processes. After a Velero operation has completed, either successfully or not, it exits. Receiving a `received EOF, stopping recv loop` message in the debug logs indicates that a plugin operation has completed. It does not mean that an error has occurred.\n\n## Restoring Knative resources {#migration-debugging-velero-admission-webhooks-knative_restoring-workarounds-for-velero-backups-that-use-admission-webhooks}\n\nResolve issues with restoring Knative resources that use admission webhooks by restoring the top-level `service.serving.knative.dev` service resource with Velero. This helps you to ensure that Knative resources are restored successfully without admission webhook errors.\n\n**Procedure**\n\n- Restore the top level `service.serving.knative.dev Service` resource by using the following command:\n\n  ```terminal\n  $ velero restore <restore_name> \\\n    --from-backup=<backup_name> --include-resources \\\n    service.serving.knative.dev\n  ```\n\n## Restoring IBM AppConnect resources {#migration-debugging-velero-admission-webhooks-ibm-appconnect_restoring-workarounds-for-velero-backups-that-use-admission-webhooks}\n\nTroubleshoot Velero restore failures for IBM(R) AppConnect resources that use admission webhooks. Verify your webhook rules and check that the installed Operator supports the backup’s version to successfully complete the restore.\n\n**Procedure**\n\n1. Check if you have any mutating admission plugins of `kind: MutatingWebhookConfiguration` in the cluster by entering/running the following command:\n\n   ```terminal\n   $ oc get mutatingwebhookconfigurations\n   ```\n2. Examine the YAML file of each `kind: MutatingWebhookConfiguration` to ensure that none of its rules block creation of the objects that are experiencing issues. For more information, see [the official Kubernetes documentation](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.23/#rulewithoperations-v1-admissionregistration-k8s-io).\n3. Check that any `spec.version` in `type: Configuration.appconnect.ibm.com/v1beta1` used at backup time is supported by the installed Operator.\n\n## Avoiding the Velero plugin panic error {#avoiding-the-velero-plugin-panic-error_restoring-workarounds-for-velero-backups-that-use-admission-webhooks}\n\nLabel a custom Backup Storage Location (BSL) to resolve Velero plugin panic errors during `imagestream` backups. This helps you to ensure the OADP controller creates the required registry secret when you manage the BSL outside the `DataProtectionApplication` (DPA) CR.\n\nA missing secret can cause a panic error for the Velero plugin during image stream backups. When the backup and the BSL are managed outside the scope of the DPA, the OADP controller does not create the relevant `oadp-<bsl_name>-<bsl_provider>-registry-secret` parameter.\n\nDuring the backup operation, the OpenShift Velero plugin panics on the `imagestream` backup, with the following panic error:\n\n```text\n024-02-27T10:46:50.028951744Z time=\"2024-02-27T10:46:50Z\" level=error msg=\"Error backing up item\"\nbackup=openshift-adp/<backup name> error=\"error executing custom action (groupResource=imagestreams.image.openshift.io,\nnamespace=<BSL Name>, name=postgres): rpc error: code = Aborted desc = plugin panicked:\nruntime error: index out of range with length 1, stack trace: goroutine 94…\n```\n\n**Procedure**\n\n1. Label the custom BSL with the relevant label by using the following command:\n\n   ```terminal\n   $ oc label backupstoragelocations.velero.io <bsl_name> app.kubernetes.io/component=bsl\n   ```\n2. After the BSL is labeled, wait until the DPA reconciles.\n\n   > [!NOTE]\n   > You can force the reconciliation by making any minor change to the DPA itself.\n\n**Verification**\n\n- After the DPA is reconciled, confirm that the parameter has been created and that the correct registry data has been populated into it by entering the following command:\n\n  ```terminal\n  $ oc -n openshift-adp get secret/oadp-<bsl_name>-<bsl_provider>-registry-secret -o json | jq -r '.data'\n  ```\n\n## Workaround for OpenShift ADP Controller segmentation fault {#workaround-for-openshift-adp-controller-segmentation-fault_restoring-workarounds-for-velero-backups-that-use-admission-webhooks}\n\nDefine either `velero` or `cloudstorage` in your Data Protection Application (DPA) configuration to prevent indefinite pod crashes. This configuration resolves a segmentation fault in the `openshift-adp-controller-manager` pod that occurs when both components are enabled.\n\nThe `openshift-adp-controller-manager` pod fails with a crash loop segmentation fault due to the following settings:\n\n- If you define both `velero` and `cloudstorage`, the `openshift-adp-controller-manager` fails.\n- If you do not define both `velero` and `cloudstorage`, the `openshift-adp-controller-manager` fails.\n\nSee *OADP-1054* for more information.\n\n**Additional resources**\n{._additional-resources}\n\n- [OADP-1054](https://issues.redhat.com/browse/OADP-1054)\n\n**Additional resources**\n{._additional-resources}\n\n- [Admission plugins](/openshift-docs-markdown/architecture/admission-plug-ins#admission-plug-ins)\n- [Webhook admission plugins](/openshift-docs-markdown/architecture/admission-plug-ins#admission-webhooks-about_admission-plug-ins)\n- [Types of webhook admission plugins](/openshift-docs-markdown/architecture/admission-plug-ins#admission-webhook-types_admission-plug-ins)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/troubleshooting/restoring-workarounds-for-velero-backups-that-use-admission-webhooks.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="restoring-workarounds-for-velero-backups-that-use-admission-webhooks" class="group scroll-mt-20">Restoring workarounds for Velero backups that use admission webhooks<a href="#restoring-workarounds-for-velero-backups-that-use-admission-webhooks" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Resolve restore failures caused by admission webhooks by applying workarounds for workloads such as Knative and IBM AppConnect resources. This helps you to successfully restore workloads that have mutating or validating admission webhooks.</p>
      <p>Velero has limited abilities to resolve admission webhook issues during a restore. If you have workloads with admission webhooks, you might need to use an additional Velero plugin or make changes to how you restore the workload. Typically, workloads with admission webhooks require you to create a resource of a specific kind first. This is especially true if your workload has child resources because admission webhooks typically block child resources.</p>
      <p>For example, creating or restoring a top-level object such as <code>service.serving.knative.dev</code> typically creates child resources automatically. If you do this first, you will not need to use Velero to create and restore these resources. This avoids the problem of child resources being blocked by an admission webhook that Velero might use.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Velero plugins are started as separate processes. After a Velero operation has completed, either successfully or not, it exits. Receiving a <code>received EOF, stopping recv loop</code> message in the debug logs indicates that a plugin operation has completed. It does not mean that an error has occurred.</p>
        </AlertDescription>
      </Alert>
      <h2 id="migration-debugging-velero-admission-webhooks-knative_restoring-workarounds-for-velero-backups-that-use-admission-webhooks" class="group scroll-mt-20">Restoring Knative resources<a href="#migration-debugging-velero-admission-webhooks-knative_restoring-workarounds-for-velero-backups-that-use-admission-webhooks" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Resolve issues with restoring Knative resources that use admission webhooks by restoring the top-level <code>service.serving.knative.dev</code> service resource with Velero. This helps you to ensure that Knative resources are restored successfully without admission webhook errors.</p>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Restore the top level <code>service.serving.knative.dev Service</code> resource by using the following command:
      <CodeBlock code={"$ velero restore <restore_name> \\\n  --from-backup=<backup_name> --include-resources \\\n  service.serving.knative.dev"} lang="terminal" />
      </li>
      </ul>
      <h2 id="migration-debugging-velero-admission-webhooks-ibm-appconnect_restoring-workarounds-for-velero-backups-that-use-admission-webhooks" class="group scroll-mt-20">Restoring IBM AppConnect resources<a href="#migration-debugging-velero-admission-webhooks-ibm-appconnect_restoring-workarounds-for-velero-backups-that-use-admission-webhooks" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Troubleshoot Velero restore failures for IBM(R) AppConnect resources that use admission webhooks. Verify your webhook rules and check that the installed Operator supports the backup’s version to successfully complete the restore.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Check if you have any mutating admission plugins of <code>kind: MutatingWebhookConfiguration</code> in the cluster by entering/running the following command:
      <CodeBlock code={"$ oc get mutatingwebhookconfigurations"} lang="terminal" />
      </li>
      <li>Examine the YAML file of each <code>kind: MutatingWebhookConfiguration</code> to ensure that none of its rules block creation of the objects that are experiencing issues. For more information, see <a href="https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.23/#rulewithoperations-v1-admissionregistration-k8s-io">the official Kubernetes documentation</a>.</li>
      <li>Check that any <code>spec.version</code> in <code>type: Configuration.appconnect.ibm.com/v1beta1</code> used at backup time is supported by the installed Operator.</li>
      </ol>
      <h2 id="avoiding-the-velero-plugin-panic-error_restoring-workarounds-for-velero-backups-that-use-admission-webhooks" class="group scroll-mt-20">Avoiding the Velero plugin panic error<a href="#avoiding-the-velero-plugin-panic-error_restoring-workarounds-for-velero-backups-that-use-admission-webhooks" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Label a custom Backup Storage Location (BSL) to resolve Velero plugin panic errors during <code>imagestream</code> backups. This helps you to ensure the OADP controller creates the required registry secret when you manage the BSL outside the <code>DataProtectionApplication</code> (DPA) CR.</p>
      <p>A missing secret can cause a panic error for the Velero plugin during image stream backups. When the backup and the BSL are managed outside the scope of the DPA, the OADP controller does not create the relevant <code>oadp-&lt;bsl_name&gt;-&lt;bsl_provider&gt;-registry-secret</code> parameter.</p>
      <p>During the backup operation, the OpenShift Velero plugin panics on the <code>imagestream</code> backup, with the following panic error:</p>
      <CodeBlock code={"024-02-27T10:46:50.028951744Z time=\"2024-02-27T10:46:50Z\" level=error msg=\"Error backing up item\"\nbackup=openshift-adp/<backup name> error=\"error executing custom action (groupResource=imagestreams.image.openshift.io,\nnamespace=<BSL Name>, name=postgres): rpc error: code = Aborted desc = plugin panicked:\nruntime error: index out of range with length 1, stack trace: goroutine 94…"} lang="text" />
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Label the custom BSL with the relevant label by using the following command:
      <CodeBlock code={"$ oc label backupstoragelocations.velero.io <bsl_name> app.kubernetes.io/component=bsl"} lang="terminal" />
      </li>
      <li>
      After the BSL is labeled, wait until the DPA reconciles.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>You can force the reconciliation by making any minor change to the DPA itself.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      After the DPA is reconciled, confirm that the parameter has been created and that the correct registry data has been populated into it by entering the following command:
      <CodeBlock code={"$ oc -n openshift-adp get secret/oadp-<bsl_name>-<bsl_provider>-registry-secret -o json | jq -r '.data'"} lang="terminal" />
      </li>
      </ul>
      <h2 id="workaround-for-openshift-adp-controller-segmentation-fault_restoring-workarounds-for-velero-backups-that-use-admission-webhooks" class="group scroll-mt-20">Workaround for OpenShift ADP Controller segmentation fault<a href="#workaround-for-openshift-adp-controller-segmentation-fault_restoring-workarounds-for-velero-backups-that-use-admission-webhooks" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Define either <code>velero</code> or <code>cloudstorage</code> in your Data Protection Application (DPA) configuration to prevent indefinite pod crashes. This configuration resolves a segmentation fault in the <code>openshift-adp-controller-manager</code> pod that occurs when both components are enabled.</p>
      <p>The <code>openshift-adp-controller-manager</code> pod fails with a crash loop segmentation fault due to the following settings:</p>
      <ul>
      <li>If you define both <code>velero</code> and <code>cloudstorage</code>, the <code>openshift-adp-controller-manager</code> fails.</li>
      <li>If you do not define both <code>velero</code> and <code>cloudstorage</code>, the <code>openshift-adp-controller-manager</code> fails.</li>
      </ul>
      <p>See <em>OADP-1054</em> for more information.</p>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="https://issues.redhat.com/browse/OADP-1054">OADP-1054</a></li></ul>
      </section>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/architecture/admission-plug-ins#admission-plug-ins">Admission plugins</a></li>
      <li><a href="/openshift-docs-markdown/architecture/admission-plug-ins#admission-webhooks-about_admission-plug-ins">Webhook admission plugins</a></li>
      <li><a href="/openshift-docs-markdown/architecture/admission-plug-ins#admission-webhook-types_admission-plug-ins">Types of webhook admission plugins</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
