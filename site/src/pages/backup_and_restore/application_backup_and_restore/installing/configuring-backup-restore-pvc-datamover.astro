---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: backup_and_restore/application_backup_and_restore/installing/configuring-backup-restore-pvc-datamover.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"configuring-backup-restore-pvc-datamover","text":"Configuring backup and restore PVCs for Data Mover"},{"depth":2,"slug":"configuring-backup-pvc_configuring-backup-restore-pvc-datamover","text":"Configuring a  backup PVC for a Data Mover backup"},{"depth":2,"slug":"configuring-restore-pvc_configuring-backup-restore-pvc-datamover","text":"Configuring a restorePVC for a Data Mover restore"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/installing/configuring-backup-restore-pvc-datamover";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring backup and restore PVCs for Data Mover";
const description = "Configure backup and restore persistent volume claims (PVCs) to optimize Data Mover operations. For storage classes like CephFS, these intermediate PVCs…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["backup_and_restore","backup_and_restore/application_backup_and_restore","backup_and_restore/application_backup_and_restore/installing"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring backup and restore PVCs for Data Mover {#configuring-backup-restore-pvc-datamover}\n\nConfigure backup and restore persistent volume claims (PVCs) to optimize Data Mover operations. For storage classes like CephFS, these intermediate PVCs allow the system to create read-only volumes from snapshots, resulting in significantly faster backups.\n\nYou create a `readonly` backup PVC by using the `nodeAgent.backupPVC` section of the `DataProtectionApplication` (DPA) and setting the `readOnly` access mode to `true`.\n\nYou can use the following fields in the `nodeAgent.backupPVC` section of the DPA to configure the backup PVC.\n\n- `storageClass`: The name of the storage class to use for the backup PVC.\n- `readOnly`: Indicates if the backup PVC should be mounted as read-only. Setting this field to `true` also requires you to set the `spcNoRelabeling` field to `true`.\n- `spcNoRelabeling`: Disables automatic relabeling of the volume if set to `true`. You can set this field to `true` only when `readOnly` is `true`. When the `readOnly` flag is `true`, SELinux relabeling of the volume is not possible. This causes the Data Mover backup to fail. Therefore, when you are using the `readOnly` access mode for the CephFS storage class, you must disable relabeling.\n\n## Configuring a  backup PVC for a Data Mover backup {#configuring-backup-pvc_configuring-backup-restore-pvc-datamover}\n\nConfigure backup persistent volume claim (PVC) settings in the `DataProtectionApplication` (DPA) to optimize Data Mover backup performance for different storage classes. The feature gives you read-only access modes for faster data movement.\n\n**Prerequisites**\n\n- You have installed the OADP Operator.\n\n**Procedure**\n\n1. Configure the `nodeAgent.backupPVC` section in the DPA as shown in the following example:\n\n   ```yaml {title=\"Example Data Protection Application\"}\n   apiVersion: oadp.openshift.io/v1alpha1\n   kind: DataProtectionApplication\n   metadata:\n     name: ts-dpa\n     namespace: openshift-adp\n   spec:\n     backupLocations:\n     - velero:\n         credential:\n           key: cloud\n           name: cloud-credentials-gcp\n         default: true\n         objectStorage:\n           bucket: oadp...2jw\n           prefix: velero\n         provider: gcp\n     configuration:\n       nodeAgent:\n         enable: true\n         uploaderType: kopia\n         backupPVC:\n           storage-class-1:\n             readOnly: true\n             spcNoRelabeling: true\n             storageClass: gp3-csi\n           storage-class-2:\n             readOnly: false\n             spcNoRelabeling: false\n             storageClass: gp3-csi\n       velero:\n         defaultPlugins:\n         - gcp\n         - openshift\n         - csi\n   ```\n\n   where:\n\n   `backupPVC`\n   :   Specifies the `backupPVC` section. In this example, the `backupPVC` section has configurations for two storage classes, `storage-class-1` and `storage-class-2`.\n\n   `readOnly`\n   :   Specifies that the `backupPVC` for `storage-class-1` is configured as `readOnly`.\n\n   `spcNoRelabeling`\n   :   Specifies that the `spcNoRelabeling` field is set to `true` because the `backupPVC` for `storage-class-1` is `readOnly`.\n2. Create a `Backup` custom resource by using the following configuration:\n\n   ```yaml {title=\"Example Backup\"}\n   apiVersion: velero.io/v1\n   kind: Backup\n   metadata:\n     name: test-backup\n     namespace: openshift-adp\n   spec:\n     includedNamespaces:\n     - <application_namespace>\n     snapshotMoveData: true\n   ```\n\n   `snapshotMoveData`\n   :   Set to `true` for a Data Mover backup.\n\n**Verification**\n\n- Verify that the backup PVCs are created as read-only (`ROX`) by running the following command:\n\n  ```terminal {title=\"Example command\"}\n  $ oc get pvc -n openshift-adp -w\n  ```\n\n  ```terminal\n  test-backup1-l..d   Bound   pvc-1298.....22f8   2Gi        ROX            standard-csi   <unset>                 37s\n  test-backup1-l..d   Bound   pvc-1298....022f8   2Gi        ROX            standard-csi   <unset>                 37s\n  ```\n\n## Configuring a restorePVC for a Data Mover restore {#configuring-restore-pvc_configuring-backup-restore-pvc-datamover}\n\nConfigure restore persistent volume claim (PVC) settings in the `DataProtectionApplication` (DPA) to optimize Data Mover restore operations and enable parallel volume restores. This improves restore performance by distributing restore pods across nodes.\n\nA `restorePVC` is an intermediate PVC that is used to write data during the Data Mover restore operation.\n\nYou can configure the `restorePVC` in the `DataProtectionApplication` (DPA) object by using the `ignoreDelayBinding` field. Setting the `ignoreDelayBinding` field to `true` allows the restore operation to ignore the `WaitForFirstConsumer` binding mode. The data movement restore operation then creates the restore pod and provisions the associated volume to an arbitrary node.\n\nThe `ignoreDelayBinding` setting is helpful in scenarios where multiple volume restores are happening in parallel. With the `ignoreDelayBinding` field set to `true`, the restore pods can be spread evenly to all nodes.\n\n**Prerequisites**\n\n- You have installed the OADP Operator.\n- You have a created a Data Mover backup of an application.\n\n**Procedure**\n\n- Configure the `restorePVC` section in the DPA as shown in the following example:\n\n  ```yaml {title=\"Example Data Protection Application\"}\n  apiVersion: oadp.openshift.io/v1alpha1\n  kind: DataProtectionApplication\n  metadata:\n    name: ts-dpa\n    namespace: openshift-adp\n  spec:\n  #  ...\n    configuration:\n      nodeAgent:\n        enable: true\n        uploaderType: kopia\n      restorePVC:\n        ignoreDelayBinding: true\n  ```\n\n  where:\n\n  `restorePVC`\n  :   Specifies the `restorePVC` section.\n\n  `ignoreDelayBinding`\n  :   Set the `ignoreDelayBinding` field to `true`.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/installing/configuring-backup-restore-pvc-datamover.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="configuring-backup-restore-pvc-datamover" class="group scroll-mt-20">Configuring backup and restore PVCs for Data Mover<a href="#configuring-backup-restore-pvc-datamover" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Configure backup and restore persistent volume claims (PVCs) to optimize Data Mover operations. For storage classes like CephFS, these intermediate PVCs allow the system to create read-only volumes from snapshots, resulting in significantly faster backups.</p>
      <p>You create a <code>readonly</code> backup PVC by using the <code>nodeAgent.backupPVC</code> section of the <code>DataProtectionApplication</code> (DPA) and setting the <code>readOnly</code> access mode to <code>true</code>.</p>
      <p>You can use the following fields in the <code>nodeAgent.backupPVC</code> section of the DPA to configure the backup PVC.</p>
      <ul>
      <li><code>storageClass</code>: The name of the storage class to use for the backup PVC.</li>
      <li><code>readOnly</code>: Indicates if the backup PVC should be mounted as read-only. Setting this field to <code>true</code> also requires you to set the <code>spcNoRelabeling</code> field to <code>true</code>.</li>
      <li><code>spcNoRelabeling</code>: Disables automatic relabeling of the volume if set to <code>true</code>. You can set this field to <code>true</code> only when <code>readOnly</code> is <code>true</code>. When the <code>readOnly</code> flag is <code>true</code>, SELinux relabeling of the volume is not possible. This causes the Data Mover backup to fail. Therefore, when you are using the <code>readOnly</code> access mode for the CephFS storage class, you must disable relabeling.</li>
      </ul>
      <h2 id="configuring-backup-pvc_configuring-backup-restore-pvc-datamover" class="group scroll-mt-20">Configuring a  backup PVC for a Data Mover backup<a href="#configuring-backup-pvc_configuring-backup-restore-pvc-datamover" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Configure backup persistent volume claim (PVC) settings in the <code>DataProtectionApplication</code> (DPA) to optimize Data Mover backup performance for different storage classes. The feature gives you read-only access modes for faster data movement.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have installed the OADP Operator.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Configure the <code>nodeAgent.backupPVC</code> section in the DPA as shown in the following example:
      <CodeBlock code={"apiVersion: oadp.openshift.io/v1alpha1\nkind: DataProtectionApplication\nmetadata:\n  name: ts-dpa\n  namespace: openshift-adp\nspec:\n  backupLocations:\n  - velero:\n      credential:\n        key: cloud\n        name: cloud-credentials-gcp\n      default: true\n      objectStorage:\n        bucket: oadp...2jw\n        prefix: velero\n      provider: gcp\n  configuration:\n    nodeAgent:\n      enable: true\n      uploaderType: kopia\n      backupPVC:\n        storage-class-1:\n          readOnly: true\n          spcNoRelabeling: true\n          storageClass: gp3-csi\n        storage-class-2:\n          readOnly: false\n          spcNoRelabeling: false\n          storageClass: gp3-csi\n    velero:\n      defaultPlugins:\n      - gcp\n      - openshift\n      - csi"} lang="yaml" title="Example Data Protection Application" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>backupPVC</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the <code>backupPVC</code> section. In this example, the <code>backupPVC</code> section has configurations for two storage classes, <code>storage-class-1</code> and <code>storage-class-2</code>.</p></dd>
      <dt class="font-semibold"><code>readOnly</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies that the <code>backupPVC</code> for <code>storage-class-1</code> is configured as <code>readOnly</code>.</p></dd>
      <dt class="font-semibold"><code>spcNoRelabeling</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies that the <code>spcNoRelabeling</code> field is set to <code>true</code> because the <code>backupPVC</code> for <code>storage-class-1</code> is <code>readOnly</code>.</p></dd>
      </dl>
      </li>
      <li>
      Create a <code>Backup</code> custom resource by using the following configuration:
      <CodeBlock code={"apiVersion: velero.io/v1\nkind: Backup\nmetadata:\n  name: test-backup\n  namespace: openshift-adp\nspec:\n  includedNamespaces:\n  - <application_namespace>\n  snapshotMoveData: true"} lang="yaml" title="Example Backup" />
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>snapshotMoveData</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Set to <code>true</code> for a Data Mover backup.</p></dd>
      </dl>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      Verify that the backup PVCs are created as read-only (<code>ROX</code>) by running the following command:
      <CodeBlock code={"$ oc get pvc -n openshift-adp -w"} lang="terminal" title="Example command" />
      <CodeBlock code={"test-backup1-l..d   Bound   pvc-1298.....22f8   2Gi        ROX            standard-csi   <unset>                 37s\ntest-backup1-l..d   Bound   pvc-1298....022f8   2Gi        ROX            standard-csi   <unset>                 37s"} lang="terminal" />
      </li>
      </ul>
      <h2 id="configuring-restore-pvc_configuring-backup-restore-pvc-datamover" class="group scroll-mt-20">Configuring a restorePVC for a Data Mover restore<a href="#configuring-restore-pvc_configuring-backup-restore-pvc-datamover" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Configure restore persistent volume claim (PVC) settings in the <code>DataProtectionApplication</code> (DPA) to optimize Data Mover restore operations and enable parallel volume restores. This improves restore performance by distributing restore pods across nodes.</p>
      <p>A <code>restorePVC</code> is an intermediate PVC that is used to write data during the Data Mover restore operation.</p>
      <p>You can configure the <code>restorePVC</code> in the <code>DataProtectionApplication</code> (DPA) object by using the <code>ignoreDelayBinding</code> field. Setting the <code>ignoreDelayBinding</code> field to <code>true</code> allows the restore operation to ignore the <code>WaitForFirstConsumer</code> binding mode. The data movement restore operation then creates the restore pod and provisions the associated volume to an arbitrary node.</p>
      <p>The <code>ignoreDelayBinding</code> setting is helpful in scenarios where multiple volume restores are happening in parallel. With the <code>ignoreDelayBinding</code> field set to <code>true</code>, the restore pods can be spread evenly to all nodes.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have installed the OADP Operator.</li>
      <li>You have a created a Data Mover backup of an application.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Configure the <code>restorePVC</code> section in the DPA as shown in the following example:
      <CodeBlock code={"apiVersion: oadp.openshift.io/v1alpha1\nkind: DataProtectionApplication\nmetadata:\n  name: ts-dpa\n  namespace: openshift-adp\nspec:\n#  ...\n  configuration:\n    nodeAgent:\n      enable: true\n      uploaderType: kopia\n    restorePVC:\n      ignoreDelayBinding: true"} lang="yaml" title="Example Data Protection Application" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>restorePVC</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the <code>restorePVC</code> section.</p></dd>
      <dt class="font-semibold"><code>ignoreDelayBinding</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Set the <code>ignoreDelayBinding</code> field to <code>true</code>.</p></dd>
      </dl>
      </li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
