---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: backup_and_restore/application_backup_and_restore/oadp-advanced-topics/oadp-backing-up-data-one-cluster-restoring-another-cluster.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"oadp-backing-up-data-one-cluster-restoring-another-cluster","text":"Backing up data from one cluster and restoring it to another cluster"},{"depth":2,"slug":"oadp-about-backing-and-restoring-from-cluster-to-cluster_oadp-backing-up-data-one-cluster-restoring-another-cluster","text":"About backing up data from one cluster and restoring it on another cluster"},{"depth":3,"slug":"oadp-cluster-to-cluster-operators_oadp-backing-up-data-one-cluster-restoring-another-cluster","text":"Operators"},{"depth":3,"slug":"oadp-cluster-to-cluster-velero_oadp-backing-up-data-one-cluster-restoring-another-cluster","text":"Use of Velero"},{"depth":2,"slug":"oadp-pod-volume-backup_oadp-backing-up-data-one-cluster-restoring-another-cluster","text":"About determining which pod volumes to back up"},{"depth":3,"slug":"pod-volume-limitations_oadp-backing-up-data-one-cluster-restoring-another-cluster","text":"Limitations"},{"depth":3,"slug":"oadp-backing-up-opt-in_oadp-backing-up-data-one-cluster-restoring-another-cluster","text":"Backing up pod volumes by using the opt-in method"},{"depth":3,"slug":"oadp-backing-up-opt-out_oadp-backing-up-data-one-cluster-restoring-another-cluster","text":"Backing up pod volumes by using the opt-out method"},{"depth":2,"slug":"oadp-cluster-to-cluster-uid-and-gid-ranges_oadp-backing-up-data-one-cluster-restoring-another-cluster","text":"UID and GID ranges"},{"depth":2,"slug":"oadp-backing-and-restoring-from-cluster-to-cluster_oadp-backing-up-data-one-cluster-restoring-another-cluster","text":"Backing up data from one cluster and restoring it to another cluster"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/oadp-advanced-topics/oadp-backing-up-data-one-cluster-restoring-another-cluster";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Backing up data from one cluster and restoring it to another cluster";
const description = "Explore how to back up application data from one OpenShift Container Platform cluster and restore it to another cluster. While more complex than…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["backup_and_restore","backup_and_restore/application_backup_and_restore","backup_and_restore/application_backup_and_restore/oadp-advanced-topics"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Backing up data from one cluster and restoring it to another cluster {#oadp-backing-up-data-one-cluster-restoring-another-cluster}\n\nExplore how to back up application data from one OpenShift Container Platform cluster and restore it to another cluster. While more complex than single-cluster operations, OpenShift API for Data Protection provides the tools to manage this cross-cluster data recovery.\n\n## About backing up data from one cluster and restoring it on another cluster {#oadp-about-backing-and-restoring-from-cluster-to-cluster_oadp-backing-up-data-one-cluster-restoring-another-cluster}\n\nBack up application data from one OpenShift Container Platform cluster and restore it to another using OADP.\n\nOpenShift API for Data Protection (OADP) is designed to back up and restore application data in the same OpenShift Container Platform cluster. You can use OADP to back up application data from one OpenShift Container Platform cluster and restore it on another cluster. However, doing so is more complicated than using OADP to back up and restore on the same cluster.\n\nTo successfully use OADP to back up data from one cluster and restore it to another cluster, you must take into account the following factors, in addition to the prerequisites and procedures that apply to using OADP to back up and restore data on the same cluster:\n\n- Operators\n- Use of Velero\n- UID and GID ranges\n\n### Operators {#oadp-cluster-to-cluster-operators_oadp-backing-up-data-one-cluster-restoring-another-cluster}\n\nYou must exclude Operators from the backup of an application for backup and restore to succeed.\n\n### Use of Velero {#oadp-cluster-to-cluster-velero_oadp-backing-up-data-one-cluster-restoring-another-cluster}\n\nVelero, which OADP is built upon, does not natively support migrating persistent volume snapshots across cloud providers. To migrate volume snapshot data between cloud platforms, you must *either* enable the Velero Restic file system backup option, which backs up volume contents at the file system level, *or* use the OADP Data Mover for CSI snapshots.\n\n> [!NOTE]\n> In OADP 1.1 and earlier, the Velero Restic file system backup option is called `restic`. In OADP 1.2 and later, the Velero Restic file system backup option is called `file-system-backup`.\n\n- You must also use Velero’s File System Backup to migrate data between AWS regions or between Microsoft Azure regions.\n- Velero does not support restoring data to a cluster with an *earlier* Kubernetes version than the source cluster.\n- It is theoretically possible to migrate workloads to a destination with a *later* Kubernetes version than the source, but you must consider the compatibility of API groups between clusters for each custom resource. If a Kubernetes version upgrade breaks the compatibility of core or native API groups, you must first update the impacted custom resources.\n\n- [File System Backup](https://velero.io/docs/main/file-system-backup/)\n\n## About determining which pod volumes to back up {#oadp-pod-volume-backup_oadp-backing-up-data-one-cluster-restoring-another-cluster}\n\nBefore starting a File System Backup (FSB), you must specify which pod volumes to back up. Velero calls this process discovering volumes. You can use either the opt-in or opt-out approach to help Velero choose between an FSB, a volume snapshot, or a Data Mover backup.\n\n- **Opt-in approach**: With the opt-in approach, volumes are backed up using snapshot or Data Mover by default. FSB is used on specific volumes that are opted-in by annotations.\n- **Opt-out approach**: With the opt-out approach, volumes are backed up using FSB by default. Snapshots or Data Mover is used on specific volumes that are opted-out by annotations.\n\n### Limitations {#pod-volume-limitations_oadp-backing-up-data-one-cluster-restoring-another-cluster}\n\n- FSB does not support backing up and restoring `hostpath` volumes. However, FSB does support backing up and restoring local volumes.\n- Velero uses a static, common encryption key for all backup repositories it creates. **This static key means that anyone who can access your backup storage can also decrypt your backup data**. It is essential that you limit access to backup storage.\n- For PVCs, every incremental backup chain is maintained across pod reschedules.\n\n  For pod volumes that are *not* PVCs, such as `emptyDir` volumes, if a pod is deleted or recreated, for example, by a `ReplicaSet` or a deployment, the next backup of those volumes will be a full backup and not an incremental backup. It is assumed that the lifecycle of a pod volume is defined by its pod.\n- Even though backup data can be kept incrementally, backing up large files, such as a database, can take a long time. This is because FSB uses deduplication to find the difference that needs to be backed up.\n- FSB reads and writes data from volumes by accessing the file system of the node on which the pod is running. For this reason, FSB can only back up volumes that are mounted from a pod and not directly from a PVC. Some Velero users have overcome this limitation by running a staging pod, such as a BusyBox or Alpine container with an infinite sleep, to mount these PVC and PV pairs before performing a Velero backup..\n- FSB expects volumes to be mounted under `<hostPath>/<pod UID>`, with `<hostPath>` being configurable. Some Kubernetes systems, for example, vCluster, do not mount volumes under the `<pod UID>` subdirectory, and VFSB does not work with them as expected.\n\n### Backing up pod volumes by using the opt-in method {#oadp-backing-up-opt-in_oadp-backing-up-data-one-cluster-restoring-another-cluster}\n\nUse the opt-in method to specify the exact pod volumes you want to back up using File System Backup (FSB). By applying specific annotations, you can selectively include only the volumes you need, which helps you to manage storage and backup efficiency.\n\n**Procedure**\n\n- On each pod that contains one or more volumes that you want to back up, enter the following command:\n\n  ```terminal\n  $ oc -n <your_pod_namespace> annotate pod/<your_pod_name> \\\n    backup.velero.io/backup-volumes=<your_volume_name_1>, \\ <your_volume_name_2>>,...,<your_volume_name_n>\n  ```\n\n  where:\n\n  `<your_volume_name_x>`\n  :   specifies the name of the xth volume in the pod specification.\n\n### Backing up pod volumes by using the opt-out method {#oadp-backing-up-opt-out_oadp-backing-up-data-one-cluster-restoring-another-cluster}\n\nUse the opt-out method to exclude specific pod volumes from your default File System Backup (FSB). While this approach automatically backs up all pod volumes, to customize your backup operations you can use annotations to skip specific ones.\n\nWhen using the opt-out approach, all pod volumes are backed up by using File System Backup (FSB), although there are some exceptions:\n\n- Volumes that mount the default service account token, secrets, and configuration maps.\n- `hostPath` volumes\n\nYou can use the opt-out method to specify which volumes **not** to back up. You can do this by using the `backup.velero.io/backup-volumes-excludes` command.\n\n**Procedure**\n\n- On each pod that contains one or more volumes that you do not want to back up, run the following command:\n\n  ```terminal\n  $ oc -n <your_pod_namespace> annotate pod/<your_pod_name> \\\n    backup.velero.io/backup-volumes-excludes=<your_volume_name_1>, \\ <your_volume_name_2>>,...,<your_volume_name_n>\n  ```\n\n  where:\n\n  `<your_volume_name_x>`\n  :   specifies the name of the xth volume in the pod specification.\n\n  > [!NOTE]\n  > You can enable this behavior for all Velero backups by running the `velero install` command with the `--default-volumes-to-fs-backup` flag.\n\n## UID and GID ranges {#oadp-cluster-to-cluster-uid-and-gid-ranges_oadp-backing-up-data-one-cluster-restoring-another-cluster}\n\nAddress potential User ID (UID) and Group ID (GID) range conflicts when moving data between clusters. Reviewing these mitigations helps you avoid access issues and maintain security contexts after a restore.\n\nSummary of the issues\n:   The namespace UID and GID ranges might change depending on the destination cluster. OADP does not back up and restore OpenShift UID range metadata. If the backed up application requires a specific UID, ensure the range is available upon restore. For more information about OpenShift’s UID and GID ranges, see *A Guide to OpenShift and UIDs*.\n\nDetailed description of the issues\n:   When you create a namespace in OpenShift Container Platform by using the shell command `oc create namespace`, OpenShift Container Platform assigns the namespace a unique User ID (UID) range from its available pool of UIDs, a Supplemental Group (GID) range, and unique SELinux MCS labels. This information is stored in the `metadata.annotations` field of the cluster. This information is part of the Security Context Constraints (SCC) annotations, which comprise of the following components:\n\n    - `openshift.io/sa.scc.mcs`\n    - `openshift.io/sa.scc.supplemental-groups`\n    - `openshift.io/sa.scc.uid-range`\n\nWhen you use OADP to restore the namespace, it automatically uses the information in `metadata.annotations` without resetting it for the destination cluster. As a result, the workload might not have access to the backed up data if any of the following is true:\n\n- There is an existing namespace with other SCC annotations, for example, on another cluster. In this case, OADP uses the existing namespace during the backup instead of the namespace you want to restore.\n- A label selector was used during the backup, but the namespace in which the workloads are executed does not have the label. In this case, OADP does not back up the namespace, but creates a new namespace during the restore that does not contain the annotations of the backed up namespace. This results in a new UID range being assigned to the namespace.\n\n  This can be an issue for customer workloads if OpenShift Container Platform assigns a pod a `securityContext` UID to a pod based on namespace annotations that have changed since the persistent volume data was backed up.\n- The UID of the container no longer matches the UID of the file owner.\n- An error occurs because OpenShift Container Platform has not changed the UID range of the destination cluster to match the backup cluster data. As a result, the backup cluster has a different UID than the destination cluster, which means that the application cannot read or write data on the destination cluster.\n\nMitigations\n:   You can use one or more of the following mitigations to resolve the UID and GID range issues:\n\n- Simple mitigations:\n\n  - If you use a label selector in the `Backup` CR to filter the objects to include in the backup, be sure to add this label selector to the namespace that contains the workspace.\n  - Remove any pre-existing version of a namespace on the destination cluster before attempting to restore a namespace with the same name.\n- Advanced mitigations:\n\n  - Fix UID ranges after migration by *Resolving overlapping UID ranges in OpenShift namespaces after migration*. Step 1 is optional.\n\nFor an in-depth discussion of UID and GID ranges in OpenShift Container Platform with an emphasis on overcoming issues in backing up data on one cluster and restoring it on another, see *A Guide to OpenShift and UIDs*.\n\n- [A Guide to OpenShift and UIDs](https://cloud.redhat.com/blog/a-guide-to-openshift-and-uids)\n- [Resolving overlapping UID ranges in OpenShift namespaces after migration](https://access.redhat.com/articles/6844071)\n\n## Backing up data from one cluster and restoring it to another cluster {#oadp-backing-and-restoring-from-cluster-to-cluster_oadp-backing-up-data-one-cluster-restoring-another-cluster}\n\nReview the specific prerequisites and procedure differences to successfully back up data on one cluster and restore it to another. This helps you adapt standard OADP tasks for cross-cluster data recovery.\n\n**Prerequisites**\n\n- All relevant prerequisites for backing up and restoring on your platform (for example, AWS, Microsoft Azure, Google Cloud, and so on), especially the prerequisites for the Data Protection Application (DPA), are described in the relevant sections of this guide.\n\n**Procedure**\n\n1. Make the following additions to the procedures given for your platform:\n\n   - Ensure that the backup store location (BSL) and volume snapshot location have the same names and paths to restore resources to another cluster.\n   - Share the same object storage location credentials across the clusters.\n   - For best results, use OADP to create the namespace on the destination cluster.\n   - If you use the Velero `file-system-backup` option, enable the `--default-volumes-to-fs-backup` flag for use during backup by running the following command:\n\n     ```terminal\n     $ velero backup create <backup_name> --default-volumes-to-fs-backup <any_other_options>\n     ```\n\n     > [!NOTE]\n     > In OADP 1.2 and later, the Velero Restic option is called `file-system-backup`.\n\n     > [!IMPORTANT]\n     > Before restoring a CSI back up, edit the `VolumeSnapshotClass` custom resource (CR), and set the  `snapshot.storage.kubernetes.io/is-default-class parameter` to false. Otherwise, the restore will partially fail due to the same value in the `VolumeSnapshotClass` in the target cluster for the same drive.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/oadp-advanced-topics/oadp-backing-up-data-one-cluster-restoring-another-cluster.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="oadp-backing-up-data-one-cluster-restoring-another-cluster" class="group scroll-mt-20">Backing up data from one cluster and restoring it to another cluster<a href="#oadp-backing-up-data-one-cluster-restoring-another-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Explore how to back up application data from one OpenShift Container Platform cluster and restore it to another cluster. While more complex than single-cluster operations, OpenShift API for Data Protection provides the tools to manage this cross-cluster data recovery.</p>
      <h2 id="oadp-about-backing-and-restoring-from-cluster-to-cluster_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="group scroll-mt-20">About backing up data from one cluster and restoring it on another cluster<a href="#oadp-about-backing-and-restoring-from-cluster-to-cluster_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Back up application data from one OpenShift Container Platform cluster and restore it to another using OADP.</p>
      <p>OpenShift API for Data Protection (OADP) is designed to back up and restore application data in the same OpenShift Container Platform cluster. You can use OADP to back up application data from one OpenShift Container Platform cluster and restore it on another cluster. However, doing so is more complicated than using OADP to back up and restore on the same cluster.</p>
      <p>To successfully use OADP to back up data from one cluster and restore it to another cluster, you must take into account the following factors, in addition to the prerequisites and procedures that apply to using OADP to back up and restore data on the same cluster:</p>
      <ul>
      <li>Operators</li>
      <li>Use of Velero</li>
      <li>UID and GID ranges</li>
      </ul>
      <h3 id="oadp-cluster-to-cluster-operators_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="group scroll-mt-20">Operators<a href="#oadp-cluster-to-cluster-operators_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>You must exclude Operators from the backup of an application for backup and restore to succeed.</p>
      <h3 id="oadp-cluster-to-cluster-velero_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="group scroll-mt-20">Use of Velero<a href="#oadp-cluster-to-cluster-velero_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Velero, which OADP is built upon, does not natively support migrating persistent volume snapshots across cloud providers. To migrate volume snapshot data between cloud platforms, you must <em>either</em> enable the Velero Restic file system backup option, which backs up volume contents at the file system level, <em>or</em> use the OADP Data Mover for CSI snapshots.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>In OADP 1.1 and earlier, the Velero Restic file system backup option is called <code>restic</code>. In OADP 1.2 and later, the Velero Restic file system backup option is called <code>file-system-backup</code>.</p>
        </AlertDescription>
      </Alert>
      <ul>
      <li>You must also use Velero’s File System Backup to migrate data between AWS regions or between Microsoft Azure regions.</li>
      <li>Velero does not support restoring data to a cluster with an <em>earlier</em> Kubernetes version than the source cluster.</li>
      <li>It is theoretically possible to migrate workloads to a destination with a <em>later</em> Kubernetes version than the source, but you must consider the compatibility of API groups between clusters for each custom resource. If a Kubernetes version upgrade breaks the compatibility of core or native API groups, you must first update the impacted custom resources.</li>
      </ul>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="https://velero.io/docs/main/file-system-backup/">File System Backup</a></li></ul>
      </section>
      <h2 id="oadp-pod-volume-backup_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="group scroll-mt-20">About determining which pod volumes to back up<a href="#oadp-pod-volume-backup_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Before starting a File System Backup (FSB), you must specify which pod volumes to back up. Velero calls this process discovering volumes. You can use either the opt-in or opt-out approach to help Velero choose between an FSB, a volume snapshot, or a Data Mover backup.</p>
      <ul>
      <li><strong>Opt-in approach</strong>: With the opt-in approach, volumes are backed up using snapshot or Data Mover by default. FSB is used on specific volumes that are opted-in by annotations.</li>
      <li><strong>Opt-out approach</strong>: With the opt-out approach, volumes are backed up using FSB by default. Snapshots or Data Mover is used on specific volumes that are opted-out by annotations.</li>
      </ul>
      <h3 id="pod-volume-limitations_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="group scroll-mt-20">Limitations<a href="#pod-volume-limitations_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <ul>
      <li>FSB does not support backing up and restoring <code>hostpath</code> volumes. However, FSB does support backing up and restoring local volumes.</li>
      <li>Velero uses a static, common encryption key for all backup repositories it creates. <strong>This static key means that anyone who can access your backup storage can also decrypt your backup data</strong>. It is essential that you limit access to backup storage.</li>
      <li>
      For PVCs, every incremental backup chain is maintained across pod reschedules.
      <p>For pod volumes that are <em>not</em> PVCs, such as <code>emptyDir</code> volumes, if a pod is deleted or recreated, for example, by a <code>ReplicaSet</code> or a deployment, the next backup of those volumes will be a full backup and not an incremental backup. It is assumed that the lifecycle of a pod volume is defined by its pod.</p>
      </li>
      <li>Even though backup data can be kept incrementally, backing up large files, such as a database, can take a long time. This is because FSB uses deduplication to find the difference that needs to be backed up.</li>
      <li>FSB reads and writes data from volumes by accessing the file system of the node on which the pod is running. For this reason, FSB can only back up volumes that are mounted from a pod and not directly from a PVC. Some Velero users have overcome this limitation by running a staging pod, such as a BusyBox or Alpine container with an infinite sleep, to mount these PVC and PV pairs before performing a Velero backup..</li>
      <li>FSB expects volumes to be mounted under <code>&lt;hostPath&gt;/&lt;pod UID&gt;</code>, with <code>&lt;hostPath&gt;</code> being configurable. Some Kubernetes systems, for example, vCluster, do not mount volumes under the <code>&lt;pod UID&gt;</code> subdirectory, and VFSB does not work with them as expected.</li>
      </ul>
      <h3 id="oadp-backing-up-opt-in_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="group scroll-mt-20">Backing up pod volumes by using the opt-in method<a href="#oadp-backing-up-opt-in_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">Use the opt-in method to specify the exact pod volumes you want to back up using File System Backup (FSB). By applying specific annotations, you can selectively include only the volumes you need, which helps you to manage storage and backup efficiency.</p>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      On each pod that contains one or more volumes that you want to back up, enter the following command:
      <CodeBlock code={"$ oc -n <your_pod_namespace> annotate pod/<your_pod_name> \\\n  backup.velero.io/backup-volumes=<your_volume_name_1>, \\ <your_volume_name_2>>,...,<your_volume_name_n>"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;your_volume_name_x&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>specifies the name of the xth volume in the pod specification.</p></dd>
      </dl>
      </li>
      </ul>
      <h3 id="oadp-backing-up-opt-out_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="group scroll-mt-20">Backing up pod volumes by using the opt-out method<a href="#oadp-backing-up-opt-out_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">Use the opt-out method to exclude specific pod volumes from your default File System Backup (FSB). While this approach automatically backs up all pod volumes, to customize your backup operations you can use annotations to skip specific ones.</p>
      <p>When using the opt-out approach, all pod volumes are backed up by using File System Backup (FSB), although there are some exceptions:</p>
      <ul>
      <li>Volumes that mount the default service account token, secrets, and configuration maps.</li>
      <li><code>hostPath</code> volumes</li>
      </ul>
      <p>You can use the opt-out method to specify which volumes <strong>not</strong> to back up. You can do this by using the <code>backup.velero.io/backup-volumes-excludes</code> command.</p>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      On each pod that contains one or more volumes that you do not want to back up, run the following command:
      <CodeBlock code={"$ oc -n <your_pod_namespace> annotate pod/<your_pod_name> \\\n  backup.velero.io/backup-volumes-excludes=<your_volume_name_1>, \\ <your_volume_name_2>>,...,<your_volume_name_n>"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;your_volume_name_x&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>specifies the name of the xth volume in the pod specification.</p></dd>
      </dl>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>You can enable this behavior for all Velero backups by running the <code>velero install</code> command with the <code>--default-volumes-to-fs-backup</code> flag.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      <h2 id="oadp-cluster-to-cluster-uid-and-gid-ranges_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="group scroll-mt-20">UID and GID ranges<a href="#oadp-cluster-to-cluster-uid-and-gid-ranges_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Address potential User ID (UID) and Group ID (GID) range conflicts when moving data between clusters. Reviewing these mitigations helps you avoid access issues and maintain security contexts after a restore.</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Summary of the issues</dt>
      <dd class="ml-6 text-muted-foreground"><p>The namespace UID and GID ranges might change depending on the destination cluster. OADP does not back up and restore OpenShift UID range metadata. If the backed up application requires a specific UID, ensure the range is available upon restore. For more information about OpenShift’s UID and GID ranges, see <em>A Guide to OpenShift and UIDs</em>.</p></dd>
      <dt class="font-semibold">Detailed description of the issues</dt>
      <dd class="ml-6 text-muted-foreground">
      <p>When you create a namespace in OpenShift Container Platform by using the shell command <code>oc create namespace</code>, OpenShift Container Platform assigns the namespace a unique User ID (UID) range from its available pool of UIDs, a Supplemental Group (GID) range, and unique SELinux MCS labels. This information is stored in the <code>metadata.annotations</code> field of the cluster. This information is part of the Security Context Constraints (SCC) annotations, which comprise of the following components:</p>
      <ul>
      <li><code>openshift.io/sa.scc.mcs</code></li>
      <li><code>openshift.io/sa.scc.supplemental-groups</code></li>
      <li><code>openshift.io/sa.scc.uid-range</code></li>
      </ul>
      </dd>
      </dl>
      <p>When you use OADP to restore the namespace, it automatically uses the information in <code>metadata.annotations</code> without resetting it for the destination cluster. As a result, the workload might not have access to the backed up data if any of the following is true:</p>
      <ul>
      <li>There is an existing namespace with other SCC annotations, for example, on another cluster. In this case, OADP uses the existing namespace during the backup instead of the namespace you want to restore.</li>
      <li>
      A label selector was used during the backup, but the namespace in which the workloads are executed does not have the label. In this case, OADP does not back up the namespace, but creates a new namespace during the restore that does not contain the annotations of the backed up namespace. This results in a new UID range being assigned to the namespace.
      <p>This can be an issue for customer workloads if OpenShift Container Platform assigns a pod a <code>securityContext</code> UID to a pod based on namespace annotations that have changed since the persistent volume data was backed up.</p>
      </li>
      <li>The UID of the container no longer matches the UID of the file owner.</li>
      <li>An error occurs because OpenShift Container Platform has not changed the UID range of the destination cluster to match the backup cluster data. As a result, the backup cluster has a different UID than the destination cluster, which means that the application cannot read or write data on the destination cluster.</li>
      </ul>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Mitigations</dt>
      <dd class="ml-6 text-muted-foreground"><p>You can use one or more of the following mitigations to resolve the UID and GID range issues:</p></dd>
      </dl>
      <ul>
      <li>
      Simple mitigations:
      <ul>
      <li>If you use a label selector in the <code>Backup</code> CR to filter the objects to include in the backup, be sure to add this label selector to the namespace that contains the workspace.</li>
      <li>Remove any pre-existing version of a namespace on the destination cluster before attempting to restore a namespace with the same name.</li>
      </ul>
      </li>
      <li>
      Advanced mitigations:
      <ul><li>Fix UID ranges after migration by <em>Resolving overlapping UID ranges in OpenShift namespaces after migration</em>. Step 1 is optional.</li></ul>
      </li>
      </ul>
      <p>For an in-depth discussion of UID and GID ranges in OpenShift Container Platform with an emphasis on overcoming issues in backing up data on one cluster and restoring it on another, see <em>A Guide to OpenShift and UIDs</em>.</p>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="https://cloud.redhat.com/blog/a-guide-to-openshift-and-uids">A Guide to OpenShift and UIDs</a></li>
      <li><a href="https://access.redhat.com/articles/6844071">Resolving overlapping UID ranges in OpenShift namespaces after migration</a></li>
      </ul>
      </section>
      <h2 id="oadp-backing-and-restoring-from-cluster-to-cluster_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="group scroll-mt-20">Backing up data from one cluster and restoring it to another cluster<a href="#oadp-backing-and-restoring-from-cluster-to-cluster_oadp-backing-up-data-one-cluster-restoring-another-cluster" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Review the specific prerequisites and procedure differences to successfully back up data on one cluster and restore it to another. This helps you adapt standard OADP tasks for cross-cluster data recovery.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>All relevant prerequisites for backing up and restoring on your platform (for example, AWS, Microsoft Azure, Google Cloud, and so on), especially the prerequisites for the Data Protection Application (DPA), are described in the relevant sections of this guide.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Make the following additions to the procedures given for your platform:
      <ul>
      <li>Ensure that the backup store location (BSL) and volume snapshot location have the same names and paths to restore resources to another cluster.</li>
      <li>Share the same object storage location credentials across the clusters.</li>
      <li>For best results, use OADP to create the namespace on the destination cluster.</li>
      <li>
      If you use the Velero <code>file-system-backup</code> option, enable the <code>--default-volumes-to-fs-backup</code> flag for use during backup by running the following command:
      <CodeBlock code={"$ velero backup create <backup_name> --default-volumes-to-fs-backup <any_other_options>"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>In OADP 1.2 and later, the Velero Restic option is called <code>file-system-backup</code>.</p>
        </AlertDescription>
      </Alert>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Before restoring a CSI back up, edit the <code>VolumeSnapshotClass</code> custom resource (CR), and set the  <code>snapshot.storage.kubernetes.io/is-default-class parameter</code> to false. Otherwise, the restore will partially fail due to the same value in the <code>VolumeSnapshotClass</code> in the target cluster for the same drive.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
