---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: backup_and_restore/application_backup_and_restore/oadp-use-cases/oadp-usecase-restore-different-namespace.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"oadp-use-cases-restore","text":"OpenShift API for Data Protection (OADP) restore use case"},{"depth":2,"slug":"oadp-usecase-restore_oadp-use-cases-restore","text":"Restoring an application to a different namespace using OADP"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/oadp-use-cases/oadp-usecase-restore-different-namespace";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "OpenShift API for Data Protection (OADP) restore use case";
const description = "Following is a use case for using OADP to restore a backup to a different namespace." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["backup_and_restore","backup_and_restore/application_backup_and_restore","backup_and_restore/application_backup_and_restore/oadp-use-cases"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# OpenShift API for Data Protection (OADP) restore use case {#oadp-use-cases-restore}\n\nFollowing is a use case for using OADP to restore a backup to a different namespace.\n\n## Restoring an application to a different namespace using OADP {#oadp-usecase-restore_oadp-use-cases-restore}\n\nRestore a backup of an application by using OADP to a new target namespace, `test-restore-application`. To restore a backup, you create a restore custom resource (CR) as shown in the following example. In the restore CR, the source namespace refers to the application namespace that you included in the backup. You then verify the restore by changing your project to the new restored namespace and verifying the resources.\n\n**Prerequisites**\n\n- You installed the OADP Operator.\n- You have the backup of an application to be restored.\n\n**Procedure**\n\n1. Create a restore CR as shown in the following example:\n\n   ```yaml\n   apiVersion: velero.io/v1\n   kind: Restore\n   metadata:\n     name: test-restore\n     namespace: openshift-adp\n   spec:\n     backupName: <backup_name>\n     restorePVs: true\n     namespaceMapping:\n       <application_namespace>: test-restore-application\n   ```\n\n   where:\n\n   `test-restore`\n   :   Specifies the name of the restore CR.\n\n   `<backup_name>`\n   :   Specifies the name of the backup.\n\n   `<application_namespace>`\n   :   Specifies the target namespace to restore to. `namespaceMapping` maps the source application namespace to the target application namespace. `test-restore-application` is the name of target namespace where you want to restore the backup.\n2. Apply the restore CR by running the following command:\n\n   ```terminal\n   $ oc apply -f <restore_cr_filename>\n   ```\n\n**Verification**\n\n1. Verify that the restore is in the `Completed` phase by running the following command:\n\n   ```terminal\n   $ oc describe restores.velero.io <restore_name> -n openshift-adp\n   ```\n2. Change to the restored namespace `test-restore-application` by running the following command:\n\n   ```terminal\n   $ oc project test-restore-application\n   ```\n3. Verify the restored resources such as persistent volume claim (pvc), service (svc), deployment, secret, and config map by running the following command:\n\n   ```terminal\n   $ oc get pvc,svc,deployment,secret,configmap\n   ```\n\n   ```terminal {title=\"Example output\"}\n   NAME                          STATUS   VOLUME\n   persistentvolumeclaim/mysql   Bound    pvc-9b3583db-...-14b86\n\n   NAME               TYPE        CLUSTER-IP     EXTERNAL-IP   PORT(S)    AGE\n   service/mysql      ClusterIP   172....157     <none>        3306/TCP   2m56s\n   service/todolist   ClusterIP   172.....15     <none>        8000/TCP   2m56s\n\n   NAME                    READY   UP-TO-DATE   AVAILABLE   AGE\n   deployment.apps/mysql   0/1     1            0           2m55s\n\n   NAME                                         TYPE                      DATA   AGE\n   secret/builder-dockercfg-6bfmd               kubernetes.io/dockercfg   1      2m57s\n   secret/default-dockercfg-hz9kz               kubernetes.io/dockercfg   1      2m57s\n   secret/deployer-dockercfg-86cvd              kubernetes.io/dockercfg   1      2m57s\n   secret/mysql-persistent-sa-dockercfg-rgp9b   kubernetes.io/dockercfg   1      2m57s\n\n   NAME                                 DATA   AGE\n   configmap/kube-root-ca.crt           1      2m57s\n   configmap/openshift-service-ca.crt   1      2m57s\n   ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/oadp-use-cases/oadp-usecase-restore-different-namespace.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="oadp-use-cases-restore" class="group scroll-mt-20">OpenShift API for Data Protection (OADP) restore use case<a href="#oadp-use-cases-restore" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Following is a use case for using OADP to restore a backup to a different namespace.</p>
      <h2 id="oadp-usecase-restore_oadp-use-cases-restore" class="group scroll-mt-20">Restoring an application to a different namespace using OADP<a href="#oadp-usecase-restore_oadp-use-cases-restore" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Restore a backup of an application by using OADP to a new target namespace, <code>test-restore-application</code>. To restore a backup, you create a restore custom resource (CR) as shown in the following example. In the restore CR, the source namespace refers to the application namespace that you included in the backup. You then verify the restore by changing your project to the new restored namespace and verifying the resources.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You installed the OADP Operator.</li>
      <li>You have the backup of an application to be restored.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a restore CR as shown in the following example:
      <CodeBlock code={"apiVersion: velero.io/v1\nkind: Restore\nmetadata:\n  name: test-restore\n  namespace: openshift-adp\nspec:\n  backupName: <backup_name>\n  restorePVs: true\n  namespaceMapping:\n    <application_namespace>: test-restore-application"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>test-restore</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the restore CR.</p></dd>
      <dt class="font-semibold"><code>&lt;backup_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the backup.</p></dd>
      <dt class="font-semibold"><code>&lt;application_namespace&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the target namespace to restore to. <code>namespaceMapping</code> maps the source application namespace to the target application namespace. <code>test-restore-application</code> is the name of target namespace where you want to restore the backup.</p></dd>
      </dl>
      </li>
      <li>
      Apply the restore CR by running the following command:
      <CodeBlock code={"$ oc apply -f <restore_cr_filename> "} lang="terminal" />
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>
      Verify that the restore is in the <code>Completed</code> phase by running the following command:
      <CodeBlock code={"$ oc describe restores.velero.io <restore_name> -n openshift-adp "} lang="terminal" />
      </li>
      <li>
      Change to the restored namespace <code>test-restore-application</code> by running the following command:
      <CodeBlock code={"$ oc project test-restore-application"} lang="terminal" />
      </li>
      <li>
      Verify the restored resources such as persistent volume claim (pvc), service (svc), deployment, secret, and config map by running the following command:
      <CodeBlock code={"$ oc get pvc,svc,deployment,secret,configmap"} lang="terminal" />
      <CodeBlock code={"NAME                          STATUS   VOLUME                                     \npersistentvolumeclaim/mysql   Bound    pvc-9b3583db-...-14b86 \n\nNAME               TYPE        CLUSTER-IP     EXTERNAL-IP   PORT(S)    AGE\nservice/mysql      ClusterIP   172....157     <none>        3306/TCP   2m56s\nservice/todolist   ClusterIP   172.....15     <none>        8000/TCP   2m56s\n\nNAME                    READY   UP-TO-DATE   AVAILABLE   AGE\ndeployment.apps/mysql   0/1     1            0           2m55s\n\nNAME                                         TYPE                      DATA   AGE\nsecret/builder-dockercfg-6bfmd               kubernetes.io/dockercfg   1      2m57s\nsecret/default-dockercfg-hz9kz               kubernetes.io/dockercfg   1      2m57s\nsecret/deployer-dockercfg-86cvd              kubernetes.io/dockercfg   1      2m57s\nsecret/mysql-persistent-sa-dockercfg-rgp9b   kubernetes.io/dockercfg   1      2m57s\n\nNAME                                 DATA   AGE\nconfigmap/kube-root-ca.crt           1      2m57s\nconfigmap/openshift-service-ca.crt   1      2m57s"} lang="terminal" title="Example output" />
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
