---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-creating-backup-hooks-doc.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"oadp-creating-backup-hooks-doc","text":"Creating backup hooks"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-creating-backup-hooks-doc";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Creating backup hooks";
const description = "Create backup hooks to run commands in a container in a pod by editing the Backup custom resource (CR). This helps you to run pre-backup and post-backup…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["backup_and_restore","backup_and_restore/application_backup_and_restore","backup_and_restore/application_backup_and_restore/backing_up_and_restoring"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Creating backup hooks {#oadp-creating-backup-hooks-doc}\n\nCreate backup hooks to run commands in a container in a pod by editing the `Backup` custom resource (CR). This helps you to run pre-backup and post-backup actions such as quiescing a database or flushing data to disk.\n\nThe commands can be configured to performed before any custom action processing (*Pre* hooks), or after all custom actions have been completed and any additional items specified by the custom action have been backed up (*Post* hooks).\n\n**Procedure**\n\n- Add a hook to the `spec.hooks` block of the `Backup` CR, as in the following example:\n\n  ```yaml\n  apiVersion: velero.io/v1\n  kind: Backup\n  metadata:\n    name: <backup>\n    namespace: openshift-adp\n  spec:\n    hooks:\n      resources:\n        - name: <hook_name>\n          includedNamespaces:\n          - <namespace>\n          excludedNamespaces:\n          - <namespace>\n          includedResources: []\n          - pods\n          excludedResources: []\n          labelSelector:\n            matchLabels:\n              app: velero\n              component: server\n          pre:\n            - exec:\n                container: <container>\n                command:\n                - /bin/uname\n                - -a\n                onError: Fail\n                timeout: 30s\n          post:\n  ...\n  ```\n\n  where:\n\n  `<namespace>`\n  :   Optional: Specifies the namespaces to which the hook applies. If this value is not specified, the hook applies to all namespaces.\n\n  `excludedNamespaces`\n  :   Optional: Specifies the namespaces to which the hook does not apply.\n\n  `pods`\n  :   Currently, pods are the only supported resource that hooks can apply to.\n\n  `excludedResources`\n  :   Optional: Specifies the resources to which the hook does not apply.\n\n  `labelSelector`\n  :   Optional: This hook only applies to objects matching the label. If this value is not specified, the hook applies to all objects.\n\n  `pre`\n  :   Specifies an array of hooks to run before the backup.\n\n  `<container>`\n  :   Optional: Specifies the container in which the command runs. If the container is not specified, the command runs in the first container in the pod.\n\n  `/bin/uname`\n  :   Specifies the entry point for the `init` container being added.\n\n  `onError: Fail`\n  :   Specifies the error handling behavior. Allowed values are `Fail` and `Continue`. The default is `Fail`.\n\n  `timeout: 30s`\n  :   Optional: Specifies how long to wait for the commands to run. The default is `30s`.\n\n  `post`\n  :   Specifies an array of hooks to run after the backup, with the same parameters as the pre-backup hooks.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-creating-backup-hooks-doc.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="oadp-creating-backup-hooks-doc" class="group scroll-mt-20">Creating backup hooks<a href="#oadp-creating-backup-hooks-doc" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Create backup hooks to run commands in a container in a pod by editing the <code>Backup</code> custom resource (CR). This helps you to run pre-backup and post-backup actions such as quiescing a database or flushing data to disk.</p>
      <p>The commands can be configured to performed before any custom action processing (<em>Pre</em> hooks), or after all custom actions have been completed and any additional items specified by the custom action have been backed up (<em>Post</em> hooks).</p>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Add a hook to the <code>spec.hooks</code> block of the <code>Backup</code> CR, as in the following example:
      <CodeBlock code={"apiVersion: velero.io/v1\nkind: Backup\nmetadata:\n  name: <backup>\n  namespace: openshift-adp\nspec:\n  hooks:\n    resources:\n      - name: <hook_name>\n        includedNamespaces:\n        - <namespace>\n        excludedNamespaces:\n        - <namespace>\n        includedResources: []\n        - pods\n        excludedResources: []\n        labelSelector:\n          matchLabels:\n            app: velero\n            component: server\n        pre:\n          - exec:\n              container: <container>\n              command:\n              - /bin/uname\n              - -a\n              onError: Fail\n              timeout: 30s\n        post:\n..."} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;namespace&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional: Specifies the namespaces to which the hook applies. If this value is not specified, the hook applies to all namespaces.</p></dd>
      <dt class="font-semibold"><code>excludedNamespaces</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional: Specifies the namespaces to which the hook does not apply.</p></dd>
      <dt class="font-semibold"><code>pods</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Currently, pods are the only supported resource that hooks can apply to.</p></dd>
      <dt class="font-semibold"><code>excludedResources</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional: Specifies the resources to which the hook does not apply.</p></dd>
      <dt class="font-semibold"><code>labelSelector</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional: This hook only applies to objects matching the label. If this value is not specified, the hook applies to all objects.</p></dd>
      <dt class="font-semibold"><code>pre</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies an array of hooks to run before the backup.</p></dd>
      <dt class="font-semibold"><code>&lt;container&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional: Specifies the container in which the command runs. If the container is not specified, the command runs in the first container in the pod.</p></dd>
      <dt class="font-semibold"><code>/bin/uname</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the entry point for the <code>init</code> container being added.</p></dd>
      <dt class="font-semibold"><code>onError: Fail</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the error handling behavior. Allowed values are <code>Fail</code> and <code>Continue</code>. The default is <code>Fail</code>.</p></dd>
      <dt class="font-semibold"><code>timeout: 30s</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional: Specifies how long to wait for the commands to run. The default is <code>30s</code>.</p></dd>
      <dt class="font-semibold"><code>post</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies an array of hooks to run after the backup, with the same parameters as the pre-backup hooks.</p></dd>
      </dl>
      </li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
