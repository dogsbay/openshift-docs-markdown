---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-creating-backup-cr.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"oadp-creating-backup-cr-doc","text":"Creating a Backup CR"}];
const nav = navData;
const currentPath = "/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-creating-backup-cr";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Creating a Backup CR";
const description = "Back up Kubernetes resources, internal images, and persistent volumes (PVs) by creating a Backup custom resource (CR). This helps you to protect your…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["backup_and_restore","backup_and_restore/application_backup_and_restore","backup_and_restore/application_backup_and_restore/backing_up_and_restoring"];
const pageTaxonomies = null;
const tagsIndexPath = "/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Creating a Backup CR {#oadp-creating-backup-cr-doc}\n\nBack up Kubernetes resources, internal images, and persistent volumes (PVs) by creating a `Backup` custom resource (CR). This helps you to protect your application data and configuration for disaster recovery.\n\n**Prerequisites**\n\n- You must install the OpenShift API for Data Protection (OADP) Operator.\n- The `DataProtectionApplication` CR must be in a `Ready` state.\n- Backup location prerequisites:\n\n  - You must have S3 object storage configured for Velero.\n  - You must have a backup location configured in the `DataProtectionApplication` CR.\n- Snapshot location prerequisites:\n\n  - Your cloud provider must have a native snapshot API or support Container Storage Interface (CSI) snapshots.\n  - For CSI snapshots, you must create a `VolumeSnapshotClass` CR to register the CSI driver.\n  - You must have a volume location configured in the `DataProtectionApplication` CR.\n\n**Procedure**\n\n1. Retrieve the `backupStorageLocations` CRs by entering the following command:\n\n   ```terminal\n   $ oc get backupstoragelocations.velero.io -n openshift-adp\n   ```\n\n   ```terminal\n   NAMESPACE       NAME              PHASE       LAST VALIDATED   AGE   DEFAULT\n   openshift-adp   velero-sample-1   Available   11s              31m\n   ```\n2. Create a `Backup` CR, as in the following example:\n\n   ```yaml\n   apiVersion: velero.io/v1\n   kind: Backup\n   metadata:\n     name: <backup>\n     labels:\n       velero.io/storage-location: default\n     namespace: openshift-adp\n   spec:\n     hooks: {}\n     includedNamespaces:\n     - <namespace>\n     includedResources: []\n     excludedResources: []\n     storageLocation: <velero-sample-1>\n     ttl: 720h0m0s\n     labelSelector:\n       matchLabels:\n         app: <label_1>\n         app: <label_2>\n         app: <label_3>\n     orLabelSelectors:\n     - matchLabels:\n         app: <label_1>\n         app: <label_2>\n         app: <label_3>\n   ```\n\n   where:\n\n   `<namespace>`\n   :   Specifies an array of namespaces to back up.\n\n   `includedResources`\n   :   Optional: Specifies an array of resources to include in the backup. Resources might be shortcuts (for example, `po` for `pods`) or fully-qualified. If unspecified, all resources are included.\n\n   `excludedResources`\n   :   Optional: Specifies an array of resources to exclude from the backup. Resources might be shortcuts (for example, `po` for `pods`) or fully-qualified.\n\n   `<velero-sample-1>`\n   :   Specifies the name of the `backupStorageLocations` CR.\n\n   `labelSelector`\n   :   Specifies a map of {key,value} pairs of backup resources that have **all** the specified labels.\n\n   `orLabelSelectors`\n   :   Specifies a map of {key,value} pairs of backup resources that have **one or more** of the specified labels.\n3. Verify that the status of the `Backup` CR is `Completed`:\n\n   ```terminal\n   $ oc get backups.velero.io -n openshift-adp <backup> -o jsonpath='{.status.phase}'\n   ```\n","mdUrl":"/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-creating-backup-cr.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={""}
  navMode={"client"}
  pagefindUrl={"/pagefind/"}
  favicon={"/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="oadp-creating-backup-cr-doc" class="group scroll-mt-20">Creating a Backup CR<a href="#oadp-creating-backup-cr-doc" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="/_attributes/attributes-openshift-dedicated.md /_attributes/common-attributes.md"></p>
      <p>Back up Kubernetes resources, internal images, and persistent volumes (PVs) by creating a <code>Backup</code> custom resource (CR). This helps you to protect your application data and configuration for disaster recovery.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You must install the OpenShift API for Data Protection (OADP) Operator.</li>
      <li>The <code>DataProtectionApplication</code> CR must be in a <code>Ready</code> state.</li>
      <li>
      Backup location prerequisites:
      <ul>
      <li>You must have S3 object storage configured for Velero.</li>
      <li>You must have a backup location configured in the <code>DataProtectionApplication</code> CR.</li>
      </ul>
      </li>
      <li>
      Snapshot location prerequisites:
      <ul>
      <li>Your cloud provider must have a native snapshot API or support Container Storage Interface (CSI) snapshots.</li>
      <li>For CSI snapshots, you must create a <code>VolumeSnapshotClass</code> CR to register the CSI driver.</li>
      <li>You must have a volume location configured in the <code>DataProtectionApplication</code> CR.</li>
      </ul>
      </li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Retrieve the <code>backupStorageLocations</code> CRs by entering the following command:
      <CodeBlock code={"$ oc get backupstoragelocations.velero.io -n openshift-adp"} lang="terminal" />
      <CodeBlock code={"NAMESPACE       NAME              PHASE       LAST VALIDATED   AGE   DEFAULT\nopenshift-adp   velero-sample-1   Available   11s              31m"} lang="terminal" />
      </li>
      <li>
      Create a <code>Backup</code> CR, as in the following example:
      <CodeBlock code={"apiVersion: velero.io/v1\nkind: Backup\nmetadata:\n  name: <backup>\n  labels:\n    velero.io/storage-location: default\n  namespace: openshift-adp\nspec:\n  hooks: {}\n  includedNamespaces:\n  - <namespace>\n  includedResources: []\n  excludedResources: []\n  storageLocation: <velero-sample-1>\n  ttl: 720h0m0s\n  labelSelector:\n    matchLabels:\n      app: <label_1>\n      app: <label_2>\n      app: <label_3>\n  orLabelSelectors:\n  - matchLabels:\n      app: <label_1>\n      app: <label_2>\n      app: <label_3>"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;namespace&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies an array of namespaces to back up.</p></dd>
      <dt class="font-semibold"><code>includedResources</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional: Specifies an array of resources to include in the backup. Resources might be shortcuts (for example, <code>po</code> for <code>pods</code>) or fully-qualified. If unspecified, all resources are included.</p></dd>
      <dt class="font-semibold"><code>excludedResources</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional: Specifies an array of resources to exclude from the backup. Resources might be shortcuts (for example, <code>po</code> for <code>pods</code>) or fully-qualified.</p></dd>
      <dt class="font-semibold"><code>&lt;velero-sample-1&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the <code>backupStorageLocations</code> CR.</p></dd>
      <dt class="font-semibold"><code>labelSelector</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a map of {"{"}key,value{"}"} pairs of backup resources that have <strong>all</strong> the specified labels.</p></dd>
      <dt class="font-semibold"><code>orLabelSelectors</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a map of {"{"}key,value{"}"} pairs of backup resources that have <strong>one or more</strong> of the specified labels.</p></dd>
      </dl>
      </li>
      <li>
      Verify that the status of the <code>Backup</code> CR is <code>Completed</code>:
      <CodeBlock code={"$ oc get backups.velero.io -n openshift-adp <backup> -o jsonpath='{.status.phase}'"} lang="terminal" />
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
