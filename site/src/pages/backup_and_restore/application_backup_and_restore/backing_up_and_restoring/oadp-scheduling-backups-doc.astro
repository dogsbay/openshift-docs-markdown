---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-scheduling-backups-doc.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"oadp-scheduling-backups-doc","text":"Scheduling backups using Schedule CR"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-scheduling-backups-doc";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Scheduling backups using Schedule CR";
const description = "Schedule backup operations by creating a Schedule custom resource (CR) with a Cron expression. This helps you to automate recurring backups of your…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["backup_and_restore","backup_and_restore/application_backup_and_restore","backup_and_restore/application_backup_and_restore/backing_up_and_restoring"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Scheduling backups using Schedule CR {#oadp-scheduling-backups-doc}\n\nSchedule backup operations by creating a `Schedule` custom resource (CR) with a Cron expression. This helps you to automate recurring backups of your application data at regular intervals.\n\nYou schedule backups by creating a `Schedule` custom resource (CR) instead of a `Backup` CR.\n\n> [!WARNING]\n> Leave enough time in your backup schedule for a backup to finish before another backup is created.\n>\n> For example, if a backup of a namespace typically takes 10 minutes, do not schedule backups more frequently than every 15 minutes.\n\n**Prerequisites**\n\n- You must install the OpenShift API for Data Protection (OADP) Operator.\n- The `DataProtectionApplication` CR must be in a `Ready` state.\n\n**Procedure**\n\n1. Retrieve the `backupStorageLocations` CRs:\n\n   ```terminal\n   $ oc get backupStorageLocations -n openshift-adp\n   ```\n\n   ```terminal\n   NAMESPACE       NAME              PHASE       LAST VALIDATED   AGE   DEFAULT\n   openshift-adp   velero-sample-1   Available   11s              31m\n   ```\n2. Create a `Schedule` CR, as in the following example:\n\n   ```yaml\n   $ cat << EOF | oc apply -f -\n   apiVersion: velero.io/v1\n   kind: Schedule\n   metadata:\n     name: <schedule>\n     namespace: openshift-adp\n   spec:\n     schedule: 0 7 * * *\n     template:\n       hooks: {}\n       includedNamespaces:\n       - <namespace>\n       storageLocation: <velero-sample-1>\n       defaultVolumesToFsBackup: true\n       ttl: 720h0m0s\n   EOF\n   ```\n\n   where:\n\n   `schedule: 0 7 * * *`\n   :   Specifies the `cron` expression to schedule the backup, for example, `0 7 * * *` to perform a backup every day at 7:00.\n\n   > [!NOTE]\n   > To schedule a backup at specific intervals, enter the `<duration_in_minutes>` in the following format:\n   >\n   > ```terminal\n   >   schedule: \"*/10 * * * *\"\n   > ```\n   >\n   > Enter the minutes value between quotation marks (`\" \"`).\n\n`<namespace>`\n:   Specifies an array of namespaces to back up.\n\n`<velero-sample-1>`\n:   Specifies the name of the `backupStorageLocations` CR.\n\n`defaultVolumesToFsBackup: true`\n:   Optional: In OADP version 1.2 and later, add the `defaultVolumesToFsBackup: true` key-value pair to your configuration when performing backups of volumes with Restic. In OADP version 1.1, add the `defaultVolumesToRestic: true` key-value pair when you back up volumes with Restic.\n\n**Verification**\n\n- Verify that the status of the `Schedule` CR is `Completed` after the scheduled backup runs:\n\n  ```terminal\n  $ oc get schedule -n openshift-adp <schedule> -o jsonpath='{.status.phase}'\n  ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-scheduling-backups-doc.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="oadp-scheduling-backups-doc" class="group scroll-mt-20">Scheduling backups using Schedule CR<a href="#oadp-scheduling-backups-doc" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Schedule backup operations by creating a <code>Schedule</code> custom resource (CR) with a Cron expression. This helps you to automate recurring backups of your application data at regular intervals.</p>
      <p>You schedule backups by creating a <code>Schedule</code> custom resource (CR) instead of a <code>Backup</code> CR.</p>
      <Alert variant="warning" class="my-4">
        <AlertTitle>Warning</AlertTitle>
        <AlertDescription>
          <p>Leave enough time in your backup schedule for a backup to finish before another backup is created.</p>
          <p>For example, if a backup of a namespace typically takes 10 minutes, do not schedule backups more frequently than every 15 minutes.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You must install the OpenShift API for Data Protection (OADP) Operator.</li>
      <li>The <code>DataProtectionApplication</code> CR must be in a <code>Ready</code> state.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Retrieve the <code>backupStorageLocations</code> CRs:
      <CodeBlock code={"$ oc get backupStorageLocations -n openshift-adp"} lang="terminal" />
      <CodeBlock code={"NAMESPACE       NAME              PHASE       LAST VALIDATED   AGE   DEFAULT\nopenshift-adp   velero-sample-1   Available   11s              31m"} lang="terminal" />
      </li>
      <li>
      Create a <code>Schedule</code> CR, as in the following example:
      <CodeBlock code={"$ cat << EOF | oc apply -f -\napiVersion: velero.io/v1\nkind: Schedule\nmetadata:\n  name: <schedule>\n  namespace: openshift-adp\nspec:\n  schedule: 0 7 * * *\n  template:\n    hooks: {}\n    includedNamespaces:\n    - <namespace>\n    storageLocation: <velero-sample-1>\n    defaultVolumesToFsBackup: true\n    ttl: 720h0m0s\nEOF"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>schedule: 0 7 * * *</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the <code>cron</code> expression to schedule the backup, for example, <code>0 7 * * *</code> to perform a backup every day at 7:00.</p></dd>
      </dl>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>To schedule a backup at specific intervals, enter the <code>&lt;duration_in_minutes&gt;</code> in the following format:</p>
          <CodeBlock code={"  schedule: \"*/10 * * * *\""} lang="terminal" />
          <p>Enter the minutes value between quotation marks (<code>" "</code>).</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;namespace&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies an array of namespaces to back up.</p></dd>
      <dt class="font-semibold"><code>&lt;velero-sample-1&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the <code>backupStorageLocations</code> CR.</p></dd>
      <dt class="font-semibold"><code>defaultVolumesToFsBackup: true</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional: In OADP version 1.2 and later, add the <code>defaultVolumesToFsBackup: true</code> key-value pair to your configuration when performing backups of volumes with Restic. In OADP version 1.1, add the <code>defaultVolumesToRestic: true</code> key-value pair when you back up volumes with Restic.</p></dd>
      </dl>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      Verify that the status of the <code>Schedule</code> CR is <code>Completed</code> after the scheduled backup runs:
      <CodeBlock code={"$ oc get schedule -n openshift-adp <schedule> -o jsonpath='{.status.phase}'"} lang="terminal" />
      </li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
