---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-parallel-backup-processing-in-oadp.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"oadp-parallel-backup-processing-in-oadp_{{ context }}","text":"Parallel backup processing in {{ oadp_short }}"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-parallel-backup-processing-in-oadp";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Parallel backup processing in {{ oadp_short }}";
const description = "With parallel backup processing in {{ oadp_full }}, you can back up several applications from different namespaces simultaneously. This concurrent…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["backup_and_restore","backup_and_restore/application_backup_and_restore","backup_and_restore/application_backup_and_restore/backing_up_and_restoring"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Parallel backup processing in {{ oadp_short }} {#oadp-parallel-backup-processing-in-oadp_{{ context }}}\n\nWith parallel backup processing in {{ oadp_full }}, you can back up several applications from different namespaces simultaneously. This concurrent processing ensures that a single large backup task does not delay the backups for other applications in your cluster.\n\nBy default, Velero processes backup resources sequentially - one resource in the `InProgress` phase at a time. Parallel backup changes this behavior to handle backups concurrently.\n\nThe following considerations apply for enabling parallel backups in {{ oadp_short }}:\n\n- Resource allocation: Increasing the concurrency settings requires additional CPU and memory for the Velero container. Monitor resource consumption to ensure cluster stability.\n- Backup scope: Parallel processing provides the most benefit for backups that contain a large number of Kubernetes resources or many smaller volumes. Backups dominated by a few large volumes see less benefit because most time is spent waiting for asynchronous data movement to complete.\n- Namespace partitioning: To keep data integrity, {{ oadp_short }} uses namespace-level isolation when processing parallel backups. Two backups cannot run simultaneously if they share any included namespaces. **Parallel backup execution examples**\n\n  | Scenario | Included namespaces | Processing behavior |\n  | --- | --- | --- |\n  | Backup A and Backup B | `namespace1` and `namespace2` | Parallel: The namespaces are isolated. |\n  | Backup A and Backup B | `namespace1` and `namespace1` | Serialized: The shared namespace creates a lock. |\n  | Full cluster and Backup A | All namespaces and `namespace1` | Serialized: A cluster-wide backup includes all namespaces and locks the entire cluster. |\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-parallel-backup-processing-in-oadp.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={true}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <p class="/_attributes/common-attributes.md"></p>
      <h1 id="oadp-parallel-backup-processing-in-oadp_{{ context }}" class="group scroll-mt-20">Parallel backup processing in {"{"}{"{"} oadp_short {"}"}{"}"}<a href="#oadp-parallel-backup-processing-in-oadp_{{ context }}" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>With parallel backup processing in {"{"}{"{"} oadp_full {"}"}{"}"}, you can back up several applications from different namespaces simultaneously. This concurrent processing ensures that a single large backup task does not delay the backups for other applications in your cluster.</p>
      <p>By default, Velero processes backup resources sequentially - one resource in the <code>InProgress</code> phase at a time. Parallel backup changes this behavior to handle backups concurrently.</p>
      <p>The following considerations apply for enabling parallel backups in {"{"}{"{"} oadp_short {"}"}{"}"}:</p>
      <ul>
      <li>Resource allocation: Increasing the concurrency settings requires additional CPU and memory for the Velero container. Monitor resource consumption to ensure cluster stability.</li>
      <li>Backup scope: Parallel processing provides the most benefit for backups that contain a large number of Kubernetes resources or many smaller volumes. Backups dominated by a few large volumes see less benefit because most time is spent waiting for asynchronous data movement to complete.</li>
      <li>
      Namespace partitioning: To keep data integrity, {"{"}{"{"} oadp_short {"}"}{"}"} uses namespace-level isolation when processing parallel backups. Two backups cannot run simultaneously if they share any included namespaces. <strong>Parallel backup execution examples</strong>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Scenario</TableHead>
      <TableHead>Included namespaces</TableHead>
      <TableHead>Processing behavior</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>Backup A and Backup B</TableCell>
      <TableCell><code>namespace1</code> and <code>namespace2</code></TableCell>
      <TableCell>Parallel: The namespaces are isolated.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Backup A and Backup B</TableCell>
      <TableCell><code>namespace1</code> and <code>namespace1</code></TableCell>
      <TableCell>Serialized: The shared namespace creates a lock.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Full cluster and Backup A</TableCell>
      <TableCell>All namespaces and <code>namespace1</code></TableCell>
      <TableCell>Serialized: A cluster-wide backup includes all namespaces and locks the entire cluster.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      </li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
