---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: backup_and_restore/application_backup_and_restore/backing_up_and_restoring/backing-up-applications.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"backing-up-applications","text":"Backing up applications"},{"depth":2,"slug":"oadp-review-backup-restore_backing-up-applications","text":"Previewing resources before running backup and restore"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/backing-up-applications";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Backing up applications";
const description = "Back up applications by creating a Backup custom resource (CR) using snapshots, CSI, or File System Backup with Kopia or Restic." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["backup_and_restore","backup_and_restore/application_backup_and_restore","backup_and_restore/application_backup_and_restore/backing_up_and_restoring"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Backing up applications {#backing-up-applications}\n\nBack up applications by creating a `Backup` custom resource (CR) using snapshots, CSI, or File System Backup with Kopia or Restic.\n\nFrequent backups might consume storage on the backup storage location. Check the frequency of backups, retention time, and the amount of data of the persistent volumes (PVs) if using non-local backups, for example, S3 buckets. Because all backups are retained until they expire, check the time to live (TTL) setting of the schedule.\n\nReview the following information regarding backing up applications by using OADP:\n\n- The `Backup` CR creates backup files for Kubernetes resources and internal images on S3 object storage.\n- If you use Velero’s snapshot feature to back up data stored on the persistent volume, only snapshot related information is stored in the S3 bucket along with the OpenShift object data.\n- If your cloud provider has a native snapshot API or supports CSI snapshots, the `Backup` CR backs up persistent volumes (PVs) by creating snapshots. For more information about working with CSI snapshots, see *Backing up persistent volumes with CSI snapshots*. For more information about CSI volume snapshots, see *CSI volume snapshots*.\n- If the underlying storage or the backup bucket are part of the same cluster, then the data might be lost in case of disaster. Ensure you configure your backup storage off-cluster.\n- If your cloud provider does not support snapshots or if your applications are on NFS data volumes, you can create backups by using Kopia or Restic. See *Backing up applications with File System Backup: Kopia or Restic*.\n- You can create backup hooks to run commands before or after the backup operation. See *Creating backup hooks*.\n- You can schedule backups by creating a `Schedule` CR instead of a `Backup` CR. See *Scheduling backups using Schedule CR*.\n- OpenShift Container Platform 4.22 enforces a pod security admission (PSA) policy that can hinder the readiness of pods during a Restic restore process.\n\n  This issue has been resolved in the OADP 1.1.6 and OADP 1.2.2 releases, therefore it is recommended that users upgrade to these releases.\n\n  For more information, see *Restic restore partially failing on Red Hat OpenShift Container Platform 4.15 due to changed PSA policy*.\n\n  > [!IMPORTANT]\n  > The OpenShift API for Data Protection (OADP) does not support backing up volume snapshots that were created by other software.\n\n  > [!IMPORTANT]\n  > The `.../.snapshot` directory is a snapshot copy directory, which is used by several NFS servers. This directory has read-only access by default, so Velero cannot restore to this directory.\n  >\n  > Do not give Velero write access to the `.snapshot` directory, and disable client access to this directory.\n\n## Previewing resources before running backup and restore {#oadp-review-backup-restore_backing-up-applications}\n\nPreview the backup and restore resources in advance by doing a dry run of the backup and restore operations. This helps you to verify which resources will be included before committing to a full backup or restore.\n\nOADP backs up application resources based on the type, namespace, or label. This means that you can view the resources after the backup is complete. Similarly, you can view the restored objects based on the namespace, persistent volume (PV), or label after a restore operation is complete.\n\n**Prerequisites**\n\n- You have installed the OADP Operator.\n\n**Procedure**\n\n1. To preview the resources included in the backup before running the actual backup, run the following command:\n\n   ```terminal\n   $ velero backup create <backup-name> --snapshot-volumes false\n   ```\n\n   Specify the value of `--snapshot-volumes` parameter as `false`.\n2. To know more details about the backup resources, run the following command:\n\n   ```terminal\n   $ velero describe backup <backup_name> --details\n   ```\n\n   Replace `<backup_name>` with the name of the backup.\n3. To preview the resources included in the restore before running the actual restore, run the following command:\n\n   ```terminal\n   $ velero restore create --from-backup <backup_name>\n   ```\n\n   Replace `<backup_name>` with the name of the backup.\n\n   > [!IMPORTANT]\n   > The `velero restore create` command creates restore resources in the cluster. You must delete the resources created as part of the restore, after you review the resources.\n4. To know more details about the restore resources, run the following command:\n\n   ```terminal\n   $ velero describe restore <restore_name> --details\n   ```\n\n   Replace `<restore_name>` with the name of the restore.\n\n**Additional resources**\n{._additional-resources}\n\n- [Creating a Backup CR](/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-creating-backup-cr#oadp-creating-backup-cr-doc)\n- [Creating backup hooks](/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-creating-backup-hooks-doc#oadp-creating-backup-hooks-doc)\n- [Scheduling backups using Schedule CR](/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-scheduling-backups-doc#oadp-scheduling-backups-doc)\n- [Installing Operators on clusters for administrators](/openshift-docs-markdown/operators/admin/olm-adding-operators-to-cluster#olm-installing-operators-from-software-catalog_olm-adding-operators-to-a-cluster)\n- [Backing up persistent volumes with CSI snapshots](/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-backing-up-pvs-csi-doc#oadp-backing-up-pvs-csi-doc)\n- [CSI volume snapshots](/openshift-docs-markdown/storage/container_storage_interface/persistent-storage-csi-snapshots#persistent-storage-csi-snapshots)\n- [Backing up applications with File System Backup: Kopia or Restic](/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-backing-up-applications-restic-doc#oadp-backing-up-applications-restic-doc)\n- [Installing Operators in namespaces for non-administrators](/openshift-docs-markdown/operators/user/olm-installing-operators-in-namespace#olm-installing-operators-in-namespace)\n- [Restic restore partially failing on OCP 4.15 due to changed PSA policy](/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/troubleshooting/restic-issues#oadp-restic-restore-failing-psa-policy_restic-issues)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/backing-up-applications.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="backing-up-applications" class="group scroll-mt-20">Backing up applications<a href="#backing-up-applications" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Back up applications by creating a <code>Backup</code> custom resource (CR) using snapshots, CSI, or File System Backup with Kopia or Restic.</p>
      <p>Frequent backups might consume storage on the backup storage location. Check the frequency of backups, retention time, and the amount of data of the persistent volumes (PVs) if using non-local backups, for example, S3 buckets. Because all backups are retained until they expire, check the time to live (TTL) setting of the schedule.</p>
      <p>Review the following information regarding backing up applications by using OADP:</p>
      <ul>
      <li>The <code>Backup</code> CR creates backup files for Kubernetes resources and internal images on S3 object storage.</li>
      <li>If you use Velero’s snapshot feature to back up data stored on the persistent volume, only snapshot related information is stored in the S3 bucket along with the OpenShift object data.</li>
      <li>If your cloud provider has a native snapshot API or supports CSI snapshots, the <code>Backup</code> CR backs up persistent volumes (PVs) by creating snapshots. For more information about working with CSI snapshots, see <em>Backing up persistent volumes with CSI snapshots</em>. For more information about CSI volume snapshots, see <em>CSI volume snapshots</em>.</li>
      <li>If the underlying storage or the backup bucket are part of the same cluster, then the data might be lost in case of disaster. Ensure you configure your backup storage off-cluster.</li>
      <li>If your cloud provider does not support snapshots or if your applications are on NFS data volumes, you can create backups by using Kopia or Restic. See <em>Backing up applications with File System Backup: Kopia or Restic</em>.</li>
      <li>You can create backup hooks to run commands before or after the backup operation. See <em>Creating backup hooks</em>.</li>
      <li>You can schedule backups by creating a <code>Schedule</code> CR instead of a <code>Backup</code> CR. See <em>Scheduling backups using Schedule CR</em>.</li>
      <li>
      OpenShift Container Platform 4.22 enforces a pod security admission (PSA) policy that can hinder the readiness of pods during a Restic restore process.
      <p>This issue has been resolved in the OADP 1.1.6 and OADP 1.2.2 releases, therefore it is recommended that users upgrade to these releases.</p>
      <p>For more information, see <em>Restic restore partially failing on Red Hat OpenShift Container Platform 4.15 due to changed PSA policy</em>.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>The OpenShift API for Data Protection (OADP) does not support backing up volume snapshots that were created by other software.</p>
        </AlertDescription>
      </Alert>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>The <code>.../.snapshot</code> directory is a snapshot copy directory, which is used by several NFS servers. This directory has read-only access by default, so Velero cannot restore to this directory.</p>
          <p>Do not give Velero write access to the <code>.snapshot</code> directory, and disable client access to this directory.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      <h2 id="oadp-review-backup-restore_backing-up-applications" class="group scroll-mt-20">Previewing resources before running backup and restore<a href="#oadp-review-backup-restore_backing-up-applications" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Preview the backup and restore resources in advance by doing a dry run of the backup and restore operations. This helps you to verify which resources will be included before committing to a full backup or restore.</p>
      <p>OADP backs up application resources based on the type, namespace, or label. This means that you can view the resources after the backup is complete. Similarly, you can view the restored objects based on the namespace, persistent volume (PV), or label after a restore operation is complete.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have installed the OADP Operator.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      To preview the resources included in the backup before running the actual backup, run the following command:
      <CodeBlock code={"$ velero backup create <backup-name> --snapshot-volumes false"} lang="terminal" />
      <p>Specify the value of <code>--snapshot-volumes</code> parameter as <code>false</code>.</p>
      </li>
      <li>
      To know more details about the backup resources, run the following command:
      <CodeBlock code={"$ velero describe backup <backup_name> --details"} lang="terminal" />
      <p>Replace <code>&lt;backup_name&gt;</code> with the name of the backup.</p>
      </li>
      <li>
      To preview the resources included in the restore before running the actual restore, run the following command:
      <CodeBlock code={"$ velero restore create --from-backup <backup_name>"} lang="terminal" />
      <p>Replace <code>&lt;backup_name&gt;</code> with the name of the backup.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>The <code>velero restore create</code> command creates restore resources in the cluster. You must delete the resources created as part of the restore, after you review the resources.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      To know more details about the restore resources, run the following command:
      <CodeBlock code={"$ velero describe restore <restore_name> --details"} lang="terminal" />
      <p>Replace <code>&lt;restore_name&gt;</code> with the name of the restore.</p>
      </li>
      </ol>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-creating-backup-cr#oadp-creating-backup-cr-doc">Creating a Backup CR</a></li>
      <li><a href="/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-creating-backup-hooks-doc#oadp-creating-backup-hooks-doc">Creating backup hooks</a></li>
      <li><a href="/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-scheduling-backups-doc#oadp-scheduling-backups-doc">Scheduling backups using Schedule CR</a></li>
      <li><a href="/openshift-docs-markdown/operators/admin/olm-adding-operators-to-cluster#olm-installing-operators-from-software-catalog_olm-adding-operators-to-a-cluster">Installing Operators on clusters for administrators</a></li>
      <li><a href="/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-backing-up-pvs-csi-doc#oadp-backing-up-pvs-csi-doc">Backing up persistent volumes with CSI snapshots</a></li>
      <li><a href="/openshift-docs-markdown/storage/container_storage_interface/persistent-storage-csi-snapshots#persistent-storage-csi-snapshots">CSI volume snapshots</a></li>
      <li><a href="/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/backing_up_and_restoring/oadp-backing-up-applications-restic-doc#oadp-backing-up-applications-restic-doc">Backing up applications with File System Backup: Kopia or Restic</a></li>
      <li><a href="/openshift-docs-markdown/operators/user/olm-installing-operators-in-namespace#olm-installing-operators-in-namespace">Installing Operators in namespaces for non-administrators</a></li>
      <li><a href="/openshift-docs-markdown/backup_and_restore/application_backup_and_restore/troubleshooting/restic-issues#oadp-restic-restore-failing-psa-policy_restic-issues">Restic restore partially failing on OCP 4.15 due to changed PSA policy</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
