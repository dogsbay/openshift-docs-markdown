---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/compliance_operator/co-management/compliance-operator-uninstallation.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"compliance-operator-uninstallation","text":"Uninstalling the Compliance Operator"},{"depth":2,"slug":"compliance-operator-uninstall_compliance-operator-uninstallation","text":"Uninstalling the OpenShift Compliance Operator from OpenShift Container Platform using the web console"},{"depth":2,"slug":"compliance-operator-uninstall-cli_compliance-operator-uninstallation","text":"Uninstalling the OpenShift Compliance Operator from OpenShift Container Platform using the CLI"},{"depth":2,"slug":"additional-resources_compliance-operator-uninstallation","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/compliance_operator/co-management/compliance-operator-uninstallation";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Uninstalling the Compliance Operator";
const description = "You can remove the OpenShift Compliance Operator from your cluster by using the OpenShift Container Platform web console or the OpenShift CLI (oc)." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/compliance_operator","security/compliance_operator/co-management"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Uninstalling the Compliance Operator {#compliance-operator-uninstallation}\n\nYou can remove the OpenShift Compliance Operator from your cluster by using the OpenShift Container Platform web console or the OpenShift CLI (`oc`).\n\n## Uninstalling the OpenShift Compliance Operator from OpenShift Container Platform using the web console {#compliance-operator-uninstall_compliance-operator-uninstallation}\n\nTo remove the Compliance Operator, you must first delete the objects in the namespace. After the objects are removed, you can remove the Operator and its namespace by deleting the **openshift-compliance** project.\n\n**Prerequisites**\n\n- Access to an OpenShift Container Platform cluster by using an account with `cluster-admin` permissions.\n- The OpenShift Compliance Operator is installed.\n\n**Procedure**\n\n1. Go to the **Ecosystem** -> **Installed Operators** -> **Compliance Operator** page.\n\n   1. Click **All instances**.\n   2. In **All namespaces**, click the Options menu ![](kebab.png \"Options menu\") and delete all ScanSettingBinding, ComplainceSuite, ComplianceScan, and ProfileBundle objects.\n2. Switch to the **Administration** -> **Ecosystem** -> **Installed Operators** page.\n3. Click the Options menu ![](kebab.png \"Options menu\") on the **Compliance Operator** entry and select **Uninstall Operator**.\n4. Switch to the **Home** -> **Projects** page.\n5. Search for 'compliance'.\n6. Click the Options menu ![](kebab.png \"Options menu\") next to the **openshift-compliance** project, and select **Delete Project**.\n\n   1. Confirm the deletion by typing `openshift-compliance` in the dialog box, and click **Delete**.\n\n## Uninstalling the OpenShift Compliance Operator from OpenShift Container Platform using the CLI {#compliance-operator-uninstall-cli_compliance-operator-uninstallation}\n\nTo remove the Compliance Operator, you must first delete the objects in the namespace. After the objects are removed, you can remove the Operator and its namespace by deleting the **openshift-compliance** project.\n\n**Prerequisites**\n\n- Access to an OpenShift Container Platform cluster by using an account with `cluster-admin` permissions.\n- The OpenShift Compliance Operator is installed.\n\n**Procedure**\n\n1. Delete all objects in the namespace.\n\n   1. Delete the `ScanSettingBinding` objects:\n\n      ```terminal\n      $ oc delete ssb --all -n openshift-compliance\n      ```\n   2. Delete the `ScanSetting` objects:\n\n      ```terminal\n      $ oc delete ss --all -n openshift-compliance\n      ```\n   3. Delete the `ComplianceSuite` objects:\n\n      ```terminal\n      $ oc delete suite --all -n openshift-compliance\n      ```\n   4. Delete the `ComplianceScan` objects:\n\n      ```terminal\n      $ oc delete scan --all -n openshift-compliance\n      ```\n   5. Delete the `ProfileBundle` objects:\n\n      ```terminal\n      $ oc delete profilebundle.compliance --all -n openshift-compliance\n      ```\n2. Delete the Subscription object:\n\n   ```terminal\n   $ oc delete sub --all -n openshift-compliance\n   ```\n3. Delete the CSV object:\n\n   ```terminal\n   $ oc delete csv --all -n openshift-compliance\n   ```\n4. Delete the project:\n\n   ```terminal\n   $ oc delete project openshift-compliance\n   ```\n\n   ```terminal {title=\"Example output\"}\n   project.project.openshift.io \"openshift-compliance\" deleted\n   ```\n\n**Verification**\n\n1. Confirm the namespace is deleted:\n\n   ```terminal\n   $ oc get project/openshift-compliance\n   ```\n\n   ```terminal {title=\"Example output\"}\n   Error from server (NotFound): namespaces \"openshift-compliance\" not found\n   ```\n\n## Additional resources {#additional-resources_compliance-operator-uninstallation}\n\n- [Installing the Compliance Operator](/openshift-docs-markdown/security/compliance_operator/compliance-operator-installation#compliance-operator-installation)\n- [Managing the Compliance Operator](/openshift-docs-markdown/security/compliance_operator/compliance-operator-manage#compliance-operator-understanding)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/compliance_operator/co-management/compliance-operator-uninstallation.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="compliance-operator-uninstallation" class="group scroll-mt-20">Uninstalling the Compliance Operator<a href="#compliance-operator-uninstallation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can remove the OpenShift Compliance Operator from your cluster by using the OpenShift Container Platform web console or the OpenShift CLI (<code>oc</code>).</p>
      <h2 id="compliance-operator-uninstall_compliance-operator-uninstallation" class="group scroll-mt-20">Uninstalling the OpenShift Compliance Operator from OpenShift Container Platform using the web console<a href="#compliance-operator-uninstall_compliance-operator-uninstallation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To remove the Compliance Operator, you must first delete the objects in the namespace. After the objects are removed, you can remove the Operator and its namespace by deleting the <strong>openshift-compliance</strong> project.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>Access to an OpenShift Container Platform cluster by using an account with <code>cluster-admin</code> permissions.</li>
      <li>The OpenShift Compliance Operator is installed.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Go to the <strong>Ecosystem</strong> -&gt; <strong>Installed Operators</strong> -&gt; <strong>Compliance Operator</strong> page.
      <ol>
      <li>Click <strong>All instances</strong>.</li>
      <li>In <strong>All namespaces</strong>, click the Options menu <img src="kebab.png" alt="" loading="lazy" /> and delete all ScanSettingBinding, ComplainceSuite, ComplianceScan, and ProfileBundle objects.</li>
      </ol>
      </li>
      <li>Switch to the <strong>Administration</strong> -&gt; <strong>Ecosystem</strong> -&gt; <strong>Installed Operators</strong> page.</li>
      <li>Click the Options menu <img src="kebab.png" alt="" loading="lazy" /> on the <strong>Compliance Operator</strong> entry and select <strong>Uninstall Operator</strong>.</li>
      <li>Switch to the <strong>Home</strong> -&gt; <strong>Projects</strong> page.</li>
      <li>Search for 'compliance'.</li>
      <li>
      Click the Options menu <img src="kebab.png" alt="" loading="lazy" /> next to the <strong>openshift-compliance</strong> project, and select <strong>Delete Project</strong>.
      <ol><li>Confirm the deletion by typing <code>openshift-compliance</code> in the dialog box, and click <strong>Delete</strong>.</li></ol>
      </li>
      </ol>
      <h2 id="compliance-operator-uninstall-cli_compliance-operator-uninstallation" class="group scroll-mt-20">Uninstalling the OpenShift Compliance Operator from OpenShift Container Platform using the CLI<a href="#compliance-operator-uninstall-cli_compliance-operator-uninstallation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To remove the Compliance Operator, you must first delete the objects in the namespace. After the objects are removed, you can remove the Operator and its namespace by deleting the <strong>openshift-compliance</strong> project.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>Access to an OpenShift Container Platform cluster by using an account with <code>cluster-admin</code> permissions.</li>
      <li>The OpenShift Compliance Operator is installed.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Delete all objects in the namespace.
      <ol>
      <li>
      Delete the <code>ScanSettingBinding</code> objects:
      <CodeBlock code={"$ oc delete ssb --all -n openshift-compliance"} lang="terminal" />
      </li>
      <li>
      Delete the <code>ScanSetting</code> objects:
      <CodeBlock code={"$ oc delete ss --all -n openshift-compliance"} lang="terminal" />
      </li>
      <li>
      Delete the <code>ComplianceSuite</code> objects:
      <CodeBlock code={"$ oc delete suite --all -n openshift-compliance"} lang="terminal" />
      </li>
      <li>
      Delete the <code>ComplianceScan</code> objects:
      <CodeBlock code={"$ oc delete scan --all -n openshift-compliance"} lang="terminal" />
      </li>
      <li>
      Delete the <code>ProfileBundle</code> objects:
      <CodeBlock code={"$ oc delete profilebundle.compliance --all -n openshift-compliance"} lang="terminal" />
      </li>
      </ol>
      </li>
      <li>
      Delete the Subscription object:
      <CodeBlock code={"$ oc delete sub --all -n openshift-compliance"} lang="terminal" />
      </li>
      <li>
      Delete the CSV object:
      <CodeBlock code={"$ oc delete csv --all -n openshift-compliance"} lang="terminal" />
      </li>
      <li>
      Delete the project:
      <CodeBlock code={"$ oc delete project openshift-compliance"} lang="terminal" />
      <CodeBlock code={"project.project.openshift.io \"openshift-compliance\" deleted"} lang="terminal" title="Example output" />
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>
      Confirm the namespace is deleted:
      <CodeBlock code={"$ oc get project/openshift-compliance"} lang="terminal" />
      <CodeBlock code={"Error from server (NotFound): namespaces \"openshift-compliance\" not found"} lang="terminal" title="Example output" />
      </li>
      </ol>
      <h2 id="additional-resources_compliance-operator-uninstallation" class="group scroll-mt-20">Additional resources<a href="#additional-resources_compliance-operator-uninstallation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/security/compliance_operator/compliance-operator-installation#compliance-operator-installation">Installing the Compliance Operator</a></li>
      <li><a href="/openshift-docs-markdown/security/compliance_operator/compliance-operator-manage#compliance-operator-understanding">Managing the Compliance Operator</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
