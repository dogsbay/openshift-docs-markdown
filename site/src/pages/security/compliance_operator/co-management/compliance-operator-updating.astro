---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/compliance_operator/co-management/compliance-operator-updating.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";

const headings = [{"depth":1,"slug":"compliance-operator-updating","text":"Updating the Compliance Operator"},{"depth":2,"slug":"olm-preparing-upgrade_compliance-operator-updating","text":"About preparing for an Operator update"},{"depth":2,"slug":"olm-changing-update-channel_compliance-operator-updating","text":"Changing the update channel for an Operator"},{"depth":2,"slug":"olm-approving-pending-upgrade_compliance-operator-updating","text":"Approving a pending Operator update manually"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/compliance_operator/co-management/compliance-operator-updating";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Updating the Compliance Operator";
const description = "As a cluster administrator, you can update the Compliance Operator on your OpenShift Container Platform cluster." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/compliance_operator","security/compliance_operator/co-management"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Updating the Compliance Operator {#compliance-operator-updating}\n\nAs a cluster administrator, you can update the Compliance Operator on your OpenShift Container Platform cluster.\n\n> [!IMPORTANT]\n> Updating your OpenShift Container Platform cluster to version 4.14 might cause the Compliance Operator to not work as expected. This is due to an ongoing known issue. For more information, see [OCPBUGS-18025](https://issues.redhat.com/browse/OCPBUGS-18025).\n\n## About preparing for an Operator update {#olm-preparing-upgrade_compliance-operator-updating}\n\nYou can change the update channel to start tracking and receiving updates from a newer channel to access new features and bug fixes. The subscription of an installed Operator specifies an update channel that tracks and receives updates for the Operator.\n\nThe names of update channels in a subscription can differ between Operators, but the naming scheme typically follows a common convention within a given Operator. For example, channel names might follow a minor release update stream for the application provided by the Operator (`1.2`, `1.3`) or a release frequency (`stable`, `fast`).\n\n> [!NOTE]\n> You cannot change installed Operators to a channel that is older than the current channel.\n\nRed Hat Customer Portal Labs include an application that helps administrators prepare to update their Operators.\n\nYou can use these tools to search for Operators and verify the available Operator versions per update channel across different releases of OpenShift Container Platform. Operators managed by Cluster Version Operator (CVO) are not included.\n\n## Changing the update channel for an Operator {#olm-changing-update-channel_compliance-operator-updating}\n\nTo change the update channel for an installed Operator, you can use the OpenShift Container Platform web console. The update channel determines which Operator versions your subscription tracks and receives.\n\n> [!TIP]\n> If the approval strategy in the subscription is set to **Automatic**, the update process initiates as soon as a new Operator version is available in the selected channel. If the approval strategy is set to **Manual**, you must manually approve pending updates.\n\n**Prerequisites**\n\n- An Operator previously installed using Operator Lifecycle Manager (OLM).\n\n**Procedure**\n\n1. In the web console, navigate to **Ecosystem** → **Installed Operators**.\n2. Click the name of the Operator you want to change the update channel for.\n3. Click the **Subscription** tab.\n4. Click the name of the update channel under **Update channel**.\n5. Click the newer update channel that you want to change to, then click **Save**.\n6. For subscriptions with an **Automatic** approval strategy, the update begins automatically. Navigate back to the **Ecosystem** → **Installed Operators** page to monitor the progress of the update. When complete, the status changes to **Succeeded** and **Up to date**.\n\n   For subscriptions with a **Manual** approval strategy, you can manually approve the update from the **Subscription** tab.\n\n## Approving a pending Operator update manually {#olm-approving-pending-upgrade_compliance-operator-updating}\n\nIf an installed Operator has the approval strategy in its subscription set to **Manual**, you must manually approve the update before installation can begin. Manual approval reviews the changes and control when updates are applied to prevent unexpected downtime.\n\n**Prerequisites**\n\n- An Operator previously installed using Operator Lifecycle Manager (OLM).\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, navigate to **Ecosystem** → **Installed Operators**.\n2. Operators that have a pending update display a status with **Upgrade available**. Click the name of the Operator you want to update.\n3. Click the **Subscription** tab. Any updates requiring approval are displayed next to **Upgrade status**. For example, it might display **1 requires approval**.\n4. Click **1 requires approval**, then click **Preview Install Plan**.\n5. Review the resources that are listed as available for update. When satisfied, click **Approve**.\n6. Navigate back to the **Ecosystem** → **Installed Operators** page to monitor the progress of the update. When complete, the status changes to **Succeeded** and **Up to date**.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/compliance_operator/co-management/compliance-operator-updating.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="compliance-operator-updating" class="group scroll-mt-20">Updating the Compliance Operator<a href="#compliance-operator-updating" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">As a cluster administrator, you can update the Compliance Operator on your OpenShift Container Platform cluster.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Updating your OpenShift Container Platform cluster to version 4.14 might cause the Compliance Operator to not work as expected. This is due to an ongoing known issue. For more information, see <a href="https://issues.redhat.com/browse/OCPBUGS-18025">OCPBUGS-18025</a>.</p>
        </AlertDescription>
      </Alert>
      <h2 id="olm-preparing-upgrade_compliance-operator-updating" class="group scroll-mt-20">About preparing for an Operator update<a href="#olm-preparing-upgrade_compliance-operator-updating" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can change the update channel to start tracking and receiving updates from a newer channel to access new features and bug fixes. The subscription of an installed Operator specifies an update channel that tracks and receives updates for the Operator.</p>
      <p>The names of update channels in a subscription can differ between Operators, but the naming scheme typically follows a common convention within a given Operator. For example, channel names might follow a minor release update stream for the application provided by the Operator (<code>1.2</code>, <code>1.3</code>) or a release frequency (<code>stable</code>, <code>fast</code>).</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>You cannot change installed Operators to a channel that is older than the current channel.</p>
        </AlertDescription>
      </Alert>
      <p>Red Hat Customer Portal Labs include an application that helps administrators prepare to update their Operators.</p>
      <p>You can use these tools to search for Operators and verify the available Operator versions per update channel across different releases of OpenShift Container Platform. Operators managed by Cluster Version Operator (CVO) are not included.</p>
      <h2 id="olm-changing-update-channel_compliance-operator-updating" class="group scroll-mt-20">Changing the update channel for an Operator<a href="#olm-changing-update-channel_compliance-operator-updating" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To change the update channel for an installed Operator, you can use the OpenShift Container Platform web console. The update channel determines which Operator versions your subscription tracks and receives.</p>
      <Alert variant="tip" class="my-4">
        <AlertTitle>Tip</AlertTitle>
        <AlertDescription>
          <p>If the approval strategy in the subscription is set to <strong>Automatic</strong>, the update process initiates as soon as a new Operator version is available in the selected channel. If the approval strategy is set to <strong>Manual</strong>, you must manually approve pending updates.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul><li>An Operator previously installed using Operator Lifecycle Manager (OLM).</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the web console, navigate to <strong>Ecosystem</strong> → <strong>Installed Operators</strong>.</li>
      <li>Click the name of the Operator you want to change the update channel for.</li>
      <li>Click the <strong>Subscription</strong> tab.</li>
      <li>Click the name of the update channel under <strong>Update channel</strong>.</li>
      <li>Click the newer update channel that you want to change to, then click <strong>Save</strong>.</li>
      <li>
      For subscriptions with an <strong>Automatic</strong> approval strategy, the update begins automatically. Navigate back to the <strong>Ecosystem</strong> → <strong>Installed Operators</strong> page to monitor the progress of the update. When complete, the status changes to <strong>Succeeded</strong> and <strong>Up to date</strong>.
      <p>For subscriptions with a <strong>Manual</strong> approval strategy, you can manually approve the update from the <strong>Subscription</strong> tab.</p>
      </li>
      </ol>
      <h2 id="olm-approving-pending-upgrade_compliance-operator-updating" class="group scroll-mt-20">Approving a pending Operator update manually<a href="#olm-approving-pending-upgrade_compliance-operator-updating" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">If an installed Operator has the approval strategy in its subscription set to <strong>Manual</strong>, you must manually approve the update before installation can begin. Manual approval reviews the changes and control when updates are applied to prevent unexpected downtime.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>An Operator previously installed using Operator Lifecycle Manager (OLM).</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, navigate to <strong>Ecosystem</strong> → <strong>Installed Operators</strong>.</li>
      <li>Operators that have a pending update display a status with <strong>Upgrade available</strong>. Click the name of the Operator you want to update.</li>
      <li>Click the <strong>Subscription</strong> tab. Any updates requiring approval are displayed next to <strong>Upgrade status</strong>. For example, it might display <strong>1 requires approval</strong>.</li>
      <li>Click <strong>1 requires approval</strong>, then click <strong>Preview Install Plan</strong>.</li>
      <li>Review the resources that are listed as available for update. When satisfied, click <strong>Approve</strong>.</li>
      <li>Navigate back to the <strong>Ecosystem</strong> → <strong>Installed Operators</strong> page to monitor the progress of the update. When complete, the status changes to <strong>Succeeded</strong> and <strong>Up to date</strong>.</li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
