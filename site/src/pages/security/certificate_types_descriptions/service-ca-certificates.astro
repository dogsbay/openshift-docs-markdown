---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/certificate_types_descriptions/service-ca-certificates.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";

const headings = [{"depth":1,"slug":"cert-types-service-ca-certificates","text":"Service CA certificates"},{"depth":2,"slug":"cert-types-service-ca-certificates-purpose_cert-types-service-ca-certificates","text":"Purpose"},{"depth":2,"slug":"cert-types-service-ca-certificates-expiration_cert-types-service-ca-certificates","text":"Expiration"},{"depth":2,"slug":"cert-types-service-ca-certificates-management_cert-types-service-ca-certificates","text":"Management"},{"depth":2,"slug":"cert-types-service-ca-certificates-services_cert-types-service-ca-certificates","text":"Services"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/certificate_types_descriptions/service-ca-certificates";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Service CA certificates";
const description = "Review service certificate authority (CA) certificate rotation, expiration, and Operator-managed signing in OpenShift Container Platform to plan…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/certificate_types_descriptions"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Service CA certificates {#cert-types-service-ca-certificates}\n\nReview service certificate authority (CA) certificate rotation, expiration, and Operator-managed signing in OpenShift Container Platform to plan maintenance for internal service serving certificates.\n\n## Purpose {#cert-types-service-ca-certificates-purpose_cert-types-service-ca-certificates}\n\n`service-ca` is an Operator that creates a self-signed CA when an OpenShift Container Platform cluster is deployed.\n\n## Expiration {#cert-types-service-ca-certificates-expiration_cert-types-service-ca-certificates}\n\nA custom expiration term is not supported. The self-signed CA is stored in the `service-ca/signing-key` secret. The certificate is in `tls.crt`, the private key is in `tls.key`, and the CA bundle is in `ca-bundle.crt`.\n\nOther services can request a service serving certificate by annotating a service resource with `service.beta.openshift.io/serving-cert-secret-name: <secret name>`. In response, the Operator generates a new certificate as `tls.crt`, and a private key as `tls.key`, in the named secret. The certificate is valid for two years.\n\nOther services can request that the CA bundle for the service CA be injected into API service or config map resources by annotating with `service.beta.openshift.io/inject-cabundle: true` to support validating certificates generated from the service CA. In response, the Operator writes the current CA bundle to the `CABundle` field of an API service or as `service-ca.crt` to a config map.\n\nAs of OpenShift Container Platform 4.3.5, automated rotation is supported and is backported to some 4.2.z and 4.3.z releases. For any release supporting automated rotation, the service CA is valid for 26 months and is automatically refreshed when there is less than 13 months validity left. If necessary, you can manually refresh the service CA.\n\nThe 26-month service CA validity period is longer than the expected upgrade interval for a supported OpenShift Container Platform cluster. Non-control plane consumers of service CA certificates are refreshed after CA rotation and before the expiration of the pre-rotation CA.\n\n> [!WARNING]\n> A manually-rotated service CA does not maintain trust with the previous service CA. You might experience a temporary service disruption until the pods in the cluster are restarted, which ensures that pods are using service serving certificates issued by the new service CA.\n\n> [!IMPORTANT]\n> Applications using the `service-ca` certificate must be capable of dynamically reloading CA certificates. When automated rotation occurs, pods that cannot reload CA certificates dynamically might require a restart to rebuild certificate trust.\n\n## Management {#cert-types-service-ca-certificates-management_cert-types-service-ca-certificates}\n\nThese certificates are managed by the system and not the user.\n\n## Services {#cert-types-service-ca-certificates-services_cert-types-service-ca-certificates}\n\nServices that use service CA certificates include:\n\n- `cluster-autoscaler-operator`\n- `cluster-monitoring-operator`\n- `cluster-authentication-operator`\n- `cluster-image-registry-operator`\n- `cluster-ingress-operator`\n- `cluster-kube-apiserver-operator`\n- `cluster-kube-controller-manager-operator`\n- `cluster-kube-scheduler-operator`\n- `cluster-networking-operator`\n- `cluster-openshift-apiserver-operator`\n- `cluster-openshift-controller-manager-operator`\n- `cluster-samples-operator`\n- `cluster-storage-operator`\n- `machine-config-operator`\n- `console-operator`\n- `insights-operator`\n- `machine-api-operator`\n- `operator-lifecycle-manager`\n- CSI driver operators\n\nThis is not a comprehensive list.\n\n**Additional resources**\n{._additional-resources}\n\n- [Manually rotate the service CA certificate](/openshift-docs-markdown/security/certificates/service-serving-certificate#manually-rotate-service-ca_service-serving-certificate)\n- [Securing service traffic using service serving certificate secrets](/openshift-docs-markdown/security/certificates/service-serving-certificate#add-service-serving)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/certificate_types_descriptions/service-ca-certificates.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="cert-types-service-ca-certificates" class="group scroll-mt-20">Service CA certificates<a href="#cert-types-service-ca-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Review service certificate authority (CA) certificate rotation, expiration, and Operator-managed signing in OpenShift Container Platform to plan maintenance for internal service serving certificates.</p>
      <h2 id="cert-types-service-ca-certificates-purpose_cert-types-service-ca-certificates" class="group scroll-mt-20">Purpose<a href="#cert-types-service-ca-certificates-purpose_cert-types-service-ca-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p><code>service-ca</code> is an Operator that creates a self-signed CA when an OpenShift Container Platform cluster is deployed.</p>
      <h2 id="cert-types-service-ca-certificates-expiration_cert-types-service-ca-certificates" class="group scroll-mt-20">Expiration<a href="#cert-types-service-ca-certificates-expiration_cert-types-service-ca-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>A custom expiration term is not supported. The self-signed CA is stored in the <code>service-ca/signing-key</code> secret. The certificate is in <code>tls.crt</code>, the private key is in <code>tls.key</code>, and the CA bundle is in <code>ca-bundle.crt</code>.</p>
      <p>Other services can request a service serving certificate by annotating a service resource with <code>service.beta.openshift.io/serving-cert-secret-name: &lt;secret name&gt;</code>. In response, the Operator generates a new certificate as <code>tls.crt</code>, and a private key as <code>tls.key</code>, in the named secret. The certificate is valid for two years.</p>
      <p>Other services can request that the CA bundle for the service CA be injected into API service or config map resources by annotating with <code>service.beta.openshift.io/inject-cabundle: true</code> to support validating certificates generated from the service CA. In response, the Operator writes the current CA bundle to the <code>CABundle</code> field of an API service or as <code>service-ca.crt</code> to a config map.</p>
      <p>As of OpenShift Container Platform 4.3.5, automated rotation is supported and is backported to some 4.2.z and 4.3.z releases. For any release supporting automated rotation, the service CA is valid for 26 months and is automatically refreshed when there is less than 13 months validity left. If necessary, you can manually refresh the service CA.</p>
      <p>The 26-month service CA validity period is longer than the expected upgrade interval for a supported OpenShift Container Platform cluster. Non-control plane consumers of service CA certificates are refreshed after CA rotation and before the expiration of the pre-rotation CA.</p>
      <Alert variant="warning" class="my-4">
        <AlertTitle>Warning</AlertTitle>
        <AlertDescription>
          <p>A manually-rotated service CA does not maintain trust with the previous service CA. You might experience a temporary service disruption until the pods in the cluster are restarted, which ensures that pods are using service serving certificates issued by the new service CA.</p>
        </AlertDescription>
      </Alert>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Applications using the <code>service-ca</code> certificate must be capable of dynamically reloading CA certificates. When automated rotation occurs, pods that cannot reload CA certificates dynamically might require a restart to rebuild certificate trust.</p>
        </AlertDescription>
      </Alert>
      <h2 id="cert-types-service-ca-certificates-management_cert-types-service-ca-certificates" class="group scroll-mt-20">Management<a href="#cert-types-service-ca-certificates-management_cert-types-service-ca-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>These certificates are managed by the system and not the user.</p>
      <h2 id="cert-types-service-ca-certificates-services_cert-types-service-ca-certificates" class="group scroll-mt-20">Services<a href="#cert-types-service-ca-certificates-services_cert-types-service-ca-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Services that use service CA certificates include:</p>
      <ul>
      <li><code>cluster-autoscaler-operator</code></li>
      <li><code>cluster-monitoring-operator</code></li>
      <li><code>cluster-authentication-operator</code></li>
      <li><code>cluster-image-registry-operator</code></li>
      <li><code>cluster-ingress-operator</code></li>
      <li><code>cluster-kube-apiserver-operator</code></li>
      <li><code>cluster-kube-controller-manager-operator</code></li>
      <li><code>cluster-kube-scheduler-operator</code></li>
      <li><code>cluster-networking-operator</code></li>
      <li><code>cluster-openshift-apiserver-operator</code></li>
      <li><code>cluster-openshift-controller-manager-operator</code></li>
      <li><code>cluster-samples-operator</code></li>
      <li><code>cluster-storage-operator</code></li>
      <li><code>machine-config-operator</code></li>
      <li><code>console-operator</code></li>
      <li><code>insights-operator</code></li>
      <li><code>machine-api-operator</code></li>
      <li><code>operator-lifecycle-manager</code></li>
      <li>CSI driver operators</li>
      </ul>
      <p>This is not a comprehensive list.</p>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/security/certificates/service-serving-certificate#manually-rotate-service-ca_service-serving-certificate">Manually rotate the service CA certificate</a></li>
      <li><a href="/openshift-docs-markdown/security/certificates/service-serving-certificate#add-service-serving">Securing service traffic using service serving certificate secrets</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
