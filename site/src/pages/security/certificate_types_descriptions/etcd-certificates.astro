---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/certificate_types_descriptions/etcd-certificates.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"cert-types-etcd-certificates","text":"etcd certificates"},{"depth":2,"slug":"etcd-certificate-types_cert-types-etcd-certificates","text":"etcd certificate types"},{"depth":2,"slug":"rotating-certificate-authority_cert-types-etcd-certificates","text":"Rotating the etcd certificate"},{"depth":2,"slug":"removing-unused-ca-bundle_cert-types-etcd-certificates","text":"Removing an unused certificate authority from the bundle"},{"depth":2,"slug":"etcd-cert-alerts-metrics-signer_cert-types-etcd-certificates","text":"etcd certificate rotation alerts and metrics signer certificates"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/certificate_types_descriptions/etcd-certificates";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "etcd certificates";
const description = "Manage etcd certificates in OpenShift Container Platform, including rotating certificates, removing unused certificate authorities, and understanding…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/certificate_types_descriptions"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# etcd certificates {#cert-types-etcd-certificates}\n\nManage etcd certificates in OpenShift Container Platform, including rotating certificates, removing unused certificate authorities, and understanding certificate types.\n\netcd certificates are signed by the etcd-signer. The certificates come from a certificate authority (CA) that is generated by the bootstrap process.\n\nThe CA certificates are valid for 10 years. The peer, client, and server certificates are valid for three years.\n\nThese certificates are managed only by the system and are automatically rotated.\n\n## etcd certificate types {#etcd-certificate-types_cert-types-etcd-certificates}\n\nReview the etcd peer, client, server, and metric certificate types and related secrets, so you know which certificate applies when configuring or troubleshooting etcd security.\n\netcd certificates are used for encrypted communication between etcd member peers and encrypted client traffic. The following certificates are generated and used by etcd and other processes that communicate with etcd:\n\n- Peer certificates: Used for communication between etcd members.\n- Client certificates: Used for encrypted server-client communication. Client certificates are currently only used by the API server. Except for the proxy, ensure that no other service connects to etcd directly except for the proxy. Client secrets such as `etcd-client`, `etcd-metric-client`, `etcd-metric-signer`, and `etcd-signer` are added to the `openshift-config`, `openshift-etcd`, `openshift-etcd-operator`, and `openshift-kube-apiserver` namespaces.\n- Server certificates: Used by the etcd server for authenticating client requests.\n- Metric certificates: All metric consumers connect to the proxy with metric-client certificates.\n\n## Rotating the etcd certificate {#rotating-certificate-authority_cert-types-etcd-certificates}\n\nYou can manually rotate the etcd certificate before its automatic, scheduled rotation by backing up and deleting the current signer certificate.\n\n**Procedure**\n\n1. Make a backup copy of the current signer certificate by running the following command:\n\n   ```terminal\n   $ oc get secret -n openshift-etcd etcd-signer -oyaml > signer_backup_secret.yaml\n   ```\n2. Delete the existing signer certificate by running the following command:\n\n   ```terminal\n   $ oc delete secret -n openshift-etcd etcd-signer\n   ```\n\n**Verification**\n\n- Wait for the static pod roll out by running the following command. The static pod roll out can take a few minutes to complete.\n\n  ```terminal\n  $ oc wait --for=condition=Progressing=False --timeout=15m clusteroperator/etcd\n  ```\n\n## Removing an unused certificate authority from the bundle {#removing-unused-ca-bundle_cert-types-etcd-certificates}\n\nAfter a manual etcd or metrics signer rotation, delete the `etcd-ca-bundle` or `etcd-metrics-ca-bundl` as appropriate. When the cluster reconciles, unused certificate authority (CA) keys are removed. This ensures that components only trust the current signer.\n\n**Procedure**\n\n- Delete the key by running the following command:\n\n  ```terminal\n  $ oc delete configmap -n openshift-etcd etcd-ca-bundle\n  ```\n\n**Verification**\n\n- Wait for the static pod rollout by running the following command. The bundle regenerates with the current signer certificate and all unknown or unused keys are deleted.\n\n  ```terminal\n  $ oc adm wait-for-stable-cluster --minimum-stable-period 2m\n  ```\n\n## etcd certificate rotation alerts and metrics signer certificates {#etcd-cert-alerts-metrics-signer_cert-types-etcd-certificates}\n\nMonitor etcd signer expiration alerts and rotate the metrics signer using the `etcd-metric-signer` parameter and the `etcd-metrics-ca-bundle` when required, so you avoid certificate expiration and maintain secure etcd metrics traffic.\n\n`etcdSignerCAExpirationWarning`\n:   Occurs 730 days until the signer expires.\n\n`etcdSignerCAExpirationCritical`\n:   Occurs 365 days until the signer expires.\n\nThese alerts track the expiration date of the signer certificate authorities in the `openshift-etcd` namespace.\n\nYou can rotate the certificate for the following reasons:\n\n- You receive an expiration alert.\n- The private key is leaked.\n\n> [!IMPORTANT]\n> When a private key is leaked, you must rotate all of the certificates.\n\nThere is a separate etcd signer for the OpenShift Container Platform metrics system. To rotate the separate etcd signer, follow the steps in \"Rotating the etcd certificate\" using the following parameters:\n\n- Use the `etcd-metric-signer` parameter instead of the `etcd-signer`\n- Use `etcd-metrics-ca-bundle` bundle instead ofthe  `etcd-ca-bundle`\n\n**Additional resources**\n{._additional-resources}\n\n- [Restoring to an earlier cluster state](/openshift-docs-markdown/backup_and_restore/control_plane_backup_and_restore/disaster_recovery/scenario-2-restoring-cluster-state#dr-restoring-cluster-state)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/certificate_types_descriptions/etcd-certificates.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="cert-types-etcd-certificates" class="group scroll-mt-20">etcd certificates<a href="#cert-types-etcd-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Manage etcd certificates in OpenShift Container Platform, including rotating certificates, removing unused certificate authorities, and understanding certificate types.</p>
      <p>etcd certificates are signed by the etcd-signer. The certificates come from a certificate authority (CA) that is generated by the bootstrap process.</p>
      <p>The CA certificates are valid for 10 years. The peer, client, and server certificates are valid for three years.</p>
      <p>These certificates are managed only by the system and are automatically rotated.</p>
      <h2 id="etcd-certificate-types_cert-types-etcd-certificates" class="group scroll-mt-20">etcd certificate types<a href="#etcd-certificate-types_cert-types-etcd-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Review the etcd peer, client, server, and metric certificate types and related secrets, so you know which certificate applies when configuring or troubleshooting etcd security.</p>
      <p>etcd certificates are used for encrypted communication between etcd member peers and encrypted client traffic. The following certificates are generated and used by etcd and other processes that communicate with etcd:</p>
      <ul>
      <li>Peer certificates: Used for communication between etcd members.</li>
      <li>Client certificates: Used for encrypted server-client communication. Client certificates are currently only used by the API server. Except for the proxy, ensure that no other service connects to etcd directly except for the proxy. Client secrets such as <code>etcd-client</code>, <code>etcd-metric-client</code>, <code>etcd-metric-signer</code>, and <code>etcd-signer</code> are added to the <code>openshift-config</code>, <code>openshift-etcd</code>, <code>openshift-etcd-operator</code>, and <code>openshift-kube-apiserver</code> namespaces.</li>
      <li>Server certificates: Used by the etcd server for authenticating client requests.</li>
      <li>Metric certificates: All metric consumers connect to the proxy with metric-client certificates.</li>
      </ul>
      <h2 id="rotating-certificate-authority_cert-types-etcd-certificates" class="group scroll-mt-20">Rotating the etcd certificate<a href="#rotating-certificate-authority_cert-types-etcd-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can manually rotate the etcd certificate before its automatic, scheduled rotation by backing up and deleting the current signer certificate.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Make a backup copy of the current signer certificate by running the following command:
      <CodeBlock code={"$ oc get secret -n openshift-etcd etcd-signer -oyaml > signer_backup_secret.yaml"} lang="terminal" />
      </li>
      <li>
      Delete the existing signer certificate by running the following command:
      <CodeBlock code={"$ oc delete secret -n openshift-etcd etcd-signer"} lang="terminal" />
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      Wait for the static pod roll out by running the following command. The static pod roll out can take a few minutes to complete.
      <CodeBlock code={"$ oc wait --for=condition=Progressing=False --timeout=15m clusteroperator/etcd"} lang="terminal" />
      </li>
      </ul>
      <h2 id="removing-unused-ca-bundle_cert-types-etcd-certificates" class="group scroll-mt-20">Removing an unused certificate authority from the bundle<a href="#removing-unused-ca-bundle_cert-types-etcd-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">After a manual etcd or metrics signer rotation, delete the <code>etcd-ca-bundle</code> or <code>etcd-metrics-ca-bundl</code> as appropriate. When the cluster reconciles, unused certificate authority (CA) keys are removed. This ensures that components only trust the current signer.</p>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Delete the key by running the following command:
      <CodeBlock code={"$ oc delete configmap -n openshift-etcd etcd-ca-bundle"} lang="terminal" />
      </li>
      </ul>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      Wait for the static pod rollout by running the following command. The bundle regenerates with the current signer certificate and all unknown or unused keys are deleted.
      <CodeBlock code={"$ oc adm wait-for-stable-cluster --minimum-stable-period 2m"} lang="terminal" />
      </li>
      </ul>
      <h2 id="etcd-cert-alerts-metrics-signer_cert-types-etcd-certificates" class="group scroll-mt-20">etcd certificate rotation alerts and metrics signer certificates<a href="#etcd-cert-alerts-metrics-signer_cert-types-etcd-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Monitor etcd signer expiration alerts and rotate the metrics signer using the <code>etcd-metric-signer</code> parameter and the <code>etcd-metrics-ca-bundle</code> when required, so you avoid certificate expiration and maintain secure etcd metrics traffic.</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>etcdSignerCAExpirationWarning</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Occurs 730 days until the signer expires.</p></dd>
      <dt class="font-semibold"><code>etcdSignerCAExpirationCritical</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Occurs 365 days until the signer expires.</p></dd>
      </dl>
      <p>These alerts track the expiration date of the signer certificate authorities in the <code>openshift-etcd</code> namespace.</p>
      <p>You can rotate the certificate for the following reasons:</p>
      <ul>
      <li>You receive an expiration alert.</li>
      <li>The private key is leaked.</li>
      </ul>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>When a private key is leaked, you must rotate all of the certificates.</p>
        </AlertDescription>
      </Alert>
      <p>There is a separate etcd signer for the OpenShift Container Platform metrics system. To rotate the separate etcd signer, follow the steps in "Rotating the etcd certificate" using the following parameters:</p>
      <ul>
      <li>Use the <code>etcd-metric-signer</code> parameter instead of the <code>etcd-signer</code></li>
      <li>Use <code>etcd-metrics-ca-bundle</code> bundle instead ofthe  <code>etcd-ca-bundle</code></li>
      </ul>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="/openshift-docs-markdown/backup_and_restore/control_plane_backup_and_restore/disaster_recovery/scenario-2-restoring-cluster-state#dr-restoring-cluster-state">Restoring to an earlier cluster state</a></li></ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
