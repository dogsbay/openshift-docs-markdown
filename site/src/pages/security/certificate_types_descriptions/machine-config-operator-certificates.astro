---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/certificate_types_descriptions/machine-config-operator-certificates.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";

const headings = [{"depth":1,"slug":"cert-types-machine-config-operator-certificates","text":"Machine Config Operator certificates"},{"depth":2,"slug":"machine-config-operator-certificates-overview_cert-types-machine-config-operator-certificates","text":"Machine Config Operator certificates overview"},{"depth":3,"slug":"cert-types-machine-config-operator-certificates-details_cert-types-machine-config-operator-certificates","text":"Provisioning details"},{"depth":3,"slug":"cert-types-machine-config-operator-certificates-trust_cert-types-machine-config-operator-certificates","text":"Provisioning chain of trust"},{"depth":2,"slug":"machine-config-operator-certificates-reference_cert-types-machine-config-operator-certificates","text":"Machine Config Operator certificates reference"},{"depth":3,"slug":"cert-types-machine-config-operator-certificates-materials_cert-types-machine-config-operator-certificates","text":"Key material inside a cluster"},{"depth":3,"slug":"cert-types-machine-config-operator-certificates-mgmt_cert-types-machine-config-operator-certificates","text":"Management"},{"depth":3,"slug":"cert-types-machine-config-operator-certificates-exp_cert-types-machine-config-operator-certificates","text":"Expiration"},{"depth":3,"slug":"cert-types-machine-config-operator-certificates-custom_cert-types-machine-config-operator-certificates","text":"Customization"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/certificate_types_descriptions/machine-config-operator-certificates";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Machine Config Operator certificates";
const description = "Understand Machine Config Operator (MCO) certificates used to secure node connections to the Machine Config Server (MCS) during cluster provisioning…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/certificate_types_descriptions"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Machine Config Operator certificates {#cert-types-machine-config-operator-certificates}\n\nUnderstand Machine Config Operator (MCO) certificates used to secure node connections to the Machine Config Server (MCS) during cluster provisioning, including their lifecycle, rotation, and support boundaries.\n\n## Machine Config Operator certificates overview {#machine-config-operator-certificates-overview_cert-types-machine-config-operator-certificates}\n\nLearn how Machine Config Operator (MCO) certificates secure node connections to the Machine Config Server (MCS) during cluster provisioning, so you can plan for certificate maintenance and for troubleshooting node provisioning issues.\n\nThis certificate authority (CA) is used to secure connections from nodes to the MCS during initial provisioning.\n\nThere are two certificates:\n\n- A self-signed CA, the `machine-config-server-ca` config map (MCS CA).\n- A derived certificate, the `machine-config-server-tls` secret (MCS certificate).\n\n### Provisioning details {#cert-types-machine-config-operator-certificates-details_cert-types-machine-config-operator-certificates}\n\nOpenShift Container Platform installations that use Red Hat Enterprise Linux CoreOS (RHCOS) are installed by using Ignition. This process is split into two parts:\n\n- An Ignition config is created that references a URL for the full configuration served by the MCS.\n- For user-provisioned infrastructure installation methods, the Ignition config manifests as a `worker.ign` file created by the `openshift-install` command. For installer-provisioned infrastructure installation methods that use the Machine API Operator, this configuration appears as the `worker-user-data` secret.\n\n> [!IMPORTANT]\n> Currently, there is no supported way to block or restrict the machine config server endpoint. The machine config server must be exposed to the network so that newly-provisioned machines, which have no existing configuration or state, are able to fetch their configuration. In this model, the root of trust is the certificate signing requests (CSR) endpoint, which is where the kubelet sends its certificate signing request for approval to join the cluster. Because of this, machine configs should not be used to distribute sensitive information, such as secrets and certificates.\n>\n> To ensure that the machine config server endpoints, ports 22623 and 22624, are secured in bare metal scenarios, customers must configure proper network policies.\n\n### Provisioning chain of trust {#cert-types-machine-config-operator-certificates-trust_cert-types-machine-config-operator-certificates}\n\nThe MCS CA is injected into the Ignition configuration under the `security.tls.certificateAuthorities` configuration field. The MCS then provides the complete configuration using the MCS certificate presented by the web server.\n\nThe client validates that the MCS certificate presented by the server has a chain of trust to an authority it recognizes. In this case, the MCS CA is that authority, and it signs the MCS certificate. This ensures that the client is accessing the correct server. The client in this case is Ignition running on a machine in the initial RAM filesystem (initramfs).\n\n## Machine Config Operator certificates reference {#machine-config-operator-certificates-reference_cert-types-machine-config-operator-certificates}\n\nUse this reference to locate Machine Config Operator (MCO) certificate key material, rotation requirements, and support boundaries, so you can plan for certificate maintenance and for scheduling rotation before certificates expire.\n\n### Key material inside a cluster {#cert-types-machine-config-operator-certificates-materials_cert-types-machine-config-operator-certificates}\n\nThe following objects are stored in the `openshift-machine-config-operator` namespace:\n\n- The Machine Config Server (MCS) certificate authority (CA) bundle is stored as the `machine-config-server-ca` config map. The MCS CA bundle stores all valid CAs for the `MachineConfigServer` TLS certificate.\n- The MCS CA signing key is stored as the `machine-config-server-ca` secret. The MCS CA signing key is used to sign the `MachineConfigServer` TLS certificate.\n- The MCS certificate is stored as the `machine-config-server-tls` secret, which contains the `MachineConfigServer` TLS certificate and key.\n\nThe `machine-config-server-ca` config map is used in the following ways:\n\n- The certificate controller updates the `*-user-data` secrets in the `openshift-machine-api` namespace any time the `machine-config-server-ca` configmap is updated.\n- The Machine Config Operator renders the `master-user-data-managed` and `worker-user-data-managed` secrets from the `machine-config-server-ca` configmap.\n\n### Management {#cert-types-machine-config-operator-certificates-mgmt_cert-types-machine-config-operator-certificates}\n\nAt this time, directly modifying either of these certificates is not supported.\n\n### Expiration {#cert-types-machine-config-operator-certificates-exp_cert-types-machine-config-operator-certificates}\n\nThe MCS CA and MCS certificate are valid for 10 years and are automatically rotated by the MCO at 8 years.\n\nThe issued serving certificates are valid for 10 years.\n\n> [!NOTE]\n> This automatic certificate rotation applies only to clusters that use machine sets. For clusters that do not use machine sets, such as vSphere user-provisioned infrastructure clusters, you are required to manually rotate these certificates. For more information on manual certificate rotation, see the Red Hat Knowledgebase article *Regenerating CA certificates for the Machine Config Server*.\n\n### Customization {#cert-types-machine-config-operator-certificates-custom_cert-types-machine-config-operator-certificates}\n\nYou cannot customize the MCO certificates.\n\n**Additional resources**\n{._additional-resources}\n\n- [About the Machine Config Operator](/openshift-docs-markdown/machine_configuration/index#about-machine-config-operator_machine-config-overview)\n- [About the OVN-Kubernetes network plugin](/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/about-ovn-kubernetes#about-ovn-kubernetes)\n- [Regenerating CA certificates for the Machine Config Server](https://access.redhat.com/articles/regenerating_cluster_certificates#regenerating-ca-certificates-for-the-machine-config-server-5)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/certificate_types_descriptions/machine-config-operator-certificates.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="cert-types-machine-config-operator-certificates" class="group scroll-mt-20">Machine Config Operator certificates<a href="#cert-types-machine-config-operator-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Understand Machine Config Operator (MCO) certificates used to secure node connections to the Machine Config Server (MCS) during cluster provisioning, including their lifecycle, rotation, and support boundaries.</p>
      <h2 id="machine-config-operator-certificates-overview_cert-types-machine-config-operator-certificates" class="group scroll-mt-20">Machine Config Operator certificates overview<a href="#machine-config-operator-certificates-overview_cert-types-machine-config-operator-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Learn how Machine Config Operator (MCO) certificates secure node connections to the Machine Config Server (MCS) during cluster provisioning, so you can plan for certificate maintenance and for troubleshooting node provisioning issues.</p>
      <p>This certificate authority (CA) is used to secure connections from nodes to the MCS during initial provisioning.</p>
      <p>There are two certificates:</p>
      <ul>
      <li>A self-signed CA, the <code>machine-config-server-ca</code> config map (MCS CA).</li>
      <li>A derived certificate, the <code>machine-config-server-tls</code> secret (MCS certificate).</li>
      </ul>
      <h3 id="cert-types-machine-config-operator-certificates-details_cert-types-machine-config-operator-certificates" class="group scroll-mt-20">Provisioning details<a href="#cert-types-machine-config-operator-certificates-details_cert-types-machine-config-operator-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>OpenShift Container Platform installations that use Red Hat Enterprise Linux CoreOS (RHCOS) are installed by using Ignition. This process is split into two parts:</p>
      <ul>
      <li>An Ignition config is created that references a URL for the full configuration served by the MCS.</li>
      <li>For user-provisioned infrastructure installation methods, the Ignition config manifests as a <code>worker.ign</code> file created by the <code>openshift-install</code> command. For installer-provisioned infrastructure installation methods that use the Machine API Operator, this configuration appears as the <code>worker-user-data</code> secret.</li>
      </ul>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Currently, there is no supported way to block or restrict the machine config server endpoint. The machine config server must be exposed to the network so that newly-provisioned machines, which have no existing configuration or state, are able to fetch their configuration. In this model, the root of trust is the certificate signing requests (CSR) endpoint, which is where the kubelet sends its certificate signing request for approval to join the cluster. Because of this, machine configs should not be used to distribute sensitive information, such as secrets and certificates.</p>
          <p>To ensure that the machine config server endpoints, ports 22623 and 22624, are secured in bare metal scenarios, customers must configure proper network policies.</p>
        </AlertDescription>
      </Alert>
      <h3 id="cert-types-machine-config-operator-certificates-trust_cert-types-machine-config-operator-certificates" class="group scroll-mt-20">Provisioning chain of trust<a href="#cert-types-machine-config-operator-certificates-trust_cert-types-machine-config-operator-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The MCS CA is injected into the Ignition configuration under the <code>security.tls.certificateAuthorities</code> configuration field. The MCS then provides the complete configuration using the MCS certificate presented by the web server.</p>
      <p>The client validates that the MCS certificate presented by the server has a chain of trust to an authority it recognizes. In this case, the MCS CA is that authority, and it signs the MCS certificate. This ensures that the client is accessing the correct server. The client in this case is Ignition running on a machine in the initial RAM filesystem (initramfs).</p>
      <h2 id="machine-config-operator-certificates-reference_cert-types-machine-config-operator-certificates" class="group scroll-mt-20">Machine Config Operator certificates reference<a href="#machine-config-operator-certificates-reference_cert-types-machine-config-operator-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Use this reference to locate Machine Config Operator (MCO) certificate key material, rotation requirements, and support boundaries, so you can plan for certificate maintenance and for scheduling rotation before certificates expire.</p>
      <h3 id="cert-types-machine-config-operator-certificates-materials_cert-types-machine-config-operator-certificates" class="group scroll-mt-20">Key material inside a cluster<a href="#cert-types-machine-config-operator-certificates-materials_cert-types-machine-config-operator-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The following objects are stored in the <code>openshift-machine-config-operator</code> namespace:</p>
      <ul>
      <li>The Machine Config Server (MCS) certificate authority (CA) bundle is stored as the <code>machine-config-server-ca</code> config map. The MCS CA bundle stores all valid CAs for the <code>MachineConfigServer</code> TLS certificate.</li>
      <li>The MCS CA signing key is stored as the <code>machine-config-server-ca</code> secret. The MCS CA signing key is used to sign the <code>MachineConfigServer</code> TLS certificate.</li>
      <li>The MCS certificate is stored as the <code>machine-config-server-tls</code> secret, which contains the <code>MachineConfigServer</code> TLS certificate and key.</li>
      </ul>
      <p>The <code>machine-config-server-ca</code> config map is used in the following ways:</p>
      <ul>
      <li>The certificate controller updates the <code>*-user-data</code> secrets in the <code>openshift-machine-api</code> namespace any time the <code>machine-config-server-ca</code> configmap is updated.</li>
      <li>The Machine Config Operator renders the <code>master-user-data-managed</code> and <code>worker-user-data-managed</code> secrets from the <code>machine-config-server-ca</code> configmap.</li>
      </ul>
      <h3 id="cert-types-machine-config-operator-certificates-mgmt_cert-types-machine-config-operator-certificates" class="group scroll-mt-20">Management<a href="#cert-types-machine-config-operator-certificates-mgmt_cert-types-machine-config-operator-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>At this time, directly modifying either of these certificates is not supported.</p>
      <h3 id="cert-types-machine-config-operator-certificates-exp_cert-types-machine-config-operator-certificates" class="group scroll-mt-20">Expiration<a href="#cert-types-machine-config-operator-certificates-exp_cert-types-machine-config-operator-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The MCS CA and MCS certificate are valid for 10 years and are automatically rotated by the MCO at 8 years.</p>
      <p>The issued serving certificates are valid for 10 years.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>This automatic certificate rotation applies only to clusters that use machine sets. For clusters that do not use machine sets, such as vSphere user-provisioned infrastructure clusters, you are required to manually rotate these certificates. For more information on manual certificate rotation, see the Red Hat Knowledgebase article <em>Regenerating CA certificates for the Machine Config Server</em>.</p>
        </AlertDescription>
      </Alert>
      <h3 id="cert-types-machine-config-operator-certificates-custom_cert-types-machine-config-operator-certificates" class="group scroll-mt-20">Customization<a href="#cert-types-machine-config-operator-certificates-custom_cert-types-machine-config-operator-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>You cannot customize the MCO certificates.</p>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/machine_configuration/index#about-machine-config-operator_machine-config-overview">About the Machine Config Operator</a></li>
      <li><a href="/openshift-docs-markdown/networking/ovn_kubernetes_network_provider/about-ovn-kubernetes#about-ovn-kubernetes">About the OVN-Kubernetes network plugin</a></li>
      <li><a href="https://access.redhat.com/articles/regenerating_cluster_certificates#regenerating-ca-certificates-for-the-machine-config-server-5">Regenerating CA certificates for the Machine Config Server</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
