---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/external_secrets_operator/external-secrets-operator-proxy.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"external-secrets-operator-proxy","text":"About the egress proxy for the External Secrets Operator for Red Hat OpenShift"},{"depth":2,"slug":"external-secrets-operator-configure-proxy_external-secrets-operator-proxy","text":"Configuring the egress proxy for the External Secrets Operator for Red Hat OpenShift"},{"depth":2,"slug":"external-resources-operator-proxy_additional-resources","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/external_secrets_operator/external-secrets-operator-proxy";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "About the egress proxy for the External Secrets Operator for Red Hat OpenShift";
const description = "If a cluster-wide egress proxy is configured in OpenShift Container Platform, the Operator Lifecycle Manager (OLM) automatically configures Operators that…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/external_secrets_operator"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# About the egress proxy for the External Secrets Operator for Red Hat OpenShift {#external-secrets-operator-proxy}\n\nIf a cluster-wide egress proxy is configured in OpenShift Container Platform, the Operator Lifecycle Manager (OLM) automatically configures Operators that it manages with the cluster-wide proxy. OLM automatically updates all of the Operator deployments with the `HTTP_PROXY`, `HTTPS_PROXY`, and `NO_PROXY` environment variables.\n\n## Configuring the egress proxy for the External Secrets Operator for Red Hat OpenShift {#external-secrets-operator-configure-proxy_external-secrets-operator-proxy}\n\nThe egress proxy can be configured in the `ExternalSecretsConfig` or the `ExternalSecretsManager` custom resource (CR). The Operator and the operand make use of the OpenShift Container Platform supported certificate authority (CA) bundle for the proxy validations.\n\nThe Operator can automatically create and manage a `NetworkPolicy` such as `eso-sys-allow-proxy-egress`, that allows all `external-secrets` pods to reach the proxy server. You control this behavior by using the `networkPolicyProvisioning` field. The field can be set in either the `ExternalSecretsConfig` CR or the `ExternalSecretsManager` CR, and can be configured independently of proxy URL fields. For example, when the proxy is provided by Operator Lifecycle Manager (OLM) environment variables at the cluster level, you can set only `networkPolicyProvisioning` in either CR without specifying any proxy URLs.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- You have created the `ExternalSecretsConfig` custom CR.\n\n**Procedure**\n\n1. To set the proxy in the `ExternalSecretsConfig` resource, perform the following steps:\n\n   1. Edit the `ExternalSecretsConfig` resource by running the following command:\n\n      ```terminal\n      $ oc edit externalsecretsconfigs.operator.openshift.io cluster\n      ```\n   2. Edit the `spec.appConfig.proxy` section to set the proxy values as follows:\n\n      ```yaml\n      apiVersion: operator.openshift.io/v1alpha1\n      kind: ExternalSecretsConfig\n      ...\n      spec:\n        appConfig:\n          proxy:\n            httpProxy: <http_proxy>\n            httpsProxy: <https_proxy>\n            noProxy: <no_proxy>\n            networkPolicyProvisioning: Managed\n      ```\n\n      where:\n\n      `spec.appConfig.proxy.httpProxy`\n      :   Specifies the proxy URL for the HTTP requests.\n\n`spec.appConfig.proxy.httpsProxy`\n:   Specifies the proxy URL for the HTTPS requests.\n\n`spec.appConfig.proxy.noProxy`\n:   Specifies a comma-separated list of hostnames, CIDRs, IPs or a combination of these, for which the proxy should not be used.\n\n`spec.appConfig.proxy.networkPolicyProvisioning`\n:   Specifies whether the Operator automatically creates and manages the `eso-sys-allow-proxy-egress` `NetworkPolicy`. Accepted values are `Managed`, which is the default, and `Unmanaged`. When set to `Managed`, the Operator creates the policy based on the proxy URL port and deletes it when the proxy is removed. When set to `Unmanaged`, the Operator does not create or delete the policy and you are responsible for managing proxy egress traffic.\n\n1. To set the proxy in the `ExternalSecretsManager` CR, perform the following steps:\n\n   1. Edit the `ExternalSecretsManager` CR by running the following command:\n\n      ```terminal\n      $ oc edit externalsecretsmanagers.operator.openshift.io cluster\n      ```\n   2. Edit the `spec.globalConfig.proxy` section to set the proxy values as follows:\n\n      ```yaml\n      apiVersion: operator.openshift.io/v1alpha1\n      kind: ExternalSecretsManager\n      ...\n      spec:\n        globalConfig:\n          proxy:\n            httpProxy: <http_proxy>\n            httpsProxy: <https_proxy>\n            noProxy: <no_proxy>\n            networkPolicyProvisioning: Managed\n      ```\n\n      where:\n\n      `spec.appConfig.proxy.httpProxy`\n      :   Specifies the proxy URL for the HTTP requests.\n\n`spec.appConfig.proxy.httpsProxy`\n:   Specifies the proxy URL for the HTTPS requests.\n\n`spec.appConfig.proxy.noProxy`\n:   Specifies a comma-separated list of hostnames, CIDRs, IPs or a combination of these, for which the proxy should not be used.\n\n`spec.appConfig.proxy.networkPolicyProvisioning`\n:   Specifies whether the Operator automatically creates and manages the `eso-sys-allow-proxy-egress` `NetworkPolicy`. The values are`Managed`, which is the default, and `Unmanaged`.\n\n    ```\n      :::note\n\n      When `networkPolicyProvisioning` is set in both the `ExternalSecretsConfig` CR and the `ExternalSecretsManager` CR, the value in the `ExternalSecretsConfig` CR takes precedence.\n\n      :::\n    ```\n\n1. If the proxy is configured at the cluster level through OLM environment variables and you only want to control `NetworkPolicy` provisioning without specifying proxy URLs in a CR, set only the `networkPolicyProvisioning` field in either CR as follows:\n\n   ```yaml\n   apiVersion: operator.openshift.io/v1alpha1\n   kind: ExternalSecretsConfig\n   ...\n   spec:\n     applicationConfig:\n       proxy:\n         networkPolicyProvisioning: Unmanaged\n   ```\n\n**Verification**\n\n1. Verify that the proxy egress `NetworkPolicy` was created by running the following command:\n\n   ```terminal\n   $ oc get networkpolicy eso-sys-allow-proxy-egress -n external-secrets -o yaml\n   ```\n\n   The policy should show an egress rule allowing transmission control protocol (TCP) traffic on the port derived from the configured proxy URL.\n2. Verify that the proxy configuration is applied to the `external-secrets` deployment by running the following command:\n\n   ```terminal\n   $ oc set env deployment/external-secrets -n external-secrets --list | grep -i proxy\n   ```\n\n## Additional resources {#external-resources-operator-proxy_additional-resources}\n\n- [Configuring proxy support in Operator Lifecycle Manager](/openshift-docs-markdown/operators/admin/olm-configuring-proxy-support#olm-configuring-proxy-support)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/external_secrets_operator/external-secrets-operator-proxy.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="external-secrets-operator-proxy" class="group scroll-mt-20">About the egress proxy for the External Secrets Operator for Red Hat OpenShift<a href="#external-secrets-operator-proxy" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>If a cluster-wide egress proxy is configured in OpenShift Container Platform, the Operator Lifecycle Manager (OLM) automatically configures Operators that it manages with the cluster-wide proxy. OLM automatically updates all of the Operator deployments with the <code>HTTP_PROXY</code>, <code>HTTPS_PROXY</code>, and <code>NO_PROXY</code> environment variables.</p>
      <h2 id="external-secrets-operator-configure-proxy_external-secrets-operator-proxy" class="group scroll-mt-20">Configuring the egress proxy for the External Secrets Operator for Red Hat OpenShift<a href="#external-secrets-operator-configure-proxy_external-secrets-operator-proxy" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The egress proxy can be configured in the <code>ExternalSecretsConfig</code> or the <code>ExternalSecretsManager</code> custom resource (CR). The Operator and the operand make use of the OpenShift Container Platform supported certificate authority (CA) bundle for the proxy validations.</p>
      <p>The Operator can automatically create and manage a <code>NetworkPolicy</code> such as <code>eso-sys-allow-proxy-egress</code>, that allows all <code>external-secrets</code> pods to reach the proxy server. You control this behavior by using the <code>networkPolicyProvisioning</code> field. The field can be set in either the <code>ExternalSecretsConfig</code> CR or the <code>ExternalSecretsManager</code> CR, and can be configured independently of proxy URL fields. For example, when the proxy is provided by Operator Lifecycle Manager (OLM) environment variables at the cluster level, you can set only <code>networkPolicyProvisioning</code> in either CR without specifying any proxy URLs.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>You have created the <code>ExternalSecretsConfig</code> custom CR.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      To set the proxy in the <code>ExternalSecretsConfig</code> resource, perform the following steps:
      <ol>
      <li>
      Edit the <code>ExternalSecretsConfig</code> resource by running the following command:
      <CodeBlock code={"$ oc edit externalsecretsconfigs.operator.openshift.io cluster"} lang="terminal" />
      </li>
      <li>
      Edit the <code>spec.appConfig.proxy</code> section to set the proxy values as follows:
      <CodeBlock code={"apiVersion: operator.openshift.io/v1alpha1\nkind: ExternalSecretsConfig\n...\nspec:\n  appConfig:\n    proxy:\n      httpProxy: <http_proxy>\n      httpsProxy: <https_proxy>\n      noProxy: <no_proxy>\n      networkPolicyProvisioning: Managed\n"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.appConfig.proxy.httpProxy</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the proxy URL for the HTTP requests.</p></dd>
      </dl>
      </li>
      </ol>
      </li>
      </ol>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.appConfig.proxy.httpsProxy</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the proxy URL for the HTTPS requests.</p></dd>
      <dt class="font-semibold"><code>spec.appConfig.proxy.noProxy</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a comma-separated list of hostnames, CIDRs, IPs or a combination of these, for which the proxy should not be used.</p></dd>
      <dt class="font-semibold"><code>spec.appConfig.proxy.networkPolicyProvisioning</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies whether the Operator automatically creates and manages the <code>eso-sys-allow-proxy-egress</code> <code>NetworkPolicy</code>. Accepted values are <code>Managed</code>, which is the default, and <code>Unmanaged</code>. When set to <code>Managed</code>, the Operator creates the policy based on the proxy URL port and deletes it when the proxy is removed. When set to <code>Unmanaged</code>, the Operator does not create or delete the policy and you are responsible for managing proxy egress traffic.</p></dd>
      </dl>
      <ol>
      <li>
      To set the proxy in the <code>ExternalSecretsManager</code> CR, perform the following steps:
      <ol>
      <li>
      Edit the <code>ExternalSecretsManager</code> CR by running the following command:
      <CodeBlock code={"$ oc edit externalsecretsmanagers.operator.openshift.io cluster"} lang="terminal" />
      </li>
      <li>
      Edit the <code>spec.globalConfig.proxy</code> section to set the proxy values as follows:
      <CodeBlock code={"apiVersion: operator.openshift.io/v1alpha1\nkind: ExternalSecretsManager\n...\nspec:\n  globalConfig:\n    proxy:\n      httpProxy: <http_proxy>\n      httpsProxy: <https_proxy>\n      noProxy: <no_proxy>\n      networkPolicyProvisioning: Managed"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.appConfig.proxy.httpProxy</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the proxy URL for the HTTP requests.</p></dd>
      </dl>
      </li>
      </ol>
      </li>
      </ol>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.appConfig.proxy.httpsProxy</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the proxy URL for the HTTPS requests.</p></dd>
      <dt class="font-semibold"><code>spec.appConfig.proxy.noProxy</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a comma-separated list of hostnames, CIDRs, IPs or a combination of these, for which the proxy should not be used.</p></dd>
      <dt class="font-semibold"><code>spec.appConfig.proxy.networkPolicyProvisioning</code></dt>
      <dd class="ml-6 text-muted-foreground">
      <p>Specifies whether the Operator automatically creates and manages the <code>eso-sys-allow-proxy-egress</code> <code>NetworkPolicy</code>. The values are<code>Managed</code>, which is the default, and <code>Unmanaged</code>.</p>
      <CodeBlock code={"  :::note\n\n  When `networkPolicyProvisioning` is set in both the `ExternalSecretsConfig` CR and the `ExternalSecretsManager` CR, the value in the `ExternalSecretsConfig` CR takes precedence.\n  \n  :::"} lang="plaintext" />
      </dd>
      </dl>
      <ol>
      <li>
      If the proxy is configured at the cluster level through OLM environment variables and you only want to control <code>NetworkPolicy</code> provisioning without specifying proxy URLs in a CR, set only the <code>networkPolicyProvisioning</code> field in either CR as follows:
      <CodeBlock code={"apiVersion: operator.openshift.io/v1alpha1\nkind: ExternalSecretsConfig\n...\nspec:\n  applicationConfig:\n    proxy:\n      networkPolicyProvisioning: Unmanaged"} lang="yaml" />
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>
      Verify that the proxy egress <code>NetworkPolicy</code> was created by running the following command:
      <CodeBlock code={"$ oc get networkpolicy eso-sys-allow-proxy-egress -n external-secrets -o yaml"} lang="terminal" />
      <p>The policy should show an egress rule allowing transmission control protocol (TCP) traffic on the port derived from the configured proxy URL.</p>
      </li>
      <li>
      Verify that the proxy configuration is applied to the <code>external-secrets</code> deployment by running the following command:
      <CodeBlock code={"$ oc set env deployment/external-secrets -n external-secrets --list | grep -i proxy"} lang="terminal" />
      </li>
      </ol>
      <h2 id="external-resources-operator-proxy_additional-resources" class="group scroll-mt-20">Additional resources<a href="#external-resources-operator-proxy_additional-resources" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul><li><a href="/openshift-docs-markdown/operators/admin/olm-configuring-proxy-support#olm-configuring-proxy-support">Configuring proxy support in Operator Lifecycle Manager</a></li></ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
