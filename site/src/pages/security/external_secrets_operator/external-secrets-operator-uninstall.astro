---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/external_secrets_operator/external-secrets-operator-uninstall.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"external-secrets-operator-uninstall","text":"Uninstalling the External Secrets Operator for Red Hat OpenShift"},{"depth":2,"slug":"external-secrets-operator-uninstall-console_external-secrets-operator-uninstall","text":"Uninstalling the External Secrets Operator for Red Hat OpenShift using the web console"},{"depth":2,"slug":"external-secrets-remove-resources_external-secrets-operator-uninstall","text":"Removing External Secrets Operator for Red Hat OpenShift resources by using the web console"},{"depth":2,"slug":"external-secrets-remove-resources-cli_external-secrets-operator-uninstall","text":"Removing External Secrets Operator for Red Hat OpenShift resources by using the CLI"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/external_secrets_operator/external-secrets-operator-uninstall";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Uninstalling the External Secrets Operator for Red Hat OpenShift";
const description = "You can remove the External Secrets Operator for Red Hat OpenShift from OpenShift Container Platform by uninstalling the Operator and removing its related…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/external_secrets_operator"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Uninstalling the External Secrets Operator for Red Hat OpenShift {#external-secrets-operator-uninstall}\n\nYou can remove the External Secrets Operator for Red Hat OpenShift from OpenShift Container Platform by uninstalling the Operator and removing its related resources.\n\n## Uninstalling the External Secrets Operator for Red Hat OpenShift using the web console {#external-secrets-operator-uninstall-console_external-secrets-operator-uninstall}\n\nYou can uninstall the External Secrets Operator for Red Hat OpenShift from your cluster using the OpenShift Container Platform web console. Uninstalling the Operator does not automatically delete the `ExternalSecrets` custom resources or the running `external-secrets` application workload. These resources remain in the cluster to prevent accidental data loss and must be removed manually if they are no longer needed.\n\n**Prerequisites**\n\n- You have access to the cluster with `cluster-admin` privileges.\n- You have access to the OpenShift Container Platform web console.\n- The External Secrets Operator is installed.\n\n**Procedure**\n\n1. Log in to the OpenShift Container Platform web console.\n2. Uninstall the External Secrets Operator for Red Hat OpenShift using the following steps:\n\n   1. Navigate to **Ecosystem** → **Installed Operators**.\n   2. Click the Options menu ![](/openshift-docs-markdown/_assets/images/kebab.png \"Options menu\") next to the **External Secrets Operator for Red Hat OpenShift** entry and click **Uninstall Operator**.\n   3. In the confirmation dialog, click **Uninstall**.\n\n## Removing External Secrets Operator for Red Hat OpenShift resources by using the web console {#external-secrets-remove-resources_external-secrets-operator-uninstall}\n\nAfter you have uninstalled the External Secrets Operator for Red Hat OpenShift, you can optionally eliminate its associated resources from your cluster.\n\n**Prerequisites**\n\n- You have access to the cluster with `cluster-admin` privileges.\n- You have access to the OpenShift Container Platform web console.\n\n**Procedure**\n\n1. Log in to the OpenShift Container Platform web console.\n2. Remove the deployments of the `external-secrets` application components in the `external-secrets` namespace:\n\n   1. Click the **Project** drop-down menu to see a list of all available projects, and select the **external-secrets** project.\n   2. Navigate to **Workloads** → **Deployments**.\n   3. Select the deployment that you want to delete.\n   4. Click the **Actions** drop-down menu, and select **Delete Deployment** to see a confirmation dialog box.\n   5. Click **Delete** to delete the deployment.\n3. Remove the custom resource definitions (CRDs) that were installed by the External Secrets Operator using the following steps:\n\n   1. Navigate to **Administration** → **CustomResourceDefinitions**.\n   2. Choose `external-secrets.io/component: controller` from the suggestions in the **Label** field to filter the CRDs.\n   3. Click the Options menu ![](/openshift-docs-markdown/_assets/images/kebab.png \"Options menu\") next to each of the following CRDs, and select **Delete Custom Resource Definition**:\n\n      - ACRAccessToken\n      - ClusterExternalSecret\n      - ClusterGenerator\n      - ClusterPushSecret\n      - ClusterSecretStore\n      - ECRAuthorizationToken\n      - ExternalSecret\n      - GCRAccessToken\n      - GeneratorState\n      - GithubAccessToken\n      - Grafana\n      - MFA\n      - Password\n      - PushSecret\n      - QuayAccessToken\n      - SecretStore\n      - SSHKey\n      - STSSessionToken\n      - UUID\n      - VaultDynamicSecret\n      - Webhook\n4. Remove the `external-secrets-operator` namespace using the following steps:\n\n   1. Navigate to **Administration** → **Namespaces**.\n   2. Click the Options menu ![](/openshift-docs-markdown/_assets/images/kebab.png \"Options menu\") next to the **External Secrets Operator** and select **Delete Namespace**.\n   3. In the confirmation dialog, enter `external-secrets-operator` in the field and click **Delete**.\n\n## Removing External Secrets Operator for Red Hat OpenShift resources by using the CLI {#external-secrets-remove-resources-cli_external-secrets-operator-uninstall}\n\nAfter you have uninstalled the External Secrets Operator for Red Hat OpenShift, you can optionally eliminate its associated resources from your cluster by using the command-line interface (CLI).\n\n**Prerequisites**\n\n- You have access to the cluster with `cluster-admin` privileges.\n\n**Procedure**\n\n1. Delete the deployments of the `external-secrets` application components in the `external-secrets` namespace by running the following command:\n\n   ```terminal\n   $ oc delete deployment -n external-secrets -l app=external-secrets\n   ```\n2. Delete the custom resource definitions (CRDs) that were installed by the External Secrets Operator by running the following command:\n\n   ```terminal\n   $ oc delete customresourcedefinitions.apiextensions.k8s.io -l external-secrets.io/component=controller\n   ```\n3. Delete the `external-secrets-operator` namespace by running the following command:\n\n   ```terminal\n   $ oc delete project external-secrets-operator\n   ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/external_secrets_operator/external-secrets-operator-uninstall.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="external-secrets-operator-uninstall" class="group scroll-mt-20">Uninstalling the External Secrets Operator for Red Hat OpenShift<a href="#external-secrets-operator-uninstall" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">You can remove the External Secrets Operator for Red Hat OpenShift from OpenShift Container Platform by uninstalling the Operator and removing its related resources.</p>
      <h2 id="external-secrets-operator-uninstall-console_external-secrets-operator-uninstall" class="group scroll-mt-20">Uninstalling the External Secrets Operator for Red Hat OpenShift using the web console<a href="#external-secrets-operator-uninstall-console_external-secrets-operator-uninstall" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can uninstall the External Secrets Operator for Red Hat OpenShift from your cluster using the OpenShift Container Platform web console. Uninstalling the Operator does not automatically delete the <code>ExternalSecrets</code> custom resources or the running <code>external-secrets</code> application workload. These resources remain in the cluster to prevent accidental data loss and must be removed manually if they are no longer needed.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster with <code>cluster-admin</code> privileges.</li>
      <li>You have access to the OpenShift Container Platform web console.</li>
      <li>The External Secrets Operator is installed.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Log in to the OpenShift Container Platform web console.</li>
      <li>
      Uninstall the External Secrets Operator for Red Hat OpenShift using the following steps:
      <ol>
      <li>Navigate to <strong>Ecosystem</strong> → <strong>Installed Operators</strong>.</li>
      <li>Click the Options menu <img src="/openshift-docs-markdown/_assets/images/kebab.png" alt="" loading="lazy" /> next to the <strong>External Secrets Operator for Red Hat OpenShift</strong> entry and click <strong>Uninstall Operator</strong>.</li>
      <li>In the confirmation dialog, click <strong>Uninstall</strong>.</li>
      </ol>
      </li>
      </ol>
      <h2 id="external-secrets-remove-resources_external-secrets-operator-uninstall" class="group scroll-mt-20">Removing External Secrets Operator for Red Hat OpenShift resources by using the web console<a href="#external-secrets-remove-resources_external-secrets-operator-uninstall" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">After you have uninstalled the External Secrets Operator for Red Hat OpenShift, you can optionally eliminate its associated resources from your cluster.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster with <code>cluster-admin</code> privileges.</li>
      <li>You have access to the OpenShift Container Platform web console.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Log in to the OpenShift Container Platform web console.</li>
      <li>
      Remove the deployments of the <code>external-secrets</code> application components in the <code>external-secrets</code> namespace:
      <ol>
      <li>Click the <strong>Project</strong> drop-down menu to see a list of all available projects, and select the <strong>external-secrets</strong> project.</li>
      <li>Navigate to <strong>Workloads</strong> → <strong>Deployments</strong>.</li>
      <li>Select the deployment that you want to delete.</li>
      <li>Click the <strong>Actions</strong> drop-down menu, and select <strong>Delete Deployment</strong> to see a confirmation dialog box.</li>
      <li>Click <strong>Delete</strong> to delete the deployment.</li>
      </ol>
      </li>
      <li>
      Remove the custom resource definitions (CRDs) that were installed by the External Secrets Operator using the following steps:
      <ol>
      <li>Navigate to <strong>Administration</strong> → <strong>CustomResourceDefinitions</strong>.</li>
      <li>Choose <code>external-secrets.io/component: controller</code> from the suggestions in the <strong>Label</strong> field to filter the CRDs.</li>
      <li>
      Click the Options menu <img src="/openshift-docs-markdown/_assets/images/kebab.png" alt="" loading="lazy" /> next to each of the following CRDs, and select <strong>Delete Custom Resource Definition</strong>:
      <ul>
      <li>ACRAccessToken</li>
      <li>ClusterExternalSecret</li>
      <li>ClusterGenerator</li>
      <li>ClusterPushSecret</li>
      <li>ClusterSecretStore</li>
      <li>ECRAuthorizationToken</li>
      <li>ExternalSecret</li>
      <li>GCRAccessToken</li>
      <li>GeneratorState</li>
      <li>GithubAccessToken</li>
      <li>Grafana</li>
      <li>MFA</li>
      <li>Password</li>
      <li>PushSecret</li>
      <li>QuayAccessToken</li>
      <li>SecretStore</li>
      <li>SSHKey</li>
      <li>STSSessionToken</li>
      <li>UUID</li>
      <li>VaultDynamicSecret</li>
      <li>Webhook</li>
      </ul>
      </li>
      </ol>
      </li>
      <li>
      Remove the <code>external-secrets-operator</code> namespace using the following steps:
      <ol>
      <li>Navigate to <strong>Administration</strong> → <strong>Namespaces</strong>.</li>
      <li>Click the Options menu <img src="/openshift-docs-markdown/_assets/images/kebab.png" alt="" loading="lazy" /> next to the <strong>External Secrets Operator</strong> and select <strong>Delete Namespace</strong>.</li>
      <li>In the confirmation dialog, enter <code>external-secrets-operator</code> in the field and click <strong>Delete</strong>.</li>
      </ol>
      </li>
      </ol>
      <h2 id="external-secrets-remove-resources-cli_external-secrets-operator-uninstall" class="group scroll-mt-20">Removing External Secrets Operator for Red Hat OpenShift resources by using the CLI<a href="#external-secrets-remove-resources-cli_external-secrets-operator-uninstall" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">After you have uninstalled the External Secrets Operator for Red Hat OpenShift, you can optionally eliminate its associated resources from your cluster by using the command-line interface (CLI).</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You have access to the cluster with <code>cluster-admin</code> privileges.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Delete the deployments of the <code>external-secrets</code> application components in the <code>external-secrets</code> namespace by running the following command:
      <CodeBlock code={"$ oc delete deployment -n external-secrets -l app=external-secrets"} lang="terminal" />
      </li>
      <li>
      Delete the custom resource definitions (CRDs) that were installed by the External Secrets Operator by running the following command:
      <CodeBlock code={"$ oc delete customresourcedefinitions.apiextensions.k8s.io -l external-secrets.io/component=controller"} lang="terminal" />
      </li>
      <li>
      Delete the <code>external-secrets-operator</code> namespace by running the following command:
      <CodeBlock code={"$ oc delete project external-secrets-operator"} lang="terminal" />
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
