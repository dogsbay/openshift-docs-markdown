---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/external_secrets_operator/external-secrets-operator-config-net-policy.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"external-secrets-operator-config-net-policy","text":"Configuring network policy for the operand"},{"depth":2,"slug":"external-secrets-operator-egress-allow-all-traffic_external-secrets-operator-uninstall","text":"Adding a custom network policy to allow egress to all external providers"},{"depth":2,"slug":"external-secrets-operator-egress-specific-provider_external-secrets-operator-uninstall","text":"Adding a custom network policy to allow egress to a specific provider"},{"depth":2,"slug":"external-secrets-operator-ingress-egress-rules_external-secrets-operator-uninstall","text":"Default ingress and egress rules"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/external_secrets_operator/external-secrets-operator-config-net-policy";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring network policy for the operand";
const description = "The External Secrets Operator for Red Hat OpenShift for OpenShift Container Platform includes pre-defined NetworkPolicies for security that rejects all…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/external_secrets_operator"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring network policy for the operand {#external-secrets-operator-config-net-policy}\n\nThe External Secrets Operator for Red Hat OpenShift for OpenShift Container Platform includes pre-defined `NetworkPolicies` for security that rejects all egress traffic and allows traffic towards services that are required for the operand functionality. You must configure additional custom policies to allow the `external-secrets` controller to egress traffic towards external providers. These configurable policies are set through the `ExternalSecretsConfig` custom resource to establish the egress allow policy.\n\n## Adding a custom network policy to allow egress to all external providers {#external-secrets-operator-egress-allow-all-traffic_external-secrets-operator-uninstall}\n\nYou must configure custom policies through the `ExternalSecretsConfig` custom resource to allow all egress to all external providers.\n\n**Prerequisites**\n\n- An `ExternalSecretsConfig` must be predefined.\n- You must be able to define specific egress rules, including destination ports and protocols.\n\n**Procedure**\n\n1. Edit the `ExternalSecretsConfig` CR by running the following command:\n\n   ```terminal\n   $ oc edit externalsecretsconfigs.operator.openshift.io cluster\n   ```\n2. Set the policy by editing the `networkPolicies` section:\n\n   ```yaml\n   apiVersion: operator.openshift.io/v1alpha1\n   kind: ExternalSecretsConfig\n   metadata:\n     name: cluster\n   spec:\n     controllerConfig:\n       networkPolicies:\n         - name: allow-external-secrets-egress\n           componentName: CoreController\n           egress: # Allow all egress traffic\n   ```\n\n## Adding a custom network policy to allow egress to a specific provider {#external-secrets-operator-egress-specific-provider_external-secrets-operator-uninstall}\n\nYou must configure custom policies through the `ExternalSecretsConfig` custom resource to allow all egress to a specific provider.\n\n**Prerequisites**\n\n- An `ExternalSecretsConfig` must be predefined.\n- You must be able to define specific egress rules, including destination ports and protocols\n\n**Procedure**\n\n1. Edit the `ExternalSecretsConfig` CR by running the following command:\n\n   ```terminal\n   $ oc edit externalsecretsconfigs.operator.openshift.io cluster\n   ```\n2. Set the policy by editing the `networkPolicies` section. The following example shows how to allow egress to Amazon Web Services (AWS) endpoints.\n\n   ```yaml\n   apiVersion: operator.openshift.io/v1alpha1\n   kind: ExternalSecretsConfig\n   metadata:\n     name: cluster\n   spec:\n     controllerConfig:\n       networkPolicies:\n         - componentName: ExternalSecretsCoreController\n           egress:\n             # Allow egress to Kubernetes API server, AWS endpoints, and DNS\n             - ports:\n                 - port: 443   # HTTPS (AWS Secrets Manager)\n                   protocol: TCP\n         - name: allow-external-secrets-egress\n   ```\n\n   where:\n\n   componentName\n   :   Specifies the name for the core controller which is `ExternalSecretsCoreController`. Egress rules must specify the required ports, such as Transmission Control Protocol (TCP) port 443, for services such as the AWS Secrets Manager.\n\n## Default ingress and egress rules {#external-secrets-operator-ingress-egress-rules_external-secrets-operator-uninstall}\n\nThe ingress and egress rules are necessary to build a secure setup where every component acts with the least amount of privilege necessary. These rules protect your cluster by strictly blocking unnecessary traffic and only allowing the outbound connections needed to fetch secrets. They also permit the specific inbound connections required to validate webhooks and observe system performance.\n\nThe following table summarizes the specific ports and protocols used by each component.\n\n| Component | Ingress ports | Egress ports | Description |\n| --- | --- | --- | --- |\n| `external-secrets` | 8080 | 6443 | Allows retrieving metrics and interacting with the API server |\n| `external-secrets-webhook` | 8080/10250 | 6443 | Allows retrieving metrics, handling webhook requests, and interacting with the API server |\n| `external-secrets-cert-controller` | 8080 | 6443 | Allows retrieving metrics and interacting with the API server |\n| `external-secrets-bitwarden-server` | 9998 | 6443 | Handles Bitwarden server connections and interacts with the API server |\n| `external-secrets-allow-dns` |  | 5353 | Enables DNS lookups to find external secret providers. |\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/external_secrets_operator/external-secrets-operator-config-net-policy.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="external-secrets-operator-config-net-policy" class="group scroll-mt-20">Configuring network policy for the operand<a href="#external-secrets-operator-config-net-policy" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">The External Secrets Operator for Red Hat OpenShift for OpenShift Container Platform includes pre-defined <code>NetworkPolicies</code> for security that rejects all egress traffic and allows traffic towards services that are required for the operand functionality. You must configure additional custom policies to allow the <code>external-secrets</code> controller to egress traffic towards external providers. These configurable policies are set through the <code>ExternalSecretsConfig</code> custom resource to establish the egress allow policy.</p>
      <h2 id="external-secrets-operator-egress-allow-all-traffic_external-secrets-operator-uninstall" class="group scroll-mt-20">Adding a custom network policy to allow egress to all external providers<a href="#external-secrets-operator-egress-allow-all-traffic_external-secrets-operator-uninstall" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You must configure custom policies through the <code>ExternalSecretsConfig</code> custom resource to allow all egress to all external providers.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>An <code>ExternalSecretsConfig</code> must be predefined.</li>
      <li>You must be able to define specific egress rules, including destination ports and protocols.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Edit the <code>ExternalSecretsConfig</code> CR by running the following command:
      <CodeBlock code={"$ oc edit externalsecretsconfigs.operator.openshift.io cluster"} lang="terminal" />
      </li>
      <li>
      Set the policy by editing the <code>networkPolicies</code> section:
      <CodeBlock code={"apiVersion: operator.openshift.io/v1alpha1\nkind: ExternalSecretsConfig\nmetadata:\n  name: cluster\nspec:\n  controllerConfig:\n    networkPolicies:\n      - name: allow-external-secrets-egress\n        componentName: CoreController\n        egress: # Allow all egress traffic"} lang="yaml" />
      </li>
      </ol>
      <h2 id="external-secrets-operator-egress-specific-provider_external-secrets-operator-uninstall" class="group scroll-mt-20">Adding a custom network policy to allow egress to a specific provider<a href="#external-secrets-operator-egress-specific-provider_external-secrets-operator-uninstall" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You must configure custom policies through the <code>ExternalSecretsConfig</code> custom resource to allow all egress to a specific provider.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>An <code>ExternalSecretsConfig</code> must be predefined.</li>
      <li>You must be able to define specific egress rules, including destination ports and protocols</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Edit the <code>ExternalSecretsConfig</code> CR by running the following command:
      <CodeBlock code={"$ oc edit externalsecretsconfigs.operator.openshift.io cluster"} lang="terminal" />
      </li>
      <li>
      Set the policy by editing the <code>networkPolicies</code> section. The following example shows how to allow egress to Amazon Web Services (AWS) endpoints.
      <CodeBlock code={"apiVersion: operator.openshift.io/v1alpha1\nkind: ExternalSecretsConfig\nmetadata:\n  name: cluster\nspec:\n  controllerConfig:\n    networkPolicies:\n      - componentName: ExternalSecretsCoreController\n        egress:\n          # Allow egress to Kubernetes API server, AWS endpoints, and DNS\n          - ports:\n              - port: 443   # HTTPS (AWS Secrets Manager)\n                protocol: TCP\n      - name: allow-external-secrets-egress"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">componentName</dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name for the core controller which is <code>ExternalSecretsCoreController</code>. Egress rules must specify the required ports, such as Transmission Control Protocol (TCP) port 443, for services such as the AWS Secrets Manager.</p></dd>
      </dl>
      </li>
      </ol>
      <h2 id="external-secrets-operator-ingress-egress-rules_external-secrets-operator-uninstall" class="group scroll-mt-20">Default ingress and egress rules<a href="#external-secrets-operator-ingress-egress-rules_external-secrets-operator-uninstall" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The ingress and egress rules are necessary to build a secure setup where every component acts with the least amount of privilege necessary. These rules protect your cluster by strictly blocking unnecessary traffic and only allowing the outbound connections needed to fetch secrets. They also permit the specific inbound connections required to validate webhooks and observe system performance.</p>
      <p>The following table summarizes the specific ports and protocols used by each component.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Component</TableHead>
      <TableHead>Ingress ports</TableHead>
      <TableHead>Egress ports</TableHead>
      <TableHead>Description</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>external-secrets</code></TableCell>
      <TableCell>8080</TableCell>
      <TableCell>6443</TableCell>
      <TableCell>Allows retrieving metrics and interacting with the API server</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>external-secrets-webhook</code></TableCell>
      <TableCell>8080/10250</TableCell>
      <TableCell>6443</TableCell>
      <TableCell>Allows retrieving metrics, handling webhook requests, and interacting with the API server</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>external-secrets-cert-controller</code></TableCell>
      <TableCell>8080</TableCell>
      <TableCell>6443</TableCell>
      <TableCell>Allows retrieving metrics and interacting with the API server</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>external-secrets-bitwarden-server</code></TableCell>
      <TableCell>9998</TableCell>
      <TableCell>6443</TableCell>
      <TableCell>Handles Bitwarden server connections and interacts with the API server</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>external-secrets-allow-dns</code></TableCell>
      <TableCell></TableCell>
      <TableCell>5353</TableCell>
      <TableCell>Enables DNS lookups to find external secret providers.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
