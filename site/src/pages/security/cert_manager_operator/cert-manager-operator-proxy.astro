---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/cert_manager_operator/cert-manager-operator-proxy.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"cert-manager-operator-proxy","text":"Configuring the egress proxy for the {{ cert_manager_operator }}"},{"depth":2,"slug":"cert-manager-proxy-support_cert-manager-operator-proxy","text":"Injecting a custom CA certificate for the cert-manager Operator for Red Hat OpenShift"},{"depth":2,"slug":"cert-manager-operator-proxy_additional-resources","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/cert_manager_operator/cert-manager-operator-proxy";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring the egress proxy for the {{ cert_manager_operator }}";
const description = "If a cluster-wide egress proxy is configured in OpenShift Container Platform, Operator Lifecycle Manager (OLM) automatically configures Operators that it…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/cert_manager_operator"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring the egress proxy for the {{ cert_manager_operator }} {#cert-manager-operator-proxy}\n\nIf a cluster-wide egress proxy is configured in OpenShift Container Platform, Operator Lifecycle Manager (OLM) automatically configures Operators that it manages with the cluster-wide proxy. OLM automatically updates all of the Operator’s deployments with the `HTTP_PROXY`, `HTTPS_PROXY`, `NO_PROXY` environment variables.\n\nYou can inject any CA certificates that are required for proxying HTTPS connections into the cert-manager Operator for Red Hat OpenShift.\n\n## Injecting a custom CA certificate for the cert-manager Operator for Red Hat OpenShift {#cert-manager-proxy-support_cert-manager-operator-proxy}\n\nIf your OpenShift Container Platform cluster has the cluster-wide proxy enabled, you can inject any CA certificates that are required for proxying HTTPS connections into the cert-manager Operator for Red Hat OpenShift.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- You have enabled the cluster-wide proxy for OpenShift Container Platform.\n\n**Procedure**\n\n1. Create a config map in the `cert-manager` namespace by running the following command:\n\n   ```terminal\n   $ oc create configmap trusted-ca -n cert-manager\n   ```\n2. Inject the CA bundle that is trusted by OpenShift Container Platform into the config map by running the following command:\n\n   ```terminal\n   $ oc label cm trusted-ca config.openshift.io/inject-trusted-cabundle=true -n cert-manager\n   ```\n3. Update the deployment for the cert-manager Operator for Red Hat OpenShift to use the config map by running the following command:\n\n   ```terminal\n   $ oc -n cert-manager-operator patch subscription openshift-cert-manager-operator --type='merge' -p '{\"spec\":{\"config\":{\"env\":[{\"name\":\"TRUSTED_CA_CONFIGMAP_NAME\",\"value\":\"trusted-ca\"}]}}}'\n   ```\n\n**Verification**\n\n1. Verify that the deployments have finished rolling out by running the following command:\n\n   ```terminal\n   $ oc rollout status deployment/cert-manager-operator-controller-manager -n cert-manager-operator && \\\n   oc rollout status deployment/cert-manager -n cert-manager && \\\n   oc rollout status deployment/cert-manager-webhook -n cert-manager && \\\n   oc rollout status deployment/cert-manager-cainjector -n cert-manager\n   ```\n\n   ```terminal {title=\"Example output\"}\n   deployment \"cert-manager-operator-controller-manager\" successfully rolled out\n   deployment \"cert-manager\" successfully rolled out\n   deployment \"cert-manager-webhook\" successfully rolled out\n   deployment \"cert-manager-cainjector\" successfully rolled out\n   ```\n2. Verify that the CA bundle was mounted as a volume by running the following command:\n\n   ```terminal\n   $ oc get deployment cert-manager -n cert-manager -o=jsonpath={.spec.template.spec.'containers[0].volumeMounts'}\n   ```\n\n   ```terminal {title=\"Example output\"}\n   [{\"mountPath\":\"/etc/pki/tls/certs/cert-manager-tls-ca-bundle.crt\",\"name\":\"trusted-ca\",\"subPath\":\"ca-bundle.crt\"}]\n   ```\n3. Verify that the source of the CA bundle is the `trusted-ca` config map by running the following command:\n\n   ```terminal\n   $ oc get deployment cert-manager -n cert-manager -o=jsonpath={.spec.template.spec.volumes}\n   ```\n\n   ```terminal {title=\"Example output\"}\n   [{\"configMap\":{\"defaultMode\":420,\"name\":\"trusted-ca\"},\"name\":\"trusted-ca\"}]\n   ```\n\n## Additional resources {#cert-manager-operator-proxy_additional-resources}\n\n- [Configuring proxy support in Operator Lifecycle Manager](/openshift-docs-markdown/operators/admin/olm-configuring-proxy-support#olm-configuring-proxy-support)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/cert_manager_operator/cert-manager-operator-proxy.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="cert-manager-operator-proxy" class="group scroll-mt-20">Configuring the egress proxy for the {"{"}{"{"} cert_manager_operator {"}"}{"}"}<a href="#cert-manager-operator-proxy" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>If a cluster-wide egress proxy is configured in OpenShift Container Platform, Operator Lifecycle Manager (OLM) automatically configures Operators that it manages with the cluster-wide proxy. OLM automatically updates all of the Operator’s deployments with the <code>HTTP_PROXY</code>, <code>HTTPS_PROXY</code>, <code>NO_PROXY</code> environment variables.</p>
      <p>You can inject any CA certificates that are required for proxying HTTPS connections into the cert-manager Operator for Red Hat OpenShift.</p>
      <h2 id="cert-manager-proxy-support_cert-manager-operator-proxy" class="group scroll-mt-20">Injecting a custom CA certificate for the cert-manager Operator for Red Hat OpenShift<a href="#cert-manager-proxy-support_cert-manager-operator-proxy" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">If your OpenShift Container Platform cluster has the cluster-wide proxy enabled, you can inject any CA certificates that are required for proxying HTTPS connections into the cert-manager Operator for Red Hat OpenShift.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>You have enabled the cluster-wide proxy for OpenShift Container Platform.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a config map in the <code>cert-manager</code> namespace by running the following command:
      <CodeBlock code={"$ oc create configmap trusted-ca -n cert-manager"} lang="terminal" />
      </li>
      <li>
      Inject the CA bundle that is trusted by OpenShift Container Platform into the config map by running the following command:
      <CodeBlock code={"$ oc label cm trusted-ca config.openshift.io/inject-trusted-cabundle=true -n cert-manager"} lang="terminal" />
      </li>
      <li>
      Update the deployment for the cert-manager Operator for Red Hat OpenShift to use the config map by running the following command:
      <CodeBlock code={"$ oc -n cert-manager-operator patch subscription openshift-cert-manager-operator --type='merge' -p '{\"spec\":{\"config\":{\"env\":[{\"name\":\"TRUSTED_CA_CONFIGMAP_NAME\",\"value\":\"trusted-ca\"}]}}}'"} lang="terminal" />
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>
      Verify that the deployments have finished rolling out by running the following command:
      <CodeBlock code={"$ oc rollout status deployment/cert-manager-operator-controller-manager -n cert-manager-operator && \\\noc rollout status deployment/cert-manager -n cert-manager && \\\noc rollout status deployment/cert-manager-webhook -n cert-manager && \\\noc rollout status deployment/cert-manager-cainjector -n cert-manager"} lang="terminal" />
      <CodeBlock code={"deployment \"cert-manager-operator-controller-manager\" successfully rolled out\ndeployment \"cert-manager\" successfully rolled out\ndeployment \"cert-manager-webhook\" successfully rolled out\ndeployment \"cert-manager-cainjector\" successfully rolled out"} lang="terminal" title="Example output" />
      </li>
      <li>
      Verify that the CA bundle was mounted as a volume by running the following command:
      <CodeBlock code={"$ oc get deployment cert-manager -n cert-manager -o=jsonpath={.spec.template.spec.'containers[0].volumeMounts'}"} lang="terminal" />
      <CodeBlock code={"[{\"mountPath\":\"/etc/pki/tls/certs/cert-manager-tls-ca-bundle.crt\",\"name\":\"trusted-ca\",\"subPath\":\"ca-bundle.crt\"}]"} lang="terminal" title="Example output" />
      </li>
      <li>
      Verify that the source of the CA bundle is the <code>trusted-ca</code> config map by running the following command:
      <CodeBlock code={"$ oc get deployment cert-manager -n cert-manager -o=jsonpath={.spec.template.spec.volumes}"} lang="terminal" />
      <CodeBlock code={"[{\"configMap\":{\"defaultMode\":420,\"name\":\"trusted-ca\"},\"name\":\"trusted-ca\"}]"} lang="terminal" title="Example output" />
      </li>
      </ol>
      <h2 id="cert-manager-operator-proxy_additional-resources" class="group scroll-mt-20">Additional resources<a href="#cert-manager-operator-proxy_additional-resources" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul><li><a href="/openshift-docs-markdown/operators/admin/olm-configuring-proxy-support#olm-configuring-proxy-support">Configuring proxy support in Operator Lifecycle Manager</a></li></ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
