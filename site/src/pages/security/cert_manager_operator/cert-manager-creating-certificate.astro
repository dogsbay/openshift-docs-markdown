---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/cert_manager_operator/cert-manager-creating-certificate.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"cert-manager-creating-certificate","text":"Configuring certificates with an issuer"},{"depth":2,"slug":"cert-manager-certificate-mgmt_cert-manager-creating-certificate","text":"Creating certificates for user workloads"},{"depth":2,"slug":"cert-manager-certificate-api-server_cert-manager-creating-certificate","text":"Creating certificates for the API server"},{"depth":2,"slug":"cert-manager-certificate-ingress_cert-manager-creating-certificate","text":"Creating certificates for the Ingress Controller"},{"depth":2,"slug":"additional-resources_cert-manager-creating-certificate","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/cert_manager_operator/cert-manager-creating-certificate";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring certificates with an issuer";
const description = "By using the cert-manager Operator for Red Hat OpenShift, you can manage certificates, handling tasks such as renewal and issuance, for workloads within…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/cert_manager_operator"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring certificates with an issuer {#cert-manager-creating-certificate}\n\nBy using the cert-manager Operator for Red Hat OpenShift, you can manage certificates, handling tasks such as renewal and issuance, for workloads within the cluster, as well as components interacting externally to the cluster.\n\n## Creating certificates for user workloads {#cert-manager-certificate-mgmt_cert-manager-creating-certificate}\n\nTo secure communications for your applications, create and manage TLS certificates for your workloads by using the cert-manager Operator for Red Hat OpenShift\n\n**Prerequisites**\n\n- You have access to the cluster with `cluster-admin` privileges.\n- You have installed the cert-manager Operator for Red Hat OpenShift.\n\n**Procedure**\n\n1. Create an issuer. For more information, see \"Configuring an issuer\" in the \"Additional resources\" section.\n2. Create a certificate:\n\n   1. Create a YAML file, for example, `certificate.yaml`, that defines the `Certificate` object:\n\n      ```yaml\n      apiVersion: cert-manager.io/v1\n      kind: Certificate\n      metadata:\n        name: <tls_cert>\n        namespace: <issuer_namespace>\n      spec:\n        isCA: false\n        commonName: '<common_name>'\n        secretName: <secret_name>\n        dnsNames:\n        - \"<domain_name>\"\n        issuerRef:\n          name: <issuer_name>\n          kind: Issuer\n      ```\n\n      where:\n\n      `<tls_cert>`\n      :   Specifies a name for the certificate.\n\n      `<issuer_namespace>`\n      :   Specifies the namespace of the issuer.\n\n      `<common_name>`\n      :   Specifies the common name (CN).\n\n      `<secret_name>`\n      :   Specifies the name of the secret to create that contains the certificate.\n\n      `<domain_name>`\n      :   Specifies the domain name.\n\n      `<issuer_name>`\n      :   Specifies the name of the issuer.\n   2. Create the `Certificate` object by running the following command:\n\n      ```terminal\n      $ oc create -f certificate.yaml\n      ```\n\n**Verification**\n\n- Verify that the certificate is created and ready to use by running the following command:\n\n  ```terminal\n  $ oc get certificate -w -n <issuer_namespace>\n  ```\n\n  Once certificate is in `Ready` status, workloads on your cluster can start using the generated certificate secret.\n\n## Creating certificates for the API server {#cert-manager-certificate-api-server_cert-manager-creating-certificate}\n\nTo secure interactions with the cluster control plane, create TLS certificates for the API server by using the cert-manager Operator for Red Hat OpenShift.\n\n**Prerequisites**\n\n- You have access to the cluster with `cluster-admin` privileges.\n- You have installed version 1.13.0 or later of the cert-manager Operator for Red Hat OpenShift.\n\n**Procedure**\n\n1. Create an issuer. For more information, see \"Configuring an issuer\" in the \"Additional resources\" section.\n2. Create a certificate:\n\n   1. Create a YAML file, for example, `certificate.yaml`, that defines the `Certificate` object:\n\n      ```yaml\n      apiVersion: cert-manager.io/v1\n      kind: Certificate\n      metadata:\n        name: <tls_cert>\n        namespace: openshift-config\n      spec:\n        isCA: false\n        commonName: \"api.<cluster_base_domain>\"\n        secretName: <secret_name>\n        dnsNames:\n        - \"api.<cluster_base_domain>\"\n        issuerRef:\n          name: <issuer_name>\n          kind: Issuer\n      ```\n\n      where:\n\n      `<tls_cert>`\n      :   Specifies a name for the certificate.\n\n      `<cluster_base_domain>`\n      :   Specifies the common name (CN).\n\n      `<secret_name>`\n      :   Specifies the name of the secret to create that contains the certificate.\n\n      `<issuer_name>`\n      :   Specifies the name of the issuer.\n   2. Create the `Certificate` object by running the following command:\n\n      ```terminal\n      $ oc create -f certificate.yaml\n      ```\n3. Add the API server named certificate. For more information, see \"Adding an API server named certificate\" section in the \"Additional resources\" section.\n\n   > [!NOTE]\n   > To ensure the certificates are updated, run the `oc login` command again after the certificate is created.\n\n**Verification**\n\n- Verify that the certificate is created and ready to use by running the following command:\n\n  ```terminal\n  $ oc get certificate -w -n openshift-config\n  ```\n\n  Once certificate is in `Ready` status, API server on your cluster can start using the generated certificate secret.\n\n## Creating certificates for the Ingress Controller {#cert-manager-certificate-ingress_cert-manager-creating-certificate}\n\nYou can create a certificate for the Ingress Controller and then replace bootstrapped default self-signed certificates with cert-manager-managed external certificates.\n\n> [!NOTE]\n> Before using the procedure, ensure you understand the following Ingress Controller behaviors:\n>\n> - When certificates are renewed or rotated by using the cert-manager Operator, only the contents of the secret, such as the certificate and key, are updated. The secret name remains unchanged. Kubelet automatically propagates these updates to the mounted volume, allowing the router to detect the file changes and hot-reload the new certificate and key. As a result, no rolling update of the router deployment is triggered or required.\n> - The secret name is referenced in the Ingress Controller configuration. If you want to replace the default ingress certificate or use different secret name in Ingress Controller configuration, you must patch or edit the configuration to apply the change. This operation triggers a rolling update for router pods where new router pods load the new cert/key pair.\n\n**Prerequisites**\n\n- You have access to the cluster with `cluster-admin` privileges.\n- You have installed version 1.13.0 or later of the cert-manager Operator for Red Hat OpenShift.\n\n**Procedure**\n\n1. Create an issuer. For more information, see \"Configuring an issuer\" in the \"Additional resources\" section.\n2. Create a certificate:\n\n   1. Create a YAML file, for example, `certificate.yaml`, that defines the `Certificate` object:\n\n      ```yaml {title=\"Example certificate.yaml file\"}\n      apiVersion: cert-manager.io/v1\n      kind: Certificate\n      metadata:\n        name: <tls_cert>\n        namespace: openshift-ingress\n      spec:\n        isCA: false\n        commonName: \"apps.<cluster_base_domain>\"\n        secretName: <secret_name>\n        dnsNames:\n        - \"apps.<cluster_base_domain>\"\n        - \"\\*.apps.<cluster_base_domain>\"\n        issuerRef:\n          name: <issuer_name>\n          kind: Issuer\n      ```\n\n      where:\n\n      `<tls_cert>`\n      :   Specifies the name for the certificate.\n\n      `<cluster_base_domain>`\n      :   Specifies the common name (CN).\n\n      `<secret_name>`\n      :   Specifies the name of the secret to create that contains the certificate.\n\n      `<cluster_base_domain>`\n      :   Specifies the DNS name of the ingress.\n\n      `<issuer_name>`\n      :   Specifies the name of the issuer.\n   2. Create the `Certificate` object by running the following command:\n\n      ```terminal\n      $ oc create -f certificate.yaml\n      ```\n3. Replace the default ingress certificate. For more information, see \"Replacing the default ingress certificate\" section in the \"Additional resources\" section.\n\n**Verification**\n\n1. Verify that the certificate is created and ready to use by running the following command:\n\n   ```terminal\n   $ oc get certificate -n openshift-ingress\n   ```\n2. Verify the definition and content of the secret object by running the following command:\n\n   ```terminal\n   $ oc get secret <secretName> -n openshift-ingress\n   ```\n3. Verify that the default TLS certificate has the correct configuration details for the Ingress Controller by running the following command:\n\n   ```terminal\n   $ oc get ingresscontroller default -n openshift-ingress-operator -o yaml | grep -A2 defaultCertificate\n   ```\n\n   After the certificate is in `Ready` status, the Ingress Controller on your cluster can start using the generated certificate secret.\n\n**Additional resources**\n\n- [Red Hat Knowledgebase solution](https://access.redhat.com/solutions/4542531)\n\n## Additional resources {#additional-resources_cert-manager-creating-certificate}\n\n- [Supported issuer types](/openshift-docs-markdown/security/cert_manager_operator/index#cert-manager-issuer-types_cert-manager-operator-about)\n- [Configuring an ACME issuer](/openshift-docs-markdown/security/cert_manager_operator/cert-manager-operator-issuer-acme#cert-manager-operator-issuer-acme)\n- [Adding an API server named certificate](/openshift-docs-markdown/security/certificates/api-server#customize-certificates-api-add-named_api-server-certificates)\n- [Replacing the default ingress certificate](/openshift-docs-markdown/security/certificates/replacing-default-ingress-certificate#replacing-default-ingress)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/cert_manager_operator/cert-manager-creating-certificate.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="cert-manager-creating-certificate" class="group scroll-mt-20">Configuring certificates with an issuer<a href="#cert-manager-creating-certificate" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>By using the cert-manager Operator for Red Hat OpenShift, you can manage certificates, handling tasks such as renewal and issuance, for workloads within the cluster, as well as components interacting externally to the cluster.</p>
      <h2 id="cert-manager-certificate-mgmt_cert-manager-creating-certificate" class="group scroll-mt-20">Creating certificates for user workloads<a href="#cert-manager-certificate-mgmt_cert-manager-creating-certificate" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To secure communications for your applications, create and manage TLS certificates for your workloads by using the cert-manager Operator for Red Hat OpenShift</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster with <code>cluster-admin</code> privileges.</li>
      <li>You have installed the cert-manager Operator for Red Hat OpenShift.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Create an issuer. For more information, see "Configuring an issuer" in the "Additional resources" section.</li>
      <li>
      Create a certificate:
      <ol>
      <li>
      Create a YAML file, for example, <code>certificate.yaml</code>, that defines the <code>Certificate</code> object:
      <CodeBlock code={"apiVersion: cert-manager.io/v1\nkind: Certificate\nmetadata:\n  name: <tls_cert>\n  namespace: <issuer_namespace>\nspec:\n  isCA: false\n  commonName: '<common_name>'\n  secretName: <secret_name>\n  dnsNames:\n  - \"<domain_name>\"\n  issuerRef:\n    name: <issuer_name>\n    kind: Issuer"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;tls_cert&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a name for the certificate.</p></dd>
      <dt class="font-semibold"><code>&lt;issuer_namespace&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the namespace of the issuer.</p></dd>
      <dt class="font-semibold"><code>&lt;common_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the common name (CN).</p></dd>
      <dt class="font-semibold"><code>&lt;secret_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the secret to create that contains the certificate.</p></dd>
      <dt class="font-semibold"><code>&lt;domain_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the domain name.</p></dd>
      <dt class="font-semibold"><code>&lt;issuer_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the issuer.</p></dd>
      </dl>
      </li>
      <li>
      Create the <code>Certificate</code> object by running the following command:
      <CodeBlock code={"$ oc create -f certificate.yaml"} lang="terminal" />
      </li>
      </ol>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      Verify that the certificate is created and ready to use by running the following command:
      <CodeBlock code={"$ oc get certificate -w -n <issuer_namespace>"} lang="terminal" />
      <p>Once certificate is in <code>Ready</code> status, workloads on your cluster can start using the generated certificate secret.</p>
      </li>
      </ul>
      <h2 id="cert-manager-certificate-api-server_cert-manager-creating-certificate" class="group scroll-mt-20">Creating certificates for the API server<a href="#cert-manager-certificate-api-server_cert-manager-creating-certificate" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To secure interactions with the cluster control plane, create TLS certificates for the API server by using the cert-manager Operator for Red Hat OpenShift.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster with <code>cluster-admin</code> privileges.</li>
      <li>You have installed version 1.13.0 or later of the cert-manager Operator for Red Hat OpenShift.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Create an issuer. For more information, see "Configuring an issuer" in the "Additional resources" section.</li>
      <li>
      Create a certificate:
      <ol>
      <li>
      Create a YAML file, for example, <code>certificate.yaml</code>, that defines the <code>Certificate</code> object:
      <CodeBlock code={"apiVersion: cert-manager.io/v1\nkind: Certificate\nmetadata:\n  name: <tls_cert>\n  namespace: openshift-config\nspec:\n  isCA: false\n  commonName: \"api.<cluster_base_domain>\"\n  secretName: <secret_name>\n  dnsNames:\n  - \"api.<cluster_base_domain>\"\n  issuerRef:\n    name: <issuer_name>\n    kind: Issuer"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;tls_cert&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a name for the certificate.</p></dd>
      <dt class="font-semibold"><code>&lt;cluster_base_domain&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the common name (CN).</p></dd>
      <dt class="font-semibold"><code>&lt;secret_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the secret to create that contains the certificate.</p></dd>
      <dt class="font-semibold"><code>&lt;issuer_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the issuer.</p></dd>
      </dl>
      </li>
      <li>
      Create the <code>Certificate</code> object by running the following command:
      <CodeBlock code={"$ oc create -f certificate.yaml"} lang="terminal" />
      </li>
      </ol>
      </li>
      <li>
      Add the API server named certificate. For more information, see "Adding an API server named certificate" section in the "Additional resources" section.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>To ensure the certificates are updated, run the <code>oc login</code> command again after the certificate is created.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul>
      <li>
      Verify that the certificate is created and ready to use by running the following command:
      <CodeBlock code={"$ oc get certificate -w -n openshift-config"} lang="terminal" />
      <p>Once certificate is in <code>Ready</code> status, API server on your cluster can start using the generated certificate secret.</p>
      </li>
      </ul>
      <h2 id="cert-manager-certificate-ingress_cert-manager-creating-certificate" class="group scroll-mt-20">Creating certificates for the Ingress Controller<a href="#cert-manager-certificate-ingress_cert-manager-creating-certificate" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can create a certificate for the Ingress Controller and then replace bootstrapped default self-signed certificates with cert-manager-managed external certificates.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Before using the procedure, ensure you understand the following Ingress Controller behaviors:</p>
          <ul>
          <li>When certificates are renewed or rotated by using the cert-manager Operator, only the contents of the secret, such as the certificate and key, are updated. The secret name remains unchanged. Kubelet automatically propagates these updates to the mounted volume, allowing the router to detect the file changes and hot-reload the new certificate and key. As a result, no rolling update of the router deployment is triggered or required.</li>
          <li>The secret name is referenced in the Ingress Controller configuration. If you want to replace the default ingress certificate or use different secret name in Ingress Controller configuration, you must patch or edit the configuration to apply the change. This operation triggers a rolling update for router pods where new router pods load the new cert/key pair.</li>
          </ul>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster with <code>cluster-admin</code> privileges.</li>
      <li>You have installed version 1.13.0 or later of the cert-manager Operator for Red Hat OpenShift.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Create an issuer. For more information, see "Configuring an issuer" in the "Additional resources" section.</li>
      <li>
      Create a certificate:
      <ol>
      <li>
      Create a YAML file, for example, <code>certificate.yaml</code>, that defines the <code>Certificate</code> object:
      <CodeBlock code={"apiVersion: cert-manager.io/v1\nkind: Certificate\nmetadata:\n  name: <tls_cert>\n  namespace: openshift-ingress\nspec:\n  isCA: false\n  commonName: \"apps.<cluster_base_domain>\"\n  secretName: <secret_name>\n  dnsNames:\n  - \"apps.<cluster_base_domain>\"\n  - \"\\*.apps.<cluster_base_domain>\"\n  issuerRef:\n    name: <issuer_name>\n    kind: Issuer"} lang="yaml" title="Example certificate.yaml file" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;tls_cert&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name for the certificate.</p></dd>
      <dt class="font-semibold"><code>&lt;cluster_base_domain&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the common name (CN).</p></dd>
      <dt class="font-semibold"><code>&lt;secret_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the secret to create that contains the certificate.</p></dd>
      <dt class="font-semibold"><code>&lt;cluster_base_domain&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the DNS name of the ingress.</p></dd>
      <dt class="font-semibold"><code>&lt;issuer_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the issuer.</p></dd>
      </dl>
      </li>
      <li>
      Create the <code>Certificate</code> object by running the following command:
      <CodeBlock code={"$ oc create -f certificate.yaml"} lang="terminal" />
      </li>
      </ol>
      </li>
      <li>Replace the default ingress certificate. For more information, see "Replacing the default ingress certificate" section in the "Additional resources" section.</li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>
      Verify that the certificate is created and ready to use by running the following command:
      <CodeBlock code={"$ oc get certificate -n openshift-ingress"} lang="terminal" />
      </li>
      <li>
      Verify the definition and content of the secret object by running the following command:
      <CodeBlock code={"$ oc get secret <secretName> -n openshift-ingress"} lang="terminal" />
      </li>
      <li>
      Verify that the default TLS certificate has the correct configuration details for the Ingress Controller by running the following command:
      <CodeBlock code={"$ oc get ingresscontroller default -n openshift-ingress-operator -o yaml | grep -A2 defaultCertificate"} lang="terminal" />
      <p>After the certificate is in <code>Ready</code> status, the Ingress Controller on your cluster can start using the generated certificate secret.</p>
      </li>
      </ol>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="https://access.redhat.com/solutions/4542531">Red Hat Knowledgebase solution</a></li></ul>
      <h2 id="additional-resources_cert-manager-creating-certificate" class="group scroll-mt-20">Additional resources<a href="#additional-resources_cert-manager-creating-certificate" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/security/cert_manager_operator/index#cert-manager-issuer-types_cert-manager-operator-about">Supported issuer types</a></li>
      <li><a href="/openshift-docs-markdown/security/cert_manager_operator/cert-manager-operator-issuer-acme#cert-manager-operator-issuer-acme">Configuring an ACME issuer</a></li>
      <li><a href="/openshift-docs-markdown/security/certificates/api-server#customize-certificates-api-add-named_api-server-certificates">Adding an API server named certificate</a></li>
      <li><a href="/openshift-docs-markdown/security/certificates/replacing-default-ingress-certificate#replacing-default-ingress">Replacing the default ingress certificate</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
