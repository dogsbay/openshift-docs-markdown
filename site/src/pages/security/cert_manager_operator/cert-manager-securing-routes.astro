---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/cert_manager_operator/cert-manager-securing-routes.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"cert-manager-securing-routes","text":"Securing routes with the {{ cert_manager_operator }}"},{"depth":2,"slug":"cert-manager-configuring-routes_cert-manager-securing-routes","text":"Configuring certificates to secure routes in your cluster"},{"depth":2,"slug":"additional-resources_cert-manager-securing-routes","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/cert_manager_operator/cert-manager-securing-routes";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Securing routes with the {{ cert_manager_operator }}";
const description = "In the OpenShift Container Platform, the route API is extended to provide a configurable option to reference TLS certificates via secrets. With externally…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/cert_manager_operator"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Securing routes with the {{ cert_manager_operator }} {#cert-manager-securing-routes}\n\nIn the OpenShift Container Platform, the route API is extended to provide a configurable option to reference TLS certificates via secrets. With externally managed certificates enabled, you can minimize errors from manual intervention, streamline the certificate management process, and enable the OpenShift Container Platform router to promptly serve the referenced certificate.\n\n## Configuring certificates to secure routes in your cluster {#cert-manager-configuring-routes_cert-manager-securing-routes}\n\nTo encrypt traffic between external clients and your applications, configure certificates for routes in your OpenShift Container Platform cluster. You can secure your routes by defining TLS termination types, such as edge, passthrough, or re-encrypt, to match your specific security policies.\n\n**Prerequisites**\n\n- You have installed version 1.14.0 or later of the cert-manager Operator for Red Hat OpenShift.\n- You have `create` permission on the `routes/custom-host` sub-resource, which is used for both creating and updating routes.\n- You have a `Service` resource that you want to expose.\n\n**Procedure**\n\n1. Create an `Issuer` to configure the HTTP-01 solver by running the following command. For other ACME issuer types, see \"Configuring ACME an issuer\".\n\n   ```yaml\n   $ oc create -f - << EOF\n   apiVersion: cert-manager.io/v1\n   kind: Issuer\n   metadata:\n     name: letsencrypt-acme\n     namespace: <namespace>\n   spec:\n     acme:\n       server: https://acme-v02.api.letsencrypt.org/directory\n       privateKeySecretRef:\n         name: letsencrypt-acme-account-key\n       solvers:\n         - http01:\n             ingress:\n               ingressClassName: openshift-default\n   EOF\n   ```\n\n   where:\n\n   `<namespace>`\n   :   Specifies the namespace where the `Issuer` is located. It must be the same as the route namespace.\n2. Create a `Certificate` object for the route by running the following command. The `secretName` specifies the TLS secret that is going to be issued and managed by cert-manager and will also be referenced in your route in the following steps.\n\n   ```yaml\n   $ oc create -f - << EOF\n   apiVersion: cert-manager.io/v1\n   kind: Certificate\n   metadata:\n     name: example-route-cert\n     namespace: <namespace>\n   spec:\n     commonName: <common_host>\n     dnsNames:\n       - <hostname>\n     usages:\n       - server auth\n     issuerRef:\n       kind: Issuer\n       name: letsencrypt-acme\n     secretName: <secret_name>\n   EOF\n   ```\n\n   where:\n\n   `<namespace>`\n   :   Specifies the `namespace` where the `Certificate` resource is located. It should be the same as the namespace of your route.\n\n   `<common_host>`\n   :   Specifies the common name of your certificate by using the hostname of the route.\n\n   `<hostname>`\n   :   Specifies the hostname of your route to the DNS names of your certificate.\n\n   `<secret_name>`\n   :   Specifies the name of the secret that contains the certificate.\n3. Create a `Role` to provide the router service account permissions to read the referenced secret by using the following command:\n\n   ```terminal\n   $ oc create role secret-reader \\\n     --verb=get,list,watch \\\n     --resource=secrets \\\n     --resource-name=<secret_name> \\\n     --namespace=<namespace>\n   ```\n\n   where:\n\n   `<secret_name>`\n   :   Specifies the name of the secret that you want to grant access to. It should be consistent with your `secretName` specified in the `Certificate` resource.\n\n   `<namespace>`\n   :   Specifies the namespace where both your secret and route are located.\n4. Create a `RoleBinding` resource to bind the router service account with the newly created `Role` resource by using the following command:\n\n   ```terminal\n   $ oc create rolebinding secret-reader-binding \\\n     --role=secret-reader \\\n     --serviceaccount=openshift-ingress:router \\\n     --namespace=<namespace>\n   ```\n\n   where:\n\n   `<namespace>`\n   :   Specifies the namespace where both your secret and route are located.\n5. Create a route for your service resource, that uses edge TLS termination and a custom hostname, by running the following command. The hostname is used when creating a `Certificate` resource in the next step.\n\n   ```terminal\n   $ oc create route edge <route_name> \\\n     --service=<service_name> \\\n     --hostname=<hostname> \\\n     --namespace=<namespace>\n   ```\n\n   where:\n\n   `<route_name>`\n   :   Specifies the name of your route.\n\n   `<service_name>`\n   :   Specifies the service you want to expose.\n\n   `<hostname>`\n   :   Specifies the hostname of your route.\n\n   `<namespace>`\n   :   Specifies the namespace where your route is located.\n6. To reference the secret and use the certificate issued by `cert-manager`, update the `.spec.tls.externalCertificate` field in the route by using the following command:\n\n   ```terminal\n   $ oc patch route <route_name> \\\n     -n <namespace> \\\n     --type=merge \\\n     -p '{\"spec\":{\"tls\":{\"externalCertificate\":{\"name\":\"<secret_name>\"}}}}'\n   ```\n\n   where:\n\n   `<route_name>`\n   :   Specifies the name of your route.\n\n   `<namespace>`\n   :   Specifies the namespace where both your secret and route are located.\n\n   `<secret_name>`\n   :   Specifies the name of the secret that contains the certificate.\n\n**Verification**\n\n1. Verify that the certificate is created and ready to use by running the following command:\n\n   ```terminal\n   $ oc get certificate -n <namespace>\n   $ oc get secret -n <namespace>\n   ```\n\n   where:\n\n   `<namespace>`\n   :   Specifies the namespace where both your secret and route are located.\n2. Verify that the router is using the referenced external certificate by running the following command. The command should return with the status code `200 OK`.\n\n   ```terminal\n   $ curl -IsS https://<hostname>\n   ```\n\n   where:\n\n   `<hostname>`\n   :   Specifies the hostname of your route.\n3. Verify the `subject`, `subjectAltName`, and `issuer` fields of your server certificate are all as expected from the curl verbose outputs by running the following command:\n\n   ```terminal\n   $ curl -v https://<hostname>\n   ```\n\n   where:\n\n   `<hostname>`\n   :   Specifies the hostname of your route.\n\nThe certificate from the referenced secret secures the route. The `cert-manager` component issues the certificate and automatically manages the certificate lifecycle.\n\n## Additional resources {#additional-resources_cert-manager-securing-routes}\n\n- [Creating a route with externally managed certificate](/openshift-docs-markdown/networking/ingress_load_balancing/routes/nw-configuring-routes#nw-ingress-route-secret-load-external-cert_secured-routes)\n- [Configuring an ACME issuer](/openshift-docs-markdown/security/cert_manager_operator/cert-manager-operator-issuer-acme#cert-manager-operator-issuer-acme)\n- [Externally managed certificates](/openshift-docs-markdown/networking/ingress_load_balancing/routes/securing-routes#nw-ingress-route-secret-load-external-cert_secured-routes)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/cert_manager_operator/cert-manager-securing-routes.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="cert-manager-securing-routes" class="group scroll-mt-20">Securing routes with the {"{"}{"{"} cert_manager_operator {"}"}{"}"}<a href="#cert-manager-securing-routes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>In the OpenShift Container Platform, the route API is extended to provide a configurable option to reference TLS certificates via secrets. With externally managed certificates enabled, you can minimize errors from manual intervention, streamline the certificate management process, and enable the OpenShift Container Platform router to promptly serve the referenced certificate.</p>
      <h2 id="cert-manager-configuring-routes_cert-manager-securing-routes" class="group scroll-mt-20">Configuring certificates to secure routes in your cluster<a href="#cert-manager-configuring-routes_cert-manager-securing-routes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To encrypt traffic between external clients and your applications, configure certificates for routes in your OpenShift Container Platform cluster. You can secure your routes by defining TLS termination types, such as edge, passthrough, or re-encrypt, to match your specific security policies.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have installed version 1.14.0 or later of the cert-manager Operator for Red Hat OpenShift.</li>
      <li>You have <code>create</code> permission on the <code>routes/custom-host</code> sub-resource, which is used for both creating and updating routes.</li>
      <li>You have a <code>Service</code> resource that you want to expose.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create an <code>Issuer</code> to configure the HTTP-01 solver by running the following command. For other ACME issuer types, see "Configuring ACME an issuer".
      <CodeBlock code={"$ oc create -f - << EOF\napiVersion: cert-manager.io/v1\nkind: Issuer\nmetadata:\n  name: letsencrypt-acme\n  namespace: <namespace>\nspec:\n  acme:\n    server: https://acme-v02.api.letsencrypt.org/directory\n    privateKeySecretRef:\n      name: letsencrypt-acme-account-key\n    solvers:\n      - http01:\n          ingress:\n            ingressClassName: openshift-default\nEOF"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;namespace&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the namespace where the <code>Issuer</code> is located. It must be the same as the route namespace.</p></dd>
      </dl>
      </li>
      <li>
      Create a <code>Certificate</code> object for the route by running the following command. The <code>secretName</code> specifies the TLS secret that is going to be issued and managed by cert-manager and will also be referenced in your route in the following steps.
      <CodeBlock code={"$ oc create -f - << EOF\napiVersion: cert-manager.io/v1\nkind: Certificate\nmetadata:\n  name: example-route-cert\n  namespace: <namespace>\nspec:\n  commonName: <common_host>\n  dnsNames:\n    - <hostname>\n  usages:\n    - server auth\n  issuerRef:\n    kind: Issuer\n    name: letsencrypt-acme\n  secretName: <secret_name>\nEOF"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;namespace&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the <code>namespace</code> where the <code>Certificate</code> resource is located. It should be the same as the namespace of your route.</p></dd>
      <dt class="font-semibold"><code>&lt;common_host&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the common name of your certificate by using the hostname of the route.</p></dd>
      <dt class="font-semibold"><code>&lt;hostname&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the hostname of your route to the DNS names of your certificate.</p></dd>
      <dt class="font-semibold"><code>&lt;secret_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the secret that contains the certificate.</p></dd>
      </dl>
      </li>
      <li>
      Create a <code>Role</code> to provide the router service account permissions to read the referenced secret by using the following command:
      <CodeBlock code={"$ oc create role secret-reader \\\n  --verb=get,list,watch \\\n  --resource=secrets \\\n  --resource-name=<secret_name> \\\n  --namespace=<namespace>"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;secret_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the secret that you want to grant access to. It should be consistent with your <code>secretName</code> specified in the <code>Certificate</code> resource.</p></dd>
      <dt class="font-semibold"><code>&lt;namespace&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the namespace where both your secret and route are located.</p></dd>
      </dl>
      </li>
      <li>
      Create a <code>RoleBinding</code> resource to bind the router service account with the newly created <code>Role</code> resource by using the following command:
      <CodeBlock code={"$ oc create rolebinding secret-reader-binding \\\n  --role=secret-reader \\\n  --serviceaccount=openshift-ingress:router \\\n  --namespace=<namespace>"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;namespace&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the namespace where both your secret and route are located.</p></dd>
      </dl>
      </li>
      <li>
      Create a route for your service resource, that uses edge TLS termination and a custom hostname, by running the following command. The hostname is used when creating a <code>Certificate</code> resource in the next step.
      <CodeBlock code={"$ oc create route edge <route_name> \\\n  --service=<service_name> \\\n  --hostname=<hostname> \\\n  --namespace=<namespace>"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;route_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of your route.</p></dd>
      <dt class="font-semibold"><code>&lt;service_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the service you want to expose.</p></dd>
      <dt class="font-semibold"><code>&lt;hostname&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the hostname of your route.</p></dd>
      <dt class="font-semibold"><code>&lt;namespace&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the namespace where your route is located.</p></dd>
      </dl>
      </li>
      <li>
      To reference the secret and use the certificate issued by <code>cert-manager</code>, update the <code>.spec.tls.externalCertificate</code> field in the route by using the following command:
      <CodeBlock code={"$ oc patch route <route_name> \\\n  -n <namespace> \\\n  --type=merge \\\n  -p '{\"spec\":{\"tls\":{\"externalCertificate\":{\"name\":\"<secret_name>\"}}}}'"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;route_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of your route.</p></dd>
      <dt class="font-semibold"><code>&lt;namespace&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the namespace where both your secret and route are located.</p></dd>
      <dt class="font-semibold"><code>&lt;secret_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the secret that contains the certificate.</p></dd>
      </dl>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>
      Verify that the certificate is created and ready to use by running the following command:
      <CodeBlock code={"$ oc get certificate -n <namespace>\n$ oc get secret -n <namespace>"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;namespace&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the namespace where both your secret and route are located.</p></dd>
      </dl>
      </li>
      <li>
      Verify that the router is using the referenced external certificate by running the following command. The command should return with the status code <code>200 OK</code>.
      <CodeBlock code={"$ curl -IsS https://<hostname>"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;hostname&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the hostname of your route.</p></dd>
      </dl>
      </li>
      <li>
      Verify the <code>subject</code>, <code>subjectAltName</code>, and <code>issuer</code> fields of your server certificate are all as expected from the curl verbose outputs by running the following command:
      <CodeBlock code={"$ curl -v https://<hostname>"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;hostname&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the hostname of your route.</p></dd>
      </dl>
      </li>
      </ol>
      <p>The certificate from the referenced secret secures the route. The <code>cert-manager</code> component issues the certificate and automatically manages the certificate lifecycle.</p>
      <h2 id="additional-resources_cert-manager-securing-routes" class="group scroll-mt-20">Additional resources<a href="#additional-resources_cert-manager-securing-routes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/networking/ingress_load_balancing/routes/nw-configuring-routes#nw-ingress-route-secret-load-external-cert_secured-routes">Creating a route with externally managed certificate</a></li>
      <li><a href="/openshift-docs-markdown/security/cert_manager_operator/cert-manager-operator-issuer-acme#cert-manager-operator-issuer-acme">Configuring an ACME issuer</a></li>
      <li><a href="/openshift-docs-markdown/networking/ingress_load_balancing/routes/securing-routes#nw-ingress-route-secret-load-external-cert_secured-routes">Externally managed certificates</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
