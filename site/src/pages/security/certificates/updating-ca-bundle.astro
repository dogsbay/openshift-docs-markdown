---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/certificates/updating-ca-bundle.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"updating-ca-bundle","text":"Updating the CA bundle"},{"depth":2,"slug":"ca-bundle-understanding_updating-ca-bundle","text":"Understanding the CA Bundle certificate"},{"depth":2,"slug":"ca-bundle-replacing_updating-ca-bundle","text":"Replacing the CA Bundle certificate"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/certificates/updating-ca-bundle";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Updating the CA bundle";
const description = "To trust custom certificate authorities for egress connections in OpenShift Container Platform, you can update the CA bundle by specifying custom CA…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/certificates"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Updating the CA bundle {#updating-ca-bundle}\n\nTo trust custom certificate authorities for egress connections in OpenShift Container Platform, you can update the CA bundle by specifying custom CA certificates in the cluster-wide proxy configuration.\n\n## Understanding the CA Bundle certificate {#ca-bundle-understanding_updating-ca-bundle}\n\nProxy certificates allow users to specify one or more custom certificate authority (CA) used by platform components when making egress connections.\n\nThe `trustedCA` field of the Proxy object is a reference to a config map that contains a user-provided trusted certificate authority (CA) bundle. This bundle is merged with the Red Hat Enterprise Linux CoreOS (RHCOS) trust bundle and injected into the trust store of platform components that make egress HTTPS calls. For example, `image-registry-operator` calls an external image registry to download images. If `trustedCA` is not specified, only the RHCOS trust bundle is used for proxied HTTPS connections. Provide custom CA certificates to the RHCOS trust bundle if you want to use your own certificate infrastructure.\n\nThe `trustedCA` field should only be consumed by a proxy validator. The validator is responsible for reading the certificate bundle from required key `ca-bundle.crt` and copying it to a config map named `trusted-ca-bundle` in the `openshift-config-managed` namespace. The namespace for the config map referenced by `trustedCA` is `openshift-config`:\n\n```yaml\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: user-ca-bundle\n  namespace: openshift-config\ndata:\n  ca-bundle.crt: |\n    -----BEGIN CERTIFICATE-----\n    Custom CA certificate bundle.\n    -----END CERTIFICATE-----\n```\n\n## Replacing the CA Bundle certificate {#ca-bundle-replacing_updating-ca-bundle}\n\nTo trust a custom certificate authority for egress connections in OpenShift Container Platform, you can replace the CA bundle by creating a config map with your root CA certificate and updating the cluster proxy configuration.\n\n**Procedure**\n\n1. Create a config map that includes the root CA certificate used to sign the wildcard certificate:\n\n   ```terminal\n   $ oc create configmap custom-ca \\\n        --from-file=ca-bundle.crt=</path/to/example-ca.crt> \\\n        -n openshift-config\n   ```\n\n   `</path/to/example-ca.crt>` is the path to the CA certificate bundle on your local file system.\n2. Update the cluster-wide proxy configuration with the newly created config map:\n\n   ```terminal\n   $ oc patch proxy/cluster \\\n        --type=merge \\\n        --patch='{\"spec\":{\"trustedCA\":{\"name\":\"custom-ca\"}}}'\n   ```\n\n**Additional resources**\n{._additional-resources}\n\n- [Replacing the default ingress certificate](/openshift-docs-markdown/security/certificates/replacing-default-ingress-certificate#replacing-default-ingress_replacing-default-ingress)\n- [Enabling the cluster-wide proxy](/openshift-docs-markdown/networking/configuring_network_settings/enable-cluster-wide-proxy#nw-proxy-configure-object_config-cluster-wide-proxy)\n- [Proxy certificate customization](/openshift-docs-markdown/security/certificate_types_descriptions/proxy-certificates#proxy-cert-customization_proxy-certificates)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/certificates/updating-ca-bundle.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="updating-ca-bundle" class="group scroll-mt-20">Updating the CA bundle<a href="#updating-ca-bundle" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">To trust custom certificate authorities for egress connections in OpenShift Container Platform, you can update the CA bundle by specifying custom CA certificates in the cluster-wide proxy configuration.</p>
      <h2 id="ca-bundle-understanding_updating-ca-bundle" class="group scroll-mt-20">Understanding the CA Bundle certificate<a href="#ca-bundle-understanding_updating-ca-bundle" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Proxy certificates allow users to specify one or more custom certificate authority (CA) used by platform components when making egress connections.</p>
      <p>The <code>trustedCA</code> field of the Proxy object is a reference to a config map that contains a user-provided trusted certificate authority (CA) bundle. This bundle is merged with the Red Hat Enterprise Linux CoreOS (RHCOS) trust bundle and injected into the trust store of platform components that make egress HTTPS calls. For example, <code>image-registry-operator</code> calls an external image registry to download images. If <code>trustedCA</code> is not specified, only the RHCOS trust bundle is used for proxied HTTPS connections. Provide custom CA certificates to the RHCOS trust bundle if you want to use your own certificate infrastructure.</p>
      <p>The <code>trustedCA</code> field should only be consumed by a proxy validator. The validator is responsible for reading the certificate bundle from required key <code>ca-bundle.crt</code> and copying it to a config map named <code>trusted-ca-bundle</code> in the <code>openshift-config-managed</code> namespace. The namespace for the config map referenced by <code>trustedCA</code> is <code>openshift-config</code>:</p>
      <CodeBlock code={"apiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: user-ca-bundle\n  namespace: openshift-config\ndata:\n  ca-bundle.crt: |\n    -----BEGIN CERTIFICATE-----\n    Custom CA certificate bundle.\n    -----END CERTIFICATE-----"} lang="yaml" />
      <h2 id="ca-bundle-replacing_updating-ca-bundle" class="group scroll-mt-20">Replacing the CA Bundle certificate<a href="#ca-bundle-replacing_updating-ca-bundle" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To trust a custom certificate authority for egress connections in OpenShift Container Platform, you can replace the CA bundle by creating a config map with your root CA certificate and updating the cluster proxy configuration.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a config map that includes the root CA certificate used to sign the wildcard certificate:
      <CodeBlock code={"$ oc create configmap custom-ca \\\n     --from-file=ca-bundle.crt=</path/to/example-ca.crt> \\\n     -n openshift-config"} lang="terminal" />
      <p><code>&lt;/path/to/example-ca.crt&gt;</code> is the path to the CA certificate bundle on your local file system.</p>
      </li>
      <li>
      Update the cluster-wide proxy configuration with the newly created config map:
      <CodeBlock code={"$ oc patch proxy/cluster \\\n     --type=merge \\\n     --patch='{\"spec\":{\"trustedCA\":{\"name\":\"custom-ca\"}}}'"} lang="terminal" />
      </li>
      </ol>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/security/certificates/replacing-default-ingress-certificate#replacing-default-ingress_replacing-default-ingress">Replacing the default ingress certificate</a></li>
      <li><a href="/openshift-docs-markdown/networking/configuring_network_settings/enable-cluster-wide-proxy#nw-proxy-configure-object_config-cluster-wide-proxy">Enabling the cluster-wide proxy</a></li>
      <li><a href="/openshift-docs-markdown/security/certificate_types_descriptions/proxy-certificates#proxy-cert-customization_proxy-certificates">Proxy certificate customization</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
