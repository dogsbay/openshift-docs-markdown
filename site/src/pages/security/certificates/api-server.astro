---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/certificates/api-server.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"api-server-certificates","text":"Adding API server certificates"},{"depth":2,"slug":"customize-certificates-api-add-named_api-server-certificates","text":"Adding an API server named certificate for the first time"},{"depth":2,"slug":"customize-certificates-api-renew-named_api-server-certificates","text":"Updating or renewing an existing API server named certificate"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/certificates/api-server";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Adding API server certificates";
const description = "To allow clients outside of the cluster to verify the API server’s certificate, you can replace the default API server certificate with one that is issued…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/certificates"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Adding API server certificates {#api-server-certificates}\n\nTo allow clients outside of the cluster to verify the API server’s certificate, you can replace the default API server certificate with one that is issued by a CA that clients trust.\n\nBy default, the API server certificate is issued by an internal OpenShift Container Platform cluster CA. As a result, clients outside of the cluster cannot verify the API server’s certificate.\n\n> [!NOTE]\n> In hosted control plane clusters, you can add as many custom certificates to your Kubernetes API Server as you need. However, do not add a certificate for the endpoint that worker nodes use to communicate with the control plane.\n\n## Adding an API server named certificate for the first time {#customize-certificates-api-add-named_api-server-certificates}\n\nThe default API server certificate is issued by an internal OpenShift Container Platform cluster Certificate Authority (CA). You can add alternative certificates that the API server will return based on the fully qualified domain name (FQDN) requested by the client, for example when a reverse proxy or load balancer is used.\n\n> [!NOTE]\n> Adding a custom API server named certificate for the first time triggers the `kube-apiserver-operator` to roll out a new revision of the API server pods. Node reboots are not required.\n\n**Prerequisites**\n\n- You must have a certificate for the FQDN and its corresponding private key. Each should be in a separate PEM format file.\n- The private key must be unencrypted.\n- The certificate must include the `subjectAltName` extension showing the FQDN.\n- The certificate file can contain one or more certificates in a chain. The certificate for the API server FQDN must be the first certificate in the file, followed by intermediate certificates, and ending with the root CA certificate.\n\n> [!WARNING]\n> Do not provide a named certificate for the internal load balancer (host name `api-int.<cluster_name>.<base_domain>`). Doing so will leave your cluster in a degraded state.\n\n**Procedure**\n\n1. Log in to the CLI as the `kubeadmin` user:\n\n   ```terminal\n   $ oc login -u kubeadmin -p <password> https://<fqdn>:6443\n   ```\n\n   where:\n\n   `<password>`\n   :   Specifies your cluster administrative password.\n\n   `<fqdn>`\n   :   Specifies the fully qualified domain name of the internal cluster API endpoint.\n2. Create a secret that contains the certificate chain and private key in the `openshift-config` namespace:\n\n   ```terminal\n   $ oc create secret tls <secret_name> \\\n        --cert=<path_to_certificate_file> \\\n        --key=<path_to_private_key_file> \\\n        -n openshift-config\n   ```\n\n   where:\n\n   `<secret_name>`\n   :   Specifies the name of the new secret resource that will contain the cryptographic key pair.\n\n   `<path_to_certificate_file>`\n   :   Specifies the absolute local path to your custom certificate chain file.\n\n   `<path_to_private_key_file>`\n   :   Specifies the absolute local path to the unencrypted private key file associated with the certificate.\n3. Update the API server to reference the created secret resource:\n\n   ```terminal\n   $ oc patch apiserver cluster --type=merge -p '\n   {\n     \"spec\": {\n       \"servingCerts\": {\n         \"namedCertificates\": [\n           {\n             \"names\": [\"<fqdn>\"],\n             \"servingCertificate\": {\n               \"name\": \"<secret_name>\"\n             }\n           }\n         ]\n       }\n     }\n   }'\n   ```\n\n   where:\n\n   `<fqdn>`\n   :   Specifies the fully qualified domain name for which the API server serves this custom certificate. Do not include a port number.\n\n   `<secret_name>`\n   :   Specifies the name of the secret you created in the previous step.\n4. Verify that a new revision of the Kubernetes API server rolls out by checking the operator status:\n\n   ```terminal\n   $ oc get clusteroperators kube-apiserver\n   ```\n\n   > [!NOTE]\n   > The `PROGRESSING` status column will change to `True` while the API server operator deploys the new pod revision configured with your custom certificate. Do not interrupt the process or apply additional configuration updates while the rollout is underway. Continue only after the status returns to `False` and `AVAILABLE` reads `True`.\n\n## Updating or renewing an existing API server named certificate {#customize-certificates-api-renew-named_api-server-certificates}\n\nUpdate or renew an expired or expiring named certificate that has already been configured in your cluster to avoid API availability issues. The API server pods dynamically detect and reload the updated certificate asset without disruption.\n\n> [!NOTE]\n> When an existing API server named certificate is renewed by updating its corresponding secret, a new revision of the Kubernetes API server pods does not roll out. Node reboots are not required.\n\n> [!WARNING]\n> If the renewed certificate is signed by a different root CA than the previous certificate, internal applications or custom pods that communicate with the API server might encounter X509 certificate validation errors. If these client workloads do not automatically hot-reload their truststores, you must manually restart them to force them to pick up the new certificate chain.\n\n**Prerequisites**\n\n- You have the renewed certificate chain and private key files in PEM format.\n- The secret containing the old certificate already exists in the `openshift-config` namespace and is actively referenced by the `apiserver/cluster` configuration.\n\n**Procedure**\n\n1. Log in to the CLI as the `kubeadmin` user.\n2. Update the existing secret resource in the `openshift-config` namespace with the newly issued certificate and private key:\n\n   ```terminal\n   $ oc create secret tls <existing_secret_name> \\\n        --cert=<path_to_new_cert>.crt \\\n        --key=<path_to_new_key>.key \\\n        -n openshift-config \\\n        --dry-run=client -o yaml | oc replace -f -\n   ```\n\n   where:\n\n   `<existing_secret_name>`\n   :   Specifies the target name of the existing active secret that you are replacing.\n\n   `<path_to_new_cert>.crt`\n   :   Specifies the absolute local file system path to the renewed certificate chain file.\n\n   `<path_to_new_key>.key`\n   :   Specifies the absolute local file system path to the corresponding unencrypted private key file.\n3. Verify that the `kube-apiserver` pods successfully hot-reload the updated assets without initiating a new cluster deployment revision:\n\n   ```terminal\n   $ oc get clusteroperators kube-apiserver\n   ```\n\n   Confirm that the `PROGRESSING` status column remains `False`. If the status changes to `True`, verify that your underlying `apiserver/cluster` resource parameters were not modified structural layout changes during the substitution.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/certificates/api-server.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="api-server-certificates" class="group scroll-mt-20">Adding API server certificates<a href="#api-server-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>To allow clients outside of the cluster to verify the API server’s certificate, you can replace the default API server certificate with one that is issued by a CA that clients trust.</p>
      <p>By default, the API server certificate is issued by an internal OpenShift Container Platform cluster CA. As a result, clients outside of the cluster cannot verify the API server’s certificate.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>In hosted control plane clusters, you can add as many custom certificates to your Kubernetes API Server as you need. However, do not add a certificate for the endpoint that worker nodes use to communicate with the control plane.</p>
        </AlertDescription>
      </Alert>
      <h2 id="customize-certificates-api-add-named_api-server-certificates" class="group scroll-mt-20">Adding an API server named certificate for the first time<a href="#customize-certificates-api-add-named_api-server-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The default API server certificate is issued by an internal OpenShift Container Platform cluster Certificate Authority (CA). You can add alternative certificates that the API server will return based on the fully qualified domain name (FQDN) requested by the client, for example when a reverse proxy or load balancer is used.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Adding a custom API server named certificate for the first time triggers the <code>kube-apiserver-operator</code> to roll out a new revision of the API server pods. Node reboots are not required.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You must have a certificate for the FQDN and its corresponding private key. Each should be in a separate PEM format file.</li>
      <li>The private key must be unencrypted.</li>
      <li>The certificate must include the <code>subjectAltName</code> extension showing the FQDN.</li>
      <li>The certificate file can contain one or more certificates in a chain. The certificate for the API server FQDN must be the first certificate in the file, followed by intermediate certificates, and ending with the root CA certificate.</li>
      </ul>
      <Alert variant="warning" class="my-4">
        <AlertTitle>Warning</AlertTitle>
        <AlertDescription>
          <p>Do not provide a named certificate for the internal load balancer (host name <code>api-int.&lt;cluster_name&gt;.&lt;base_domain&gt;</code>). Doing so will leave your cluster in a degraded state.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Log in to the CLI as the <code>kubeadmin</code> user:
      <CodeBlock code={"$ oc login -u kubeadmin -p <password> https://<fqdn>:6443"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;password&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies your cluster administrative password.</p></dd>
      <dt class="font-semibold"><code>&lt;fqdn&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the fully qualified domain name of the internal cluster API endpoint.</p></dd>
      </dl>
      </li>
      <li>
      Create a secret that contains the certificate chain and private key in the <code>openshift-config</code> namespace:
      <CodeBlock code={"$ oc create secret tls <secret_name> \\\n     --cert=<path_to_certificate_file> \\\n     --key=<path_to_private_key_file> \\\n     -n openshift-config"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;secret_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the new secret resource that will contain the cryptographic key pair.</p></dd>
      <dt class="font-semibold"><code>&lt;path_to_certificate_file&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the absolute local path to your custom certificate chain file.</p></dd>
      <dt class="font-semibold"><code>&lt;path_to_private_key_file&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the absolute local path to the unencrypted private key file associated with the certificate.</p></dd>
      </dl>
      </li>
      <li>
      Update the API server to reference the created secret resource:
      <CodeBlock code={"$ oc patch apiserver cluster --type=merge -p '\n{\n  \"spec\": {\n    \"servingCerts\": {\n      \"namedCertificates\": [\n        {\n          \"names\": [\"<fqdn>\"],\n          \"servingCertificate\": {\n            \"name\": \"<secret_name>\"\n          }\n        }\n      ]\n    }\n  }\n}'"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;fqdn&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the fully qualified domain name for which the API server serves this custom certificate. Do not include a port number.</p></dd>
      <dt class="font-semibold"><code>&lt;secret_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the secret you created in the previous step.</p></dd>
      </dl>
      </li>
      <li>
      Verify that a new revision of the Kubernetes API server rolls out by checking the operator status:
      <CodeBlock code={"$ oc get clusteroperators kube-apiserver"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The <code>PROGRESSING</code> status column will change to <code>True</code> while the API server operator deploys the new pod revision configured with your custom certificate. Do not interrupt the process or apply additional configuration updates while the rollout is underway. Continue only after the status returns to <code>False</code> and <code>AVAILABLE</code> reads <code>True</code>.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <h2 id="customize-certificates-api-renew-named_api-server-certificates" class="group scroll-mt-20">Updating or renewing an existing API server named certificate<a href="#customize-certificates-api-renew-named_api-server-certificates" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Update or renew an expired or expiring named certificate that has already been configured in your cluster to avoid API availability issues. The API server pods dynamically detect and reload the updated certificate asset without disruption.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>When an existing API server named certificate is renewed by updating its corresponding secret, a new revision of the Kubernetes API server pods does not roll out. Node reboots are not required.</p>
        </AlertDescription>
      </Alert>
      <Alert variant="warning" class="my-4">
        <AlertTitle>Warning</AlertTitle>
        <AlertDescription>
          <p>If the renewed certificate is signed by a different root CA than the previous certificate, internal applications or custom pods that communicate with the API server might encounter X509 certificate validation errors. If these client workloads do not automatically hot-reload their truststores, you must manually restart them to force them to pick up the new certificate chain.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have the renewed certificate chain and private key files in PEM format.</li>
      <li>The secret containing the old certificate already exists in the <code>openshift-config</code> namespace and is actively referenced by the <code>apiserver/cluster</code> configuration.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Log in to the CLI as the <code>kubeadmin</code> user.</li>
      <li>
      Update the existing secret resource in the <code>openshift-config</code> namespace with the newly issued certificate and private key:
      <CodeBlock code={"$ oc create secret tls <existing_secret_name> \\\n     --cert=<path_to_new_cert>.crt \\\n     --key=<path_to_new_key>.key \\\n     -n openshift-config \\\n     --dry-run=client -o yaml | oc replace -f -"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>&lt;existing_secret_name&gt;</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the target name of the existing active secret that you are replacing.</p></dd>
      <dt class="font-semibold"><code>&lt;path_to_new_cert&gt;.crt</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the absolute local file system path to the renewed certificate chain file.</p></dd>
      <dt class="font-semibold"><code>&lt;path_to_new_key&gt;.key</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the absolute local file system path to the corresponding unencrypted private key file.</p></dd>
      </dl>
      </li>
      <li>
      Verify that the <code>kube-apiserver</code> pods successfully hot-reload the updated assets without initiating a new cluster deployment revision:
      <CodeBlock code={"$ oc get clusteroperators kube-apiserver"} lang="terminal" />
      <p>Confirm that the <code>PROGRESSING</code> status column remains <code>False</code>. If the status changes to <code>True</code>, verify that your underlying <code>apiserver/cluster</code> resource parameters were not modified structural layout changes during the substitution.</p>
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
