---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/container_security/security-storage.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;

const headings = [{"depth":1,"slug":"security-storage","text":"Securing attached storage"},{"depth":2,"slug":"security-network-storage-persistent_security-storage","text":"Persistent volume plugins"},{"depth":2,"slug":"security-network-storage-shared_security-storage","text":"Shared storage"},{"depth":2,"slug":"security-network-storage-block_security-storage","text":"Block storage"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/container_security/security-storage";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Securing attached storage";
const description = "You should understand how OpenShift Container Platform secures attached storage to protect persistent data in containerized workloads. OpenShift Container…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/container_security"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Securing attached storage {#security-storage}\n\nYou should understand how OpenShift Container Platform secures attached storage to protect persistent data in containerized workloads. OpenShift Container Platform uses Security-Enhanced Linux (SELinux) capabilities, group ID (GID) annotations, and Container Storage Interface (CSI)-compliant storage providers to isolate storage access and prevent unauthorized data exposure.\n\n## Persistent volume plugins {#security-network-storage-persistent_security-storage}\n\nContainers are useful for both stateless and stateful applications. Protecting attached storage is a key element of securing stateful services. Using the Container Storage Interface (CSI), OpenShift Container Platform can incorporate storage from any storage back end that supports the CSI interface.\n\nOpenShift Container Platform provides plugins for multiple types of storage, including:\n\n- Red Hat OpenShift Data Foundation \\*\n- AWS Elastic Block Stores (EBS) \\*\n- AWS Elastic File System (EFS) \\*\n- Azure Disk \\*\n- Azure File \\*\n- OpenStack Cinder \\*\n- Google Compute Engine (GCE) Persistent Disks \\*\n- VMware vSphere \\*\n- Network File System (NFS)\n- FlexVolume\n- Fibre Channel\n- Internet Small Computer Systems Interface (iSCSI)\n\nPlugins for those storage types with dynamic provisioning are marked with an asterisk (\\*). Data in transit is encrypted via HTTPS for all OpenShift Container Platform components communicating with each other.\n\nYou can mount a persistent volume (PV) on a host in any way supported by your storage type. Different types of storage have different capabilities and each PV’s access modes are set to the specific modes supported by that particular volume.\n\nFor example, NFS can support multiple read/write clients, but a specific NFS PV might be exported on the server as read-only. Each PV has its own set of access modes describing that specific PV’s capabilities, such as `ReadWriteOnce`, `ReadOnlyMany`, and `ReadWriteMany`.\n\n## Shared storage {#security-network-storage-shared_security-storage}\n\nFor shared storage providers such as Network File System (NFS), the persistent volume (PV) registers its group ID (GID) as an annotation on the PV resource.\n\nThen, when the PV is claimed by the pod, the annotated GID is added to the supplemental groups of the pod, giving that pod access to the contents of the shared storage.\n\n## Block storage {#security-network-storage-block_security-storage}\n\nFor block storage providers such as AWS Elastic Block Store (EBS), Google Compute Engine (GCE) Persistent Disks, and Internet Small Computer Systems Interface (iSCSI), OpenShift Container Platform uses Security-Enhanced Linux (SELinux) capabilities to secure the root of the mounted volume for non-privileged pods, making the mounted volume owned by and only visible to the container with which it is associated.\n\n**Additional resources**\n\n- [Understanding persistent storage](/openshift-docs-markdown/storage/understanding-persistent-storage#understanding-persistent-storage)\n- [Configuring CSI volumes](/openshift-docs-markdown/storage/container_storage_interface/persistent-storage-csi#persistent-storage-csi)\n- [Dynamic provisioning](/openshift-docs-markdown/storage/dynamic-provisioning#dynamic-provisioning)\n- [Persistent storage using NFS](/openshift-docs-markdown/storage/persistent_storage/persistent-storage-nfs#persistent-storage-using-nfs)\n- [Persistent storage using AWS Elastic Block Store](/openshift-docs-markdown/storage/persistent_storage/persistent-storage-aws#persistent-storage-aws)\n- [Persistent storage using GCE Persistent Disk](/openshift-docs-markdown/storage/persistent_storage/persistent-storage-gce#persistent-storage-using-gce)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/container_security/security-storage.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="security-storage" class="group scroll-mt-20">Securing attached storage<a href="#security-storage" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You should understand how OpenShift Container Platform secures attached storage to protect persistent data in containerized workloads. OpenShift Container Platform uses Security-Enhanced Linux (SELinux) capabilities, group ID (GID) annotations, and Container Storage Interface (CSI)-compliant storage providers to isolate storage access and prevent unauthorized data exposure.</p>
      <h2 id="security-network-storage-persistent_security-storage" class="group scroll-mt-20">Persistent volume plugins<a href="#security-network-storage-persistent_security-storage" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Containers are useful for both stateless and stateful applications. Protecting attached storage is a key element of securing stateful services. Using the Container Storage Interface (CSI), OpenShift Container Platform can incorporate storage from any storage back end that supports the CSI interface.</p>
      <p>OpenShift Container Platform provides plugins for multiple types of storage, including:</p>
      <ul>
      <li>Red Hat OpenShift Data Foundation *</li>
      <li>AWS Elastic Block Stores (EBS) *</li>
      <li>AWS Elastic File System (EFS) *</li>
      <li>Azure Disk *</li>
      <li>Azure File *</li>
      <li>OpenStack Cinder *</li>
      <li>Google Compute Engine (GCE) Persistent Disks *</li>
      <li>VMware vSphere *</li>
      <li>Network File System (NFS)</li>
      <li>FlexVolume</li>
      <li>Fibre Channel</li>
      <li>Internet Small Computer Systems Interface (iSCSI)</li>
      </ul>
      <p>Plugins for those storage types with dynamic provisioning are marked with an asterisk (*). Data in transit is encrypted via HTTPS for all OpenShift Container Platform components communicating with each other.</p>
      <p>You can mount a persistent volume (PV) on a host in any way supported by your storage type. Different types of storage have different capabilities and each PV’s access modes are set to the specific modes supported by that particular volume.</p>
      <p>For example, NFS can support multiple read/write clients, but a specific NFS PV might be exported on the server as read-only. Each PV has its own set of access modes describing that specific PV’s capabilities, such as <code>ReadWriteOnce</code>, <code>ReadOnlyMany</code>, and <code>ReadWriteMany</code>.</p>
      <h2 id="security-network-storage-shared_security-storage" class="group scroll-mt-20">Shared storage<a href="#security-network-storage-shared_security-storage" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>For shared storage providers such as Network File System (NFS), the persistent volume (PV) registers its group ID (GID) as an annotation on the PV resource.</p>
      <p>Then, when the PV is claimed by the pod, the annotated GID is added to the supplemental groups of the pod, giving that pod access to the contents of the shared storage.</p>
      <h2 id="security-network-storage-block_security-storage" class="group scroll-mt-20">Block storage<a href="#security-network-storage-block_security-storage" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>For block storage providers such as AWS Elastic Block Store (EBS), Google Compute Engine (GCE) Persistent Disks, and Internet Small Computer Systems Interface (iSCSI), OpenShift Container Platform uses Security-Enhanced Linux (SELinux) capabilities to secure the root of the mounted volume for non-privileged pods, making the mounted volume owned by and only visible to the container with which it is associated.</p>
      <p><strong>Additional resources</strong></p>
      <ul>
      <li><a href="/openshift-docs-markdown/storage/understanding-persistent-storage#understanding-persistent-storage">Understanding persistent storage</a></li>
      <li><a href="/openshift-docs-markdown/storage/container_storage_interface/persistent-storage-csi#persistent-storage-csi">Configuring CSI volumes</a></li>
      <li><a href="/openshift-docs-markdown/storage/dynamic-provisioning#dynamic-provisioning">Dynamic provisioning</a></li>
      <li><a href="/openshift-docs-markdown/storage/persistent_storage/persistent-storage-nfs#persistent-storage-using-nfs">Persistent storage using NFS</a></li>
      <li><a href="/openshift-docs-markdown/storage/persistent_storage/persistent-storage-aws#persistent-storage-aws">Persistent storage using AWS Elastic Block Store</a></li>
      <li><a href="/openshift-docs-markdown/storage/persistent_storage/persistent-storage-gce#persistent-storage-using-gce">Persistent storage using GCE Persistent Disk</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
