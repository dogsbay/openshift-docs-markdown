---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/container_security/security-build.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import BlockImage from "@ui/image/Image.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"security-build","text":"Securing the build process"},{"depth":2,"slug":"security-build-once_security-build","text":"Building once, deploying everywhere"},{"depth":2,"slug":"security-build-management_security-build","text":"Managing builds"},{"depth":2,"slug":"security-build-inputs_security-build","text":"Securing inputs during builds"},{"depth":2,"slug":"security-build-designing_security-build","text":"Designing your build process"},{"depth":2,"slug":"security-build-knative_security-build","text":"Building Knative serverless applications"},{"depth":2,"slug":"additional-resources_security-build","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/container_security/security-build";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Securing the build process";
const description = "You can secure your software supply chain by using trusted base images, integrating security testing, and building once to deploy everywhere. Managing…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/container_security"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Securing the build process {#security-build}\n\nYou can secure your software supply chain by using trusted base images, integrating security testing, and building once to deploy everywhere. Managing this build process ensures production deployments match verified builds and protects the software stack where code and libraries integrate.\n\n## Building once, deploying everywhere {#security-build-once_security-build}\n\nYou can build container images once in a secure environment and deploy them unchanged across all stages. Using OpenShift Container Platform as your build standard guarantees this security, ensuring production deployments match verified builds and preventing runtime vulnerabilities.\n\nIt is also important to maintain the immutability of your containers. You should not patch running containers, but rebuild and redeploy them.\n\nAs your software moves through the stages of building, testing, and production, it is important that the tools making up your software supply chain be trusted. The following figure illustrates the process and tools that could be incorporated into a trusted software supply chain for containerized software:\n\n![trustedsupplychain](/openshift-docs-markdown/_assets/images/trustedsupplychain.png)\n\nOpenShift Container Platform can be integrated with trusted code repositories (such as GitHub) and development platforms (such as Che) for creating and managing secure code. Unit testing frameworks can validate code quality before builds.\n\nYou can inspect your containers for vulnerabilities and configuration issues at build, deploy, or runtime with Red Hat Advanced Cluster Security for Kubernetes. For images stored in Quay, you can use the Clair scanner to inspect images at rest. In addition, certified vulnerability scanners are available in the Red Hat ecosystem catalog.\n\nMonitoring tools can provide ongoing visibility of your containerized applications.\n\n- [Cucumber](https://cucumber.io/)\n- [JUnit](https://junit.org/)\n- [Red Hat Advanced Cluster Security for Kubernetes](https://www.redhat.com/en/technologies/cloud-computing/openshift/advanced-cluster-security-kubernetes)\n- [Clair scanner](https://access.redhat.com/products/red-hat-quay)\n- [Certified vulnerability scanners](https://catalog.redhat.com/software/vulnerability-scanner/search)\n- [Sysdig](https://sysdig.com)\n\n## Managing builds {#security-build-management_security-build}\n\nYou can use Source-to-Image (S2I) builder images that enable development and operations teams to collaborate on reproducible builds, using Red Hat Universal Base Images that you can freely redistribute with your applications.\n\nYou can use Source-to-Image (S2I) to combine source code and base images. *Builder images* make use of S2I to enable your development and operations teams to collaborate on a reproducible build environment. With Red Hat S2I images available as Universal Base Image (UBI) images, you can now freely redistribute your software with base images built from real RHEL RPM packages. Red Hat has removed subscription restrictions to allow this.\n\nWhen developers commit code with Git for an application by using build images, OpenShift Container Platform can perform the following functions:\n\n- Trigger, either by using webhooks on the code repository or other automated continuous integration (CI) process, to automatically assemble a new image from available artifacts, the S2I builder image, and the newly committed code.\n- Automatically deploy the newly built image for testing.\n- Promote the tested image to production where it can be automatically deployed using a CI process.\n\n![Source-to-Image Builds](/openshift-docs-markdown/_assets/images/build_process1.png)\n\nYou can use the integrated OpenShift Container Registry to manage access to final images. Both S2I and native build images are automatically pushed to your OpenShift Container Registry.\n\nIn addition to the included Jenkins for CI, you can also integrate your own build and CI environment with OpenShift Container Platform using RESTful APIs, and use any API-compliant image registry.\n\n## Securing inputs during builds {#security-build-inputs_security-build}\n\nYou can protect sensitive credentials required during builds by defining input secrets that give access to dependent resources without exposing those credentials in the final application image.\n\nIn some scenarios, build operations require credentials to access dependent resources, but it is undesirable for those credentials to be available in the final application image produced by the build. You can define input secrets for this purpose.\n\nFor example, when building a Node.js application, you can set up your private mirror for Node.js modules. To download modules from that private mirror, you must supply a custom `.npmrc` file for the build that has a URL, user name, and password. For security reasons, you do not want to expose your credentials in the application image.\n\nUsing this example scenario, you can add an input secret to a new `BuildConfig` object.\n\n**Procedure**\n\n1. Create the secret, if it does not exist:\n\n   ```terminal\n   $ oc create secret generic secret-npmrc --from-file=.npmrc=~/.npmrc\n   ```\n\n   This creates a new secret named `secret-npmrc`, which has the base64 encoded content of the `~/.npmrc` file.\n2. Add the secret to the `source` section in the existing `BuildConfig` object:\n\n   ```yaml\n   source:\n     git:\n       uri: https://github.com/sclorg/nodejs-ex.git\n     secrets:\n     - destinationDir: .\n       secret:\n         name: secret-npmrc\n   ```\n3. To include the secret in a new `BuildConfig` object, run the following command:\n\n   ```terminal\n   $ oc new-build \\\n       openshift/nodejs-010-centos7~https://github.com/sclorg/nodejs-ex.git \\\n       --build-secret secret-npmrc\n   ```\n\n## Designing your build process {#security-build-designing_security-build}\n\nYou can design your container image management to separate control across teams by using layered images, integrate automated security testing into your CI process, and sign custom containers to ensure integrity between build and deployment.\n\nYou can design your container image management and build process to use container layers so that you can separate control.\n\n![Designing Your Build Process](/openshift-docs-markdown/_assets/images/build_process2.png)\n\nFor example, an operations team manages base images, while architects manage middleware, runtimes, databases, and other solutions. Developers can then focus on application layers and focus on writing code.\n\nBecause new vulnerabilities are identified daily, you need to proactively check container content over time. To do this, you should integrate automated security testing into your build or CI process. For example:\n\n- SAST / DAST - Static and Dynamic security testing tools.\n- Scanners for real-time checking against known vulnerabilities. Tools such as these catalog the open source packages in your container, notify you of any known vulnerabilities, and update you when new vulnerabilities are discovered in previously scanned packages.\n\nYour CI process should include policies that flag builds with issues discovered by security scans so that your team can take appropriate action to address those issues. You should sign your custom built containers to ensure that nothing is tampered with between build and deployment.\n\nUsing GitOps methodology, you can use the same CI/CD mechanisms to manage not only your application configurations, but also your OpenShift Container Platform infrastructure.\n\n## Building Knative serverless applications {#security-build-knative_security-build}\n\nYou can build, deploy, and manage serverless applications by using OpenShift Serverless in OpenShift Container Platform, relying on Kubernetes and Kourier,  leveraging S2I builder images and Knative services for scalable, event-driven workloads.\n\nAs with other builds, you can use S2I images to build your containers, then serve them using Knative services. View Knative application builds through the **Topology** view of the OpenShift Container Platform web console.\n\n## Additional resources {#additional-resources_security-build}\n\n- [Understanding image builds](/openshift-docs-markdown/cicd/builds/understanding-image-builds#understanding-image-builds)\n- [Triggering and modifying builds](/openshift-docs-markdown/cicd/builds/triggering-builds-build-hooks#triggering-builds-build-hooks)\n- [Creating build inputs](/openshift-docs-markdown/cicd/builds/creating-build-inputs#creating-build-inputs)\n- [Input secrets and config maps](/openshift-docs-markdown/cicd/builds/creating-build-inputs#builds-input-secrets-configmaps_creating-build-inputs)\n- [OpenShift Serverless overview](https://docs.openshift.com/serverless/1.28/about/about-serverless.html)\n- [Viewing application composition using the Topology view](/openshift-docs-markdown/applications/odc-viewing-application-composition-using-topology-view#odc-viewing-application-composition-using-topology-view)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/container_security/security-build.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="security-build" class="group scroll-mt-20">Securing the build process<a href="#security-build" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can secure your software supply chain by using trusted base images, integrating security testing, and building once to deploy everywhere. Managing this build process ensures production deployments match verified builds and protects the software stack where code and libraries integrate.</p>
      <h2 id="security-build-once_security-build" class="group scroll-mt-20">Building once, deploying everywhere<a href="#security-build-once_security-build" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can build container images once in a secure environment and deploy them unchanged across all stages. Using OpenShift Container Platform as your build standard guarantees this security, ensuring production deployments match verified builds and preventing runtime vulnerabilities.</p>
      <p>It is also important to maintain the immutability of your containers. You should not patch running containers, but rebuild and redeploy them.</p>
      <p>As your software moves through the stages of building, testing, and production, it is important that the tools making up your software supply chain be trusted. The following figure illustrates the process and tools that could be incorporated into a trusted software supply chain for containerized software:</p>
      <BlockImage src="/openshift-docs-markdown/_assets/images/trustedsupplychain.png" alt="trustedsupplychain" />
      <p>OpenShift Container Platform can be integrated with trusted code repositories (such as GitHub) and development platforms (such as Che) for creating and managing secure code. Unit testing frameworks can validate code quality before builds.</p>
      <p>You can inspect your containers for vulnerabilities and configuration issues at build, deploy, or runtime with Red Hat Advanced Cluster Security for Kubernetes. For images stored in Quay, you can use the Clair scanner to inspect images at rest. In addition, certified vulnerability scanners are available in the Red Hat ecosystem catalog.</p>
      <p>Monitoring tools can provide ongoing visibility of your containerized applications.</p>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="https://cucumber.io/">Cucumber</a></li>
      <li><a href="https://junit.org/">JUnit</a></li>
      <li><a href="https://www.redhat.com/en/technologies/cloud-computing/openshift/advanced-cluster-security-kubernetes">Red Hat Advanced Cluster Security for Kubernetes</a></li>
      <li><a href="https://access.redhat.com/products/red-hat-quay">Clair scanner</a></li>
      <li><a href="https://catalog.redhat.com/software/vulnerability-scanner/search">Certified vulnerability scanners</a></li>
      <li><a href="https://sysdig.com">Sysdig</a></li>
      </ul>
      </section>
      <h2 id="security-build-management_security-build" class="group scroll-mt-20">Managing builds<a href="#security-build-management_security-build" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can use Source-to-Image (S2I) builder images that enable development and operations teams to collaborate on reproducible builds, using Red Hat Universal Base Images that you can freely redistribute with your applications.</p>
      <p>You can use Source-to-Image (S2I) to combine source code and base images. <em>Builder images</em> make use of S2I to enable your development and operations teams to collaborate on a reproducible build environment. With Red Hat S2I images available as Universal Base Image (UBI) images, you can now freely redistribute your software with base images built from real RHEL RPM packages. Red Hat has removed subscription restrictions to allow this.</p>
      <p>When developers commit code with Git for an application by using build images, OpenShift Container Platform can perform the following functions:</p>
      <ul>
      <li>Trigger, either by using webhooks on the code repository or other automated continuous integration (CI) process, to automatically assemble a new image from available artifacts, the S2I builder image, and the newly committed code.</li>
      <li>Automatically deploy the newly built image for testing.</li>
      <li>Promote the tested image to production where it can be automatically deployed using a CI process.</li>
      </ul>
      <BlockImage src="/openshift-docs-markdown/_assets/images/build_process1.png" alt="Source-to-Image Builds" />
      <p>You can use the integrated OpenShift Container Registry to manage access to final images. Both S2I and native build images are automatically pushed to your OpenShift Container Registry.</p>
      <p>In addition to the included Jenkins for CI, you can also integrate your own build and CI environment with OpenShift Container Platform using RESTful APIs, and use any API-compliant image registry.</p>
      <h2 id="security-build-inputs_security-build" class="group scroll-mt-20">Securing inputs during builds<a href="#security-build-inputs_security-build" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can protect sensitive credentials required during builds by defining input secrets that give access to dependent resources without exposing those credentials in the final application image.</p>
      <p>In some scenarios, build operations require credentials to access dependent resources, but it is undesirable for those credentials to be available in the final application image produced by the build. You can define input secrets for this purpose.</p>
      <p>For example, when building a Node.js application, you can set up your private mirror for Node.js modules. To download modules from that private mirror, you must supply a custom <code>.npmrc</code> file for the build that has a URL, user name, and password. For security reasons, you do not want to expose your credentials in the application image.</p>
      <p>Using this example scenario, you can add an input secret to a new <code>BuildConfig</code> object.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create the secret, if it does not exist:
      <CodeBlock code={"$ oc create secret generic secret-npmrc --from-file=.npmrc=~/.npmrc"} lang="terminal" />
      <p>This creates a new secret named <code>secret-npmrc</code>, which has the base64 encoded content of the <code>~/.npmrc</code> file.</p>
      </li>
      <li>
      Add the secret to the <code>source</code> section in the existing <code>BuildConfig</code> object:
      <CodeBlock code={"source:\n  git:\n    uri: https://github.com/sclorg/nodejs-ex.git\n  secrets:\n  - destinationDir: .\n    secret:\n      name: secret-npmrc"} lang="yaml" />
      </li>
      <li>
      To include the secret in a new <code>BuildConfig</code> object, run the following command:
      <CodeBlock code={"$ oc new-build \\\n    openshift/nodejs-010-centos7~https://github.com/sclorg/nodejs-ex.git \\\n    --build-secret secret-npmrc"} lang="terminal" />
      </li>
      </ol>
      <h2 id="security-build-designing_security-build" class="group scroll-mt-20">Designing your build process<a href="#security-build-designing_security-build" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can design your container image management to separate control across teams by using layered images, integrate automated security testing into your CI process, and sign custom containers to ensure integrity between build and deployment.</p>
      <p>You can design your container image management and build process to use container layers so that you can separate control.</p>
      <BlockImage src="/openshift-docs-markdown/_assets/images/build_process2.png" alt="Designing Your Build Process" />
      <p>For example, an operations team manages base images, while architects manage middleware, runtimes, databases, and other solutions. Developers can then focus on application layers and focus on writing code.</p>
      <p>Because new vulnerabilities are identified daily, you need to proactively check container content over time. To do this, you should integrate automated security testing into your build or CI process. For example:</p>
      <ul>
      <li>SAST / DAST - Static and Dynamic security testing tools.</li>
      <li>Scanners for real-time checking against known vulnerabilities. Tools such as these catalog the open source packages in your container, notify you of any known vulnerabilities, and update you when new vulnerabilities are discovered in previously scanned packages.</li>
      </ul>
      <p>Your CI process should include policies that flag builds with issues discovered by security scans so that your team can take appropriate action to address those issues. You should sign your custom built containers to ensure that nothing is tampered with between build and deployment.</p>
      <p>Using GitOps methodology, you can use the same CI/CD mechanisms to manage not only your application configurations, but also your OpenShift Container Platform infrastructure.</p>
      <h2 id="security-build-knative_security-build" class="group scroll-mt-20">Building Knative serverless applications<a href="#security-build-knative_security-build" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can build, deploy, and manage serverless applications by using OpenShift Serverless in OpenShift Container Platform, relying on Kubernetes and Kourier,  leveraging S2I builder images and Knative services for scalable, event-driven workloads.</p>
      <p>As with other builds, you can use S2I images to build your containers, then serve them using Knative services. View Knative application builds through the <strong>Topology</strong> view of the OpenShift Container Platform web console.</p>
      <h2 id="additional-resources_security-build" class="group scroll-mt-20">Additional resources<a href="#additional-resources_security-build" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/cicd/builds/understanding-image-builds#understanding-image-builds">Understanding image builds</a></li>
      <li><a href="/openshift-docs-markdown/cicd/builds/triggering-builds-build-hooks#triggering-builds-build-hooks">Triggering and modifying builds</a></li>
      <li><a href="/openshift-docs-markdown/cicd/builds/creating-build-inputs#creating-build-inputs">Creating build inputs</a></li>
      <li><a href="/openshift-docs-markdown/cicd/builds/creating-build-inputs#builds-input-secrets-configmaps_creating-build-inputs">Input secrets and config maps</a></li>
      <li><a href="https://docs.openshift.com/serverless/1.28/about/about-serverless.html">OpenShift Serverless overview</a></li>
      <li><a href="/openshift-docs-markdown/applications/odc-viewing-application-composition-using-topology-view#odc-viewing-application-composition-using-topology-view">Viewing application composition using the Topology view</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
