---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/container_security/security-network.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;

const headings = [{"depth":1,"slug":"security-network","text":"Securing networks"},{"depth":2,"slug":"security-network-namespaces_security-network","text":"Using network namespaces"},{"depth":2,"slug":"security-network-policies_security-network","text":"Isolating pods with network policies"},{"depth":2,"slug":"security-network-multiple-pod_security-network","text":"Using multiple pod networks"},{"depth":2,"slug":"security-network-isolating_security-network","text":"Isolating applications"},{"depth":2,"slug":"security-network-ingress_security-network","text":"Securing ingress traffic"},{"depth":2,"slug":"security-network-egress_security-network","text":"Securing egress traffic"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/container_security/security-network";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Securing networks";
const description = "You can manage network security at several levels, such as by using network namespaces and network policies." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/container_security"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Securing networks {#security-network}\n\nYou can manage network security at several levels, such as by using network namespaces and network policies.\n\nAt the pod level, network namespaces can prevent containers from seeing other pods or the host system by restricting network access. Network policies give you control over allowing and rejecting connections. You can manage ingress and egress traffic to and from your containerized applications.\n\n## Using network namespaces {#security-network-namespaces_security-network}\n\nYou can use software-defined networking (SDN) in OpenShift Container Platform to give a unified cluster network that enables communication between containers across the cluster.\n\nNetwork policy mode, by default, makes all pods in a project accessible from other pods and network endpoints. To isolate one or more pods in a project, you can create `NetworkPolicy` objects in that project to indicate the allowed incoming connections. Using multitenant mode, you can provide project-level isolation for pods and services.\n\n## Isolating pods with network policies {#security-network-policies_security-network}\n\nUsing *network policies*, you can isolate pods from each other in the same project. Network policies can deny all network access to a pod, only allow connections for the Ingress Controller, reject connections from pods in other projects, or set similar rules for how networks behave.\n\n**Additional resources**\n\n- [About network policy](/openshift-docs-markdown/networking/network_security/network_policy/about-network-policy#about-network-policy)\n\n## Using multiple pod networks {#security-network-multiple-pod_security-network}\n\nEach running container has only one network interface by default. You can use the Multus CNI plugin to create multiple CNI networks, and then attach any of those networks to a pod. In that way, you can do things such as separate private data onto a more restricted network and have multiple network interfaces on each node.\n\n**Additional resources**\n\n- [Using multiple networks](/openshift-docs-markdown/networking/multiple_networks/understanding-multiple-networks#understanding-multiple-networks)\n\n## Isolating applications {#security-network-isolating_security-network}\n\nYou can segment network traffic on a single cluster to make multitenant clusters that isolate users, teams, applications, and environments from non-global resources.\n\n## Securing ingress traffic {#security-network-ingress_security-network}\n\nThere are many security implications related to how you configure access to your Kubernetes services from outside of your OpenShift Container Platform cluster.\n\nIn addition to exposing HTTP and HTTPS routes, ingress routing allows you to set up `NodePort` or `LoadBalancer` ingress types. NodePort exposes an application’s service API object from each cluster worker. LoadBalancer lets you assign an external load balancer to an associated service API object in your OpenShift Container Platform cluster.\n\n**Additional resources**\n\n- [Configuring ingress cluster traffic](/openshift-docs-markdown/networking/ingress_load_balancing/configuring_ingress_cluster_traffic/configuring-ingress-cluster-traffic-ingress-controller#configuring-ingress-cluster-traffic-ingress-controller)\n\n## Securing egress traffic {#security-network-egress_security-network}\n\nA cluster administrator can control egress traffic by using either a router or firewall method. For example, you can use the IP allow list to control database access. A cluster administrator can assign one or more egress IP addresses to a project by configuring an egress IP address.\n\nLikewise, a cluster administrator can prevent egress traffic from going outside of an OpenShift Container Platform cluster by using an egress firewall.\n\nBy assigning a fixed egress IP address, you can have all outgoing traffic assigned to that IP address for a particular project. With the egress firewall, you can prevent a pod from connecting to an external network, prevent a pod from connecting to an internal network, or limit a pod’s access to specific internal subnets.\n\n**Additional resources**\n\n- [Configuring an egress firewall for a project](/openshift-docs-markdown/networking/network_security/egress_firewall/configuring-egress-firewall-ovn#configuring-egress-firewall-ovn)\n- [Configuring IPsec encryption](/openshift-docs-markdown/networking/network_security/configuring-ipsec-ovn#configuring-ipsec-ovn)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/container_security/security-network.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="security-network" class="group scroll-mt-20">Securing networks<a href="#security-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can manage network security at several levels, such as by using network namespaces and network policies.</p>
      <p>At the pod level, network namespaces can prevent containers from seeing other pods or the host system by restricting network access. Network policies give you control over allowing and rejecting connections. You can manage ingress and egress traffic to and from your containerized applications.</p>
      <h2 id="security-network-namespaces_security-network" class="group scroll-mt-20">Using network namespaces<a href="#security-network-namespaces_security-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can use software-defined networking (SDN) in OpenShift Container Platform to give a unified cluster network that enables communication between containers across the cluster.</p>
      <p>Network policy mode, by default, makes all pods in a project accessible from other pods and network endpoints. To isolate one or more pods in a project, you can create <code>NetworkPolicy</code> objects in that project to indicate the allowed incoming connections. Using multitenant mode, you can provide project-level isolation for pods and services.</p>
      <h2 id="security-network-policies_security-network" class="group scroll-mt-20">Isolating pods with network policies<a href="#security-network-policies_security-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Using <em>network policies</em>, you can isolate pods from each other in the same project. Network policies can deny all network access to a pod, only allow connections for the Ingress Controller, reject connections from pods in other projects, or set similar rules for how networks behave.</p>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/networking/network_security/network_policy/about-network-policy#about-network-policy">About network policy</a></li></ul>
      <h2 id="security-network-multiple-pod_security-network" class="group scroll-mt-20">Using multiple pod networks<a href="#security-network-multiple-pod_security-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Each running container has only one network interface by default. You can use the Multus CNI plugin to create multiple CNI networks, and then attach any of those networks to a pod. In that way, you can do things such as separate private data onto a more restricted network and have multiple network interfaces on each node.</p>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/networking/multiple_networks/understanding-multiple-networks#understanding-multiple-networks">Using multiple networks</a></li></ul>
      <h2 id="security-network-isolating_security-network" class="group scroll-mt-20">Isolating applications<a href="#security-network-isolating_security-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can segment network traffic on a single cluster to make multitenant clusters that isolate users, teams, applications, and environments from non-global resources.</p>
      <h2 id="security-network-ingress_security-network" class="group scroll-mt-20">Securing ingress traffic<a href="#security-network-ingress_security-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>There are many security implications related to how you configure access to your Kubernetes services from outside of your OpenShift Container Platform cluster.</p>
      <p>In addition to exposing HTTP and HTTPS routes, ingress routing allows you to set up <code>NodePort</code> or <code>LoadBalancer</code> ingress types. NodePort exposes an application’s service API object from each cluster worker. LoadBalancer lets you assign an external load balancer to an associated service API object in your OpenShift Container Platform cluster.</p>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/networking/ingress_load_balancing/configuring_ingress_cluster_traffic/configuring-ingress-cluster-traffic-ingress-controller#configuring-ingress-cluster-traffic-ingress-controller">Configuring ingress cluster traffic</a></li></ul>
      <h2 id="security-network-egress_security-network" class="group scroll-mt-20">Securing egress traffic<a href="#security-network-egress_security-network" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">A cluster administrator can control egress traffic by using either a router or firewall method. For example, you can use the IP allow list to control database access. A cluster administrator can assign one or more egress IP addresses to a project by configuring an egress IP address. </p>
      <p>Likewise, a cluster administrator can prevent egress traffic from going outside of an OpenShift Container Platform cluster by using an egress firewall.</p>
      <p>By assigning a fixed egress IP address, you can have all outgoing traffic assigned to that IP address for a particular project. With the egress firewall, you can prevent a pod from connecting to an external network, prevent a pod from connecting to an internal network, or limit a pod’s access to specific internal subnets.</p>
      <p><strong>Additional resources</strong></p>
      <ul>
      <li><a href="/openshift-docs-markdown/networking/network_security/egress_firewall/configuring-egress-firewall-ovn#configuring-egress-firewall-ovn">Configuring an egress firewall for a project</a></li>
      <li><a href="/openshift-docs-markdown/networking/network_security/configuring-ipsec-ovn#configuring-ipsec-ovn">Configuring IPsec encryption</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
