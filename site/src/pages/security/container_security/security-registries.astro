---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/container_security/security-registries.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;

const headings = [{"depth":1,"slug":"security-registries","text":"Using container registries securely"},{"depth":2,"slug":"security-registries-where_security-registries","text":"Knowing where containers come from"},{"depth":2,"slug":"security-registries-immutable_security-registries","text":"Immutable and certified containers"},{"depth":2,"slug":"security-registries-ecosystem_security-registries","text":"Getting containers from Red Hat Registry and Ecosystem Catalog"},{"depth":2,"slug":"security-registries-openshift_security-registries","text":"OpenShift Container Registry"},{"depth":2,"slug":"security-registries-quay_security-registries","text":"Storing containers using Red Hat Quay"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/container_security/security-registries";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Using container registries securely";
const description = "You can use container registries to store container images, making the images accessible to others either publicly or privately." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/container_security"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Using container registries securely {#security-registries}\n\nYou can use container registries to store container images, making the images accessible to others either publicly or privately.\n\nBy using a registry, you can include multiple versions of an image, optionally limit access to images based on different authentication methods, or make them publicly available.\n\nThere are public container registries, such as Quay.io and Docker Hub where many people and organizations share their images. The Red Hat Registry offers supported Red Hat and partner images, while the Red Hat Ecosystem Catalog offers detailed descriptions and health checks for those images. To manage your own registry, you could purchase a container registry such as Red Hat Quay.\n\nFrom a security standpoint, some registries provide special features to check and improve the health of your containers. For example, Red Hat Quay offers container vulnerability scanning with Clair security scanner, build triggers to automatically rebuild images when source code changes in GitHub and other locations, and the ability to use role-based access control (RBAC) to secure access to images.\n\n## Knowing where containers come from {#security-registries-where_security-registries}\n\nYou can use tools to scan and track the contents of your downloaded and deployed container images. However, there are many public sources of container images. When using public container registries, you can add a layer of protection by using trusted sources.\n\n## Immutable and certified containers {#security-registries-immutable_security-registries}\n\nImmutable containers are containers that will never be changed while running. You do not step into the running immutable container to replace one or more binaries. From an operational standpoint, you rebuild and redeploy an updated container image to replace a container instead of changing it.\n\nConsuming security updates is particularly important when managing immutable containers.\n\nRed Hat certified images are:\n\n- Free of known vulnerabilities in the platform components or layers\n- Compatible across the RHEL platforms, from bare metal to cloud\n- Supported by Red Hat\n\nThe list of known vulnerabilities is constantly evolving, so you must track the contents of your deployed container images, and newly downloaded images, over time. You can use Red Hat Security Advisories (RHSAs) to alert you to any newly discovered issues in Red Hat certified container images, and direct you to the updated image. Alternatively, you can go to the Red Hat Ecosystem Catalog to look up that and other security-related issues for each Red Hat image.\n\n## Getting containers from Red Hat Registry and Ecosystem Catalog {#security-registries-ecosystem_security-registries}\n\nRed Hat lists certified container images for Red Hat products and partner offerings from the Container Images section of the Red Hat Ecosystem Catalog. From that catalog, you can see details of each image, including CVE, software packages listings, and health scores.\n\nRed Hat images are actually stored in what is referred to as the *Red Hat Registry*, which is represented by a public container registry (`registry.access.redhat.com`) and an authenticated registry (`registry.redhat.io`). Both include the same set of container images, with `registry.redhat.io` including some additional images that require authentication with Red Hat subscription credentials.\n\nContainer content is monitored for vulnerabilities by Red Hat and updated regularly. When Red Hat releases security updates, such as fixes to *glibc*, DROWN, or Dirty Cow, any affected container images are also rebuilt and pushed to the Red Hat Registry.\n\nRed Hat uses a `health index` to reflect the security risk for each container provided through the Red Hat Ecosystem Catalog. Because containers consume software provided by Red Hat and the errata process, old, stale containers are insecure whereas new, fresh containers are more secure.\n\nTo illustrate the age of containers, the Red Hat Ecosystem Catalog uses a grading system. A freshness grade is a measure of the oldest and most severe security errata available for an image. \"A\" is more up to date than \"F\".  See \"Container Health Index grades as used inside the Red Hat Ecosystem Catalog\" for more details on this grading system.\n\nSee the Red Hat Product Security Center for details on security updates and vulnerabilities related to Red Hat software. Check out Red Hat Security Advisories to search for specific advisories and CVEs.\n\n**Additional resources**\n\n- [Red Hat Product Security Center](https://access.redhat.com/security/)\n- [Red Hat Security Advisories](https://access.redhat.com/security/security-updates/#/security-advisories)\n\n## OpenShift Container Registry {#security-registries-openshift_security-registries}\n\nTo manage your container images, you can use the *OpenShift Container Registry*, a private registry in OpenShift Container Platform that runs as an integrated component of the platform. The registry provides role-based access controls that allow you to manage who can pull and push which container images.\n\nOpenShift Container Platform also supports integration with other private registries that you might already be using, such as Red Hat Quay.\n\n**Additional resources**\n\n- [Integrated OpenShift image registry](/openshift-docs-markdown/registry/index#registry-overview)\n\n## Storing containers using Red Hat Quay {#security-registries-quay_security-registries}\n\nRed Hat Quay is an enterprise-quality container registry product from Red Hat. Development for Red Hat Quay is done through the upstream Project Quay. Red Hat Quay is available to deploy on-premise or through the hosted version of Red Hat Quay at Quay.io.\n\nSecurity-related features of Red Hat Quay include:\n\n- **Time machine**: Allows images with older tags to expire after a set period of time or based on a user-selected expiration time.\n- **Repository mirroring**: Lets you mirror other registries for security reasons, such hosting a public repository on Red Hat Quay behind a company firewall, or for performance reasons, to keep registries closer to where they are used.\n- **Action log storage**: Save Red Hat Quay logging output to Elasticsearch storage or Splunk to allow for later search and analysis.\n- **Clair**: Scan images against a variety of Linux vulnerability databases, based on the origins of each container image.\n- **Internal authentication**: Use the default local database to handle RBAC authentication to Red Hat Quay or choose from LDAP, Keystone (OpenStack), JWT Custom Authentication, or External Application Token authentication.\n- **External authorization (OAuth)**: Allow authorization to Red Hat Quay from GitHub, GitHub Enterprise, or Google Authentication.\n- **Access settings**: Generate tokens to allow access to Red Hat Quay from docker, rkt, anonymous access, user-created accounts, encrypted client passwords, or prefix username autocompletion.\n\nOngoing integration of Red Hat Quay with OpenShift Container Platform continues, with several OpenShift Container Platform Operators of particular interest. The Quay Bridge Operator lets you replace the internal OpenShift image registry with Red Hat Quay. The Red Hat Quay Container Security Operator lets you check vulnerabilities of images running in OpenShift Container Platform that were pulled from Red Hat Quay registries.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/container_security/security-registries.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="security-registries" class="group scroll-mt-20">Using container registries securely<a href="#security-registries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can use container registries to store container images, making the images accessible to others either publicly or privately.</p>
      <p>By using a registry, you can include multiple versions of an image, optionally limit access to images based on different authentication methods, or make them publicly available.</p>
      <p>There are public container registries, such as Quay.io and Docker Hub where many people and organizations share their images. The Red Hat Registry offers supported Red Hat and partner images, while the Red Hat Ecosystem Catalog offers detailed descriptions and health checks for those images. To manage your own registry, you could purchase a container registry such as Red Hat Quay.</p>
      <p>From a security standpoint, some registries provide special features to check and improve the health of your containers. For example, Red Hat Quay offers container vulnerability scanning with Clair security scanner, build triggers to automatically rebuild images when source code changes in GitHub and other locations, and the ability to use role-based access control (RBAC) to secure access to images.</p>
      <h2 id="security-registries-where_security-registries" class="group scroll-mt-20">Knowing where containers come from<a href="#security-registries-where_security-registries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can use tools to scan and track the contents of your downloaded and deployed container images. However, there are many public sources of container images. When using public container registries, you can add a layer of protection by using trusted sources.</p>
      <h2 id="security-registries-immutable_security-registries" class="group scroll-mt-20">Immutable and certified containers<a href="#security-registries-immutable_security-registries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Immutable containers are containers that will never be changed while running. You do not step into the running immutable container to replace one or more binaries. From an operational standpoint, you rebuild and redeploy an updated container image to replace a container instead of changing it.</p>
      <p>Consuming security updates is particularly important when managing immutable containers.</p>
      <p>Red Hat certified images are:</p>
      <ul>
      <li>Free of known vulnerabilities in the platform components or layers</li>
      <li>Compatible across the RHEL platforms, from bare metal to cloud</li>
      <li>Supported by Red Hat</li>
      </ul>
      <p>The list of known vulnerabilities is constantly evolving, so you must track the contents of your deployed container images, and newly downloaded images, over time. You can use Red Hat Security Advisories (RHSAs) to alert you to any newly discovered issues in Red Hat certified container images, and direct you to the updated image. Alternatively, you can go to the Red Hat Ecosystem Catalog to look up that and other security-related issues for each Red Hat image.</p>
      <h2 id="security-registries-ecosystem_security-registries" class="group scroll-mt-20">Getting containers from Red Hat Registry and Ecosystem Catalog<a href="#security-registries-ecosystem_security-registries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Red Hat lists certified container images for Red Hat products and partner offerings from the Container Images section of the Red Hat Ecosystem Catalog. From that catalog, you can see details of each image, including CVE, software packages listings, and health scores.</p>
      <p>Red Hat images are actually stored in what is referred to as the <em>Red Hat Registry</em>, which is represented by a public container registry (<code>registry.access.redhat.com</code>) and an authenticated registry (<code>registry.redhat.io</code>). Both include the same set of container images, with <code>registry.redhat.io</code> including some additional images that require authentication with Red Hat subscription credentials.</p>
      <p>Container content is monitored for vulnerabilities by Red Hat and updated regularly. When Red Hat releases security updates, such as fixes to <em>glibc</em>, DROWN, or Dirty Cow, any affected container images are also rebuilt and pushed to the Red Hat Registry.</p>
      <p>Red Hat uses a <code>health index</code> to reflect the security risk for each container provided through the Red Hat Ecosystem Catalog. Because containers consume software provided by Red Hat and the errata process, old, stale containers are insecure whereas new, fresh containers are more secure.</p>
      <p>To illustrate the age of containers, the Red Hat Ecosystem Catalog uses a grading system. A freshness grade is a measure of the oldest and most severe security errata available for an image. "A" is more up to date than "F".  See "Container Health Index grades as used inside the Red Hat Ecosystem Catalog" for more details on this grading system.</p>
      <p>See the Red Hat Product Security Center for details on security updates and vulnerabilities related to Red Hat software. Check out Red Hat Security Advisories to search for specific advisories and CVEs.</p>
      <p><strong>Additional resources</strong></p>
      <ul>
      <li><a href="https://access.redhat.com/security/">Red Hat Product Security Center</a></li>
      <li><a href="https://access.redhat.com/security/security-updates/#/security-advisories">Red Hat Security Advisories</a></li>
      </ul>
      <h2 id="security-registries-openshift_security-registries" class="group scroll-mt-20">OpenShift Container Registry<a href="#security-registries-openshift_security-registries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To manage your container images, you can use the <em>OpenShift Container Registry</em>, a private registry in OpenShift Container Platform that runs as an integrated component of the platform. The registry provides role-based access controls that allow you to manage who can pull and push which container images.</p>
      <p>OpenShift Container Platform also supports integration with other private registries that you might already be using, such as Red Hat Quay.</p>
      <p><strong>Additional resources</strong></p>
      <ul><li><a href="/openshift-docs-markdown/registry/index#registry-overview">Integrated OpenShift image registry</a></li></ul>
      <h2 id="security-registries-quay_security-registries" class="group scroll-mt-20">Storing containers using Red Hat Quay<a href="#security-registries-quay_security-registries" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Red Hat Quay is an enterprise-quality container registry product from Red Hat. Development for Red Hat Quay is done through the upstream Project Quay. Red Hat Quay is available to deploy on-premise or through the hosted version of Red Hat Quay at Quay.io.</p>
      <p>Security-related features of Red Hat Quay include:</p>
      <ul>
      <li><strong>Time machine</strong>: Allows images with older tags to expire after a set period of time or based on a user-selected expiration time.</li>
      <li><strong>Repository mirroring</strong>: Lets you mirror other registries for security reasons, such hosting a public repository on Red Hat Quay behind a company firewall, or for performance reasons, to keep registries closer to where they are used.</li>
      <li><strong>Action log storage</strong>: Save Red Hat Quay logging output to Elasticsearch storage or Splunk to allow for later search and analysis.</li>
      <li><strong>Clair</strong>: Scan images against a variety of Linux vulnerability databases, based on the origins of each container image.</li>
      <li><strong>Internal authentication</strong>: Use the default local database to handle RBAC authentication to Red Hat Quay or choose from LDAP, Keystone (OpenStack), JWT Custom Authentication, or External Application Token authentication.</li>
      <li><strong>External authorization (OAuth)</strong>: Allow authorization to Red Hat Quay from GitHub, GitHub Enterprise, or Google Authentication.</li>
      <li><strong>Access settings</strong>: Generate tokens to allow access to Red Hat Quay from docker, rkt, anonymous access, user-created accounts, encrypted client passwords, or prefix username autocompletion.</li>
      </ul>
      <p>Ongoing integration of Red Hat Quay with OpenShift Container Platform continues, with several OpenShift Container Platform Operators of particular interest. The Quay Bridge Operator lets you replace the internal OpenShift image registry with Red Hat Quay. The Red Hat Quay Container Security Operator lets you check vulnerabilities of images running in OpenShift Container Platform that were pulled from Red Hat Quay registries.</p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
