---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/container_security/security-deploy.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import BlockImage from "@ui/image/Image.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"security-deploy","text":"Deploying containers"},{"depth":2,"slug":"security-deploy-trigger_security-deploy","text":"Controlling container deployments with triggers"},{"depth":2,"slug":"security-deploy-image-sources_security-deploy","text":"Controlling what image sources can be deployed"},{"depth":2,"slug":"security-deploy-signature_security-deploy","text":"Using signature transports"},{"depth":2,"slug":"security-deploy-secrets_security-deploy","text":"Creating secrets and config maps"},{"depth":2,"slug":"security-deploy-continuous_security-deploy","text":"Automating continuous deployment"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/container_security/security-deploy";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Deploying containers";
const description = "You can use a variety of techniques to make sure that the containers you deploy hold the latest production-quality content and that they have not been…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/container_security"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Deploying containers {#security-deploy}\n\nYou can use a variety of techniques to make sure that the containers you deploy hold the latest production-quality content and that they have not been tampered with, such as setting up build triggers and using signatures.\n\n## Controlling container deployments with triggers {#security-deploy-trigger_security-deploy}\n\nIf something happens during the build process, or if a vulnerability is discovered after an image has been deployed, you can use tool for automated, policy-based deployment to remediate. You can use triggers to rebuild and replace images, ensuring the immutable containers process, instead of patching running containers, which is not recommended.\n\n![Secure Deployments](/openshift-docs-markdown/images/secure_deployments.png)\n\nFor example, you build an application by using three container image layers: core, middleware, and applications. An issue is discovered in the core image and that image is rebuilt. After the build is complete, the image is pushed to your OpenShift Container Registry. OpenShift Container Platform detects that the image has changed and automatically rebuilds and deploys the application image, based on the defined triggers. This change incorporates the fixed libraries and ensures that the production code is identical to the most current image.\n\nYou can use the `oc set triggers` command to set a deployment trigger. For example, to set a trigger for a deployment called deployment-example:\n\n```terminal\n$ oc set triggers deploy/deployment-example \\\n    --from-image=example:latest \\\n    --containers=web\n```\n\n## Controlling what image sources can be deployed {#security-deploy-image-sources_security-deploy}\n\nOpenShift Container Platform enables cluster administrators to apply security policy that is broad or narrow, reflecting deployment environment and security requirements.\n\nIt is important that the intended images are actually being deployed, that the images including the contained content are from trusted sources, and they have not been altered. Cryptographic signing provides this assurance. OpenShift Container Platform enables cluster administrators to apply security policy that is broad or narrow, reflecting deployment environment and security requirements. Two parameters define this policy:\n\n- one or more registries, with optional project namespace\n- trust type, such as accept, reject, or require public key(s)\n\nYou can use these policy parameters to allow, deny, or require a trust relationship for entire registries, parts of registries, or individual images. Using trusted public keys, you can ensure that the source is cryptographically verified. The policy rules apply to nodes. Policy might be applied uniformly across all nodes or targeted for different node workloads (for example, build, zone, or environment).\n\n```json {title=\"Example image signature policy file\"}\n{\n    \"default\": [{\"type\": \"reject\"}],\n    \"transports\": {\n        \"docker\": {\n            \"access.redhat.com\": [\n                {\n                    \"type\": \"signedBy\",\n                    \"keyType\": \"GPGKeys\",\n                    \"keyPath\": \"/etc/pki/rpm-gpg/RPM-GPG-KEY-redhat-release\"\n                }\n            ]\n        },\n        \"atomic\": {\n            \"172.30.1.1:5000/openshift\": [\n                {\n                    \"type\": \"signedBy\",\n                    \"keyType\": \"GPGKeys\",\n                    \"keyPath\": \"/etc/pki/rpm-gpg/RPM-GPG-KEY-redhat-release\"\n                }\n            ],\n            \"172.30.1.1:5000/production\": [\n                {\n                    \"type\": \"signedBy\",\n                    \"keyType\": \"GPGKeys\",\n                    \"keyPath\": \"/etc/pki/example.com/pubkey\"\n                }\n            ],\n            \"172.30.1.1:5000\": [{\"type\": \"reject\"}]\n        }\n    }\n}\n```\n\nThe policy can be saved onto a node as `/etc/containers/policy.json`. Saving this file to a node is best accomplished using a new `MachineConfig` object. This example enforces the following rules:\n\n- Require images from the Red Hat Registry (`registry.access.redhat.com`) to be signed by the Red Hat public key.\n- Require images from your OpenShift Container Registry in the `openshift` namespace to be signed by the Red Hat public key.\n- Require images from your OpenShift Container Registry in the `production` namespace to be signed by the public key for `example.com`.\n- Reject all other registries not specified by the global `default` definition.\n\n## Using signature transports {#security-deploy-signature_security-deploy}\n\nYou can use a signature transport as a way to store and retrieve the binary signature blob.\n\nSignature transports can be either of the following types:\n\n- `atomic`: Managed by the OpenShift Container Platform API.\n- `docker`: Served as a local file or by a web server.\n\nThe OpenShift Container Platform API manages signatures that use the `atomic` transport type. You must store the images that use this signature type in your OpenShift Container Registry. Because the docker or distribution `extensions` API auto-discovers the image signature endpoint, no additional configuration is required.\n\nSignatures that use the `docker` transport type are served by local file or web server. These signatures are more flexible; you can serve images from any container image registry and use an independent server to deliver binary signatures.\n\nHowever, the `docker` transport type requires additional configuration. You must configure the nodes with the Uniform Resource Identifier (URI) of the signature server by placing arbitrarily-named YAML files into a directory on the host system, `/etc/containers/registries.d` by default. The YAML configuration files contain a registry URI and a signature server URI, or *sigstore*:\n\n```yaml {title=\"Example registries.d file\"}\ndocker:\n    access.redhat.com:\n        sigstore: https://access.redhat.com/webassets/docker/content/sigstore\n```\n\nIn this example, the Red Hat Registry, `access.redhat.com`, is the signature server that provides signatures for the `docker` transport type. Its URI is defined in the `sigstore` parameter. You might name this file `/etc/containers/registries.d/redhat.com.yaml` and use the Machine Config Operator to automatically place the file on each node in your cluster. No service restart is required since policy and `registries.d` files are dynamically loaded by the container runtime.\n\n## Creating secrets and config maps {#security-deploy-secrets_security-deploy}\n\nYou can use the `Secret` object type to provide a mechanism to hold sensitive information such as passwords, OpenShift Container Platform client configuration files, `dockercfg` files, and private source repository credentials. Secrets decouple sensitive content from pods.\n\nYou can mount secrets into containers by using a volume plugin or the system can use secrets to perform actions on behalf of a pod.\n\nConfig maps are similar to secrets, but are designed to support working with strings that do not contain sensitive information. The `ConfigMap` object holds key-value pairs of configuration data that can be consumed in pods or used to store configuration data for system components such as controllers.\n\nFor example, to add a secret to your deployment so that it can access a private image repository, use the following procedure.\n\n**Procedure**\n\n1. Log in to the OpenShift Container Platform web console.\n2. Create a new project.\n3. Navigate to **Resources** → **Secrets** and create a new secret. Set `Secret Type` to `Image Secret` and `Authentication Type` to `Image Registry Credentials` to enter credentials for accessing a private image repository.\n4. When creating a deployment (for example, from the **Add to Project** → **Deploy Image** page), set the `Pull Secret` to your new secret.\n\n## Automating continuous deployment {#security-deploy-continuous_security-deploy}\n\nYou can integrate your own continuous deployment (CD) tooling with OpenShift Container Platform.\n\nBy leveraging continuous integration and continuous deployment (CI/CD) and OpenShift Container Platform, you can automate the process of rebuilding the application to incorporate the latest fixes, testing, and ensuring that it is deployed everywhere within the environment.\n\n**Additional resources**\n{._additional-resources}\n\n- [Input secrets and config maps](/openshift-docs-markdown/cicd/builds/creating-build-inputs#builds-input-secrets-configmaps_creating-build-inputs)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/container_security/security-deploy.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="security-deploy" class="group scroll-mt-20">Deploying containers<a href="#security-deploy" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">You can use a variety of techniques to make sure that the containers you deploy hold the latest production-quality content and that they have not been tampered with, such as setting up build triggers and using signatures.</p>
      <h2 id="security-deploy-trigger_security-deploy" class="group scroll-mt-20">Controlling container deployments with triggers<a href="#security-deploy-trigger_security-deploy" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">If something happens during the build process, or if a vulnerability is discovered after an image has been deployed, you can use tool for automated, policy-based deployment to remediate. You can use triggers to rebuild and replace images, ensuring the immutable containers process, instead of patching running containers, which is not recommended.</p>
      <BlockImage src="/openshift-docs-markdown/images/secure_deployments.png" alt="Secure Deployments" />
      <p>For example, you build an application by using three container image layers: core, middleware, and applications. An issue is discovered in the core image and that image is rebuilt. After the build is complete, the image is pushed to your OpenShift Container Registry. OpenShift Container Platform detects that the image has changed and automatically rebuilds and deploys the application image, based on the defined triggers. This change incorporates the fixed libraries and ensures that the production code is identical to the most current image.</p>
      <p>You can use the <code>oc set triggers</code> command to set a deployment trigger. For example, to set a trigger for a deployment called deployment-example:</p>
      <CodeBlock code={"$ oc set triggers deploy/deployment-example \\\n    --from-image=example:latest \\\n    --containers=web"} lang="terminal" />
      <h2 id="security-deploy-image-sources_security-deploy" class="group scroll-mt-20">Controlling what image sources can be deployed<a href="#security-deploy-image-sources_security-deploy" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">OpenShift Container Platform enables cluster administrators to apply security policy that is broad or narrow, reflecting deployment environment and security requirements.</p>
      <p>It is important that the intended images are actually being deployed, that the images including the contained content are from trusted sources, and they have not been altered. Cryptographic signing provides this assurance. OpenShift Container Platform enables cluster administrators to apply security policy that is broad or narrow, reflecting deployment environment and security requirements. Two parameters define this policy:</p>
      <ul>
      <li>one or more registries, with optional project namespace</li>
      <li>trust type, such as accept, reject, or require public key(s)</li>
      </ul>
      <p>You can use these policy parameters to allow, deny, or require a trust relationship for entire registries, parts of registries, or individual images. Using trusted public keys, you can ensure that the source is cryptographically verified. The policy rules apply to nodes. Policy might be applied uniformly across all nodes or targeted for different node workloads (for example, build, zone, or environment).</p>
      <CodeBlock code={"{\n    \"default\": [{\"type\": \"reject\"}],\n    \"transports\": {\n        \"docker\": {\n            \"access.redhat.com\": [\n                {\n                    \"type\": \"signedBy\",\n                    \"keyType\": \"GPGKeys\",\n                    \"keyPath\": \"/etc/pki/rpm-gpg/RPM-GPG-KEY-redhat-release\"\n                }\n            ]\n        },\n        \"atomic\": {\n            \"172.30.1.1:5000/openshift\": [\n                {\n                    \"type\": \"signedBy\",\n                    \"keyType\": \"GPGKeys\",\n                    \"keyPath\": \"/etc/pki/rpm-gpg/RPM-GPG-KEY-redhat-release\"\n                }\n            ],\n            \"172.30.1.1:5000/production\": [\n                {\n                    \"type\": \"signedBy\",\n                    \"keyType\": \"GPGKeys\",\n                    \"keyPath\": \"/etc/pki/example.com/pubkey\"\n                }\n            ],\n            \"172.30.1.1:5000\": [{\"type\": \"reject\"}]\n        }\n    }\n}"} lang="json" title="Example image signature policy file" />
      <p>The policy can be saved onto a node as <code>/etc/containers/policy.json</code>. Saving this file to a node is best accomplished using a new <code>MachineConfig</code> object. This example enforces the following rules:</p>
      <ul>
      <li>Require images from the Red Hat Registry (<code>registry.access.redhat.com</code>) to be signed by the Red Hat public key.</li>
      <li>Require images from your OpenShift Container Registry in the <code>openshift</code> namespace to be signed by the Red Hat public key.</li>
      <li>Require images from your OpenShift Container Registry in the <code>production</code> namespace to be signed by the public key for <code>example.com</code>.</li>
      <li>Reject all other registries not specified by the global <code>default</code> definition.</li>
      </ul>
      <h2 id="security-deploy-signature_security-deploy" class="group scroll-mt-20">Using signature transports<a href="#security-deploy-signature_security-deploy" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can use a signature transport as a way to store and retrieve the binary signature blob.</p>
      <p class="_abstract">Signature transports can be either of the following types:</p>
      <ul>
      <li><code>atomic</code>: Managed by the OpenShift Container Platform API.</li>
      <li><code>docker</code>: Served as a local file or by a web server.</li>
      </ul>
      <p>The OpenShift Container Platform API manages signatures that use the <code>atomic</code> transport type. You must store the images that use this signature type in your OpenShift Container Registry. Because the docker or distribution <code>extensions</code> API auto-discovers the image signature endpoint, no additional configuration is required.</p>
      <p>Signatures that use the <code>docker</code> transport type are served by local file or web server. These signatures are more flexible; you can serve images from any container image registry and use an independent server to deliver binary signatures.</p>
      <p>However, the <code>docker</code> transport type requires additional configuration. You must configure the nodes with the Uniform Resource Identifier (URI) of the signature server by placing arbitrarily-named YAML files into a directory on the host system, <code>/etc/containers/registries.d</code> by default. The YAML configuration files contain a registry URI and a signature server URI, or <em>sigstore</em>:</p>
      <CodeBlock code={"docker:\n    access.redhat.com:\n        sigstore: https://access.redhat.com/webassets/docker/content/sigstore"} lang="yaml" title="Example registries.d file" />
      <p>In this example, the Red Hat Registry, <code>access.redhat.com</code>, is the signature server that provides signatures for the <code>docker</code> transport type. Its URI is defined in the <code>sigstore</code> parameter. You might name this file <code>/etc/containers/registries.d/redhat.com.yaml</code> and use the Machine Config Operator to automatically place the file on each node in your cluster. No service restart is required since policy and <code>registries.d</code> files are dynamically loaded by the container runtime.</p>
      <h2 id="security-deploy-secrets_security-deploy" class="group scroll-mt-20">Creating secrets and config maps<a href="#security-deploy-secrets_security-deploy" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can use the <code>Secret</code> object type to provide a mechanism to hold sensitive information such as passwords, OpenShift Container Platform client configuration files, <code>dockercfg</code> files, and private source repository credentials. Secrets decouple sensitive content from pods.</p>
      <p class="_abstract">You can mount secrets into containers by using a volume plugin or the system can use secrets to perform actions on behalf of a pod.</p>
      <p>Config maps are similar to secrets, but are designed to support working with strings that do not contain sensitive information. The <code>ConfigMap</code> object holds key-value pairs of configuration data that can be consumed in pods or used to store configuration data for system components such as controllers.</p>
      <p>For example, to add a secret to your deployment so that it can access a private image repository, use the following procedure.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Log in to the OpenShift Container Platform web console.</li>
      <li>Create a new project.</li>
      <li>Navigate to <strong>Resources</strong> → <strong>Secrets</strong> and create a new secret. Set <code>Secret Type</code> to <code>Image Secret</code> and <code>Authentication Type</code> to <code>Image Registry Credentials</code> to enter credentials for accessing a private image repository.</li>
      <li>When creating a deployment (for example, from the <strong>Add to Project</strong> → <strong>Deploy Image</strong> page), set the <code>Pull Secret</code> to your new secret.</li>
      </ol>
      <h2 id="security-deploy-continuous_security-deploy" class="group scroll-mt-20">Automating continuous deployment<a href="#security-deploy-continuous_security-deploy" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can integrate your own continuous deployment (CD) tooling with OpenShift Container Platform. </p>
      <p>By leveraging continuous integration and continuous deployment (CI/CD) and OpenShift Container Platform, you can automate the process of rebuilding the application to incorporate the latest fixes, testing, and ensuring that it is deployed everywhere within the environment.</p>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="/openshift-docs-markdown/cicd/builds/creating-build-inputs#builds-input-secrets-configmaps_creating-build-inputs">Input secrets and config maps</a></li></ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
