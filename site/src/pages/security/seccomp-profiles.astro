---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/seccomp-profiles.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"seccomp-profiles","text":"Configuring seccomp profiles"},{"depth":2,"slug":"verifying-default-seccomp-profile_configuring-seccomp-profiles","text":"Verifying the default seccomp profile applied to a pod"},{"depth":3,"slug":"upgraded_cluster_configuring-seccomp-profiles","text":"Upgraded cluster"},{"depth":3,"slug":"newly_installed_configuring-seccomp-profiles","text":"Newly installed cluster"},{"depth":2,"slug":"custom-seccomp-profile","text":"Configuring a custom seccomp profile"},{"depth":3,"slug":"creating-custom-seccomp-profile_configuring-seccomp-profiles","text":"Creating seccomp profiles"},{"depth":3,"slug":"setting-custom-seccomp-profile_configuring-seccomp-profiles","text":"Setting up the custom seccomp profile"},{"depth":3,"slug":"applying-custom-seccomp-profile_configuring-seccomp-profiles","text":"Applying the custom seccomp profile to the workload"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/seccomp-profiles";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring seccomp profiles";
const description = "An OpenShift Container Platform container or a pod runs a single application that performs one or more well-defined tasks. The application usually…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring seccomp profiles {#seccomp-profiles}\n\nAn OpenShift Container Platform container or a pod runs a single application that performs one or more well-defined tasks. The application usually requires only a small subset of the underlying operating system kernel APIs. Secure computing mode, seccomp, is a  Linux kernel feature that can be used to limit the process running in a container to only using a subset of the available system calls.\n\nThe `restricted-v2` SCC applies to all newly created pods in 4.22. The default seccomp profile `runtime/default` is applied to these pods.\n\nSeccomp profiles are stored as JSON files on the disk.\n\n> [!IMPORTANT]\n> Seccomp profiles cannot be applied to privileged containers.\n\n## Verifying the default seccomp profile applied to a pod {#verifying-default-seccomp-profile_configuring-seccomp-profiles}\n\nOpenShift Container Platform ships with a default seccomp profile that is referenced as `runtime/default`. In 4.22, newly created pods have the Security Context Constraint (SCC) set to `restricted-v2` and the default seccomp profile applies to the pod.\n\n**Procedure**\n\n1. You can verify the Security Context Constraint (SCC) and the default seccomp profile set on a pod by running the following commands:\n\n   1. Verify what pods are running in the namespace:\n\n      ```terminal\n      $ oc get pods -n <namespace>\n      ```\n\n      For example, to verify what pods are running in the `workshop` namespace run the following:\n\n      ```terminal\n      $ oc get pods -n workshop\n      ```\n\n      ```terminal {title=\"Example output\"}\n      NAME                READY   STATUS      RESTARTS   AGE\n      parksmap-1-4xkwf    1/1     Running     0          2m17s\n      parksmap-1-deploy   0/1     Completed   0          2m22s\n      ```\n   2. Inspect the pods:\n\n      ```terminal\n      $ oc get pod parksmap-1-4xkwf -n workshop -o yaml\n      ```\n\n      ```terminal {title=\"Example output\"}\n      apiVersion: v1\n      kind: Pod\n      metadata:\n        annotations:\n          k8s.v1.cni.cncf.io/network-status: |-\n            [{\n                \"name\": \"ovn-kubernetes\",\n                \"interface\": \"eth0\",\n                \"ips\": [\n                    \"10.131.0.18\"\n                ],\n                \"default\": true,\n                \"dns\": {}\n            }]\n          k8s.v1.cni.cncf.io/network-status: |-\n            [{\n                \"name\": \"ovn-kubernetes\",\n                \"interface\": \"eth0\",\n                \"ips\": [\n                    \"10.131.0.18\"\n                ],\n                \"default\": true,\n                \"dns\": {}\n            }]\n          openshift.io/deployment-config.latest-version: \"1\"\n          openshift.io/deployment-config.name: parksmap\n          openshift.io/deployment.name: parksmap-1\n          openshift.io/generated-by: OpenShiftWebConsole\n          openshift.io/scc: restricted-v2 (1)\n          seccomp.security.alpha.kubernetes.io/pod: runtime/default (2)\n      ```\n\n      1. The `restricted-v2` SCC is added by default if your workload does not have access to a different SCC.\n      2. Newly created pods in 4.22 will have the seccomp profile configured to `runtime/default` as mandated by the SCC.\n\n### Upgraded cluster {#upgraded_cluster_configuring-seccomp-profiles}\n\nIn clusters upgraded to 4.22 all authenticated users have access to the `restricted` and `restricted-v2` SCC.\n\nA workload admitted by the SCC `restricted` for example, on a OpenShift Container Platform v4.10 cluster when upgraded may get admitted by `restricted-v2`. This is because `restricted-v2` is the more restrictive SCC between `restricted` and `restricted-v2`.\n\n> [!NOTE]\n> The workload must be able to run with `retricted-v2`.\n\nConversely with a workload that requires `privilegeEscalation: true` this workload will continue to have the `restricted` SCC available for any authenticated user. This is because `restricted-v2` does not allow `privilegeEscalation`.\n\n### Newly installed cluster {#newly_installed_configuring-seccomp-profiles}\n\nFor newly installed OpenShift Container Platform 4.11 or later clusters, the `restricted-v2` replaces the `restricted` SCC as an SCC that is available to be used by any authenticated user. A workload with `privilegeEscalation: true`, is not admitted into the cluster since `restricted-v2` is the only SCC available for authenticated users by default.\n\nThe feature `privilegeEscalation` is allowed by `restricted` but not by `restricted-v2`. More features are denied by `restricted-v2` than were allowed by `restricted` SCC.\n\nA workload with `privilegeEscalation: true` may be admitted into a newly installed OpenShift Container Platform 4.11 or later cluster. To give access to the `restricted` SCC to the ServiceAccount running the workload (or any other SCC that can admit this workload) using a RoleBinding run the following command:\n\n```terminal\n$ oc -n <workload-namespace> adm policy add-scc-to-user <scc-name> -z <serviceaccount_name>\n```\n\nIn OpenShift Container Platform 4.22 the ability to add the pod annotations `seccomp.security.alpha.kubernetes.io/pod: runtime/default` and `container.seccomp.security.alpha.kubernetes.io/<container_name>: runtime/default` is deprecated.\n\n## Configuring a custom seccomp profile {#custom-seccomp-profile}\n\nYou can configure a custom seccomp profile, which allows you to update the filters based on the application requirements. This allows cluster administrators to have greater control over the security of workloads running in OpenShift Container Platform.\n\nSeccomp security profiles list the system calls (syscalls) a process can make. Permissions are broader than SELinux, which restrict operations, such as `write`, system-wide.\n\n### Creating seccomp profiles {#creating-custom-seccomp-profile_configuring-seccomp-profiles}\n\nYou can use the `MachineConfig` object to create profiles.\n\nSeccomp can restrict system calls (syscalls) within a container, limiting the access of your application.\n\n**Prerequisites**\n\n- You have cluster admin permissions.\n- You have created a custom security context constraints (SCC). For more information, see *Additional resources*.\n\n**Procedure**\n\n- Create the `MachineConfig` object:\n\n  ```yaml\n  apiVersion: machineconfiguration.openshift.io/v1\n  kind: MachineConfig\n  metadata:\n    labels:\n      machineconfiguration.openshift.io/role: worker\n    name: custom-seccomp\n  spec:\n    config:\n      ignition:\n        version: 3.2.0\n      storage:\n        files:\n        - contents:\n            source: data:text/plain;charset=utf-8;base64,<hash>\n          filesystem: root\n          mode: 0644\n          path: /var/lib/kubelet/seccomp/seccomp-nostat.json\n  ```\n\n### Setting up the custom seccomp profile {#setting-custom-seccomp-profile_configuring-seccomp-profiles}\n\n**Prerequisite**\n\n- You have cluster administrator permissions.\n- You have created a custom security context constraints (SCC). For more information, see \"Additional resources\".\n- You have created a custom seccomp profile.\n\n**Procedure**\n\n1. Upload your custom seccomp profile to `/var/lib/kubelet/seccomp/<custom-name>.json` by using the Machine Config. See \"Additional resources\" for detailed steps.\n2. Update the custom SCC by providing reference to the created custom seccomp profile:\n\n   ```yaml\n   seccompProfiles:\n   - localhost/<custom-name>.json (1)\n   ```\n\n   1. Provide the name of your custom seccomp profile.\n\n### Applying the custom seccomp profile to the workload {#applying-custom-seccomp-profile_configuring-seccomp-profiles}\n\n**Prerequisite**\n\n- The cluster administrator has set up the custom seccomp profile. For more details, see \"Setting up the custom seccomp profile\".\n\n**Procedure**\n\n- Apply the seccomp profile to the workload by setting the `securityContext.seccompProfile.type` field as following:\n\n  ```yaml {title=\"Example\"}\n  spec:\n    securityContext:\n      seccompProfile:\n        type: Localhost\n        localhostProfile: <custom-name>.json (1)\n  ```\n\n  1. Provide the name of your custom seccomp profile.\n\n     Alternatively, you can use the pod annotations `seccomp.security.alpha.kubernetes.io/pod: localhost/<custom-name>.json`. However, this method is deprecated in OpenShift Container Platform 4.22.\n\nDuring deployment, the admission controller validates the following:\n\n- The annotations against the current SCCs allowed by the user role.\n- The SCC, which includes the seccomp profile, is allowed for the pod.\n\nIf the SCC is allowed for the pod, the kubelet runs the pod with the specified seccomp profile.\n\n> [!IMPORTANT]\n> Ensure that the seccomp profile is deployed to all worker nodes.\n\n> [!NOTE]\n> The custom SCC must have the appropriate priority to be automatically assigned to the pod or meet other conditions required by the pod, such as allowing CAP_NET_ADMIN.\n\n**Additional resources**\n{._additional-resources}\n\n- [Managing security context constraints](/openshift-docs-markdown/authentication/managing-security-context-constraints)\n- [Machine Config Overview](/openshift-docs-markdown/machine_configuration/index#machine-config-overview)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/seccomp-profiles.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="seccomp-profiles" class="group scroll-mt-20">Configuring seccomp profiles<a href="#seccomp-profiles" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>An OpenShift Container Platform container or a pod runs a single application that performs one or more well-defined tasks. The application usually requires only a small subset of the underlying operating system kernel APIs. Secure computing mode, seccomp, is a  Linux kernel feature that can be used to limit the process running in a container to only using a subset of the available system calls.</p>
      <p>The <code>restricted-v2</code> SCC applies to all newly created pods in 4.22. The default seccomp profile <code>runtime/default</code> is applied to these pods.</p>
      <p>Seccomp profiles are stored as JSON files on the disk.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Seccomp profiles cannot be applied to privileged containers.</p>
        </AlertDescription>
      </Alert>
      <h2 id="verifying-default-seccomp-profile_configuring-seccomp-profiles" class="group scroll-mt-20">Verifying the default seccomp profile applied to a pod<a href="#verifying-default-seccomp-profile_configuring-seccomp-profiles" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>OpenShift Container Platform ships with a default seccomp profile that is referenced as <code>runtime/default</code>. In 4.22, newly created pods have the Security Context Constraint (SCC) set to <code>restricted-v2</code> and the default seccomp profile applies to the pod.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      You can verify the Security Context Constraint (SCC) and the default seccomp profile set on a pod by running the following commands:
      <ol>
      <li>
      Verify what pods are running in the namespace:
      <CodeBlock code={"$ oc get pods -n <namespace>"} lang="terminal" />
      <p>For example, to verify what pods are running in the <code>workshop</code> namespace run the following:</p>
      <CodeBlock code={"$ oc get pods -n workshop"} lang="terminal" />
      <CodeBlock code={"NAME                READY   STATUS      RESTARTS   AGE\nparksmap-1-4xkwf    1/1     Running     0          2m17s\nparksmap-1-deploy   0/1     Completed   0          2m22s"} lang="terminal" title="Example output" />
      </li>
      <li>
      Inspect the pods:
      <CodeBlock code={"$ oc get pod parksmap-1-4xkwf -n workshop -o yaml"} lang="terminal" />
      <CodeBlock code={"apiVersion: v1\nkind: Pod\nmetadata:\n  annotations:\n    k8s.v1.cni.cncf.io/network-status: |-\n      [{\n          \"name\": \"ovn-kubernetes\",\n          \"interface\": \"eth0\",\n          \"ips\": [\n              \"10.131.0.18\"\n          ],\n          \"default\": true,\n          \"dns\": {}\n      }]\n    k8s.v1.cni.cncf.io/network-status: |-\n      [{\n          \"name\": \"ovn-kubernetes\",\n          \"interface\": \"eth0\",\n          \"ips\": [\n              \"10.131.0.18\"\n          ],\n          \"default\": true,\n          \"dns\": {}\n      }]\n    openshift.io/deployment-config.latest-version: \"1\"\n    openshift.io/deployment-config.name: parksmap\n    openshift.io/deployment.name: parksmap-1\n    openshift.io/generated-by: OpenShiftWebConsole\n    openshift.io/scc: restricted-v2 (1)\n    seccomp.security.alpha.kubernetes.io/pod: runtime/default (2)"} lang="terminal" title="Example output" />
      <ol>
      <li>The <code>restricted-v2</code> SCC is added by default if your workload does not have access to a different SCC.</li>
      <li>Newly created pods in 4.22 will have the seccomp profile configured to <code>runtime/default</code> as mandated by the SCC.</li>
      </ol>
      </li>
      </ol>
      </li>
      </ol>
      <h3 id="upgraded_cluster_configuring-seccomp-profiles" class="group scroll-mt-20">Upgraded cluster<a href="#upgraded_cluster_configuring-seccomp-profiles" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>In clusters upgraded to 4.22 all authenticated users have access to the <code>restricted</code> and <code>restricted-v2</code> SCC.</p>
      <p>A workload admitted by the SCC <code>restricted</code> for example, on a OpenShift Container Platform v4.10 cluster when upgraded may get admitted by <code>restricted-v2</code>. This is because <code>restricted-v2</code> is the more restrictive SCC between <code>restricted</code> and <code>restricted-v2</code>.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The workload must be able to run with <code>retricted-v2</code>.</p>
        </AlertDescription>
      </Alert>
      <p>Conversely with a workload that requires <code>privilegeEscalation: true</code> this workload will continue to have the <code>restricted</code> SCC available for any authenticated user. This is because <code>restricted-v2</code> does not allow <code>privilegeEscalation</code>.</p>
      <h3 id="newly_installed_configuring-seccomp-profiles" class="group scroll-mt-20">Newly installed cluster<a href="#newly_installed_configuring-seccomp-profiles" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>For newly installed OpenShift Container Platform 4.11 or later clusters, the <code>restricted-v2</code> replaces the <code>restricted</code> SCC as an SCC that is available to be used by any authenticated user. A workload with <code>privilegeEscalation: true</code>, is not admitted into the cluster since <code>restricted-v2</code> is the only SCC available for authenticated users by default.</p>
      <p>The feature <code>privilegeEscalation</code> is allowed by <code>restricted</code> but not by <code>restricted-v2</code>. More features are denied by <code>restricted-v2</code> than were allowed by <code>restricted</code> SCC.</p>
      <p>A workload with <code>privilegeEscalation: true</code> may be admitted into a newly installed OpenShift Container Platform 4.11 or later cluster. To give access to the <code>restricted</code> SCC to the ServiceAccount running the workload (or any other SCC that can admit this workload) using a RoleBinding run the following command:</p>
      <CodeBlock code={"$ oc -n <workload-namespace> adm policy add-scc-to-user <scc-name> -z <serviceaccount_name>"} lang="terminal" />
      <p>In OpenShift Container Platform 4.22 the ability to add the pod annotations <code>seccomp.security.alpha.kubernetes.io/pod: runtime/default</code> and <code>container.seccomp.security.alpha.kubernetes.io/&lt;container_name&gt;: runtime/default</code> is deprecated.</p>
      <h2 id="custom-seccomp-profile" class="group scroll-mt-20">Configuring a custom seccomp profile<a href="#custom-seccomp-profile" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can configure a custom seccomp profile, which allows you to update the filters based on the application requirements. This allows cluster administrators to have greater control over the security of workloads running in OpenShift Container Platform.</p>
      <p>Seccomp security profiles list the system calls (syscalls) a process can make. Permissions are broader than SELinux, which restrict operations, such as <code>write</code>, system-wide.</p>
      <h3 id="creating-custom-seccomp-profile_configuring-seccomp-profiles" class="group scroll-mt-20">Creating seccomp profiles<a href="#creating-custom-seccomp-profile_configuring-seccomp-profiles" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>You can use the <code>MachineConfig</code> object to create profiles.</p>
      <p>Seccomp can restrict system calls (syscalls) within a container, limiting the access of your application.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have cluster admin permissions.</li>
      <li>You have created a custom security context constraints (SCC). For more information, see <em>Additional resources</em>.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Create the <code>MachineConfig</code> object:
      <CodeBlock code={"apiVersion: machineconfiguration.openshift.io/v1\nkind: MachineConfig\nmetadata:\n  labels:\n    machineconfiguration.openshift.io/role: worker\n  name: custom-seccomp\nspec:\n  config:\n    ignition:\n      version: 3.2.0\n    storage:\n      files:\n      - contents:\n          source: data:text/plain;charset=utf-8;base64,<hash>\n        filesystem: root\n        mode: 0644\n        path: /var/lib/kubelet/seccomp/seccomp-nostat.json"} lang="yaml" />
      </li>
      </ul>
      <h3 id="setting-custom-seccomp-profile_configuring-seccomp-profiles" class="group scroll-mt-20">Setting up the custom seccomp profile<a href="#setting-custom-seccomp-profile_configuring-seccomp-profiles" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p><strong>Prerequisite</strong></p>
      <ul>
      <li>You have cluster administrator permissions.</li>
      <li>You have created a custom security context constraints (SCC). For more information, see "Additional resources".</li>
      <li>You have created a custom seccomp profile.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Upload your custom seccomp profile to <code>/var/lib/kubelet/seccomp/&lt;custom-name&gt;.json</code> by using the Machine Config. See "Additional resources" for detailed steps.</li>
      <li>
      Update the custom SCC by providing reference to the created custom seccomp profile:
      <CodeBlock code={"seccompProfiles:\n- localhost/<custom-name>.json (1)"} lang="yaml" />
      <ol><li>Provide the name of your custom seccomp profile.</li></ol>
      </li>
      </ol>
      <h3 id="applying-custom-seccomp-profile_configuring-seccomp-profiles" class="group scroll-mt-20">Applying the custom seccomp profile to the workload<a href="#applying-custom-seccomp-profile_configuring-seccomp-profiles" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p><strong>Prerequisite</strong></p>
      <ul><li>The cluster administrator has set up the custom seccomp profile. For more details, see "Setting up the custom seccomp profile".</li></ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Apply the seccomp profile to the workload by setting the <code>securityContext.seccompProfile.type</code> field as following:
      <CodeBlock code={"spec:\n  securityContext:\n    seccompProfile:\n      type: Localhost\n      localhostProfile: <custom-name>.json (1)"} lang="yaml" title="Example" />
      <ol>
      <li>
      Provide the name of your custom seccomp profile.
      <p>Alternatively, you can use the pod annotations <code>seccomp.security.alpha.kubernetes.io/pod: localhost/&lt;custom-name&gt;.json</code>. However, this method is deprecated in OpenShift Container Platform 4.22.</p>
      </li>
      </ol>
      </li>
      </ul>
      <p>During deployment, the admission controller validates the following:</p>
      <ul>
      <li>The annotations against the current SCCs allowed by the user role.</li>
      <li>The SCC, which includes the seccomp profile, is allowed for the pod.</li>
      </ul>
      <p>If the SCC is allowed for the pod, the kubelet runs the pod with the specified seccomp profile.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Ensure that the seccomp profile is deployed to all worker nodes.</p>
        </AlertDescription>
      </Alert>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The custom SCC must have the appropriate priority to be automatically assigned to the pod or meet other conditions required by the pod, such as allowing CAP_NET_ADMIN.</p>
        </AlertDescription>
      </Alert>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/authentication/managing-security-context-constraints">Managing security context constraints</a></li>
      <li><a href="/openshift-docs-markdown/machine_configuration/index#machine-config-overview">Machine Config Overview</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
