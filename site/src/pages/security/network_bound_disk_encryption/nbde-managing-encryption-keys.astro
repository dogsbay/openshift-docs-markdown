---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/network_bound_disk_encryption/nbde-managing-encryption-keys.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";

const headings = [{"depth":1,"slug":"nbde-managing-encryption-keys","text":"Tang server encryption key management"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/network_bound_disk_encryption/nbde-managing-encryption-keys";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Tang server encryption key management";
const description = "The cryptographic mechanism to recreate the encryption key is based on the blinded key stored on the node and the private key of the involved Tang servers." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/network_bound_disk_encryption"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Tang server encryption key management {#nbde-managing-encryption-keys}\n\nThe cryptographic mechanism to recreate the encryption key is based on the *blinded key* stored on the node and the private key of the involved Tang servers.\n\n> [!NOTE]\n> To protect against the possibility of an attacker who has obtained both the Tang server private key and the node’s encrypted disk, periodic rekeying is advisable.\n>\n> You must perform the rekeying operation for every node before you can delete the old key from the Tang server.\n\nThe following sections provide procedures for rekeying and deleting old keys.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/network_bound_disk_encryption/nbde-managing-encryption-keys.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="nbde-managing-encryption-keys" class="group scroll-mt-20">Tang server encryption key management<a href="#nbde-managing-encryption-keys" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="/_attributes/common-attributes.md"></p>
      <p>The cryptographic mechanism to recreate the encryption key is based on the <em>blinded key</em> stored on the node and the private key of the involved Tang servers.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>To protect against the possibility of an attacker who has obtained both the Tang server private key and the node’s encrypted disk, periodic rekeying is advisable.</p>
          <p>You must perform the rekeying operation for every node before you can delete the old key from the Tang server.</p>
        </AlertDescription>
      </Alert>
      <p>The following sections provide procedures for rekeying and deleting old keys.</p>
      <p class="/modules/nbde-backing-up-server-keys.md"></p>
      <p class="/modules/nbde-recovering-server-keys.md"></p>
      <p class="/modules/nbde-rekeying-tang-servers.md"></p>
      <p class="/modules/nbde-generating-a-new-tang-server-key.md"></p>
      <p class="/modules/nbde-rekeying-all-nbde-nodes.md"></p>
      <p class="/modules/nbde-troubleshooting-temporary-error-conditions.md"></p>
      <p class="/modules/nbde-troubleshooting-permanent-error-conditions.md"></p>
      <p class="/modules/nbde-deleting-old-tang-server-keys.md"></p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
