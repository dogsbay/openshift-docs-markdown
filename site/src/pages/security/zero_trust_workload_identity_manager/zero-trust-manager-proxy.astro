---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/zero_trust_workload_identity_manager/zero-trust-manager-proxy.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"zero-trust-manager-proxy_{{ context }}","text":"Configuring the egress proxy for the {{ zero_trust_full }}"},{"depth":2,"slug":"zero-trust-manager-proxy-support_zero-trust-manager-proxy","text":"Injecting a custom CA certificate for the Zero Trust Workload Identity Manager"},{"depth":2,"slug":"zero-trust-manager-proxy_additional-resources","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/zero_trust_workload_identity_manager/zero-trust-manager-proxy";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Configuring the egress proxy for the {{ zero_trust_full }}";
const description = "Operator Lifecycle Manager (OLM) automatically configures managed Operators with proxy settings when you use a cluster-wide egress proxy. To support…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/zero_trust_workload_identity_manager"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Configuring the egress proxy for the {{ zero_trust_full }} {#zero-trust-manager-proxy_{{ context }}}\n\nOperator Lifecycle Manager (OLM) automatically configures managed Operators with proxy settings when you use a cluster-wide egress proxy. To support proxying HTTPS connections, you can inject certificate authority (CA) certificates into the Zero Trust Workload Identity Manager.\n\n## Injecting a custom CA certificate for the Zero Trust Workload Identity Manager {#zero-trust-manager-proxy-support_zero-trust-manager-proxy}\n\nInject certificate authority (CA) certificates into the Zero Trust Workload Identity Manager to support proxying HTTPS connections. This configuration helps ensure that the Identity Manager can communicate securely when you enable a cluster-wide proxy.\n\n**Prerequisites**\n\n- You have access to the cluster as a user with the `cluster-admin` role.\n- You have enabled the cluster-wide proxy for OpenShift Container Platform.\n- You have installed Zero Trust Workload Identity Manager 1.0.0 or later.\n- You have deployed the SPIRE Server, SPIRE Agent, SPIFFE CSI Driver, and the SPIRE OIDC Discovery Provider operands in the cluster.\n\n**Procedure**\n\n1. Create a config map in the `zero-trust-workload-identity-manager` namespace by running the following command:\n\n   ```terminal\n   $ oc create configmap trusted-ca -n zero-trust-workload-identity-manager\n   ```\n2. Inject the CA bundle that is trusted by OpenShift Container Platform into the config map by running the following command:\n\n   ```terminal\n   $ oc label cm trusted-ca config.openshift.io/inject-trusted-cabundle=true -n zero-trust-workload-identity-manager\n   ```\n3. Update the subscription for the Zero Trust Workload Identity Manager to use the config map by running the following command:\n\n   ```terminal\n   $ oc -n zero-trust-workload-identity-manager patch subscription openshift-zero-trust-workload-identity-manager --type='merge' -p '{\"spec\":{\"config\":{\"env\":[{\"name\":\"TRUSTED_CA_BUNDLE_CONFIGMAP\",\"value\":\"trusted-ca\"}]}}}'\n   ```\n\n**Verification**\n\n1. Verify that the operands have finished rolling out by running the following command:\n\n   ```terminal\n   $ oc rollout status deployment/zero-trust-workload-identity-manager-controller-manager -n zero-trust-workload-identity-manager && \\\n   $ oc rollout status statefulset/spireserver -n zero-trust-workload-identity-manager && \\\n   $ oc rollout status daemonset/spire-agent -n zero-trust-workload-identity-manager && \\\n   $ oc rollout status deployment/spire-spiffe-oidc-discovery-provider -n zero-trust-workload-identity-manager\n   ```\n\n   ```terminal {title=\"Example output\"}\n   deployment \"zero-trust-workload-identity-manager-controller-manager\" successfully rolled out\n   statefulset \"spire-server\" successfully rolled out\n   daemonset \"spire-agent\" successfully rolled out\n   deployment \"spire-spiffe-oidc-discovery-provider\" successfully rolled out\n   ```\n2. Verify that the CA bundle was mounted as a volume by running the following command:\n\n   ```terminal\n   $ oc get deployment zero-trust-workload-identity-manager -n zero-trust-workload-identity-manager -o=jsonpath={.spec.template.spec.'containers[0].volumeMounts'}\n   ```\n\n   ```terminal\n   $ oc get statefulset spire-server -n zero-trust-workload-identity-manager -o jsonpath='{.spec.template.spec.containers[*].volumeMounts[?(@.name==\"trusted-ca-bundle\")]}'\n   ```\n\n   ```terminal\n   $ oc get daemonset spire-agent -n zero-trust-workload-identity-manager -o jsonpath='{.spec.template.spec.containers[*].volumeMounts[?(@.name==\"trusted-ca-bundle\")]}'\n   ```\n\n   ```terminal\n   $ oc get daemonset spire-spiffe-csi-driver -n zero-trust-workload-identity-manager -o jsonpath='{.spec.template.spec.containers[*].volumeMounts[?(@.name==\"trusted-ca-bundle\")]}'\n   ```\n\n   ```terminal {title=\"Example output\"}\n   [{{\"mountPath\":\"/etc/pki/ca-trust/extracted/pem\",\"name\":\"trusted-ca-bundle\",\"readOnly\":true}]\n   ```\n3. Verify that the source of the CA bundle is the `trusted-ca` config map by running the following command:\n\n   ```terminal\n   $ oc get deployment zero-trust-workload-identity-manager -n zero-trust-workload-identity-manager -o=jsonpath={.spec.template.spec.volumes}\n   ```\n\n   ```terminal\n   $ oc get statefulset spire-server -n zero-trust-workload-identity-manager -o=jsonpath='{.spec.template.spec.volumes}' | jq '.[] | select(.name==\"trusted-ca-bundle\")'\n   ```\n\n   ```terminal\n   $ oc get daemonset spire-agent -n zero-trust-workload-identity-manager -o=jsonpath='{.spec.template.spec.volumes}' | jq '.[] | select(.name==\"trusted-ca-bundle\")'\n   ```\n\n   ```terminal\n   $ oc get deployment spire-spiffe-oidc-discovery-provider -n zero-trust-workload-identity-manager -o=jsonpath='{.spec.template.spec.volumes}' | jq '.[] | select(.name==\"trusted-ca-bundle\")'\n   ```\n\n   ```terminal {title=\"Example output\"}\n   {\n     \"configMap\": {\n       \"defaultMode\": 420,\n       \"items\": [\n         {\n           \"key\": \"ca-bundle.crt\",\n           \"path\": \"tls-ca-bundle.pem\"\n         }\n       ],\n       \"name\": \"trusted-ca\"\n     },\n     \"name\": \"trusted-ca-bundle\"\n   }\n   ```\n\n## Additional resources {#zero-trust-manager-proxy_additional-resources}\n\n- [Configuring proxy support in Operator Lifecycle Manager](/openshift-docs-markdown/operators/admin/olm-configuring-proxy-support#olm-configuring-proxy-support)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/zero_trust_workload_identity_manager/zero-trust-manager-proxy.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="zero-trust-manager-proxy_{{ context }}" class="group scroll-mt-20">Configuring the egress proxy for the {"{"}{"{"} zero_trust_full {"}"}{"}"}<a href="#zero-trust-manager-proxy_{{ context }}" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Operator Lifecycle Manager (OLM) automatically configures managed Operators with proxy settings when you use a cluster-wide egress proxy. To support proxying HTTPS connections, you can inject certificate authority (CA) certificates into the Zero Trust Workload Identity Manager.</p>
      <h2 id="zero-trust-manager-proxy-support_zero-trust-manager-proxy" class="group scroll-mt-20">Injecting a custom CA certificate for the Zero Trust Workload Identity Manager<a href="#zero-trust-manager-proxy-support_zero-trust-manager-proxy" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Inject certificate authority (CA) certificates into the Zero Trust Workload Identity Manager to support proxying HTTPS connections. This configuration helps ensure that the Identity Manager can communicate securely when you enable a cluster-wide proxy.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster as a user with the <code>cluster-admin</code> role.</li>
      <li>You have enabled the cluster-wide proxy for OpenShift Container Platform.</li>
      <li>You have installed Zero Trust Workload Identity Manager 1.0.0 or later.</li>
      <li>You have deployed the SPIRE Server, SPIRE Agent, SPIFFE CSI Driver, and the SPIRE OIDC Discovery Provider operands in the cluster.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a config map in the <code>zero-trust-workload-identity-manager</code> namespace by running the following command:
      <CodeBlock code={"$ oc create configmap trusted-ca -n zero-trust-workload-identity-manager"} lang="terminal" />
      </li>
      <li>
      Inject the CA bundle that is trusted by OpenShift Container Platform into the config map by running the following command:
      <CodeBlock code={"$ oc label cm trusted-ca config.openshift.io/inject-trusted-cabundle=true -n zero-trust-workload-identity-manager"} lang="terminal" />
      </li>
      <li>
      Update the subscription for the Zero Trust Workload Identity Manager to use the config map by running the following command:
      <CodeBlock code={"$ oc -n zero-trust-workload-identity-manager patch subscription openshift-zero-trust-workload-identity-manager --type='merge' -p '{\"spec\":{\"config\":{\"env\":[{\"name\":\"TRUSTED_CA_BUNDLE_CONFIGMAP\",\"value\":\"trusted-ca\"}]}}}'"} lang="terminal" />
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>
      Verify that the operands have finished rolling out by running the following command:
      <CodeBlock code={"$ oc rollout status deployment/zero-trust-workload-identity-manager-controller-manager -n zero-trust-workload-identity-manager && \\\n$ oc rollout status statefulset/spireserver -n zero-trust-workload-identity-manager && \\\n$ oc rollout status daemonset/spire-agent -n zero-trust-workload-identity-manager && \\\n$ oc rollout status deployment/spire-spiffe-oidc-discovery-provider -n zero-trust-workload-identity-manager"} lang="terminal" />
      <CodeBlock code={"deployment \"zero-trust-workload-identity-manager-controller-manager\" successfully rolled out\nstatefulset \"spire-server\" successfully rolled out\ndaemonset \"spire-agent\" successfully rolled out\ndeployment \"spire-spiffe-oidc-discovery-provider\" successfully rolled out"} lang="terminal" title="Example output" />
      </li>
      <li>
      Verify that the CA bundle was mounted as a volume by running the following command:
      <CodeBlock code={"$ oc get deployment zero-trust-workload-identity-manager -n zero-trust-workload-identity-manager -o=jsonpath={.spec.template.spec.'containers[0].volumeMounts'}"} lang="terminal" />
      <CodeBlock code={"$ oc get statefulset spire-server -n zero-trust-workload-identity-manager -o jsonpath='{.spec.template.spec.containers[*].volumeMounts[?(@.name==\"trusted-ca-bundle\")]}'"} lang="terminal" />
      <CodeBlock code={"$ oc get daemonset spire-agent -n zero-trust-workload-identity-manager -o jsonpath='{.spec.template.spec.containers[*].volumeMounts[?(@.name==\"trusted-ca-bundle\")]}'"} lang="terminal" />
      <CodeBlock code={"$ oc get daemonset spire-spiffe-csi-driver -n zero-trust-workload-identity-manager -o jsonpath='{.spec.template.spec.containers[*].volumeMounts[?(@.name==\"trusted-ca-bundle\")]}'"} lang="terminal" />
      <CodeBlock code={"[{{\"mountPath\":\"/etc/pki/ca-trust/extracted/pem\",\"name\":\"trusted-ca-bundle\",\"readOnly\":true}]"} lang="terminal" title="Example output" />
      </li>
      <li>
      Verify that the source of the CA bundle is the <code>trusted-ca</code> config map by running the following command:
      <CodeBlock code={"$ oc get deployment zero-trust-workload-identity-manager -n zero-trust-workload-identity-manager -o=jsonpath={.spec.template.spec.volumes}"} lang="terminal" />
      <CodeBlock code={"$ oc get statefulset spire-server -n zero-trust-workload-identity-manager -o=jsonpath='{.spec.template.spec.volumes}' | jq '.[] | select(.name==\"trusted-ca-bundle\")'"} lang="terminal" />
      <CodeBlock code={"$ oc get daemonset spire-agent -n zero-trust-workload-identity-manager -o=jsonpath='{.spec.template.spec.volumes}' | jq '.[] | select(.name==\"trusted-ca-bundle\")'"} lang="terminal" />
      <CodeBlock code={"$ oc get deployment spire-spiffe-oidc-discovery-provider -n zero-trust-workload-identity-manager -o=jsonpath='{.spec.template.spec.volumes}' | jq '.[] | select(.name==\"trusted-ca-bundle\")'"} lang="terminal" />
      <CodeBlock code={"{\n  \"configMap\": {\n    \"defaultMode\": 420,\n    \"items\": [\n      {\n        \"key\": \"ca-bundle.crt\",\n        \"path\": \"tls-ca-bundle.pem\"\n      }\n    ],\n    \"name\": \"trusted-ca\"\n  },\n  \"name\": \"trusted-ca-bundle\"\n}"} lang="terminal" title="Example output" />
      </li>
      </ol>
      <h2 id="zero-trust-manager-proxy_additional-resources" class="group scroll-mt-20">Additional resources<a href="#zero-trust-manager-proxy_additional-resources" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul><li><a href="/openshift-docs-markdown/operators/admin/olm-configuring-proxy-support#olm-configuring-proxy-support">Configuring proxy support in Operator Lifecycle Manager</a></li></ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
