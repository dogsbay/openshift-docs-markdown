---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/security_profiles_operator/spo-uninstalling.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"spo-uninstalling","text":"Uninstalling the Security Profiles Operator"},{"depth":2,"slug":"spo-uninstall-console_spo-uninstalling","text":"Uninstall the Security Profiles Operator by using the web console"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/security_profiles_operator/spo-uninstalling";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Uninstalling the Security Profiles Operator";
const description = "You can remove the Security Profiles Operator from your cluster by using the OpenShift Container Platform web console." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/security_profiles_operator"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Uninstalling the Security Profiles Operator {#spo-uninstalling}\n\nYou can remove the Security Profiles Operator from your cluster by using the OpenShift Container Platform web console.\n\n## Uninstall the Security Profiles Operator by using the web console {#spo-uninstall-console_spo-uninstalling}\n\nTo remove the Security Profiles Operator, you must first delete the `seccomp` and SELinux profiles. After the profiles are removed, you can then remove the Operator and its namespace by deleting the **openshift-security-profiles** project.\n\n**Prerequisites**\n\n- You have access to the web console as a user with `cluster-admin` privileges.\n- The Security Profiles Operator is installed.\n\n**Procedure**\n\n1. Navigate to the **Ecosystem** → **Installed Operators** page.\n2. Delete all `seccomp` profiles, SELinux profiles, and webhook configurations.\n3. Switch to the **Administration** → **Ecosystem** → **Installed Operators** page.\n4. Click the Options menu ![](/openshift-docs-markdown/images/kebab.png \"Options menu\") on the **Security Profiles Operator** entry.\n5. Select **Uninstall Operator**.\n6. Switch to the **Home** → **Projects** page.\n7. Search for `security profiles`.\n8. Click the Options menu ![](/openshift-docs-markdown/images/kebab.png \"Options menu\") next to the **openshift-security-profiles** project.\n9. Select **Delete Project**.\n\n   1. Enter `openshift-security-profiles` in the dialog box.\n   2. Click **Delete**.\n10. Delete the `MutatingWebhookConfiguration` object by running the following command:\n\n    ```terminal\n    $ oc delete MutatingWebhookConfiguration spo-mutating-webhook-configuration\n    ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/security_profiles_operator/spo-uninstalling.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="spo-uninstalling" class="group scroll-mt-20">Uninstalling the Security Profiles Operator<a href="#spo-uninstalling" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">You can remove the Security Profiles Operator from your cluster by using the OpenShift Container Platform web console.</p>
      <h2 id="spo-uninstall-console_spo-uninstalling" class="group scroll-mt-20">Uninstall the Security Profiles Operator by using the web console<a href="#spo-uninstall-console_spo-uninstalling" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To remove the Security Profiles Operator, you must first delete the <code>seccomp</code> and SELinux profiles. After the profiles are removed, you can then remove the Operator and its namespace by deleting the <strong>openshift-security-profiles</strong> project.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the web console as a user with <code>cluster-admin</code> privileges.</li>
      <li>The Security Profiles Operator is installed.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Navigate to the <strong>Ecosystem</strong> → <strong>Installed Operators</strong> page.</li>
      <li>Delete all <code>seccomp</code> profiles, SELinux profiles, and webhook configurations.</li>
      <li>Switch to the <strong>Administration</strong> → <strong>Ecosystem</strong> → <strong>Installed Operators</strong> page.</li>
      <li>Click the Options menu <img src="/openshift-docs-markdown/images/kebab.png" alt="" loading="lazy" /> on the <strong>Security Profiles Operator</strong> entry.</li>
      <li>Select <strong>Uninstall Operator</strong>.</li>
      <li>Switch to the <strong>Home</strong> → <strong>Projects</strong> page.</li>
      <li>Search for <code>security profiles</code>.</li>
      <li>Click the Options menu <img src="/openshift-docs-markdown/images/kebab.png" alt="" loading="lazy" /> next to the <strong>openshift-security-profiles</strong> project.</li>
      <li>
      Select <strong>Delete Project</strong>.
      <ol>
      <li>Enter <code>openshift-security-profiles</code> in the dialog box.</li>
      <li>Click <strong>Delete</strong>.</li>
      </ol>
      </li>
      <li>
      Delete the <code>MutatingWebhookConfiguration</code> object by running the following command:
      <CodeBlock code={"$ oc delete MutatingWebhookConfiguration spo-mutating-webhook-configuration"} lang="terminal" />
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
