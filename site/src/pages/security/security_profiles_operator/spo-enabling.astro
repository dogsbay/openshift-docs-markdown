---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/security_profiles_operator/spo-enabling.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"spo-enabling","text":"Enabling the Security Profiles Operator"},{"depth":2,"slug":"spo-installing_spo-enabling","text":"Installing the Security Profiles Operator"},{"depth":2,"slug":"spo-installing-cli_spo-enabling","text":"Installing the Security Profiles Operator using the CLI"},{"depth":2,"slug":"logging-verbosity_spo-enabling","text":"Configuring logging verbosity"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/security_profiles_operator/spo-enabling";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Enabling the Security Profiles Operator";
const description = "Before you can use the Security Profiles Operator, you must ensure the Operator is deployed in the cluster." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/security_profiles_operator"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Enabling the Security Profiles Operator {#spo-enabling}\n\nBefore you can use the Security Profiles Operator, you must ensure the Operator is deployed in the cluster.\n\n> [!IMPORTANT]\n> All cluster nodes must have the same release version in order for this Operator to function properly. As an example, for nodes running RHCOS, all nodes must have the same RHCOS version.\n\n> [!IMPORTANT]\n> The Security Profiles Operator supports only Red Hat Enterprise Linux CoreOS (RHCOS) worker nodes. Red Hat Enterprise Linux (RHEL) nodes are not supported.\n\n> [!IMPORTANT]\n> The Security Profiles Operator supports `x86_64` and `ppc64le` architecture.\n\n## Installing the Security Profiles Operator {#spo-installing_spo-enabling}\n\nYou can use the OpenShift Container Platform web console to install the Security Profiles Operator. This installs the Security Profiles Operator into the `openshift-security-profiles` namespace by default. You can also verify correct installation by using the OpenShift Container Platform web console.\n\n**Prerequisites**\n\n- You must have access to the web console as a user with `cluster-admin` privileges.\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, navigate to **Ecosystem** -> **Software Catalog**.\n2. Search for the Security Profiles Operator, then click **Install**.\n3. Keep the default selection of **Installation mode** and **namespace** to ensure that the Operator will be installed to the `openshift-security-profiles` namespace.\n4. Click **Install**.\n\n**Verification**\n\nTo confirm that the installation is successful:\n\n1. Navigate to the **Ecosystem** -> **Installed Operators** page.\n2. Check that the Security Profiles Operator is installed in the `openshift-security-profiles` namespace and its status is `Succeeded`.\n\nIf the Operator is not installed successfully:\n\n1. Navigate to the **Ecosystem** -> **Installed Operators** page and inspect the `Status` column for any errors or failures.\n2. Navigate to the **Workloads** -> **Pods** page and check the logs in any pods in the `openshift-security-profiles` project that are reporting issues.\n\n## Installing the Security Profiles Operator using the CLI {#spo-installing-cli_spo-enabling}\n\nYou can install the OpenShift Container Platform Security Profiles Operator by using the command line interface.\n\n**Prerequisites**\n\n- You must have `cluster-admin` privileges.\n\n**Procedure**\n\n1. Define a `Namespace` object:\n\n   ```yaml {title=\"Example namespace-object.yaml\"}\n   apiVersion: v1\n   kind: Namespace\n   metadata:\n       name: openshift-security-profiles\n   labels:\n     openshift.io/cluster-monitoring: \"true\"\n   ```\n2. Create the `Namespace` object:\n\n   ```terminal\n   $ oc create -f namespace-object.yaml\n   ```\n3. Define an `OperatorGroup` object:\n\n   ```yaml {title=\"Example operator-group-object.yaml\"}\n   apiVersion: operators.coreos.com/v1\n   kind: OperatorGroup\n   metadata:\n     name: security-profiles-operator\n     namespace: openshift-security-profiles\n   ```\n4. Create the `OperatorGroup` object:\n\n   ```terminal\n   $ oc create -f operator-group-object.yaml\n   ```\n5. Define a `Subscription` object:\n\n   ```yaml {title=\"Example subscription-object.yaml\"}\n   apiVersion: operators.coreos.com/v1alpha1\n   kind: Subscription\n   metadata:\n     name: security-profiles-operator-sub\n     namespace: openshift-security-profiles\n   spec:\n     channel: release-alpha-rhel-8\n     installPlanApproval: Automatic\n     name: security-profiles-operator\n     source: redhat-operators\n     sourceNamespace: openshift-marketplace\n   ```\n6. Create the `Subscription` object:\n\n   ```terminal\n   $ oc create -f subscription-object.yaml\n   ```\n\n   > [!NOTE]\n   > If you are setting the global scheduler feature and enable `defaultNodeSelector`, you must create the namespace manually and update the annotations of the `openshift-security-profiles` namespace, or the namespace where the Security Profiles Operator was installed, with `openshift.io/node-selector: “”`. This removes the default node selector and prevents deployment failures.\n\n**Verification**\n\n1. Verify the installation succeeded by inspecting the following CSV file:\n\n   ```terminal\n   $ oc get csv -n openshift-security-profiles\n   ```\n2. Verify that the Security Profiles Operator is operational by running the following command:\n\n   ```terminal\n   $ oc get deploy -n openshift-security-profiles\n   ```\n\n## Configuring logging verbosity {#logging-verbosity_spo-enabling}\n\nThe Security Profiles Operator supports the default logging verbosity of `0` and an enhanced verbosity of `1`.\n\n**Procedure**\n\n- To enable enhanced logging verbosity, patch the `spod` configuration and adjust the value by running the following command:\n\n  ```terminal\n  $ oc -n openshift-security-profiles patch spod \\\n      spod --type=merge -p '{\"spec\":{\"verbosity\":1}}'\n  ```\n\n  ```terminal {title=\"Example output\"}\n  securityprofilesoperatordaemon.security-profiles-operator.x-k8s.io/spod patched\n  ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/security_profiles_operator/spo-enabling.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="spo-enabling" class="group scroll-mt-20">Enabling the Security Profiles Operator<a href="#spo-enabling" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Before you can use the Security Profiles Operator, you must ensure the Operator is deployed in the cluster.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>All cluster nodes must have the same release version in order for this Operator to function properly. As an example, for nodes running RHCOS, all nodes must have the same RHCOS version.</p>
        </AlertDescription>
      </Alert>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>The Security Profiles Operator supports only Red Hat Enterprise Linux CoreOS (RHCOS) worker nodes. Red Hat Enterprise Linux (RHEL) nodes are not supported.</p>
        </AlertDescription>
      </Alert>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>The Security Profiles Operator supports <code>x86_64</code> and <code>ppc64le</code> architecture.</p>
        </AlertDescription>
      </Alert>
      <h2 id="spo-installing_spo-enabling" class="group scroll-mt-20">Installing the Security Profiles Operator<a href="#spo-installing_spo-enabling" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can use the OpenShift Container Platform web console to install the Security Profiles Operator. This installs the Security Profiles Operator into the <code>openshift-security-profiles</code> namespace by default. You can also verify correct installation by using the OpenShift Container Platform web console.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You must have access to the web console as a user with <code>cluster-admin</code> privileges.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, navigate to <strong>Ecosystem</strong> -&gt; <strong>Software Catalog</strong>.</li>
      <li>Search for the Security Profiles Operator, then click <strong>Install</strong>.</li>
      <li>Keep the default selection of <strong>Installation mode</strong> and <strong>namespace</strong> to ensure that the Operator will be installed to the <code>openshift-security-profiles</code> namespace.</li>
      <li>Click <strong>Install</strong>.</li>
      </ol>
      <p><strong>Verification</strong></p>
      <p>To confirm that the installation is successful:</p>
      <ol>
      <li>Navigate to the <strong>Ecosystem</strong> -&gt; <strong>Installed Operators</strong> page.</li>
      <li>Check that the Security Profiles Operator is installed in the <code>openshift-security-profiles</code> namespace and its status is <code>Succeeded</code>.</li>
      </ol>
      <p>If the Operator is not installed successfully:</p>
      <ol>
      <li>Navigate to the <strong>Ecosystem</strong> -&gt; <strong>Installed Operators</strong> page and inspect the <code>Status</code> column for any errors or failures.</li>
      <li>Navigate to the <strong>Workloads</strong> -&gt; <strong>Pods</strong> page and check the logs in any pods in the <code>openshift-security-profiles</code> project that are reporting issues.</li>
      </ol>
      <h2 id="spo-installing-cli_spo-enabling" class="group scroll-mt-20">Installing the Security Profiles Operator using the CLI<a href="#spo-installing-cli_spo-enabling" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can install the OpenShift Container Platform Security Profiles Operator by using the command line interface. </p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You must have <code>cluster-admin</code> privileges.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Define a <code>Namespace</code> object:
      <CodeBlock code={"apiVersion: v1\nkind: Namespace\nmetadata:\n    name: openshift-security-profiles\nlabels:\n  openshift.io/cluster-monitoring: \"true\""} lang="yaml" title="Example namespace-object.yaml" />
      </li>
      <li>
      Create the <code>Namespace</code> object:
      <CodeBlock code={"$ oc create -f namespace-object.yaml"} lang="terminal" />
      </li>
      <li>
      Define an <code>OperatorGroup</code> object:
      <CodeBlock code={"apiVersion: operators.coreos.com/v1\nkind: OperatorGroup\nmetadata:\n  name: security-profiles-operator\n  namespace: openshift-security-profiles"} lang="yaml" title="Example operator-group-object.yaml" />
      </li>
      <li>
      Create the <code>OperatorGroup</code> object:
      <CodeBlock code={"$ oc create -f operator-group-object.yaml"} lang="terminal" />
      </li>
      <li>
      Define a <code>Subscription</code> object:
      <CodeBlock code={"apiVersion: operators.coreos.com/v1alpha1\nkind: Subscription\nmetadata:\n  name: security-profiles-operator-sub\n  namespace: openshift-security-profiles\nspec:\n  channel: release-alpha-rhel-8\n  installPlanApproval: Automatic\n  name: security-profiles-operator\n  source: redhat-operators\n  sourceNamespace: openshift-marketplace"} lang="yaml" title="Example subscription-object.yaml" />
      </li>
      <li>
      Create the <code>Subscription</code> object:
      <CodeBlock code={"$ oc create -f subscription-object.yaml"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If you are setting the global scheduler feature and enable <code>defaultNodeSelector</code>, you must create the namespace manually and update the annotations of the <code>openshift-security-profiles</code> namespace, or the namespace where the Security Profiles Operator was installed, with <code>openshift.io/node-selector: “”</code>. This removes the default node selector and prevents deployment failures.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>
      Verify the installation succeeded by inspecting the following CSV file:
      <CodeBlock code={"$ oc get csv -n openshift-security-profiles"} lang="terminal" />
      </li>
      <li>
      Verify that the Security Profiles Operator is operational by running the following command:
      <CodeBlock code={"$ oc get deploy -n openshift-security-profiles"} lang="terminal" />
      </li>
      </ol>
      <h2 id="logging-verbosity_spo-enabling" class="group scroll-mt-20">Configuring logging verbosity<a href="#logging-verbosity_spo-enabling" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">The Security Profiles Operator supports the default logging verbosity of <code>0</code> and an enhanced verbosity of <code>1</code>. </p>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      To enable enhanced logging verbosity, patch the <code>spod</code> configuration and adjust the value by running the following command:
      <CodeBlock code={"$ oc -n openshift-security-profiles patch spod \\\n    spod --type=merge -p '{\"spec\":{\"verbosity\":1}}'"} lang="terminal" />
      <CodeBlock code={"securityprofilesoperatordaemon.security-profiles-operator.x-k8s.io/spod patched"} lang="terminal" title="Example output" />
      </li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
