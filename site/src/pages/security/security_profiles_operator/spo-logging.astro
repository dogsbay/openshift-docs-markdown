---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/security_profiles_operator/spo-logging.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"spo-audit-logging","text":"Advanced Audit Logging Framework"},{"depth":2,"slug":"benefits-of-adv-aud-log_spo-audit-logging","text":"Benefits of the Advanced Audit Logging Framework"},{"depth":2,"slug":"performance-considerations_spo-audit-logging","text":"Performance considerations"},{"depth":2,"slug":"prereq-adv-audit-logging_spo-audit-logging","text":"Prerequisites for the Advanced Audit Logging Framework"},{"depth":2,"slug":"additional-resources_spo-audit-logging","text":"Additional resources"}];
const nav = navData;
const currentPath = "/security/security_profiles_operator/spo-logging";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Advanced Audit Logging Framework";
const description = "With the Advanced Audit Logging Framework in the OpenShift Container Platform Security Profiles Operator (SPO), you can correlate cluster users with…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/security_profiles_operator"];
const pageTaxonomies = null;
const tagsIndexPath = "/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Advanced Audit Logging Framework {#spo-audit-logging}\n\nWith the Advanced Audit Logging Framework in the OpenShift Container Platform Security Profiles Operator (SPO), you can correlate cluster users with actions during `oc exec`, `oc rsh`, and `oc debug` sessions.\n\nThe Advanced Audit Logging Framework in SPO 0.10.0 logs activity from an {{ op_system_first }} container back to the hosting cluster and produces detailed logs in a JSON Lines format.\n\n## Benefits of the Advanced Audit Logging Framework {#benefits-of-adv-aud-log_spo-audit-logging}\n\nThe `kubectl exec`, `oc exec`, `oc rsh` and `oc debug` commands do not pass user authentication details into the exec session on the container, making it hard to correlate Kubernetes user actions caused by actions on the host. The audit logger in SPO addresses this with mutating webhooks that inject the request UID from the Kubernetes API server as an environment variable into the session. Every request to the API server including the request to start a new exec session has a request UID. This request UID is then logged by the Advanced Audit Logging Framework. The request ID is used to correlate the activity with the API server audit logs, providing an audit trail within the node.\n\nWith the addition of the Advanced Audit Logging Framework, SPO now has two use cases:\n\n```\n* Pod auditing\n* Node auditing\n```\n\nThe use of privileged `seccompProfile` configuration is required only for the case of node auditing.\n\n> [!NOTE]\n> The Security Profiles Operator supports only {{ op_system_first }} worker nodes appropriate to the version of OpenShift Container Platform in use.\n>\n> Red Hat Enterprise Linux (RHEL) nodes are not supported.\n\n## Performance considerations {#performance-considerations_spo-audit-logging}\n\nIt is important to consider the performance cost of using seccomp profiles for extensive logging. SPO is designed to minimize this impact by primarily logging only process creations and handling them asynchronously. This approach helps prevent logging from becoming a bottleneck on your nodes.\n\nThe Advanced Audit Logging feature uses eBPF as a supplemental data source. While it is possible for eBPF to be used as a primary data source for this type of logging, that functionality is not currently a configurable feature within the Operator. For most use cases, the default asynchronous, process-creation-focused logging approach provides an excellent balance between security visibility and cluster performance.\n\n## Prerequisites for the Advanced Audit Logging Framework {#prereq-adv-audit-logging_spo-audit-logging}\n\nBefore enabling the Advanced Audit Logging Framework, ensure the following requirements are met.\n\nSecurity Profiles Operator version 0.10.0 or later is installed. The Advanced Audit Logging Framework requires Security Profiles Operator version 0.10.0 or later.\n\nFor node debugging sessions:\n\n```\n* To audit `oc debug` node sessions, CRI-O version 1.33 or later is required. It is available in OpenShift Container Platform 4.20 or later.\n* The --privileged-seccomp-profile flag must be configured in CRI-O to apply `seccompProfiles` to privileged containers.\n* The supported Linux used with the Advanced Audit Logging Framework is {{ op_system_first }} running in a container in OpenShift Container Platform 4.20 or later.\n```\n\nIf you are using the CRI-O runtime, you must configure it to allow `seccompProfile` to be applied to privileged containers. Add the following flag to your CRI-O runtime configuration: `--privileged-seccomp-profile=/var/lib/kubelet/seccomp/operator/profile1.json`. This is explained in more detail in the Advanced Audit Logging installation and enablement steps. The `--privileged-seccomp-profile` flag is available starting with OpenShift Container Platform 4.20 and later.\n\nIf you are using any version of SPO before 0.9.0, you must perform a migration procedure to install versions 0.9.0 or 0.10.0. The migration procedure converts SPO to operate on cluster-scoped resources.\n\nFirst-time installation of SPO version 0.10.0 does not require migration. Also, if you are currently on SPO 0.9.0, you do not require migration and can directly upgrade to SPO 0.10.0.\n\n> [!IMPORTANT]\n> Do not attempt to upgrade directly from SPO versions before 0.9.0 to either 0.9.0 or 0.10.0 if you are currently running SPO. You must perform the migration procedure to convert SPO for operation at the cluster level.\n>\n> This change allows Advanced Audit Logging of events inside the worker node.\n>\n> This capability is not provided for control nodes since SPO does not operate on `etcd` nodes.\n\n## Additional resources {#additional-resources_spo-audit-logging}\n\n- [About security profiles](/security/security_profiles_operator/spo-understanding#spo-about_spo-understanding)\n- [Installing the Security Profiles Operator](/security/security_profiles_operator/spo-enabling#spo-installing_spo-enabling)\n- [Migration procedure](https://access.redhat.com/articles/7130594)\n- [Use the log enricher](/security/security_profiles_operator/spo-advanced#spo-log-enricher_spo-advanced)\n- [Troubleshooting the Security Profiles Operator](/security/security_profiles_operator/spo-troubleshooting#spo-inspecting-seccomp-profiles_spo-troubleshooting)\n- [Uninstalling SPO](/security/security_profiles_operator/spo-uninstalling#spo-uninstalling)\n","mdUrl":"/security/security_profiles_operator/spo-logging.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={""}
  navMode={"client"}
  pagefindUrl={"/pagefind/"}
  favicon={"/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="spo-audit-logging" class="group scroll-mt-20">Advanced Audit Logging Framework<a href="#spo-audit-logging" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="/_attributes/common-attributes.md"></p>
      <p>With the Advanced Audit Logging Framework in the OpenShift Container Platform Security Profiles Operator (SPO), you can correlate cluster users with actions during <code>oc exec</code>, <code>oc rsh</code>, and <code>oc debug</code> sessions.</p>
      <p>The Advanced Audit Logging Framework in SPO 0.10.0 logs activity from an {"{"}{"{"} op_system_first {"}"}{"}"} container back to the hosting cluster and produces detailed logs in a JSON Lines format.</p>
      <h2 id="benefits-of-adv-aud-log_spo-audit-logging" class="group scroll-mt-20">Benefits of the Advanced Audit Logging Framework<a href="#benefits-of-adv-aud-log_spo-audit-logging" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>The <code>kubectl exec</code>, <code>oc exec</code>, <code>oc rsh</code> and <code>oc debug</code> commands do not pass user authentication details into the exec session on the container, making it hard to correlate Kubernetes user actions caused by actions on the host. The audit logger in SPO addresses this with mutating webhooks that inject the request UID from the Kubernetes API server as an environment variable into the session. Every request to the API server including the request to start a new exec session has a request UID. This request UID is then logged by the Advanced Audit Logging Framework. The request ID is used to correlate the activity with the API server audit logs, providing an audit trail within the node.</p>
      <p>With the addition of the Advanced Audit Logging Framework, SPO now has two use cases:</p>
      <CodeBlock code={"* Pod auditing\n* Node auditing"} lang="plaintext" />
      <p>The use of privileged <code>seccompProfile</code> configuration is required only for the case of node auditing.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The Security Profiles Operator supports only {"{"}{"{"} op_system_first {"}"}{"}"} worker nodes appropriate to the version of OpenShift Container Platform in use.</p>
          <p>Red Hat Enterprise Linux (RHEL) nodes are not supported.</p>
        </AlertDescription>
      </Alert>
      <h2 id="performance-considerations_spo-audit-logging" class="group scroll-mt-20">Performance considerations<a href="#performance-considerations_spo-audit-logging" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>It is important to consider the performance cost of using seccomp profiles for extensive logging. SPO is designed to minimize this impact by primarily logging only process creations and handling them asynchronously. This approach helps prevent logging from becoming a bottleneck on your nodes.</p>
      <p>The Advanced Audit Logging feature uses eBPF as a supplemental data source. While it is possible for eBPF to be used as a primary data source for this type of logging, that functionality is not currently a configurable feature within the Operator. For most use cases, the default asynchronous, process-creation-focused logging approach provides an excellent balance between security visibility and cluster performance.</p>
      <h2 id="prereq-adv-audit-logging_spo-audit-logging" class="group scroll-mt-20">Prerequisites for the Advanced Audit Logging Framework<a href="#prereq-adv-audit-logging_spo-audit-logging" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Before enabling the Advanced Audit Logging Framework, ensure the following requirements are met.</p>
      <p>Security Profiles Operator version 0.10.0 or later is installed. The Advanced Audit Logging Framework requires Security Profiles Operator version 0.10.0 or later.</p>
      <p>For node debugging sessions:</p>
      <CodeBlock code={"* To audit `oc debug` node sessions, CRI-O version 1.33 or later is required. It is available in OpenShift Container Platform 4.20 or later.\n* The --privileged-seccomp-profile flag must be configured in CRI-O to apply `seccompProfiles` to privileged containers.\n* The supported Linux used with the Advanced Audit Logging Framework is {{ op_system_first }} running in a container in OpenShift Container Platform 4.20 or later."} lang="plaintext" />
      <p>If you are using the CRI-O runtime, you must configure it to allow <code>seccompProfile</code> to be applied to privileged containers. Add the following flag to your CRI-O runtime configuration: <code>--privileged-seccomp-profile=/var/lib/kubelet/seccomp/operator/profile1.json</code>. This is explained in more detail in the Advanced Audit Logging installation and enablement steps. The <code>--privileged-seccomp-profile</code> flag is available starting with OpenShift Container Platform 4.20 and later.</p>
      <p>If you are using any version of SPO before 0.9.0, you must perform a migration procedure to install versions 0.9.0 or 0.10.0. The migration procedure converts SPO to operate on cluster-scoped resources.</p>
      <p>First-time installation of SPO version 0.10.0 does not require migration. Also, if you are currently on SPO 0.9.0, you do not require migration and can directly upgrade to SPO 0.10.0.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Do not attempt to upgrade directly from SPO versions before 0.9.0 to either 0.9.0 or 0.10.0 if you are currently running SPO. You must perform the migration procedure to convert SPO for operation at the cluster level.</p>
          <p>This change allows Advanced Audit Logging of events inside the worker node.</p>
          <p>This capability is not provided for control nodes since SPO does not operate on <code>etcd</code> nodes.</p>
        </AlertDescription>
      </Alert>
      <p class="/modules/spo-log-enrich.md"></p>
      <p class="/modules/spo-log-kube-api-compare.md"></p>
      <p class="/modules/spo-log-enabling.md"></p>
      <p class="/modules/spo-log-json-enrich.md"></p>
      <p class="/modules/spo-log-tune-rot.md"></p>
      <p class="/modules/spo-log-adv-pod.md"></p>
      <p class="/modules/spo-log-mon-audit.md"></p>
      <p class="/modules/spo-log-debug.md"></p>
      <p class="/modules/spo-log-correlate.md"></p>
      <p class="/modules/spo-log-enrich-out.md"></p>
      <p class="/modules/spo-log-kube-api-out.md"></p>
      <p class="/modules/spo-log-corr-key.md"></p>
      <p class="/modules/spo-log-corr-log.md"></p>
      <p class="/modules/spo-log-webhook.md"></p>
      <p class="/modules/spo-log-disable.md"></p>
      <h2 id="additional-resources_spo-audit-logging" class="group scroll-mt-20">Additional resources<a href="#additional-resources_spo-audit-logging" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/security/security_profiles_operator/spo-understanding#spo-about_spo-understanding">About security profiles</a></li>
      <li><a href="/security/security_profiles_operator/spo-enabling#spo-installing_spo-enabling">Installing the Security Profiles Operator</a></li>
      <li><a href="https://access.redhat.com/articles/7130594">Migration procedure</a></li>
      <li><a href="/security/security_profiles_operator/spo-advanced#spo-log-enricher_spo-advanced">Use the log enricher</a></li>
      <li><a href="/security/security_profiles_operator/spo-troubleshooting#spo-inspecting-seccomp-profiles_spo-troubleshooting">Troubleshooting the Security Profiles Operator</a></li>
      <li><a href="/security/security_profiles_operator/spo-uninstalling#spo-uninstalling">Uninstalling SPO</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
