---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/file_integrity_operator/file-integrity-operator-installation.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"installing-file-integrity-operator","text":"Installing the File Integrity Operator"},{"depth":2,"slug":"installing-file-integrity-operator-using-web-console_file-integrity-operator-installation","text":"Installing the File Integrity Operator using the web console"},{"depth":2,"slug":"installing-file-integrity-operator-using-cli_file-integrity-operator-installation","text":"Installing the File Integrity Operator using the CLI"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/file_integrity_operator/file-integrity-operator-installation";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Installing the File Integrity Operator";
const description = "Install the File Integrity Operator on your cluster by using the OpenShift Container Platform web console or the OpenShift CLI (oc)." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/file_integrity_operator"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Installing the File Integrity Operator {#installing-file-integrity-operator}\n\nInstall the File Integrity Operator on your cluster by using the OpenShift Container Platform web console or the OpenShift CLI (`oc`).\n\n> [!IMPORTANT]\n> All cluster nodes must have the same release version in order for this Operator to function properly. As an example, for nodes running RHCOS, all nodes must have the same RHCOS version.\n\n## Installing the File Integrity Operator using the web console {#installing-file-integrity-operator-using-web-console_file-integrity-operator-installation}\n\nInstall the File Integrity Operator from the OpenShift Container Platform web console by using the Software Catalog.\n\n**Prerequisites**\n\n- You must have `admin` privileges.\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, navigate to **Ecosystem** → **Software Catalog**.\n2. Search for the File Integrity Operator, then click **Install**.\n3. Keep the default selection of **Installation mode** and **namespace** to ensure that the Operator will be installed to the `openshift-file-integrity` namespace.\n4. Click **Install**.\n\n**Verification**\n\nTo confirm that the installation is successful:\n\n1. Navigate to the **Ecosystem** → **Installed Operators** page.\n2. Check that the Operator is installed in the `openshift-file-integrity` namespace and its status is `Succeeded`.\n\nIf the Operator is not installed successfully:\n\n1. Navigate to the **Ecosystem** → **Installed Operators** page and inspect the `Status` column for any errors or failures.\n2. Navigate to the **Workloads** → **Pods** page and check the logs in any pods in the `openshift-file-integrity` project that are reporting issues.\n\n## Installing the File Integrity Operator using the CLI {#installing-file-integrity-operator-using-cli_file-integrity-operator-installation}\n\nInstall the File Integrity Operator from the OpenShift CLI (`oc`) by creating `Namespace`, `OperatorGroup`, and `Subscription` objects.\n\n**Prerequisites**\n\n- You must have `admin` privileges.\n\n**Procedure**\n\n1. Create a `Namespace` object YAML file by running:\n\n   ```terminal\n   $ oc create -f <file_name>.yaml\n   ```\n\n   ```yaml {title=\"Example output\"}\n   apiVersion: v1\n   kind: Namespace\n   metadata:\n     labels:\n       openshift.io/cluster-monitoring: \"true\"\n       pod-security.kubernetes.io/enforce: privileged\n     name: openshift-file-integrity\n   ```\n\n   > [!NOTE]\n   > In OpenShift Container Platform 4.22, the pod security label must be set to `privileged` at the namespace level.\n2. Create the `OperatorGroup` object YAML file:\n\n   ```terminal\n   $ oc create -f <file-name>.yaml\n   ```\n\n   ```yaml {title=\"Example output\"}\n   apiVersion: operators.coreos.com/v1\n   kind: OperatorGroup\n   metadata:\n     name: file-integrity-operator\n     namespace: openshift-file-integrity\n   spec:\n     targetNamespaces:\n     - openshift-file-integrity\n   ```\n3. Create the `Subscription` object YAML file:\n\n   ```terminal\n   $ oc create -f <file-name>.yaml\n   ```\n\n   ```yaml {title=\"Example output\"}\n   apiVersion: operators.coreos.com/v1alpha1\n   kind: Subscription\n   metadata:\n     name: file-integrity-operator\n     namespace: openshift-file-integrity\n   spec:\n     channel: \"stable\"\n     installPlanApproval: Automatic\n     name: file-integrity-operator\n     source: redhat-operators\n     sourceNamespace: openshift-marketplace\n   ```\n\n**Verification**\n\n1. Verify the installation succeeded by inspecting the CSV file:\n\n   ```terminal\n   $ oc get csv -n openshift-file-integrity\n   ```\n2. Verify that the File Integrity Operator is up and running:\n\n   ```terminal\n   $ oc get deploy -n openshift-file-integrity\n   ```\n\n**Additional resources**\n{._additional-resources}\n\n- [Using Operator Lifecycle Manager in disconnected environments](/openshift-docs-markdown/disconnected/using-olm#olm-restricted-networks)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/file_integrity_operator/file-integrity-operator-installation.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="installing-file-integrity-operator" class="group scroll-mt-20">Installing the File Integrity Operator<a href="#installing-file-integrity-operator" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Install the File Integrity Operator on your cluster by using the OpenShift Container Platform web console or the OpenShift CLI (<code>oc</code>).</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>All cluster nodes must have the same release version in order for this Operator to function properly. As an example, for nodes running RHCOS, all nodes must have the same RHCOS version.</p>
        </AlertDescription>
      </Alert>
      <h2 id="installing-file-integrity-operator-using-web-console_file-integrity-operator-installation" class="group scroll-mt-20">Installing the File Integrity Operator using the web console<a href="#installing-file-integrity-operator-using-web-console_file-integrity-operator-installation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Install the File Integrity Operator from the OpenShift Container Platform web console by using the Software Catalog.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You must have <code>admin</code> privileges.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, navigate to <strong>Ecosystem</strong> → <strong>Software Catalog</strong>.</li>
      <li>Search for the File Integrity Operator, then click <strong>Install</strong>.</li>
      <li>Keep the default selection of <strong>Installation mode</strong> and <strong>namespace</strong> to ensure that the Operator will be installed to the <code>openshift-file-integrity</code> namespace.</li>
      <li>Click <strong>Install</strong>.</li>
      </ol>
      <p><strong>Verification</strong></p>
      <p>To confirm that the installation is successful:</p>
      <ol>
      <li>Navigate to the <strong>Ecosystem</strong> → <strong>Installed Operators</strong> page.</li>
      <li>Check that the Operator is installed in the <code>openshift-file-integrity</code> namespace and its status is <code>Succeeded</code>.</li>
      </ol>
      <p>If the Operator is not installed successfully:</p>
      <ol>
      <li>Navigate to the <strong>Ecosystem</strong> → <strong>Installed Operators</strong> page and inspect the <code>Status</code> column for any errors or failures.</li>
      <li>Navigate to the <strong>Workloads</strong> → <strong>Pods</strong> page and check the logs in any pods in the <code>openshift-file-integrity</code> project that are reporting issues.</li>
      </ol>
      <h2 id="installing-file-integrity-operator-using-cli_file-integrity-operator-installation" class="group scroll-mt-20">Installing the File Integrity Operator using the CLI<a href="#installing-file-integrity-operator-using-cli_file-integrity-operator-installation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Install the File Integrity Operator from the OpenShift CLI (<code>oc</code>) by creating <code>Namespace</code>, <code>OperatorGroup</code>, and <code>Subscription</code> objects.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>You must have <code>admin</code> privileges.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Create a <code>Namespace</code> object YAML file by running:
      <CodeBlock code={"$ oc create -f <file_name>.yaml"} lang="terminal" />
      <CodeBlock code={"apiVersion: v1\nkind: Namespace\nmetadata:\n  labels:\n    openshift.io/cluster-monitoring: \"true\"\n    pod-security.kubernetes.io/enforce: privileged\n  name: openshift-file-integrity"} lang="yaml" title="Example output" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>In OpenShift Container Platform 4.22, the pod security label must be set to <code>privileged</code> at the namespace level.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      Create the <code>OperatorGroup</code> object YAML file:
      <CodeBlock code={"$ oc create -f <file-name>.yaml"} lang="terminal" />
      <CodeBlock code={"apiVersion: operators.coreos.com/v1\nkind: OperatorGroup\nmetadata:\n  name: file-integrity-operator\n  namespace: openshift-file-integrity\nspec:\n  targetNamespaces:\n  - openshift-file-integrity"} lang="yaml" title="Example output" />
      </li>
      <li>
      Create the <code>Subscription</code> object YAML file:
      <CodeBlock code={"$ oc create -f <file-name>.yaml"} lang="terminal" />
      <CodeBlock code={"apiVersion: operators.coreos.com/v1alpha1\nkind: Subscription\nmetadata:\n  name: file-integrity-operator\n  namespace: openshift-file-integrity\nspec:\n  channel: \"stable\"\n  installPlanApproval: Automatic\n  name: file-integrity-operator\n  source: redhat-operators\n  sourceNamespace: openshift-marketplace"} lang="yaml" title="Example output" />
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>
      Verify the installation succeeded by inspecting the CSV file:
      <CodeBlock code={"$ oc get csv -n openshift-file-integrity"} lang="terminal" />
      </li>
      <li>
      Verify that the File Integrity Operator is up and running:
      <CodeBlock code={"$ oc get deploy -n openshift-file-integrity"} lang="terminal" />
      </li>
      </ol>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="/openshift-docs-markdown/disconnected/using-olm#olm-restricted-networks">Using Operator Lifecycle Manager in disconnected environments</a></li></ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
