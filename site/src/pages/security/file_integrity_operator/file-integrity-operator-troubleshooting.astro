---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: security/file_integrity_operator/file-integrity-operator-troubleshooting.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"troubleshooting-file-integrity-operator","text":"Troubleshooting the File Integrity Operator"},{"depth":2,"slug":"_general_troubleshooting","text":"General troubleshooting"},{"depth":2,"slug":"_checking_the_aide_configuration","text":"Checking the AIDE configuration"},{"depth":2,"slug":"_determining_the_fileintegrity_object_phase","text":"Determining the FileIntegrity object phase"},{"depth":2,"slug":"_determining_that_the_daemon_set_pods_are_running_on_the_expected_nodes","text":"Determining that the daemon set pods are running on the expected nodes"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/security/file_integrity_operator/file-integrity-operator-troubleshooting";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Troubleshooting the File Integrity Operator";
const description = "Use the following information to troubleshoot common issues with the File Integrity Operator." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["security","security/file_integrity_operator"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Troubleshooting the File Integrity Operator {#troubleshooting-file-integrity-operator}\n\nUse the following information to troubleshoot common issues with the File Integrity Operator.\n\n## General troubleshooting {#_general_troubleshooting}\n\nIssue\n:   You want to generally troubleshoot issues with the File Integrity Operator.\n\nResolution\n:   Enable the debug flag in the `FileIntegrity` object. The `debug` flag increases the verbosity of the daemons that run in the `DaemonSet` pods and run the AIDE checks.\n\n## Checking the AIDE configuration {#_checking_the_aide_configuration}\n\nIssue\n:   You want to check the AIDE configuration.\n\nResolution\n:   The AIDE configuration is stored in a config map with the same name as the `FileIntegrity` object. All AIDE configuration config maps are labeled with `file-integrity.openshift.io/aide-conf`.\n\n## Determining the FileIntegrity object phase {#_determining_the_fileintegrity_object_phase}\n\nIssue\n:   You want to determine if the `FileIntegrity` object exists and see its current status.\n\nResolution\n:   To see the current status of the `FileIntegrity` object, run:\n\n    ```terminal\n    $ oc get fileintegrities/worker-fileintegrity  -o jsonpath=\"{ .status }\"\n    ```\n\n    After the `FileIntegrity` object and the backing daemon set are created, the status should switch to `Active`. If it does not, check the Operator pod logs.\n\n## Determining that the daemon set pods are running on the expected nodes {#_determining_that_the_daemon_set_pods_are_running_on_the_expected_nodes}\n\nIssue\n:   You want to confirm that the daemon set exists and that its pods are running on the nodes you expect them to run on.\n\nResolution\n:   Run:\n\n    ```terminal\n    $ oc -n openshift-file-integrity get pods -lapp=aide-worker-fileintegrity\n    ```\n\n    > [!NOTE]\n    > Adding `-owide` includes the IP address of the node that the pod is running on.\n\n    To check the logs of the daemon pods, run `oc logs`.\n\n    Check the return value of the AIDE command to see if the check passed or failed.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/security/file_integrity_operator/file-integrity-operator-troubleshooting.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="troubleshooting-file-integrity-operator" class="group scroll-mt-20">Troubleshooting the File Integrity Operator<a href="#troubleshooting-file-integrity-operator" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">Use the following information to troubleshoot common issues with the File Integrity Operator.</p>
      <h2 id="_general_troubleshooting" class="group scroll-mt-20">General troubleshooting<a href="#_general_troubleshooting" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Issue</dt>
      <dd class="ml-6 text-muted-foreground"><p>You want to generally troubleshoot issues with the File Integrity Operator.</p></dd>
      <dt class="font-semibold">Resolution</dt>
      <dd class="ml-6 text-muted-foreground"><p>Enable the debug flag in the <code>FileIntegrity</code> object. The <code>debug</code> flag increases the verbosity of the daemons that run in the <code>DaemonSet</code> pods and run the AIDE checks.</p></dd>
      </dl>
      <h2 id="_checking_the_aide_configuration" class="group scroll-mt-20">Checking the AIDE configuration<a href="#_checking_the_aide_configuration" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Issue</dt>
      <dd class="ml-6 text-muted-foreground"><p>You want to check the AIDE configuration.</p></dd>
      <dt class="font-semibold">Resolution</dt>
      <dd class="ml-6 text-muted-foreground"><p>The AIDE configuration is stored in a config map with the same name as the <code>FileIntegrity</code> object. All AIDE configuration config maps are labeled with <code>file-integrity.openshift.io/aide-conf</code>.</p></dd>
      </dl>
      <h2 id="_determining_the_fileintegrity_object_phase" class="group scroll-mt-20">Determining the FileIntegrity object phase<a href="#_determining_the_fileintegrity_object_phase" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Issue</dt>
      <dd class="ml-6 text-muted-foreground"><p>You want to determine if the <code>FileIntegrity</code> object exists and see its current status.</p></dd>
      <dt class="font-semibold">Resolution</dt>
      <dd class="ml-6 text-muted-foreground">
      <p>To see the current status of the <code>FileIntegrity</code> object, run:</p>
      <CodeBlock code={"$ oc get fileintegrities/worker-fileintegrity  -o jsonpath=\"{ .status }\""} lang="terminal" />
      <p>After the <code>FileIntegrity</code> object and the backing daemon set are created, the status should switch to <code>Active</code>. If it does not, check the Operator pod logs.</p>
      </dd>
      </dl>
      <h2 id="_determining_that_the_daemon_set_pods_are_running_on_the_expected_nodes" class="group scroll-mt-20">Determining that the daemon set pods are running on the expected nodes<a href="#_determining_that_the_daemon_set_pods_are_running_on_the_expected_nodes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Issue</dt>
      <dd class="ml-6 text-muted-foreground"><p>You want to confirm that the daemon set exists and that its pods are running on the nodes you expect them to run on.</p></dd>
      <dt class="font-semibold">Resolution</dt>
      <dd class="ml-6 text-muted-foreground">
      <p>Run:</p>
      <CodeBlock code={"$ oc -n openshift-file-integrity get pods -lapp=aide-worker-fileintegrity"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Adding <code>-owide</code> includes the IP address of the node that the pod is running on.</p>
        </AlertDescription>
      </Alert>
      <p>To check the logs of the daemon pods, run <code>oc logs</code>.</p>
      <p>Check the return value of the AIDE command to see if the check passed or failed.</p>
      </dd>
      </dl>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
