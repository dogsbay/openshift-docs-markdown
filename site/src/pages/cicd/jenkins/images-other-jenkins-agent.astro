---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: cicd/jenkins/images-other-jenkins-agent.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"images-other-jenkins-agent","text":"Jenkins agent"},{"depth":2,"slug":"images-other-jenkins-agent-images_images-other-jenkins-agent","text":"Jenkins agent images"},{"depth":2,"slug":"images-other-jenkins-agent-env-var_images-other-jenkins-agent","text":"Jenkins agent environment variables"},{"depth":2,"slug":"images-other-jenkins-agent-memory_images-other-jenkins-agent","text":"Jenkins agent memory requirements"},{"depth":2,"slug":"images-other-jenkins-agent-gradle_images-other-jenkins-agent","text":"Jenkins agent Gradle builds"},{"depth":2,"slug":"images-other-jenkins-agent-pod-retention_images-other-jenkins-agent","text":"Jenkins agent pod retention"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/cicd/jenkins/images-other-jenkins-agent";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Jenkins agent";
const description = "OpenShift Container Platform provides a base image for use as a Jenkins agent." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["cicd","cicd/jenkins"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Jenkins agent {#images-other-jenkins-agent}\n\nOpenShift Container Platform provides a base image for use as a Jenkins agent.\n\nThe Base image for Jenkins agents does the following:\n\n- Pulls in both the required tools, headless Java, the Jenkins JNLP client, and the useful ones, including `git`, `tar`, `zip`, and `nss`, among others.\n- Establishes the JNLP agent as the entry point.\n- Includes the `oc` client tool for invoking command-line operations from within Jenkins jobs.\n- Provides Dockerfiles for both Red Hat Enterprise Linux (RHEL) and `localdev` images.\n\n> [!IMPORTANT]\n> Use a version of the agent image that is appropriate for your OpenShift Container Platform release version. Embedding an `oc` client version that is not compatible with the OpenShift Container Platform version can cause unexpected behavior.\n\nThe OpenShift Container Platform Jenkins image also defines the following sample `java-builder` pod template to illustrate how you can use the agent image with the Jenkins Kubernetes plugin.\n\nThe `java-builder` pod template employs two containers:\n\n- A `jnlp` container that uses the OpenShift Container Platform Base agent image and handles the JNLP contract for starting and stopping Jenkins agents.\n- A `java` container that uses the `java` OpenShift Container Platform Sample ImageStream, which contains the various Java binaries, including the Maven binary `mvn`, for building code.\n\n## Jenkins agent images {#images-other-jenkins-agent-images_images-other-jenkins-agent}\n\nThe OpenShift Container Platform Jenkins agent images are available on [Quay.io](https://quay.io) or [registry.redhat.io](https://registry.redhat.io).\n\nJenkins images are available through the Red Hat Registry:\n\n```terminal\n$ docker pull registry.redhat.io/ocp-tools-4/jenkins-rhel8:<image_tag>\n```\n\n```terminal\n$ docker pull registry.redhat.io/ocp-tools-4/jenkins-agent-base-rhel8:<image_tag>\n```\n\nTo use these images, you can either access them directly from [Quay.io](https://quay.io) or [registry.redhat.io](https://registry.redhat.io) or push them into your OpenShift Container Platform container image registry.\n\n## Jenkins agent environment variables {#images-other-jenkins-agent-env-var_images-other-jenkins-agent}\n\nEach Jenkins agent container can be configured with the following environment variables.\n\n| Variable | Definition | Example values and settings |\n| --- | --- | --- |\n| `JAVA_MAX_HEAP_PARAM`, `CONTAINER_HEAP_PERCENT`, `JENKINS_MAX_HEAP_UPPER_BOUND_MB` | These values control the maximum heap size of the Jenkins JVM. If `JAVA_MAX_HEAP_PARAM` is set, its value takes precedence. Otherwise, the maximum heap size is dynamically calculated as `CONTAINER_HEAP_PERCENT` of the container memory limit, optionally capped at `JENKINS_MAX_HEAP_UPPER_BOUND_MB` MiB.<br>By default, the maximum heap size of the Jenkins JVM is set to 50% of the container memory limit with no cap. | `JAVA_MAX_HEAP_PARAM` example setting: `-Xmx512m`<br>`CONTAINER_HEAP_PERCENT` default: `0.5`, or 50%<br>`JENKINS_MAX_HEAP_UPPER_BOUND_MB` example setting: `512 MiB` |\n| `JAVA_INITIAL_HEAP_PARAM`, `CONTAINER_INITIAL_PERCENT` | These values control the initial heap size of the Jenkins JVM. If `JAVA_INITIAL_HEAP_PARAM` is set, its value takes precedence. Otherwise, the initial heap size is dynamically calculated as `CONTAINER_INITIAL_PERCENT` of the dynamically calculated maximum heap size.<br>By default, the JVM sets the initial heap size. | `JAVA_INITIAL_HEAP_PARAM` example setting: `-Xms32m`<br>`CONTAINER_INITIAL_PERCENT` example setting: `0.1`, or 10% |\n| `CONTAINER_CORE_LIMIT` | If set, specifies an integer number of cores used for sizing numbers of internal JVM threads. | Example setting: `2` |\n| `JAVA_TOOL_OPTIONS` | Specifies options to apply to all JVMs running in this container. It is not recommended to override this value. | Default: `-XX:+UnlockExperimentalVMOptions -XX:+UseCGroupMemoryLimitForHeap -Dsun.zip.disableMemoryMapping=true` |\n| `JAVA_GC_OPTS` | Specifies Jenkins JVM garbage collection parameters. It is not recommended to override this value. | Default: `-XX:+UseParallelGC -XX:MinHeapFreeRatio=5 -XX:MaxHeapFreeRatio=10 -XX:GCTimeRatio=4 -XX:AdaptiveSizePolicyWeight=90` |\n| `JENKINS_JAVA_OVERRIDES` | Specifies additional options for the Jenkins JVM. These options are appended to all other options, including the Java options above, and can be used to override any of them, if necessary. Separate each additional option with a space and if any option contains space characters, escape them with a backslash. | Example settings: `-Dfoo -Dbar`; `-Dfoo=first\\ value -Dbar=second\\ value` |\n| `USE_JAVA_VERSION` | Specifies the version of Java version to use to run the agent in its container. The container base image has two versions of java installed: `java-11` and `java-1.8.0`. If you extend the container base image, you can specify any alternative version of java using its associated suffix. | The default value is `java-11`.<br>Example setting: `java-1.8.0` |\n\n## Jenkins agent memory requirements {#images-other-jenkins-agent-memory_images-other-jenkins-agent}\n\nA JVM is used in all Jenkins agents to host the Jenkins JNLP agent as well as to run any Java applications such as `javac`, Maven, or Gradle.\n\nBy default, the Jenkins JNLP agent JVM uses 50% of the container memory limit for its heap. This value can be modified by the `CONTAINER_HEAP_PERCENT` environment variable. It can also be capped at an upper limit or overridden entirely.\n\nBy default, any other processes run in the Jenkins agent container, such as shell scripts or `oc` commands run from pipelines, cannot use more than the remaining 50% memory limit without provoking an OOM kill.\n\nBy default, each further JVM process that runs in a Jenkins agent container uses up to 25% of the container memory limit for its heap. It might be necessary to tune this limit for many build workloads.\n\n## Jenkins agent Gradle builds {#images-other-jenkins-agent-gradle_images-other-jenkins-agent}\n\nHosting Gradle builds in the Jenkins agent on OpenShift Container Platform presents additional complications because in addition to the Jenkins JNLP agent and Gradle JVMs, Gradle spawns a third JVM to run tests if they are specified.\n\nThe following settings are suggested as a starting point for running Gradle builds in a memory constrained Jenkins agent on OpenShift Container Platform. You can modify these settings as required.\n\n- Ensure the long-lived Gradle daemon is disabled by adding `org.gradle.daemon=false` to the `gradle.properties` file.\n- Disable parallel build execution by ensuring `org.gradle.parallel=true` is not set in the `gradle.properties` file and that `--parallel` is not set as a command-line argument.\n- To prevent Java compilations running out-of-process, set `java { options.fork = false }` in the `build.gradle` file.\n- Disable multiple additional test processes by ensuring `test { maxParallelForks = 1 }` is set in the `build.gradle` file.\n- Override the Gradle JVM memory parameters by the `GRADLE_OPTS`, `JAVA_OPTS` or `JAVA_TOOL_OPTIONS` environment variables.\n- Set the maximum heap size and JVM arguments for any Gradle test JVM by defining the `maxHeapSize` and `jvmArgs` settings in `build.gradle`, or through the `-Dorg.gradle.jvmargs` command-line argument.\n\n## Jenkins agent pod retention {#images-other-jenkins-agent-pod-retention_images-other-jenkins-agent}\n\nJenkins agent pods, are deleted by default after the build completes or is stopped. This behavior can be changed by the Kubernetes plugin pod retention setting. Pod retention can be set for all Jenkins builds, with overrides for each pod template. The following behaviors are supported:\n\n- `Always` keeps the build pod regardless of build result.\n- `Default` uses the plugin value, which is the pod template only.\n- `Never` always deletes the pod.\n- `On Failure` keeps the pod if it fails during the build.\n\nYou can override pod retention in the pipeline Jenkinsfile:\n\n```groovy\npodTemplate(label: \"mypod\",\n  cloud: \"openshift\",\n  inheritFrom: \"maven\",\n  podRetention: onFailure(), (1)\n  containers: [\n    ...\n  ]) {\n  node(\"mypod\") {\n    ...\n  }\n}\n```\n\n1. Allowed values for `podRetention` are `never()`, `onFailure()`, `always()`, and `default()`.\n\n> [!WARNING]\n> Pods that are kept might continue to run and count against resource quotas.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/cicd/jenkins/images-other-jenkins-agent.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="images-other-jenkins-agent" class="group scroll-mt-20">Jenkins agent<a href="#images-other-jenkins-agent" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>OpenShift Container Platform provides a base image for use as a Jenkins agent.</p>
      <p>The Base image for Jenkins agents does the following:</p>
      <ul>
      <li>Pulls in both the required tools, headless Java, the Jenkins JNLP client, and the useful ones, including <code>git</code>, <code>tar</code>, <code>zip</code>, and <code>nss</code>, among others.</li>
      <li>Establishes the JNLP agent as the entry point.</li>
      <li>Includes the <code>oc</code> client tool for invoking command-line operations from within Jenkins jobs.</li>
      <li>Provides Dockerfiles for both Red Hat Enterprise Linux (RHEL) and <code>localdev</code> images.</li>
      </ul>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Use a version of the agent image that is appropriate for your OpenShift Container Platform release version. Embedding an <code>oc</code> client version that is not compatible with the OpenShift Container Platform version can cause unexpected behavior.</p>
        </AlertDescription>
      </Alert>
      <p>The OpenShift Container Platform Jenkins image also defines the following sample <code>java-builder</code> pod template to illustrate how you can use the agent image with the Jenkins Kubernetes plugin.</p>
      <p>The <code>java-builder</code> pod template employs two containers:</p>
      <ul>
      <li>A <code>jnlp</code> container that uses the OpenShift Container Platform Base agent image and handles the JNLP contract for starting and stopping Jenkins agents.</li>
      <li>A <code>java</code> container that uses the <code>java</code> OpenShift Container Platform Sample ImageStream, which contains the various Java binaries, including the Maven binary <code>mvn</code>, for building code.</li>
      </ul>
      <h2 id="images-other-jenkins-agent-images_images-other-jenkins-agent" class="group scroll-mt-20">Jenkins agent images<a href="#images-other-jenkins-agent-images_images-other-jenkins-agent" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>The OpenShift Container Platform Jenkins agent images are available on <a href="https://quay.io">Quay.io</a> or <a href="https://registry.redhat.io">registry.redhat.io</a>.</p>
      <p>Jenkins images are available through the Red Hat Registry:</p>
      <CodeBlock code={"$ docker pull registry.redhat.io/ocp-tools-4/jenkins-rhel8:<image_tag>"} lang="terminal" />
      <CodeBlock code={"$ docker pull registry.redhat.io/ocp-tools-4/jenkins-agent-base-rhel8:<image_tag>"} lang="terminal" />
      <p>To use these images, you can either access them directly from <a href="https://quay.io">Quay.io</a> or <a href="https://registry.redhat.io">registry.redhat.io</a> or push them into your OpenShift Container Platform container image registry.</p>
      <h2 id="images-other-jenkins-agent-env-var_images-other-jenkins-agent" class="group scroll-mt-20">Jenkins agent environment variables<a href="#images-other-jenkins-agent-env-var_images-other-jenkins-agent" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Each Jenkins agent container can be configured with the following environment variables.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Variable</TableHead>
      <TableHead>Definition</TableHead>
      <TableHead>Example values and settings</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>JAVA_MAX_HEAP_PARAM</code>, <code>CONTAINER_HEAP_PERCENT</code>, <code>JENKINS_MAX_HEAP_UPPER_BOUND_MB</code></TableCell>
      <TableCell>These values control the maximum heap size of the Jenkins JVM. If <code>JAVA_MAX_HEAP_PARAM</code> is set, its value takes precedence. Otherwise, the maximum heap size is dynamically calculated as <code>CONTAINER_HEAP_PERCENT</code> of the container memory limit, optionally capped at <code>JENKINS_MAX_HEAP_UPPER_BOUND_MB</code> MiB.<Fragment set:html={"<br>"} />By default, the maximum heap size of the Jenkins JVM is set to 50% of the container memory limit with no cap.</TableCell>
      <TableCell><code>JAVA_MAX_HEAP_PARAM</code> example setting: <code>-Xmx512m</code><Fragment set:html={"<br>"} /><code>CONTAINER_HEAP_PERCENT</code> default: <code>0.5</code>, or 50%<Fragment set:html={"<br>"} /><code>JENKINS_MAX_HEAP_UPPER_BOUND_MB</code> example setting: <code>512 MiB</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>JAVA_INITIAL_HEAP_PARAM</code>, <code>CONTAINER_INITIAL_PERCENT</code></TableCell>
      <TableCell>These values control the initial heap size of the Jenkins JVM. If <code>JAVA_INITIAL_HEAP_PARAM</code> is set, its value takes precedence. Otherwise, the initial heap size is dynamically calculated as <code>CONTAINER_INITIAL_PERCENT</code> of the dynamically calculated maximum heap size.<Fragment set:html={"<br>"} />By default, the JVM sets the initial heap size.</TableCell>
      <TableCell><code>JAVA_INITIAL_HEAP_PARAM</code> example setting: <code>-Xms32m</code><Fragment set:html={"<br>"} /><code>CONTAINER_INITIAL_PERCENT</code> example setting: <code>0.1</code>, or 10%</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>CONTAINER_CORE_LIMIT</code></TableCell>
      <TableCell>If set, specifies an integer number of cores used for sizing numbers of internal JVM threads.</TableCell>
      <TableCell>Example setting: <code>2</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>JAVA_TOOL_OPTIONS</code></TableCell>
      <TableCell>Specifies options to apply to all JVMs running in this container. It is not recommended to override this value.</TableCell>
      <TableCell>Default: <code>-XX:+UnlockExperimentalVMOptions -XX:+UseCGroupMemoryLimitForHeap -Dsun.zip.disableMemoryMapping=true</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>JAVA_GC_OPTS</code></TableCell>
      <TableCell>Specifies Jenkins JVM garbage collection parameters. It is not recommended to override this value.</TableCell>
      <TableCell>Default: <code>-XX:+UseParallelGC -XX:MinHeapFreeRatio=5 -XX:MaxHeapFreeRatio=10 -XX:GCTimeRatio=4 -XX:AdaptiveSizePolicyWeight=90</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>JENKINS_JAVA_OVERRIDES</code></TableCell>
      <TableCell>Specifies additional options for the Jenkins JVM. These options are appended to all other options, including the Java options above, and can be used to override any of them, if necessary. Separate each additional option with a space and if any option contains space characters, escape them with a backslash.</TableCell>
      <TableCell>Example settings: <code>-Dfoo -Dbar</code>; <code>-Dfoo=first\ value -Dbar=second\ value</code></TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>USE_JAVA_VERSION</code></TableCell>
      <TableCell>Specifies the version of Java version to use to run the agent in its container. The container base image has two versions of java installed: <code>java-11</code> and <code>java-1.8.0</code>. If you extend the container base image, you can specify any alternative version of java using its associated suffix.</TableCell>
      <TableCell>The default value is <code>java-11</code>.<Fragment set:html={"<br>"} />Example setting: <code>java-1.8.0</code></TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="images-other-jenkins-agent-memory_images-other-jenkins-agent" class="group scroll-mt-20">Jenkins agent memory requirements<a href="#images-other-jenkins-agent-memory_images-other-jenkins-agent" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>A JVM is used in all Jenkins agents to host the Jenkins JNLP agent as well as to run any Java applications such as <code>javac</code>, Maven, or Gradle.</p>
      <p>By default, the Jenkins JNLP agent JVM uses 50% of the container memory limit for its heap. This value can be modified by the <code>CONTAINER_HEAP_PERCENT</code> environment variable. It can also be capped at an upper limit or overridden entirely.</p>
      <p>By default, any other processes run in the Jenkins agent container, such as shell scripts or <code>oc</code> commands run from pipelines, cannot use more than the remaining 50% memory limit without provoking an OOM kill.</p>
      <p>By default, each further JVM process that runs in a Jenkins agent container uses up to 25% of the container memory limit for its heap. It might be necessary to tune this limit for many build workloads.</p>
      <h2 id="images-other-jenkins-agent-gradle_images-other-jenkins-agent" class="group scroll-mt-20">Jenkins agent Gradle builds<a href="#images-other-jenkins-agent-gradle_images-other-jenkins-agent" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Hosting Gradle builds in the Jenkins agent on OpenShift Container Platform presents additional complications because in addition to the Jenkins JNLP agent and Gradle JVMs, Gradle spawns a third JVM to run tests if they are specified.</p>
      <p>The following settings are suggested as a starting point for running Gradle builds in a memory constrained Jenkins agent on OpenShift Container Platform. You can modify these settings as required.</p>
      <ul>
      <li>Ensure the long-lived Gradle daemon is disabled by adding <code>org.gradle.daemon=false</code> to the <code>gradle.properties</code> file.</li>
      <li>Disable parallel build execution by ensuring <code>org.gradle.parallel=true</code> is not set in the <code>gradle.properties</code> file and that <code>--parallel</code> is not set as a command-line argument.</li>
      <li>To prevent Java compilations running out-of-process, set <code>java {"{"} options.fork = false {"}"}</code> in the <code>build.gradle</code> file.</li>
      <li>Disable multiple additional test processes by ensuring <code>test {"{"} maxParallelForks = 1 {"}"}</code> is set in the <code>build.gradle</code> file.</li>
      <li>Override the Gradle JVM memory parameters by the <code>GRADLE_OPTS</code>, <code>JAVA_OPTS</code> or <code>JAVA_TOOL_OPTIONS</code> environment variables.</li>
      <li>Set the maximum heap size and JVM arguments for any Gradle test JVM by defining the <code>maxHeapSize</code> and <code>jvmArgs</code> settings in <code>build.gradle</code>, or through the <code>-Dorg.gradle.jvmargs</code> command-line argument.</li>
      </ul>
      <h2 id="images-other-jenkins-agent-pod-retention_images-other-jenkins-agent" class="group scroll-mt-20">Jenkins agent pod retention<a href="#images-other-jenkins-agent-pod-retention_images-other-jenkins-agent" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Jenkins agent pods, are deleted by default after the build completes or is stopped. This behavior can be changed by the Kubernetes plugin pod retention setting. Pod retention can be set for all Jenkins builds, with overrides for each pod template. The following behaviors are supported:</p>
      <ul>
      <li><code>Always</code> keeps the build pod regardless of build result.</li>
      <li><code>Default</code> uses the plugin value, which is the pod template only.</li>
      <li><code>Never</code> always deletes the pod.</li>
      <li><code>On Failure</code> keeps the pod if it fails during the build.</li>
      </ul>
      <p>You can override pod retention in the pipeline Jenkinsfile:</p>
      <CodeBlock code={"podTemplate(label: \"mypod\",\n  cloud: \"openshift\",\n  inheritFrom: \"maven\",\n  podRetention: onFailure(), (1)\n  containers: [\n    ...\n  ]) {\n  node(\"mypod\") {\n    ...\n  }\n}"} lang="groovy" />
      <ol><li>Allowed values for <code>podRetention</code> are <code>never()</code>, <code>onFailure()</code>, <code>always()</code>, and <code>default()</code>.</li></ol>
      <Alert variant="warning" class="my-4">
        <AlertTitle>Warning</AlertTitle>
        <AlertDescription>
          <p>Pods that are kept might continue to run and count against resource quotas.</p>
        </AlertDescription>
      </Alert>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
