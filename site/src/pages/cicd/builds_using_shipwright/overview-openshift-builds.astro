---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: cicd/builds_using_shipwright/overview-openshift-builds.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";

const headings = [{"depth":1,"slug":"overview-openshift-builds","text":"Overview of Builds"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/cicd/builds_using_shipwright/overview-openshift-builds";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Overview of Builds";
const description = "Builds is an extensible build framework based on the Shipwright project, which you can use to build container images on your OpenShift Container Platform…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["cicd","cicd/builds_using_shipwright"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Overview of Builds {#overview-openshift-builds}\n\nBuilds is an extensible build framework based on the [Shipwright project](https://shipwright.io/), which you can use to build container images on your OpenShift Container Platform cluster. You can build container images from source code and Dockerfiles by using image build tools, such as Source-to-Image (S2I) and Buildah. You can create and apply build resources, view logs of build runs, and manage builds in your OpenShift Container Platform namespaces.\n\nBuilds includes the following capabilities:\n\n- Standard Kubernetes-native API for building container images from source code and Dockerfiles\n- Support for Source-to-Image (S2I) and Buildah build strategies\n- Extensibility with your own custom build strategies\n- Execution of builds from source code in a local directory\n- Shipwright CLI for creating and viewing logs, and managing builds on the cluster\n- Integrated user experience with the **Developer** perspective of the OpenShift Container Platform web console\n\n> [!NOTE]\n> Because Builds releases on a different cadence from OpenShift Container Platform, the Builds documentation is now available as a separate documentation set at [Builds for Red Hat OpenShift](https://docs.redhat.com/en/documentation/builds_for_red_hat_openshift).\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/cicd/builds_using_shipwright/overview-openshift-builds.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="overview-openshift-builds" class="group scroll-mt-20">Overview of Builds<a href="#overview-openshift-builds" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="/_attributes/common-attributes.md"></p>
      <p>Builds is an extensible build framework based on the <a href="https://shipwright.io/">Shipwright project</a>, which you can use to build container images on your OpenShift Container Platform cluster. You can build container images from source code and Dockerfiles by using image build tools, such as Source-to-Image (S2I) and Buildah. You can create and apply build resources, view logs of build runs, and manage builds in your OpenShift Container Platform namespaces.</p>
      <p>Builds includes the following capabilities:</p>
      <ul>
      <li>Standard Kubernetes-native API for building container images from source code and Dockerfiles</li>
      <li>Support for Source-to-Image (S2I) and Buildah build strategies</li>
      <li>Extensibility with your own custom build strategies</li>
      <li>Execution of builds from source code in a local directory</li>
      <li>Shipwright CLI for creating and viewing logs, and managing builds on the cluster</li>
      <li>Integrated user experience with the <strong>Developer</strong> perspective of the OpenShift Container Platform web console</li>
      </ul>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Because Builds releases on a different cadence from OpenShift Container Platform, the Builds documentation is now available as a separate documentation set at <a href="https://docs.redhat.com/en/documentation/builds_for_red_hat_openshift">Builds for Red Hat OpenShift</a>.</p>
        </AlertDescription>
      </Alert>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
