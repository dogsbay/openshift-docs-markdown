---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: ai_workloads/jobset_operator/jobset-uninstall.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;

const headings = [{"depth":1,"slug":"js-uninstall","text":"Uninstalling the JobSet Operator"},{"depth":2,"slug":"js-uninstall_js-uninstall","text":"Uninstalling the JobSet Operator"},{"depth":2,"slug":"js-remove-resources_js-uninstall","text":"Uninstalling JobSet Operator resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/ai_workloads/jobset_operator/jobset-uninstall";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Uninstalling the {{ js_operator }}";
const description = "Uninstall the JobSet Operator by using the OpenShift Container Platform web console to remove the Operator instance and its resources from your cluster." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["ai_workloads","ai_workloads/jobset_operator"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Uninstalling the JobSet Operator {#js-uninstall}\n\nUninstall the JobSet Operator by using the OpenShift Container Platform web console to remove the Operator instance and its resources from your cluster.\n\n## Uninstalling the JobSet Operator {#js-uninstall_js-uninstall}\n\nUninstall the JobSet Operator by using the OpenShift Container Platform web console to remove the Operator instance.\n\n**Prerequisites**\n\n- You have access to the cluster with `cluster-admin` privileges.\n- You have access to the OpenShift Container Platform web console.\n- You have installed the JobSet Operator.\n\n**Procedure**\n\n1. Log in to the OpenShift Container Platform web console.\n2. Navigate to **Operators** -> **Installed Operators**.\n3. Select `openshift-js-operator` from the **Project** dropdown list.\n4. Delete the `JobSetOperator` instance.\n\n   1. Click **JobSet Operator** and select the **JobSetOperator** tab.\n   2. Click the Options menu ![](kebab.png \"Options menu\") next to the **cluster** entry and select **Delete JobSetOperator**.\n   3. In the confirmation dialog, click **Delete**.\n5. Uninstall the JobSet Operator.\n\n   1. Navigate to **Operators** -> **Installed Operators**.\n   2. Click the Options menu ![](kebab.png \"Options menu\") next to the **JobSet Operator** entry and click **Uninstall Operator**.\n   3. In the confirmation dialog, click **Uninstall**.\n\n## Uninstalling JobSet Operator resources {#js-remove-resources_js-uninstall}\n\nOptionally, after uninstalling the JobSet Operator, you can remove its related resources from your cluster.\n\n**Prerequisites**\n\n- You have access to the cluster with `cluster-admin` privileges.\n- You have access to the OpenShift Container Platform web console.\n- You have uninstalled the JobSet Operator.\n\n**Procedure**\n\n1. Log in to the OpenShift Container Platform web console.\n2. Remove CRDs that were created when the JobSet Operator was installed:\n\n   1. Navigate to **Administration** -> **CustomResourceDefinitions**.\n   2. Enter `JobSetOperator` in the **Name** field to filter the CRDs.\n   3. Click the Options menu ![](kebab.png \"Options menu\") next to the **JobSetOperator** CRD and select **Delete CustomResourceDefinition**.\n   4. In the confirmation dialog, click **Delete**.\n3. Delete the `openshift-jobset-operator` namespace.\n\n   1. Navigate to **Administration** -> **Namespaces**.\n   2. Fine `openshift-jobset-operator` in the list of namespaces.\n   3. Click the Options menu ![](kebab.png \"Options menu\") next to the **openshift-jobset-operator** entry and select **Delete Namespace**.\n   4. In the confirmation dialog, enter `openshift-jobset-operator` and click **Delete**.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/ai_workloads/jobset_operator/jobset-uninstall.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="js-uninstall" class="group scroll-mt-20">Uninstalling the JobSet Operator<a href="#js-uninstall" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Uninstall the JobSet Operator by using the OpenShift Container Platform web console to remove the Operator instance and its resources from your cluster.</p>
      <h2 id="js-uninstall_js-uninstall" class="group scroll-mt-20">Uninstalling the JobSet Operator<a href="#js-uninstall_js-uninstall" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Uninstall the JobSet Operator by using the OpenShift Container Platform web console to remove the Operator instance.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster with <code>cluster-admin</code> privileges.</li>
      <li>You have access to the OpenShift Container Platform web console.</li>
      <li>You have installed the JobSet Operator.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Log in to the OpenShift Container Platform web console.</li>
      <li>Navigate to <strong>Operators</strong> -&gt; <strong>Installed Operators</strong>.</li>
      <li>Select <code>openshift-js-operator</code> from the <strong>Project</strong> dropdown list.</li>
      <li>
      Delete the <code>JobSetOperator</code> instance.
      <ol>
      <li>Click <strong>JobSet Operator</strong> and select the <strong>JobSetOperator</strong> tab.</li>
      <li>Click the Options menu <img src="kebab.png" alt="" loading="lazy" /> next to the <strong>cluster</strong> entry and select <strong>Delete JobSetOperator</strong>.</li>
      <li>In the confirmation dialog, click <strong>Delete</strong>.</li>
      </ol>
      </li>
      <li>
      Uninstall the JobSet Operator.
      <ol>
      <li>Navigate to <strong>Operators</strong> -&gt; <strong>Installed Operators</strong>.</li>
      <li>Click the Options menu <img src="kebab.png" alt="" loading="lazy" /> next to the <strong>JobSet Operator</strong> entry and click <strong>Uninstall Operator</strong>.</li>
      <li>In the confirmation dialog, click <strong>Uninstall</strong>.</li>
      </ol>
      </li>
      </ol>
      <h2 id="js-remove-resources_js-uninstall" class="group scroll-mt-20">Uninstalling JobSet Operator resources<a href="#js-remove-resources_js-uninstall" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Optionally, after uninstalling the JobSet Operator, you can remove its related resources from your cluster.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have access to the cluster with <code>cluster-admin</code> privileges.</li>
      <li>You have access to the OpenShift Container Platform web console.</li>
      <li>You have uninstalled the JobSet Operator.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Log in to the OpenShift Container Platform web console.</li>
      <li>
      Remove CRDs that were created when the JobSet Operator was installed:
      <ol>
      <li>Navigate to <strong>Administration</strong> -&gt; <strong>CustomResourceDefinitions</strong>.</li>
      <li>Enter <code>JobSetOperator</code> in the <strong>Name</strong> field to filter the CRDs.</li>
      <li>Click the Options menu <img src="kebab.png" alt="" loading="lazy" /> next to the <strong>JobSetOperator</strong> CRD and select <strong>Delete CustomResourceDefinition</strong>.</li>
      <li>In the confirmation dialog, click <strong>Delete</strong>.</li>
      </ol>
      </li>
      <li>
      Delete the <code>openshift-jobset-operator</code> namespace.
      <ol>
      <li>Navigate to <strong>Administration</strong> -&gt; <strong>Namespaces</strong>.</li>
      <li>Fine <code>openshift-jobset-operator</code> in the list of namespaces.</li>
      <li>Click the Options menu <img src="kebab.png" alt="" loading="lazy" /> next to the <strong>openshift-jobset-operator</strong> entry and select <strong>Delete Namespace</strong>.</li>
      <li>In the confirmation dialog, enter <code>openshift-jobset-operator</code> and click <strong>Delete</strong>.</li>
      </ol>
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
