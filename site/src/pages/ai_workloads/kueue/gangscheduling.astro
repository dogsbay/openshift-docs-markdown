---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: ai_workloads/kueue/gangscheduling.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"gangscheduling","text":"Gang scheduling"},{"depth":2,"slug":"configuring-gangscheduling_gangscheduling","text":"Configuring gang scheduling"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/ai_workloads/kueue/gangscheduling";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Gang scheduling";
const description = "You can use gang scheduling to ensure that a group, or gang, of related jobs starts only when all required resources are available." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["ai_workloads","ai_workloads/kueue"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Gang scheduling {#gangscheduling}\n\nYou can use gang scheduling to ensure that a group, or gang, of related jobs starts only when all required resources are available.\n\nRed Hat build of Kueue enables gang scheduling by suspending jobs until the OpenShift Container Platform cluster can guarantee the capacity to start and execute all of the related jobs in the *gang* together. This is also known as *all-or-nothing* scheduling.\n\nGang scheduling is important if you are working with expensive, limited resources, such as GPUs. Gang scheduling can prevent jobs from claiming but not using GPUs, which can improve GPU utilization and can reduce running costs. Gang scheduling can also help to prevent issues like resource segmentation and deadlocking.\n\n## Configuring gang scheduling {#configuring-gangscheduling_gangscheduling}\n\nAs a cluster administrator, you can configure gang scheduling by modifying the `gangScheduling` spec in the `Kueue` custom resource (CR).\n\n```yaml {title=\"Example Kueue CR with gang scheduling configured\"}\napiVersion: kueue.openshift.io/v1\nkind: Kueue\nmetadata:\n  name: cluster\n  labels:\n    app.kubernetes.io/managed-by: kustomize\n    app.kubernetes.io/name: kueue-operator\n  namespace: openshift-kueue-operator\nspec:\n  config:\n    gangScheduling:\n      policy: ByWorkload\n      byWorkload:\n        admission: Parallel\n# ...\n```\n\n`spec.config.gangScheduling.policy`\n:   You can set the `policy` value to enable or disable gang scheduling. The possible values are `ByWorkload`, `None`, or empty (`\"\"`). When the `policy` value is set to `ByWorkload`, each job is processed and considered for admission as a single unit. If the job does not become ready within the specified time, the entire job is evicted and retried at a later time. When the `policy` value is set to `None`, gang scheduling is disabled. When the `policy` value is empty or set to `\"\"`, the Red Hat build of Kueue Operator determines settings for gang scheduling. Currently, gang scheduling is disabled by default.\n\n`spec.config.gangScheduling.byWorkload.admission`\n:   If the `policy` value is set to `ByWorkload`, you must configure job admission settings. The possible values for the `admission` spec are `Parallel`, `Sequential`, or empty (`\"\"`). When the `admission` value is set to `Parallel`, pods from any job can be admitted at any time. This can cause a deadlock, where jobs are in contention for cluster capacity. When a deadlock occurs, the successful scheduling of pods from another job can prevent the scheduling of pods from the current job. When the `admission` value is set to `Sequential`, only pods from the currently processing job are admitted. After all of the pods from the current job have been admitted and are ready, Red Hat build of Kueue processes the next job. Sequential processing can slow down admission when the cluster has sufficient capacity for multiple jobs, but provides a higher likelihood that all of the pods for a job are scheduled together successfully. When the `admission` value is empty or set to `\"\"`, the Red Hat build of Kueue Operator determines job admission settings. Currently, the `admission` value is set to `Parallel` by default.\n\n**Additional resources**\n{._additional-resources}\n\n- [Creating a Kueue custom resource](/openshift-docs-markdown/ai_workloads/kueue/install-kueue#create-kueue-cr_install-kueue)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/ai_workloads/kueue/gangscheduling.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="gangscheduling" class="group scroll-mt-20">Gang scheduling<a href="#gangscheduling" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">You can use gang scheduling to ensure that a group, or gang, of related jobs starts only when all required resources are available.</p>
      <p>Red Hat build of Kueue enables gang scheduling by suspending jobs until the OpenShift Container Platform cluster can guarantee the capacity to start and execute all of the related jobs in the <em>gang</em> together. This is also known as <em>all-or-nothing</em> scheduling.</p>
      <p>Gang scheduling is important if you are working with expensive, limited resources, such as GPUs. Gang scheduling can prevent jobs from claiming but not using GPUs, which can improve GPU utilization and can reduce running costs. Gang scheduling can also help to prevent issues like resource segmentation and deadlocking.</p>
      <h2 id="configuring-gangscheduling_gangscheduling" class="group scroll-mt-20">Configuring gang scheduling<a href="#configuring-gangscheduling_gangscheduling" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">As a cluster administrator, you can configure gang scheduling by modifying the <code>gangScheduling</code> spec in the <code>Kueue</code> custom resource (CR).</p>
      <CodeBlock code={"apiVersion: kueue.openshift.io/v1\nkind: Kueue\nmetadata:\n  name: cluster\n  labels:\n    app.kubernetes.io/managed-by: kustomize\n    app.kubernetes.io/name: kueue-operator\n  namespace: openshift-kueue-operator\nspec:\n  config:\n    gangScheduling:\n      policy: ByWorkload\n      byWorkload:\n        admission: Parallel\n# ..."} lang="yaml" title="Example Kueue CR with gang scheduling configured" />
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.config.gangScheduling.policy</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>You can set the <code>policy</code> value to enable or disable gang scheduling. The possible values are <code>ByWorkload</code>, <code>None</code>, or empty (<code>""</code>). When the <code>policy</code> value is set to <code>ByWorkload</code>, each job is processed and considered for admission as a single unit. If the job does not become ready within the specified time, the entire job is evicted and retried at a later time. When the <code>policy</code> value is set to <code>None</code>, gang scheduling is disabled. When the <code>policy</code> value is empty or set to <code>""</code>, the Red Hat build of Kueue Operator determines settings for gang scheduling. Currently, gang scheduling is disabled by default.</p></dd>
      <dt class="font-semibold"><code>spec.config.gangScheduling.byWorkload.admission</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>If the <code>policy</code> value is set to <code>ByWorkload</code>, you must configure job admission settings. The possible values for the <code>admission</code> spec are <code>Parallel</code>, <code>Sequential</code>, or empty (<code>""</code>). When the <code>admission</code> value is set to <code>Parallel</code>, pods from any job can be admitted at any time. This can cause a deadlock, where jobs are in contention for cluster capacity. When a deadlock occurs, the successful scheduling of pods from another job can prevent the scheduling of pods from the current job. When the <code>admission</code> value is set to <code>Sequential</code>, only pods from the currently processing job are admitted. After all of the pods from the current job have been admitted and are ready, Red Hat build of Kueue processes the next job. Sequential processing can slow down admission when the cluster has sufficient capacity for multiple jobs, but provides a higher likelihood that all of the pods for a job are scheduled together successfully. When the <code>admission</code> value is empty or set to <code>""</code>, the Red Hat build of Kueue Operator determines job admission settings. Currently, the <code>admission</code> value is set to <code>Parallel</code> by default.</p></dd>
      </dl>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul><li><a href="/openshift-docs-markdown/ai_workloads/kueue/install-kueue#create-kueue-cr_install-kueue">Creating a Kueue custom resource</a></li></ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
