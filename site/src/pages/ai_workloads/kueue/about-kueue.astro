---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: ai_workloads/kueue/about-kueue.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";

const headings = [{"depth":1,"slug":"about-kueue","text":"Introduction to Red Hat build of Kueue"},{"depth":2,"slug":"about-kueue-personas","text":"Personas"},{"depth":2,"slug":"about-kueue-workflow","text":"Workflow overview"},{"depth":2,"slug":"additional-resources_about-kueue","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/ai_workloads/kueue/about-kueue";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Introduction to {{ kueue_name }}";
const description = "You can use Red Hat build of Kueue to manage when jobs in your Kubernetes cluster start, get preempted, or wait for resources. Red Hat build of Kueue…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["ai_workloads","ai_workloads/kueue"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Introduction to Red Hat build of Kueue {#about-kueue}\n\nYou can use Red Hat build of Kueue to manage when jobs in your Kubernetes cluster start, get preempted, or wait for resources. Red Hat build of Kueue optimizes resource utilization by enforcing quotas and scheduling policies across your workloads.\n\nRed Hat build of Kueue can determine when a job waits, is admitted to start by creating pods, or should be *preempted*, meaning that active pods for that job are deleted.\n\n> [!NOTE]\n> In the context of Red Hat build of Kueue, a job can be defined as a one-time or on-demand task that runs to completion.\n\nRed Hat build of Kueue is based on the Kueue open source project.\n\nRed Hat build of Kueue is compatible with environments that use heterogeneous, elastic resources, where many different resource types exist and those resources are capable of dynamic scaling.\n\nRed Hat build of Kueue does not replace any existing components in a Kubernetes cluster, but instead integrates with the existing Kubernetes API server, scheduler, and cluster autoscaler components.\n\nRed Hat build of Kueue supports all-or-nothing semantics, where either an entire job with all of its components is admitted to the cluster, or the entire job is rejected if it does not fit on the cluster.\n\n## Personas {#about-kueue-personas}\n\nDifferent personas exist in a Red Hat build of Kueue workflow.\n\nBatch administrators\n:   Batch administrators manage the cluster infrastructure and establish quotas and queues.\n\nBatch users\n:   Batch users run jobs on the cluster. Examples of batch users might be researchers, AI/ML engineers, or data scientists.\n\nServing users\n:   Serving users run jobs on the cluster. For example, to expose a trained AI/ML model for inference.\n\nPlatform developers\n:   Platform developers integrate Red Hat build of Kueue with other software. They might also contribute to the Kueue open source project.\n\n## Workflow overview {#about-kueue-workflow}\n\nThe Red Hat build of Kueue workflow can be described at a high level as follows:\n\n1. Batch administrators create and configure `ResourceFlavor`, `LocalQueue`, and `ClusterQueue` resources.\n2. User personas create jobs on the cluster.\n3. The Kubernetes API server validates and accepts job data.\n4. Red Hat build of Kueue admits jobs based on configured options, such as order or quota. It injects affinity into the job by using resource flavors, and creates a `Workload` object that corresponds to each job.\n5. The applicable controller for the job type creates pods.\n6. The Kubernetes scheduler assigns pods to a node in the cluster.\n7. The Kubernetes cluster autoscaler provisions more nodes as required.\n\n## Additional resources {#additional-resources_about-kueue}\n\n- [Kueue (upstream documentation)](https://kueue.sigs.k8s.io/docs/)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/ai_workloads/kueue/about-kueue.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="about-kueue" class="group scroll-mt-20">Introduction to Red Hat build of Kueue<a href="#about-kueue" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can use Red Hat build of Kueue to manage when jobs in your Kubernetes cluster start, get preempted, or wait for resources. Red Hat build of Kueue optimizes resource utilization by enforcing quotas and scheduling policies across your workloads.</p>
      <p>Red Hat build of Kueue can determine when a job waits, is admitted to start by creating pods, or should be <em>preempted</em>, meaning that active pods for that job are deleted.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>In the context of Red Hat build of Kueue, a job can be defined as a one-time or on-demand task that runs to completion.</p>
        </AlertDescription>
      </Alert>
      <p>Red Hat build of Kueue is based on the Kueue open source project.</p>
      <p>Red Hat build of Kueue is compatible with environments that use heterogeneous, elastic resources, where many different resource types exist and those resources are capable of dynamic scaling.</p>
      <p>Red Hat build of Kueue does not replace any existing components in a Kubernetes cluster, but instead integrates with the existing Kubernetes API server, scheduler, and cluster autoscaler components.</p>
      <p>Red Hat build of Kueue supports all-or-nothing semantics, where either an entire job with all of its components is admitted to the cluster, or the entire job is rejected if it does not fit on the cluster.</p>
      <h2 id="about-kueue-personas" class="group scroll-mt-20">Personas<a href="#about-kueue-personas" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Different personas exist in a Red Hat build of Kueue workflow.</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Batch administrators</dt>
      <dd class="ml-6 text-muted-foreground"><p>Batch administrators manage the cluster infrastructure and establish quotas and queues.</p></dd>
      <dt class="font-semibold">Batch users</dt>
      <dd class="ml-6 text-muted-foreground"><p>Batch users run jobs on the cluster. Examples of batch users might be researchers, AI/ML engineers, or data scientists.</p></dd>
      <dt class="font-semibold">Serving users</dt>
      <dd class="ml-6 text-muted-foreground"><p>Serving users run jobs on the cluster. For example, to expose a trained AI/ML model for inference.</p></dd>
      <dt class="font-semibold">Platform developers</dt>
      <dd class="ml-6 text-muted-foreground"><p>Platform developers integrate Red Hat build of Kueue with other software. They might also contribute to the Kueue open source project.</p></dd>
      </dl>
      <h2 id="about-kueue-workflow" class="group scroll-mt-20">Workflow overview<a href="#about-kueue-workflow" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>The Red Hat build of Kueue workflow can be described at a high level as follows:</p>
      <ol>
      <li>Batch administrators create and configure <code>ResourceFlavor</code>, <code>LocalQueue</code>, and <code>ClusterQueue</code> resources.</li>
      <li>User personas create jobs on the cluster.</li>
      <li>The Kubernetes API server validates and accepts job data.</li>
      <li>Red Hat build of Kueue admits jobs based on configured options, such as order or quota. It injects affinity into the job by using resource flavors, and creates a <code>Workload</code> object that corresponds to each job.</li>
      <li>The applicable controller for the job type creates pods.</li>
      <li>The Kubernetes scheduler assigns pods to a node in the cluster.</li>
      <li>The Kubernetes cluster autoscaler provisions more nodes as required.</li>
      </ol>
      <h2 id="additional-resources_about-kueue" class="group scroll-mt-20">Additional resources<a href="#additional-resources_about-kueue" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul><li><a href="https://kueue.sigs.k8s.io/docs/">Kueue (upstream documentation)</a></li></ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
