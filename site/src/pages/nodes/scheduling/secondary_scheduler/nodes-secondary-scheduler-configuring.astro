---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: nodes/scheduling/secondary_scheduler/nodes-secondary-scheduler-configuring.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"secondary-scheduler-configuring","text":"Scheduling pods using a secondary scheduler"},{"depth":2,"slug":"nodes-secondary-scheduler-install-console_secondary-scheduler-configuring","text":"Installing the Secondary Scheduler Operator"},{"depth":2,"slug":"nodes-secondary-scheduler-configuring-console_secondary-scheduler-configuring","text":"Deploying a secondary scheduler"},{"depth":2,"slug":"nodes-secondary-scheduler-pod-console_secondary-scheduler-configuring","text":"Scheduling a pod using the secondary scheduler"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/nodes/scheduling/secondary_scheduler/nodes-secondary-scheduler-configuring";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Scheduling pods using a secondary scheduler";
const description = "You can run a custom secondary scheduler in OpenShift Container Platform by installing the Secondary Scheduler Operator, deploying the secondary…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["nodes","nodes/scheduling","nodes/scheduling/secondary_scheduler"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Scheduling pods using a secondary scheduler {#secondary-scheduler-configuring}\n\nYou can run a custom secondary scheduler in OpenShift Container Platform by installing the Secondary Scheduler Operator, deploying the secondary scheduler, and setting the secondary scheduler in the pod definition.\n\n## Installing the Secondary Scheduler Operator {#nodes-secondary-scheduler-install-console_secondary-scheduler-configuring}\n\nYou can install the Secondary Scheduler Operator for Red Hat OpenShift through the OpenShift Container Platform web console to configure a secondary scheduler.\n\n**Prerequisites**\n\n- You are logged in to OpenShift Container Platform as a user with the `cluster-admin` role.\n- You have access to the OpenShift Container Platform web console.\n\n**Procedure**\n\n1. Log in to the OpenShift Container Platform web console.\n2. Create the required namespace for the Secondary Scheduler Operator for Red Hat OpenShift.\n\n   1. Navigate to **Administration** -> **Namespaces** and click **Create Namespace**.\n   2. Enter `openshift-secondary-scheduler-operator` in the **Name** field and click **Create**.\n3. Install the Secondary Scheduler Operator for Red Hat OpenShift.\n\n   1. Navigate to **Ecosystem** -> **Software Catalog**.\n   2. Enter **Secondary Scheduler Operator for Red Hat OpenShift** into the filter box.\n   3. Select the **Secondary Scheduler Operator for Red Hat OpenShift** and click **Install**.\n   4. On the **Install Operator** page:\n\n      1. The **Update channel** is set to **stable**, which installs the latest stable release of the Secondary Scheduler Operator for Red Hat OpenShift.\n      2. Select **A specific namespace on the cluster** and select **openshift-secondary-scheduler-operator** from the drop-down menu.\n      3. Select an **Update approval** strategy.\n\n         - The **Automatic** strategy allows Operator Lifecycle Manager (OLM) to automatically update the Operator when a new version is available.\n         - The **Manual** strategy requires a user with appropriate credentials to approve the Operator update.\n      4. Click **Install**.\n\n**Verification**\n\n1. Navigate to **Ecosystem** -> **Installed Operators**.\n2. Verify that **Secondary Scheduler Operator for Red Hat OpenShift** is listed with a **Status** of **Succeeded**.\n\n## Deploying a secondary scheduler {#nodes-secondary-scheduler-configuring-console_secondary-scheduler-configuring}\n\nAfter you have installed the Secondary Scheduler Operator, you can deploy a secondary scheduler to apply custom placement logic for specific pods.\n\n**Prerequisites**\n\n- You are logged in to OpenShift Container Platform as a user with the `cluster-admin` role.\n- You have access to the OpenShift Container Platform web console.\n- The Secondary Scheduler Operator for Red Hat OpenShift is installed.\n\n**Procedure**\n\n1. Log in to the OpenShift Container Platform web console.\n2. Create config map to hold the configuration for the secondary scheduler.\n\n   1. Navigate to **Workloads** -> **ConfigMaps**.\n   2. Click **Create ConfigMap**.\n   3. In the YAML editor, enter the config map definition that contains the necessary `KubeSchedulerConfiguration` configuration. For example:\n\n      ```yaml\n      apiVersion: v1\n      kind: ConfigMap\n      metadata:\n        name: \"secondary-scheduler-config\"\n        namespace: \"openshift-secondary-scheduler-operator\"\n      data:\n        \"config.yaml\": |\n          apiVersion: kubescheduler.config.k8s.io/v1\n          kind: KubeSchedulerConfiguration\n          leaderElection:\n            leaderElect: false\n          profiles:\n            - schedulerName: secondary-scheduler\n              plugins:\n                score:\n                  disabled:\n                    - name: NodeResourcesBalancedAllocation\n                    - name: NodeResourcesLeastAllocated\n      ```\n\n      where:\n\n      `metadata.name`\n      :   Specifies the name of the config map. This is used in the **Scheduler Config** field when creating the `SecondaryScheduler` CR.\n\n      `metadata.namespace`\n      :   Specifies the namespace to create the config map in. The namespace must be `openshift-secondary-scheduler-operator`.\n\n      `data.\"config.yaml\".kind`\n      :   Specifies the `KubeSchedulerConfiguration` resource for the secondary scheduler. For more information, see [`KubeSchedulerConfiguration`](https://kubernetes.io/docs/reference/config-api/kube-scheduler-config.v1/#kubescheduler-config-k8s-io-v1-KubeSchedulerConfiguration) in the Kubernetes API documentation.\n\n      `data.\"config.yaml\".profiles.schedulerName`\n      :   Specifies the name of the secondary scheduler. Pods that set their `spec.schedulerName` field to this value are scheduled with this secondary scheduler.\n\n      `data.\"config.yaml\".profiles.plugins`\n      :   Specifies the plugins to enable or disable for the secondary scheduler. For a list default scheduling plugins, see [Scheduling plugins](https://kubernetes.io/docs/reference/scheduling/config/#scheduling-plugins) in the Kubernetes documentation.\n   4. Click **Create**.\n3. Create the `SecondaryScheduler` CR:\n\n   1. Navigate to **Ecosystem** -> **Installed Operators**.\n   2. Select **Secondary Scheduler Operator for Red Hat OpenShift**.\n   3. Select the **Secondary Scheduler** tab and click **Create SecondaryScheduler**.\n   4. The **Name** field defaults to `cluster`; do not change this name.\n   5. The **schedulerConfig** field defaults to `secondary-scheduler-config`. Ensure that this value matches the name of the config map created earlier in this procedure.\n   6. In the **schedulerImage** field, enter the image name for your custom scheduler.\n\n      > [!IMPORTANT]\n      > Red Hat does not directly support the functionality of your custom secondary scheduler.\n   7. Optional: To enable high availability for the secondary scheduler, configure the following settings:\n\n      1. Expand the **topology** section.\n      2. In the **mode** field, select **HighlyAvailable**.\n      3. In the **maxReplicas** field, enter the maximum number of secondary scheduler replicas to deploy. If unset, the maximum number of replicas is `3`.\n      4. In the **tolerations** field, enter tolerations to allow scheduler replicas on tainted nodes. If unset, no taints are tolerated.\n\n      > [!NOTE]\n      > To configure node selectors, use the **YAML view** option. Add the `spec.topology.highlyAvailableTopology.nodeSelector` field and enter the necessary node labels to target a specific group of nodes for scheduler replica placement. If unset, all nodes are considered.\n   8. Click **Create**.\n\n## Scheduling a pod using the secondary scheduler {#nodes-secondary-scheduler-pod-console_secondary-scheduler-configuring}\n\nTo schedule a pod by using the secondary scheduler, set the `schedulerName` field in the pod definition.\n\n**Prerequisites**\n\n- You are logged in to OpenShift Container Platform as a user with the `cluster-admin` role.\n- You have access to the OpenShift Container Platform web console.\n- The Secondary Scheduler Operator for Red Hat OpenShift is installed.\n- A secondary scheduler is configured.\n\n**Procedure**\n\n1. Log in to the OpenShift Container Platform web console.\n2. Navigate to **Workloads** -> **Pods**.\n3. Click **Create Pod**.\n4. In the YAML editor, enter the desired pod configuration and add the `schedulerName` field:\n\n   ```yaml\n   apiVersion: v1\n   kind: Pod\n   metadata:\n     name: nginx\n     namespace: default\n   spec:\n     securityContext:\n       runAsNonRoot: true\n       seccompProfile:\n         type: RuntimeDefault\n     containers:\n       - name: nginx\n         image: nginx:1.14.2\n         ports:\n           - containerPort: 80\n         securityContext:\n           allowPrivilegeEscalation: false\n           capabilities:\n             drop: [ALL]\n     schedulerName: secondary-scheduler\n   ```\n\n   The `spec.schedulerName` field must match the name that is defined in the config map when you configured the secondary scheduler.\n5. Click **Create**.\n\n**Verification**\n\n1. Log in to the OpenShift CLI.\n2. Describe the pod using the following command:\n\n   ```terminal\n   $ oc describe pod nginx -n default\n   ```\n\n   ```text {title=\"Example output\"}\n   Name:         nginx\n   Namespace:    default\n   Priority:     0\n   Node:         ci-ln-t0w4r1k-72292-xkqs4-worker-b-xqkxp/10.0.128.3\n   ...\n   Events:\n     Type    Reason          Age   From                 Message\n     ----    ------          ----  ----                 -------\n     Normal  Scheduled       12s   secondary-scheduler  Successfully assigned default/nginx to ci-ln-t0w4r1k-72292-xkqs4-worker-b-xqkxp\n   ...\n   ```\n3. In the events table, find the event with a message similar to `Successfully assigned <namespace>/<pod_name> to <node_name>`.\n4. In the \"From\" column, verify that the event was generated from the secondary scheduler and not the default scheduler.\n\n   > [!NOTE]\n   > You can also check the `secondary-scheduler-*` pod logs in the `openshift-secondary-scheduler-namespace` to verify that the pod was scheduled by the secondary scheduler.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/nodes/scheduling/secondary_scheduler/nodes-secondary-scheduler-configuring.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="secondary-scheduler-configuring" class="group scroll-mt-20">Scheduling pods using a secondary scheduler<a href="#secondary-scheduler-configuring" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can run a custom secondary scheduler in OpenShift Container Platform by installing the Secondary Scheduler Operator, deploying the secondary scheduler, and setting the secondary scheduler in the pod definition.</p>
      <h2 id="nodes-secondary-scheduler-install-console_secondary-scheduler-configuring" class="group scroll-mt-20">Installing the Secondary Scheduler Operator<a href="#nodes-secondary-scheduler-install-console_secondary-scheduler-configuring" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can install the Secondary Scheduler Operator for Red Hat OpenShift through the OpenShift Container Platform web console to configure a secondary scheduler.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You are logged in to OpenShift Container Platform as a user with the <code>cluster-admin</code> role.</li>
      <li>You have access to the OpenShift Container Platform web console.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Log in to the OpenShift Container Platform web console.</li>
      <li>
      Create the required namespace for the Secondary Scheduler Operator for Red Hat OpenShift.
      <ol>
      <li>Navigate to <strong>Administration</strong> -&gt; <strong>Namespaces</strong> and click <strong>Create Namespace</strong>.</li>
      <li>Enter <code>openshift-secondary-scheduler-operator</code> in the <strong>Name</strong> field and click <strong>Create</strong>.</li>
      </ol>
      </li>
      <li>
      Install the Secondary Scheduler Operator for Red Hat OpenShift.
      <ol>
      <li>Navigate to <strong>Ecosystem</strong> -&gt; <strong>Software Catalog</strong>.</li>
      <li>Enter <strong>Secondary Scheduler Operator for Red Hat OpenShift</strong> into the filter box.</li>
      <li>Select the <strong>Secondary Scheduler Operator for Red Hat OpenShift</strong> and click <strong>Install</strong>.</li>
      <li>
      On the <strong>Install Operator</strong> page:
      <ol>
      <li>The <strong>Update channel</strong> is set to <strong>stable</strong>, which installs the latest stable release of the Secondary Scheduler Operator for Red Hat OpenShift.</li>
      <li>Select <strong>A specific namespace on the cluster</strong> and select <strong>openshift-secondary-scheduler-operator</strong> from the drop-down menu.</li>
      <li>
      Select an <strong>Update approval</strong> strategy.
      <ul>
      <li>The <strong>Automatic</strong> strategy allows Operator Lifecycle Manager (OLM) to automatically update the Operator when a new version is available.</li>
      <li>The <strong>Manual</strong> strategy requires a user with appropriate credentials to approve the Operator update.</li>
      </ul>
      </li>
      <li>Click <strong>Install</strong>.</li>
      </ol>
      </li>
      </ol>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>Navigate to <strong>Ecosystem</strong> -&gt; <strong>Installed Operators</strong>.</li>
      <li>Verify that <strong>Secondary Scheduler Operator for Red Hat OpenShift</strong> is listed with a <strong>Status</strong> of <strong>Succeeded</strong>.</li>
      </ol>
      <h2 id="nodes-secondary-scheduler-configuring-console_secondary-scheduler-configuring" class="group scroll-mt-20">Deploying a secondary scheduler<a href="#nodes-secondary-scheduler-configuring-console_secondary-scheduler-configuring" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">After you have installed the Secondary Scheduler Operator, you can deploy a secondary scheduler to apply custom placement logic for specific pods.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You are logged in to OpenShift Container Platform as a user with the <code>cluster-admin</code> role.</li>
      <li>You have access to the OpenShift Container Platform web console.</li>
      <li>The Secondary Scheduler Operator for Red Hat OpenShift is installed.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Log in to the OpenShift Container Platform web console.</li>
      <li>
      Create config map to hold the configuration for the secondary scheduler.
      <ol>
      <li>Navigate to <strong>Workloads</strong> -&gt; <strong>ConfigMaps</strong>.</li>
      <li>Click <strong>Create ConfigMap</strong>.</li>
      <li>
      In the YAML editor, enter the config map definition that contains the necessary <code>KubeSchedulerConfiguration</code> configuration. For example:
      <CodeBlock code={"apiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: \"secondary-scheduler-config\"\n  namespace: \"openshift-secondary-scheduler-operator\"\ndata:\n  \"config.yaml\": |\n    apiVersion: kubescheduler.config.k8s.io/v1\n    kind: KubeSchedulerConfiguration\n    leaderElection:\n      leaderElect: false\n    profiles:\n      - schedulerName: secondary-scheduler\n        plugins:\n          score:\n            disabled:\n              - name: NodeResourcesBalancedAllocation\n              - name: NodeResourcesLeastAllocated"} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>metadata.name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the config map. This is used in the <strong>Scheduler Config</strong> field when creating the <code>SecondaryScheduler</code> CR.</p></dd>
      <dt class="font-semibold"><code>metadata.namespace</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the namespace to create the config map in. The namespace must be <code>openshift-secondary-scheduler-operator</code>.</p></dd>
      <dt class="font-semibold"><code>data."config.yaml".kind</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the <code>KubeSchedulerConfiguration</code> resource for the secondary scheduler. For more information, see <a href="https://kubernetes.io/docs/reference/config-api/kube-scheduler-config.v1/#kubescheduler-config-k8s-io-v1-KubeSchedulerConfiguration"><code>KubeSchedulerConfiguration</code></a> in the Kubernetes API documentation.</p></dd>
      <dt class="font-semibold"><code>data."config.yaml".profiles.schedulerName</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the name of the secondary scheduler. Pods that set their <code>spec.schedulerName</code> field to this value are scheduled with this secondary scheduler.</p></dd>
      <dt class="font-semibold"><code>data."config.yaml".profiles.plugins</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the plugins to enable or disable for the secondary scheduler. For a list default scheduling plugins, see <a href="https://kubernetes.io/docs/reference/scheduling/config/#scheduling-plugins">Scheduling plugins</a> in the Kubernetes documentation.</p></dd>
      </dl>
      </li>
      <li>Click <strong>Create</strong>.</li>
      </ol>
      </li>
      <li>
      Create the <code>SecondaryScheduler</code> CR:
      <ol>
      <li>Navigate to <strong>Ecosystem</strong> -&gt; <strong>Installed Operators</strong>.</li>
      <li>Select <strong>Secondary Scheduler Operator for Red Hat OpenShift</strong>.</li>
      <li>Select the <strong>Secondary Scheduler</strong> tab and click <strong>Create SecondaryScheduler</strong>.</li>
      <li>The <strong>Name</strong> field defaults to <code>cluster</code>; do not change this name.</li>
      <li>The <strong>schedulerConfig</strong> field defaults to <code>secondary-scheduler-config</code>. Ensure that this value matches the name of the config map created earlier in this procedure.</li>
      <li>
      In the <strong>schedulerImage</strong> field, enter the image name for your custom scheduler.
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Red Hat does not directly support the functionality of your custom secondary scheduler.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>
      Optional: To enable high availability for the secondary scheduler, configure the following settings:
      <ol>
      <li>Expand the <strong>topology</strong> section.</li>
      <li>In the <strong>mode</strong> field, select <strong>HighlyAvailable</strong>.</li>
      <li>In the <strong>maxReplicas</strong> field, enter the maximum number of secondary scheduler replicas to deploy. If unset, the maximum number of replicas is <code>3</code>.</li>
      <li>In the <strong>tolerations</strong> field, enter tolerations to allow scheduler replicas on tainted nodes. If unset, no taints are tolerated.</li>
      </ol>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>To configure node selectors, use the <strong>YAML view</strong> option. Add the <code>spec.topology.highlyAvailableTopology.nodeSelector</code> field and enter the necessary node labels to target a specific group of nodes for scheduler replica placement. If unset, all nodes are considered.</p>
        </AlertDescription>
      </Alert>
      </li>
      <li>Click <strong>Create</strong>.</li>
      </ol>
      </li>
      </ol>
      <h2 id="nodes-secondary-scheduler-pod-console_secondary-scheduler-configuring" class="group scroll-mt-20">Scheduling a pod using the secondary scheduler<a href="#nodes-secondary-scheduler-pod-console_secondary-scheduler-configuring" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">To schedule a pod by using the secondary scheduler, set the <code>schedulerName</code> field in the pod definition.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You are logged in to OpenShift Container Platform as a user with the <code>cluster-admin</code> role.</li>
      <li>You have access to the OpenShift Container Platform web console.</li>
      <li>The Secondary Scheduler Operator for Red Hat OpenShift is installed.</li>
      <li>A secondary scheduler is configured.</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Log in to the OpenShift Container Platform web console.</li>
      <li>Navigate to <strong>Workloads</strong> -&gt; <strong>Pods</strong>.</li>
      <li>Click <strong>Create Pod</strong>.</li>
      <li>
      In the YAML editor, enter the desired pod configuration and add the <code>schedulerName</code> field:
      <CodeBlock code={"apiVersion: v1\nkind: Pod\nmetadata:\n  name: nginx\n  namespace: default\nspec:\n  securityContext:\n    runAsNonRoot: true\n    seccompProfile:\n      type: RuntimeDefault\n  containers:\n    - name: nginx\n      image: nginx:1.14.2\n      ports:\n        - containerPort: 80\n      securityContext:\n        allowPrivilegeEscalation: false\n        capabilities:\n          drop: [ALL]\n  schedulerName: secondary-scheduler"} lang="yaml" />
      <p>The <code>spec.schedulerName</code> field must match the name that is defined in the config map when you configured the secondary scheduler.</p>
      </li>
      <li>Click <strong>Create</strong>.</li>
      </ol>
      <p><strong>Verification</strong></p>
      <ol>
      <li>Log in to the OpenShift CLI.</li>
      <li>
      Describe the pod using the following command:
      <CodeBlock code={"$ oc describe pod nginx -n default"} lang="terminal" />
      <CodeBlock code={"Name:         nginx\nNamespace:    default\nPriority:     0\nNode:         ci-ln-t0w4r1k-72292-xkqs4-worker-b-xqkxp/10.0.128.3\n...\nEvents:\n  Type    Reason          Age   From                 Message\n  ----    ------          ----  ----                 -------\n  Normal  Scheduled       12s   secondary-scheduler  Successfully assigned default/nginx to ci-ln-t0w4r1k-72292-xkqs4-worker-b-xqkxp\n..."} lang="text" title="Example output" />
      </li>
      <li>In the events table, find the event with a message similar to <code>Successfully assigned &lt;namespace&gt;/&lt;pod_name&gt; to &lt;node_name&gt;</code>.</li>
      <li>
      In the "From" column, verify that the event was generated from the secondary scheduler and not the default scheduler.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>You can also check the <code>secondary-scheduler-*</code> pod logs in the <code>openshift-secondary-scheduler-namespace</code> to verify that the pod was scheduled by the secondary scheduler.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
