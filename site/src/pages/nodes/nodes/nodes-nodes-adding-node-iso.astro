---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: nodes/nodes/nodes-nodes-adding-node-iso.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";

const headings = [{"depth":1,"slug":"adding-node-iso","text":"Adding worker nodes to an on-premise cluster"},{"depth":2,"slug":"additional-resources_adding-node-iso","text":"Additional resources"}];
const nav = navData;
const currentPath = "/nodes/nodes/nodes-nodes-adding-node-iso";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Adding worker nodes to an on-premise cluster";
const description = "You can add worker nodes to on-premise clusters by using the OpenShift CLI (oc) to generate an ISO image, which can then be used to boot one or more nodes…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["nodes","nodes/nodes"];
const pageTaxonomies = null;
const tagsIndexPath = "/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Adding worker nodes to an on-premise cluster {#adding-node-iso}\n\nYou can add worker nodes to on-premise clusters by using the OpenShift CLI (`oc`) to generate an ISO image, which can then be used to boot one or more nodes in your target cluster. This process can be used regardless of how you installed your cluster.\n\nYou can add one or more nodes at a time while customizing each node with more complex configurations, such as static network configuration, or you can specify only the MAC address of each node. Any required configurations that are not specified during ISO generation are retrieved from the target cluster and applied to the new nodes.\n\n> [!NOTE]\n> `Machine` or `BareMetalHost` resources are not automatically created after a node has been successfully added to the cluster.\n\nPreflight validation checks are also performed when booting the ISO image to inform you of failure-causing issues before you attempt to boot each node.\n\nSupported platforms\n:   The following platforms are supported for this method of adding nodes:\n\n    - `baremetal`\n    - `vsphere`\n    - `nutanix`\n    - `none`\n\nSupported architectures\n:   The following architecture combinations have been validated to work when adding worker nodes using this process:\n\n- `amd64` worker nodes on `amd64` or `arm64` clusters\n- `arm64` worker nodes on `amd64` or `arm64` clusters\n- `s390x` worker nodes on `s390x` clusters\n- `ppc64le` worker nodes on `ppc64le` clusters\n\nAdding nodes to your cluster\n:   You can add nodes with this method in the following two ways:\n\n- Adding one or more nodes using a configuration file.\n\n  You can specify configurations for one or more nodes in the `nodes-config.yaml` file before running the `oc adm node-image create` command. This is useful if you want to add more than one node at a time, or if you are specifying complex configurations.\n- Adding a single node using only command flags.\n\n  You can add a node by running the `oc adm node-image create` command with flags to specify your configurations. This is useful if you want to add only a single node at a time, and have only simple configurations to specify for that node.\n\n## Additional resources {#additional-resources_adding-node-iso}\n\n- [Root device hints](/installing/installing_bare_metal/ipi/ipi-install-installation-workflow#root-device-hints_ipi-install-installation-workflow)\n","mdUrl":"/nodes/nodes/nodes-nodes-adding-node-iso.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={""}
  navMode={"client"}
  pagefindUrl={"/pagefind/"}
  favicon={"/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="adding-node-iso" class="group scroll-mt-20">Adding worker nodes to an on-premise cluster<a href="#adding-node-iso" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="/_attributes/common-attributes.md"></p>
      <p>You can add worker nodes to on-premise clusters by using the OpenShift CLI (<code>oc</code>) to generate an ISO image, which can then be used to boot one or more nodes in your target cluster. This process can be used regardless of how you installed your cluster.</p>
      <p>You can add one or more nodes at a time while customizing each node with more complex configurations, such as static network configuration, or you can specify only the MAC address of each node. Any required configurations that are not specified during ISO generation are retrieved from the target cluster and applied to the new nodes.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p><code>Machine</code> or <code>BareMetalHost</code> resources are not automatically created after a node has been successfully added to the cluster.</p>
        </AlertDescription>
      </Alert>
      <p>Preflight validation checks are also performed when booting the ISO image to inform you of failure-causing issues before you attempt to boot each node.</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Supported platforms</dt>
      <dd class="ml-6 text-muted-foreground">
      <p>The following platforms are supported for this method of adding nodes:</p>
      <ul>
      <li><code>baremetal</code></li>
      <li><code>vsphere</code></li>
      <li><code>nutanix</code></li>
      <li><code>none</code></li>
      </ul>
      </dd>
      <dt class="font-semibold">Supported architectures</dt>
      <dd class="ml-6 text-muted-foreground"><p>The following architecture combinations have been validated to work when adding worker nodes using this process:</p></dd>
      </dl>
      <ul>
      <li><code>amd64</code> worker nodes on <code>amd64</code> or <code>arm64</code> clusters</li>
      <li><code>arm64</code> worker nodes on <code>amd64</code> or <code>arm64</code> clusters</li>
      <li><code>s390x</code> worker nodes on <code>s390x</code> clusters</li>
      <li><code>ppc64le</code> worker nodes on <code>ppc64le</code> clusters</li>
      </ul>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Adding nodes to your cluster</dt>
      <dd class="ml-6 text-muted-foreground"><p>You can add nodes with this method in the following two ways:</p></dd>
      </dl>
      <ul>
      <li>
      Adding one or more nodes using a configuration file.
      <p>You can specify configurations for one or more nodes in the <code>nodes-config.yaml</code> file before running the <code>oc adm node-image create</code> command. This is useful if you want to add more than one node at a time, or if you are specifying complex configurations.</p>
      </li>
      <li>
      Adding a single node using only command flags.
      <p>You can add a node by running the <code>oc adm node-image create</code> command with flags to specify your configurations. This is useful if you want to add only a single node at a time, and have only simple configurations to specify for that node.</p>
      </li>
      </ul>
      <p class="/modules/adding-node-iso-flags.md"></p>
      <p class="/modules/adding-node-iso-configs.md"></p>
      <h2 id="additional-resources_adding-node-iso" class="group scroll-mt-20">Additional resources<a href="#additional-resources_adding-node-iso" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul><li><a href="/installing/installing_bare_metal/ipi/ipi-install-installation-workflow#root-device-hints_ipi-install-installation-workflow">Root device hints</a></li></ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
