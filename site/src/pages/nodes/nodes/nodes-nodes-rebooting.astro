---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: nodes/nodes/nodes-nodes-rebooting.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"nodes-nodes-rebooting","text":"Understanding node rebooting"},{"depth":2,"slug":"nodes-nodes-rebooting-infrastructure_nodes-nodes-rebooting","text":"About rebooting nodes running critical infrastructure"},{"depth":2,"slug":"nodes-nodes-rebooting-affinity_nodes-nodes-rebooting","text":"Rebooting a node using pod anti-affinity"},{"depth":2,"slug":"nodes-nodes-rebooting-router_nodes-nodes-rebooting","text":"Understanding how to reboot nodes running routers"},{"depth":2,"slug":"nodes-nodes-rebooting-gracefully_nodes-nodes-rebooting","text":"Rebooting a node gracefully"},{"depth":2,"slug":"additional-resources_nodes-nodes-rebooting","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/nodes/nodes/nodes-nodes-rebooting";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Understanding node rebooting";
const description = "Review the following information to learn about rebooting a node without causing an outage for applications running on the platform by first evacuating…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["nodes","nodes/nodes"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Understanding node rebooting {#nodes-nodes-rebooting}\n\nReview the following information to learn about rebooting a node without causing an outage for applications running on the platform by first evacuating the pods on the node.\n\nFor pods that are made highly available by the routing tier, nothing else needs to be done. For other pods needing storage, typically databases, it is critical to ensure that they can remain in operation with one pod temporarily going offline. While implementing resiliency for stateful pods is different for each application, in all cases it is important to configure the scheduler to use node anti-affinity to ensure that the pods are properly spread across available nodes.\n\nAnother challenge is how to handle nodes that are running critical infrastructure such as the router or the registry. The same node evacuation process applies, though it is important to understand certain edge cases.\n\n## About rebooting nodes running critical infrastructure {#nodes-nodes-rebooting-infrastructure_nodes-nodes-rebooting}\n\nWhen rebooting nodes that host critical OpenShift Container Platform infrastructure components, such as router pods, registry pods, and monitoring pods, ensure that there are at least three nodes available to run these components.\n\nThe following scenario demonstrates how service interruptions can occur with applications running on OpenShift Container Platform when only two nodes are available:\n\n- Node A is marked unschedulable and all pods are evacuated.\n- The registry pod running on that node is now redeployed on node B. Node B is now running both registry pods.\n- Node B is now marked unschedulable and is evacuated.\n- The service exposing the two pod endpoints on node B loses all endpoints, for a brief period of time, until they are redeployed to node A.\n\nWhen using three nodes for infrastructure components, this process does not result in a service disruption. However, due to pod scheduling, the last node that is evacuated and brought back into rotation does not have a registry pod. One of the other nodes has two registry pods. To schedule the third registry pod on the last node, use pod anti-affinity to prevent the scheduler from locating two registry pods on the same node.\n\n## Rebooting a node using pod anti-affinity {#nodes-nodes-rebooting-affinity_nodes-nodes-rebooting}\n\nYou can use pod anti-affinity to spread the workloads on a node to other nodes before performing a graceful node restart.\n\nPod anti-affinity is slightly different from node anti-affinity. Node anti-affinity can be violated if there are no other suitable locations to deploy a pod. Pod anti-affinity can be set to either required or preferred.\n\nWith this in place, if only two infrastructure nodes are available and one is rebooted, the container image registry pod is prevented from running on the other node. `**oc get pods**` reports the pod as unready until a suitable node is available. Once a node is available and all pods are back in ready state, the next node can be restarted.\n\nThe following procedure demonstrates how to reboot a node by using pod anti-affinity.\n\n**Procedure**\n\n1. Edit the node specification to configure pod anti-affinity:\n\n   ```yaml\n   apiVersion: v1\n   kind: Pod\n   metadata:\n     name: with-pod-antiaffinity\n   spec:\n     affinity:\n       podAntiAffinity:\n         preferredDuringSchedulingIgnoredDuringExecution:\n         - weight: 100\n           podAffinityTerm:\n             labelSelector:\n               matchExpressions:\n               - key: registry\n                 operator: In\n                 values:\n                 - default\n             topologyKey: kubernetes.io/hostname\n   #...\n   ```\n\n   where:\n\n   `spec.affinity.podAntiAffinity`\n   :   Specifies the stanza to configure pod anti-affinity.\n\n   `spec.affinity.podAntiAffinity.preferredDuringSchedulingIgnoredDuringExecution`\n   :   Specifies a preferred rule.\n\n   `spec.affinity.podAntiAffinity.preferredDuringSchedulingIgnoredDuringExecution.weight`\n   :   Specifies a weight for a preferred rule. The node with the highest weight is preferred.\n\n   `spec.affinity.podAntiAffinity.preferredDuringSchedulingIgnoredDuringExecution.podAffinityTerm.labelSelector.matchExpressions.key`\n   :   Specifies a pod label that determines when the anti-affinity rule applies. Define a key and value for the label.\n\n   `spec.affinity.podAntiAffinity.preferredDuringSchedulingIgnoredDuringExecution.podAffinityTerm.labelSelector.matchExpressions.operator`\n   :   Specifies the relationship between the label on the existing pod and the set of values in the `matchExpression` parameters in the specification for the new pod. Can be `In`, `NotIn`, `Exists`, or `DoesNotExist`. This example assumes the container image registry pod has a label of `registry=default`. Pod anti-affinity can use any Kubernetes match expression.\n2. Enable the `MatchInterPodAffinity` scheduler predicate in the scheduling policy file.\n3. Perform a graceful restart of the node.\n\n## Understanding how to reboot nodes running routers {#nodes-nodes-rebooting-router_nodes-nodes-rebooting}\n\nReview the following information to learn how to reboot a node that hosts a router pod.\n\nIn most cases, a pod running an OpenShift Container Platform router exposes a host port.\n\nThe `PodFitsPorts` scheduler predicate ensures that no router pods using the same port can run on the same node, and pod anti-affinity is achieved. If the routers are relying on IP failover for high availability, there is nothing else that is needed.\n\nFor router pods relying on an external service such as AWS Elastic Load Balancing for high availability, it is that service’s responsibility to react to router pod restarts.\n\nIn rare cases, a router pod may not have a host port configured. In those cases, it is important to follow the recommended restart process for infrastructure nodes.\n\n## Rebooting a node gracefully {#nodes-nodes-rebooting-gracefully_nodes-nodes-rebooting}\n\nYou can perform a graceful restart of a node, where all workloads are moved to other nodes, without data loss or service disruption.\n\nBefore rebooting a node, it is recommended to backup etcd data to avoid any data loss on the node.\n\n> [!NOTE]\n> For single-node OpenShift clusters that require users to perform the `oc login` command rather than having the certificates in `kubeconfig` file to manage the cluster, the `oc adm` commands might not be available after cordoning and draining the node. This is because the `openshift-oauth-apiserver` pod is not running due to the cordon. You can use SSH to access the nodes as indicated in the following procedure.\n>\n> In a single-node OpenShift cluster, pods cannot be rescheduled when cordoning and draining. However, doing so gives the pods, especially your workload pods, time to properly stop and release associated resources.\n\nThe following procedure demonstrates how to perform a graceful restart of a node.\n\n**Procedure**\n\n1. Mark the node as unschedulable:\n\n   ```terminal\n   $ oc adm cordon <node1>\n   ```\n2. Drain the node to remove all the running pods:\n\n   ```terminal\n   $ oc adm drain <node1> --ignore-daemonsets --delete-emptydir-data --force\n   ```\n\n   You might receive errors that pods associated with custom pod disruption budgets (PDB) cannot be evicted.\n\n   ```terminal {title=\"Example error\"}\n   error when evicting pods/\"rails-postgresql-example-1-72v2w\" -n \"rails\" (will retry after 5s): Cannot evict pod as it would violate the pod's disruption budget.\n   ```\n\n   In this case, run the drain command again, adding the `disable-eviction` flag, which bypasses the PDB checks:\n\n   ```terminal\n   $ oc adm drain <node1> --ignore-daemonsets --delete-emptydir-data --force --disable-eviction\n   ```\n3. Access the node in debug mode:\n\n   ```terminal\n   $ oc debug node/<node1>\n   ```\n4. Change your root directory to `/host`:\n\n   ```terminal\n   $ chroot /host\n   ```\n5. Restart the node:\n\n   ```terminal\n   $ systemctl reboot\n   ```\n\n   In a moment, the node enters the `NotReady` state.\n\n   > [!NOTE]\n   > With some single-node OpenShift clusters, the `oc` commands might not be available after you cordon and drain the node because the `openshift-oauth-apiserver` pod is not running. You can use SSH to connect to the node and perform the reboot.\n   >\n   > ```terminal\n   > $ ssh core@<master-node>.<cluster_name>.<base_domain>\n   > ```\n   >\n   > ```terminal\n   > $ sudo systemctl reboot\n   > ```\n6. After the reboot is complete, mark the node as schedulable by running the following command:\n\n   ```terminal\n   $ oc adm uncordon <node1>\n   ```\n\n   > [!NOTE]\n   > With some single-node OpenShift clusters, the `oc` commands might not be available after you cordon and drain the node because the `openshift-oauth-apiserver` pod is not running. You can use SSH to connect to the node and uncordon it.\n   >\n   > ```terminal\n   > $ ssh core@<target_node>\n   > ```\n   >\n   > ```terminal\n   > $ sudo oc adm uncordon <node> --kubeconfig /etc/kubernetes/static-pod-resources/kube-apiserver-certs/secrets/node-kubeconfigs/localhost.kubeconfig\n   > ```\n7. Verify that the node is ready:\n\n   ```terminal\n   $ oc get node <node1>\n   ```\n\n   ```terminal {title=\"Example output\"}\n   NAME    STATUS  ROLES    AGE     VERSION\n   <node1> Ready   worker   6d22h   v1.18.3+b0068a8\n   ```\n\n## Additional resources {#additional-resources_nodes-nodes-rebooting}\n\n- [Placing pods relative to other pods using affinity and anti-affinity rules](/openshift-docs-markdown/nodes/scheduling/nodes-scheduler-pod-affinity#nodes-scheduler-pod-affinity)\n- [Backing up etcd data](/openshift-docs-markdown/backup_and_restore/control_plane_backup_and_restore/backing-up-etcd#backup-etcd)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/nodes/nodes/nodes-nodes-rebooting.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="nodes-nodes-rebooting" class="group scroll-mt-20">Understanding node rebooting<a href="#nodes-nodes-rebooting" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Review the following information to learn about rebooting a node without causing an outage for applications running on the platform by first evacuating the pods on the node.</p>
      <p>For pods that are made highly available by the routing tier, nothing else needs to be done. For other pods needing storage, typically databases, it is critical to ensure that they can remain in operation with one pod temporarily going offline. While implementing resiliency for stateful pods is different for each application, in all cases it is important to configure the scheduler to use node anti-affinity to ensure that the pods are properly spread across available nodes.</p>
      <p>Another challenge is how to handle nodes that are running critical infrastructure such as the router or the registry. The same node evacuation process applies, though it is important to understand certain edge cases.</p>
      <h2 id="nodes-nodes-rebooting-infrastructure_nodes-nodes-rebooting" class="group scroll-mt-20">About rebooting nodes running critical infrastructure<a href="#nodes-nodes-rebooting-infrastructure_nodes-nodes-rebooting" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">When rebooting nodes that host critical OpenShift Container Platform infrastructure components, such as router pods, registry pods, and monitoring pods, ensure that there are at least three nodes available to run these components.</p>
      <p>The following scenario demonstrates how service interruptions can occur with applications running on OpenShift Container Platform when only two nodes are available:</p>
      <ul>
      <li>Node A is marked unschedulable and all pods are evacuated.</li>
      <li>The registry pod running on that node is now redeployed on node B. Node B is now running both registry pods.</li>
      <li>Node B is now marked unschedulable and is evacuated.</li>
      <li>The service exposing the two pod endpoints on node B loses all endpoints, for a brief period of time, until they are redeployed to node A.</li>
      </ul>
      <p>When using three nodes for infrastructure components, this process does not result in a service disruption. However, due to pod scheduling, the last node that is evacuated and brought back into rotation does not have a registry pod. One of the other nodes has two registry pods. To schedule the third registry pod on the last node, use pod anti-affinity to prevent the scheduler from locating two registry pods on the same node.</p>
      <h2 id="nodes-nodes-rebooting-affinity_nodes-nodes-rebooting" class="group scroll-mt-20">Rebooting a node using pod anti-affinity<a href="#nodes-nodes-rebooting-affinity_nodes-nodes-rebooting" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can use pod anti-affinity to spread the workloads on a node to other nodes before performing a graceful node restart.</p>
      <p>Pod anti-affinity is slightly different from node anti-affinity. Node anti-affinity can be violated if there are no other suitable locations to deploy a pod. Pod anti-affinity can be set to either required or preferred.</p>
      <p>With this in place, if only two infrastructure nodes are available and one is rebooted, the container image registry pod is prevented from running on the other node. <code>**oc get pods**</code> reports the pod as unready until a suitable node is available. Once a node is available and all pods are back in ready state, the next node can be restarted.</p>
      <p>The following procedure demonstrates how to reboot a node by using pod anti-affinity.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Edit the node specification to configure pod anti-affinity:
      <CodeBlock code={"apiVersion: v1\nkind: Pod\nmetadata:\n  name: with-pod-antiaffinity\nspec:\n  affinity:\n    podAntiAffinity:\n      preferredDuringSchedulingIgnoredDuringExecution:\n      - weight: 100\n        podAffinityTerm:\n          labelSelector:\n            matchExpressions:\n            - key: registry\n              operator: In\n              values:\n              - default\n          topologyKey: kubernetes.io/hostname\n#..."} lang="yaml" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.affinity.podAntiAffinity</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the stanza to configure pod anti-affinity.</p></dd>
      <dt class="font-semibold"><code>spec.affinity.podAntiAffinity.preferredDuringSchedulingIgnoredDuringExecution</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a preferred rule.</p></dd>
      <dt class="font-semibold"><code>spec.affinity.podAntiAffinity.preferredDuringSchedulingIgnoredDuringExecution.weight</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a weight for a preferred rule. The node with the highest weight is preferred.</p></dd>
      <dt class="font-semibold"><code>spec.affinity.podAntiAffinity.preferredDuringSchedulingIgnoredDuringExecution.podAffinityTerm.labelSelector.matchExpressions.key</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies a pod label that determines when the anti-affinity rule applies. Define a key and value for the label.</p></dd>
      <dt class="font-semibold"><code>spec.affinity.podAntiAffinity.preferredDuringSchedulingIgnoredDuringExecution.podAffinityTerm.labelSelector.matchExpressions.operator</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the relationship between the label on the existing pod and the set of values in the <code>matchExpression</code> parameters in the specification for the new pod. Can be <code>In</code>, <code>NotIn</code>, <code>Exists</code>, or <code>DoesNotExist</code>. This example assumes the container image registry pod has a label of <code>registry=default</code>. Pod anti-affinity can use any Kubernetes match expression.</p></dd>
      </dl>
      </li>
      <li>Enable the <code>MatchInterPodAffinity</code> scheduler predicate in the scheduling policy file.</li>
      <li>Perform a graceful restart of the node.</li>
      </ol>
      <h2 id="nodes-nodes-rebooting-router_nodes-nodes-rebooting" class="group scroll-mt-20">Understanding how to reboot nodes running routers<a href="#nodes-nodes-rebooting-router_nodes-nodes-rebooting" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">Review the following information to learn how to reboot a node that hosts a router pod. </p>
      <p>In most cases, a pod running an OpenShift Container Platform router exposes a host port.</p>
      <p>The <code>PodFitsPorts</code> scheduler predicate ensures that no router pods using the same port can run on the same node, and pod anti-affinity is achieved. If the routers are relying on IP failover for high availability, there is nothing else that is needed.</p>
      <p>For router pods relying on an external service such as AWS Elastic Load Balancing for high availability, it is that service’s responsibility to react to router pod restarts.</p>
      <p>In rare cases, a router pod may not have a host port configured. In those cases, it is important to follow the recommended restart process for infrastructure nodes.</p>
      <h2 id="nodes-nodes-rebooting-gracefully_nodes-nodes-rebooting" class="group scroll-mt-20">Rebooting a node gracefully<a href="#nodes-nodes-rebooting-gracefully_nodes-nodes-rebooting" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can perform a graceful restart of a node, where all workloads are moved to other nodes, without data loss or service disruption. </p>
      <p>Before rebooting a node, it is recommended to backup etcd data to avoid any data loss on the node.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>For single-node OpenShift clusters that require users to perform the <code>oc login</code> command rather than having the certificates in <code>kubeconfig</code> file to manage the cluster, the <code>oc adm</code> commands might not be available after cordoning and draining the node. This is because the <code>openshift-oauth-apiserver</code> pod is not running due to the cordon. You can use SSH to access the nodes as indicated in the following procedure.</p>
          <p>In a single-node OpenShift cluster, pods cannot be rescheduled when cordoning and draining. However, doing so gives the pods, especially your workload pods, time to properly stop and release associated resources.</p>
        </AlertDescription>
      </Alert>
      <p>The following procedure demonstrates how to perform a graceful restart of a node.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Mark the node as unschedulable:
      <CodeBlock code={"$ oc adm cordon <node1>"} lang="terminal" />
      </li>
      <li>
      Drain the node to remove all the running pods:
      <CodeBlock code={"$ oc adm drain <node1> --ignore-daemonsets --delete-emptydir-data --force"} lang="terminal" />
      <p>You might receive errors that pods associated with custom pod disruption budgets (PDB) cannot be evicted.</p>
      <CodeBlock code={"error when evicting pods/\"rails-postgresql-example-1-72v2w\" -n \"rails\" (will retry after 5s): Cannot evict pod as it would violate the pod's disruption budget."} lang="terminal" title="Example error" />
      <p>In this case, run the drain command again, adding the <code>disable-eviction</code> flag, which bypasses the PDB checks:</p>
      <CodeBlock code={"$ oc adm drain <node1> --ignore-daemonsets --delete-emptydir-data --force --disable-eviction"} lang="terminal" />
      </li>
      <li>
      Access the node in debug mode:
      <CodeBlock code={"$ oc debug node/<node1>"} lang="terminal" />
      </li>
      <li>
      Change your root directory to <code>/host</code>:
      <CodeBlock code={"$ chroot /host"} lang="terminal" />
      </li>
      <li>
      Restart the node:
      <CodeBlock code={"$ systemctl reboot"} lang="terminal" />
      <p>In a moment, the node enters the <code>NotReady</code> state.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>With some single-node OpenShift clusters, the <code>oc</code> commands might not be available after you cordon and drain the node because the <code>openshift-oauth-apiserver</code> pod is not running. You can use SSH to connect to the node and perform the reboot.</p>
          <CodeBlock code={"$ ssh core@<master-node>.<cluster_name>.<base_domain>"} lang="terminal" />
          <CodeBlock code={"$ sudo systemctl reboot"} lang="terminal" />
        </AlertDescription>
      </Alert>
      </li>
      <li>
      After the reboot is complete, mark the node as schedulable by running the following command:
      <CodeBlock code={"$ oc adm uncordon <node1>"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>With some single-node OpenShift clusters, the <code>oc</code> commands might not be available after you cordon and drain the node because the <code>openshift-oauth-apiserver</code> pod is not running. You can use SSH to connect to the node and uncordon it.</p>
          <CodeBlock code={"$ ssh core@<target_node>"} lang="terminal" />
          <CodeBlock code={"$ sudo oc adm uncordon <node> --kubeconfig /etc/kubernetes/static-pod-resources/kube-apiserver-certs/secrets/node-kubeconfigs/localhost.kubeconfig"} lang="terminal" />
        </AlertDescription>
      </Alert>
      </li>
      <li>
      Verify that the node is ready:
      <CodeBlock code={"$ oc get node <node1>"} lang="terminal" />
      <CodeBlock code={"NAME    STATUS  ROLES    AGE     VERSION\n<node1> Ready   worker   6d22h   v1.18.3+b0068a8"} lang="terminal" title="Example output" />
      </li>
      </ol>
      <h2 id="additional-resources_nodes-nodes-rebooting" class="group scroll-mt-20">Additional resources<a href="#additional-resources_nodes-nodes-rebooting" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/nodes/scheduling/nodes-scheduler-pod-affinity#nodes-scheduler-pod-affinity">Placing pods relative to other pods using affinity and anti-affinity rules</a></li>
      <li><a href="/openshift-docs-markdown/backup_and_restore/control_plane_backup_and_restore/backing-up-etcd#backup-etcd">Backing up etcd data</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
