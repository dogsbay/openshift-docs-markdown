---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: nodes/nodes/nodes-nodes-working.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"nodes-nodes-working","text":"Working with nodes"},{"depth":2,"slug":"nodes-nodes-working-evacuating_nodes-nodes-working","text":"Evacuating pods on nodes"},{"depth":2,"slug":"nodes-nodes-working-updating_nodes-nodes-working","text":"Understanding how to update labels on nodes"},{"depth":2,"slug":"nodes-nodes-working-marking_nodes-nodes-working","text":"Understanding how to mark nodes as unschedulable or schedulable"},{"depth":2,"slug":"sno-clusters-reboot-without-drain_nodes-nodes-working","text":"Handling errors in single-node OpenShift clusters when the node reboots without draining application pods"},{"depth":2,"slug":"nodes-nodes-working-deleting_nodes-nodes-working","text":"Deleting nodes from a cluster"},{"depth":3,"slug":"nodes-nodes-working-deleting-bare-metal_nodes-nodes-working","text":"Deleting nodes from a bare metal cluster"},{"depth":2,"slug":"additional-resources_nodes-nodes-working","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/nodes/nodes/nodes-nodes-working";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Working with nodes";
const description = "As an administrator, you can perform several tasks to make your clusters more efficient." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["nodes","nodes/nodes"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Working with nodes {#nodes-nodes-working}\n\nAs an administrator, you can perform several tasks to make your clusters more efficient.\n\n## Evacuating pods on nodes {#nodes-nodes-working-evacuating_nodes-nodes-working}\n\nYou can remove, or evacuate, pods from a given node or nodes. Evacuating pods allows you to migrate all or selected pods to other nodes.\n\nYou can evacuate only pods that are backed by a replication controller. The replication controller creates new pods on other nodes and removes the existing pods from the specified node(s).\n\nBare pods, meaning those not backed by a replication controller, are unaffected by default. You can evacuate a subset of pods by specifying a pod selector. Because pod selectors are based on labels, all of the pods with the specified label are evacuated.\n\n**Procedure**\n\n1. Mark the nodes as unschedulable before performing the pod evacuation.\n\n   1. Mark the node as unschedulable by running the following command:\n\n      ```terminal\n      $ oc adm cordon <node1>\n      ```\n\n      ```terminal {title=\"Example output\"}\n      node/<node1> cordoned\n      ```\n   2. Check that the node status is `Ready,SchedulingDisabled` by running the following command:\n\n      ```terminal\n      $ oc get node <node1>\n      ```\n\n      ```terminal {title=\"Example output\"}\n      NAME        STATUS                     ROLES     AGE       VERSION\n      <node1>     Ready,SchedulingDisabled   worker    1d        v1.35.4\n      ```\n2. Evacuate the pods by using one of the following methods:\n\n   - Evacuate all or selected pods on one or more nodes by running the `oc adm drain` command:\n\n     ```terminal\n     $ oc adm drain <node1> <node2> [--pod-selector=<pod_selector>]\n     ```\n   - Force the deletion of bare pods by using the `--force` option with the `oc adm drain` command. When set to `true`, deletion continues even if there are pods not managed by a replication controller, replica set, job, daemon set, or stateful set.\n\n     ```terminal\n     $ oc adm drain <node1> <node2> --force=true\n     ```\n   - Set a period of time in seconds for each pod to terminate gracefully by using the `--grace-period` option with the `oc adm drain` command. If negative, the default value specified in the pod will be used:\n\n     ```terminal\n     $ oc adm drain <node1> <node2> --grace-period=-1\n     ```\n   - Ignore pods managed by daemon sets by using the `--ignore-daemonsets=true` option with the `oc adm drain` command:\n\n     ```terminal\n     $ oc adm drain <node1> <node2> --ignore-daemonsets=true\n     ```\n   - Set the length of time to wait before giving up using the `--timeout`  option with the `oc adm drain` command. A value of `0` sets an infinite length of time.\n\n     ```terminal\n     $ oc adm drain <node1> <node2> --timeout=5s\n     ```\n   - Delete pods even if there are pods using `emptyDir` volumes by setting the `--delete-emptydir-data=true` option with the `oc adm drain` command. Local data is deleted when the node is drained.\n\n     ```terminal\n     $ oc adm drain <node1> <node2> --delete-emptydir-data=true\n     ```\n   - List objects that would be migrated without actually performing the evacuation, by using the `--dry-run=true` option with the `oc adm drain` command:\n\n     ```terminal\n     $ oc adm drain <node1> <node2>  --dry-run=true\n     ```\n\n     Instead of specifying specific node names (for example, `<node1> <node2>`), you can use the `--selector=<node_selector>` option with the `oc adm drain` command to evacuate pods on selected nodes.\n3. Mark the node as schedulable when done by using the following command.\n\n   ```terminal\n   $ oc adm uncordon <node1>\n   ```\n\n## Understanding how to update labels on nodes {#nodes-nodes-working-updating_nodes-nodes-working}\n\nYou can update any label on a node in order to adapt your cluster to evolving needs.\n\nNode labels are not persisted after a node is deleted even if the node is backed up by a Machine.\n\n> [!NOTE]\n> Any change to a `MachineSet` object is not applied to existing machines owned by the compute machine set. For example, labels edited or added to an existing `MachineSet` object are not propagated to existing machines and nodes associated with the compute machine set.\n\n- The following command adds or updates labels on a node:\n\n  ```terminal\n  $ oc label node <node> <key_1>=<value_1> ... <key_n>=<value_n>\n  ```\n\n  For example:\n\n  ```terminal\n  $ oc label nodes webconsole-7f7f6 unhealthy=true\n  ```\n\n  > [!TIP]\n  > You can alternatively apply the following YAML to apply the label:\n  >\n  > ```yaml\n  > kind: Node\n  > apiVersion: v1\n  > metadata:\n  >   name: webconsole-7f7f6\n  >   labels:\n  >     unhealthy: 'true'\n  > #...\n  > ```\n- The following command updates all pods in the namespace:\n\n  ```terminal\n  $ oc label pods --all <key_1>=<value_1>\n  ```\n\n  For example:\n\n  ```terminal\n  $ oc label pods --all status=unhealthy\n  ```\n\n> [!IMPORTANT]\n> In OpenShift Container Platform 4.12 and later, newly installed clusters include both the `node-role.kubernetes.io/control-plane` and `node-role.kubernetes.io/master` labels on control plane nodes by default.\n>\n> In OpenShift Container Platform versions earlier than 4.12, the `node-role.kubernetes.io/control-plane` label is not added by default. Therefore, you must manually add the `node-role.kubernetes.io/control-plane` label to control plane nodes in clusters upgraded from earlier versions.\n\n## Understanding how to mark nodes as unschedulable or schedulable {#nodes-nodes-working-marking_nodes-nodes-working}\n\nYou can mark a node as unschedulable in order to block any new pods from being scheduled on the node.\n\nWhen you mark a node as unschedulable, existing pods on the node are not affected.\n\nBy default, healthy nodes with a `Ready` status are marked as schedulable, which means that you can place new pods on the node.\n\n- The following command marks a node or nodes as unschedulable:\n\n  ```terminal {title=\"Example output\"}\n  $ oc adm cordon <node>\n  ```\n\n  For example:\n\n  ```terminal\n  $ oc adm cordon node1.example.com\n  ```\n\n  ```terminal {title=\"Example output\"}\n  node/node1.example.com cordoned\n\n  NAME                 LABELS                                        STATUS\n  node1.example.com    kubernetes.io/hostname=node1.example.com      Ready,SchedulingDisabled\n  ```\n- The following command marks a currently unschedulable node or nodes as schedulable:\n\n  ```terminal\n  $ oc adm uncordon <node1>\n  ```\n\n  Instead of specifying specific node names (for example, `<node>`), you can use the `--selector=<node_selector>` option to mark selected nodes as schedulable or unschedulable.\n\n## Handling errors in single-node OpenShift clusters when the node reboots without draining application pods {#sno-clusters-reboot-without-drain_nodes-nodes-working}\n\nYou can remove failed pods from a node by using the `--field-selector status.phase=Failed` flag with the `oc delete pods` command.\n\nIn single-node OpenShift clusters and in OpenShift Container Platform clusters in general, a situation can arise where a node reboot occurs without first draining the node. This can occur where an application pod requesting devices fails with the `UnexpectedAdmissionError` error. `Deployment`, `ReplicaSet`, or `DaemonSet` errors are reported because the application pods that require those devices start before the pod serving those devices. You cannot control the order of pod restarts.\n\nWhile this behavior is to be expected, it can cause a pod to remain on the cluster even though it has failed to deploy successfully. The pod continues to report `UnexpectedAdmissionError`. This issue is mitigated by the fact that application pods are typically included in a `Deployment`, `ReplicaSet`, or `DaemonSet`. If a pod is in this error state, it is of little concern because another instance should be running. Belonging to a `Deployment`, `ReplicaSet`, or `DaemonSet` guarantees the successful creation and execution of subsequent pods and ensures the successful deployment of the application.\n\nThere is ongoing work upstream to ensure that such pods are gracefully terminated. Until that work is resolved, run the following command for a single-node OpenShift cluster to remove the failed pods:\n\n```terminal\n$ oc delete pods --field-selector status.phase=Failed -n _<POD_NAMESPACE>_\n```\n\n> [!NOTE]\n> The option to drain the node is unavailable for single-node OpenShift clusters.\n\n## Deleting nodes from a cluster {#nodes-nodes-working-deleting_nodes-nodes-working}\n\nYou can delete a node from a OpenShift Container Platform cluster by scaling down the appropriate `MachineSet` object.\n\n> [!IMPORTANT]\n> When a cluster is integrated with a cloud provider, you must delete the corresponding machine to delete a node. Do not try to use the `oc delete node` command for this task.\n\nWhen you delete a node by using the CLI, the node object is deleted in Kubernetes, but the pods that exist on the node are not deleted. Any bare pods that are not backed by a replication controller become inaccessible to OpenShift Container Platform. Pods backed by replication controllers are rescheduled to other available nodes. You must delete local manifest pods.\n\n> [!NOTE]\n> If you are running cluster on bare metal, you cannot delete a node by editing `MachineSet` objects. Compute machine sets are only available when a cluster is integrated with a cloud provider. Instead you must unschedule and drain the node before manually deleting it.\n\n**Procedure**\n\n1. View the compute machine sets that are in the cluster by running the following command:\n\n   ```terminal\n   $ oc get machinesets -n openshift-machine-api\n   ```\n\n   The compute machine sets are listed in the form of `<cluster-id>-worker-<aws-region-az>`.\n2. Scale down the compute machine set by using one of the following methods:\n\n   - Specify the number of replicas to scale down to by running the following command:\n\n     ```terminal\n     $ oc scale --replicas=2 machineset <machine-set-name> -n openshift-machine-api\n     ```\n   - Edit the compute machine set custom resource by running the following command:\n\n     ```terminal\n     $ oc edit machineset <machine-set-name> -n openshift-machine-api\n     ```\n\n     ```yaml {title=\"Example output\"}\n     apiVersion: machine.openshift.io/v1beta1\n     kind: MachineSet\n     metadata:\n       # ...\n       name: <machine-set-name>\n       namespace: openshift-machine-api\n       # ...\n     spec:\n       replicas: 2\n       # ...\n     ```\n\n     where:\n\n     `spec.replicas`\n     :   Specifies the number of replicas to scale down to.\n\n### Deleting nodes from a bare metal cluster {#nodes-nodes-working-deleting-bare-metal_nodes-nodes-working}\n\nYou can delete a node from a OpenShift Container Platform cluster that does not use machine sets by using the `oc delete node` command and decommissioning the node.\n\nWhen you delete a node using the CLI, the node object is deleted in Kubernetes, but the pods that exist on the node are not deleted. Any bare pods not backed by a replication controller become inaccessible to OpenShift Container Platform. Pods backed by replication controllers are rescheduled to other available nodes. You must delete local manifest pods.\n\nThe following procedure deletes a node from an OpenShift Container Platform cluster running on bare metal.\n\n**Procedure**\n\n1. Mark the node as unschedulable:\n\n   ```terminal\n   $ oc adm cordon <node_name>\n   ```\n2. Drain all pods on the node:\n\n   ```terminal\n   $ oc adm drain <node_name> --force=true\n   ```\n\n   This step might fail if the node is offline or unresponsive. Even if the node does not respond, the node might still be running a workload that writes to shared storage. To avoid data corruption, power down the physical hardware before you proceed.\n3. Delete the node from the cluster:\n\n   ```terminal\n   $ oc delete node <node_name>\n   ```\n\n   Although the node object is now deleted from the cluster, it can still rejoin the cluster after reboot or if the kubelet service is restarted. To permanently delete the node and all its data, you must [decommission the node](https://access.redhat.com/solutions/84663).\n4. If you powered down the physical hardware, turn it back on so that the node can rejoin the cluster.\n\n## Additional resources {#additional-resources_nodes-nodes-working}\n\n- [Evacuating pods on nodes](/openshift-docs-markdown/nodes/nodes/nodes-nodes-working#nodes-nodes-working-evacuating_nodes-nodes-working)\n- [Manually scaling a compute machine set](/openshift-docs-markdown/machine_management/manually-scaling-machineset#machineset-manually-scaling-manually-scaling-machineset)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/nodes/nodes/nodes-nodes-working.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="nodes-nodes-working" class="group scroll-mt-20">Working with nodes<a href="#nodes-nodes-working" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>As an administrator, you can perform several tasks to make your clusters more efficient.</p>
      <h2 id="nodes-nodes-working-evacuating_nodes-nodes-working" class="group scroll-mt-20">Evacuating pods on nodes<a href="#nodes-nodes-working-evacuating_nodes-nodes-working" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can remove, or evacuate, pods from a given node or nodes. Evacuating pods allows you to migrate all or selected pods to other nodes.</p>
      <p>You can evacuate only pods that are backed by a replication controller. The replication controller creates new pods on other nodes and removes the existing pods from the specified node(s).</p>
      <p>Bare pods, meaning those not backed by a replication controller, are unaffected by default. You can evacuate a subset of pods by specifying a pod selector. Because pod selectors are based on labels, all of the pods with the specified label are evacuated.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Mark the nodes as unschedulable before performing the pod evacuation.
      <ol>
      <li>
      Mark the node as unschedulable by running the following command:
      <CodeBlock code={"$ oc adm cordon <node1>"} lang="terminal" />
      <CodeBlock code={"node/<node1> cordoned"} lang="terminal" title="Example output" />
      </li>
      <li>
      Check that the node status is <code>Ready,SchedulingDisabled</code> by running the following command:
      <CodeBlock code={"$ oc get node <node1>"} lang="terminal" />
      <CodeBlock code={"NAME        STATUS                     ROLES     AGE       VERSION\n<node1>     Ready,SchedulingDisabled   worker    1d        v1.35.4"} lang="terminal" title="Example output" />
      </li>
      </ol>
      </li>
      <li>
      Evacuate the pods by using one of the following methods:
      <ul>
      <li>
      Evacuate all or selected pods on one or more nodes by running the <code>oc adm drain</code> command:
      <CodeBlock code={"$ oc adm drain <node1> <node2> [--pod-selector=<pod_selector>]"} lang="terminal" />
      </li>
      <li>
      Force the deletion of bare pods by using the <code>--force</code> option with the <code>oc adm drain</code> command. When set to <code>true</code>, deletion continues even if there are pods not managed by a replication controller, replica set, job, daemon set, or stateful set.
      <CodeBlock code={"$ oc adm drain <node1> <node2> --force=true"} lang="terminal" />
      </li>
      <li>
      Set a period of time in seconds for each pod to terminate gracefully by using the <code>--grace-period</code> option with the <code>oc adm drain</code> command. If negative, the default value specified in the pod will be used:
      <CodeBlock code={"$ oc adm drain <node1> <node2> --grace-period=-1"} lang="terminal" />
      </li>
      <li>
      Ignore pods managed by daemon sets by using the <code>--ignore-daemonsets=true</code> option with the <code>oc adm drain</code> command:
      <CodeBlock code={"$ oc adm drain <node1> <node2> --ignore-daemonsets=true"} lang="terminal" />
      </li>
      <li>
      Set the length of time to wait before giving up using the <code>--timeout</code>  option with the <code>oc adm drain</code> command. A value of <code>0</code> sets an infinite length of time.
      <CodeBlock code={"$ oc adm drain <node1> <node2> --timeout=5s"} lang="terminal" />
      </li>
      <li>
      Delete pods even if there are pods using <code>emptyDir</code> volumes by setting the <code>--delete-emptydir-data=true</code> option with the <code>oc adm drain</code> command. Local data is deleted when the node is drained.
      <CodeBlock code={"$ oc adm drain <node1> <node2> --delete-emptydir-data=true"} lang="terminal" />
      </li>
      <li>
      List objects that would be migrated without actually performing the evacuation, by using the <code>--dry-run=true</code> option with the <code>oc adm drain</code> command:
      <CodeBlock code={"$ oc adm drain <node1> <node2>  --dry-run=true"} lang="terminal" />
      <p>Instead of specifying specific node names (for example, <code>&lt;node1&gt; &lt;node2&gt;</code>), you can use the <code>--selector=&lt;node_selector&gt;</code> option with the <code>oc adm drain</code> command to evacuate pods on selected nodes.</p>
      </li>
      </ul>
      </li>
      <li>
      Mark the node as schedulable when done by using the following command.
      <CodeBlock code={"$ oc adm uncordon <node1>"} lang="terminal" />
      </li>
      </ol>
      <h2 id="nodes-nodes-working-updating_nodes-nodes-working" class="group scroll-mt-20">Understanding how to update labels on nodes<a href="#nodes-nodes-working-updating_nodes-nodes-working" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can update any label on a node in order to adapt your cluster to evolving needs.</p>
      <p>Node labels are not persisted after a node is deleted even if the node is backed up by a Machine.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Any change to a <code>MachineSet</code> object is not applied to existing machines owned by the compute machine set. For example, labels edited or added to an existing <code>MachineSet</code> object are not propagated to existing machines and nodes associated with the compute machine set.</p>
        </AlertDescription>
      </Alert>
      <ul>
      <li>
      The following command adds or updates labels on a node:
      <CodeBlock code={"$ oc label node <node> <key_1>=<value_1> ... <key_n>=<value_n>"} lang="terminal" />
      <p>For example:</p>
      <CodeBlock code={"$ oc label nodes webconsole-7f7f6 unhealthy=true"} lang="terminal" />
      <Alert variant="tip" class="my-4">
        <AlertTitle>Tip</AlertTitle>
        <AlertDescription>
          <p>You can alternatively apply the following YAML to apply the label:</p>
          <CodeBlock code={"kind: Node\napiVersion: v1\nmetadata:\n  name: webconsole-7f7f6\n  labels:\n    unhealthy: 'true'\n#..."} lang="yaml" />
        </AlertDescription>
      </Alert>
      </li>
      <li>
      The following command updates all pods in the namespace:
      <CodeBlock code={"$ oc label pods --all <key_1>=<value_1>"} lang="terminal" />
      <p>For example:</p>
      <CodeBlock code={"$ oc label pods --all status=unhealthy"} lang="terminal" />
      </li>
      </ul>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>In OpenShift Container Platform 4.12 and later, newly installed clusters include both the <code>node-role.kubernetes.io/control-plane</code> and <code>node-role.kubernetes.io/master</code> labels on control plane nodes by default.</p>
          <p>In OpenShift Container Platform versions earlier than 4.12, the <code>node-role.kubernetes.io/control-plane</code> label is not added by default. Therefore, you must manually add the <code>node-role.kubernetes.io/control-plane</code> label to control plane nodes in clusters upgraded from earlier versions.</p>
        </AlertDescription>
      </Alert>
      <h2 id="nodes-nodes-working-marking_nodes-nodes-working" class="group scroll-mt-20">Understanding how to mark nodes as unschedulable or schedulable<a href="#nodes-nodes-working-marking_nodes-nodes-working" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can mark a node as unschedulable in order to block any new pods from being scheduled on the node. </p>
      <p>When you mark a node as unschedulable, existing pods on the node are not affected.</p>
      <p>By default, healthy nodes with a <code>Ready</code> status are marked as schedulable, which means that you can place new pods on the node.</p>
      <ul>
      <li>
      The following command marks a node or nodes as unschedulable:
      <CodeBlock code={"$ oc adm cordon <node>"} lang="terminal" title="Example output" />
      <p>For example:</p>
      <CodeBlock code={"$ oc adm cordon node1.example.com"} lang="terminal" />
      <CodeBlock code={"node/node1.example.com cordoned\n\nNAME                 LABELS                                        STATUS\nnode1.example.com    kubernetes.io/hostname=node1.example.com      Ready,SchedulingDisabled"} lang="terminal" title="Example output" />
      </li>
      <li>
      The following command marks a currently unschedulable node or nodes as schedulable:
      <CodeBlock code={"$ oc adm uncordon <node1>"} lang="terminal" />
      <p>Instead of specifying specific node names (for example, <code>&lt;node&gt;</code>), you can use the <code>--selector=&lt;node_selector&gt;</code> option to mark selected nodes as schedulable or unschedulable.</p>
      </li>
      </ul>
      <h2 id="sno-clusters-reboot-without-drain_nodes-nodes-working" class="group scroll-mt-20">Handling errors in single-node OpenShift clusters when the node reboots without draining application pods<a href="#sno-clusters-reboot-without-drain_nodes-nodes-working" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can remove failed pods from a node by using the <code>--field-selector status.phase=Failed</code> flag with the <code>oc delete pods</code> command.</p>
      <p>In single-node OpenShift clusters and in OpenShift Container Platform clusters in general, a situation can arise where a node reboot occurs without first draining the node. This can occur where an application pod requesting devices fails with the <code>UnexpectedAdmissionError</code> error. <code>Deployment</code>, <code>ReplicaSet</code>, or <code>DaemonSet</code> errors are reported because the application pods that require those devices start before the pod serving those devices. You cannot control the order of pod restarts.</p>
      <p>While this behavior is to be expected, it can cause a pod to remain on the cluster even though it has failed to deploy successfully. The pod continues to report <code>UnexpectedAdmissionError</code>. This issue is mitigated by the fact that application pods are typically included in a <code>Deployment</code>, <code>ReplicaSet</code>, or <code>DaemonSet</code>. If a pod is in this error state, it is of little concern because another instance should be running. Belonging to a <code>Deployment</code>, <code>ReplicaSet</code>, or <code>DaemonSet</code> guarantees the successful creation and execution of subsequent pods and ensures the successful deployment of the application.</p>
      <p>There is ongoing work upstream to ensure that such pods are gracefully terminated. Until that work is resolved, run the following command for a single-node OpenShift cluster to remove the failed pods:</p>
      <CodeBlock code={"$ oc delete pods --field-selector status.phase=Failed -n _<POD_NAMESPACE>_"} lang="terminal" />
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The option to drain the node is unavailable for single-node OpenShift clusters.</p>
        </AlertDescription>
      </Alert>
      <h2 id="nodes-nodes-working-deleting_nodes-nodes-working" class="group scroll-mt-20">Deleting nodes from a cluster<a href="#nodes-nodes-working-deleting_nodes-nodes-working" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can delete a node from a OpenShift Container Platform cluster by scaling down the appropriate <code>MachineSet</code> object.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>When a cluster is integrated with a cloud provider, you must delete the corresponding machine to delete a node. Do not try to use the <code>oc delete node</code> command for this task.</p>
        </AlertDescription>
      </Alert>
      <p>When you delete a node by using the CLI, the node object is deleted in Kubernetes, but the pods that exist on the node are not deleted. Any bare pods that are not backed by a replication controller become inaccessible to OpenShift Container Platform. Pods backed by replication controllers are rescheduled to other available nodes. You must delete local manifest pods.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If you are running cluster on bare metal, you cannot delete a node by editing <code>MachineSet</code> objects. Compute machine sets are only available when a cluster is integrated with a cloud provider. Instead you must unschedule and drain the node before manually deleting it.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      View the compute machine sets that are in the cluster by running the following command:
      <CodeBlock code={"$ oc get machinesets -n openshift-machine-api"} lang="terminal" />
      <p>The compute machine sets are listed in the form of <code>&lt;cluster-id&gt;-worker-&lt;aws-region-az&gt;</code>.</p>
      </li>
      <li>
      Scale down the compute machine set by using one of the following methods:
      <ul>
      <li>
      Specify the number of replicas to scale down to by running the following command:
      <CodeBlock code={"$ oc scale --replicas=2 machineset <machine-set-name> -n openshift-machine-api"} lang="terminal" />
      </li>
      <li>
      Edit the compute machine set custom resource by running the following command:
      <CodeBlock code={"$ oc edit machineset <machine-set-name> -n openshift-machine-api"} lang="terminal" />
      <CodeBlock code={"apiVersion: machine.openshift.io/v1beta1\nkind: MachineSet\nmetadata:\n  # ...\n  name: <machine-set-name>\n  namespace: openshift-machine-api\n  # ...\nspec:\n  replicas: 2\n  # ..."} lang="yaml" title="Example output" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>spec.replicas</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Specifies the number of replicas to scale down to.</p></dd>
      </dl>
      </li>
      </ul>
      </li>
      </ol>
      <h3 id="nodes-nodes-working-deleting-bare-metal_nodes-nodes-working" class="group scroll-mt-20">Deleting nodes from a bare metal cluster<a href="#nodes-nodes-working-deleting-bare-metal_nodes-nodes-working" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p class="_abstract">You can delete a node from a OpenShift Container Platform cluster that does not use machine sets by using the <code>oc delete node</code> command and decommissioning the node.</p>
      <p>When you delete a node using the CLI, the node object is deleted in Kubernetes, but the pods that exist on the node are not deleted. Any bare pods not backed by a replication controller become inaccessible to OpenShift Container Platform. Pods backed by replication controllers are rescheduled to other available nodes. You must delete local manifest pods.</p>
      <p>The following procedure deletes a node from an OpenShift Container Platform cluster running on bare metal.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Mark the node as unschedulable:
      <CodeBlock code={"$ oc adm cordon <node_name>"} lang="terminal" />
      </li>
      <li>
      Drain all pods on the node:
      <CodeBlock code={"$ oc adm drain <node_name> --force=true"} lang="terminal" />
      <p>This step might fail if the node is offline or unresponsive. Even if the node does not respond, the node might still be running a workload that writes to shared storage. To avoid data corruption, power down the physical hardware before you proceed.</p>
      </li>
      <li>
      Delete the node from the cluster:
      <CodeBlock code={"$ oc delete node <node_name>"} lang="terminal" />
      <p>Although the node object is now deleted from the cluster, it can still rejoin the cluster after reboot or if the kubelet service is restarted. To permanently delete the node and all its data, you must <a href="https://access.redhat.com/solutions/84663">decommission the node</a>.</p>
      </li>
      <li>If you powered down the physical hardware, turn it back on so that the node can rejoin the cluster.</li>
      </ol>
      <h2 id="additional-resources_nodes-nodes-working" class="group scroll-mt-20">Additional resources<a href="#additional-resources_nodes-nodes-working" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/nodes/nodes/nodes-nodes-working#nodes-nodes-working-evacuating_nodes-nodes-working">Evacuating pods on nodes</a></li>
      <li><a href="/openshift-docs-markdown/machine_management/manually-scaling-machineset#machineset-manually-scaling-manually-scaling-machineset">Manually scaling a compute machine set</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
