---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: nodes/containers/nodes-containers-copying-files.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"nodes-containers-copying-files","text":"Copying files to or from OpenShift Container Platform containers"},{"depth":2,"slug":"nodes-containers-copying-files-about_nodes-containers-copying-files","text":"Understanding how to copy files"},{"depth":2,"slug":"nodes-containers-copying-files-procedure_nodes-containers-copying-files","text":"Copying files to and from containers"},{"depth":2,"slug":"nodes-containers-copying-files-rsync_nodes-containers-copying-files","text":"Using advanced Rsync features"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/nodes/containers/nodes-containers-copying-files";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Copying files to or from OpenShift Container Platform containers";
const description = "You can use the oc rsync command, or remote sync, to copy local files to or from a remote directory in a OpenShift Container Platform container." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["nodes","nodes/containers"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Copying files to or from OpenShift Container Platform containers {#nodes-containers-copying-files}\n\nYou can use the `oc rsync` command, or remote sync, to copy local files to or from a remote directory in a OpenShift Container Platform container.\n\nThe `oc rsync` command leverages the local Rsync tool on the client machine and the remote container for tasks such as copying database archives to and from your pods for backup and restore purposes, or copying source code changes into a running pod for development debugging.\n\n> [!NOTE]\n> If the Rsync tool is not found locally or in the remote container, a **tar** archive is created locally and sent to the container where the **tar** utility is used to extract the files. If **tar** is not available in the remote container, the copy fails.\n\n## Understanding how to copy files {#nodes-containers-copying-files-about_nodes-containers-copying-files}\n\nYou can use the `oc rsync` command, or remote sync, for copying database archives to and from your pods for backup and restore purposes. You can also use `oc rsync` to copy source code changes into a running pod for development debugging, when the running pod supports hot reload of source files.\n\n```terminal\n$ oc rsync <source> <destination> [-c <container>]\n```\n\nWhen using the `oc rysnc` command, note the following requirements.\n\nSpecifying the Copy Source\n:   The source argument of the `oc rsync` command must point to either a local directory or a pod directory. Individual files are not supported.\n\n    When specifying a pod directory, the directory name must be prefixed with the pod name:\n\n    ```terminal\n    <pod name>:<dir>\n    ```\n\n    If the directory name ends in a path separator (`/`), only the contents of the directory are copied to the destination. Otherwise, the directory and its contents are copied to the destination.\n\nSpecifying the Copy Destination\n:   The destination argument of the `oc rsync` command must point to a directory. If the directory does not exist, but `rsync` is used for copy, the directory is created for you.\n\nDeleting Files at the Destination\n:   The `--delete` flag can be used to delete any files in the remote directory that are not in the local directory.\n\nContinuous Syncing on File Change\n:   Using the `--watch` option causes the command to monitor the source path for any file system changes, and synchronizes changes when they occur. With this argument, the command runs continuously.\n\n    Synchronization occurs after short quiet periods to ensure a rapidly changing file system does not result in continuous synchronization calls.\n\n    When using the `--watch` option, the behavior is effectively the same as manually invoking `oc rsync` repeatedly, including any arguments normally passed to `oc rsync`. Therefore, you can control the behavior via the same flags used with manual invocations of `oc rsync`, such as `--delete`.\n\n## Copying files to and from containers {#nodes-containers-copying-files-procedure_nodes-containers-copying-files}\n\nYou can use the `oc rsync` command for copying local files to or from a container.\n\n**Prerequisites**\n\n- rsync must be installed. The `oc rsync` command uses the local `rsync` tool, if present on the client machine and the remote container.\n\n  If `rsync` is not found locally or in the remote container, a **tar** archive is created locally and sent to the container where the **tar** utility is used to extract the files. If **tar** is not available in the remote container, the copy will fail.\n\n  The **tar** copy method does not provide the same functionality as `oc rsync`. For example, `oc rsync` creates the destination directory if it does not exist and only sends files that are different between the source and the destination.\n\n  > [!NOTE]\n  > In Windows, the `cwRsync` client should be installed and added to the PATH for use with the `oc rsync` command.\n\n**Procedure**\n\n- To copy a local directory to a pod directory:\n\n  ```terminal\n  $ oc rsync <local-dir> <pod-name>:/<remote-dir> -c <container-name>\n  ```\n\n  For example:\n\n  ```terminal\n  $ oc rsync /home/user/source devpod1234:/src -c user-container\n  ```\n- To copy a pod directory to a local directory:\n\n  ```terminal\n  $ oc rsync devpod1234:/src /home/user/source\n  ```\n\n  ```terminal {title=\"Example output\"}\n  $ oc rsync devpod1234:/src/status.txt /home/user/\n  ```\n\n## Using advanced Rsync features {#nodes-containers-copying-files-rsync_nodes-containers-copying-files}\n\nYou can use the additional command-line options that are available with the standard `rsync` command if needed.\n\nThe `oc rsync` command exposes fewer command-line options than standard `rsync` command. In the case that you want to use a standard `rsync` command-line option that is not available with the `oc rsync` command, for example the `--exclude-from=FILE` option, it might be possible to use standard `rsync` with the `--rsh` (`-e`) option or `RSYNC_RSH` environment variable as a workaround, as shown in the following examples:\n\n```terminal\n$ rsync --rsh='oc rsh' --exclude-from=<file_name> <local-dir> <pod-name>:/<remote-dir>\n```\n\nor:\n\nExport the `RSYNC_RSH` variable:\n\n```terminal\n$ export RSYNC_RSH='oc rsh'\n```\n\nThen, run the rsync command:\n\n```terminal\n$ rsync --exclude-from=<file_name> <local-dir> <pod-name>:/<remote-dir>\n```\n\nBoth of the above examples configure the standard `rsync` command to use `oc rsh` as its remote shell program to enable it to connect to the remote pod, and are an alternative to running `oc rsync`.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/nodes/containers/nodes-containers-copying-files.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="nodes-containers-copying-files" class="group scroll-mt-20">Copying files to or from OpenShift Container Platform containers<a href="#nodes-containers-copying-files" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can use the <code>oc rsync</code> command, or remote sync, to copy local files to or from a remote directory in a OpenShift Container Platform container.</p>
      <p>The <code>oc rsync</code> command leverages the local Rsync tool on the client machine and the remote container for tasks such as copying database archives to and from your pods for backup and restore purposes, or copying source code changes into a running pod for development debugging.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If the Rsync tool is not found locally or in the remote container, a <strong>tar</strong> archive is created locally and sent to the container where the <strong>tar</strong> utility is used to extract the files. If <strong>tar</strong> is not available in the remote container, the copy fails.</p>
        </AlertDescription>
      </Alert>
      <h2 id="nodes-containers-copying-files-about_nodes-containers-copying-files" class="group scroll-mt-20">Understanding how to copy files<a href="#nodes-containers-copying-files-about_nodes-containers-copying-files" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can use the <code>oc rsync</code> command, or remote sync, for copying database archives to and from your pods for backup and restore purposes. You can also use <code>oc rsync</code> to copy source code changes into a running pod for development debugging, when the running pod supports hot reload of source files.</p>
      <CodeBlock code={"$ oc rsync <source> <destination> [-c <container>]"} lang="terminal" />
      <p>When using the <code>oc rysnc</code> command, note the following requirements.</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">Specifying the Copy Source</dt>
      <dd class="ml-6 text-muted-foreground">
      <p>The source argument of the <code>oc rsync</code> command must point to either a local directory or a pod directory. Individual files are not supported.</p>
      <p>When specifying a pod directory, the directory name must be prefixed with the pod name:</p>
      <CodeBlock code={"<pod name>:<dir>"} lang="terminal" />
      <p>If the directory name ends in a path separator (<code>/</code>), only the contents of the directory are copied to the destination. Otherwise, the directory and its contents are copied to the destination.</p>
      </dd>
      <dt class="font-semibold">Specifying the Copy Destination</dt>
      <dd class="ml-6 text-muted-foreground"><p>The destination argument of the <code>oc rsync</code> command must point to a directory. If the directory does not exist, but <code>rsync</code> is used for copy, the directory is created for you.</p></dd>
      <dt class="font-semibold">Deleting Files at the Destination</dt>
      <dd class="ml-6 text-muted-foreground"><p>The <code>--delete</code> flag can be used to delete any files in the remote directory that are not in the local directory.</p></dd>
      <dt class="font-semibold">Continuous Syncing on File Change</dt>
      <dd class="ml-6 text-muted-foreground">
      <p>Using the <code>--watch</code> option causes the command to monitor the source path for any file system changes, and synchronizes changes when they occur. With this argument, the command runs continuously.</p>
      <p>Synchronization occurs after short quiet periods to ensure a rapidly changing file system does not result in continuous synchronization calls.</p>
      <p>When using the <code>--watch</code> option, the behavior is effectively the same as manually invoking <code>oc rsync</code> repeatedly, including any arguments normally passed to <code>oc rsync</code>. Therefore, you can control the behavior via the same flags used with manual invocations of <code>oc rsync</code>, such as <code>--delete</code>.</p>
      </dd>
      </dl>
      <h2 id="nodes-containers-copying-files-procedure_nodes-containers-copying-files" class="group scroll-mt-20">Copying files to and from containers<a href="#nodes-containers-copying-files-procedure_nodes-containers-copying-files" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can use the <code>oc rsync</code> command for copying local files to or from a container.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>
      rsync must be installed. The <code>oc rsync</code> command uses the local <code>rsync</code> tool, if present on the client machine and the remote container.
      <p>If <code>rsync</code> is not found locally or in the remote container, a <strong>tar</strong> archive is created locally and sent to the container where the <strong>tar</strong> utility is used to extract the files. If <strong>tar</strong> is not available in the remote container, the copy will fail.</p>
      <p>The <strong>tar</strong> copy method does not provide the same functionality as <code>oc rsync</code>. For example, <code>oc rsync</code> creates the destination directory if it does not exist and only sends files that are different between the source and the destination.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>In Windows, the <code>cwRsync</code> client should be installed and added to the PATH for use with the <code>oc rsync</code> command.</p>
        </AlertDescription>
      </Alert>
      </li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      To copy a local directory to a pod directory:
      <CodeBlock code={"$ oc rsync <local-dir> <pod-name>:/<remote-dir> -c <container-name>"} lang="terminal" />
      <p>For example:</p>
      <CodeBlock code={"$ oc rsync /home/user/source devpod1234:/src -c user-container"} lang="terminal" />
      </li>
      <li>
      To copy a pod directory to a local directory:
      <CodeBlock code={"$ oc rsync devpod1234:/src /home/user/source"} lang="terminal" />
      <CodeBlock code={"$ oc rsync devpod1234:/src/status.txt /home/user/"} lang="terminal" title="Example output" />
      </li>
      </ul>
      <h2 id="nodes-containers-copying-files-rsync_nodes-containers-copying-files" class="group scroll-mt-20">Using advanced Rsync features<a href="#nodes-containers-copying-files-rsync_nodes-containers-copying-files" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can use the additional command-line options that are available with the standard <code>rsync</code> command if needed. </p>
      <p>The <code>oc rsync</code> command exposes fewer command-line options than standard <code>rsync</code> command. In the case that you want to use a standard <code>rsync</code> command-line option that is not available with the <code>oc rsync</code> command, for example the <code>--exclude-from=FILE</code> option, it might be possible to use standard <code>rsync</code> with the <code>--rsh</code> (<code>-e</code>) option or <code>RSYNC_RSH</code> environment variable as a workaround, as shown in the following examples:</p>
      <CodeBlock code={"$ rsync --rsh='oc rsh' --exclude-from=<file_name> <local-dir> <pod-name>:/<remote-dir>"} lang="terminal" />
      <p>or:</p>
      <p>Export the <code>RSYNC_RSH</code> variable:</p>
      <CodeBlock code={"$ export RSYNC_RSH='oc rsh'"} lang="terminal" />
      <p>Then, run the rsync command:</p>
      <CodeBlock code={"$ rsync --exclude-from=<file_name> <local-dir> <pod-name>:/<remote-dir>"} lang="terminal" />
      <p>Both of the above examples configure the standard <code>rsync</code> command to use <code>oc rsh</code> as its remote shell program to enable it to connect to the remote pod, and are an alternative to running <code>oc rsync</code>.</p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
