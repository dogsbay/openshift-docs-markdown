---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: nodes/cma/nodes-cma-autoscaling-custom-install.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"nodes-cma-autoscaling-custom-install","text":"Installing the custom metrics autoscaler"},{"depth":2,"slug":"nodes-cma-autoscaling-custom-install_nodes-cma-autoscaling-custom-install","text":"Installing the custom metrics autoscaler"},{"depth":2,"slug":"nodes-cma-autoscaling-keda-controller-edit_nodes-cma-autoscaling-custom-install","text":"Editing the Keda Controller CR"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/nodes/cma/nodes-cma-autoscaling-custom-install";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Installing the custom metrics autoscaler";
const description = "You can use the OpenShift Container Platform web console to install the Custom Metrics Autoscaler Operator." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["nodes","nodes/cma"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Installing the custom metrics autoscaler {#nodes-cma-autoscaling-custom-install}\n\nYou can use the OpenShift Container Platform web console to install the Custom Metrics Autoscaler Operator.\n\nThe installation creates the following five CRDs:\n\n- `ClusterTriggerAuthentication`\n- `KedaController`\n- `ScaledJob`\n- `ScaledObject`\n- `TriggerAuthentication`\n\nThe installation process also creates the `KedaController` custom resource (CR). You can modify the default `KedaController` CR, if needed. For more information, see \"Editing the Keda Controller CR\".\n\n> [!NOTE]\n> If you are installing a Custom Metrics Autoscaler Operator version lower than 2.17.2, you must manually create the Keda Controller CR. You can use the procedure described in \"Editing the Keda Controller CR\" to create the CR.\n\n## Installing the custom metrics autoscaler {#nodes-cma-autoscaling-custom-install_nodes-cma-autoscaling-custom-install}\n\nYou can use the following procedure to install the Custom Metrics Autoscaler Operator.\n\n**Prerequisites**\n\n- Remove any previously-installed Technology Preview versions of the Cluster Metrics Autoscaler Operator.\n- Remove any versions of the community-based KEDA.\n\n  Also, remove the KEDA 1.x custom resource definitions by running the following commands:\n\n  ```terminal\n  $ oc delete crd scaledobjects.keda.k8s.io\n  ```\n\n  ```terminal\n  $ oc delete crd triggerauthentications.keda.k8s.io\n  ```\n- Optional: If you need the Custom Metrics Autoscaler Operator to connect to off-cluster services, such as an external Kafka cluster or an external Prometheus service, put any required service CA certificates into a config map. The config map must exist in the same namespace where the Operator is installed. For example:\n\n  ```terminal\n  $ oc create configmap -n openshift-keda thanos-cert  --from-file=ca-cert.pem\n  ```\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, click **Ecosystem** -> **Software Catalog**.\n2. Choose **Custom Metrics Autoscaler** from the list of available Operators, and click **Install**.\n3. On the **Install Operator** page, ensure that the **All namespaces on the cluster (default)** option is selected for **Installation Mode**. This installs the Operator in all namespaces.\n4. Ensure that the **openshift-keda** namespace is selected for **Installed Namespace**. OpenShift Container Platform creates the namespace, if not present in your cluster.\n5. Click **Install**.\n6. Verify the installation by listing the Custom Metrics Autoscaler Operator components:\n\n   1. Navigate to **Workloads** -> **Pods**.\n   2. Select the `openshift-keda` project from the drop-down menu and verify that the `custom-metrics-autoscaler-operator-*` pod is running.\n   3. Navigate to **Workloads** -> **Deployments** to verify that the `custom-metrics-autoscaler-operator` deployment is running.\n7. Optional: Verify the installation in the OpenShift CLI using the following commands:\n\n   ```terminal\n   $ oc get all -n openshift-keda\n   ```\n\n   The output appears similar to the following:\n\n   ```terminal {title=\"Example output\"}\n   NAME                                                      READY   STATUS    RESTARTS   AGE\n   pod/custom-metrics-autoscaler-operator-5fd8d9ffd8-xt4xp   1/1     Running   0          18m\n\n   NAME                                                 READY   UP-TO-DATE   AVAILABLE   AGE\n   deployment.apps/custom-metrics-autoscaler-operator   1/1     1            1           18m\n\n   NAME                                                            DESIRED   CURRENT   READY   AGE\n   replicaset.apps/custom-metrics-autoscaler-operator-5fd8d9ffd8   1         1         1       18m\n   ```\n\n## Editing the Keda Controller CR {#nodes-cma-autoscaling-keda-controller-edit_nodes-cma-autoscaling-custom-install}\n\nYou can use the following procedure to modify the `KedaController` custom resource (CR), which is automatically installed during the installation of the Custom Metrics Autoscaler Operator.\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, click **Ecosystem** -> **Installed Operators**.\n2. Click **Custom Metrics Autoscaler**.\n3. On the **Operator Details** page, click the **KedaController** tab.\n4. On the **KedaController** tab, click **Create KedaController** and edit the file.\n\n   ```yaml\n   kind: KedaController\n   apiVersion: keda.sh/v1alpha1\n   metadata:\n     name: keda\n     namespace: openshift-keda\n   spec:\n     watchNamespace: '' (1)\n     operator:\n       logLevel: info (2)\n       logEncoder: console (3)\n       caConfigMaps: (4)\n       - thanos-cert\n       - kafka-cert\n       volumeMounts: (5)\n       - mountPath: /<path_to_directory>\n         name: <name>\n       volumes: (6)\n       - name: <volume_name>\n         emptyDir:\n           medium: Memory\n     metricsServer:\n       logLevel: '0' (7)\n       auditConfig: (8)\n         logFormat: \"json\"\n         logOutputVolumeClaim: \"persistentVolumeClaimName\"\n         policy:\n           rules:\n           - level: Metadata\n           omitStages: [\"RequestReceived\"]\n           omitManagedFields: false\n         lifetime:\n           maxAge: \"2\"\n           maxBackup: \"1\"\n           maxSize: \"50\"\n     serviceAccount: {}\n   ```\n\n   1. Specifies a single namespace in which the Custom Metrics Autoscaler Operator scales applications. Leave it blank or leave it empty to scale applications in all namespaces. This field should have a namespace or be empty. The default value is empty.\n   2. Specifies the level of verbosity for the Custom Metrics Autoscaler Operator log messages. The allowed values are `debug`, `info`, `error`. The default is `info`.\n   3. Specifies the logging format for the Custom Metrics Autoscaler Operator log messages. The allowed values are `console` or `json`. The default is `console`.\n   4. Optional: Specifies one or more config maps with CA certificates, which the Custom Metrics Autoscaler Operator can use to connect securely to TLS-enabled metrics sources.\n   5. Optional: Add the container mount path.\n   6. Optional: Add a `volumes` block to list each projected volume source.\n   7. Specifies the logging level for the Custom Metrics Autoscaler Metrics Server. The allowed values are `0` for `info` and `4` for `debug`. The default is `0`.\n   8. Activates audit logging for the Custom Metrics Autoscaler Operator and specifies the audit policy to use, as described in the \"Configuring audit logging\" section.\n5. Click **Save** to save the changes.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/nodes/cma/nodes-cma-autoscaling-custom-install.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="nodes-cma-autoscaling-custom-install" class="group scroll-mt-20">Installing the custom metrics autoscaler<a href="#nodes-cma-autoscaling-custom-install" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can use the OpenShift Container Platform web console to install the Custom Metrics Autoscaler Operator.</p>
      <p>The installation creates the following five CRDs:</p>
      <ul>
      <li><code>ClusterTriggerAuthentication</code></li>
      <li><code>KedaController</code></li>
      <li><code>ScaledJob</code></li>
      <li><code>ScaledObject</code></li>
      <li><code>TriggerAuthentication</code></li>
      </ul>
      <p>The installation process also creates the <code>KedaController</code> custom resource (CR). You can modify the default <code>KedaController</code> CR, if needed. For more information, see "Editing the Keda Controller CR".</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>If you are installing a Custom Metrics Autoscaler Operator version lower than 2.17.2, you must manually create the Keda Controller CR. You can use the procedure described in "Editing the Keda Controller CR" to create the CR.</p>
        </AlertDescription>
      </Alert>
      <h2 id="nodes-cma-autoscaling-custom-install_nodes-cma-autoscaling-custom-install" class="group scroll-mt-20">Installing the custom metrics autoscaler<a href="#nodes-cma-autoscaling-custom-install_nodes-cma-autoscaling-custom-install" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can use the following procedure to install the Custom Metrics Autoscaler Operator.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>Remove any previously-installed Technology Preview versions of the Cluster Metrics Autoscaler Operator.</li>
      <li>
      Remove any versions of the community-based KEDA.
      <p>Also, remove the KEDA 1.x custom resource definitions by running the following commands:</p>
      <CodeBlock code={"$ oc delete crd scaledobjects.keda.k8s.io"} lang="terminal" />
      <CodeBlock code={"$ oc delete crd triggerauthentications.keda.k8s.io"} lang="terminal" />
      </li>
      <li>
      Optional: If you need the Custom Metrics Autoscaler Operator to connect to off-cluster services, such as an external Kafka cluster or an external Prometheus service, put any required service CA certificates into a config map. The config map must exist in the same namespace where the Operator is installed. For example:
      <CodeBlock code={"$ oc create configmap -n openshift-keda thanos-cert  --from-file=ca-cert.pem"} lang="terminal" />
      </li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, click <strong>Ecosystem</strong> -&gt; <strong>Software Catalog</strong>.</li>
      <li>Choose <strong>Custom Metrics Autoscaler</strong> from the list of available Operators, and click <strong>Install</strong>.</li>
      <li>On the <strong>Install Operator</strong> page, ensure that the <strong>All namespaces on the cluster (default)</strong> option is selected for <strong>Installation Mode</strong>. This installs the Operator in all namespaces.</li>
      <li>Ensure that the <strong>openshift-keda</strong> namespace is selected for <strong>Installed Namespace</strong>. OpenShift Container Platform creates the namespace, if not present in your cluster.</li>
      <li>Click <strong>Install</strong>.</li>
      <li>
      Verify the installation by listing the Custom Metrics Autoscaler Operator components:
      <ol>
      <li>Navigate to <strong>Workloads</strong> -&gt; <strong>Pods</strong>.</li>
      <li>Select the <code>openshift-keda</code> project from the drop-down menu and verify that the <code>custom-metrics-autoscaler-operator-*</code> pod is running.</li>
      <li>Navigate to <strong>Workloads</strong> -&gt; <strong>Deployments</strong> to verify that the <code>custom-metrics-autoscaler-operator</code> deployment is running.</li>
      </ol>
      </li>
      <li>
      Optional: Verify the installation in the OpenShift CLI using the following commands:
      <CodeBlock code={"$ oc get all -n openshift-keda"} lang="terminal" />
      <p>The output appears similar to the following:</p>
      <CodeBlock code={"NAME                                                      READY   STATUS    RESTARTS   AGE\npod/custom-metrics-autoscaler-operator-5fd8d9ffd8-xt4xp   1/1     Running   0          18m\n\nNAME                                                 READY   UP-TO-DATE   AVAILABLE   AGE\ndeployment.apps/custom-metrics-autoscaler-operator   1/1     1            1           18m\n\nNAME                                                            DESIRED   CURRENT   READY   AGE\nreplicaset.apps/custom-metrics-autoscaler-operator-5fd8d9ffd8   1         1         1       18m"} lang="terminal" title="Example output" />
      </li>
      </ol>
      <h2 id="nodes-cma-autoscaling-keda-controller-edit_nodes-cma-autoscaling-custom-install" class="group scroll-mt-20">Editing the Keda Controller CR<a href="#nodes-cma-autoscaling-keda-controller-edit_nodes-cma-autoscaling-custom-install" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can use the following procedure to modify the <code>KedaController</code> custom resource (CR), which is automatically installed during the installation of the Custom Metrics Autoscaler Operator.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, click <strong>Ecosystem</strong> -&gt; <strong>Installed Operators</strong>.</li>
      <li>Click <strong>Custom Metrics Autoscaler</strong>.</li>
      <li>On the <strong>Operator Details</strong> page, click the <strong>KedaController</strong> tab.</li>
      <li>
      On the <strong>KedaController</strong> tab, click <strong>Create KedaController</strong> and edit the file.
      <CodeBlock code={"kind: KedaController\napiVersion: keda.sh/v1alpha1\nmetadata:\n  name: keda\n  namespace: openshift-keda\nspec:\n  watchNamespace: '' (1)\n  operator:\n    logLevel: info (2)\n    logEncoder: console (3)\n    caConfigMaps: (4)\n    - thanos-cert\n    - kafka-cert\n    volumeMounts: (5)\n    - mountPath: /<path_to_directory>\n      name: <name>\n    volumes: (6)\n    - name: <volume_name>\n      emptyDir:\n        medium: Memory\n  metricsServer:\n    logLevel: '0' (7)\n    auditConfig: (8)\n      logFormat: \"json\"\n      logOutputVolumeClaim: \"persistentVolumeClaimName\"\n      policy:\n        rules:\n        - level: Metadata\n        omitStages: [\"RequestReceived\"]\n        omitManagedFields: false\n      lifetime:\n        maxAge: \"2\"\n        maxBackup: \"1\"\n        maxSize: \"50\"\n  serviceAccount: {}"} lang="yaml" />
      <ol>
      <li>Specifies a single namespace in which the Custom Metrics Autoscaler Operator scales applications. Leave it blank or leave it empty to scale applications in all namespaces. This field should have a namespace or be empty. The default value is empty.</li>
      <li>Specifies the level of verbosity for the Custom Metrics Autoscaler Operator log messages. The allowed values are <code>debug</code>, <code>info</code>, <code>error</code>. The default is <code>info</code>.</li>
      <li>Specifies the logging format for the Custom Metrics Autoscaler Operator log messages. The allowed values are <code>console</code> or <code>json</code>. The default is <code>console</code>.</li>
      <li>Optional: Specifies one or more config maps with CA certificates, which the Custom Metrics Autoscaler Operator can use to connect securely to TLS-enabled metrics sources.</li>
      <li>Optional: Add the container mount path.</li>
      <li>Optional: Add a <code>volumes</code> block to list each projected volume source.</li>
      <li>Specifies the logging level for the Custom Metrics Autoscaler Metrics Server. The allowed values are <code>0</code> for <code>info</code> and <code>4</code> for <code>debug</code>. The default is <code>0</code>.</li>
      <li>Activates audit logging for the Custom Metrics Autoscaler Operator and specifies the audit policy to use, as described in the "Configuring audit logging" section.</li>
      </ol>
      </li>
      <li>Click <strong>Save</strong> to save the changes.</li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
