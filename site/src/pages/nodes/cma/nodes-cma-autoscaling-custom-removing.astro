---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: nodes/cma/nodes-cma-autoscaling-custom-removing.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"nodes-cma-autoscaling-custom-removing","text":"Removing the Custom Metrics Autoscaler Operator"},{"depth":2,"slug":"nodes-cma-autoscaling-custom-uninstalling_nodes-cma-autoscaling-custom-removing","text":"Uninstalling the Custom Metrics Autoscaler Operator"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/nodes/cma/nodes-cma-autoscaling-custom-removing";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Removing the Custom Metrics Autoscaler Operator";
const description = "You can remove the custom metrics autoscaler from your OpenShift Container Platform cluster. After removing the Custom Metrics Autoscaler Operator, remove…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["nodes","nodes/cma"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Removing the Custom Metrics Autoscaler Operator {#nodes-cma-autoscaling-custom-removing}\n\nYou can remove the custom metrics autoscaler from your OpenShift Container Platform cluster. After removing the Custom Metrics Autoscaler Operator, remove other components associated with the Operator to avoid potential issues.\n\n> [!NOTE]\n> Delete the `KedaController` custom resource (CR) first. If you do not delete the `KedaController` CR, OpenShift Container Platform can hang when you delete the `openshift-keda` project. If you delete the Custom Metrics Autoscaler Operator before deleting the CR, you are not able to delete the CR.\n\n## Uninstalling the Custom Metrics Autoscaler Operator {#nodes-cma-autoscaling-custom-uninstalling_nodes-cma-autoscaling-custom-removing}\n\nUse the following procedure to remove the custom metrics autoscaler from your OpenShift Container Platform cluster.\n\n**Prerequisites**\n\n- The Custom Metrics Autoscaler Operator must be installed.\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, click **Ecosystem** → **Installed Operators**.\n2. Switch to the **openshift-keda** project.\n3. Remove the `KedaController` custom resource.\n\n   1. Find the **CustomMetricsAutoscaler**  Operator and click the **KedaController** tab.\n   2. Find the custom resource, and then click **Delete KedaController**.\n   3. Click **Uninstall**.\n4. Remove the Custom Metrics Autoscaler Operator:\n\n   1. Click **Ecosystem** → **Installed Operators**.\n   2. Find the **CustomMetricsAutoscaler**  Operator and click the Options menu ![](/openshift-docs-markdown/_assets/images/kebab.png \"Options menu\") and select **Uninstall Operator**.\n   3. Click **Uninstall**.\n5. Optional: Use the OpenShift CLI to remove the custom metrics autoscaler components:\n\n   1. Delete the custom metrics autoscaler CRDs:\n\n      - `clustertriggerauthentications.keda.sh`\n      - `kedacontrollers.keda.sh`\n      - `scaledjobs.keda.sh`\n      - `scaledobjects.keda.sh`\n      - `triggerauthentications.keda.sh`\n\n      ```terminal\n      $ oc delete crd clustertriggerauthentications.keda.sh kedacontrollers.keda.sh scaledjobs.keda.sh scaledobjects.keda.sh triggerauthentications.keda.sh\n      ```\n\n      Deleting the CRDs removes the associated roles, cluster roles, and role bindings. However, there might be a few cluster roles that must be manually deleted.\n   2. List any custom metrics autoscaler cluster roles:\n\n      ```terminal\n      $ oc get clusterrole | grep keda.sh\n      ```\n   3. Delete the listed custom metrics autoscaler cluster roles. For example:\n\n      ```terminal\n      $ oc delete clusterrole.keda.sh-v1alpha1-admin\n      ```\n   4. List any custom metrics autoscaler cluster role bindings:\n\n      ```terminal\n      $ oc get clusterrolebinding | grep keda.sh\n      ```\n   5. Delete the listed custom metrics autoscaler cluster role bindings. For example:\n\n      ```terminal\n      $ oc delete clusterrolebinding.keda.sh-v1alpha1-admin\n      ```\n6. Delete the custom metrics autoscaler project:\n\n   ```terminal\n   $ oc delete project openshift-keda\n   ```\n7. Delete the Cluster Metric Autoscaler Operator:\n\n   ```terminal\n   $ oc delete operator/openshift-custom-metrics-autoscaler-operator.openshift-keda\n   ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/nodes/cma/nodes-cma-autoscaling-custom-removing.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="nodes-cma-autoscaling-custom-removing" class="group scroll-mt-20">Removing the Custom Metrics Autoscaler Operator<a href="#nodes-cma-autoscaling-custom-removing" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can remove the custom metrics autoscaler from your OpenShift Container Platform cluster. After removing the Custom Metrics Autoscaler Operator, remove other components associated with the Operator to avoid potential issues.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Delete the <code>KedaController</code> custom resource (CR) first. If you do not delete the <code>KedaController</code> CR, OpenShift Container Platform can hang when you delete the <code>openshift-keda</code> project. If you delete the Custom Metrics Autoscaler Operator before deleting the CR, you are not able to delete the CR.</p>
        </AlertDescription>
      </Alert>
      <h2 id="nodes-cma-autoscaling-custom-uninstalling_nodes-cma-autoscaling-custom-removing" class="group scroll-mt-20">Uninstalling the Custom Metrics Autoscaler Operator<a href="#nodes-cma-autoscaling-custom-uninstalling_nodes-cma-autoscaling-custom-removing" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Use the following procedure to remove the custom metrics autoscaler from your OpenShift Container Platform cluster.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>The Custom Metrics Autoscaler Operator must be installed.</li></ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, click <strong>Ecosystem</strong> → <strong>Installed Operators</strong>.</li>
      <li>Switch to the <strong>openshift-keda</strong> project.</li>
      <li>
      Remove the <code>KedaController</code> custom resource.
      <ol>
      <li>Find the <strong>CustomMetricsAutoscaler</strong>  Operator and click the <strong>KedaController</strong> tab.</li>
      <li>Find the custom resource, and then click <strong>Delete KedaController</strong>.</li>
      <li>Click <strong>Uninstall</strong>.</li>
      </ol>
      </li>
      <li>
      Remove the Custom Metrics Autoscaler Operator:
      <ol>
      <li>Click <strong>Ecosystem</strong> → <strong>Installed Operators</strong>.</li>
      <li>Find the <strong>CustomMetricsAutoscaler</strong>  Operator and click the Options menu <img src="/openshift-docs-markdown/_assets/images/kebab.png" alt="" loading="lazy" /> and select <strong>Uninstall Operator</strong>.</li>
      <li>Click <strong>Uninstall</strong>.</li>
      </ol>
      </li>
      <li>
      Optional: Use the OpenShift CLI to remove the custom metrics autoscaler components:
      <ol>
      <li>
      Delete the custom metrics autoscaler CRDs:
      <ul>
      <li><code>clustertriggerauthentications.keda.sh</code></li>
      <li><code>kedacontrollers.keda.sh</code></li>
      <li><code>scaledjobs.keda.sh</code></li>
      <li><code>scaledobjects.keda.sh</code></li>
      <li><code>triggerauthentications.keda.sh</code></li>
      </ul>
      <CodeBlock code={"$ oc delete crd clustertriggerauthentications.keda.sh kedacontrollers.keda.sh scaledjobs.keda.sh scaledobjects.keda.sh triggerauthentications.keda.sh"} lang="terminal" />
      <p>Deleting the CRDs removes the associated roles, cluster roles, and role bindings. However, there might be a few cluster roles that must be manually deleted.</p>
      </li>
      <li>
      List any custom metrics autoscaler cluster roles:
      <CodeBlock code={"$ oc get clusterrole | grep keda.sh"} lang="terminal" />
      </li>
      <li>
      Delete the listed custom metrics autoscaler cluster roles. For example:
      <CodeBlock code={"$ oc delete clusterrole.keda.sh-v1alpha1-admin"} lang="terminal" />
      </li>
      <li>
      List any custom metrics autoscaler cluster role bindings:
      <CodeBlock code={"$ oc get clusterrolebinding | grep keda.sh"} lang="terminal" />
      </li>
      <li>
      Delete the listed custom metrics autoscaler cluster role bindings. For example:
      <CodeBlock code={"$ oc delete clusterrolebinding.keda.sh-v1alpha1-admin"} lang="terminal" />
      </li>
      </ol>
      </li>
      <li>
      Delete the custom metrics autoscaler project:
      <CodeBlock code={"$ oc delete project openshift-keda"} lang="terminal" />
      </li>
      <li>
      Delete the Cluster Metric Autoscaler Operator:
      <CodeBlock code={"$ oc delete operator/openshift-custom-metrics-autoscaler-operator.openshift-keda"} lang="terminal" />
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
