---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: service_mesh/v2x/ossm-route-migration.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"ossm-route-migration","text":"Route migration"},{"depth":2,"slug":"ossm-migrating-from-ior-to-explicitly-managed-routes_route-migration","text":"Migrating from Istio OpenShift Routing to explicitly-managed routes"},{"depth":2,"slug":"additional-resources_route-migration","text":"Additional resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/service_mesh/v2x/ossm-route-migration";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Route migration";
const description = "Automatic route creation, also known as Istio OpenShift Routing (IOR), is a deprecated feature that is disabled by default for any ServiceMeshControlPlane…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["service_mesh","service_mesh/v2x"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Route migration {#ossm-route-migration}\n\nAutomatic route creation, also known as Istio OpenShift Routing (IOR), is a deprecated feature that is disabled by default for any `ServiceMeshControlPlane` resource that was created using Red Hat OpenShift Service Mesh 2.5 and later. Migrating from IOR to explicitly-managed routes provides a more flexible way to manage and configure ingress gateways. When route resources are explicitly created they can be managed alongside the other gateway and application resources as part of a GitOps management model.\n\n## Migrating from Istio OpenShift Routing to explicitly-managed routes {#ossm-migrating-from-ior-to-explicitly-managed-routes_route-migration}\n\nThis procedure explains how to disable Istio OpenShift Routing (IOR) in Red Hat OpenShift Service Mesh, and how to continue to use and manage Routes that were originally created using IOR. This procedure also provides an example of how to explicitly create a new Route targeting an existing gateway `Service` object.\n\n**Prerequisites**\n\n- Before migrating to explicitly-managed routes, export the existing route configurations managed by Istio OpenShift Routing (IOR) to files. Save the files so that in the future you can recreate the route configurations without requiring IOR.\n\n**Procedure**\n\n- Modify the `ServiceMeshControlPlane` resource to disable IOR:\n\n  ```yaml\n  apiVersion: maistra.io/v2\n  kind: ServiceMeshControlPlane\n  spec:\n    gateways:\n      openshiftRoute:\n        enabled: false\n  ```\n\n  You can continue to use the old routes that were previously created using IOR or you can create routes that explicitly target the ingress gateway `Service` object. The following example specifies how to create routes that explicitly target the ingress gateway `Service` object:\n\n  ```yaml\n  kind: Route\n  apiVersion: route.openshift.io/v1\n  metadata:\n    name: example-gateway\n    namespace: istio-system (1)\n  spec:\n    host: www.example.com\n    to:\n      kind: Service\n      name: istio-ingressgateway (2)\n      weight: 100\n    port:\n      targetPort: http2\n    wildcardPolicy: None\n  ```\n\n  1. Specify new routes in the same namespace as the ingress gateway `Service` object.\n  2. Use the name of ingress gateway `Service` object that is the target.\n\n## Additional resources {#additional-resources_route-migration}\n\n- [Creating an HTTP-based Route](/openshift-docs-markdown/networking/ingress_load_balancing/routes/creating-basic-routes#nw-creating-a-route_route-configuration)\n- [Understanding automatic routes](/openshift-docs-markdown/service_mesh/v2x/ossm-traffic-manage#ossm-auto-route_traffic-management)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/service_mesh/v2x/ossm-route-migration.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="ossm-route-migration" class="group scroll-mt-20">Route migration<a href="#ossm-route-migration" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Automatic route creation, also known as Istio OpenShift Routing (IOR), is a deprecated feature that is disabled by default for any <code>ServiceMeshControlPlane</code> resource that was created using Red Hat OpenShift Service Mesh 2.5 and later. Migrating from IOR to explicitly-managed routes provides a more flexible way to manage and configure ingress gateways. When route resources are explicitly created they can be managed alongside the other gateway and application resources as part of a GitOps management model.</p>
      <h2 id="ossm-migrating-from-ior-to-explicitly-managed-routes_route-migration" class="group scroll-mt-20">Migrating from Istio OpenShift Routing to explicitly-managed routes<a href="#ossm-migrating-from-ior-to-explicitly-managed-routes_route-migration" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>This procedure explains how to disable Istio OpenShift Routing (IOR) in Red Hat OpenShift Service Mesh, and how to continue to use and manage Routes that were originally created using IOR. This procedure also provides an example of how to explicitly create a new Route targeting an existing gateway <code>Service</code> object.</p>
      <p><strong>Prerequisites</strong></p>
      <ul><li>Before migrating to explicitly-managed routes, export the existing route configurations managed by Istio OpenShift Routing (IOR) to files. Save the files so that in the future you can recreate the route configurations without requiring IOR.</li></ul>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Modify the <code>ServiceMeshControlPlane</code> resource to disable IOR:
      <CodeBlock code={"apiVersion: maistra.io/v2\nkind: ServiceMeshControlPlane\nspec:\n  gateways:\n    openshiftRoute:\n      enabled: false"} lang="yaml" />
      <p>You can continue to use the old routes that were previously created using IOR or you can create routes that explicitly target the ingress gateway <code>Service</code> object. The following example specifies how to create routes that explicitly target the ingress gateway <code>Service</code> object:</p>
      <CodeBlock code={"kind: Route\napiVersion: route.openshift.io/v1\nmetadata:\n  name: example-gateway\n  namespace: istio-system (1)\nspec:\n  host: www.example.com\n  to:\n    kind: Service\n    name: istio-ingressgateway (2)\n    weight: 100\n  port:\n    targetPort: http2\n  wildcardPolicy: None"} lang="yaml" />
      <ol>
      <li>Specify new routes in the same namespace as the ingress gateway <code>Service</code> object.</li>
      <li>Use the name of ingress gateway <code>Service</code> object that is the target.</li>
      </ol>
      </li>
      </ul>
      <h2 id="additional-resources_route-migration" class="group scroll-mt-20">Additional resources<a href="#additional-resources_route-migration" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li><a href="/openshift-docs-markdown/networking/ingress_load_balancing/routes/creating-basic-routes#nw-creating-a-route_route-configuration">Creating an HTTP-based Route</a></li>
      <li><a href="/openshift-docs-markdown/service_mesh/v2x/ossm-traffic-manage#ossm-auto-route_traffic-management">Understanding automatic routes</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
