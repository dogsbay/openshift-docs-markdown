---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: service_mesh/v2x/ossm-performance-scalability.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"ossm-performance-scalability","text":"Performance and scalability"},{"depth":2,"slug":"ossm-recommended-resources_performance-scalability","text":"Setting limits on compute resources"},{"depth":2,"slug":"ossm-load-test-results_performance-scalability","text":"Load test results"},{"depth":3,"slug":"_smproductshortname_control_plane_performance","text":"Service Mesh Control plane performance"},{"depth":3,"slug":"_data_plane_performance","text":"Data plane performance"},{"depth":4,"slug":"_cpu_and_memory_consumption","text":"CPU and memory consumption"},{"depth":4,"slug":"_additional_latency","text":"Additional latency"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/service_mesh/v2x/ossm-performance-scalability";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Performance and scalability";
const description = "The default ServiceMeshControlPlane settings are not intended for production use; they are designed to install successfully on a default OpenShift…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["service_mesh","service_mesh/v2x"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Performance and scalability {#ossm-performance-scalability}\n\nThe default `ServiceMeshControlPlane` settings are not intended for production use; they are designed to install successfully on a default OpenShift Container Platform installation, which is a resource-limited environment. After you have verified a successful SMCP installation, you should modify the settings defined within the SMCP to suit your environment.\n\n## Setting limits on compute resources {#ossm-recommended-resources_performance-scalability}\n\nBy default, `spec.proxy` has the settings `cpu: 10m` and  `memory: 128M`. If you are using Pilot, `spec.runtime.components.pilot` has the same default values.\n\nThe settings in the following example are based on 1,000 services and 1,000 requests per second. You can change the values for `cpu` and `memory` in the `ServiceMeshControlPlane`.\n\n**Procedure**\n\n1. In the OpenShift Container Platform web console, click **Ecosystem** -> **Installed Operators**.\n2. Click the **Project** menu and select the project where you installed the Service Mesh control plane, for example **istio-system**.\n3. Click the Red Hat OpenShift Service Mesh Operator. In the **Istio Service Mesh Control Plane** column, click the name of your `ServiceMeshControlPlane`, for example `basic`.\n4. Add the name of your standalone Jaeger instance to the `ServiceMeshControlPlane`.\n\n   1. Click the **YAML** tab.\n   2. Set the values for `spec.proxy.runtime.container.resources.requests.cpu`, `spec.proxy.runtime.container.resources.requests.memory`, `components.kiali.container`, and `components.global.oauthproxy` in your `ServiceMeshControlPlane` resource.\n\n      ```yaml {title=\"Example version 2.6 ServiceMeshControlPlane\"}\n      apiVersion: maistra.io/v2\n      kind: ServiceMeshControlPlane\n      metadata:\n        name: basic\n        namespace: istio-system\n      spec:\n        version: v{{ MaistraVersion }}\n        proxy:\n          runtime:\n            container:\n              resources:\n                requests:\n                  cpu: 600m\n                  memory: 50Mi\n                limits: {}\n        runtime:\n          components:\n            pilot:\n              container:\n                resources:\n                  requests:\n                    cpu: 1000m\n                    memory: 1.6Gi\n                  limits: {}\n            kiali:\n              container:\n                resources:\n                  limits:\n                    cpu: \"90m\"\n                    memory: \"245Mi\"\n                  requests:\n                    cpu: \"30m\"\n                    memory: \"108Mi\"\n            global.oauthproxy:\n              container:\n                resources:\n                  requests:\n                    cpu: \"101m\"\n                    memory: \"256Mi\"\n                  limits:\n                    cpu: \"201m\"\n                    memory: \"512Mi\"\n      ```\n   3. To set values for Red Hat OpenShift Distributed Tracing Platform (Jaeger), see \"Configuring and deploying the distributed tracing platform Jaeger\".\n   4. Click **Save**.\n\n**Verification**\n\n- Click **Reload** to verify that the `ServiceMeshControlPlane` resource was configured correctly.\n\n## Load test results {#ossm-load-test-results_performance-scalability}\n\nThe upstream Istio community load tests mesh consists of **1000** services and **2000** sidecars with 70,000 mesh-wide requests per second. Running the tests using Istio 1.12.3, generated the following results:\n\n- The Envoy proxy uses **0.35 vCPU** and **40 MB memory** per 1000 requests per second going through the proxy.\n- Istiod uses **1 vCPU** and **1.5 GB** of memory.\n- The Envoy proxy adds **2.65 ms** to the 90th percentile latency.\n- The legacy `istio-telemetry` service (disabled by default in Service Mesh 2.0) uses **0.6 vCPU** per 1000 mesh-wide requests per second for deployments that use Mixer. The data plane components, the Envoy proxies, handle data flowing through the system. The Service Mesh control plane component, Istiod, configures the data plane. The data plane and control plane have distinct performance concerns.\n\n### Service Mesh Control plane performance {#_smproductshortname_control_plane_performance}\n\nIstiod configures sidecar proxies based on user authored configuration files and the current state of the system. In a Kubernetes environment, Custom Resource Definitions (CRDs) and deployments constitute the configuration and state of the system. The Istio configuration objects like gateways and virtual services, provide the user-authored configuration. To produce the configuration for the proxies, Istiod processes the combined configuration and system state from the Kubernetes environment and the user-authored configuration.\n\nThe Service Mesh control plane supports thousands of services, spread across thousands of pods with a similar number of user authored virtual services and other configuration objects. Istiod’s CPU and memory requirements scale with the number of configurations and possible system states. The CPU consumption scales with the following factors:\n\n- The rate of deployment changes.\n- The rate of configuration changes.\n- The number of proxies connecting to Istiod.\n\nHowever this part is inherently horizontally scalable.\n\n### Data plane performance {#_data_plane_performance}\n\nData plane performance depends on many factors, for example:\n\n- Number of client connections\n- Target request rate\n- Request size and response size\n- Number of proxy worker threads\n- Protocol\n- CPU cores\n- Number and types of proxy filters, specifically telemetry v2 related filters.\n\nThe latency, throughput, and the proxies' CPU and memory consumption are measured as a function of these factors.\n\n#### CPU and memory consumption {#_cpu_and_memory_consumption}\n\nSince the sidecar proxy performs additional work on the data path, it consumes CPU and memory. As of Istio 1.12.3, a proxy consumes about 0.5 vCPU per 1000 requests per second.\n\nThe memory consumption of the proxy depends on the total configuration state the proxy holds. A large number of listeners, clusters, and routes can increase memory usage.\n\nSince the proxy normally does not buffer the data passing through, request rate does not affect the memory consumption.\n\n#### Additional latency {#_additional_latency}\n\nSince Istio injects a sidecar proxy on the data path, latency is an important consideration. Istio adds an authentication filter, a telemetry filter, and a metadata exchange filter to the proxy. Every additional filter adds to the path length inside the proxy and affects latency.\n\nThe Envoy proxy collects raw telemetry data after a response is sent to the client. The time spent collecting raw telemetry for a request does not contribute to the total time taken to complete that request. However, because the worker is busy handling the request, the worker does not start handling the next request immediately. This process adds to the queue wait time of the next request and affects average and tail latencies. The actual tail latency depends on the traffic pattern.\n\nInside the mesh, a request traverses the client-side proxy and then the server-side proxy. In the default configuration of Istio 1.12.3 (that is, Istio with telemetry v2), the two proxies add about 1.7 ms and 2.7 ms to the 90th and 99th percentile latency, respectively, over the baseline data plane latency.\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/service_mesh/v2x/ossm-performance-scalability.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="ossm-performance-scalability" class="group scroll-mt-20">Performance and scalability<a href="#ossm-performance-scalability" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>The default <code>ServiceMeshControlPlane</code> settings are not intended for production use; they are designed to install successfully on a default OpenShift Container Platform installation, which is a resource-limited environment. After you have verified a successful SMCP installation, you should modify the settings defined within the SMCP to suit your environment.</p>
      <h2 id="ossm-recommended-resources_performance-scalability" class="group scroll-mt-20">Setting limits on compute resources<a href="#ossm-recommended-resources_performance-scalability" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>By default, <code>spec.proxy</code> has the settings <code>cpu: 10m</code> and  <code>memory: 128M</code>. If you are using Pilot, <code>spec.runtime.components.pilot</code> has the same default values.</p>
      <p>The settings in the following example are based on 1,000 services and 1,000 requests per second. You can change the values for <code>cpu</code> and <code>memory</code> in the <code>ServiceMeshControlPlane</code>.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>In the OpenShift Container Platform web console, click <strong>Ecosystem</strong> -&gt; <strong>Installed Operators</strong>.</li>
      <li>Click the <strong>Project</strong> menu and select the project where you installed the Service Mesh control plane, for example <strong>istio-system</strong>.</li>
      <li>Click the Red Hat OpenShift Service Mesh Operator. In the <strong>Istio Service Mesh Control Plane</strong> column, click the name of your <code>ServiceMeshControlPlane</code>, for example <code>basic</code>.</li>
      <li>
      Add the name of your standalone Jaeger instance to the <code>ServiceMeshControlPlane</code>.
      <ol>
      <li>Click the <strong>YAML</strong> tab.</li>
      <li>
      Set the values for <code>spec.proxy.runtime.container.resources.requests.cpu</code>, <code>spec.proxy.runtime.container.resources.requests.memory</code>, <code>components.kiali.container</code>, and <code>components.global.oauthproxy</code> in your <code>ServiceMeshControlPlane</code> resource.
      <CodeBlock code={"apiVersion: maistra.io/v2\nkind: ServiceMeshControlPlane\nmetadata:\n  name: basic\n  namespace: istio-system\nspec:\n  version: v{{ MaistraVersion }}\n  proxy:\n    runtime:\n      container:\n        resources:\n          requests:\n            cpu: 600m\n            memory: 50Mi\n          limits: {}\n  runtime:\n    components:\n      pilot:\n        container:\n          resources:\n            requests:\n              cpu: 1000m\n              memory: 1.6Gi\n            limits: {}\n      kiali:\n        container:\n          resources:\n            limits:\n              cpu: \"90m\"\n              memory: \"245Mi\"\n            requests:\n              cpu: \"30m\"\n              memory: \"108Mi\"\n      global.oauthproxy:\n        container:\n          resources:\n            requests:\n              cpu: \"101m\"\n              memory: \"256Mi\"\n            limits:\n              cpu: \"201m\"\n              memory: \"512Mi\""} lang="yaml" title="Example version 2.6 ServiceMeshControlPlane" />
      </li>
      <li>To set values for Red Hat OpenShift Distributed Tracing Platform (Jaeger), see "Configuring and deploying the distributed tracing platform Jaeger".</li>
      <li>Click <strong>Save</strong>.</li>
      </ol>
      </li>
      </ol>
      <p><strong>Verification</strong></p>
      <ul><li>Click <strong>Reload</strong> to verify that the <code>ServiceMeshControlPlane</code> resource was configured correctly.</li></ul>
      <h2 id="ossm-load-test-results_performance-scalability" class="group scroll-mt-20">Load test results<a href="#ossm-load-test-results_performance-scalability" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>The upstream Istio community load tests mesh consists of <strong>1000</strong> services and <strong>2000</strong> sidecars with 70,000 mesh-wide requests per second. Running the tests using Istio 1.12.3, generated the following results:</p>
      <ul>
      <li>The Envoy proxy uses <strong>0.35 vCPU</strong> and <strong>40 MB memory</strong> per 1000 requests per second going through the proxy.</li>
      <li>Istiod uses <strong>1 vCPU</strong> and <strong>1.5 GB</strong> of memory.</li>
      <li>The Envoy proxy adds <strong>2.65 ms</strong> to the 90th percentile latency.</li>
      <li>The legacy <code>istio-telemetry</code> service (disabled by default in Service Mesh 2.0) uses <strong>0.6 vCPU</strong> per 1000 mesh-wide requests per second for deployments that use Mixer. The data plane components, the Envoy proxies, handle data flowing through the system. The Service Mesh control plane component, Istiod, configures the data plane. The data plane and control plane have distinct performance concerns.</li>
      </ul>
      <h3 id="_smproductshortname_control_plane_performance" class="group scroll-mt-20">Service Mesh Control plane performance<a href="#_smproductshortname_control_plane_performance" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Istiod configures sidecar proxies based on user authored configuration files and the current state of the system. In a Kubernetes environment, Custom Resource Definitions (CRDs) and deployments constitute the configuration and state of the system. The Istio configuration objects like gateways and virtual services, provide the user-authored configuration. To produce the configuration for the proxies, Istiod processes the combined configuration and system state from the Kubernetes environment and the user-authored configuration.</p>
      <p>The Service Mesh control plane supports thousands of services, spread across thousands of pods with a similar number of user authored virtual services and other configuration objects. Istiod’s CPU and memory requirements scale with the number of configurations and possible system states. The CPU consumption scales with the following factors:</p>
      <ul>
      <li>The rate of deployment changes.</li>
      <li>The rate of configuration changes.</li>
      <li>The number of proxies connecting to Istiod.</li>
      </ul>
      <p>However this part is inherently horizontally scalable.</p>
      <h3 id="_data_plane_performance" class="group scroll-mt-20">Data plane performance<a href="#_data_plane_performance" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Data plane performance depends on many factors, for example:</p>
      <ul>
      <li>Number of client connections</li>
      <li>Target request rate</li>
      <li>Request size and response size</li>
      <li>Number of proxy worker threads</li>
      <li>Protocol</li>
      <li>CPU cores</li>
      <li>Number and types of proxy filters, specifically telemetry v2 related filters.</li>
      </ul>
      <p>The latency, throughput, and the proxies' CPU and memory consumption are measured as a function of these factors.</p>
      <h4 id="_cpu_and_memory_consumption" class="group scroll-mt-20">CPU and memory consumption<a href="#_cpu_and_memory_consumption" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h4>
      <p>Since the sidecar proxy performs additional work on the data path, it consumes CPU and memory. As of Istio 1.12.3, a proxy consumes about 0.5 vCPU per 1000 requests per second.</p>
      <p>The memory consumption of the proxy depends on the total configuration state the proxy holds. A large number of listeners, clusters, and routes can increase memory usage.</p>
      <p>Since the proxy normally does not buffer the data passing through, request rate does not affect the memory consumption.</p>
      <h4 id="_additional_latency" class="group scroll-mt-20">Additional latency<a href="#_additional_latency" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h4>
      <p>Since Istio injects a sidecar proxy on the data path, latency is an important consideration. Istio adds an authentication filter, a telemetry filter, and a metadata exchange filter to the proxy. Every additional filter adds to the path length inside the proxy and affects latency.</p>
      <p>The Envoy proxy collects raw telemetry data after a response is sent to the client. The time spent collecting raw telemetry for a request does not contribute to the total time taken to complete that request. However, because the worker is busy handling the request, the worker does not start handling the next request immediately. This process adds to the queue wait time of the next request and affects average and tail latencies. The actual tail latency depends on the traffic pattern.</p>
      <p>Inside the mesh, a request traverses the client-side proxy and then the server-side proxy. In the default configuration of Istio 1.12.3 (that is, Istio with telemetry v2), the two proxies add about 1.7 ms and 2.7 ms to the 90th and 99th percentile latency, respectively, over the baseline data plane latency.</p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
