---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: service_mesh/v2x/removing-ossm.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"removing-ossm","text":"Uninstalling Service Mesh"},{"depth":2,"slug":"ossm-control-plane-remove_removing-ossm","text":"Removing the Red Hat OpenShift Service Mesh control plane"},{"depth":3,"slug":"ossm-control-plane-remove-operatorhub_removing-ossm","text":"Removing the Service Mesh control plane using the web console"},{"depth":3,"slug":"ossm-control-plane-remove-cli_removing-ossm","text":"Removing the Service Mesh control plane using the CLI"},{"depth":2,"slug":"ossm-operatorhub-remove-operators_removing-ossm","text":"Removing the installed Operators"},{"depth":3,"slug":"ossm-remove-operator-servicemesh_removing-ossm","text":"Removing the Operators"},{"depth":2,"slug":"ossm-remove-cleanup_removing-ossm","text":"Clean up Operator resources"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/service_mesh/v2x/removing-ossm";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Uninstalling Service Mesh";
const description = "To uninstall Red Hat OpenShift Service Mesh from an existing OpenShift Container Platform instance and remove its resources, you must delete the control…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["service_mesh","service_mesh/v2x"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Uninstalling Service Mesh {#removing-ossm}\n\nTo uninstall Red Hat OpenShift Service Mesh from an existing OpenShift Container Platform instance and remove its resources, you must delete the control plane, delete the Operators, and run commands to manually remove some resources.\n\n## Removing the Red Hat OpenShift Service Mesh control plane {#ossm-control-plane-remove_removing-ossm}\n\nTo uninstall Service Mesh from an existing OpenShift Container Platform instance, first you delete the Service Mesh control plane and the Operators. Then, you run commands to remove residual resources.\n\n### Removing the Service Mesh control plane using the web console {#ossm-control-plane-remove-operatorhub_removing-ossm}\n\nYou can remove the Red Hat OpenShift Service Mesh control plane by using the web console.\n\n**Procedure**\n\n1. Log in to the OpenShift Container Platform web console.\n2. Click the **Project** menu and select the project where you installed the Service Mesh control plane, for example **istio-system**.\n3. Navigate to **Ecosystem** → **Installed Operators**.\n4. Click **Service Mesh Control Plane** under **Provided APIs**.\n5. Click the `ServiceMeshControlPlane` menu ![](/openshift-docs-markdown/_assets/images/kebab.png \"Options menu\").\n6. Click **Delete Service Mesh Control Plane**.\n7. Click **Delete** on the confirmation dialog window to remove the `ServiceMeshControlPlane`.\n\n### Removing the Service Mesh control plane using the CLI {#ossm-control-plane-remove-cli_removing-ossm}\n\nYou can remove the Red Hat OpenShift Service Mesh control plane by using the CLI.  In this example, `istio-system` is the name of the control plane project.\n\n**Procedure**\n\n1. Log in to the OpenShift Container Platform CLI.\n2. Run the following command to delete the `ServiceMeshMemberRoll` resource.\n\n   ```terminal\n   $ oc delete smmr -n istio-system default\n   ```\n3. Run this command to retrieve the name of the installed `ServiceMeshControlPlane`:\n\n   ```terminal\n   $ oc get smcp -n istio-system\n   ```\n4. Replace `<name_of_custom_resource>` with the output from the previous command, and run this command to remove the custom resource:\n\n   ```terminal\n   $ oc delete smcp -n istio-system <name_of_custom_resource>\n   ```\n\n## Removing the installed Operators {#ossm-operatorhub-remove-operators_removing-ossm}\n\nYou must remove the Operators to successfully remove Red Hat OpenShift Service Mesh. After you remove the Red Hat OpenShift Service Mesh Operator, you must remove the Kiali Operator, the Red Hat OpenShift Distributed Tracing Platform (Jaeger) Operator, and the OpenShift Elasticsearch Operator.\n\n### Removing the Operators {#ossm-remove-operator-servicemesh_removing-ossm}\n\nFollow this procedure to remove the Operators that make up Red Hat OpenShift Service Mesh. Repeat the steps for each of the following Operators.\n\n- Red Hat OpenShift Service Mesh\n- Kiali\n- Red Hat OpenShift Distributed Tracing Platform (Jaeger)\n- OpenShift Elasticsearch\n\n**Procedure**\n\n1. Log in to the OpenShift Container Platform web console.\n2. From the **Ecosystem** → **Installed Operators** page, scroll or type a keyword into the **Filter by name** to find each Operator. Then, click the Operator name.\n3. On the **Operator Details** page, select **Uninstall Operator** from the **Actions** menu. Follow the prompts to uninstall each Operator.\n\n## Clean up Operator resources {#ossm-remove-cleanup_removing-ossm}\n\nYou can manually remove resources left behind after removing the Red Hat OpenShift Service Mesh Operator using the OpenShift Container Platform web console.\n\n**Prerequisites**\n\n- An account with cluster administration access. If you use Red Hat OpenShift Dedicated, you must have an account with the `dedicated-admin` role.\n- Access to the OpenShift CLI (`oc`).\n\n**Procedure**\n\n1. Log in to the OpenShift Container Platform CLI as a cluster administrator.\n2. Run the following commands to clean up resources after uninstalling the Operators. If you intend to keep using Distributed Tracing Platform (Jaeger) as a stand-alone service without service mesh, do not delete the Jaeger resources.\n\n   > [!NOTE]\n   > The OpenShift Elasticsearch Operator is installed in `openshift-operators-redhat` by default. The other Operators are installed in the `openshift-operators` namespace by default. If you installed the Operators in another namespace, replace `openshift-operators` with the name of the project where the Red Hat OpenShift Service Mesh Operator was installed.\n\n   ```terminal\n   $ oc -n openshift-operators delete ds -lmaistra-version\n   ```\n\n   ```terminal\n   $ oc delete clusterrole/istio-admin clusterrole/istio-cni clusterrolebinding/istio-cni clusterrole/ossm-cni clusterrolebinding/ossm-cni\n   ```\n\n   ```terminal\n   $ oc delete clusterrole istio-view istio-edit\n   ```\n\n   ```terminal\n   $ oc delete clusterrole jaegers.jaegertracing.io-v1-admin jaegers.jaegertracing.io-v1-crdview jaegers.jaegertracing.io-v1-edit jaegers.jaegertracing.io-v1-view\n   ```\n\n   ```terminal\n   $ oc get crds -o name | grep '.*\\.istio\\.io' | xargs -r -n 1 oc delete\n   ```\n\n   ```terminal\n   $ oc get crds -o name | grep '.*\\.maistra\\.io' | xargs -r -n 1 oc delete\n   ```\n\n   ```terminal\n   $ oc get crds -o name | grep '.*\\.kiali\\.io' | xargs -r -n 1 oc delete\n   ```\n\n   ```terminal\n   $ oc delete crds jaegers.jaegertracing.io\n   ```\n\n   ```terminal\n   $ oc delete cm -n openshift-operators -lmaistra-version\n   ```\n\n   ```terminal\n   $ oc delete sa -n openshift-operators -lmaistra-version\n   ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/service_mesh/v2x/removing-ossm.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="removing-ossm" class="group scroll-mt-20">Uninstalling Service Mesh<a href="#removing-ossm" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>To uninstall Red Hat OpenShift Service Mesh from an existing OpenShift Container Platform instance and remove its resources, you must delete the control plane, delete the Operators, and run commands to manually remove some resources.</p>
      <h2 id="ossm-control-plane-remove_removing-ossm" class="group scroll-mt-20">Removing the Red Hat OpenShift Service Mesh control plane<a href="#ossm-control-plane-remove_removing-ossm" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>To uninstall Service Mesh from an existing OpenShift Container Platform instance, first you delete the Service Mesh control plane and the Operators. Then, you run commands to remove residual resources.</p>
      <h3 id="ossm-control-plane-remove-operatorhub_removing-ossm" class="group scroll-mt-20">Removing the Service Mesh control plane using the web console<a href="#ossm-control-plane-remove-operatorhub_removing-ossm" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>You can remove the Red Hat OpenShift Service Mesh control plane by using the web console.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Log in to the OpenShift Container Platform web console.</li>
      <li>Click the <strong>Project</strong> menu and select the project where you installed the Service Mesh control plane, for example <strong>istio-system</strong>.</li>
      <li>Navigate to <strong>Ecosystem</strong> → <strong>Installed Operators</strong>.</li>
      <li>Click <strong>Service Mesh Control Plane</strong> under <strong>Provided APIs</strong>.</li>
      <li>Click the <code>ServiceMeshControlPlane</code> menu <img src="/openshift-docs-markdown/_assets/images/kebab.png" alt="" loading="lazy" />.</li>
      <li>Click <strong>Delete Service Mesh Control Plane</strong>.</li>
      <li>Click <strong>Delete</strong> on the confirmation dialog window to remove the <code>ServiceMeshControlPlane</code>.</li>
      </ol>
      <h3 id="ossm-control-plane-remove-cli_removing-ossm" class="group scroll-mt-20">Removing the Service Mesh control plane using the CLI<a href="#ossm-control-plane-remove-cli_removing-ossm" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>You can remove the Red Hat OpenShift Service Mesh control plane by using the CLI.  In this example, <code>istio-system</code> is the name of the control plane project.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Log in to the OpenShift Container Platform CLI.</li>
      <li>
      Run the following command to delete the <code>ServiceMeshMemberRoll</code> resource.
      <CodeBlock code={"$ oc delete smmr -n istio-system default"} lang="terminal" />
      </li>
      <li>
      Run this command to retrieve the name of the installed <code>ServiceMeshControlPlane</code>:
      <CodeBlock code={"$ oc get smcp -n istio-system"} lang="terminal" />
      </li>
      <li>
      Replace <code>&lt;name_of_custom_resource&gt;</code> with the output from the previous command, and run this command to remove the custom resource:
      <CodeBlock code={"$ oc delete smcp -n istio-system <name_of_custom_resource>"} lang="terminal" />
      </li>
      </ol>
      <h2 id="ossm-operatorhub-remove-operators_removing-ossm" class="group scroll-mt-20">Removing the installed Operators<a href="#ossm-operatorhub-remove-operators_removing-ossm" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You must remove the Operators to successfully remove Red Hat OpenShift Service Mesh. After you remove the Red Hat OpenShift Service Mesh Operator, you must remove the Kiali Operator, the Red Hat OpenShift Distributed Tracing Platform (Jaeger) Operator, and the OpenShift Elasticsearch Operator.</p>
      <h3 id="ossm-remove-operator-servicemesh_removing-ossm" class="group scroll-mt-20">Removing the Operators<a href="#ossm-remove-operator-servicemesh_removing-ossm" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Follow this procedure to remove the Operators that make up Red Hat OpenShift Service Mesh. Repeat the steps for each of the following Operators.</p>
      <ul>
      <li>Red Hat OpenShift Service Mesh</li>
      <li>Kiali</li>
      <li>Red Hat OpenShift Distributed Tracing Platform (Jaeger)</li>
      <li>OpenShift Elasticsearch</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Log in to the OpenShift Container Platform web console.</li>
      <li>From the <strong>Ecosystem</strong> → <strong>Installed Operators</strong> page, scroll or type a keyword into the <strong>Filter by name</strong> to find each Operator. Then, click the Operator name.</li>
      <li>On the <strong>Operator Details</strong> page, select <strong>Uninstall Operator</strong> from the <strong>Actions</strong> menu. Follow the prompts to uninstall each Operator.</li>
      </ol>
      <h2 id="ossm-remove-cleanup_removing-ossm" class="group scroll-mt-20">Clean up Operator resources<a href="#ossm-remove-cleanup_removing-ossm" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>You can manually remove resources left behind after removing the Red Hat OpenShift Service Mesh Operator using the OpenShift Container Platform web console.</p>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>An account with cluster administration access. If you use Red Hat OpenShift Dedicated, you must have an account with the <code>dedicated-admin</code> role.</li>
      <li>Access to the OpenShift CLI (<code>oc</code>).</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>Log in to the OpenShift Container Platform CLI as a cluster administrator.</li>
      <li>
      Run the following commands to clean up resources after uninstalling the Operators. If you intend to keep using Distributed Tracing Platform (Jaeger) as a stand-alone service without service mesh, do not delete the Jaeger resources.
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The OpenShift Elasticsearch Operator is installed in <code>openshift-operators-redhat</code> by default. The other Operators are installed in the <code>openshift-operators</code> namespace by default. If you installed the Operators in another namespace, replace <code>openshift-operators</code> with the name of the project where the Red Hat OpenShift Service Mesh Operator was installed.</p>
        </AlertDescription>
      </Alert>
      <CodeBlock code={"$ oc -n openshift-operators delete ds -lmaistra-version"} lang="terminal" />
      <CodeBlock code={"$ oc delete clusterrole/istio-admin clusterrole/istio-cni clusterrolebinding/istio-cni clusterrole/ossm-cni clusterrolebinding/ossm-cni"} lang="terminal" />
      <CodeBlock code={"$ oc delete clusterrole istio-view istio-edit"} lang="terminal" />
      <CodeBlock code={"$ oc delete clusterrole jaegers.jaegertracing.io-v1-admin jaegers.jaegertracing.io-v1-crdview jaegers.jaegertracing.io-v1-edit jaegers.jaegertracing.io-v1-view"} lang="terminal" />
      <CodeBlock code={"$ oc get crds -o name | grep '.*\\.istio\\.io' | xargs -r -n 1 oc delete"} lang="terminal" />
      <CodeBlock code={"$ oc get crds -o name | grep '.*\\.maistra\\.io' | xargs -r -n 1 oc delete"} lang="terminal" />
      <CodeBlock code={"$ oc get crds -o name | grep '.*\\.kiali\\.io' | xargs -r -n 1 oc delete"} lang="terminal" />
      <CodeBlock code={"$ oc delete crds jaegers.jaegertracing.io"} lang="terminal" />
      <CodeBlock code={"$ oc delete cm -n openshift-operators -lmaistra-version"} lang="terminal" />
      <CodeBlock code={"$ oc delete sa -n openshift-operators -lmaistra-version"} lang="terminal" />
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
