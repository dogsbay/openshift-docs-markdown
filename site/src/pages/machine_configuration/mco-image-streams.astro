---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: machine_configuration/mco-image-streams.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"mco-image-streams","text":"Setting the RHCOS version in a cluster"},{"depth":2,"slug":"mco-image-streams-updating_mco-image-streams","text":"Updating the nodes in an existing cluster from RHCOS 9 to RHCOS 10"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/machine_configuration/mco-image-streams";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Setting the RHCOS version in a cluster";
const description = "You can create an OpenShift Container Platform cluster that uses Red Hat Enterprise Linux CoreOS (RHCOS) 10.x or update an existing cluster to RHCOS 10.x…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["machine_configuration"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Setting the RHCOS version in a cluster {#mco-image-streams}\n\nYou can create an OpenShift Container Platform cluster that uses Red Hat Enterprise Linux CoreOS (RHCOS) 10.x or update an existing cluster to RHCOS 10.x, which is available as a Technology Preview feature in OpenShift Container Platform 4.21.2 and greater. By running Red Hat Enterprise Linux CoreOS (RHCOS) 10.x as a Technology Preview feature, you can test how the operating system works with your cluster and your hardware, anticipate changes, and report bugs to Red Hat.\n\nBy default, RHCOS 9.x is installed on OpenShift Container Platform clusters starting with 4.13.\n\nAt any time, you can revert the cluster back to RHCOS 9.x, if needed.\n\n> [!IMPORTANT]\n> Using RHCOS 10.x in an OpenShift Container Platform cluster is a Technology Preview feature only. Technology Preview features are not supported with Red Hat production service level agreements (SLAs) and might not be functionally complete. Red Hat does not recommend using them in production. These features provide early access to upcoming product features, enabling customers to test functionality and provide feedback during the development process.\n>\n> For more information about the support scope of Red Hat Technology Preview features, see [Technology Preview Features Support Scope](https://access.redhat.com/support/offerings/techpreview/).\n\nRHCOS is a purpose-designed operating system for use with containers that is deployed by default on all OpenShift Container Platform nodes. Each version of RHCOS is based on a specific version of Red Hat Enterprise Linux (RHEL). For OpenShift Container Platform 4.13 and greater, the RHCOS version is based on RHEL 9.x.\n\nRunning a cluster with RHCOS 10.x is for **testing purposes only** on test clusters, and should not be used on production clusters. For example, by testing your cluster with RHCOS 10.x, you can ensure that any existing hardware operates as expected with the new operating system.\n\nYou can use one of the following methods to run the nodes in a cluster on RHCOS 10.x:\n\n- Upgrading an existing 4.21.2 or later cluster to RHCOS 10.x. For more information, see \"Updating the nodes in an existing cluster from RHCOS 9 to RHCOS 10\".\n- Deploying RHCOS 10.x on a new OpenShift Container Platform cluster. For more information, see \"Installation configuration parameters\".\n\n## Updating the nodes in an existing cluster from RHCOS 9 to RHCOS 10 {#mco-image-streams-updating_mco-image-streams}\n\nFor an existing OpenShift Container Platform 4.21.2 or later cluster, you can move the nodes in your machine config pool to Red Hat Enterprise Linux CoreOS (RHCOS) 10.x. By running Red Hat Enterprise Linux CoreOS (RHCOS) 10.x as a Technology Preview feature, you can test how the operating system works with your cluster and your hardware, anticipate changes, and report bugs to Red Hat.\n\nUse the following procedure for an OpenShift Container Platform 4.22.x cluster. For an OpenShift Container Platform 4.21.x cluster that is 4.21.2 or later, see the [How to deploy a RHCOS 10 OpenShift Container Platform cluster knowledgebase article](https://access.redhat.com/articles/7138399).\n\n> [!IMPORTANT]\n> Running a cluster with a mixture of RHCOS 9.x and 10.x nodes is not supported. You must move all of your nodes to RHCOS 10.x.\n\n**Prerequisites**\n\n- You have updated the boot image in your cluster to at least RHCOS 9.x. Note that the boot image on each node remains at RHCOS 9.x after installing or upgrading to RHCOS 10.x. After you configure RHCOS 10.x in your cluster, new nodes boot using RHCOS 9.x initially and automatically upgrade to RHCOS 10.x. For more information, see \"Manually updating the boot image\".\n- You have enabled the `TechPreviewNoUpgrade` feature set in your cluster’s `FeatureGate` custom resource (CR). For more information, see \"Enabling features using feature gates\".\n\n**Procedure**\n\n1. Confirm that your cluster has the RHCOS 10.x stream available by running the following command:\n\n   ```terminal\n   $ oc get osImageStreams/cluster -o yaml | grep rhel-10\n   ```\n\n   ```terminal {title=\"Example output\"}\n     - name: rhel-10\n   ```\n\n   It can take several minutes for the `osImageStream` object to become available after you enable the `TechPreviewNoUpgrade` feature set.\n2. Update the nodes by using one of the following procedures:\n\n   - Update all of the nodes in your cluster to RHCOS 10:\n\n     1. Edit the `OSImageStream` custom resource by running the following command:\n\n        ```terminal\n        $ oc edit osimagestream cluster\n        ```\n     2. Add or edit the `defaultStream` parameter to specify `rhel-10`:\n\n        ```terminal\n        apiVersion: machineconfiguration.openshift.io/v1alpha1\n        kind: OSImageStream\n        metadata:\n          annotations:\n            machineconfiguration.openshift.io/release-image-version: c4a08067821f304642e731fdcca0c8c6a6b19484\n          creationTimestamp: \"2026-04-13T17:27:41Z\"\n          generation: 1\n          name: cluster\n          resourceVersion: \"36503\"\n          uid: f2ef4c15-4c1b-4117-850e-ae6adf408c4f\n        spec:\n          defaultStream: rhel-10\n        status:\n          availableStreams:\n          - name: rhel-10\n            osExtensionsImage: quay.io/openshift-release-dev/ocp-v4.0-art-dev@sha256:34baf90f333d89690a2f99b3ab746f8a43fee99b1218a8a058f75231f7c7ab53\n            osImage: quay.io/openshift-release-dev/ocp-v4.0-art-dev@sha256:b208f0f861d009008b43a103e64d087f6da59e480bb0292d401895e041095da7\n          - name: rhel-9\n            osExtensionsImage: quay.io/openshift-release-dev/ocp-v4.0-art-dev@sha256:4aa864da633b1ce0a3612992a75849ff2b7d289699fa9b9b400522371a77d3ea\n            osImage: quay.io/openshift-release-dev/ocp-v4.0-art-dev@sha256:cb34964bd5d957a1226e9fb082a591b650eca339ebd4aad15343d02fc21130dd\n          defaultStream: rhel-9\n        ```\n\n        The `spec.defaultStream: rhel-10` parameter directs the Machine Config Operator (MCO) to update the nodes to the image referenced in `status.availableStreams.osImage` value under `name: rhel-10`.\n   - Update all machine config pools to RHCOS 10:\n\n     1. Update the worker machine config pool to RHCOS 10 by using the following command:\n\n        ```terminal\n        $ oc patch mcp worker --type merge -p '{\"spec\":{\"osImageStream\":{\"name\":\"rhel-10\"}}}'\n        ```\n     2. Update the control plane machine config pool to RHCOS 10 by using the following command:\n\n        ```terminal\n        $ oc patch mcp master --type merge -p '{\"spec\":{\"osImageStream\":{\"name\":\"rhel-10\"}}}'\n        ```\n     3. Update all custom machine config pools to RHCOS 10 by using the following command with the name of the machine config pool to update:\n\n        ```terminal\n        $ oc patch mcp <mcp_name> --type merge -p '{\"spec\":{\"osImageStream\":{\"name\":\"rhel-10\"}}}'\n        ```\n\n        Replace `<mcp_name>` with the names of the custom machine config pools to update.\n\n     > [!IMPORTANT]\n     > Running a cluster with a mixture of RHCOS 9.x and 10.x nodes is not supported. You must move all of your nodes to RHCOS 10.x.\n\n     Wait for the pools to finish rolling out the update.\n\n     .Verification\n3. After the nodes have returned to the READY state, examine the `/etc/redhat-release` file to see the current RHCOS version on the nodes:\n\n   1. Log in to a node by using the following command:\n\n      ```terminal\n      $ oc debug node/<node_name>\n      ```\n\n      Replace `<node_name>` with the name of the node.\n   2. Set `/host` as the root directory within the debug shell by using the following command:\n\n      ```terminal\n      $ chroot /host\n      ```\n   3. Look at the contents of the `/etc/redhat-release` file by using the following command:\n\n      ```terminal\n      $ cat /etc/redhat-release\n      ```\n\n      The output should appear similar to  the following example:\n\n      ```terminal {title=\"Example output\"}\n      Red Hat Enterprise Linux release 10.2 (Coughlan)\n      ```\n\n**Additional resources**\n{._additional-resources}\n\n- [Enabling features using feature gates](/openshift-docs-markdown/nodes/clusters/nodes-cluster-enabling-features#nodes-cluster-enabling-features)\n- [Manually updating the boot image](/openshift-docs-markdown/machine_configuration/mco-update-boot-images-manual#mco-update-boot-images-manual)\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/machine_configuration/mco-image-streams.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="mco-image-streams" class="group scroll-mt-20">Setting the RHCOS version in a cluster<a href="#mco-image-streams" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p class="_abstract">You can create an OpenShift Container Platform cluster that uses Red Hat Enterprise Linux CoreOS (RHCOS) 10.x or update an existing cluster to RHCOS 10.x, which is available as a Technology Preview feature in OpenShift Container Platform 4.21.2 and greater. By running Red Hat Enterprise Linux CoreOS (RHCOS) 10.x as a Technology Preview feature, you can test how the operating system works with your cluster and your hardware, anticipate changes, and report bugs to Red Hat. </p>
      <p>By default, RHCOS 9.x is installed on OpenShift Container Platform clusters starting with 4.13.</p>
      <p>At any time, you can revert the cluster back to RHCOS 9.x, if needed.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Using RHCOS 10.x in an OpenShift Container Platform cluster is a Technology Preview feature only. Technology Preview features are not supported with Red Hat production service level agreements (SLAs) and might not be functionally complete. Red Hat does not recommend using them in production. These features provide early access to upcoming product features, enabling customers to test functionality and provide feedback during the development process.</p>
          <p>For more information about the support scope of Red Hat Technology Preview features, see <a href="https://access.redhat.com/support/offerings/techpreview/">Technology Preview Features Support Scope</a>.</p>
        </AlertDescription>
      </Alert>
      <p>RHCOS is a purpose-designed operating system for use with containers that is deployed by default on all OpenShift Container Platform nodes. Each version of RHCOS is based on a specific version of Red Hat Enterprise Linux (RHEL). For OpenShift Container Platform 4.13 and greater, the RHCOS version is based on RHEL 9.x.</p>
      <p>Running a cluster with RHCOS 10.x is for <strong>testing purposes only</strong> on test clusters, and should not be used on production clusters. For example, by testing your cluster with RHCOS 10.x, you can ensure that any existing hardware operates as expected with the new operating system.</p>
      <p>You can use one of the following methods to run the nodes in a cluster on RHCOS 10.x:</p>
      <ul>
      <li>Upgrading an existing 4.21.2 or later cluster to RHCOS 10.x. For more information, see "Updating the nodes in an existing cluster from RHCOS 9 to RHCOS 10".</li>
      <li>Deploying RHCOS 10.x on a new OpenShift Container Platform cluster. For more information, see "Installation configuration parameters".</li>
      </ul>
      <h2 id="mco-image-streams-updating_mco-image-streams" class="group scroll-mt-20">Updating the nodes in an existing cluster from RHCOS 9 to RHCOS 10<a href="#mco-image-streams-updating_mco-image-streams" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">For an existing OpenShift Container Platform 4.21.2 or later cluster, you can move the nodes in your machine config pool to Red Hat Enterprise Linux CoreOS (RHCOS) 10.x. By running Red Hat Enterprise Linux CoreOS (RHCOS) 10.x as a Technology Preview feature, you can test how the operating system works with your cluster and your hardware, anticipate changes, and report bugs to Red Hat.</p>
      <p>Use the following procedure for an OpenShift Container Platform 4.22.x cluster. For an OpenShift Container Platform 4.21.x cluster that is 4.21.2 or later, see the <a href="https://access.redhat.com/articles/7138399">How to deploy a RHCOS 10 OpenShift Container Platform cluster knowledgebase article</a>.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Running a cluster with a mixture of RHCOS 9.x and 10.x nodes is not supported. You must move all of your nodes to RHCOS 10.x.</p>
        </AlertDescription>
      </Alert>
      <p><strong>Prerequisites</strong></p>
      <ul>
      <li>You have updated the boot image in your cluster to at least RHCOS 9.x. Note that the boot image on each node remains at RHCOS 9.x after installing or upgrading to RHCOS 10.x. After you configure RHCOS 10.x in your cluster, new nodes boot using RHCOS 9.x initially and automatically upgrade to RHCOS 10.x. For more information, see "Manually updating the boot image".</li>
      <li>You have enabled the <code>TechPreviewNoUpgrade</code> feature set in your cluster’s <code>FeatureGate</code> custom resource (CR). For more information, see "Enabling features using feature gates".</li>
      </ul>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Confirm that your cluster has the RHCOS 10.x stream available by running the following command:
      <CodeBlock code={"$ oc get osImageStreams/cluster -o yaml | grep rhel-10"} lang="terminal" />
      <CodeBlock code={"  - name: rhel-10"} lang="terminal" title="Example output" />
      <p>It can take several minutes for the <code>osImageStream</code> object to become available after you enable the <code>TechPreviewNoUpgrade</code> feature set.</p>
      </li>
      <li>
      Update the nodes by using one of the following procedures:
      <ul>
      <li>
      Update all of the nodes in your cluster to RHCOS 10:
      <ol>
      <li>
      Edit the <code>OSImageStream</code> custom resource by running the following command:
      <CodeBlock code={"$ oc edit osimagestream cluster"} lang="terminal" />
      </li>
      <li>
      Add or edit the <code>defaultStream</code> parameter to specify <code>rhel-10</code>:
      <CodeBlock code={"apiVersion: machineconfiguration.openshift.io/v1alpha1\nkind: OSImageStream\nmetadata:\n  annotations:\n    machineconfiguration.openshift.io/release-image-version: c4a08067821f304642e731fdcca0c8c6a6b19484\n  creationTimestamp: \"2026-04-13T17:27:41Z\"\n  generation: 1\n  name: cluster\n  resourceVersion: \"36503\"\n  uid: f2ef4c15-4c1b-4117-850e-ae6adf408c4f\nspec:\n  defaultStream: rhel-10\nstatus:\n  availableStreams:\n  - name: rhel-10\n    osExtensionsImage: quay.io/openshift-release-dev/ocp-v4.0-art-dev@sha256:34baf90f333d89690a2f99b3ab746f8a43fee99b1218a8a058f75231f7c7ab53\n    osImage: quay.io/openshift-release-dev/ocp-v4.0-art-dev@sha256:b208f0f861d009008b43a103e64d087f6da59e480bb0292d401895e041095da7\n  - name: rhel-9\n    osExtensionsImage: quay.io/openshift-release-dev/ocp-v4.0-art-dev@sha256:4aa864da633b1ce0a3612992a75849ff2b7d289699fa9b9b400522371a77d3ea\n    osImage: quay.io/openshift-release-dev/ocp-v4.0-art-dev@sha256:cb34964bd5d957a1226e9fb082a591b650eca339ebd4aad15343d02fc21130dd\n  defaultStream: rhel-9"} lang="terminal" />
      <p>The <code>spec.defaultStream: rhel-10</code> parameter directs the Machine Config Operator (MCO) to update the nodes to the image referenced in <code>status.availableStreams.osImage</code> value under <code>name: rhel-10</code>.</p>
      </li>
      </ol>
      </li>
      <li>
      Update all machine config pools to RHCOS 10:
      <ol>
      <li>
      Update the worker machine config pool to RHCOS 10 by using the following command:
      <CodeBlock code={"$ oc patch mcp worker --type merge -p '{\"spec\":{\"osImageStream\":{\"name\":\"rhel-10\"}}}'"} lang="terminal" />
      </li>
      <li>
      Update the control plane machine config pool to RHCOS 10 by using the following command:
      <CodeBlock code={"$ oc patch mcp master --type merge -p '{\"spec\":{\"osImageStream\":{\"name\":\"rhel-10\"}}}'"} lang="terminal" />
      </li>
      <li>
      Update all custom machine config pools to RHCOS 10 by using the following command with the name of the machine config pool to update:
      <CodeBlock code={"$ oc patch mcp <mcp_name> --type merge -p '{\"spec\":{\"osImageStream\":{\"name\":\"rhel-10\"}}}'"} lang="terminal" />
      <p>Replace <code>&lt;mcp_name&gt;</code> with the names of the custom machine config pools to update.</p>
      </li>
      </ol>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>Running a cluster with a mixture of RHCOS 9.x and 10.x nodes is not supported. You must move all of your nodes to RHCOS 10.x.</p>
        </AlertDescription>
      </Alert>
      <p>Wait for the pools to finish rolling out the update.</p>
      <p>.Verification</p>
      </li>
      </ul>
      </li>
      <li>
      After the nodes have returned to the READY state, examine the <code>/etc/redhat-release</code> file to see the current RHCOS version on the nodes:
      <ol>
      <li>
      Log in to a node by using the following command:
      <CodeBlock code={"$ oc debug node/<node_name>"} lang="terminal" />
      <p>Replace <code>&lt;node_name&gt;</code> with the name of the node.</p>
      </li>
      <li>
      Set <code>/host</code> as the root directory within the debug shell by using the following command:
      <CodeBlock code={"$ chroot /host"} lang="terminal" />
      </li>
      <li>
      Look at the contents of the <code>/etc/redhat-release</code> file by using the following command:
      <CodeBlock code={"$ cat /etc/redhat-release"} lang="terminal" />
      <p>The output should appear similar to  the following example:</p>
      <CodeBlock code={"Red Hat Enterprise Linux release 10.2 (Coughlan)"} lang="terminal" title="Example output" />
      </li>
      </ol>
      </li>
      </ol>
      <section class="related-resources _additional-resources">
      <p class="related-resources-title">Additional resources</p>
      <ul>
      <li><a href="/openshift-docs-markdown/nodes/clusters/nodes-cluster-enabling-features#nodes-cluster-enabling-features">Enabling features using feature gates</a></li>
      <li><a href="/openshift-docs-markdown/machine_configuration/mco-update-boot-images-manual#mco-update-boot-images-manual">Manually updating the boot image</a></li>
      </ul>
      </section>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
