---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: machine_configuration/machine-configs-garbage-collection.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"machine-configs-garbage-collection","text":"Managing unused rendered machine configs"},{"depth":2,"slug":"machineconfig-garbage-collect-viewing_machine-configs-garbage-collection","text":"Viewing rendered machine configs"},{"depth":2,"slug":"machineconfig-garbage-collect-removing_machine-configs-garbage-collection","text":"Removing unused rendered machine configs"}];
const nav = navData;
const currentPath = "/openshift-docs-markdown/machine_configuration/machine-configs-garbage-collection";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "/docs",
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Managing unused rendered machine configs";
const description = "You can remove old, unused rendered machine configs by using the oc adm prune renderedmachineconfigs command. By removing these objects, you can free up…" || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = ["machine_configuration"];
const pageTaxonomies = null;
const tagsIndexPath = "/openshift-docs-markdown/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Managing unused rendered machine configs {#machine-configs-garbage-collection}\n\nYou can remove old, unused rendered machine configs by using the `oc adm prune renderedmachineconfigs` command. By removing these objects, you can free up disk space and reduce performance issues.\n\nThe Machine Config Operator (MCO) does not perform any garbage collection activities. This means that all rendered machine configs remain in the cluster. Each time a user or controller applies a new machine config, the MCO creates new rendered configs for each affected machine config pool. Over time, this can lead to a large number of rendered machine configs, which can make working with machine configs confusing. Having too many rendered machine configs can also contribute to disk space issues and performance issues with etcd.\n\nBy using the `oc adm prune renderedmachineconfigs` command with the `--confirm` flag, you can remove all unused rendered machine configs or only those in a specific machine config pool. You can also remove a specific number of unused rendered machine configs, keeping some older machine configs in case you want to check older configurations.\n\nYou can use the `oc adm prune renderedmachineconfigs` command without the `--confirm` flag to see which rendered machine configs would be removed.\n\nUse the `list` subcommand to display all the rendered machine configs in the cluster or a specific machine config pool.\n\n> [!NOTE]\n> The `oc adm prune renderedmachineconfigs` command deletes only rendered machine configs that are not in use. If a rendered machine configs are in use by a machine config pool, the rendered machine config is not deleted. In this case, the command output specifies the reason that the rendered machine config was not deleted.\n\n## Viewing rendered machine configs {#machineconfig-garbage-collect-viewing_machine-configs-garbage-collection}\n\nYou can view a list of rendered machine configs by using the `oc adm prune renderedmachineconfigs` command with the `list` subcommand to determine which objects you can remove.\n\nFor example, the command in the following procedure would list all rendered machine configs for the `worker` machine config pool.\n\n**Procedure**\n\n- Optional: List the rendered machine configs by using the following command:\n\n  ```terminal\n  $ oc adm prune renderedmachineconfigs list --in-use=false --pool-name=worker\n  ```\n\n  where:\n\n  list\n  :   Displays a list of rendered machine configs in your cluster.\n\n  `--in-use`\n  :   Optional: Specifies whether to display only the used machine configs or all machine configs from the specified pool. If `true`, the output lists the rendered machine configs that are being used by a machine config pool. If `false`, the output lists all rendered machine configs in the cluster. The default value is `false`.\n\n  `--pool-name`\n  :   Optional: Specifies the machine config pool from which to display the machine configs.\n\n  ```terminal {title=\"Example output\"}\n  worker\n\n  rendered-worker-f38bf61ced3c920cf5a29a200ed43243 -- 2025-01-21 13:45:01 +0000 UTC (Currently in use: false)\n  rendered-worker-fc94397dc7c43808c7014683c208956e-- 2025-01-30 17:20:53 +0000 UTC (Currently in use: false)\n  rendered-worker-708c652868f7597eaa1e2622edc366ef -- 2025-01-31 18:01:16 +0000 UTC (Currently in use: true)\n  ```\n- List the rendered machine configs that you can remove automatically by running the following command. Any rendered machine config marked with the `as it’s currently in use` message in the command output cannot be removed.\n\n  ```terminal\n  $ oc adm prune renderedmachineconfigs --pool-name=worker\n  ```\n\n  The command runs in dry-run mode, and no machine configs are removed. where:\n\n  `--pool-name`\n  :   Optional: Displays the machine configs in the specified machine config pool.\n\n  ```terminal {title=\"Example output\"}\n  Dry run enabled - no modifications will be made. Add --confirm to remove rendered machine configs.\n  dry-run deleting rendered MachineConfig rendered-worker-f38bf61ced3c920cf5a29a200ed43243\n  dry-run deleting MachineConfig rendered-worker-fc94397dc7c43808c7014683c208956e\n  Skip dry-run deleting rendered MachineConfig rendered-worker-708c652868f7597eaa1e2622edc366ef as it's currently in use\n  ```\n\n## Removing unused rendered machine configs {#machineconfig-garbage-collect-removing_machine-configs-garbage-collection}\n\nYou can remove unused rendered machine configs by using the `oc adm prune renderedmachineconfigs` command with the `--confirm` command, reducing disk space and performance issues.\n\nIf any rendered machine config is not deleted, the command output indicates which was not deleted and lists the reason for skipping the deletion.\n\n**Procedure**\n\n1. Optional: List the rendered machine configs that you can remove automatically by running the following command. Any rendered machine config marked with the `as it’s currently in use` message in the command output cannot be removed.\n\n   ```terminal\n   $ oc adm prune renderedmachineconfigs --pool-name=worker\n   ```\n\n   ```terminal {title=\"Example output\"}\n   Dry run enabled - no modifications will be made. Add --confirm to remove rendered machine configs.\n   dry-run deleting rendered MachineConfig rendered-worker-f38bf61ced3c920cf5a29a200ed43243\n   dry-run deleting MachineConfig rendered-worker-fc94397dc7c43808c7014683c208956e\n   Skip dry-run deleting rendered MachineConfig rendered-worker-708c652868f7597eaa1e2622edc366ef as it's currently in use\n   ```\n\n   where:\n\n   pool-name\n   :   Optional: Specifies the machine config pool where you want to delete the machine configs from.\n2. Remove the unused rendered machine configs by running the following command. The command in the following procedure would delete the two oldest unused rendered machine configs in the `worker` machine config pool.\n\n   ```terminal\n   $ oc adm prune renderedmachineconfigs --pool-name=worker --count=2 --confirm\n   ```\n\n   where:\n\n   `--count`\n   :   Optional: Specifies the maximum number of unused rendered machine configs you want to delete, starting with the oldest.\n\n   `--confirm`\n   :   Indicates that pruning should occur, instead of performing a dry-run.\n\n   `--pool-name`\n   :   Optional: Specifies the machine config pool from which you want to delete the machine. If not specified, all the pools are evaluated.\n\n   ```terminal {title=\"Example output\"}\n   deleting rendered MachineConfig rendered-worker-f38bf61ced3c920cf5a29a200ed43243\n   deleting rendered MachineConfig rendered-worker-fc94397dc7c43808c7014683c208956e\n   Skip deleting rendered MachineConfig rendered-worker-708c652868f7597eaa1e2622edc366ef as it's currently in use\n   ```\n","mdUrl":"https://dogsbay.github.io/openshift-docs-markdown/machine_configuration/machine-configs-garbage-collection.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/openshift-docs-markdown"}
  navMode={"client"}
  pagefindUrl={"/openshift-docs-markdown/pagefind/"}
  favicon={"/openshift-docs-markdown/favicon.ico"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="machine-configs-garbage-collection" class="group scroll-mt-20">Managing unused rendered machine configs<a href="#machine-configs-garbage-collection" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>You can remove old, unused rendered machine configs by using the <code>oc adm prune renderedmachineconfigs</code> command. By removing these objects, you can free up disk space and reduce performance issues.</p>
      <p>The Machine Config Operator (MCO) does not perform any garbage collection activities. This means that all rendered machine configs remain in the cluster. Each time a user or controller applies a new machine config, the MCO creates new rendered configs for each affected machine config pool. Over time, this can lead to a large number of rendered machine configs, which can make working with machine configs confusing. Having too many rendered machine configs can also contribute to disk space issues and performance issues with etcd.</p>
      <p>By using the <code>oc adm prune renderedmachineconfigs</code> command with the <code>--confirm</code> flag, you can remove all unused rendered machine configs or only those in a specific machine config pool. You can also remove a specific number of unused rendered machine configs, keeping some older machine configs in case you want to check older configurations.</p>
      <p>You can use the <code>oc adm prune renderedmachineconfigs</code> command without the <code>--confirm</code> flag to see which rendered machine configs would be removed.</p>
      <p>Use the <code>list</code> subcommand to display all the rendered machine configs in the cluster or a specific machine config pool.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The <code>oc adm prune renderedmachineconfigs</code> command deletes only rendered machine configs that are not in use. If a rendered machine configs are in use by a machine config pool, the rendered machine config is not deleted. In this case, the command output specifies the reason that the rendered machine config was not deleted.</p>
        </AlertDescription>
      </Alert>
      <h2 id="machineconfig-garbage-collect-viewing_machine-configs-garbage-collection" class="group scroll-mt-20">Viewing rendered machine configs<a href="#machineconfig-garbage-collect-viewing_machine-configs-garbage-collection" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can view a list of rendered machine configs by using the <code>oc adm prune renderedmachineconfigs</code> command with the <code>list</code> subcommand to determine which objects you can remove.</p>
      <p>For example, the command in the following procedure would list all rendered machine configs for the <code>worker</code> machine config pool.</p>
      <p><strong>Procedure</strong></p>
      <ul>
      <li>
      Optional: List the rendered machine configs by using the following command:
      <CodeBlock code={"$ oc adm prune renderedmachineconfigs list --in-use=false --pool-name=worker"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">list</dt>
      <dd class="ml-6 text-muted-foreground"><p>Displays a list of rendered machine configs in your cluster.</p></dd>
      <dt class="font-semibold"><code>--in-use</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional: Specifies whether to display only the used machine configs or all machine configs from the specified pool. If <code>true</code>, the output lists the rendered machine configs that are being used by a machine config pool. If <code>false</code>, the output lists all rendered machine configs in the cluster. The default value is <code>false</code>.</p></dd>
      <dt class="font-semibold"><code>--pool-name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional: Specifies the machine config pool from which to display the machine configs.</p></dd>
      </dl>
      <CodeBlock code={"worker\n\nrendered-worker-f38bf61ced3c920cf5a29a200ed43243 -- 2025-01-21 13:45:01 +0000 UTC (Currently in use: false)\nrendered-worker-fc94397dc7c43808c7014683c208956e-- 2025-01-30 17:20:53 +0000 UTC (Currently in use: false)\nrendered-worker-708c652868f7597eaa1e2622edc366ef -- 2025-01-31 18:01:16 +0000 UTC (Currently in use: true)"} lang="terminal" title="Example output" />
      </li>
      <li>
      List the rendered machine configs that you can remove automatically by running the following command. Any rendered machine config marked with the <code>as it’s currently in use</code> message in the command output cannot be removed.
      <CodeBlock code={"$ oc adm prune renderedmachineconfigs --pool-name=worker"} lang="terminal" />
      <p>The command runs in dry-run mode, and no machine configs are removed. where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>--pool-name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional: Displays the machine configs in the specified machine config pool.</p></dd>
      </dl>
      <CodeBlock code={"Dry run enabled - no modifications will be made. Add --confirm to remove rendered machine configs.\ndry-run deleting rendered MachineConfig rendered-worker-f38bf61ced3c920cf5a29a200ed43243\ndry-run deleting MachineConfig rendered-worker-fc94397dc7c43808c7014683c208956e\nSkip dry-run deleting rendered MachineConfig rendered-worker-708c652868f7597eaa1e2622edc366ef as it's currently in use"} lang="terminal" title="Example output" />
      </li>
      </ul>
      <h2 id="machineconfig-garbage-collect-removing_machine-configs-garbage-collection" class="group scroll-mt-20">Removing unused rendered machine configs<a href="#machineconfig-garbage-collect-removing_machine-configs-garbage-collection" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p class="_abstract">You can remove unused rendered machine configs by using the <code>oc adm prune renderedmachineconfigs</code> command with the <code>--confirm</code> command, reducing disk space and performance issues.</p>
      <p>If any rendered machine config is not deleted, the command output indicates which was not deleted and lists the reason for skipping the deletion.</p>
      <p><strong>Procedure</strong></p>
      <ol>
      <li>
      Optional: List the rendered machine configs that you can remove automatically by running the following command. Any rendered machine config marked with the <code>as it’s currently in use</code> message in the command output cannot be removed.
      <CodeBlock code={"$ oc adm prune renderedmachineconfigs --pool-name=worker"} lang="terminal" />
      <CodeBlock code={"Dry run enabled - no modifications will be made. Add --confirm to remove rendered machine configs.\ndry-run deleting rendered MachineConfig rendered-worker-f38bf61ced3c920cf5a29a200ed43243\ndry-run deleting MachineConfig rendered-worker-fc94397dc7c43808c7014683c208956e\nSkip dry-run deleting rendered MachineConfig rendered-worker-708c652868f7597eaa1e2622edc366ef as it's currently in use"} lang="terminal" title="Example output" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold">pool-name</dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional: Specifies the machine config pool where you want to delete the machine configs from.</p></dd>
      </dl>
      </li>
      <li>
      Remove the unused rendered machine configs by running the following command. The command in the following procedure would delete the two oldest unused rendered machine configs in the <code>worker</code> machine config pool.
      <CodeBlock code={"$ oc adm prune renderedmachineconfigs --pool-name=worker --count=2 --confirm"} lang="terminal" />
      <p>where:</p>
      <dl class="my-4 space-y-2">
      <dt class="font-semibold"><code>--count</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional: Specifies the maximum number of unused rendered machine configs you want to delete, starting with the oldest.</p></dd>
      <dt class="font-semibold"><code>--confirm</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Indicates that pruning should occur, instead of performing a dry-run.</p></dd>
      <dt class="font-semibold"><code>--pool-name</code></dt>
      <dd class="ml-6 text-muted-foreground"><p>Optional: Specifies the machine config pool from which you want to delete the machine. If not specified, all the pools are evaluated.</p></dd>
      </dl>
      <CodeBlock code={"deleting rendered MachineConfig rendered-worker-f38bf61ced3c920cf5a29a200ed43243\ndeleting rendered MachineConfig rendered-worker-fc94397dc7c43808c7014683c208956e\nSkip deleting rendered MachineConfig rendered-worker-708c652868f7597eaa1e2622edc366ef as it's currently in use"} lang="terminal" title="Example output" />
      </li>
      </ol>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>
