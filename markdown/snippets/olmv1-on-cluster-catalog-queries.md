{%- set _mod_docs_content_type = "SNIPPET" %}

Currently in {{ olmv1_first }}, you cannot query on-cluster catalogs managed by catalogd. In OLM v1, you must use the `opm` and `jq` CLI tools to query the catalog registry.