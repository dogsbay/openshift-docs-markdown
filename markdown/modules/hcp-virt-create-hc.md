{%- set _mod_docs_content_type = "CONCEPT" %}
# Hosted clusters with the KubeVirt platform {id="hcp-virt-create-hc_{{ context }}"}

With {{ product_title }} 4.14 or later, you can create a hosted cluster with KubeVirt by using the command-line interface (CLI), the console, or by using external infrastructure.