{%- set _mod_docs_content_type = "CONCEPT" %}
# Adding worker nodes {id="adding-worker-nodes_{{ context }}"}

After you deploy your {{ product_title }} cluster, you can add worker nodes to scale cluster resources using the method that matches your installation type and environment. {._abstract}

After you deploy your {{ product_title }} cluster, you can add worker nodes to scale cluster resources. There are different ways you can add worker nodes depending on the installation method and the environment of your cluster.