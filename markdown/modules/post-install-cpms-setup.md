{%- set _mod_docs_content_type = "CONCEPT" %}
# Managing control plane machines {id="post-install-cpms-setup_{{ context }}"}

Use control plane machine sets to manage control plane machines with capabilities similar to compute machine sets. {._abstract}

Control plane machine sets provide management capabilities for control plane machines that are similar to what compute machine sets provide for compute machines. The availability and initial status of control plane machine sets on your cluster depend on your cloud provider and the version of {{ product_title }} that you installed.