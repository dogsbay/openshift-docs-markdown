{%- set _mod_docs_content_type = "CONCEPT" %}
# Secure the MicroShift API and control plane {id="microshift-secure-api-control-plane_{{ context }}"}

Configure certificates and TLS to ensure that only trusted clients can communicate with the {{ microshift_short }} API and that traffic is authenticated and encrypted.