{%- set _mod_docs_content_type = "CONCEPT" %}
# Customize networking configuration {id="microshift-customize-networking_{{ context }}"}

After installing {{ microshift_short }}, customize the default networking settings to match your edge deployment needs. You can configure OVN-Kubernetes options, IPv6 networking, HTTP proxy settings, and hostname resolution.