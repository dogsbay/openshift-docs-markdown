{%- set _mod_docs_content_type = "REFERENCE" %}
# Image stream images {id="images-imagestream-image_{{ context }}"}

Image stream images are API resource objects in {{ product_title }} that retrieve specific container images from image streams. You can use image stream images to access metadata about particular image SHA identifiers.