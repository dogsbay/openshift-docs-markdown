{%- set _mod_docs_content_type = "CONCEPT" %}
# Working with image streams {id="working-with-image-streams_{{ context }}"}

To organize and manage container images in {{ product_title }}, you can use image streams and image stream tags. By using image streams, you can track image versions and simplify deployments.