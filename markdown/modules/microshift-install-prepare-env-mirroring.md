{%- set _mod_docs_content_type = "CONCEPT" %}
# Prepare your environment for mirroring {id="microshift-install-prepare-env-mirroring_{{ context }}"}

If your edge nodes operate in a disconnected or restricted network, you can mirror the {{ microshift_short }} container images into a private registry so that you can install the platform without internet access.