{%- set _mod_docs_content_type = "CONCEPT" %}
# Installing the {{ gcp_short }} Filestore CSI Driver Operator {id="persistent-storage-csi-olm-operator-install-overview_{{ context }}"}

Since the Google Compute Platform ({{ gcp_short }}) Filestore Container Storage Interface (CSI) Driver Operator is not installed in {{ product_title }} by default, you must install the {{ gcp_short }} Filestore CSI Driver Operator in your cluster.