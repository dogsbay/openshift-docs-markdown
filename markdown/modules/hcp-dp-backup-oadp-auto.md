{%- set _mod_docs_content_type = "PROCEDURE" %}
# Backing up the data plane workload by using the {{ oadp_short }} Operator {id="hcp-dp-backup-oadp-auto_{{ context }}"}

You can back up the data plane workload by using the {{ oadp_short }} Operator.  {._abstract}

However, if the data plane workload is not important, you can skip this procedure.

**Procedure**

*   To back up the data plane workload, follow the steps in "Backing up applications".