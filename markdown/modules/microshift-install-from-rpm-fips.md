{%- set _mod_docs_content_type = "CONCEPT" %}
# Install from an RPM package {id="microshift-install-from-rpm-fips_{{ context }}"}

You can install {{ microshift_short }} from an RPM package on an existing {{ op_system_base }} {{ op_system_version_major }} system. You can also prepare for FIPS mode and add optional extensions such as GitOps or Observability.