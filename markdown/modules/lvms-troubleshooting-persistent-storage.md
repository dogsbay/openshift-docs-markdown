{%- set _mod_docs_content_type = "CONCEPT" %}
# Troubleshooting persistent storage {id="lvms-troubleshooting-persistent-storage_{{ context }}"}

If persistent storage issues occur with {{ lvms_first }}, such as persistent volume claims (PVCs) stuck in a pending state, missing components, or node and disk failures, you can diagnose and resolve the problem by reviewing logs and recovering affected resources.