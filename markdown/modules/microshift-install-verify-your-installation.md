{%- set _mod_docs_content_type = "CONCEPT" %}
# Verify your installation {id="microshift-install-verify-your-installation_{{ context }}"}

After installation completes, you can retrieve your kubeconfig files and access the {{ microshift_short }} node with the `oc` CLI to verify the cluster is running correctly and begin deploying workloads.