{%- set _mod_docs_content_type = "CONCEPT" %}
# Configuring your {{ gcp_short }} project {id="installation-gcp-user-infra-config-project_{{ context }}"}

Before you can install {{ product_title }}, you must configure a {{ gcp_first }} project to host it. Proper project configuration provides the API services, service account, and permissions that the installation requires.