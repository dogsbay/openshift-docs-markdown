{%- set _mod_docs_content_type = "CONCEPT" %}
# Recommended cluster configurations to run vDU applications {id="ztp-du-cluster-config-reference_{{ context }}"}

Clusters running virtualized distributed unit (vDU) applications require a highly tuned and optimized configuration. The following information describes the various elements that you require to support vDU workloads in {{ product_title }} {{ product_version }} clusters.