{%- set _mod_docs_content_type = "CONCEPT" %}
# Managing the NodeNetworkConfigurationPolicy manifest file {id="virt-manage-nncp-cli_{{ context }}"}

To configure network interfaces on nodes in your {{ product_title }} cluster, you can manage the `NodeNetworkConfigurationPolicy` manifest file by using the CLI.