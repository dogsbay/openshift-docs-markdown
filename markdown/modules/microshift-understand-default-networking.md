{%- set _mod_docs_content_type = "CONCEPT" %}
# Understand default networking capabilities {id="microshift-understand-default-networking_{{ context }}"}

Understand how networking works out of the box in {{ microshift_short }} so that you can determine if the default configuration suits your needs or identify where you need to customize it.