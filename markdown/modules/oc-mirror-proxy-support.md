{%- set _mod_docs_content_type = "CONCEPT" %}
# oc-mirror plugin v2 support proxy setting {id="oc-mirror-proxy-support_{{ context }}"}

The oc-mirror plugin v2 can operate in a proxy-configured environment. The plugin can use system proxy settings to retrieve images for {{ product_title }}, Operator catalog, and the `additionalImages` registry.