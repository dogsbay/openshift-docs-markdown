{%- set _mod_docs_content_type = "CONCEPT" %}
# Mirroring an image set to a mirror registry {id="mirroring-image-set"}

You can use the oc-mirror CLI plugin to mirror images to a mirror registry in a partially disconnected environment or in a fully disconnected environment. {._abstract}

These procedures assume that you already have your mirror registry set up.