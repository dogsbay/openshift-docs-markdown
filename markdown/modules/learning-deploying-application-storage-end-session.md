{%- set _mod_docs_content_type = "PROCEDURE" %}
# Ending the session {id="learning-deploying-application-storage-end-session_{{ context }}"}

To securely close your workspace and free up system resources, end your session from the terminal. {._abstract}

**Procedure**

*   Type `exit` in your terminal to quit the session and return to the command line interface (CLI).