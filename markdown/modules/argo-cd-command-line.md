{%- set _mod_docs_content_type = "PROCEDURE" %}
# Argo CD CLI tool {id="command-line-tool_{{ context }}"}

The Argo CD CLI tool is a tool used to configure Argo CD through the command line. {{ gitops_title }} does not support this binary. Use the OpenShift Console to configure the Argo CD. {._abstract}