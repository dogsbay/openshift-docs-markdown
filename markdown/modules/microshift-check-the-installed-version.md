{%- set _mod_docs_content_type = "CONCEPT" %}
# Check the installed version {id="microshift-check-the-installed-version_{{ context }}"}

You can check which version of {{ microshift_short }} is running on your system by using the command-line interface, the API, or by checking the etcd version.