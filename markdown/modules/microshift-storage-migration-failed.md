{%- set _mod_docs_content_type = "CONCEPT" %}
# Storage migration failure {id="microshift-storage-migration-failed_{{ context }}"}

Storage migration failures typically result from incompatible changes to custom resources (CRs) between {{ microshift_short }} versions. If a storage migration fails, the CR versions are likely incompatible and require manual review.