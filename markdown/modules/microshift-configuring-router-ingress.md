{%- set _mod_docs_content_type = "CONCEPT" %}
# Configuring router ingress {id="microshift-configuring-router-ingress_{{ context }}"}

Configure the `listenAddress` setting if your {{ microshift_short }} applications need to listen only for data traffic. You can also configure specific ports and IP addresses for network connections. Use the combination required to customize the endpoint configuration for your use case.