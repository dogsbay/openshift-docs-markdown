{%- set _mod_docs_content_type = "REFERENCE" %}
# Logging into a cluster after an installation {id="logging-into-a-cluster-after-installation_{{ context }}"}

After an installation, the `kubeadmin` user exists by default. You can log in to your cluster as the `kubeadmin` user by using the CLI or the {{ product_title }} web console.