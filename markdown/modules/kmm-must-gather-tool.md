{%- set _mod_docs_content_type = "CONCEPT" %}
# Using the must-gather tool {id="kmm-must-gather-tool_{{ context }}"}

To collect Kernel Module Management debugging data for Red&#160;Hat Support on {{ product_title }}, you can run the `oc adm must-gather` command with KMM-specific arguments.