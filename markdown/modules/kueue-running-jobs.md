{%- set _mod_docs_content_type = "PROCEDURE" %}
# Running jobs with {{ product_title }} {id="running-jobs_{{ context }}"}

When you run a job, {{ product_title }} creates a corresponding workload for the job, which is represented by a `Workload` object.

**Prerequisites**

{% include "./snippets/prereqs-snippet-yaml-user.md" %}