{%- set _mod_docs_content_type = "CONCEPT" %}
# Telco RAN DU reference configuration CRs {id="telco-ran-du-reference-configuration-crs_{{ context }}"}

Use the following custom resources (CRs) to configure and deploy {{ product_title }} clusters with the telco RAN DU profile.
Use the CRs to form the common baseline used in all the specific use models unless otherwise indicated. {._abstract}


:::note

You can extract the complete set of RAN DU CRs from the `ztp-site-generate` container image.
See "Preparing the {{ ztp }} site configuration repository" for more information.

:::