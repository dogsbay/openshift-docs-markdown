{%- set _mod_docs_content_type = "CONCEPT" %}
# Auditing exposed network ports {id="microshift-exposed-audit-ports_{{ context }}"}

On {{ microshift_short }}, the host port can be opened by a workload in the following cases. You can check logs to view the network services.