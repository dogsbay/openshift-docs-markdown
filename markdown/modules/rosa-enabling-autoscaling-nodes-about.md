{%- set _mod_docs_content_type = "CONCEPT" %}
# Enabling autoscaling nodes on a cluster {id="nodes-enabling-autoscaling-nodes_{{ context }}"}

Disable autoscaling on worker nodes to set a fixed number of nodes available by editing the machine pool definition for an existing cluster.