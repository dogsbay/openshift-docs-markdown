{%- set _mod_docs_content_type = "CONCEPT" %}
# Uninstall MicroShift {id="microshift-install-uninstall_{{ context }}"}

If you no longer need the cluster or need to reset the environment, you can uninstall {{ microshift_short }} to cleanly remove the software and free up system resources.