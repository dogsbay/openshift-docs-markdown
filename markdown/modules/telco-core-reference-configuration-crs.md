{%- set _mod_docs_content_type = "CONCEPT" %}
# Telco core reference configuration CRs {id="telco-core-reference-configuration-crs_{{ context }}"}

Use the following custom resources (CRs) to configure and deploy {{ product_title }} clusters with the telco core profile.
Use the CRs to form the common baseline used in all the specific use models unless otherwise indicated.