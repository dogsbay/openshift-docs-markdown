{%- set _mod_docs_content_type = "CONCEPT" %}
# New features and enhancements {id="microshift-4-22-new-features-enhancements_{{ context }}"}

This release adds improvements related to the following components and concepts. {._abstract}

## {{ microshift_short }} is supported on {{ op_system_base_full }} {{ op_system_version }} {id="microshift-4-22-RHEL-9-8-support-feature1_{{ context }}"}

With this release, {{ microshift_short }} is supported on {{ op_system_base_full }} {{ op_system_version }}.

## Ingress TLS configuration documentation {id="microshift-4-22-ingress-tls-doc_{{ context }}"}

With this release, {{ microshift_short }} documentation adds procedures and prerequisites for the default ingress router certificate (`ingress.certificateSecret`) and for TLS on Kubernetes `Ingress` objects.

*   [Using ingress control for a {{ microshift_short }} node](/microshift_configuring/microshift-ingress-controller#microshift-ingress-controller)