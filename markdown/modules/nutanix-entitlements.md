{%- set _mod_docs_content_type = "CONCEPT" %}
# Internet access for Prism Central {id="nutanix-entitlements_{{ context }}"}

Prism Central requires internet access to obtain the {{ op_system_first }} image that is required to install the cluster. The {{ op_system }} image for Nutanix is available at `rhcos.mirror.openshift.com`. {._abstract}