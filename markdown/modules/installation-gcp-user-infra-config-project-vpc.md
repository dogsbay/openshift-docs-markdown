{%- set _mod_docs_content_type = "CONCEPT" %}
# Configuring the {{ gcp_short }} project that hosts your cluster {id="installation-gcp-user-infra-config-project-vpc_{{ context }}"}

Before you can install {{ product_title }}, you must configure a {{ gcp_full }} project to host it. Proper project configuration provides the API services, service account, and permissions that the installation requires.