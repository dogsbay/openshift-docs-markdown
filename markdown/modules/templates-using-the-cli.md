{%- set _mod_docs_content_type = "CONCEPT" %}
# Creating objects from templates by using the CLI {id="templates-using-the-cli_{{ context }}"}

You can use the CLI to create objects from templates on your {{ product_title }} cluster by processing a template into a list of objects in your project. Use CLI commands to manage template labels, parameters, and generated object lists.