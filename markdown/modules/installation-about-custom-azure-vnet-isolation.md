{%- set _mod_docs_content_type = "CONCEPT" %}
# Isolation between clusters {id="installation-about-custom-azure-vnet-isolation_{{ context }}"}

Because the cluster is unable to modify network security groups in an existing subnet, there is no way to isolate clusters from each other on the VNet. {._abstract}