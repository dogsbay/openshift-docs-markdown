{%- set _mod_docs_content_type = "CONCEPT" %}
# About collecting and storing Kubernetes events {id="cluster-logging-collecting-storing-kubernetes-events-about_{{ context }}"}

The {{ product_title }} Event Router is a pod that watches Kubernetes events and logs them for collection by {{ product_title }} Logging. You must manually deploy the Event Router.