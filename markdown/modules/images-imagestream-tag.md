{%- set _mod_docs_content_type = "REFERENCE" %}
# Image stream tags {id="images-imagestream-tag_{{ context }}"}

Image stream tags are named pointers to images in image streams in {{ product_title }}. You can configure image stream tags to reference specific versions of container images.