{%- set _mod_docs_content_type = "CONCEPT" %}
# Example Apache authentication configuration using request header {id="example-apache-auth-config-using-request-header_{{ context }}"}

Review this example to configure an Apache authentication proxy with the request header identity provider. Use this example to set up a proxy and connect it to {{ product_title }}. {._abstract}

The example configures an Apache authentication proxy for {{ product_title }} by using the request header identity provider.