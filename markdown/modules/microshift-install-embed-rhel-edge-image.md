{%- set _mod_docs_content_type = "CONCEPT" %}
# Embed in a RHEL for Edge image {id="microshift-install-embed-rhel-edge-image_{{ context }}"}

You can embed {{ microshift_short }} in a {{ op_system_base }} for Edge image to deploy a customized, bootable edge operating system with Kubernetes pre-installed.