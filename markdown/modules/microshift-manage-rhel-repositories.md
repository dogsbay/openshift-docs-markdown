{%- set _mod_docs_content_type = "CONCEPT" %}
# Manage {{ op_system_base }} repositories for RPM updates {id="microshift-manage-rhel-repositories_{{ context }}"}

When you apply manual RPM updates, carefully manage your {{ op_system_base_full }} repositories to avoid creating an unsupported configuration or breaking your node. Keeping {{ microshift_short }} and {{ op_system_base }} in a supported configuration helps ensure reliable updates.