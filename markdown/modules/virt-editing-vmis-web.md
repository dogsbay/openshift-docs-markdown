{%- set _mod_docs_content_type = "PROCEDURE" %}
# Editing a standalone virtual machine instance using the web console {id="virt-editing-vmis-web_{{ context }}"}

You can edit the annotations and labels of a standalone virtual machine instance (VMI) using the web console. Other fields are not editable. {._abstract}

**Procedure**

1.  In the {{ product_title }} console, click **Virtualization** → **VirtualMachines** from the side menu.
1.  Select a standalone VMI to open the **VirtualMachineInstance details** page.
1.  On the **Details** tab, click the pencil icon beside **Annotations** or **Labels**.
1.  Make the relevant changes and click **Save**.