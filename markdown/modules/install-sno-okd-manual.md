{%- set _mod_docs_content_type = "CONCEPT" %}
# Installing {{ sno_okd }} manually {id="install-sno-installing-sno-okd-manually_{{ context }}"}

To install {{ product_title }} on a single node, first generate the installation ISO, and then boot the server from the ISO. You can monitor the installation using the `openshift-install` installation program.