{%- set _mod_docs_content_type = "CONCEPT" %}
# Running the network verification manually {id="running-network-verification-manually_{{ context }}"}

After installing a {{ product_title }} cluster, you can run the network verification checks manually by using {{ cluster_manager_first }} or the ROSA CLI (`rosa`).