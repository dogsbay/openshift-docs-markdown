{%- set _mod_docs_content_type = "CONCEPT" %}
# About changing the cluster network MTU to support Local Zones or Wavelength Zone {id="change-cluster-mtu-support-zones-about_{{ context }}"}

You might need to change the maximum transmission unit (MTU) value for the cluster network so that your cluster infrastructure can support {{ zone_type }} subnets.