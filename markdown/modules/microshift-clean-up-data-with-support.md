{%- set _mod_docs_content_type = "CONCEPT" %}
# Clean up data with support {id="microshift-clean-up-data-with-support_{{ context }}"}

When working with Red&#160;Hat Support to resolve system conflicts, you can use the `microshift-cleanup-data` script to safely remove {{ microshift_short }} data, configuration, network data, and custom certificates.