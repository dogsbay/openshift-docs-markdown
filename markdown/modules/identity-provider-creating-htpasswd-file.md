{%- set _mod_docs_content_type = "CONCEPT" %}
# Creating the htpasswd file {id="identity-provider-creating-htpasswd-file_{{ context }}"}

To configure the `htpasswd` identity provider, create an `htpasswd` file so usernames and hashed passwords are available for the cluster secret. The following procedures describe how to create the file on Linux and Windows Operating Systems. {._abstract}

*   Creating an `htpasswd` file using Linux
*   Creating an `htpasswd` file using Windows