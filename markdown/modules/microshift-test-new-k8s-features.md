{%- set _mod_docs_content_type = "CONCEPT" %}
# Test new Kubernetes features in MicroShift {id="microshift-test-new-k8s-features_{{ context }}"}

Use feature gates to test new Kubernetes features for potential use in your {{ microshift_short }} deployments. Feature gates let you experiment with upcoming Kubernetes capabilities to evaluate their potential benefits for specific edge use cases.