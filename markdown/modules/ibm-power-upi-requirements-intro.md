{%- set _mod_docs_content_type = "CONCEPT" %}
# Requirements for a cluster with user-provisioned infrastructure {id="ibm-power-upi-requirements-intro_{{ context }}"}

For a {{ product_title }} cluster with user-provisioned infrastructure on {{ ibm_power_name }}, you must deploy all required machines meeting the hardware, network, DNS, and load balancing requirements.