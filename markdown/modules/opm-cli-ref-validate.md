{%- set _mod_docs_content_type = "REFERENCE" %}
# validate {id="opm-cli-ref-validate_{{ context }}"}

Validate the declarative config JSON file(s) in a given directory.

```terminal title="Command syntax"
$ opm validate <directory> [<flags>]
```