{%- set _mod_docs_content_type = "CONCEPT" %}
# Requirements for a cluster using the platform "none" option {id="installation-requirements-platform-none_{{ context }}"}

There are additional requirements when installing a cluster using the platform "none" option with the Agent-based Installer. {._abstract}


:::important

See "Deploying OpenShift 4.x on non-tested platforms using the bare metal install method" before you attempt to install an {{ product_title }} cluster in virtualized or cloud environments.

:::