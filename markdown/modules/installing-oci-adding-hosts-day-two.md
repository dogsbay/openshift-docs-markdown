{%- set _mod_docs_content_type = "PROCEDURE" %}
# Adding hosts to the cluster following the installation {id="installing-oci-adding-hosts-day-two_{{ context }}"}

After creating a cluster with the {{ ai_full }}, you can use the {{ hybrid_console }} to add new host nodes to the cluster and approve their certificate signing requests (CSRs). {._abstract}

**Procedure**

*   See the instructions in [Adding Nodes to a Cluster (Oracle documentation)](https://docs.oracle.com/en-us/iaas/Content/openshift-on-oci/adding-nodes.htm).