{%- set _mod_docs_content_type = "CONCEPT" %}
# Install with image mode for RHEL {id="microshift-install-with-image-mode-rhel_{{ context }}"}

You can install {{ microshift_short }} with image mode for {{ op_system_base }} to deploy and manage your edge OS and Kubernetes platform as a single container image.