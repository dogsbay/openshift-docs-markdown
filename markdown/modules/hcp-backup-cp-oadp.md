{%- set _mod_docs_content_type = "CONCEPT" %}
# Control plane workload backup {id="hcp-backup-cp-oadp_{{ context }}"}

You can back up the control plane workload by creating the `Backup` custom resource (CR).  {._abstract}

The steps vary depending on whether your platform is {{ aws_short }} or bare metal.