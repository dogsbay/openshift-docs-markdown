# Creating the {{ SMProductName }} control plane {id="ossm-control-plane-deploy_{{ context }}"}

The `ServiceMeshControlPlane` resource defines the configuration to be used during installation.

You can deploy a basic installation of the {{ SMProductShortName }} control plane by using the {{ product_title }} web console or from the command line using the `oc` client tool.

To get started, install a basic instance of {{ SMProductShortName }} with the following steps. You can customize the `ServiceMeshControlPlane` resource later.