{%- set _mod_docs_content_type = "CONCEPT" %}
# {{ microshift_short }} Kubernetes APIs {id="microshift-k8s-apis_{{ context }}"}

You can manage the fully accessible Kubernetes API in {{ microshift_short }} by using the `kubectl` or {{ OCP }} (`oc`) command-line tools. {._abstract}

Using these command-line tools with {{ microshift_short }} can help you access all of the resources you need to work with your deployments.