{%- set _mod_docs_content_type = "CONCEPT" %}

# Persistent storage {id="microshift-persistent-storage_{{ context }}"}

Persistent storage in {{ microshift_short }} enables stateful applications to retain data beyond the lifecycle of individual pods. You can use persistent volumes (PVs) to provision storage and persistent volume claims (PVCs) to request storage for your applications.