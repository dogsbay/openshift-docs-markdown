{%- set _mod_docs_content_type = "REFERENCE" %}
# Supported {{ rhoai }} custom resource definitions {id="microshift-rhoai-supported-crds_{{ context }}"}

You can review the following information to learn about the {{ rhoai }} custom resource definitions (CRDs).  {._abstract}

The following CRD are supported:

*   `InferenceServices`
*   `TrainedModels`
*   `ServingRuntimes`
*   `InferenceGraphs`
*   `ClusterStorageContainers`
*   `ClusterLocalModels`
*   `LocalModelNodeGroups`