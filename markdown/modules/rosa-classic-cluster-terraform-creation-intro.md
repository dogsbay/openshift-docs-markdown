{%- set _mod_docs_content_type = "CONCEPT" %}
# Creating a default {{ product_title }} cluster using Terraform {id="rosa-classic-cluster-terraform-creation-intro_{{ context }}"}

Terraform provisions account-wide IAM roles and a {{ product_title }} cluster with a managed OIDC configuration.