{%- set _mod_docs_content_type = "CONCEPT" %}
# Enhancing the {{ product_title }} web console with Operators {id="optional-capabilities-operators_{{ context }}"}

Cluster administrators can install Operators from the software catalog to extend the {{ product_title }} web console beyond layered products. For example, the {{ web_terminal_op }} adds a browser-based terminal with common CLI tools for interacting with the cluster.